<--START
TRACE ON

######################################SOL 037 START ##############################
IF(BANCS.STDIN.mySolId==037)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)

		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)

				IF(CUSTS.ROLE.user=="DM17631") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF


				IF(CUSTS.ROLE.user=="DK27470") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF


				IF(CUSTS.ROLE.user=="JG33044") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="LM42126") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF


				IF(CUSTS.ROLE.user=="LM07095") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF


				IF(CUSTS.ROLE.user=="TK35594") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF
		

       				GOTO NEXTREC37

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC37:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF


#########################################SOL 037 END##########################################



#####################################SOL 038 START##########################################

IF(BANCS.STDIN.mySolId==038)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)
		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)


				IF(CUSTS.ROLE.user=="CM36166") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="PM15876") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="ST07655") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BCA)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="GKF42114") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="EG35644") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

		

       				GOTO NEXTREC38

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC38:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF

########################################SOL 038 END##########################################


######################################SOL 039 START##########################################

IF(BANCS.STDIN.mySolId==039)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)

		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)


				IF(CUSTS.ROLE.user=="AM36222") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="EM33186") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="NN35500") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

       				GOTO NEXTREC39

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC39:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF

######################################SOL 039 END##########################################


######################################SOL 040 START##########################################

IF(BANCS.STDIN.mySolId==040)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)

		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)

				IF(CUSTS.ROLE.user=="EM27471") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="MM27528") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="AN15564") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="GK35615") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="EG22774") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF


       				GOTO NEXTREC40

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC40:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF

######################################SOL 040 END##########################################


######################################SOL 041 START##########################################

IF(BANCS.STDIN.mySolId==041)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)

		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)

				IF(CUSTS.ROLE.user=="JM19167") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="BNF42087") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF


       				GOTO NEXTREC41

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC41:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF

######################################SOL 041 END##########################################


######################################SOL 042 START##########################################

IF(BANCS.STDIN.mySolId==042)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)

		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)

				IF(CUSTS.ROLE.user=="HM35676") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF


				IF(CUSTS.ROLE.user=="MW27501") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

       				GOTO NEXTREC42

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC42:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF

######################################SOL 042 END##########################################


######################################SOL 043 START##########################################

IF(BANCS.STDIN.mySolId==043)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)

		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)

				IF(CUSTS.ROLE.user=="LK36169") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="JK17860") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="EKF42107") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF



       				GOTO NEXTREC43

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC43:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF

######################################SOL 043 END##########################################

######################################SOL 044 START##########################################

IF(BANCS.STDIN.mySolId==044)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)

		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)

				IF(CUSTS.ROLE.user=="PM32863") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="CN33244") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="BN33074") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF


				IF(CUSTS.ROLE.user=="MM33075") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="KN35629") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="LM35957") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF




       				GOTO NEXTREC44

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC44:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF

######################################SOL 044 END##########################################

######################################SOL 045 START##########################################

IF(BANCS.STDIN.mySolId==045)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)

		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)


				IF(CUSTS.ROLE.user=="JN35670") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="AM32717") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="KW35545") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="EK36189") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

       				GOTO NEXTREC45

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC45:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF

######################################SOL 045 END##########################################

######################################SOL 046 START##########################################

IF(BANCS.STDIN.mySolId==046)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)

		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)

				IF(CUSTS.ROLE.user=="MK35587") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="FK32740") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="EW36204") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="MR32762") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="HW17816") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="WK34939") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="DVF42104") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF



       				GOTO NEXTREC46

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC46:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF

######################################SOL 046 END##########################################


######################################SOL 047 START##########################################

IF(BANCS.STDIN.mySolId==047)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)

		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)

				IF(CUSTS.ROLE.user=="IK15733") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="SW32779") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="KN35636") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="CN06700") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF


       				GOTO NEXTREC47

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC47:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF

######################################SOL 047 END##########################################

######################################SOL 048 START##########################################

IF(BANCS.STDIN.mySolId==048)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)

		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)

				IF(CUSTS.ROLE.user=="JO36219") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="JK31605") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="FM22566") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="GM33088") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF
				

				IF(CUSTS.ROLE.user=="IB35590") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF


				IF(CUSTS.ROLE.user=="KN36165") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF



       				GOTO NEXTREC48

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC48:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF

######################################SOL 048 END##########################################

######################################SOL 049 START##########################################

IF(BANCS.STDIN.mySolId==049)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)

		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)


				IF(CUSTS.ROLE.user=="CO05062") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="AE35664") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF




				IF(CUSTS.ROLE.user=="KL22521") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF


				IF(CUSTS.ROLE.user=="CB31614") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="GL36161") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="JJF42118") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="COF42097") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF



       				GOTO NEXTREC49

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC49:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF

######################################SOL 049 END##########################################

######################################SOL 050 START##########################################

IF(BANCS.STDIN.mySolId==050)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)

		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)

				IF(CUSTS.ROLE.user=="LL33037") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="FO27484") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="FW34299") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="MW33033") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="CO04352") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BCA)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="DT32728") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="JK32754") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF



       				GOTO NEXTREC50

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC50:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF

######################################SOL 050 END##########################################

######################################SOL 051 START##########################################

IF(BANCS.STDIN.mySolId==051)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)

		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)

				IF(CUSTS.ROLE.user=="NR35633") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="GO13973") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="SO22738") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="EO35597") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="KO27530") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="AS36154") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="FM15741") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF


       				GOTO NEXTREC51

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC51:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF

######################################SOL 051 END##########################################

######################################SOL 052 START##########################################

IF(BANCS.STDIN.mySolId==052)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)

		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)

				IF(CUSTS.ROLE.user=="IK32033") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="SK22840") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.AGR)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="LJ32757") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="HM35657") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="GL36161") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="IW35779") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF


       				GOTO NEXTREC52

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC52:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF

######################################SOL 052 END##########################################

######################################SOL 053 START##########################################

IF(BANCS.STDIN.mySolId==053)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)

		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)

				IF(CUSTS.ROLE.user=="HK33034") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="KY24814") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="LO35645") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="SS31570") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="MO36194") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF


       				GOTO NEXTREC53

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC53:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF

######################################SOL 053 END##########################################

######################################SOL 054 START##########################################

IF(BANCS.STDIN.mySolId==054)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)

		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)

				IF(CUSTS.ROLE.user=="RC02213") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BCA)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="BW15572") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BCA)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="MK15843") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF



       				GOTO NEXTREC54

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC54:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF

######################################SOL 054 END##########################################

######################################SOL 055 START##########################################

IF(BANCS.STDIN.mySolId==055)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)

		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)

				IF(CUSTS.ROLE.user=="PG33225") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="FK36211") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF


       				GOTO NEXTREC55

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC55:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF

######################################SOL 055 END##########################################

######################################SOL 057 START##########################################

IF(BANCS.STDIN.mySolId==057)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)

		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)

				IF(CUSTS.ROLE.user=="LW22553") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="VK22540") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="PM08076") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="JG42162") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.AGR)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="BM31593") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="GN17628") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF


       				GOTO NEXTREC57

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC57:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF

######################################SOL 057 END##########################################

######################################SOL 058 START##########################################

IF(BANCS.STDIN.mySolId==058)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)

		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)

				IF(CUSTS.ROLE.user=="JK33050") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="HM35612") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF


       				GOTO NEXTREC58

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC58:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF

######################################SOL 058 END##########################################

######################################SOL 059 START##########################################

IF(BANCS.STDIN.mySolId==059)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)

		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)

				IF(CUSTS.ROLE.user=="NN31599") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="GW08397") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="PK11591") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="EW35548") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

       				GOTO NEXTREC59

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC59:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF

######################################SOL 059 END##########################################

######################################SOL 060 START##########################################

IF(BANCS.STDIN.mySolId==060)THEN
	#{
		#selecting authoriser user id
		sv_s = "authuser" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id!='CCL' AND SOL_ID=?SVAR AND ROWNUM=1 AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		CUSTS.ROLE.authuser=BANCS.OUTPARAM.authuser
		PRINT(CUSTS.ROLE.authuser)

		sv_s = "user" 
		sv_s = sv_s + " |SELECT USER_ID FROM TBAADM.UPR where role_id='CCL' AND SOL_ID=?SVAR"
		BANCS.INPARAM.BINDVARS =  BANCS.STDIN.mySolId

		sv_v = urhk_dbcursorOpenWithBind(sv_s)
       		PRINT(sv_v)	

		IF(sv_v == 0) THEN
       	 	#{
        		CUSTS.ROLE.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       			print(CUSTS.ROLE.CURSOR_NUMBER)
       			sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
        		print(sv_b)
			BANCS.INPUT.RECCOUNT = CINT("1")
        		print(BANCS.INPUT.RECCOUNT)

			WHILE(sv_b == "0")
        		#{
				CUSTS.ROLE.user= BANCS.OUTPARAM.user
				print(CUSTS.ROLE.user)

				IF(CUSTS.ROLE.user=="AK33171") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.PB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="VN42149") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="BM27460") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.GRP)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

				IF(CUSTS.ROLE.user=="CK04532") THEN
				#{

					sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + CUSTS.ROLE.user)
					PRINT(sv_u)

					sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
					PRINT(sv_u)

					IF (sv_u == 0) THEN
					#{
			
						sv_u = urhk_CopyOutToIn("")
						PRINT(sv_u)
				
						sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + CUSTS.ROLE.BB)
						PRINT(sv_u)
						sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + CUSTS.ROLE.authuser)
						PRINT(sv_k)

						sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
						PRINT(sv_u)

						IF (sv_u == 0) THEN
						#{
							print("SUCCESS")
						#}
						ENDIF
					#}
					ENDIF

				#}
				ENDIF

       				GOTO NEXTREC60

      			 	BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        			PRINT(BANCS.INPUT.RECCOUNT)

        			NEXTREC60:
       			 	sv_b = urhk_dbCursorFetch(CUSTS.ROLE.CURSOR_NUMBER)
       				print(sv_b)
				
			#}
			DO


		#}
		ENDIF	
		
				
	#}
	ENDIF

######################################SOL 060 END##########################################
TRACE OFF
END-->