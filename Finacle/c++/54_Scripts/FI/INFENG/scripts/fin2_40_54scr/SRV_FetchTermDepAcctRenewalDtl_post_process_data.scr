#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: SRV_FetchTermDepAcctRenewalDtl_post_process_data.scr
# Type: NA|default
# Description: SRV_FetchTermDepAcctRenewalDtl_post_process_data.scr
# Author:
# Date:
# Modification Log:
#============================================================

################################################################################
# Menu								: HTDREN
# Source Name						: SRV_FetchTermDepAcctRenewalDtl_post_process_data.scr
# Date								: 24-10-2016
# Description						: This script is to make mandatory fields
# Input Values						: None
# Output Values						: None
# Called Scripts					: None
# Calling Script					:
# Author     						: Manoj
# Bank								: Equity Bank,KENYA
# Modification History				:
#		< Version No.> 		<Date>		<Author Name> 		<Description>
#		 0.1				24-10-2016 	jane			Original Version
###############################################################################
<--START
CHECKSUM="028bf65868c868187b93b7311262a1804ce9b603b01b9fb4aec4f2807cf4008c"
	TRACE ON
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST","TDREN") == 0) THEN
	#{
		CREATECLASS("CUST","TDREN",5)
	#}
	ENDIF
	
	sv_a=urhk_B2k_PrintRepos("BANCS")
	
	sv_a = urhk_SRV_GetVal("renewalData.effOpenDate")
	CUST.TDREN.date=BANCS.OUTPARAM.srvValue
	PRINT(CUST.TDREN.date)
	
	sv_a = urhk_SRV_GetVal("renewalData.tdAcctId.foracid")
	CUST.TDREN.foracid=BANCS.OUTPARAM.srvValue
	PRINT(CUST.TDREN.foracid)
	
	sv_a = urhk_getAcctDetailsInRepository(CUST.TDREN.foracid)
	IF(sv_a==0)THEN
	#{
		CUST.TDREN.schmCode=BANCS.OUTPARAM.schmCode
		PRINT(CUST.TDREN.schmCode)
		CUST.TDREN.schmType=BANCS.OUTPARAM.schmType
		PRINT(CUST.TDREN.schmType)
		CUST.TDREN.opndate=BANCS.OUTPARAM.acctOpenDate
		PRINT(CUST.TDREN.opndate)
	#}
	ENDIF
	
	
	CUST.TDREN.date1 = MID$(CUST.TDREN.opndate,0,10)
	PRINT(CUST.TDREN.date1)
	
	CUST.TDREN.date2="14-09-2016"
	PRINT(CUST.TDREN.date2)
	
	BANCS.INPARAM.date1 = CUST.TDREN.date1
	BANCS.INPARAM.date2 = CUST.TDREN.date2
	sv_u = urhk_B2k_dateDiff("")
	PRINT(sv_u)
	
	IF (sv_u == 0) THEN
	#{
		
		PRINT(BANCS.OUTPARAM.dateDiff)
		CUST.TDREN.dateDiff = CINT(BANCS.OUTPARAM.dateDiff)
		PRINT(CUST.TDREN.dateDiff)
		
		
		IF (CUST.TDREN.dateDiff < 0) THEN
		#{
			
			sv_u = urhk_SRV_SetErr("CANNOT USE TDREN..KINDLY CLOSE THIS A/C AND OPEN A NEW ONE")
			BANCS.OUTPUT.successOrFailure = "F"
			EXITSCRIPT
			
		#}
		ENDIF
	#}
	ENDIF
	
	TRACE OFF
	EXITSCRIPT
END-->
