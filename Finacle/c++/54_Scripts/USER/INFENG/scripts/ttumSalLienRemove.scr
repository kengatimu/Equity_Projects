#============================================================
# Product: Finacle Core
# Category: User Defined
# Script: ttumSalLien.scr
# Type: NA|default
# Description: ttumSalLien.scr
# Author:Mark Nderitu
# Date:25-03-2025
# Modification Log:
#============================================================

<--START

trace on

	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	IF (CLASSEXISTS("CUST", "SALTTUM") == 0) THEN
	#{
		CREATECLASS("CUST", "SALTTUM", 5)
	#}
	ENDIF
	
	 sv_r = urhk_b2k_sleep("15")
	lv_b = urhk_B2K_PrintRepos("BANCS")
print(lv_b)

	#EXITSCRIPT
	BANCS.OUTPUT.successOrFailure = "S"
	CUST.SALTTUM.Remarks = "Remmittance Commission"
	CUST.SALTTUM.lien_reason_code= "LIRM"
	
	CUST.SALTTUM.b2k_type=""
	CUST.SALTTUM.b2k_id=""
	CUST.SALTTUM.lien_amt=""
	CUST.SALTTUM.newLienAmt = "0"
	CUST.SALTTUM.tranIdArr = ""
	CUST.SALTTUM.drAcctId = ""
	
	lv_q = "TRAN_ID, TRAN_DATE, LIEN_FLG, BANK_ID, TRAN_AMT, FORACID|"
	lv_q = lv_q+"select TRAN_ID, TRAN_DATE, LIEN_FLG, BANK_ID, TRAN_AMT, FORACID from CUSTOM.CSALBPP_TRANS where bank_id =?SVAR and LIEN_FLG =?SVAR"
	
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + "N"
    PRINT(BANCS.INPARAM.BINDVARS)

sv_b = urhk_dbCursorOpenWithBind(lv_q)
print(sv_b)

if(sv_b == 0)then
#{
        sv_d = BANCS.OUTPARAM.DB_CURSOR_NUMBER
        print(sv_d)
        sv_e = urhk_dbCursorFetch(sv_d)
        print(sv_e)
		
		while(sv_e == 0)
        #{
               
			   
			   CUST.SALTTUM.tranIdArr = BANCS.OUTPARAM.TRAN_ID
                PRINT(CUST.SALTTUM.tranIdArr)
				CUST.SALTTUM.TRAN_DATE = BANCS.OUTPARAM.TRAN_DATE
                PRINT(CUST.SALTTUM.TRAN_DATE)
				CUST.SALTTUM.LIEN_FLG = BANCS.OUTPARAM.LIEN_FLG
                PRINT(CUST.SALTTUM.LIEN_FLG)
				CUST.SALTTUM.TRAN_AMT = BANCS.OUTPARAM.TRAN_AMT
				CUST.SALTTUM.FORACID = BANCS.OUTPARAM.FORACID
				
                #sv_l = strlen(CUST.SALTTUM.tranIdArr)
                #if(sv_l == 0) then
                #        GOTO OUTSIDELOOP
                #endif

                
				
				print(CUST.SALTTUM.tranIdArr)

       


	
	 
	
	sv_s ="drAcctId,b2k_type,b2k_id,lien_amt,lien_expiry_date,lien_start_date|select "
	sv_s = sv_s+" gam.foracid,alt.B2K_TYPE,alt.B2K_id,alt.LIEN_AMT,to_char(alt.LIEN_EXPIRY_DATE,'dd-mm-yyyy'),to_char(alt.LIEN_START_DATE,'dd-mm-yyyy') "
	sv_s = sv_s+" from tbaadm.alt,tbaadm.gam where alt.bank_id =?SVAR and "
	sv_s = sv_s+" alt.acid in(select acid from tbaadm.dtd where bank_id =?SVAR   and PART_TRAN_TYPE ='D' and pstd_flg='Y' )"
	sv_s = sv_s+" and alt.lien_remarks =?SVAR and alt.LIEN_REASON_CODE =?SVAR "
	sv_s = sv_s+" and gam.acid =alt.acid and gam.bank_id =alt.bank_id and gam.foracid =?SVAR and alt.lien_amt =?SVAR"
	 
	 PRINT(sv_s)
        #BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId +"|" +BANCS.STDIN.contextBankId +"|" +CUST.SALTTUM.tranIdArr +"|" 
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId +"|" +BANCS.STDIN.contextBankId +"|"   		
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.SALTTUM.Remarks +"|"+CUST.SALTTUM.lien_reason_code +"|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.SALTTUM.FORACID +"|"+ CUST.SALTTUM.TRAN_AMT
        PRINT(BANCS.INPARAM.BINDVARS)

        sv_c = urhk_dbCursorOpenWithBind(sv_s)
        PRINT(sv_c)

        IF (sv_c==0) THEN
        #{
            CUST.SALTTUM.DbcursorNumber = BANCS.OUTPARAM.DB_CURSOR_NUMBER
            sv_k = urhk_dbCursorFetch(CUST.SALTTUM.DbcursorNumber)
            PRINT(sv_k)

            WHILE (sv_k == 0)
            #{
			
			CUST.SALTTUM.drAcctId=BANCS.OUTPARAM.drAcctId
			print(CUST.SALTTUM.drAcctId)
                CUST.SALTTUM.b2k_type=BANCS.OUTPARAM.b2k_type
                PRINT(CUST.SALTTUM.b2k_type)
                        
					
				CUST.SALTTUM.b2k_id=BANCS.OUTPARAM.b2k_id
                PRINT(CUST.SALTTUM.b2k_id)
					
				CUST.SALTTUM.lien_amt=BANCS.OUTPARAM.lien_amt
				PRINT(CUST.SALTTUM.lien_amt)
				
				CUST.SALTTUM.lien_expiry_date=BANCS.OUTPARAM.lien_expiry_date
				PRINT(CUST.SALTTUM.lien_expiry_date)
				CUST.SALTTUM.lien_start_date=BANCS.OUTPARAM.lien_start_date
				PRINT(CUST.SALTTUM.lien_start_date)

			
	 
	
				BANCS.INPARAM.b2kId = CUST.SALTTUM.b2k_id
				BANCS.INPARAM.acctNum = CUST.SALTTUM.drAcctId
				BANCS.INPARAM.blockMode = "M"
				BANCS.INPARAM.lienAmt = cdouble(CUST.SALTTUM.lien_amt) * -1
				BANCS.INPARAM.expiryDate = LEFT$(BANCS.STDIN.BODDate,10)
				BANCS.INPARAM.remarks = CUST.SALTTUM.Remarks
				BANCS.INPARAM.reasonCode = CUST.SALTTUM.lien_reason_code
				BANCS.INPARAM.bodDate = LEFT$(BANCS.STDIN.BODDate,10)
				BANCS.INPARAM.lienValueDate = LEFT$(BANCS.STDIN.BODDate,10)
				sv_u = urhk_updateLien("")
				print(sv_u)
				
				
				 lv_p = " update CUSTOM.csalbpp_trans set LIEN_FLG='D' where bank_id=?SVAR and TRAN_AMT =?SVAR and TRAN_DATE =?SVAR "
				 lv_p = lv_p+" and FORACID =?SVAR and LIEN_FLG='N' "
                        BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + CUST.SALTTUM.lien_amt + "|" + CUST.SALTTUM.TRAN_DATE + "|" + CUST.SALTTUM.drAcctId
                        lv_p = urhk_dbSqlWithBind(lv_p)

				
			sv_k = urhk_dbCursorFetch(CUST.SALTTUM.DbcursorNumber)
            PRINT(sv_k)

            #}
            DO

        sv_c = urhk_dbCursorClose(CUST.SALTTUM.DbcursorNumber)
        PRINT(sv_c)
else
		 BANCS.INPARAM.b2kId = ""
		 EXITSCRIPT
	
	#}
	ENDIF
	
	sv_e = urhk_dbCursorFetch(sv_d)
            PRINT(sv_e)
	 #}
     do
	 sv_e = urhk_dbCursorClose(sv_d)
        PRINT(sv_e)
		
	#}
	ENDIF

EXITSCRIPT
END-->
