#==============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cubpDfstxndp041.scr
# Type: NA|default
# Description: cubpDfstxndp041.scr
# Author:Mukavane Peter
# Date:22 Nov 2025
# Modification Log:
#=================================================================

<--START
CHECKSUM="b01126fc2eb2686faed5b1b21368141b21da5b54b62cd964689a3ce463fa2da5"
	#TRACE ON
	
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CIETXN Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","DFSTRN")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","DFSTRN",5)
	#}
	ENDIF
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	
	CUST.DFSTRN.menuId = ""
	IF(FIELDEXISTS(BANCS.STDIN.menuId))THEN
	#{
		CUST.DFSTRN.menuId = BANCS.STDIN.menuId
		PRINT(CUST.DFSTRN.menuId)
	#}
	ENDIF
	
	IF(CUST.DFSTRN.menuId == "CUBP")THEN
	#{
                sv_u = urhk_SRV_GetVal("tranHdr.tranIdentifier.tranId")
                print(sv_u)

                CUST.DFSTRN.dfstranIdOrg = BANCS.OUTPARAM.srvValue
                print(CUST.DFSTRN.dfstranIdOrg)

                CUST.DFSTRN.dfstranId = TRIM(BANCS.OUTPARAM.srvValue)
                print(CUST.DFSTRN.dfstranId)

                sv_u = urhk_SRV_GetVal("tranHdr.tranIdentifier.tranDate")
                print(sv_u)
                CUST.DFSTRN.tranDate = TRIM(BANCS.OUTPARAM.srvValue)
                print(CUST.DFSTRN.tranDate)

		sv_u = urhk_SRV_NumOfRecs("tranDtl.partTranDetailLL")
		PRINT(sv_u)
		
		IF(sv_u != 0)THEN
		#{
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		sv_j = CINT(BANCS.OUTPARAM.srvValue)
		PRINT(sv_j)
		
		sv_n = 0
		WHILE(sv_n < sv_j)
		#{

			CUST.DFSTRN.msgStr = "tranDtl.partTranDetailLL.<rec_" + FORMAT$(sv_n, "%d") + ">.acctId.foracid"
			sv_u = urhk_SRV_GetVal(CUST.DFSTRN.msgStr)
			IF(sv_u != 0) THEN
			#{
				GOTO NEXTFETCH
			#}
			ENDIF
			
			PRINT(BANCS.OUTPARAM.srvValue)
			CUST.DFSTRN.foracid = BANCS.OUTPARAM.srvValue
	
			IF(CUST.DFSTRN.foracid == "0810284143079") THEN
			#{
                		lv_j = urhk_B2k_ForacidToAcid(CUST.DFSTRN.foracid)
                		if(lv_j == 0) then
                       			CUST.DFSTRN.dfsAcid = BANCS.OUTPARAM.Acid
                		else
                		        CUST.DFSTRN.dfsAcid = ""
                		endif
				#sv_u = urhk_SRV_SetVal("tranDtl.partTranDetailLL.<rec_" + FORMAT$(sv_n, "%d") + ">.tranParticulars2|" + CUST.DFSTRN.dfstranId)
				#print(sv_u)
				lv_x = "update tbaadm.dtd set TRAN_PARTICULAR_2 = '"+CUST.DFSTRN.dfstranId+"' where tran_id = '"+CUST.DFSTRN.dfstranIdOrg+"' and acid = '"+CUST.DFSTRN.dfsAcid+"' and bank_id = '"+BANCS.STDIN.contextBankId+"' and to_char(tran_date,'DD-MM-YYYY HH24:MI:SS') = '"+CUST.DFSTRN.tranDate+"' "
				print(lv_x)
				lv_y = urhk_dbSqlWithBind(lv_x)
        			PRINT(lv_y)
			#}
			ENDIF					
			
			NEXTFETCH:	
			sv_n = sv_n + 1
		#}
		DO
			
	#}
	ENDIF
		
		
	ENDOFSCRIPT:
		
END-->
