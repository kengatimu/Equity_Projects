#============================================================
# Product: Finacle Core
# Category: User Defined
# Script: ttumSalLien.scr
# Type: NA|default
# Description: ttumSalLien.scr
# Author:Mark Nderitu
# Date:25-03-2025
# Modification Log:
#============================================================

<--START



	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	IF (CLASSEXISTS("CUST", "SALTTUM") == 0) THEN
	#{
		CREATECLASS("CUST", "SALTTUM", 5)
	#}
	ENDIF
	
	
	 CUST.SALTTUM.expiryDate="31-12-2099"
			
	CUST.SALTTUM.acctOwnerShip = BANCS.OUTPARAM.acctOwnerShip
	print(CUST.SALTTUM.acctOwnerShip)
	CUST.SALTTUM.PartTranType =  MTT.InputDetails.PartTranType
	IF((CUST.SALTTUM.acctOwnerShip =="C") AND (CUST.SALTTUM.PartTranType =="D"))THEN
	
	print(MTT.PartTranDetails.RefAmount)
	
	 
	CUST.SALTTUM.drAcctId = CUST.SALTTUM.Account
	CUST.SALTTUM.Remarks =  CUST.SALTTUM.Remarks
	if(CUST.SALTTUM.RefAmount<0)then
	CUST.SALTTUM.LienAmt = CUST.SALTTUM.RefAmount * -1
	else
	CUST.SALTTUM.LienAmt = CUST.SALTTUM.RefAmount
	endif
	
	
    BANCS.INPARAM.acctNum = CUST.SALTTUM.drAcctId
    BANCS.INPARAM.blockMode = "A"
    BANCS.INPARAM.lienAmt = cdouble(CUST.SALTTUM.LienAmt)
    BANCS.INPARAM.expiryDate = LEFT$(BANCS.STDIN.BODDate,10)
    BANCS.INPARAM.remarks = CUST.SALTTUM.Remarks
    BANCS.INPARAM.reasonCode = "LIRM"
    BANCS.INPARAM.bodDate = LEFT$(BANCS.STDIN.BODDate,10)
    BANCS.INPARAM.lienValueDate = LEFT$(BANCS.STDIN.BODDate,10)
    sv_u = urhk_updateLien("")
    print(sv_u)
	
	ENDIF
	
	

EXITSCRIPT
END-->