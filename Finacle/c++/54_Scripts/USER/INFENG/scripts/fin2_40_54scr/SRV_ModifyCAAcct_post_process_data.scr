#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: SRV_ModifyCAAcct_post_process_data.scr
# Type: NA|default
# Description: SRV_ModifyCAAcct_post_process_data.scr
# Author:
# Date:
# Modification Log:
#============================================================

#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: SRV_ModifyCAAcct_post_process_data.scr
# event: NA
# description: NA
#=================================================

<--START
CHECKSUM="a69b02c18c62a43b25d2b9f28e3aeb3968c7ff0697ecdbc702a7a4812fdd9238"
	TRACE ON
	#-----------------------------
	# Calling the custom script
	#-----------------------------
	
	sv_u = urhk_B2k_printRepos("BANCS")
	print(sv_u)

	sv_r = CLASSEXISTS("BANCS","INPARAM")

	IF (sv_r == 0) THEN
	CREATECLASS("BANCS","INPARAM",5)

	ENDIF

	IF (CLASSEXISTS("CUST","ACCTMOD") == 0) THEN
	CREATECLASS("CUST","ACCTMOD",5)

	ENDIF

	sv_u=urhk_SRV_GetVal("acctCommonInfo.acctClassMISC.freeText2")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
	CUST.ACCTMOD.freeText2 = TOUPPER(BANCS.OUTPARAM.srvValue)
	PRINT(CUST.ACCTMOD.freeText2)
	#}
	ENDIF

	sv_u=urhk_SRV_GetVal("acctCommonInfo.acctClassMISC.freeText4")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
	CUST.ACCTMOD.freeText4 = TOUPPER(BANCS.OUTPARAM.srvValue)
	PRINT(CUST.ACCTMOD.freeText4)
	#}
	ENDIF

	sv_u=urhk_SRV_GetVal("acctCommonInfo.acctClassMISC.freeText6")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
	CUST.ACCTMOD.freeText6 = TOUPPER(BANCS.OUTPARAM.srvValue)
	PRINT(CUST.ACCTMOD.freeText6)
	#}
	ENDIF

	sv_u=urhk_SRV_GetVal("acctCommonInfo.acctClassMISC.freeText8")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
	CUST.ACCTMOD.freeText8 = TOUPPER(BANCS.OUTPARAM.srvValue)
	PRINT(CUST.ACCTMOD.freeText8)
	#}
	ENDIF
	sv_u=urhk_SRV_GetVal("acctCommonInfo.acctClassMISC.freeText10")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
	CUST.ACCTMOD.freeText10 = TOUPPER(BANCS.OUTPARAM.srvValue)
	PRINT(CUST.ACCTMOD.freeText10)
	#}
	ENDIF
	sv_u=urhk_SRV_GetVal("acctCommonInfo.acctClassMISC.freeText12")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
	CUST.ACCTMOD.freeText12 = TOUPPER(BANCS.OUTPARAM.srvValue)
	PRINT(CUST.ACCTMOD.freeText12)
	#}
	ENDIF
	sv_u=urhk_SRV_GetVal("acctCommonInfo.acctClassMISC.freeText14")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
	CUST.ACCTMOD.freeText14 = TOUPPER(BANCS.OUTPARAM.srvValue)
	PRINT(CUST.ACCTMOD.freeText14)
	#}
	ENDIF

	print (BANCS.OUTPUT.fAccount)


	IF (CUST.ACCTMOD.freeText2 !="") THEN

		sv_s = "count|"
		sv_s = sv_s + " select count (PHONENO) from crmuser.phoneemail where "
		sv_s = sv_s + " orgkey in (select NMA_KEY_ID from tbaadm.aas where acid in"
		sv_s = sv_s + " (select acid from tbaadm.gam where foracid = ?SVAR )) " 
		sv_s = sv_s + " and bank_id =?SVAR  and PHONEOREMAIL ='PHONE' and phoneno =?SVAR"
		BANCS.INPARAM.BINDVARS = BANCS.OUTPUT.fAccount + "|" + BANCS.STDIN.contextBankId + "|" + CUST.ACCTMOD.freeText2 
		print(BANCS.INPARAM.BINDVARS)
		print(sv_s)
		sv_d=urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		IF (sv_d == 0) THEN

			IF (BANCS.OUTPARAM.count !="") THEN

				CUST.ACCTMOD.count = BANCS.OUTPARAM.count
				PRINT(CUST.ACCTMOD.count)

				IF (CINT(CUST.ACCTMOD.count)==0) THEN
					print("invalid mobile")
					sv_u = urhk_SRV_SetErr("invalid Mobile: " + CUST.ACCTMOD.freeText2 + ":/update correct phone of the nominee")
					BANCS.OUTPUT.successOrFailure = "F"
						EXITSCRIPT

				ENDIF
			ENDIF
		ENDIF
	ENDIF

		IF (CUST.ACCTMOD.freeText4 !="") THEN

		sv_s = "count|"
		sv_s = sv_s + " select count (PHONENO) from crmuser.phoneemail where "
		sv_s = sv_s + " orgkey in (select NMA_KEY_ID from tbaadm.aas where acid in"
		sv_s = sv_s + " (select acid from tbaadm.gam where foracid = ?SVAR )) " 
		sv_s = sv_s + " and bank_id =?SVAR  and PHONEOREMAIL ='PHONE' and phoneno =?SVAR"
		BANCS.INPARAM.BINDVARS = BANCS.OUTPUT.fAccount + "|" + BANCS.STDIN.contextBankId + "|" + CUST.ACCTMOD.freeText4 
		print(BANCS.INPARAM.BINDVARS)
		print(sv_s)
		sv_d=urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		IF (sv_d == 0) THEN

			IF (BANCS.OUTPARAM.count !="") THEN

				CUST.ACCTMOD.count = BANCS.OUTPARAM.count
				PRINT(CUST.ACCTMOD.count)

				IF (CINT(CUST.ACCTMOD.count)==0) THEN
					print("invalid mobile")
					sv_u = urhk_SRV_SetErr("invalid Mobile: " + CUST.ACCTMOD.freeText4 + ":/update correct phone of the nominee")
					BANCS.OUTPUT.successOrFailure = "F"
						EXITSCRIPT

				ENDIF
			ENDIF
		ENDIF
	ENDIF

	IF (CUST.ACCTMOD.freeText6 !="") THEN

		sv_s = "count|"
		sv_s = sv_s + " select count (PHONENO) from crmuser.phoneemail where "
		sv_s = sv_s + " orgkey in (select NMA_KEY_ID from tbaadm.aas where acid in"
		sv_s = sv_s + " (select acid from tbaadm.gam where foracid = ?SVAR )) " 
		sv_s = sv_s + " and bank_id =?SVAR  and PHONEOREMAIL ='PHONE' and phoneno =?SVAR"
		BANCS.INPARAM.BINDVARS = BANCS.OUTPUT.fAccount + "|" + BANCS.STDIN.contextBankId + "|" + CUST.ACCTMOD.freeText6 
		print(BANCS.INPARAM.BINDVARS)
		print(sv_s)
		sv_d=urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		IF (sv_d == 0) THEN

			IF (BANCS.OUTPARAM.count !="") THEN

				CUST.ACCTMOD.count = BANCS.OUTPARAM.count
				PRINT(CUST.ACCTMOD.count)

				IF (CINT(CUST.ACCTMOD.count)==0) THEN
					print("invalid mobile")
					sv_u = urhk_SRV_SetErr("invalid Mobile: " + CUST.ACCTMOD.freeText6 + ":/update correct phone of the nominee")
					BANCS.OUTPUT.successOrFailure = "F"
						EXITSCRIPT

				ENDIF
			ENDIF
		ENDIF
	ENDIF

		IF (CUST.ACCTMOD.freeText8 !="") THEN

		sv_s = "count|"
		sv_s = sv_s + " select count (PHONENO) from crmuser.phoneemail where "
		sv_s = sv_s + " orgkey in (select NMA_KEY_ID from tbaadm.aas where acid in"
		sv_s = sv_s + " (select acid from tbaadm.gam where foracid = ?SVAR )) " 
		sv_s = sv_s + " and bank_id =?SVAR  and PHONEOREMAIL ='PHONE' and phoneno =?SVAR"
		BANCS.INPARAM.BINDVARS = BANCS.OUTPUT.fAccount + "|" + BANCS.STDIN.contextBankId + "|" + CUST.ACCTMOD.freeText8
		print(BANCS.INPARAM.BINDVARS)
		print(sv_s)
		sv_d=urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		IF (sv_d == 0) THEN

			IF (BANCS.OUTPARAM.count !="") THEN

				CUST.ACCTMOD.count = BANCS.OUTPARAM.count
				PRINT(CUST.ACCTMOD.count)

				IF (CINT(CUST.ACCTMOD.count)==0) THEN
					print("invalid mobile")
					sv_u = urhk_SRV_SetErr("invalid Mobile: " + CUST.ACCTMOD.freeText8 + ":/update correct phone of the nominee")
					BANCS.OUTPUT.successOrFailure = "F"
						EXITSCRIPT

				ENDIF
			ENDIF
		ENDIF
	ENDIF

	IF (CUST.ACCTMOD.freeText10 !="") THEN

		sv_s = "count|"
		sv_s = sv_s + " select count (PHONENO) from crmuser.phoneemail where "
		sv_s = sv_s + " orgkey in (select NMA_KEY_ID from tbaadm.aas where acid in"
		sv_s = sv_s + " (select acid from tbaadm.gam where foracid = ?SVAR )) " 
		sv_s = sv_s + " and bank_id =?SVAR  and PHONEOREMAIL ='PHONE' and phoneno =?SVAR"
		BANCS.INPARAM.BINDVARS = BANCS.OUTPUT.fAccount + "|" + BANCS.STDIN.contextBankId + "|" + CUST.ACCTMOD.freeText10 
		print(BANCS.INPARAM.BINDVARS)
		print(sv_s)
		sv_d=urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		IF (sv_d == 0) THEN

			IF (BANCS.OUTPARAM.count !="") THEN

				CUST.ACCTMOD.count = BANCS.OUTPARAM.count
				PRINT(CUST.ACCTMOD.count)

				IF (CINT(CUST.ACCTMOD.count)==0) THEN
					print("invalid mobile")
					sv_u = urhk_SRV_SetErr("invalid Mobile: " + CUST.ACCTMOD.freeText10 + ":/update correct phone of the nominee")
					BANCS.OUTPUT.successOrFailure = "F"
						EXITSCRIPT

				ENDIF
			ENDIF
		ENDIF
	ENDIF
	IF (CUST.ACCTMOD.freeText12 !="") THEN

		sv_s = "count|"
		sv_s = sv_s + " select count (PHONENO) from crmuser.phoneemail where "
		sv_s = sv_s + " orgkey in (select NMA_KEY_ID from tbaadm.aas where acid in"
		sv_s = sv_s + " (select acid from tbaadm.gam where foracid = ?SVAR )) " 
		sv_s = sv_s + " and bank_id =?SVAR  and PHONEOREMAIL ='PHONE' and phoneno =?SVAR"
		BANCS.INPARAM.BINDVARS = BANCS.OUTPUT.fAccount + "|" + BANCS.STDIN.contextBankId + "|" + CUST.ACCTMOD.freeText12 
		print(BANCS.INPARAM.BINDVARS)
		print(sv_s)
		sv_d=urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		IF (sv_d == 0) THEN

			IF (BANCS.OUTPARAM.count !="") THEN

				CUST.ACCTMOD.count = BANCS.OUTPARAM.count
				PRINT(CUST.ACCTMOD.count)

				IF (CINT(CUST.ACCTMOD.count)==0) THEN
					print("invalid mobile")
					sv_u = urhk_SRV_SetErr("invalid Mobile: " + CUST.ACCTMOD.freeText12 + ":/update correct phone of the nominee")
					BANCS.OUTPUT.successOrFailure = "F"
						EXITSCRIPT

				ENDIF
			ENDIF
		ENDIF
	ENDIF

	IF (CUST.ACCTMOD.freeText14 !="") THEN

		sv_s = "count|"
		sv_s = sv_s + " select count (PHONENO) from crmuser.phoneemail where "
		sv_s = sv_s + " orgkey in (select NMA_KEY_ID from tbaadm.aas where acid in"
		sv_s = sv_s + " (select acid from tbaadm.gam where foracid = ?SVAR )) " 
		sv_s = sv_s + " and bank_id =?SVAR  and PHONEOREMAIL ='PHONE' and phoneno =?SVAR"
		BANCS.INPARAM.BINDVARS = BANCS.OUTPUT.fAccount + "|" + BANCS.STDIN.contextBankId + "|" + CUST.ACCTMOD.freeText14
		print(BANCS.INPARAM.BINDVARS)
		print(sv_s)
		sv_d=urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		IF (sv_d == 0) THEN

			IF (BANCS.OUTPARAM.count !="") THEN

				CUST.ACCTMOD.count = BANCS.OUTPARAM.count
				PRINT(CUST.ACCTMOD.count)

				IF (CINT(CUST.ACCTMOD.count)==0) THEN
					print("invalid mobile")
					sv_u = urhk_SRV_SetErr("invalid Mobile: " + CUST.ACCTMOD.freeText14 + ":/update correct phone of the nominee")
					BANCS.OUTPUT.successOrFailure = "F"
						EXITSCRIPT

				ENDIF
			ENDIF
		ENDIF
	ENDIF


	CALL("wthmn035.scr")
	TRACE OFF
EXITSCRIPT
END-->
