######################################################################################################################
# Source Name						: s_clgznoprtnmn005.scr
# Menu								: CPARAM
# Date								: 23-01-2013
# Description						: Validation
# Input Values						: ZoneCode
# Output Values						: Yes or No.
# Called Scripts					: SRV_FetchOCZoneDtlForOpen_validate_data.scr
# Calling Script					: getcparamvalue.scr
# Author     						: Iranna
# Bank								: Equity Bank,KENYA
# Modification History				:
#		< Version No.> 		<Date>		<Author Name> 		<Description>
#		 0.1				21-01-2013 	Iranna			   Original Version
########################################################################################################################

<--START
CHECKSUM="061fbc0cf90c59f9b64d5e881eef8c469ae0c5943e90eda94d27cb73e05863e5"
	TRACE ON
	
	#############################################################################
	#Custom Repository creation
	#############################################################################
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST","CLRREST") == 0) THEN
	#{
		CREATECLASS("CUST","CLRREST",5)
	#}
	ENDIF
	
	CUST.CLRREST.errMsg 	= ""
	CUST.CLRREST.headOffSol = ""
	CUST.CLRREST.zoneCode1  = ""
	CUST.CLRREST.zoneCode2  = ""
	CUST.CLRREST.zoneCode3 	= ""
	CUST.CLRREST.zoneCode4  = ""
	CUST.CLRREST.zoneCode5  = ""
	CUST.CLRREST.zoneCode6  = ""
	
	#############################################################################
	# Can't open zone under these zone
	#############################################################################
	
	sv_u = urhk_SRV_GetVal("zoneCode")
	print(sv_u)
	CUST.CLRREST.zoneCode=BANCS.OUTPARAM.srvValue
	print(CUST.CLRREST.zoneCode)
	sv_z = CUST.CLRREST.zoneCode
	
	BANCS.INPUT.moduleName="CLEARING"
	BANCS.INPUT.parameterId="SOL_000"
	call("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.CLRREST.headOffSol = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		CUST.CLRREST.headOffSol = ""
		CUST.CLRREST.errMsg = "CPARAM set up not done for head offc sol"
		GOTO FINISH
	#}
	ENDIF
	
	BANCS.INPUT.parameterId="ZONE_EQBOWZEURO"
	call("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.CLRREST.zoneCode1 = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		CUST.CLRREST.errMsg = "Zone Code setup is not done in CPARAM"
		GOTO FINISH
	#}
	ENDIF
	
	BANCS.INPUT.parameterId="ZONE_BCQ_EQBOWZ"
	call("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.CLRREST.zoneCode2 = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		CUST.CLRREST.errMsg = "Zone Code setup is not done in CPARAM"
		GOTO FINISH
	#}
	ENDIF
	
	BANCS.INPUT.parameterId="ZONE_EQBOWZ"
	call("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.CLRREST.zoneCode3 = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		CUST.CLRREST.errMsg = "Zone Code setup is not done in CPARAM"
		GOTO FINISH
	#}
	ENDIF
	
	BANCS.INPUT.parameterId="ZONE_EQBOWZGBP"
	call("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.CLRREST.zoneCode4 = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		CUST.CLRREST.errMsg = "Zone Code setup is not done in CPARAM"
		GOTO FINISH
	#}
	ENDIF
	
	BANCS.INPUT.parameterId="ZONE_EQBOWZUSD"
	call ("getcparamvalue.scr")
	IF(BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.CLRREST.zoneCode5 = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		CUST.CLRREST.errMsg = "Zone Code setup is not done in CPARAM"
		GOTO FINISH
	#}
	ENDIF
	
	BANCS.INPUT.parameterId="ZONE_EQBOW_INHS"
	call ("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.CLRREST.zoneCode6 = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		CUST.CLRREST.errMsg = "Zone Code setup is not done in CPARAM"
		GOTO FINISH
	#}
	ENDIF
	
	
	PRINT(BANCS.STDIN.homeSolId)
	PRINT(CUST.CLRREST.headOffSol)
	PRINT(CUST.CLRREST.zoneCode)
	
	
	###IF ((BANCS.STDIN.homeSolId != CUST.CLRREST.headOffSol) AND ((CUST.CLRREST.zoneCode==CUST.CLRREST.zoneCode1) OR (CUST.CLRREST.zoneCode==CUST.CLRREST.zoneCode2) OR (CUST.CLRREST.zoneCode==CUST.CLRREST.zoneCode3) OR (CUST.CLRREST.zoneCode==CUST.CLRREST.zoneCode4)OR (CUST.CLRREST.zoneCode==CUST.CLRREST.zoneCode5) OR (CUST.CLRREST.zoneCode==CUST.CLRREST.zoneCode6))) THEN
	#{
		###sv_a = urhk_SRV_SetErr("zoneCode|"+CUST.CLRREST.zoneCode +" zone can only be opened in head office" )
		###print(sv_a)
		###GOTO FINISH
	#}
	###ENDIF
	
	FINISH:
		IF (CUST.CLRREST.errMsg != "") THEN
		#{
			sv_a = urhk_SRV_SetErr(CUST.CLRREST.errMsg)
			print(sv_a)
		#}
		ENDIF
		
		TRACE OFF
		EXITSCRIPT
END-->
