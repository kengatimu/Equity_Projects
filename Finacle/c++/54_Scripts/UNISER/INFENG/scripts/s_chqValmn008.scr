############################################################################################################									
# Source Name						: s_chqValmn008.scr								
# Date								: 17-03-2013									
# Description						: This script is used to get the zone code,zone date,set num							
# Input								: None									
# Output							: To get populate zone code,zone date,set num.										
# Called Script						: chqValmn008.scr								
# Calling Script					: 	SRV_ReleaseOcSet_post_process_data.scr							
# Author     						: Shilpa									
# Bank								: 								
# Modification History				:				
#		< Version No.> 			<Date>		<Author Name> 		<Description>									
#		 0.1					17-03-2013 	Shilpa 			Original Version									
############################################################################################################

#######################################################################
# Creating repository and class
#######################################################################
<--START
TRACE ON

IF(REPEXISTS("CUST") == 0) THEN
#{
    CREATEREP("CUST")
#}
ENDIF

IF(CLASSEXISTS("CUST","CHQVAL") == 0) THEN
#{
    CREATECLASS("CUST","CHQVAL",5)
#}
ENDIF

#######################################################################
# Value from Front end
#######################################################################
CUST.CHQVAL.errMsg = ""

sv_u=urhk_SRV_GetVal("ocSet.zoneHeader.zoneDate")
CUST.CHQVAL.clgzodate=BANCS.OUTPARAM.srvValue
print(CUST.CHQVAL.clgzodate)

sv_u=urhk_SRV_GetVal("ocSet.zoneHeader.zoneCode")
CUST.CHQVAL.clgzocode=BANCS.OUTPARAM.srvValue
print(CUST.CHQVAL.clgzocode)

sv_u=urhk_SRV_GetVal("ocSet.setInfo.setNum")
CUST.CHQVAL.setnum=BANCS.OUTPARAM.srvValue
print(CUST.CHQVAL.setnum)

sv_f = "0"
sv_o = CINT(sv_f)
print(sv_o)
#####################################################################
# Calling getcparamvalue.scr script for BACID
#####################################################################

BANCS.INPUT.moduleName = "CLEARING"
BANCS.INPUT.parameterId = "BACID5"
CALL ("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "")THEN
#{
	CUST.CHQVAL.bacid = BANCS.OUTPUT.parameterVal
    print(CUST.CHQVAL.bacid)
	
#}
ELSE
#{
	CUST.CHQVAL.errMsg = "BACID IS NOT SET"
	GOTO ENDSCRIPT 
#}
ENDIF
#######################################################################
# Calling getcparamvalue.scr script for INCOME ACCOUNT BACID
#######################################################################

BANCS.INPUT.moduleName = "CLEARING"
BANCS.INPUT.parameterId = "INCOMEBACID"
CALL ("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "")THEN
#{
	CUST.CHQVAL.incmbacid = BANCS.OUTPUT.parameterVal
    print(CUST.CHQVAL.incmbacid)
	
#}
ELSE
#{
	CUST.CHQVAL.errMsg = "BACID IS NOT SET"
	GOTO ENDSCRIPT 
#}
ENDIF

#######################################################################
# Calling getcparamvalue.scr script for CRNCY
#######################################################################
	CUST.CHQVAL.crncy = BANCS.STDIN.homeCrncyCode
    print(CUST.CHQVAL.crncy)

#######################################################################
# Calling getcparamvalue.scr script for BANKCODEVAL
#######################################################################

BANCS.INPUT.moduleName = "CLEARING"
BANCS.INPUT.parameterId = "BANKCODEVAL"
CALL ("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "")THEN
#{
	CUST.CHQVAL.bankcode = BANCS.OUTPUT.parameterVal
    print(CUST.CHQVAL.bankcode)
	
#}
ELSE
#{
	CUST.CHQVAL.errMsg = "BACID IS NOT SET"
	GOTO ENDSCRIPT 
#}
ENDIF

#######################################################################
# Calling getcparamvalue.scr script for OLDBACID
#######################################################################

BANCS.INPUT.moduleName = "CLEARING"
BANCS.INPUT.parameterId = "OLDBACID"
CALL ("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "")THEN
#{
	CUST.CHQVAL.oldbacid = BANCS.OUTPUT.parameterVal
    print(CUST.CHQVAL.oldbacid)
	
#}
ELSE
#{
	CUST.CHQVAL.errMsg = "BACID IS NOT SET"
	GOTO ENDSCRIPT 
#}
ENDIF

#######################################################################
# Calling getcparamvalue.scr script for ACCTNUM
#######################################################################

BANCS.INPUT.moduleName = "CLEARING"
BANCS.INPUT.parameterId = "ACCTNUM5"
CALL ("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "")THEN
#{
	CUST.CHQVAL.acctnum1 = BANCS.OUTPUT.parameterVal
    print(CUST.CHQVAL.acctnum1)
	
#}
ELSE
#{
	CUST.CHQVAL.errMsg = "ACCTNUM IS NOT SET"
	GOTO ENDSCRIPT 
#}
ENDIF

#######################################################################
# Calling getcparamvalue.scr script for ACCTNUM
#######################################################################

BANCS.INPUT.moduleName = "CLEARING"
BANCS.INPUT.parameterId = "ACCTNUM6"
CALL ("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "")THEN
#{
	CUST.CHQVAL.acctnum2 = BANCS.OUTPUT.parameterVal
    print(CUST.CHQVAL.acctnum2)
	
#}
ELSE
#{
	CUST.CHQVAL.errMsg = "ACCTNUM IS NOT SET"
	GOTO ENDSCRIPT 
#}
ENDIF

#######################################################################
# Calling getcparamvalue.scr script for ACCTNUM
#######################################################################

BANCS.INPUT.moduleName = "CLEARING"
BANCS.INPUT.parameterId = "ACCTNUM7"
CALL ("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "")THEN
#{
	CUST.CHQVAL.acctnum3 = BANCS.OUTPUT.parameterVal
    print(CUST.CHQVAL.acctnum3)
	
#}
ELSE
#{
	CUST.CHQVAL.errMsg = "ACCTNUM IS NOT SET"
	GOTO ENDSCRIPT 
#}
ENDIF

#######################################################################
# Calling getcparamvalue.scr script for ACCTNUM
#######################################################################

BANCS.INPUT.moduleName = "CLEARING"
BANCS.INPUT.parameterId = "ACCTNUM8"
CALL ("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "")THEN
#{
	CUST.CHQVAL.acctnum4 = BANCS.OUTPUT.parameterVal
    print(CUST.CHQVAL.acctnum4)
	
#}
ELSE
#{
	CUST.CHQVAL.errMsg = "ACCTNUM IS NOT SET"
	GOTO ENDSCRIPT 
#}
ENDIF


#######################################################################
# Calling getcparamvalue.scr script for ACCTNUM
#######################################################################

BANCS.INPUT.moduleName = "CLEARING"
BANCS.INPUT.parameterId = "ACCTNUM9"
CALL ("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "")THEN
#{
	CUST.CHQVAL.acctnum5 = BANCS.OUTPUT.parameterVal
    print(CUST.CHQVAL.acctnum5)
	
#}
ELSE
#{
	CUST.CHQVAL.errMsg = "ACCTNUM IS NOT SET"
	GOTO ENDSCRIPT 
#}
ENDIF

#-----------------------------------------------------------------------------------------------
#Calling getcparamvalue.scr script for NORRATECODE
#-----------------------------------------------------------------------------------------------
BANCS.INPUT.moduleName = "GENERAL"
BANCS.INPUT.parameterId = "NORRATECODE"

CALL ("getcparamvalue.scr")
print(BANCS.OUTPUT.parameterVal)

IF (BANCS.OUTPUT.parameterVal != "")THEN
#{
	CUST.CHQVAL.refCode = BANCS.OUTPUT.parameterVal
	print(CUST.CHQVAL.refCode)
#}
ELSE
#{
	CUST.CHQVAL.errMsg = "NORRATECODE setup not done in CPARAM"
	GOTO ENDSCRIPT 
#}
ENDIF

#-----------------------------------------------------------------------------------------------
#Getting ZONECODE1 
#-----------------------------------------------------------------------------------------------
BANCS.INPUT.moduleName = "CLEARING"
BANCS.INPUT.parameterId = "ZONECODE1"

CALL ("getcparamvalue.scr")
print(BANCS.OUTPUT.parameterVal)

IF (BANCS.OUTPUT.parameterVal != "")THEN
#{
	CUST.CHQVAL.zonecode1 = BANCS.OUTPUT.parameterVal
	print(CUST.CHQVAL.zonecode1)
#}
ELSE
#{
	CUST.CHQVAL.errMsg = "ZONECODE setup not done in CPARAM"
	GOTO ENDSCRIPT 
#}
ENDIF

#-----------------------------------------------------------------------------------------------
#Getting ZONECODE2 
#-----------------------------------------------------------------------------------------------
BANCS.INPUT.moduleName = "CLEARING"
BANCS.INPUT.parameterId = "ZONECODE2"

CALL ("getcparamvalue.scr")
print(BANCS.OUTPUT.parameterVal)

IF (BANCS.OUTPUT.parameterVal != "")THEN
#{
	CUST.CHQVAL.zonecode2 = BANCS.OUTPUT.parameterVal
	print(CUST.CHQVAL.zonecode2)
#}
ELSE
#{
	CUST.CHQVAL.errMsg = "ZONECODE setup not done in CPARAM"
	GOTO ENDSCRIPT 
#}
ENDIF

#-----------------------------------------------------------------------------------------------
#Getting STAFF 
#-----------------------------------------------------------------------------------------------
BANCS.INPUT.moduleName = "CLEARING"
BANCS.INPUT.parameterId = "STAFF"

CALL ("getcparamvalue.scr")
print(BANCS.OUTPUT.parameterVal)

IF (BANCS.OUTPUT.parameterVal != "")THEN
#{
	CUST.CHQVAL.staff = BANCS.OUTPUT.parameterVal
	print(CUST.CHQVAL.staff)
#}
ELSE
#{
	CUST.CHQVAL.errMsg = "NORRATECODE setup not done in CPARAM"
	GOTO ENDSCRIPT 
#}
ENDIF

#-----------------------------------------------------------------------------------------------
#Getting REFRAMT 
#-----------------------------------------------------------------------------------------------
BANCS.INPUT.moduleName = "CLEARING"
BANCS.INPUT.parameterId = "REFRAMT"

CALL ("getcparamvalue.scr")
print(BANCS.OUTPUT.parameterVal)

IF (BANCS.OUTPUT.parameterVal != "")THEN
#{
	CUST.CHQVAL.refAmt = BANCS.OUTPUT.parameterVal
	print(CUST.CHQVAL.refAmt)
#}
ELSE
#{
	CUST.CHQVAL.errMsg = "NORRATECODE setup not done in CPARAM"
	GOTO ENDSCRIPT 
#}
ENDIF
#------------------------------------------------------------------
#  Hardcoding the bacid for Clearing Account
#  & validation for the bacid.
#------------------------------------------------------------------
sv_q = CUST.CHQVAL.bacid
sv_i = sv_q + "|" +CUST.CHQVAL.crncy+ "|" + BANCS.STDIN.mySolId
print(sv_i)
sv_u = urhk_B2k_BacidToAcctId(sv_i)
print(sv_u)
IF (sv_u == 1 ) THEN
#{
	GOTO ENDSCRIPT 
#}
ENDIF
sv_m = BANCS.OUTPARAM.AcctId
print(sv_m)

#------------------------------------------------------------------
#  Hardcoding the bacid for Clearing Account(Income Account)
#  & validation for the bacid.
#------------------------------------------------------------------


sv_q = CUST.CHQVAL.incmbacid 
sv_i = sv_q + "|" +CUST.CHQVAL.crncy+ "|" + BANCS.STDIN.mySolId
print(sv_i)
sv_u = urhk_B2k_BacidToAcctId(sv_i)
print(sv_u)
IF (sv_u == 1 ) THEN
#{
	GOTO ENDSCRIPT
#} 
ENDIF
sv_x = BANCS.OUTPARAM.AcctId
print(sv_x)

#---------------------------------------------------------------------------------
# Cursor Initialize
#---------------------------------------------------------------------------------

CUST.CHQVAL.clgzonedate = MID$(CUST.CHQVAL.clgzodate,0,10)
PRINT(CUST.CHQVAL.clgzonedate)


sv_s = "bkcd,brcd,payacid,instramt,trancode, "
sv_s = sv_s + " instid,instrdate| "
sv_s = sv_s + " SELECT BANK_CODE,BR_CODE,PAYING_ACCT_ID,INSTRMNT_AMT,TRAN_CODE, "
sv_s = sv_s + " INSTRMNT_ID,to_char(INSTRMNT_DATE,'dd-mm-yyyy HH24:MI:SS')  "
sv_s = sv_s + " FROM TBAADM.OCI WHERE  "
sv_s = sv_s + " CLG_ZONE_DATE = to_date(?SVAR,'DD-MM-YYYY') "
sv_s = sv_s + " AND CLG_ZONE_CODE = ?SVAR " 
sv_s = sv_s + " AND SOL_ID = ?SVAR " 
sv_s = sv_s + " AND BANK_ID = ?SVAR "
sv_s = sv_s + " AND DEL_FLG!='Y' "
sv_s = sv_s + " AND SET_NUM = ?SVAR "
print(sv_s)
BANCS.INPARAM.BINDVARS =  CUST.CHQVAL.clgzonedate + "|" + CUST.CHQVAL.clgzocode + "|" + BANCS.STDIN.mySolId + "|" +BANCS.STDIN.contextBankId + "|" +CUST.CHQVAL.setnum
PRINT(BANCS.INPARAM.BINDVARS)
sv_c = urhk_dbCursorOpenWithBind(sv_s)
print(sv_c)
IF (sv_c == 0) then
	sv_n = BANCS.OUTPARAM.DB_CURSOR_NUMBER
    print(sv_n)
	CUST.CHQVAL.cursorNum = BANCS.OUTPARAM.DB_CURSOR_NUMBER  
	sv_f = urhk_dbCursorFetch(sv_n)   
    print(sv_f)
	while(sv_f == 0)
    CUST.CHQVAL.bkcd= BANCS.OUTPARAM.bkcd
	print(CUST.CHQVAL.bkcd) 
    print(BANCS.OUTPARAM.payacid)
	sv_z = BANCS.OUTPARAM.payacid
    print(sv_z)
    sv_b = cdouble(BANCS.OUTPARAM.instramt)
	print(sv_b)
    CUST.CHQVAL.trancode = trim(BANCS.OUTPARAM.trancode)
	print(CUST.CHQVAL.trancode)
	CUST.CHQVAL.instid = trim(BANCS.OUTPARAM.instid)
	print(CUST.CHQVAL.instid)
	CUST.CHQVAL.instid = LPAD(CUST.CHQVAL.instid, 8, ' ')
	print(CUST.CHQVAL.instid)
	CUST.CHQVAL.instrdate = trim(BANCS.OUTPARAM.instrdate)
	print(CUST.CHQVAL.instrdate)
#    IF (CUST.CHQVAL.instrdate == "" ) THEN
#   	#{
#		CUST.CHQVAL.errMsg = "Instrument date should not be null"
#		sv_n = "echo Instrument date should not be null >> error.log"
#		sv_g = system(sv_n)
#		GOTO ENDSCRIPT 
#   	#}
#    ENDIF
      
#    IF (CUST.CHQVAL.instid == "" ) THEN
#    #{
#		CUST.CHQVAL.errMsg = "Instrument id should not be null"
#		sv_n = "echo Instrument id >> error.log"
#		sv_g = system(sv_n)
#		GOTO ENDSCRIPT 
#   	#}
#       ENDIF

    CUST.CHQVAL.brcd = BANCS.OUTPARAM.brcd
    print(CUST.CHQVAL.brcd)
#######################################################################
#  Checking the bank code and branch code
#######################################################################
print(BANCS.OUTPARAM.bkcd)
print(CUST.CHQVAL.bankcode)

IF ( CUST.CHQVAL.bkcd == CUST.CHQVAL.bankcode) THEN
#{
    
    sv_q = "brcode|SELECT BR_CODE FROM TBAADM.SOL"
	sv_q = sv_q + " WHERE BR_CODE = ?SVAR "
    sv_q = sv_q + " and BANK_ID = ?SVAR "
    print(sv_q)
    BANCS.INPARAM.BINDVARS =  CUST.CHQVAL.brcd + "|" +BANCS.STDIN.contextBankId
    print(BANCS.INPARAM.BINDVARS)
	sv_w = urhk_dbSelectWithBind(sv_q)
	IF (sv_w == 0) THEN
	#{
		CUST.CHQVAL.paycid = sv_z
		print(CUST.CHQVAL.paycid) 
		sv_q = "foracid|SELECT FORACID FROM TBAADM.GAM "
		sv_q = sv_q + " WHERE FORACID = ?SVAR "
		sv_q = sv_q + " AND BANK_ID = ?SVAR "
		print(sv_q)
		BANCS.INPARAM.BINDVARS =  CUST.CHQVAL.paycid + "|" +BANCS.STDIN.contextBankId
		print(BANCS.INPARAM.BINDVARS)
		sv_u = urhk_dbSelectWithBind(sv_q)
		IF (sv_u == 1 ) THEN
		#{
			sv_q = "foracid|SELECT FORACID FROM CUSTOM.OLD_ACCT_MAP "
			sv_q = sv_q + " WHERE OLD_NUM = ?SVAR "
			sv_q = sv_q + " AND BANK_ID = ?SVAR "
			print(sv_q)    
			BANCS.INPARAM.BINDVARS =  CUST.CHQVAL.paycid + "|" +BANCS.STDIN.contextBankId
			print(BANCS.INPARAM.BINDVARS)
			sv_j = urhk_dbSelectWithBind(sv_q)
			print(sv_j)
			IF (sv_j == 1) THEN
			#{
				sv_q = CUST.CHQVAL.oldbacid
				sv_i = sv_q + "|" +CUST.CHQVAL.crncy+ "|" + BANCS.STDIN.mySolId
				print(sv_i)
				sv_u = urhk_B2k_BacidToAcctId(sv_i)
				print(sv_u)
				IF (sv_u == 1 ) THEN
				#{
					GOTO ENDSCRIPT 
				#}
				ENDIF
				sv_p = BANCS.OUTPARAM.AcctId
				print(sv_p)
			#}
			ELSE
			#{
				sv_p = BANCS.OUTPARAM.foracid
				print(sv_p)
			#}
			ENDIF
		ELSE
		#{
			sv_p =CUST.CHQVAL.paycid 
			print(sv_p)
		#}
		ENDIF
	#}
	ELSE
	#{
		GOTO ENDSCRIPT 
	#}
	ENDIF

###############################################################################################################
# To check instrment type
###############################################################################################################
sv_s = "insttype|SELECT INSTRMNT_TYPE FROM TBAADM.CTC where "
sv_s = sv_s + " CLG_TRAN_CODE= ?SVAR "
sv_S = sv_s + " AND BANK_ID = ?SVAR "
sv_s = sv_s + " AND DEL_FLG!='Y' "
print(sv_s)
BANCS.INPARAM.BINDVARS =  CUST.CHQVAL.trancode + "|" +BANCS.STDIN.contextBankId
sv_d = urhk_dbSelectWithBind(sv_s)
IF (sv_d == 0) THEN
#{
	CUST.CHQVAL.insttype = trim(BANCS.OUTPARAM.insttype)
    print(CUST.CHQVAL.insttype)
#}
ELSE
#{
    CUST.CHQVAL.insttype = "CHQ"
    print(CUST.CHQVAL.insttype)
#}
ENDIF

##############################################################################################################
# TO get date diff between instrdate and bod date
#############################################################################################################

#sv_s = urhk_B2K_getDateDiffFromBODDate(CUST.CHQVAL.instrdate)
#print(sv_s)
#CUST.CHQVAL.DIFF = CINT(BANCS.OUTPARAM.DateDiff)
#print(CUST.CHQVAL.DIFF)
#IF (trim(CUST.CHQVAL.DIFF) == "") THEN
#{
#	CUST.CHQVAL.DIFF = "0"
#}
#ENDIF
#print(CUST.CHQVAL.DIFF)

#------------------------------------------------------
#Exempting staff Acc from the Inhouse chq chrgs
#------------------------------------------------------
sv_u="staff|"
sv_u = sv_u + "SElECT SCHM_CODE from TBAADM.GAM "
sv_u = sv_u + " WHERE FORACID = ?SVAR "
sv_u = sv_u + " AND BANK_ID = ?SVAR "
print(sv_u)
BANCS.INPARAM.BINDVARS =  sv_p  + "|" +BANCS.STDIN.contextBankId
print(BANCS.INPARAM.BINDVARS)
sv_d = urhk_dbSelectWithBind(sv_u)
IF (sv_d == 0) THEN
#{
	CUST.CHQVAL.schmcode = BANCS.OUTPARAM.staff
	print(CUST.CHQVAL.schmcode)
#}
ENDIF


#---------------------------------------------------------------
# Passing transaction inputs to the service
# Adding and Posting the transaction
#---------------------------------------------------------------
sv_u = urhk_SetUrhkInp("tranHdr.tranIdentifier.tranDate|"+BANCS.STDIN.BODDate)
sv_u = urhk_SetUrhkInp("tranHdr.tranTypeSubType.tranType|T")
sv_u = urhk_SetUrhkInp("tranHdr.tranTypeSubType.tranSubType|BI")
sv_u = urhk_SetUrhkInp("tranHdr.tranRmks|CLEARING")
sv_u = urhk_SetUrhkInp("tranDtl.ignoreXcpnFlg|I")
sv_u = urhk_SetUrhkInp("tranDtl.pstFlg|Y")

IF ((sv_p !=CUST.CHQVAL.acctnum1) AND  (sv_p!=CUST.CHQVAL.acctnum2) AND (sv_p!=CUST.CHQVAL.acctnum3) AND (sv_p!=CUST.CHQVAL.acctnum4) AND (sv_p!=CUST.CHQVAL.acctnum5))THEN
#{
	IF (((CUST.CHQVAL.clgzocode ==CUST.CHQVAL.zonecode1) OR (CUST.CHQVAL.clgzocode ==CUST.CHQVAL.zonecode2)) AND (CUST.CHQVAL.schmcode != CUST.CHQVAL.staff))THEN 
	#{
	#-----------------------------------------------------------------------------
	# Income To Clearing Income Account 25 bobs in Inhouse zones
	#-----------------------------------------------------------------------------
	
    CUST.CHQVAL.particular = "Clearing Charges"
	sv_o = sv_o + CINT("1")
	sv_k = Format$(sv_o,"%d")
	sv_c = "<rec_"+sv_k+">."
	sv_a = "tranDtl.partTranDetailLL." +sv_c
	sv_u = urhk_SetUrhkInp(sv_a + "key.serial_num|"+sv_k)
	sv_u = urhk_SetUrhkInp(sv_a + "pTranIdentifier.tranDate|"+BANCS.STDIN.BODDate)
    sv_u = urhk_SetUrhkInp(sv_a + "partTranType.code|C")
    sv_u = urhk_SetUrhkInp(sv_a + "acctId.foracid|"+trim(sv_x))
    sv_u = urhk_SetUrhkInp(sv_a + "tranParticulars|"+CUST.CHQVAL.particular)
	sv_u = urhk_SetUrhkInp(sv_a + "tranRmks| ")
    sv_u = urhk_SetUrhkInp(sv_a + "refNum| ")
	sv_u = urhk_SetUrhkInp(sv_a + "valueDate|"+BANCS.STDIN.BODDate)
	sv_u = urhk_SetUrhkInp(sv_a + "glDate|"+BANCS.STDIN.BODDate)
	sv_u = urhk_SetUrhkInp(sv_a + "tranAmt|"+CUST.CHQVAL.refAmt+"|"+ BANCS.STDIN.homeCrncyCode)
    sv_u = urhk_SetUrhkInp(sv_a + "refCrncyCode.crncyCode|" + BANCS.STDIN.homeCrncyCode)
    sv_u = urhk_SetUrhkInp(sv_a + "refAmt|"+CUST.CHQVAL.refAmt+"|"+ BANCS.STDIN.homeCrncyCode)
	sv_u = urhk_SetUrhkInp(sv_a + "entryUserId.userId|" + BANCS.STDIN.userId)
	sv_u = urhk_SetUrhkInp(sv_a + "vfdUserId.userId|" + BANCS.STDIN.userId)
	sv_u = urhk_SetUrhkInp(sv_a + "rateCode.refCode|" + CUST.CHQVAL.refCode)
      
	#------------------------------------------------------------------------------------------------
	# Customer Debit Partran
	#-------------------------------------------------------------------------------------------------
    CUST.CHQVAL.particular = "Clearing Charges"
	sv_o = sv_o + CINT("1")
	sv_k = Format$(sv_o,"%d")
	sv_c = "<rec_"+sv_k+">."
	sv_a = "tranDtl.partTranDetailLL." +sv_c
	sv_u = urhk_SetUrhkInp(sv_a + "key.serial_num|"+sv_k)
	sv_u = urhk_SetUrhkInp(sv_a + "pTranIdentifier.tranDate|"+BANCS.STDIN.BODDate)
    sv_u = urhk_SetUrhkInp(sv_a + "partTranType.code|D")
 	sv_u = urhk_SetUrhkInp(sv_a + "acctId.foracid|"+trim(sv_p))
	sv_u = urhk_SetUrhkInp(sv_a + "tranParticulars|"+CUST.CHQVAL.particular)
 	sv_u = urhk_SetUrhkInp(sv_a + "tranRmks| ")
    sv_u = urhk_SetUrhkInp(sv_a + "refNum| ")
	sv_u = urhk_SetUrhkInp(sv_a + "valueDate|"+BANCS.STDIN.BODDate)
	sv_u = urhk_SetUrhkInp(sv_a + "glDate|"+BANCS.STDIN.BODDate)
	sv_u = urhk_SetUrhkInp(sv_a + "tranAmt|"+CUST.CHQVAL.refAmt+"|"+ BANCS.STDIN.homeCrncyCode)
	sv_u = urhk_SetUrhkInp(sv_a + "refCrncyCode.crncyCode|" + BANCS.STDIN.homeCrncyCode)
	sv_u = urhk_SetUrhkInp(sv_a + "refAmt|"+CUST.CHQVAL.refAmt+"|"+ BANCS.STDIN.homeCrncyCode)
	sv_u = urhk_SetUrhkInp(sv_a + "entryUserId.userId|" + BANCS.STDIN.userId)
	sv_u = urhk_SetUrhkInp(sv_a + "vfdUserId.userId|" + BANCS.STDIN.userId)
	sv_u = urhk_SetUrhkInp(sv_a + "rateCode.refCode|" + CUST.CHQVAL.refCode)
	#}
	ENDIF
#}	
ENDIF
#-----------------------------------------------------------------------------
# customer credit
#-----------------------------------------------------------------------------
CUST.CHQVAL.particular = "Inhse Clg Chq No."+(CUST.CHQVAL.instid)
CUST.CHQVAL.refAmt = sv_b
sv_o = sv_o + CINT("1")
sv_k = Format$(sv_o,"%d")
sv_c = "<rec_"+sv_k+">."

sv_a = "tranDtl.partTranDetailLL." +sv_c
sv_u = urhk_SetUrhkInp(sv_a + "key.serial_num|"+sv_k)
sv_u = urhk_SetUrhkInp(sv_a + "pTranIdentifier.tranDate|"+BANCS.STDIN.BODDate)
sv_u = urhk_SetUrhkInp(sv_a + "partTranType.code|C")
sv_u = urhk_SetUrhkInp(sv_a + "acctId.foracid|"+(sv_m))
sv_u = urhk_SetUrhkInp(sv_a + "tranParticulars|"+CUST.CHQVAL.particular)
sv_u = urhk_SetUrhkInp(sv_a + "tranRmks| ")
sv_u = urhk_SetUrhkInp(sv_a + "refNum| ")
sv_u = urhk_SetUrhkInp(sv_a + "valueDate|"+BANCS.STDIN.BODDate)
sv_u = urhk_SetUrhkInp(sv_a + "glDate|"+BANCS.STDIN.BODDate)
sv_u = urhk_SetUrhkInp(sv_a + "tranAmt|"+CUST.CHQVAL.refAmt+"|"+ BANCS.STDIN.homeCrncyCode)
sv_u = urhk_SetUrhkInp(sv_a + "refCrncyCode.crncyCode|" + BANCS.STDIN.homeCrncyCode)
sv_u = urhk_SetUrhkInp(sv_a + "refAmt|"+CUST.CHQVAL.refAmt+"|"+ BANCS.STDIN.homeCrncyCode)
sv_u = urhk_SetUrhkInp(sv_a + "entryUserId.userId|" + BANCS.STDIN.userId)
sv_u = urhk_SetUrhkInp(sv_a + "vfdUserId.userId|" + BANCS.STDIN.userId)
sv_u = urhk_SetUrhkInp(sv_a + "rateCode.refCode|" + CUST.CHQVAL.refCode)
   
#------------------------------------------------------------------------------------------------
# Customer Debit 
#-------------------------------------------------------------------------------------------------
CUST.CHQVAL.particular = "Clearing Chq No."+(CUST.CHQVAL.instid)
CUST.CHQVAL.refAmt = (sv_b)
sv_o = sv_o + CINT("1")
sv_k = Format$(sv_o,"%d")
sv_c = "<rec_"+sv_k+">."
sv_a = "tranDtl.partTranDetailLL." +sv_c
sv_u = urhk_SetUrhkInp(sv_a + "key.serial_num|"+sv_k)
sv_u = urhk_SetUrhkInp(sv_a + "pTranIdentifier.tranDate|"+BANCS.STDIN.BODDate)
sv_u = urhk_SetUrhkInp(sv_a + "partTranType.code|D")
sv_u = urhk_SetUrhkInp(sv_a + "acctId.foracid|"+trim(sv_p))
sv_u = urhk_SetUrhkInp(sv_a + "tranParticulars|"+CUST.CHQVAL.particular)
sv_u = urhk_SetUrhkInp(sv_a + "tranRmks| ")
sv_u = urhk_SetUrhkInp(sv_a + "refNum| ")
sv_u = urhk_SetUrhkInp(sv_a + "valueDate|"+BANCS.STDIN.BODDate)
sv_u = urhk_SetUrhkInp(sv_a + "glDate|"+BANCS.STDIN.BODDate)
sv_u = urhk_SetUrhkInp(sv_a + "tranAmt|"+CUST.CHQVAL.refAmt+"|"+ BANCS.STDIN.homeCrncyCode)
sv_u = urhk_SetUrhkInp(sv_a + "refCrncyCode.crncyCode|" + BANCS.STDIN.homeCrncyCode)
sv_u = urhk_SetUrhkInp(sv_a + "refAmt|"+CUST.CHQVAL.refAmt+"|"+ BANCS.STDIN.homeCrncyCode)
sv_u = urhk_SetUrhkInp(sv_a + "entryUserId.userId|" + BANCS.STDIN.userId)
sv_u = urhk_SetUrhkInp(sv_a + "vfdUserId.userId|" + BANCS.STDIN.userId)
sv_u = urhk_SetUrhkInp(sv_a + "rateCode.refCode|" + CUST.CHQVAL.refCode)
sv_u = urhk_SetUrhkInp(sv_a + "instrmnt.instrmntType|" + CUST.CHQVAL.insttype)
sv_u = urhk_SetUrhkInp(sv_a + "instrmnt.instrmntDate|" + CUST.CHQVAL.instrdate)
sv_u = urhk_SetUrhkInp(sv_a + "instrmnt.instrmntNum|" + CUST.CHQVAL.instid)
sv_u = urhk_SetUrhkOut("tranDtl.tranIdentifier.tranId|tranId")
sv_u = urhk_ExecSrv("SRV_AddXferTran|retain_all_output = Y")
print(sv_u)
IF (sv_u != 0) THEN
#{
	sv_u = urhk_CopyOutToIn("")
	sv_u = urhk_ExecSrv("SRV_AddXferTran|retain_all_output = Y")
	print(sv_u)
#}
ENDIF

IF (sv_u == 0) THEN
#{
	IF (FIELDEXISTS(BANCS.OUTPARAM.tranId) == 1) THEN
	#{
		print(BANCS.OUTPARAM.tranId)
	#}
	ENDIF
	sv_u = urhk_CopyOutToIn("")
	sv_u = urhk_ExecSrv("SRV_FetchTranDtlsForPost|retain_all_output = Y")
	print(sv_u)
	IF (sv_u == 0) THEN
	#{
		sv_u = urhk_CopyOutToIn("")
		print(sv_u)
		sv_u = urhk_ExecSrv("SRV_PostTran|retain_all_output = Y")
		print(sv_u)
		IF (sv_u != 0 ) THEN
		#{
			sv_u = urhk_CopyOutToIn("")
			sv_u = urhk_ExecSrv("SRV_PostTran|retain_all_output = Y")
			print(sv_u)
		#}
		ENDIF
	#}
	ENDIF
#}
ENDIF

#}
ENDIF
#############################################################################################
# Error
###############################################################################################
IF (sv_u != 0) THEN
#{
sv_x = ""
	IF (FIELDEXISTS(BANCS.OUTPARAM.Error_num) == 1) THEN
	#{
		print(BANCS.OUTPARAM.Error_num)
		sv_v = BANCS.OUTPARAM.Error_num
	#}
	ELSE
	#{
		sv_v = 0
	#}
	ENDIF
		IF (sv_v > 0) then
		#{
			sv_i = 1
			WHILE (sv_i <= sv_v)
			#{
				sv_f = FORMAT$(sv_i,"%d")
				sv_e = ("BANCS").("OUTPARAM").("ErrorMesg_"+sv_f)
				sv_x = sv_x + ("BANCS").("OUTPARAM").("ErrorMesg_"+sv_f)
				sv_e = sv_e + " " + ("BANCS").("OUTPARAM").("Tag_"+sv_f)
				sv_e = sv_e + " Error Code:" + ("BANCS").("OUTPARAM").("ErrorCode_"+sv_f)
				print(sv_e)
				sv_i = sv_i + 1
				sv_u = system("echo '"+sv_e+"' >>MTT1117.log")
				print(sv_u)
			#}
			DO
		#}
		ENDIF
sv_u = urhk_SRV_setErr(sv_x)
BANCS.OUTPUT.successOrFailure="F"
#}
ENDIF

	sv_f = urhk_dbCursorFetch(sv_n)
	do
	sv_x = urhk_dbCursorClose(sv_n)
	print(sv_x)
#}	
ENDIF

ENDSCRIPT :
IF(CUST.CHQVAL.errMsg != "") THEN
#{
        sv_u = urhk_SRV_setErr(CUST.CHQVAL.errMsg)
	BANCS.OUTPUT.successOrFailure="F"
#}
ENDIF
TRACE OFF
EXITSCRIPT
END-->
