#------------------------------------------------------------------------------------------------
#  Name            : FI_demandisic1.scr
#  Description     : Fetch data for customer segmentation (Demand isic 3)
#  Date            : 17-03-2022
#  Author          : James Maina
#  Input           : NA
#  Output          : NA
#  Called Script   : NA
#  Menu Option     :  (API)
#  Srl. No         Date            Author               Description.
#  -------         ------          ------               ------------
#  1.0             17-03-2022      James Maina		Original Version
#-----------------------------------------------------------------------------------------------
<--START
TRACE ON

	sv_u=REPEXISTS("CUST")
	IF(sv_u==0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF

	sv_u=CLASSEXISTS("CUST", "CSEG")
	IF(sv_u==0) THEN
	#{
		CREATECLASS("CUST", "CSEG", 5)
	#}
	ENDIF

	sv_t=urhk_b2k_printrepos("BANCS")
	print(sv_t)

	IF( (FIELDEXISTS(("BANCS").("INPUT").("CATEGORYTYPE"))== 0) ) THEN
	#{
   		CUST.CSEG.errMsg="please enter CATEGORYTYPE"
		sv_t = "ERROR | " +CUST.CSEG.errMsg
		sv_u = urhk_SetOrbOut(sv_t)
   		GOTO ENDSCRIPT
	#}
	ENDIF

	#---------------------------------------------------------------------------------------------------------------
	# Variable Initialization
	#---------------------------------------------------------------------------------------------------------------

	CUST.CSEG.CATEGORYTYPE= BANCS.INPUT.CATEGORYTYPE
	print(CUST.CSEG.CATEGORYTYPE)		


	sv_s="DEMAND1_CODE, DEMAND1_DESC |"
	sv_s=sv_s + "SELECT C.VALUE, L.LOCALETEXT FROM CRMUSER.CATEGORIES C, CRMUSER.CATEGORY_LANG L WHERE  "
	sv_s=sv_s + "C.BANK_ID=?SVAR AND L.BANK_ID=?SVAR AND C.CATEGORYID=L.CATEGORYID  AND "
	sv_s=sv_s + "C.CATEGORYTYPE=?SVAR " 
	print(sv_s)

	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.contextBankId + "|" + CUST.CSEG.CATEGORYTYPE
	print(BANCS.INPARAM.BINDVARS)

	sv_v = urhk_dbcursorOpenWithBind(sv_s)
       	PRINT(sv_v)	

	IF(sv_v == 0) THEN
       	 #{
        	CUST.CSEG.CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
       		print(CUST.CSEG.CURSOR_NUMBER)
       		sv_b = urhk_dbCursorFetch(CUST.CSEG.CURSOR_NUMBER)
        	print(sv_b)
		BANCS.INPUT.RECCOUNT = CINT("1")
        	print(BANCS.INPUT.RECCOUNT)

		sv_i=1
		print(sv_i)

		WHILE(sv_b == "0")
        	#{
			CUST.CSEG.DEMAND1_CODE= BANCS.OUTPARAM.DEMAND1_CODE
			print(CUST.CSEG.DEMAND1_CODE)

			CUST.CSEG.DEMAND1_DESC= BANCS.OUTPARAM.DEMAND1_DESC
			print(CUST.CSEG.DEMAND1_DESC)

			sv_s="DEMANDISIC1_CODE"+Format$(sv_i, "%d")+ "|" + CUST.CSEG.DEMAND1_CODE
			sv_u = urhk_SetOrbOut(sv_s)

			sv_s="DEMANDISIC1_DESC"+Format$(sv_i, "%d")+ "|" + CUST.CSEG.DEMAND1_DESC
			print(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)


       			GOTO NEXTREC

      			BANCS.INPUT.RECCOUNT = CINT(BANCS.INPUT.RECCOUNT) + 1
        		PRINT(BANCS.INPUT.RECCOUNT)

        		NEXTREC:
			sv_i = sv_i + 1 
       			sv_b = urhk_dbCursorFetch(CUST.CSEG.CURSOR_NUMBER)
       			print(sv_b)
		#}
		DO
	#}
	ENDIF


ENDOFSCRIPT:
END-->

