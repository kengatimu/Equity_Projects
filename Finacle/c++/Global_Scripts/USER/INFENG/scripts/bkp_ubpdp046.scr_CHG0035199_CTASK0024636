#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: ubpdp046.scr
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START
CHECKSUM="5b21c6e87cc6e0fe8a663f070c46a030bfe74709fe8af29d8af7f3a8caebafc3"
	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp046.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called from HBLRG_ADD_validate_data.scr
	#
	# Called Script         :
	# Calling script        :		ubpdp039.scr
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           16-04-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	#========================================================
	# Check whether CUST Repository exists else create it
	#========================================================
	IF(REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	#========================================================
	# Check whether CBLREG Class exists else create it
	#========================================================
	IF(CLASSEXISTS("CUST","CBLREG") == 0) THEN
	#{
		CREATECLASS ("CUST","CBLREG",5)
	#}
	ENDIF


	sv_k=urhk_SRV_GetVal("biller_id")
	PRINT(sv_k)
	IF(sv_k == 0)THEN
	#{
		CUST.CBLREG.biller_id=BANCS.OUTPARAM.srvValue
		PRINT(CUST.CBLREG.biller_id)
	#}
	ENDIF
	
	
	sv_u=urhk_SRV_GetVal("funcCode")
	PRINT(sv_u)
	IF(sv_u == 0) THEN
	#{
		CUST.CBLREG.funcCode=BANCS.OUTPARAM.srvValue
		PRINT(CUST.CBLREG.funcCode)
		
		IF (CUST.CBLREG.funcCode =="A")  THEN
		#{
			
			sv_u=urhk_SRV_GetVal("biller_id")
			PRINT(sv_u)
			IF(sv_u == 0)THEN
			#{
				CUST.CBLREG.biller_id=BANCS.OUTPARAM.srvValue
				PRINT(CUST.CBLREG.biller_id)
			#}
			ENDIF
			
			BANCS.INPARAM.BINDVARS=CUST.CBLREG.biller_id
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_s = "count|"
			sv_s = sv_s + " SELECT COUNT(*)"
			sv_s = sv_s + " FROM TBAADM.BRGT"
			sv_s = sv_s + " WHERE BILLER_ID =?SVAR"
			sv_s = sv_s + " AND DEL_FLG != 'Y'"
			PRINT(sv_s)
			sv_d =  urhk_dbSelectWithBind(sv_s)
			PRINT(sv_d)
			IF (sv_d == 0 ) THEN
			#{
				CUST.CBLREG.count = BANCS.OUTPARAM.count
				IF(CUST.CBLREG.count>0)THEN
				#{
					CUST.CBLREG.errMessage="Biller Id already Exist."
					sv_u=urhk_SRV_SetErr("biller_id|"+CUST.CBLREG.errMessage)
					PRINT(sv_u)
					sv_u=urhk_SetOrbOut("SuccessOrFailure|N")
					GOTO ENDOFSCRIPT
				#}
				ENDIF
			#}
			ENDIF
			
		#}
		ENDIF
	#}
	ENDIF
	
	
	
	
	
	#========================================================
	# Getting the value for Account Number
	#========================================================
	sv_u=urhk_SRV_GetVal("free_text1")
	PRINT(sv_u)
	IF(sv_u == 0)THEN
	#{
		CUST.CBLREG.acctNum=BANCS.OUTPARAM.srvValue
		PRINT(CUST.CBLREG.acctNum)
		IF(CUST.CBLREG.acctNum!="")THEN
		#{
			
			sv_u=urhk_SRV_GetVal("free_text2")
			PRINT(sv_u)
			IF(sv_u == 0)THEN
			#{
				CUST.CBLREG.bankId=BANCS.OUTPARAM.srvValue
				PRINT(CUST.CBLREG.bankId)
				IF(CUST.CBLREG.bankId=="")THEN
				#{
					CUST.CBLREG.errMessage="Enter a valid Bank ID corresponding to A/c Id entered."
					sv_u=urhk_SRV_SetErr("free_text2|"+CUST.CBLREG.errMessage)
					PRINT(sv_u)
					sv_u=urhk_SetOrbOut("SuccessOrFailure|N")
					GOTO ENDOFSCRIPT
				#}
				ENDIF
			#}
			ENDIF
			
			
			
			BANCS.INPARAM.BINDVARS=CUST.CBLREG.acctNum+"|Y|N"
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_s = "count3|"
			sv_s = sv_s + " SELECT COUNT(3)"
			sv_s = sv_s + " FROM TBAADM.GAM"
			sv_s = sv_s + " WHERE FORACID =?SVAR"
			sv_s = sv_s + " AND ENTITY_CRE_FLG =?SVAR"
			sv_s = sv_s + " AND DEL_FLG =?SVAR"
			PRINT(sv_s)
			sv_d =  urhk_dbSelectWithBind(sv_s)
			PRINT(sv_d)
			IF (sv_d == 0 ) THEN
			#{
				CUST.CBLREG.count3 = BANCS.OUTPARAM.count3
				IF(CUST.CBLREG.count3=="0")THEN
				#{
					CUST.CBLREG.errMessage="Enter a valid account Id"
					sv_u=urhk_SRV_SetErr("free_text1|"+CUST.CBLREG.errMessage)
					PRINT(sv_u)
					sv_u=urhk_SetOrbOut("SuccessOrFailure|N")
					GOTO ENDOFSCRIPT
				#}
				ENDIF
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF

	#========================================================
	# Validating if the account already exist
	#========================================================

	

		IF ((CUST.CBLREG.acctNum!="1002201344977") AND (CUST.CBLREG.acctNum!="1001200956771")) THEN

			BANCS.INPARAM.BINDVARS=CUST.CBLREG.acctNum + "|N|" + CUST.CBLREG.biller_id + "|"+  BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_s = "count,billers|"
			sv_s = sv_s + " SELECT COUNT(biller_id), LISTAGG(biller_id, ',') within group (order by biller_id)   "
			sv_s = sv_s + " FROM TBAADM.brgt"
			sv_s = sv_s + " WHERE free_text1 =?SVAR AND DEL_FLG =?SVAR "
			sv_s = sv_s + " AND BILLER_ID ! = ?SVAR  AND BANK_ID  =?SVAR "
			PRINT(sv_s)
			sv_d =  urhk_dbSelectWithBind(sv_s)
			PRINT(sv_d)
			IF (sv_d == 0 ) THEN
			#{
				CUST.CBLREG.count = BANCS.OUTPARAM.count
				IF (CINT(CUST.CBLREG.count) > 0) THEN
				#{
					CUST.CBLREG.errMessage="Account exists multiple billerid - "
					sv_u=urhk_SRV_SetErr("free_text1|"+CUST.CBLREG.errMessage + BANCS.OUTPARAM.billers)
					PRINT(sv_u)
					sv_u=urhk_SetOrbOut("SuccessOrFailure|N")
					GOTO ENDOFSCRIPT
				#}
				ENDIF
			#}
			ENDIF
		ENDIF
	

	#========================================================
	# Validating the bank ID
	#========================================================
	sv_u=urhk_SRV_GetVal("free_text2")
	PRINT(sv_u)
	IF(sv_u == 0)THEN
	#{
		CUST.CBLREG.bankId=BANCS.OUTPARAM.srvValue
		PRINT(CUST.CBLREG.bankId)
		IF(CUST.CBLREG.bankId!="")THEN
		#{
			
			sv_u=urhk_SRV_GetVal("free_text1")
			PRINT(sv_u)
			IF(sv_u == 0)THEN
			#{
				CUST.CBLREG.acctNum=BANCS.OUTPARAM.srvValue
				PRINT(CUST.CBLREG.acctNum)
				IF(CUST.CBLREG.acctNum=="")THEN
				#{
					CUST.CBLREG.errMessage="Enter a valid A/c Id. corresponding to Bank Id entered."
					sv_u=urhk_SRV_SetErr("free_text1|"+CUST.CBLREG.errMessage)
					PRINT(sv_u)
					sv_u=urhk_SetOrbOut("SuccessOrFailure|N")
					GOTO ENDOFSCRIPT
				#}
				ENDIF
			#}
			ENDIF
			
			BANCS.INPARAM.BINDVARS=CUST.CBLREG.bankId+"|N"
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_s = "count1|"
			sv_s = sv_s + " SELECT COUNT(1)"
			sv_s = sv_s + " FROM TBAADM.GCT"
			sv_s = sv_s + " WHERE BANK_ID =?SVAR"
			sv_s = sv_s + " AND NVL(DEL_FLG, 'N') = ?SVAR"
			PRINT(sv_s)
			sv_d =  urhk_dbSelectWithBind(sv_s)
			PRINT(sv_d)
			IF (sv_d == 0 ) THEN
			#{
				CUST.CBLREG.count1 = BANCS.OUTPARAM.count1
				PRINT(CUST.CBLREG.count1)
				IF(CUST.CBLREG.count1=="0")THEN
				#{
					CUST.CBLREG.errMessage="Invalid Bank ID"
					sv_u=urhk_SRV_SetErr("free_text2|"+CUST.CBLREG.errMessage)
					PRINT(sv_u)
					sv_u=urhk_SetOrbOut("SuccessOrFailure|N")
					GOTO ENDOFSCRIPT
				#}
				ENDIF
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	#========================================================
	# Validating if the A/C ID. entered is corresponding to
	# bank Id entered
	#========================================================
	IF ((FIELDEXISTS(CUST.CBLREG.bankId) == 1) AND (FIELDEXISTS(CUST.CBLREG.acctNum) == 1))THEN
	#{
		IF((CUST.CBLREG.bankId!="") AND (CUST.CBLREG.acctNum!=""))THEN
		#{
			BANCS.INPARAM.BINDVARS=CUST.CBLREG.acctNum+"|"+CUST.CBLREG.bankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_s = "count2|"
			sv_s = sv_s + " SELECT COUNT(2)"
			sv_s = sv_s + " FROM TBAADM.GAM"
			sv_s = sv_s + " WHERE FORACID =?SVAR"
			sv_s = sv_s + " AND BANK_ID =?SVAR"
			PRINT(sv_s)
			sv_d =  urhk_dbSelectWithBind(sv_s)
			PRINT(sv_d)
			IF (sv_d == 0 ) THEN
			#{
				CUST.CBLREG.count2 = BANCS.OUTPARAM.count2
				IF(CUST.CBLREG.count2=="0")THEN
				#{
					CUST.CBLREG.errMessage="A/C ID entered does not belong to the Bank ID entered"
					sv_u=urhk_SRV_SetErr("free_text1|"+CUST.CBLREG.errMessage)
					PRINT(sv_u)
					sv_u=urhk_SetOrbOut("SuccessOrFailure|N")
					GOTO ENDOFSCRIPT
				#}
				ENDIF
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	sv_u=urhk_SRV_GetVal("free_text3")
	PRINT(sv_u)
	IF(sv_u == 0)THEN
	#{
		CUST.CBLREG.chrgEventId=TRIM(TOUPPER(BANCS.OUTPARAM.srvValue))
		PRINT(CUST.CBLREG.chrgEventId)
		IF(CUST.CBLREG.chrgEventId!="")THEN
		#{
			
			BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId+"|"+CUST.CBLREG.chrgEventId+"|N|Y"
			sv_s = "count3|"
			sv_s = sv_s + " SELECT COUNT(3)"
			sv_s = sv_s + " FROM TBAADM.PTT "
			sv_s = sv_s + " WHERE BANK_ID = ?SVAR"
			sv_s = sv_s + " AND EVENT_ID = ?SVAR"
			sv_s = sv_s + " AND NVL(DEL_FLG, 'N') = ?SVAR"
			sv_s = sv_s + " AND NVL(ENTITY_CRE_FLG, 'Y') = ?SVAR"
			sv_d =  urhk_dbSelectWithBind(sv_s)
			PRINT(sv_d)
			IF (sv_d == 0 ) THEN
			#{
				CUST.CBLREG.count3 = BANCS.OUTPARAM.count3
				IF(CUST.CBLREG.count3=="0")THEN
				#{
					CUST.CBLREG.errMessage="Please enter a valid Charge Event ID"
					sv_u=urhk_SRV_SetErr("free_text3|"+CUST.CBLREG.errMessage)
					PRINT(sv_u)
					sv_u=urhk_SetOrbOut("SuccessOrFailure|N")
					GOTO ENDOFSCRIPT
				#}
				ENDIF
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	#========================================================
	# Validating if the val Type entered is corresponding to
	# bank Id entered
	#========================================================
	sv_u=urhk_SRV_GetVal("free_text4")
	PRINT(sv_u)
	IF(sv_u == 0)THEN
	#{
		CUST.CBLREG.valType=TRIM(TOUPPER(BANCS.OUTPARAM.srvValue))
		PRINT(CUST.CBLREG.valType)
		IF(CUST.CBLREG.valType!="")THEN
		#{
			
			BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId+"|"+CUST.CBLREG.valType+"|N|Y"
			sv_s = "count4|"
			sv_s = sv_s + " SELECT COUNT(4)"
			sv_s = sv_s + " FROM CUSTOM.C_CPVALUE "
			sv_s = sv_s + " WHERE BANK_ID = ?SVAR"
			sv_s = sv_s + " AND PARAMETER_ID = ?SVAR"
			sv_s = sv_s + " AND NVL(DEL_FLG, 'N') = ?SVAR"
			sv_s = sv_s + " AND NVL(ENTITY_CRE_FLG, 'Y') = ?SVAR"
			sv_d =  urhk_dbSelectWithBind(sv_s)
			PRINT(sv_d)
			IF (sv_d == 0 ) THEN
			#{
				CUST.CBLREG.count4 = BANCS.OUTPARAM.count4
				IF(CUST.CBLREG.count4=="0")THEN
				#{
					CUST.CBLREG.errMessage="Please enter a valid Validation Type"
					sv_u=urhk_SRV_SetErr("free_text4|"+CUST.CBLREG.errMessage)
					PRINT(sv_u)
					sv_u=urhk_SetOrbOut("SuccessOrFailure|N")
					GOTO ENDOFSCRIPT
				#}
				ENDIF
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	ENDOFSCRIPT:
		#TRACE OFF
		EXITSCRIPT
END-->
