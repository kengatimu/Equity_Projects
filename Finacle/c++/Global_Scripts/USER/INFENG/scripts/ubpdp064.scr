#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: ubpdp064.scr
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START
CHECKSUM="cef398b072cc2ae4339a06de56a3f2f06926f48ff2c14b3ede82bb8ca52de9ce"
	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp064.scr
	# Author                :       Swati Ganjoo
	# Description           :       This scripts gets called from hoccmdo012.scr  for getting the values of biller
	#                               details on crit page on blur of tran id
	#                               values
	# Called Script         :
	# Calling Function      :
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           27-08-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	BANCS.INPARAM.BINDVARS = CUST.HOCCM.zoneCode
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.HOCCM.setNum
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.HOCCM.critCrAcctNum
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.HOCCM.critTranId
	print(BANCS.INPARAM.BINDVARS)
	sv_s = "count1|"
	sv_s = sv_s + " SELECT COUNT(1)"
	sv_s = sv_s + " FROM CUSTOM.C_UBP "
	sv_s = sv_s + " WHERE CLG_ZONE_CODE = ?SVAR "
	#sv_s = sv_s + " AND CLG_ZONE_DATE = TO_DATE(?SVAR,'DD-MM-YYYY') "
	sv_s = sv_s + " AND BILLER_BNK_ID = ?SVAR "
	sv_s = sv_s + " AND SET_NUM = LPAD(?SVAR,6) "
	sv_s = sv_s + " AND BILLER_ACCT_ID = ?SVAR "
	sv_s = sv_s + " AND FREETEXT7 = ?SVAR "
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	IF(sv_d != 0)THEN
	#{
		# error
		sv_w = "SUCCESSORFAILURE|N"
		sv_u = urhk_SetOrbOut(sv_w)
		sv_m = "Set Num is not valid for Zone Code."
		sv_z = "Error|" + sv_m
		sv_u = urhk_SetOrbOut(sv_z)
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		CUST.HOCCM.count1 = BANCS.OUTPARAM.count1
		print(CUST.HOCCM.count1)
	#}
	ENDIF
	IF(CUST.HOCCM.count1>0)THEN
	#{
		BANCS.INPARAM.BINDVARS = CUST.HOCCM.zoneCode
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.HOCCM.setNum
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.HOCCM.critCrAcctNum
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.HOCCM.critTranId
		print(BANCS.INPARAM.BINDVARS)
		sv_s = "billerId,typOfServ,valType|"
		sv_s = sv_s + " SELECT BILLER_ID,TYP_OF_SERV,VAL_TYPE"
		sv_s = sv_s + " FROM CUSTOM.C_UBP "
		sv_s = sv_s + " WHERE CLG_ZONE_CODE = ?SVAR "
		#sv_s = sv_s + " AND CLG_ZONE_DATE = TO_DATE(?SVAR,'DD-MM-YYYY') "
		sv_s = sv_s + " AND BILLER_BNK_ID = ?SVAR "
		sv_s = sv_s + " AND SET_NUM = LPAD(?SVAR,6) "
		sv_s = sv_s + " AND BILLER_ACCT_ID = ?SVAR "
		sv_s = sv_s + " AND FREETEXT7 = ?SVAR "
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		print(sv_d)
		IF(sv_d != 0)THEN
		#{
			# error
			sv_w = "SUCCESSORFAILURE|N"
			sv_u = urhk_SetOrbOut(sv_w)
			sv_m = "Set Num is not valid for Zone Code."
			sv_z = "Error|" + sv_m
			sv_u = urhk_SetOrbOut(sv_z)
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			CUST.HOCCM.billPayYes="Y"
			CUST.HOCCM.billerId = BANCS.OUTPARAM.billerId
			print(CUST.HOCCM.billerId)
			CUST.HOCCM.typOfServ = BANCS.OUTPARAM.typOfServ
			print(CUST.HOCCM.typOfServ)
			CUST.HOCCM.valType = BANCS.OUTPARAM.valType
			print(CUST.HOCCM.valType)
		#}
		ENDIF
	#}
	ENDIF
END-->
