<--start
trace on

sv_g = ""
sv_f = ""
PRINT(sv_i)

sv_j = urhk_B2k_ForacidToAcid(sv_i)

IF(sv_j == 0) THEN
	#{
		sv_g = BANCS.OUTPARAM.Acid
	#}
ENDIF

BANCS.INPARAM.acid = sv_g
PRINT(BANCS.INPARAM.acid)

IF (sv_g != "") THEN
	#{
		sv_n = urhk_getAcctStatus("")
		IF (sv_n == 0) THEN
			#{
				sv_f = BANCS.OUTPARAM.acctStatus
			#}
		ENDIF
	#}
ENDIF

PRINT(sv_f)

IF (sv_f == "D") THEN
	#{
		sv_t = sv_i + ","
		PRINT(sv_t)
	#}
ENDIF

end-->
