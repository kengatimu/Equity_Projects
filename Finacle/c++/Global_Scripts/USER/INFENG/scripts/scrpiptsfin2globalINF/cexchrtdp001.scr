#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cexchrtdp001.scr
# Type: NA|default
# Description: cexchrtdp001.scr
# Author: shivakrishna.d
# Date: 8/27/18 4:35 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="685620e38c445fab0c762ceb8e88798ebe83e7f1fd901f864b51fc9ba7885f00"
	# Script body goes here
	TRACE ON
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CEXCH Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CEXCH")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CEXCH",5)
	#}
	ENDIF
	#==========================================================*
	# Intilializing the Variables
	#==========================================================*
	CUST.CEXCH.count="0"
	CUST.CEXCH.errorFlg = "N"
	CUST.CEXCH.errorMsg = " "
	CUST.CEXCH.roleId=" "
	CUST.CEXCH.roleDesc=" "
	IF(FIELDEXISTS(BANCS.INPUT.roleId))THEN
	#{
		IF(TRIM(BANCS.INPUT.roleId) != "")THEN
		#{
			CUST.CEXCH.roleId = TOUPPER(TRIM(BANCS.INPUT.roleId))
			PRINT(CUST.CEXCH.roleId)
		#}
		ELSE
                #{
                	GOTO ENDOFSCRIPT

		ENDIF
	#}
	ELSE
		#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	sv_s="count|SELECT COUNT(1) FROM TBAADM.RPT WHERE BANK_ID=?SVAR AND ROLE_ID=?SVAR AND ENTITY_CRE_FLG='Y' AND DEL_FLG='N'"
	BANCS.INPARAM.BINDVARS=BANCS.STDIN.contextBankId + "|"+ CUST.CEXCH.roleId
	PRINT(sv_s)
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d != 0)THEN
	#{
		#sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
		#sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
		CUST.CEXCH.errorFlg = "Y"
		CUST.CEXCH.errorMsg = "Error in Fetching Data"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	CUST.CEXCH.count =BANCS.OUTPARAM.count
	IF(CUST.CEXCH.count == "0") THEN
	#{
		#sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
		#sv_t= urhk_SetOrbOut("Invalid Role Id")
		CUST.CEXCH.errorFlg = "Y"
		CUST.CEXCH.errorMsg = "Invalid Role Id"
		GOTO ENDOFSCRIPT
		
	#}
	ENDIF
	IF(CUST.CEXCH.count > "0") THEN
	#{
        sv_s="roleDesc|SELECT ROLE_DESC FROM TBAADM.RPT WHERE BANK_ID=?SVAR AND ROLE_ID=?SVAR AND ENTITY_CRE_FLG='Y' AND DEL_FLG='N'"
        BANCS.INPARAM.BINDVARS=BANCS.STDIN.contextBankId + "|"+ CUST.CEXCH.roleId
        PRINT(sv_s)
        PRINT(BANCS.INPARAM.BINDVARS)
        sv_d = urhk_dbSelectWithBind(sv_s)
        PRINT(sv_d)
        IF(sv_d != 0)THEN
        #{
                #sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
                #sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
                CUST.CEXCH.errorFlg = "Y"
                CUST.CEXCH.errorMsg = "Error in Fetching Data"
                GOTO ENDOFSCRIPT
        #}
        ENDIF
        CUST.CEXCH.roleDesc =BANCS.OUTPARAM.roleDesc
	#}
	ENDIF

	#sv_s=urhk_SetOrbOut("SuccessOrFailure|Y")
	ENDOFSCRIPT:
		sv_s=urhk_SetOrbOut("errorFlg|"+CUST.CEXCH.errorFlg)
		sv_s=urhk_SetOrbOut("errorMsg|"+CUST.CEXCH.errorMsg)
		sv_s=urhk_SetOrbOut("roleId|"+CUST.CEXCH.roleId)
		sv_s=urhk_SetOrbOut("roleDesc|"+CUST.CEXCH.roleDesc)
		
		TRACE OFF
END-->
