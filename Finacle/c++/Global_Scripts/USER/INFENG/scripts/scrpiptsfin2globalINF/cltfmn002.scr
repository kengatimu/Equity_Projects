#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cltfmn002.scr
# Type: NA|default
# Description: script on clicking of submit in CLTFM menu
# Author: haneesha.kanumuru
# Date: 2/7/18 12:26 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="b849e649b882216c26134915153112f054f1ebcc6cead6d4441e11a87239625c"
	
	# Script body goes here
	TRACE ON
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CLTFM Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CLTFM")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CLTFM",5)
	#}
	ENDIF
	CUST.CLTFM.cifId =""
	CUST.CLTFM.schmCode = ""
	CUST.CLTFM.tax=""
	CUST.CLTFM.acctId =""
	#==========================================================*
	# Intilializing the Variables
	#==========================================================*
	
	IF(FIELDEXISTS(BANCS.INPUT.funcCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.funcCode) != "")THEN
		#{
			CUST.CLTFM.funcCode = TOUPPER(TRIM(BANCS.INPUT.funcCode))
			PRINT(CUST.CLTFM.funcCode)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.schmCode)) THEN
	#{
		IF(TRIM(BANCS.INPUT.schmCode) != "")THEN
		#{
			CUST.CLTFM.schmCode= TOUPPER(TRIM(BANCS.INPUT.schmCode))
			PRINT(CUST.CLTFM.schmCode)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.type)) THEN
	#{
		IF(TRIM(BANCS.INPUT.type) != "")THEN
		#{
			CUST.CLTFM.type= TOUPPER(TRIM(BANCS.INPUT.type))
			PRINT(CUST.CLTFM.type)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.cifId)) THEN
	#{
		IF(TRIM(BANCS.INPUT.cifId) != "")THEN
		#{
			CUST.CLTFM.cifId= TOUPPER(TRIM(BANCS.INPUT.cifId))
			PRINT(CUST.CLTFM.cifId)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.acctId)) THEN
	#{
		IF(TRIM(BANCS.INPUT.acctId) != "")THEN
		#{
			CUST.CLTFM.acctId= TOUPPER(TRIM(BANCS.INPUT.acctId))
			PRINT(CUST.CLTFM.acctId)
		#}
		ENDIF
	#}
	ENDIF
	
	
	IF(FIELDEXISTS(BANCS.INPUT.tax)) THEN
	#{
		IF(TRIM(BANCS.INPUT.tax) != "")THEN
		#{
			CUST.CLTFM.tax= TOUPPER(TRIM(BANCS.INPUT.tax))
			PRINT(CUST.CLTFM.tax)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.percentage)) THEN
	#{
		IF(TRIM(BANCS.INPUT.percentage) != "")THEN
		#{
			CUST.CLTFM.percentage= TOUPPER(TRIM(BANCS.INPUT.percentage))
			PRINT(CUST.CLTFM.percentage)
			#}
		ELSE
			#{
			CUST.CLTFM.percentage = "0"
			PRINT(CUST.CLTFM.percentage)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(BANCS.INPUT.percentage =="") THEN
	#{
		CUST.CLTFM.percentage = "0"
		PRINT(CUST.CLTFM.percentage)
	#}
	ENDIF
	
	#	IF(BANCS.INPUT.tax =="ON") THEN
	#	#{
	#		CUST.CLTFM.tax = 'Y'
	#		#}
	#	ELSE
	#		#{
	#		CUST.CLTFM.tax ='N'
	#	#}
	#	ENDIF
	#----------------------------------------------------------------------------
	# FUNCTION ADDITION
	#----------------------------------------------------------------------------
	IF (CUST.CLTFM.funcCode == "A") THEN
	#{
		IF(CUST.CLTFM.cifId != "")THEN
		#{
			BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode != "") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
		#}
		ENDIF
		IF(CUST.CLTFM.acctId !="") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
			
			
		#}
		ENDIF
		
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS  + "|" + CUST.CLTFM.tax + "|" + CUST.CLTFM.type + "|" +BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.userId + "|" + BANCS.STDIN.userId +"|" + CUST.CLTFM.percentage
		
		
		
		PRINT(BANCS.INPARAM.BINDVARS)
		IF(CUST.CLTFM.cifId != "")THEN
		#{
			
			sv_s = "insert into custom.cltfm(SCHEME_CODE,CIF_ID,ACCOUNT_NUMBER,TAX_APPLICABLE,TYPE_LEVEL,BANK_ID,ENTITY_CRE_FLAG,DEL_FLG,RCRE_TIME,RCRE_USER_ID,LCHG_TIME,LCHG_USER_ID,TAX_PERCENTAGE)"
			sv_s = sv_s + " values('NA',?SVAR,'NA',?SVAR,?SVAR,?SVAR,'N','N',sysdate,?SVAR,sysdate,?SVAR,?SVAR)"
			
			
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode !="") THEN
		#{
			
			sv_s = "insert into custom.cltfm(SCHEME_CODE,CIF_ID,ACCOUNT_NUMBER,TAX_APPLICABLE,TYPE_LEVEL,BANK_ID,ENTITY_CRE_FLAG,DEL_FLG,RCRE_TIME,RCRE_USER_ID,LCHG_TIME,LCHG_USER_ID,TAX_PERCENTAGE)"
			sv_s = sv_s + " values(?SVAR,'NA','NA',?SVAR,?SVAR,?SVAR,'N','N',sysdate,?SVAR,sysdate,?SVAR,?SVAR)"
			
		#}
		ENDIF
		IF(CUST.CLTFM.acctId !="") THEN
		#{
			sv_s = "insert into custom.cltfm(SCHEME_CODE,CIF_ID,ACCOUNT_NUMBER,TAX_APPLICABLE,TYPE_LEVEL,BANK_ID,ENTITY_CRE_FLAG,DEL_FLG,RCRE_TIME,RCRE_USER_ID,LCHG_TIME,LCHG_USER_ID,TAX_PERCENTAGE)"
			sv_s = sv_s + " values('NA','NA',?SVAR,?SVAR,?SVAR,?SVAR,'N','N',sysdate,?SVAR,sysdate,?SVAR,?SVAR)"
			
			
		#}
		ENDIF
		
		PRINT(sv_s)
		
		sv_d = urhk_dbSqlWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d != 0)THEN
		#{
			#ERROR
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbOut("Error|Database Insertion failed")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		CUST.CLTFM.resultMsg = "Record has been Added successfully"
		sv_u = urhk_SetOrbOut("RESULT_MSG|"+CUST.CLTFM.resultMsg)
		PRINT(sv_u)
	#}
	ENDIF
	
	#----------------------------------------------------------------------------
	# FUNCTION MODIFY
	#----------------------------------------------------------------------------
	IF (CUST.CLTFM.funcCode == "M") THEN
	#{
		IF(CUST.CLTFM.cifId != "")THEN
		#{
			BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode != "") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
		#}
		ENDIF
		IF(CUST.CLTFM.acctId !="") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
			
			
		#}
		ENDIF
		BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" + BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
		
		PRINT(BANCS.INPARAM.BINDVARS)
		IF(CUST.CLTFM.cifId != "") THEN
		#{
			sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
			sv_s = sv_s + " SELECT 1 FROM CUSTOM.CLTFM_MOD"
			sv_s = sv_s + " WHERE"
			sv_s = sv_s + " TYPE_LEVEL = ?SVAR  AND CIF_ID =?SVAR AND BANK_ID = ?SVAR)"
			
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode !="") THEN
		#{
			sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
			sv_s = sv_s + " SELECT 1 FROM CUSTOM.CLTFM_MOD"
			sv_s = sv_s + " WHERE"
			sv_s = sv_s + " TYPE_LEVEL = ?SVAR AND  SCHEME_CODE=?SVAR AND BANK_ID = ?SVAR)"
		#}
		ENDIF
		
		IF(CUST.CLTFM.acctId != "") THEN
		#{
			sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
			sv_s = sv_s + " SELECT 1 FROM CUSTOM.CLTFM_MOD"
			sv_s = sv_s + " WHERE"
			sv_s = sv_s + " TYPE_LEVEL = ?SVAR AND  ACCOUNT_NUMBER=?SVAR AND BANK_ID = ?SVAR)"
		#}
		ENDIF
		
		PRINT(sv_s)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d == 0) THEN
		#{
			IF(CUST.CLTFM.cifId != "")THEN
			#{
				BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
			#}
			ENDIF
			IF(CUST.CLTFM.schmCode != "") THEN
			#{
				BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
			#}
			ENDIF
			IF(CUST.CLTFM.acctId !="") THEN
			#{
				BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
				
				
			#}
			ENDIF
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.tax + "|" + CUST.CLTFM.percentage + "|" + BANCS.STDIN.userId + "|" + CUST.CLTFM.type + "|" + BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
			
			PRINT(BANCS.INPARAM.BINDVARS)
			IF(CUST.CLTFM.cifId != "") THEN
			#{
				sv_s = "update custom.cltfm_mod set"
				sv_s = sv_s + " TAX_APPLICABLE = ?SVAR,"
				sv_s = sv_s + " TAX_PERCENTAGE = ?SVAR,"
				sv_s = sv_s + " LCHG_USER_ID=?SVAR,"
				sv_s = sv_s + " LCHG_TIME = sysdate,"
				sv_s = sv_s + " ENTITY_CRE_FLAG = 'N'"
				sv_s = sv_s + " where TYPE_LEVEL=?SVAR  AND CIF_ID= ?SVAR AND DEL_FLG='N' AND BANK_ID=?SVAR"
				
			#}
			ENDIF
			IF(CUST.CLTFM.schmCode !="") THEN
			#{
				sv_s = "update custom.cltfm_mod set"
				sv_s = sv_s + " TAX_APPLICABLE = ?SVAR,"
				sv_s = sv_s + " TAX_PERCENTAGE = ?SVAR,"
				sv_s = sv_s + " LCHG_USER_ID=?SVAR,"
				sv_s = sv_s + " LCHG_TIME = sysdate,"
				sv_s = sv_s + " ENTITY_CRE_FLAG = 'N'"
				sv_s = sv_s + " where TYPE_LEVEL=?SVAR AND SCHEME_CODE=?SVAR  AND DEL_FLG='N' AND BANK_ID=?SVAR"
				
			#}
			ENDIF
			IF(CUST.CLTFM.acctId != "") THEN
			#{
				sv_s = "update custom.cltfm_mod set"
				sv_s = sv_s + " TAX_APPLICABLE = ?SVAR,"
				sv_s = sv_s + " TAX_PERCENTAGE = ?SVAR,"
				sv_s = sv_s + " LCHG_USER_ID=?SVAR,"
				sv_s = sv_s + " LCHG_TIME = sysdate,"
				sv_s = sv_s + " ENTITY_CRE_FLAG = 'N'"
				sv_s = sv_s + " where TYPE_LEVEL=?SVAR AND ACCOUNT_NUMBER=?SVAR  AND DEL_FLG='N' AND BANK_ID=?SVAR"
				
			#}
			ENDIF
			PRINT(sv_s)
			
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0)THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error_1|Error^Database Updation Failed^settlccy")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			#}
		ELSE
			#{
			IF(CUST.CLTFM.cifId != "")THEN
			#{
				BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
			#}
			ENDIF
			IF(CUST.CLTFM.schmCode != "") THEN
			#{
				BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
			#}
			ENDIF
			IF(CUST.CLTFM.acctId !="") THEN
			#{
				BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
				
				
			#}
			ENDIF
			BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS  + "|" + CUST.CLTFM.tax + "|" + CUST.CLTFM.type + "|" +BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.userId + "|" + BANCS.STDIN.userId + "|" + CUST.CLTFM.percentage
			
			PRINT(BANCS.INPARAM.BINDVARS)
			IF(CUST.CLTFM.cifId != "")THEN
			#{
				
				sv_s = "insert into custom.cltfm_mod(SCHEME_CODE,CIF_ID,ACCOUNT_NUMBER,TAX_APPLICABLE,TYPE_LEVEL,BANK_ID,ENTITY_CRE_FLAG,DEL_FLG,RCRE_TIME,RCRE_USER_ID,LCHG_TIME,LCHG_USER_ID,TAX_PERCENTAGE)"
				sv_s = sv_s + " values('NA',?SVAR,'NA',?SVAR,?SVAR,?SVAR,'N','N',sysdate,?SVAR,sysdate,?SVAR,?SVAR)"
				
				
			#}
			ENDIF
			IF(CUST.CLTFM.schmCode !="") THEN
			#{
				
				sv_s = "insert into custom.cltfm_mod(SCHEME_CODE,CIF_ID,ACCOUNT_NUMBER,TAX_APPLICABLE,TYPE_LEVEL,BANK_ID,ENTITY_CRE_FLAG,DEL_FLG,RCRE_TIME,RCRE_USER_ID,LCHG_TIME,LCHG_USER_ID,TAX_PERCENTAGE)"
				sv_s = sv_s + " values(?SVAR,'NA','NA',?SVAR,?SVAR,?SVAR,'N','N',sysdate,?SVAR,sysdate,?SVAR,?SVAR)"
				
			#}
			ENDIF
			IF(CUST.CLTFM.acctId !="") THEN
			#{
				sv_s = "insert into custom.cltfm_mod(SCHEME_CODE,CIF_ID,ACCOUNT_NUMBER,TAX_APPLICABLE,TYPE_LEVEL,BANK_ID,ENTITY_CRE_FLAG,DEL_FLG,RCRE_TIME,RCRE_USER_ID,LCHG_TIME,LCHG_USER_ID,TAX_PERCENTAGE)"
				sv_s = sv_s + " values('NA','NA',?SVAR,?SVAR,?SVAR,?SVAR,'N','N',sysdate,?SVAR,sysdate,?SVAR,?SVAR)"
				
				
			#}
			ENDIF
			
			PRINT(sv_s)
			
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			IF(sv_d != 0)THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database Insertion failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		CUST.CLTFM.resultMsg = "Record has been Modified successfully"
		sv_u = urhk_SetOrbOut("RESULT_MSG|"+CUST.CLTFM.resultMsg)
		PRINT(sv_u)
	#}
	ENDIF
	
	#----------------------------------------------------------------------------
	# FUNCTION MODIFY
	#----------------------------------------------------------------------------
	
	IF(CUST.CLTFM.funcCode == "V") THEN
	#{
		IF(CUST.CLTFM.cifId != "")THEN
		#{
			BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode != "") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
		#}
		ENDIF
		IF(CUST.CLTFM.acctId !="") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
			
			
		#}
		ENDIF
		BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" + BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
		
		PRINT(BANCS.INPARAM.BINDVARS)
		IF(CUST.CLTFM.cifId != "") THEN
		#{
			sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
			sv_s = sv_s + " SELECT 1 FROM CUSTOM.CLTFM_MOD"
			sv_s = sv_s + " WHERE"
			sv_s = sv_s + " TYPE_LEVEL = ?SVAR  AND CIF_ID =?SVAR AND BANK_ID = ?SVAR)"
			
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode !="") THEN
		#{
			sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
			sv_s = sv_s + " SELECT 1 FROM CUSTOM.CLTFM_MOD"
			sv_s = sv_s + " WHERE"
			sv_s = sv_s + " TYPE_LEVEL = ?SVAR AND  SCHEME_CODE=?SVAR AND BANK_ID = ?SVAR)"
		#}
		ENDIF
		
		IF(CUST.CLTFM.acctId != "") THEN
		#{
			sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
			sv_s = sv_s + " SELECT 1 FROM CUSTOM.CLTFM_MOD"
			sv_s = sv_s + " WHERE"
			sv_s = sv_s + " TYPE_LEVEL = ?SVAR AND  ACCOUNT_NUMBER=?SVAR AND BANK_ID = ?SVAR)"
		#}
		ENDIF
		PRINT(sv_s)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d == 0) THEN
		#{
			IF(CUST.CLTFM.cifId != "")THEN
			#{
				BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
			#}
			ENDIF
			IF(CUST.CLTFM.schmCode != "") THEN
			#{
				BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
			#}
			ENDIF
			IF(CUST.CLTFM.acctId !="") THEN
			#{
				BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
				
				
			#}
			ENDIF
			BANCS.INPARAM.BINDVARS =  BANCS.STDIN.userId + "|" + CUST.CLTFM.type + "|" + BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
			
			PRINT(BANCS.INPARAM.BINDVARS)
			IF(CUST.CLTFM.cifId != "")THEN
			#{
				sv_s = "update custom.cltfm_mod set"
				sv_s = sv_s + " ENTITY_CRE_FLAG ='Y',"
				#sv_s = sv_s + " TAX_APPLICABLE = ?SVAR,"
				sv_s = sv_s + " LCHG_USER_ID=?SVAR,"
				sv_s = sv_s + " LCHG_TIME = sysdate"
				#sv_s = sv_s + " ENTITY_CRE_FLAG = 'N'"
				sv_s = sv_s + " where TYPE_LEVEL=?SVAR  AND CIF_ID= ?SVAR  AND BANK_ID=?SVAR"
			#}
			ENDIF
			IF(CUST.CLTFM.schmCode !="") THEN
			#{
				sv_s = "update custom.cltfm_mod set"
				sv_s = sv_s + " ENTITY_CRE_FLAG ='Y',"
				#sv_s = sv_s + " TAX_APPLICABLE = ?SVAR,"
				sv_s = sv_s + " LCHG_USER_ID=?SVAR,"
				sv_s = sv_s + " LCHG_TIME = sysdate"
				#sv_s = sv_s + " ENTITY_CRE_FLAG = 'N'"
				sv_s = sv_s + " where TYPE_LEVEL=?SVAR AND SCHEME_CODE=?SVAR  AND BANK_ID=?SVAR"
			#}
			ENDIF
			IF(CUST.CLTFM.acctId != "") THEN
			#{
				sv_s = "update custom.cltfm_mod set"
				sv_s = sv_s + " ENTITY_CRE_FLAG ='Y',"
				#sv_s = sv_s + " TAX_APPLICABLE = ?SVAR,"
				sv_s = sv_s + " LCHG_USER_ID=?SVAR,"
				sv_s = sv_s + " LCHG_TIME = sysdate"
				#sv_s = sv_s + " ENTITY_CRE_FLAG = 'N'"
				sv_s = sv_s + " where TYPE_LEVEL=?SVAR AND ACCOUNT_NUMBER=?SVAR  AND BANK_ID=?SVAR"
				
			#}
			ENDIF
			
			PRINT(sv_s)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database update failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			
			IF(CUST.CLTFM.cifId != "")THEN
			#{
				BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
			#}
			ENDIF
			IF(CUST.CLTFM.schmCode != "") THEN
			#{
				BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
			#}
			ENDIF
			IF(CUST.CLTFM.acctId !="") THEN
			#{
				BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
				
				
			#}
			ENDIF
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" + BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
			
			PRINT(BANCS.INPARAM.BINDVARS)
			IF(CUST.CLTFM.cifId != "")THEN
			#{
				sv_s = "delete from custom.cltfm where TYPE_LEVEL=?SVAR  AND CIF_ID= ?SVAR  AND BANK_ID=?SVAR"
				
			#}
			ENDIF
			IF(CUST.CLTFM.schmCode !="") THEN
			#{
				sv_s = "delete from custom.cltfm where TYPE_LEVEL=?SVAR AND SCHEME_CODE=?SVAR  AND BANK_ID=?SVAR"
			#}
			ENDIF
			IF(CUST.CLTFM.acctId !="") THEN
			#{
				sv_s = "delete from custom.cltfm where TYPE_LEVEL=?SVAR AND ACCOUNT_NUMBER=?SVAR  AND BANK_ID=?SVAR"
				
			#}
			ENDIF
			PRINT(sv_s)
			
			sv_d = urhk_dbSqlWithBind(sv_s)
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database delete failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			IF(CUST.CLTFM.cifId != "")THEN
			#{
				BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
			#}
			ENDIF
			IF(CUST.CLTFM.schmCode != "") THEN
			#{
				BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
			#}
			ENDIF
			IF(CUST.CLTFM.acctId !="") THEN
			#{
				BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
				
				
			#}
			ENDIF
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" + BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
			
			PRINT(BANCS.INPARAM.BINDVARS)
			IF(CUST.CLTFM.cifId != "")THEN
			#{
				sv_s = "insert into custom.cltfm select * from custom.cltfm_mod where TYPE_LEVEL=?SVAR AND CIF_ID=?SVAR  AND BANK_ID=?SVAR"
			#}
			ENDIF
			IF(CUST.CLTFM.schmCode !="") THEN
			#{
				sv_s = "insert into custom.cltfm select * from custom.cltfm_mod where TYPE_LEVEL=?SVAR AND SCHEME_CODE=?SVAR  AND BANK_ID=?SVAR"
				
			#}
			ENDIF
			IF(CUST.CLTFM.acctId !="") THEN
			#{
				sv_s = "insert into custom.cltfm select * from custom.cltfm_mod where TYPE_LEVEL=?SVAR AND ACCOUNT_NUMBER=?SVAR  AND BANK_ID=?SVAR"
				
			#}
			ENDIF
			PRINT(sv_s)
			sv_d = urhk_dbSqlWithBind(sv_s)
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database insertion failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			IF(CUST.CLTFM.cifId != "")THEN
			#{
				BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
			#}
			ENDIF
			IF(CUST.CLTFM.schmCode != "") THEN
			#{
				BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
			#}
			ENDIF
			IF(CUST.CLTFM.acctId !="") THEN
			#{
				BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
				
				
			#}
			ENDIF
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" + BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
			
			PRINT(BANCS.INPARAM.BINDVARS)
			IF(CUST.CLTFM.cifId != "")THEN
			#{
				sv_s = "delete from custom.cltfm_mod where TYPE_LEVEL=?SVAR  AND CIF_ID= ?SVAR  AND BANK_ID=?SVAR"
				
			#}
			ENDIF
			IF(CUST.CLTFM.schmCode !="") THEN
			#{
				sv_s = "delete from custom.cltfm_mod where TYPE_LEVEL=?SVAR AND SCHEME_CODE=?SVAR  AND BANK_ID=?SVAR"
			#}
			ENDIF
			IF(CUST.CLTFM.acctId !="") THEN
			#{
				sv_s = "delete from custom.cltfm_mod where TYPE_LEVEL=?SVAR AND ACCOUNT_NUMBER=?SVAR  AND BANK_ID=?SVAR"
				
			#}
			ENDIF
			PRINT(sv_s)
			
			sv_d = urhk_dbSqlWithBind(sv_s)
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database delete failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			
			#}
		ELSE
			#{
			IF(CUST.CLTFM.cifId != "")THEN
			#{
				BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
			#}
			ENDIF
			IF(CUST.CLTFM.schmCode != "") THEN
			#{
				BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
			#}
			ENDIF
			IF(CUST.CLTFM.acctId !="") THEN
			#{
				BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
				
				
			#}
			ENDIF
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" + BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
			
			PRINT(BANCS.INPARAM.BINDVARS)
			IF(CUST.CLTFM.cifId != "")THEN
			#{
				sv_s = "update custom.cltfm set ENTITY_CRE_FLAG='Y' where TYPE_LEVEL=?SVAR  AND CIF_ID= ?SVAR  AND BANK_ID=?SVAR"
			#}
			ENDIF
			IF(CUST.CLTFM.schmCode !="") THEN
			#{
				sv_s = "update custom.cltfm set ENTITY_CRE_FLAG='Y' where TYPE_LEVEL=?SVAR AND SCHEME_CODE=?SVAR  AND BANK_ID=?SVAR"
			#}
			ENDIF
			IF(CUST.CLTFM.acctId !="") THEN
			#{
				sv_s = "update custom.cltfm set ENTITY_CRE_FLAG='Y' where TYPE_LEVEL=?SVAR AND ACCOUNT_NUMBER=?SVAR  AND BANK_ID=?SVAR"
				
			#}
			ENDIF
			PRINT(sv_s)
			sv_d = urhk_dbSqlWithBind(sv_s)
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database update failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			
		#}
		ENDIF
		
		CUST.CLTFM.resultMsg = "Record has been Verified successfully"
		sv_u = urhk_SetOrbOut("RESULT_MSG|"+CUST.CLTFM.resultMsg)
		PRINT(sv_u)
	#}
	ENDIF
	#----------------------------------------------------------------------------
	# FUNCTION Delete
	#----------------------------------------------------------------------------
	IF(CUST.CLTFM.funcCode == "X") THEN
	#{
		IF(CUST.CLTFM.cifId != "")THEN
		#{
			BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode != "") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
		#}
		ENDIF
		IF(CUST.CLTFM.acctId !="") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
			
			
		#}
		ENDIF
		BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" + BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
		
		PRINT(BANCS.INPARAM.BINDVARS)
		IF(CUST.CLTFM.cifId != "") THEN
		#{
			sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
			sv_s = sv_s + " SELECT 1 FROM CUSTOM.CLTFM_MOD"
			sv_s = sv_s + " WHERE"
			sv_s = sv_s + " TYPE_LEVEL = ?SVAR  AND CIF_ID =?SVAR AND BANK_ID = ?SVAR)"
			
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode !="") THEN
		#{
			sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
			sv_s = sv_s + " SELECT 1 FROM CUSTOM.CLTFM_MOD"
			sv_s = sv_s + " WHERE"
			sv_s = sv_s + " TYPE_LEVEL = ?SVAR AND  SCHEME_CODE=?SVAR AND BANK_ID = ?SVAR)"
		#}
		ENDIF
		
		IF(CUST.CLTFM.acctId != "") THEN
		#{
			sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
			sv_s = sv_s + " SELECT 1 FROM CUSTOM.CLTFM_MOD"
			sv_s = sv_s + " WHERE"
			sv_s = sv_s + " TYPE_LEVEL = ?SVAR AND  ACCOUNT_NUMBER=?SVAR AND BANK_ID = ?SVAR)"
		#}
		ENDIF
		PRINT(sv_s)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d == 0) THEN
		#{
			IF(CUST.CLTFM.cifId != "")THEN
			#{
				BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
			#}
			ENDIF
			IF(CUST.CLTFM.schmCode != "") THEN
			#{
				BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
			#}
			ENDIF
			IF(CUST.CLTFM.acctId !="") THEN
			#{
				BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
				
				
			#}
			ENDIF
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" + BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
			
			PRINT(BANCS.INPARAM.BINDVARS)
			IF(CUST.CLTFM.cifId != "") THEN
			#{
				sv_s = "delete from custom.cltfm_mod where TYPE_LEVEL=?SVAR  AND CIF_ID= ?SVAR  AND BANK_ID=?SVAR"
			#}
			ENDIF
			IF(CUST.CLTFM.schmCode !="") THEN
			#{
				sv_s = "delete from custom.cltfm_mod where TYPE_LEVEL=?SVAR AND SCHEME_CODE=?SVAR  AND BANK_ID=?SVAR"
				
			#}
			ENDIF
			IF(CUST.CLTFM.acctId!="") THEN
			#{
				sv_s = "delete from custom.cltfm_mod where TYPE_LEVEL=?SVAR AND ACCOUNT_NUMBER=?SVAR  AND BANK_ID=?SVAR"
				
			#}
			ENDIF
			PRINT(sv_s)
			
			sv_d = urhk_dbSqlWithBind(sv_s)
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database delete failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			#}
		ELSE
			#{
			IF(CUST.CLTFM.cifId != "")THEN
			#{
				BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
			#}
			ENDIF
			IF(CUST.CLTFM.schmCode != "") THEN
			#{
				BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
			#}
			ENDIF
			IF(CUST.CLTFM.acctId !="") THEN
			#{
				BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
				
				
			#}
			ENDIF
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" + BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
			
			PRINT(BANCS.INPARAM.BINDVARS)
			IF(CUST.CLTFM.cifId != "") THEN
			#{
				sv_s = "delete from custom.cltfm where TYPE_LEVEL=?SVAR  AND CIF_ID= ?SVAR  AND BANK_ID=?SVAR AND ENTITY_CRE_FLAG='N'"
			#}
			ENDIF
			IF(CUST.CLTFM.schmCode!="") THEN
			#{
				sv_s = "delete from custom.cltfm where TYPE_LEVEL=?SVAR AND SCHEME_CODE=?SVAR   AND BANK_ID=?SVAR AND ENTITY_CRE_FLAG='N'"
				
			#}
			ENDIF
			IF(CUST.CLTFM.acctId!="") THEN
			#{
				sv_s = "delete from custom.cltfm where TYPE_LEVEL=?SVAR AND ACCOUNT_NUMBER=?SVAR   AND BANK_ID=?SVAR AND ENTITY_CRE_FLAG='N'"
				
			#}
			ENDIF
			PRINT(sv_s)
			
			sv_d = urhk_dbSqlWithBind(sv_s)
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database delete failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		CUST.CLTFM.resultMsg = "Record has been cancelled successfully"
		sv_u = urhk_SetOrbOut("RESULT_MSG|"+CUST.CLTFM.resultMsg)
		PRINT(sv_u)
	#}
	ENDIF
	
	sv_z = urhk_SetOrbOut("SuccessOrFailure|Y")
	PRINT(sv_z)
	ENDOFSCRIPT:
END-->
