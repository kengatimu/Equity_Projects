#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cltfmdp002.scr
# Type: NA|default
# Description: validation script for schemecode in CLTFM menu
# Author: haneesha.kanumuru
# Date: 2/7/18 4:50 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="daaf59b063b0d0652723c1b9a4a5461038bacd7e2f635d8b2fb064e514e37406"
	# Script body goes here
	TRACE ON
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CLTFM Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CLTFM")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CLTFM",5)
	#}
	ENDIF
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	CUST.CLTFM.schmCode =""
	CUST.CLTFM.errorFlg ="N"
	CUST.CLTFM.errorMsg = " "
	CUST.CLTFM.count = "0"
	IF(FIELDEXISTS(BANCS.INPUT.schmCode)) THEN
	#{
		IF(TRIM(BANCS.INPUT.schmCode) != "")THEN
		#{
			CUST.CLTFM.schmCode= TOUPPER(TRIM(BANCS.INPUT.schmCode))
			PRINT(CUST.CLTFM.schmCode)
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.CLTFM.errorFlg = "Y"
		CUST.CLTFM.errorMsg = "Invalid Input for fnExecuteScript"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_s = "count|"
	sv_s = sv_s + " SELECT COUNT(*)"
	sv_s = sv_s + " FROM tbaadm.gsp "
	sv_s = sv_s + " WHERE SCHM_CODE = ?SVAR"
	sv_s = sv_s + " AND DEL_FLG != 'Y'"
	#sv_s = sv_s + " AND entity_cre_flg !='N'"
	sv_s = sv_s + " AND schm_type='LAA'"
	sv_s = sv_s + " AND bank_id=?SVAR"
	
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode + "|" +BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	CUST.CLTFM.count = BANCS.OUTPARAM.count
	PRINT(CUST.CLTFM.count)
	
	IF( CUST.CLTFM.count == "0" )THEN
	#{
		
		CUST.CLTFM.errorFlg = "Y"
		CUST.CLTFM.errorMsg = "Invalid Scheme Code"
	#}
	ENDIF
	
	ENDOFSCRIPT:
		sv_u = urhk_SetOrbOut("errorFlg|"+CUST.CLTFM.errorFlg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("errorMsg|"+CUST.CLTFM.errorMsg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("schmCode|"+CUST.CLTFM.schmCode)
		
		PRINT(sv_u)
END-->
