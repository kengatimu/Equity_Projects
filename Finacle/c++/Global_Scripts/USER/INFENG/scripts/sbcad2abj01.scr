<--START
TRACE ON

sv_a=urhk_B2k_PrintRepos("BANCS")
print(sv_a)

IF(REPEXISTS("CUST") == 0 ) THEN
#{
    CREATEREP("CUST")
#}
ENDIF
IF (CLASSEXISTS("CUST","DATA") == 0) THEN
#{
    CREATECLASS("CUST","DATA",5)
#}
ENDIF

lv_q = "cifId|select  cif_id  from tbaadm.gam where BANK_ID = ?SVAR AND MODE_OF_OPER_CODE in ('ETS','SELF') "
lv_q = lv_q + "and acid in (select acid from tbaadm.smt where bank_id =?SVAR and acct_status ='D') and sol_id =?SVAR "
lv_q = lv_q + "and schm_type in ('SBA','CAA') "
print(lv_q)

BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.mySolId
PRINT(BANCS.INPARAM.BINDVARS)

sv_b = urhk_dbCursorOpenWithBind(lv_q)
print(sv_b)

if(sv_b == 0)then
#{
        sv_d = BANCS.OUTPARAM.DB_CURSOR_NUMBER
        print(sv_d)
        sv_e = urhk_dbCursorFetch(sv_d)
        print(sv_e)

        WHILE(sv_e == 0)
        #{
		
		CUST.DATA.cifId = BANCS.OUTPARAM.cifId
                PRINT(CUST.DATA.cifId)
##################		

lv_a = "cifId,countCif|select unique(cif_id),count(cif_id) from tbaadm.gam where BANK_ID = ?SVAR AND MODE_OF_OPER_CODE in ('ETS','SELF') "
lv_a = lv_a + "and schm_type in ('SBA','CAA') and cif_id =?SVAR group by  cif_id "
print(lv_a)

BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId +"|"+CUST.DATA.cifId
PRINT(BANCS.INPARAM.BINDVARS)

sv_c = urhk_dbCursorOpenWithBind(lv_a)
print(sv_c)

if(sv_c == 0)then
#{
        sv_n = BANCS.OUTPARAM.DB_CURSOR_NUMBER
        print(sv_n)
        sv_f = urhk_dbCursorFetch(sv_n)
        print(sv_f)

        WHILE(sv_f == 0)
        #{
                CUST.DATA.cifId = BANCS.OUTPARAM.cifId
                PRINT(CUST.DATA.cifId)
				CUST.DATA.countCif = BANCS.OUTPARAM.countCif
				PRINT(CUST.DATA.countCif)
			if(CUST.DATA.countCif >1)then
			#{
                lv_q = "foracid|select foracid from tbaadm.gam where cif_id = ?SVAR and BANK_ID = ?SVAR and schm_type in ('SBA','CAA') AND SOL_ID = ?SVAR "
				lv_q = lv_q+"and cif_id in(select cif_id from tbaadm.gam where( "
				lv_q = lv_q+"  acid in(select acid from tbaadm.smt where bank_id =?SVAR and acct_status ='A')))"
                print(lv_q)

                BANCS.INPARAM.BINDVARS = CUST.DATA.cifId +"|"+ BANCS.STDIN.contextBankId +"|"+ BANCS.STDIN.mySolId +"|"+ BANCS.STDIN.contextBankId
                PRINT(BANCS.INPARAM.BINDVARS)

                sv_c = urhk_dbCursorOpenWithBind(lv_q)
                print(sv_c)

                if(sv_c == 0)then
                #{
                        lv_b = BANCS.OUTPARAM.DB_CURSOR_NUMBER
                        print(lv_b)
                        sv_f = urhk_dbCursorFetch(lv_b)
                        print(sv_f)

                        WHILE(sv_f == 0)
                        #{
                                CUST.DATA.foracid = BANCS.OUTPARAM.foracid
                                PRINT(CUST.DATA.foracid)

								if(CUST.DATA.foracid != "")THEN
								#{
									CALLSCRIPTIFEXIST("sbcad2abj02.scr")
								#}
								ENDIF
                                sv_f = urhk_dbCursorFetch(lv_b)
                        #}
                        DO
                        sv_c = urhk_dbCursorClose(lv_b)
                #}
                ENDIF
			#}
			ELSE
			#{
			lv_q = ""
			lv_q = lv_q+"foracid|select foracid from tbaadm.gam where bank_id =?SVAR and acid in( "
			lv_q = lv_q+"select acid from tbaadm.aas where bank_id =?SVAR   "
			lv_q = lv_q+"and NMA_KEY_ID in(select cif_id from tbaadm.gam where bank_id =?SVAR  "
			lv_q = lv_q+"and acid in(select acid from tbaadm.smt where bank_id =?SVAR and acct_status ='A') "
			lv_q = lv_q+"AND MODE_OF_OPER_CODE in ('ETS','SELF'))) AND MODE_OF_OPER_CODE in ('ETS','SELF') and SOL_ID =?SVAR AND CIF_ID =?SVAR"
			
			BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId +"|"+ BANCS.STDIN.contextBankId +"|"+ BANCS.STDIN.contextBankId
			BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS +"|"+ BANCS.STDIN.contextBankId +"|"+ BANCS.STDIN.mySolId +"|"+ CUST.DATA.cifId
                PRINT(BANCS.INPARAM.BINDVARS)

                sv_c = urhk_dbCursorOpenWithBind(lv_q)
                print(sv_c)

                if(sv_c == 0)then
                #{
                        lv_b = BANCS.OUTPARAM.DB_CURSOR_NUMBER
                        print(lv_b)
                        sv_f = urhk_dbCursorFetch(lv_b)
                        print(sv_f)

                        WHILE(sv_f == 0)
                        #{
                                CUST.DATA.foracid = BANCS.OUTPARAM.foracid
                                PRINT(CUST.DATA.foracid)

								if(CUST.DATA.foracid != "")THEN
								#{
									CALLSCRIPTIFEXIST("sbcad2abj02.scr")
								#}
								ENDIF
                                sv_f = urhk_dbCursorFetch(lv_b)
                        #}
                        DO
                        sv_c = urhk_dbCursorClose(lv_b)
                #}
                ENDIF
                
			#}
			ENDIF
		sv_f = urhk_dbCursorFetch(sv_n)
        #}
        DO
        sv_c = urhk_dbCursorClose(sv_n)
#}
ENDIF


sv_e = urhk_dbCursorFetch(sv_d)
#}
        DO
        sv_c = urhk_dbCursorClose(sv_d)
#}
ENDIF


ENDOFSCRIPT:
exitscript
TRACE OFF
END-->
