#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: EFMModify.scr
# Type: NA|default
# Description: EFMModify.scr
# Author:
# Date:
# Modification Log:
#============================================================

# =======================================================================
#                  History Of Changes
# =======================================================================
#  Ver     Date        Who?          	  Description
#  ===     =========   =============      =================
#
#
# =======================================================================
<--START
CHECKSUM="e086ee160cb98c4383c9decd1e4ebb39d82cd9eb4afc8072a1a6a2329ed75cfd"
	TRACE ON
	
	#============================================================================
	# Create the necessary Repositories and Classes
	#============================================================================
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
		#}
	ELSE
		#{
		DELETEREP("CUST")
		CREATEREP("CUST")
	#}
	ENDIF
	IF (CLASSEXISTS("CUST", "FIEFM") == 0) THEN
	#{
		CREATECLASS("CUST", "FIEFM", 5)
		#}
	ELSE
		#{
		DELETECLASS("CUST", "FIEFM")
		CREATECLASS("CUST", "FIEFM", 5)
	#}
	ENDIF
	
	sv_q = urhk_B2k_PRINTRepos("BANCS")
	PRINT(sv_q)
	
	
	IF (FIELDEXISTS(("BANCS").("INPUT").("EFMMod_REQ.EmployeeID")) == 1) THEN
	#{
		CUST.FIEFM.v_emp_id = ("BANCS").("INPUT").("EFMMod_REQ.EmployeeID")
		PRINT("emp_id is")
		PRINT(CUST.FIEFM.v_emp_id)
		
		#}
	ELSE
	#{
		sv_a = "EFMMod_RES.Status|ERROR"
		sv_u = urhk_SetOrbOut(sv_a)
		sv_b = "EFMMod_RES.ErrorMessage|Employee ID is mandatory"
		sv_u = urhk_SetOrbOut(sv_b)
		EXITSCRIPT
		
	#}
	ENDIF
		
	CUST.FIEFM.v_h_func_code = "M"
		
	IF (FIELDEXISTS(("BANCS").("INPUT").("EFMMod_REQ.EmailID")) == 1) THEN
	#{
		CUST.FIEFM.v_emp_email_id = ("BANCS").("INPUT").("EFMMod_REQ.EmailID")
		
	#}
	ENDIF
	
	IF (FIELDEXISTS(("BANCS").("INPUT").("EFMMod_REQ.solID")) == 1) THEN
	#{
		CUST.FIEFM.v_sol_id = ("BANCS").("INPUT").("EFMMod_REQ.solID")
		
	#}
	ELSE
	#{
		CUST.FIEFM.v_sol_id = " "
	#}
	ENDIF
	
	CUST.FIEFM.v_sol_id_desc = " "
	#============================================================================
	# Validate length of Emp_ID for table insertion
	#============================================================================
	sv_p = STRLEN(CUST.FIEFM.v_emp_id)
	PRINT(sv_p)
	IF (sv_p > 10 ) THEN
	#{
		PRINT("Emp Id cannot be greate than 10")
		sv_a = "EFMMod_RES.Status|ERROR"
		sv_u = urhk_SetOrbOut(sv_a)
		sv_b = "EFMMod_RES.ErrorMessage|Employee ID cannot be greater than 10 characters"
		sv_u = urhk_SetOrbOut(sv_b)
		EXITSCRIPT
	#}
	ELSE
	#{
		PRINT(sv_p)
	#}
	ENDIF
	
	sv_a = urhk_SetUrhkInp("emp_id|" +  CUST.FIEFM.v_emp_id)
	sv_a = urhk_SetUrhkInp("h_func_code|" +  CUST.FIEFM.v_h_func_code)
	
		
	################### FETCH FOR MODIFY STARTS ############################
	
	sv_c = urhk_SetUrhkInp("emp_id|" + CUST.FIEFM.v_emp_id)
	sv_c = urhk_ExecFwkSrv("HEFM|FetchForModify|retain_all_output = Y")
	PRINT(sv_c)
	
	sv_c = BANCS.OUTPARAM.successOrFailure
	IF (sv_c != "S") THEN
	#{
		PRINT("Error")
		sv_c = BANCS.OUTPARAM.Error_num
		PRINT(sv_c)
		IF (sv_c > 0) THEN
		#{
			sv_d = 1
			
			WHILE (sv_d <= sv_c)
			#{
				# ----------------------------------------------------------------------
				# Error Handling
				# ----------------------------------------------------------------------
				
				sv_f = FORMAT$(sv_d,"%d")
				sv_x = ("BANCS").("OUTPARAM").("ErrorMesg_"+sv_f)
				PRINT(sv_x)
				sv_y = ("BANCS").("OUTPARAM").("ErrorCode_"+sv_f)
				PRINT(sv_y)
				sv_z = ("BANCS").("OUTPARAM").("Tag_"+sv_f)
				PRINT(sv_z)
				
				
				("CUST").("FIEFM").("Error_" + sv_f) = "EFMMod_RES.ErrorMessage_"+sv_f + "|" +sv_x
				IF (sv_z != "") THEN
				#{
					("CUST").("FIEFM").("FieldName_" + sv_f) = "EFMMod_RES.FieldName_"+sv_f + "|" +sv_z
				#}
				ENDIF
				
				sv_d = sv_d + 1
			#}
			DO
			
			
			sv_a = "EFMMod_RES.EFMStatus|ERROR"
			sv_u = urhk_SetOrbOut(sv_a)
			
			sv_d = 1
			WHILE (sv_d <= sv_c)
			#{
				
				sv_f = FORMAT$(sv_d,"%d")
				sv_u = urhk_SetOrbOut(("CUST").("FIEFM").("Error_" + sv_f))
				
				IF (sv_z != "") THEN
				#{
					sv_u = urhk_SetOrbOut(("CUST").("FIEFM").("FieldName_" + sv_f))
				#}
				ENDIF
				
				sv_d = sv_d + 1
			#}
			DO
			
			EXITSCRIPT
		#}
		#}
		ENDIF
	ELSE 
	#{
		sv_z = "EFMMod_RES.EFMStatus|SUCCESS"
		sv_u = urhk_SetOrbOut(sv_z)
		sv_z = "EFMMod_RES.EMP_ID| "+CUST.FIEFM.v_emp_id 
		sv_u = urhk_SetOrbOut(sv_z)
		sv_z = "EFMMod_RES.MESSAGE| "+CUST.FIEFM.v_emp_id +" MODIFIED SUCCESSFULLY. "
		sv_u = urhk_SetOrbOut(sv_z)
	#}
	ENDIF
	
	
	#################                   FETCHFORMODIFY ENDS                       ###########################
	
	sv_a = urhk_CopyOutToIn("")
	PRINT (sv_a)
		
	sv_q = urhk_B2k_PRINTRepos("BANCS")
	PRINT(sv_q)
	
	IF (FIELDEXISTS(CUST.FIEFM.v_emp_email_id) == 1) THEN
	#{
		sv_a = urhk_SetUrhkInp("emp_email_id|" +  CUST.FIEFM.v_emp_email_id)
		PRINT (sv_a)
		
	#}
	ENDIF
	
	IF(TRIM(CUST.FIEFM.v_sol_id) != "")THEN
	#{
		sv_a = urhk_SetUrhkInp("sol_id|" +  CUST.FIEFM.v_sol_id)
		sv_a = urhk_SetUrhkInp("sol_id_desc|" +  CUST.FIEFM.v_sol_id_desc)
	#}
	ENDIF
	
	sv_a = urhk_ExecFwkSrv("HEFM|Modify|retain_all_output = Y")
	PRINT(sv_a)
	# The status of the service is stored in BANCS.OUTPARAM.successOrFailure
	sv_c = BANCS.OUTPARAM.successOrFailure
	IF (sv_c != "S") THEN
	#{
		PRINT("Error")
		PRINT(sv_c)
		
		
		sv_c = BANCS.OUTPARAM.Error_num
		PRINT(sv_c)
		
		
		IF (sv_c > 0) THEN
		#{
			sv_d = 1
			
			WHILE (sv_d <= sv_c)
			#{
				# ----------------------------------------------------------------------
				# Error Handling
				# ----------------------------------------------------------------------
				
				sv_f = FORMAT$(sv_d,"%d")
				sv_x = ("BANCS").("OUTPARAM").("ErrorMesg_"+sv_f)
				PRINT(sv_x)
				sv_y = ("BANCS").("OUTPARAM").("ErrorCode_"+sv_f)
				PRINT(sv_y)
				sv_z = ("BANCS").("OUTPARAM").("Tag_"+sv_f)
				PRINT(sv_z)
				
				
				("CUST").("FIEFM").("Error_" + sv_f) = "EFMMod_RES.ErrorMessage_"+sv_f + "|" +sv_x
				IF (sv_z != "") THEN
				#{
					("CUST").("FIEFM").("FieldName_" + sv_f) = "EFMMod_RES.FieldName_"+sv_f + "|" +sv_z
				#}
				ENDIF
				PRINT(("CUST").("FIEFM").("Error_" + sv_f))
				#print(("CUST").("FIEFM").("FieldName_" + sv_f))
				
				
				sv_d = sv_d + 1
			#}
			DO
			
			
			sv_a = "EFMMod_RES.EFMStatus|ERROR"
			sv_u = urhk_SetOrbOut(sv_a)
			
			sv_d = 1
			WHILE (sv_d <= sv_c)
			#{
				
				sv_f = FORMAT$(sv_d,"%d")
				sv_u = urhk_SetOrbOut(("CUST").("FIEFM").("Error_" + sv_f))
				
				IF (sv_z != "") THEN
				#{
					sv_u = urhk_SetOrbOut(("CUST").("FIEFM").("FieldName_"+sv_f))
				#}
				ENDIF
				
				sv_d = sv_d + 1
			#}
			DO
			
			EXITSCRIPT
		#}
		ENDIF
		#}
	ELSE
		#{
		sv_z = "EFMMod_RES.EFMStatus|SUCCESS"
		sv_u = urhk_SetOrbOut(sv_z)
		sv_z = "EFMMod_RES.MESSAGE| "+CUST.FIEFM.v_emp_id +" MODIFIED SUCCESSFULLY. "
		sv_u = urhk_SetOrbOut(sv_z)
		
	#}
	ENDIF
	
	###################################  MODIFY ENDS ##################################
	
	
	# Result available in the registered repository variable.
	
	PRINT(CUST.FIEFM.v_emp_id)
	
	# Fetching the added data for verification
	sv_c = urhk_SetUrhkInp("emp_id|" + CUST.FIEFM.v_emp_id)
	sv_c = urhk_ExecFwkSrv("HEFM|FetchForVerify|same_user_verify =Y|retain_all_output = Y")
	PRINT(sv_c)
	
	sv_c = BANCS.OUTPARAM.successOrFailure
	IF (sv_c != "S") THEN
	#{
		PRINT("Error")
		sv_c = BANCS.OUTPARAM.Error_num
		PRINT(sv_c)
		IF (sv_c > 0) THEN
		#{
			sv_d = 1
			
			WHILE (sv_d <= sv_c)
			#{
				# ----------------------------------------------------------------------
				# Error Handling
				# ----------------------------------------------------------------------
				
				sv_f = FORMAT$(sv_d,"%d")
				sv_x = ("BANCS").("OUTPARAM").("ErrorMesg_"+sv_f)
				PRINT(sv_x)
				sv_y = ("BANCS").("OUTPARAM").("ErrorCode_"+sv_f)
				PRINT(sv_y)
				sv_z = ("BANCS").("OUTPARAM").("Tag_"+sv_f)
				PRINT(sv_z)
				
				
				("CUST").("FIEFM").("Error_" + sv_f) = "EFMMod_RES.ErrorMessage_"+sv_f + "|" +sv_x
				IF (sv_z != "") THEN
				#{
					("CUST").("FIEFM").("FieldName_" + sv_f) = "EFMMod_RES.FieldName_"+sv_f + "|" +sv_z
				#}
				ENDIF
				
				sv_d = sv_d + 1
			#}
			DO
			
			
			sv_a = "EFMMod_RES.EFMStatus|ERROR"
			sv_u = urhk_SetOrbOut(sv_a)
			
			sv_d = 1
			WHILE (sv_d <= sv_c)
			#{
				
				sv_f = FORMAT$(sv_d,"%d")
				sv_u = urhk_SetOrbOut(("CUST").("FIEFM").("Error_" + sv_f))
				
				IF (sv_z != "") THEN
				#{
					sv_u = urhk_SetOrbOut(("CUST").("FIEFM").("FieldName_" + sv_f))
				#}
				ENDIF
				
				sv_d = sv_d + 1
			#}
			DO
			
			EXITSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	##################################   FETCH FOR VERIFY ENDS ###################################
	
	# Copying the fetched data as input to verufy service using urhk_CopyOutToIn hook
	sv_c = urhk_CopyOutToIn("")
	PRINT (sv_c)
	
	sv_c = urhk_SetUrhkInp("emp_id|" +  CUST.FIEFM.v_emp_id)
	sv_c = urhk_ExecFwkSrv("HEFM|Verify|same_user_verify = Y|retain_all_output =Y")
	PRINT (sv_c)
	sv_c = BANCS.OUTPARAM.successOrFailure
	IF (sv_c != "S") THEN
	#{
		PRINT("Error")
		sv_c = BANCS.OUTPARAM.Error_num
		PRINT (sv_c)
		IF (sv_c > 0) THEN
		#{
			sv_d = 1
			
			WHILE (sv_d <= sv_c)
			#{
				# ----------------------------------------------------------------------
				# Error Handling
				# ----------------------------------------------------------------------
				
				sv_f = FORMAT$(sv_d,"%d")
				sv_x = ("BANCS").("OUTPARAM").("ErrorMesg_"+sv_f)
				PRINT(sv_x)
				sv_y = ("BANCS").("OUTPARAM").("ErrorCode_"+sv_f)
				PRINT(sv_y)
				sv_z = ("BANCS").("OUTPARAM").("Tag_"+sv_f)
				PRINT(sv_z)
				
				
				("CUST").("FIEFM").("Error_" + sv_f) = "EFMMod_RES.ErrorMessage_"+sv_f + "|" +sv_x
				IF (sv_z != "") THEN
				#{
					("CUST").("FIEFM").("FieldName_" + sv_f) = "EFMMod_RES.FieldName_"+sv_f + "|" +sv_z
				#}
				ENDIF
				
				sv_d = sv_d + 1
			#}
			DO
			
			
			sv_a = "EFMMod_RES.EFMStatus|ERROR"
			sv_u = urhk_SetOrbOut(sv_a)
			
			sv_d = 1
			WHILE (sv_d <= sv_c)
			#{
				
				sv_f = FORMAT$(sv_d,"%d")
				sv_u = urhk_SetOrbOut(("CUST").("FIEFM").("Error_" + sv_f))
				
				IF (sv_z != "") THEN
				#{
					sv_u = urhk_SetOrbOut(("CUST").("FIEFM").("FieldName_" + sv_f))
				#}
				ENDIF
				
				sv_d = sv_d + 1
			#}
			DO
			
			EXITSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	
	EXITSCRIPT
	TRACE OFF
END-->
