#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: s_ast0057mn003.scr
# Type: NA|default
# Description: s_ast0057mn003.scr
# Author:
# Date:
# Modification Log:
#============================================================

#-------------------------------------------------------------------------------------
#	Name			: s_ast0057mn003.scr
#	Description		: This script is used for deleting the records from custom table during cancel
#	Date			: 13-06-2013
#	Author			: Prashanth
#	Input			: NA
#	Output			: NA
#	Called Script	: SRV_CancelDisbData_post_process_data.scr
#	Calling Script	:
#	Menu Option		: HLADISB
#	Srl. No		Date			Author			Description.
#	-------		----			------			----------------
#	1.0			13-06-2013		Prashanth		Original Version
#-------------------------------------------------------------------------------------
<--START
CHECKSUM="463d915d15f7993e257e11db5aeebf2eaca09a60d2c24d62d7ea96adfc8c5920"
	TRACE ON
	
	#------------------------------------------------------------------------------
	#	Creating repository and class
	#	Initializing the variables
	#	Getting all the values from CPARAM
	#------------------------------------------------------------------------------
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST","LADISB") == 0) THEN
	#{
		CREATECLASS("CUST","LADISB",5)
	#}
	ENDIF
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	CUST.LADISB.errMsg       = ""
	
	#-------------------------------------------------------------------------------------------
	# Getting foracid from from frontend
	#-------------------------------------------------------------------------------------------
	sv_u = urhk_SRV_GetVal("acctDisburseTranLA.laAcct.foracid")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
		CUST.LADISB.foracid = BANCS.OUTPARAM.srvValue
		PRINT(CUST.LADISB.foracid)
		#}
	ELSE
		#{
		CUST.LADISB.errMsg = "Error: urhk_SRV_GetVal failed for foracid"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	#--------------------------------------------------------------------
	#DELETING THE RECORD FOR GIVEN FORACID
	#--------------------------------------------------------------------
	sv_s = ""
	sv_s = " DELETE FROM "
	sv_s = sv_s + " CUSTOM.C_LADISB_PAYOFF_OLDLOAN A"
	sv_s = sv_s + " WHERE A.loan_foracid = ?SVAR "
	sv_s = sv_s + " AND A.bank_id = ?SVAR"
	
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS = ""
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.LADISB.foracid + "|"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSQLWithBind(sv_s)
	PRINT(sv_d)
	
	IF ( sv_d != 0 ) THEN
	#{
		CUST.LADISB.errMsg = "Error occured in Deleting rows from LADISB_PAYOFF_OLDLOAN custom table "
		PRINT(CUST.LADISB.errMsg)
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		PRINT(sv_d)
	#}
	ENDIF
	
	ENDOFSCRIPT:
		IF (CUST.LADISB.errMsg != "") THEN
		#{
			PRINT(CUST.LADISB.errMsg)
		#}
		ENDIF
		
		EXITSCRIPT
		TRACE OFF
END-->
