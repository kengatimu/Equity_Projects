#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: FIN_GL_Interest_Report_Insert.scr
# Type: NA|default
# Description: FIN_GL_Interest_Report_Insert.scr
# Author:
# Date:
# Modification Log:
#============================================================

#---------------------------------------------------------------------
# Name    : FIN_GL_Interest_Report.scr
# Date 	  : Oct 25th 2010
# Author  : Parikshith
# Desc    :
# Modification History  :
#       S.No.      Date               Name               Description
#       -----    ----------           ----------        ----------------
#       1       Oct 15th 2012         Parikshith    Original Version
#----------------------------------------------------------------------

<--START
CHECKSUM="794a8ab18e7aa43a75aacab3e1239ef7c2c86cd8517d4cd920aa50cd738e7132"
	TRACE ON
	########################################
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	IF (CLASSEXISTS("CUST", "DATA") == 0) THEN
	#{
		CREATECLASS("CUST", "DATA", 5)
	#}
	ENDIF
	CUST.DATA.bankId = BANCS.STDIN.contextBankId
	CUST.DATA.userId = BANCS.STDIN.userId
	CUST.DATA.setId = BANCS.STDIN.mySolId
	CUST.DATA.toFileName = ""
	CUST.DATA.insertFileName = "NR_FIN_GL_Interest_Report_Insert.sql"
	PRINT(CUST.DATA.bankId)
	PRINT(BANCS.STDIN.homeCrncyCode)
	
	#---------------------------------------------------------------------------
	#  GETTING THE PROCESS ID
	#---------------------------------------------------------------------------
	lv_d = urhk_B2k_PID("")
	IF (lv_d == 0) THEN
	#{
		CUST.DATA.pid = BANCS.OUTPARAM.PID
		PRINT(CUST.DATA.pid)
		
	#}
	ENDIF
	
	
	CUST.DATA.inputStr = CUST.DATA.bankId + "!" + BANCS.STDIN.homeCrncyCode + "!" + CUST.DATA.setId
	CUST.DATA.inputStr = CUST.DATA.inputStr + "!" + MID$(BANCS.STDIN.BODDate,0,10)
	
	
	#-----------------------------------------------------------------------------------------
	#Calling the sql to insert data into CUSTOM.C_GLIR_EOD_HIST table for the previous day
	#-----------------------------------------------------------------------------------------
	
	lv_a = "exebatch bauu9151 " + CUST.DATA.insertFileName + " " + CUST.DATA.inputStr
	PRINT(lv_a)
	lv_u = SYSTEM(lv_a)
	PRINT(lv_u)
	IF (lv_u != 0) THEN
	#{
		CUST.DATA.errMsg = "Insert into CUSTOM.C_GLIR_EOD_HIST failed"
		GOTO FAILURE
	#}
	ENDIF
	
	FAILURE:
		#----------------------------------------------------
		#----------------Exit the script---------------------
		#----------------------------------------------------
		
		IF (CLASSEXISTS("CUST", "DATA") != 0) THEN
		#{
			DELETECLASS("CUST", "DATA")
		#}
		ENDIF
		IF (REPEXISTS("CUST") != 0) THEN
		#{
			DELETEREP("CUST")
		#}
		ENDIF
		
		TRACE OFF
END-->
