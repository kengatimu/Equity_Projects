#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: ubpdp069.scr
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START
CHECKSUM="eda7d1feb7030ebf1f3b7f61d3e4587f0a3466fead632f3287ba7cc732dff16c"
	# Script body goes here
	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp061.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets inserts data in custom.c_ubp_details table
	# Called Script         :
	# Calling Script        :		ubpdp014.scr
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           26-08-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	#========================================================
	# Check whether CUST Repository exists else create it
	#========================================================
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	#========================================================
	# Check whether UBP Class exists else create it
	#========================================================
	sv_r = CLASSEXISTS("CUST","UBP")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","UBP",5)
	#}
	ENDIF
	CUST.UBP.errorFlg="N"
	#========================================================
	# Generating the Ref. Num
	#========================================================
	sv_s = "billPayRefNum|"
	sv_s = sv_s + " SELECT CUSTOM.BILL_PAY_REF_NUM.nextval"
	sv_s = sv_s + " FROM DUAL"
	PRINT(sv_s)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d!=0)THEN
	#{
		CUST.UBP.errorFlg="Y"
		#CUST.UBP.errorMsg="The sequence BILL_PAY_REF_NUM doesnot exist."
		CUST.UBP.errorCode = "UBP_ERR007"
		CALL("ubpdp044.scr")
		CUST.CIECLG.errorMsg=CUST.UBP.errorDesc
		CUST.CIECLG.scriptSuccess = "N"
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		print(sv_u)
		sv_u = urhk_SetOrbOut("Error_1|Error^"+CUST.CIECLG.errorMsg+"^random")
		print(sv_u)
		GOTO ENDOFSCRIPT
		
		
		#}
	ELSE
		#{
		CUST.UBP.billPayRefNum=BANCS.OUTPARAM.billPayRefNum
		PRINT(CUST.UBP.billPayRefNum)
		CUST.CIECLG.billPayRefNumIecd=CUST.UBP.billPayRefNum
		print(CUST.CIECLG.billPayRefNumIecd)
	#}
	ENDIF
	#========================================================
	# Setting the values to be inserted in CUBP table
	#========================================================
	CUST.UBP.modeOfPay=CUST.CIECLG.funcCode
	IF(FIELDEXISTS(CUST.CIECLG.billerId) == 1)THEN
	#{
		CUST.UBP.billerId=CUST.CIECLG.billerId
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.crAcctNum) == 1)THEN
	#{
		CUST.UBP.billerAcctId=CUST.CIECLG.crAcctNum
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.crAcctNumEntityId) == 1)THEN
	#{
		CUST.UBP.billerBnkId=CUST.CIECLG.crAcctNumEntityId
	#}
	ENDIF
	
	
	IF(FIELDEXISTS(CUST.CIECLG.payingAcct) == 1)THEN
	#{
		CUST.UBP.payerAcctId=CUST.CIECLG.payingAcct
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.zoneCcy) == 1)THEN
	#{
		CUST.UBP.refCrncy=CUST.CIECLG.zoneCcy
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.name) == 1)THEN
	#{
		CUST.UBP.name=CUST.CIECLG.name
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.instrumentAmt) == 1)THEN
	#{
		CUST.UBP.billAmt = CUST.CIECLG.instrumentAmt
		IF(FIELDEXISTS(CUST.CIECLG.chargeAmt) == 1)THEN
		#{
			CUST.UBP.totalAmt= CDOUBLE(CUST.CIECLG.instrumentAmt)+CDOUBLE(CUST.CIECLG.chargeAmt)
			#}
		ELSE
			#{
			CUST.UBP.totalAmt= CDOUBLE(CUST.CIECLG.instrumentAmt)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.billRefNum) == 1)THEN
	#{
		CUST.UBP.billerRefNum = CUST.CIECLG.billRefNum
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.tranParticular) == 1)THEN
	#{
		CUST.UBP.tranParticulars = CUST.CIECLG.tranParticular
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.tranRmks) == 1)THEN
	#{
		CUST.UBP.tranrmks = CUST.CIECLG.tranRmks
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.STDIN.BODDate) == 1)THEN
	#{
		CUST.UBP.interEntityTranDate = BANCS.STDIN.BODDate
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.typOfServ) == 1)THEN
	#{
		CUST.UBP.typOfServ = CUST.CIECLG.typOfServ
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.valType) == 1)THEN
	#{
		CUST.UBP.valType = CUST.CIECLG.valType
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.zoneCode) == 1)THEN
	#{
		CUST.UBP.clrZoneCode = CUST.CIECLG.zoneCode
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.zoneDate) == 1)THEN
	#{
		CUST.UBP.clrZoneDate = CUST.CIECLG.zoneDate
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.setNum) == 1)THEN
	#{
		CUST.UBP.setNum = CUST.CIECLG.setNum
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.retStrngValHid) == 1)THEN
	#{
		CUST.UBP.retStrngValHid = CUST.CIECLG.retStrngValHid
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.appCatgry) == 1)THEN
	#{
		CUST.UBP.appCategory = CUST.CIECLG.appCatgry
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.freetext1) == 1)THEN
	#{
		CUST.UBP.freeText1 = CUST.CIECLG.freetext1
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.freetext2) == 1)THEN
	#{
		CUST.UBP.freeText2 = CUST.CIECLG.freetext2
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.freetext3) == 1)THEN
	#{
		CUST.UBP.freeText3 = CUST.CIECLG.freetext3
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.freetext4) == 1)THEN
	#{
		CUST.UBP.freeText4 = CUST.CIECLG.freetext4
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.freetext5) == 1)THEN
	#{
		CUST.UBP.freeText5 = CUST.CIECLG.freetext5
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.freetext6) == 1)THEN
	#{
		CUST.UBP.freeText6 = CUST.CIECLG.freetext6
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.freetext7) == 1)THEN
	#{
		CUST.UBP.freeText7 = CUST.CIECLG.freetext7
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.CIECLG.riskNote) == 1)THEN
	#{
		CUST.UBP.riskNote = CUST.CIECLG.riskNote
	#}
	ENDIF
	
	#========================================================
	# Calling the script for inserting the data.
	#========================================================
	call("ubpdp014.scr")
	
	ENDOFSCRIPT:
		IF(CUST.UBP.errorFlg=="Y")THEN
		#{
			CUST.UBP.errorFlg="Y"
			#CUST.UBP.errorMsg="Insertion to the scr failed"
			CUST.UBP.errorCode = "UBP_ERR028"
			CALL("ubpdp044.scr")
			CUST.CIECLG.errorMsg=CUST.UBP.errorDesc
			CUST.CIECLG.scriptSuccess = "N"
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			print(sv_u)
			sv_u = urhk_SetOrbOut("Error_1|Error^"+CUST.CIECLG.errorMsg+"^random")
			
		#}
		ENDIF
		EXITSCRIPT
		TRACE OFF
END-->
