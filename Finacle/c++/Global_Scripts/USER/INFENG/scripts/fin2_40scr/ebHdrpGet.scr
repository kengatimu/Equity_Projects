<--start
trace on
	sv_u = urhk_B2K_PrintRepos("BANCS")
	print(sv_u)
	if(fieldexists(BANCS.STDIN.menuId)==1)then
		if(BANCS.STDIN.menuId=="HDRP")then
			sv_u = urhk_SetOrbOut("Id|" + BANCS.STDIN.menuId )
		else
			sv_u = urhk_SetOrbOut("Id|NA")
		endif
	endif
	exitscript
trace off
end-->
