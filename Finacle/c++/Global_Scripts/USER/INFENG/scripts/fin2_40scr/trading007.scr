#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: trading007.scr
# event: NA
# description: q
#=================================================

#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# event: NA
# description: 23
#=================================================
#Test script for report

#-------------------------------------------------------------------------------------
<--START
CHECKSUM="30c175230a626382cda351b87a05256c7de77283c59a2f29ec753d7c01f0cda6"
	TRACE ON
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST", "DENOM") == 0) THEN
	#{
		CREATECLASS("CUST", "DENOM", 5)
	#}
	ENDIF
	
	CUST.DENOM.chrgAmount="10"
	CUST.DENOM.totexchgamt="2500"
	CUST.DENOM.rate="12"
	CUST.DENOM.CrncyCode1="USD"
	CUST.DENOM.CrncyCode2="KES"
	CUST.DENOM.totamt="2500.25"
	
	
	CUST.DENOM.Bankname="EBL"
	
	print(CUST.DENOM.chrgAmount)
	print(CUST.DENOM.totexchgamt)
	print(CUST.DENOM.rate)
	print(CUST.DENOM.CrncyCode1)
	print(CUST.DENOM.CrncyCode2)
	print(CUST.DENOM.totamt)
	
	CUST.DENOM.totamt_wrd = ""
	print(CUST.DENOM.totamt_wrd)
	
	
	# ------------------------------------------------------------------------------
	# Get key for List File Name
	# ------------------------------------------------------------------------------
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.mySolId + "|" + BANCS.STDIN.contextBankId
	print(BANCS.INPARAM.BINDVARS)
	sv_l = "sol_desc|select SOL_DESC from tbaadm.sol where SOL_ID = ?SVAR AND  bank_id = ?SVAR "
	
	PRINT(sv_l)
	sv_d = urhk_dbSelectWithBind(sv_l)
	IF (sv_d != 0) THEN
	#{
		PRINT("Error in getting key. Exiting")
		GOTO STEP1
		#}
	ELSE
		#{
		CUST.DENOM.sol_desc = BANCS.OUTPARAM.sol_desc
		
	#}
	ENDIF
	
	sv_s = "key|SELECT TO_CHAR(sysdate,'DDMMYYYYHH24MISS') FROM dual"
	
	PRINT(sv_s)
	sv_d = urhk_dbSelectWithBind(sv_s)
	IF (sv_d != 0) THEN
	#{
		PRINT("Error in getting key. Exiting")
		GOTO STEP1
		#}
	ELSE
		#{
		CUST.DENOM.key = BANCS.OUTPARAM.key
	#}
	ENDIF
	
	
	
	CUST.DENOM.lstFile = "SharePurchase_" + BANCS.STDIN.userId + "_" + CUST.DENOM.key + ".LST"
	sv_e = "touch " + CUST.DENOM.lstFile
	PRINT(sv_e)
	sv_m = SYSTEM(sv_e)
	PRINT(sv_m)
	
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.mySolId + "|" + BANCS.STDIN.contextBankId
	print(BANCS.INPARAM.BINDVARS)
	sv_s = "addr1,addr2| "
	sv_s = sv_s + " SELECT addr_1,addr_2 FROM tbaadm.sol"
	sv_s = sv_s + " WHERE sol_id = ?SVAR"
	sv_s = sv_s + " AND del_flg <> 'Y'"
	sv_s = sv_s + " AND bank_id = ?SVAR"
	PRINT(sv_s)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	IF (sv_d == 0) THEN
	#{
		CUST.DENOM.brAddr1 = BANCS.OUTPARAM.addr1
		PRINT(CUST.DENOM.brAddr1)
		CUST.DENOM.brAddr2 = BANCS.OUTPARAM.addr2
		PRINT(CUST.DENOM.brAddr2)
		#}
	ELSE
		#{
		print("Query execuion failed")
		CUST.DENOM.brAddr1 = ""
		CUST.DENOM.brAddr2 = ""
	#}
	ENDIF
	sv_a = CUST.DENOM.totamt
	sv_b = ""
	sv_c = getposition(CUST.DENOM.totamt, '.')
	print(sv_c)
	
	IF (sv_c != 0) THEN
	#{
		
		sv_a = Left$(CUST.DENOM.totamt, (sv_c - 1))
		print(sv_a)
		
		sv_b = Right$(CUST.DENOM.totamt, 2)
		print(sv_b)
		
	#}
	ENDIF
	
	BANCS.INPARAM.BINDVARS = sv_a
	
	
	CUST.DENOM.totamt_wrd = ""
	
	
	print(CUST.DENOM.totamt_wrd)
	print(BANCS.STDIN.myBankCode)
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.myBankCode + "|" + BANCS.STDIN.contextBankId
	sv_s = "Bankname|select BANK_NAME from TBAADM.BANK_CODE_TABLE where bank_code = ?SVAR AND  bank_id = ?SVAR"
	PRINT(sv_s)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF (sv_d == 0) THEN
	#{
		CUST.DENOM.Bankname = BANCS.OUTPARAM.Bankname
		
		PRINT(CUST.DENOM.Bankname)
		#}
	ELSE
		#{
		CUST.DENOM.Bankname = ""
		
	#}
	ENDIF
	
	#---------------CHanged for BW PRD Error - GIRI--
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + CUST.DENOM.CrncyCode2
	
	sv_s = "crncyname,dcmldesc|select crncy_name,dcml_desc from tbaadm.cnc where bank_id=?SVAR "
	sv_s = sv_s + "  AND crncy_code=?SVAR "
	sv_s = sv_s + "  AND ENTITY_CRE_FLG = 'Y' "
	sv_s = sv_s + "  AND DEL_FLG = 'N' "
	print(sv_s)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	IF (sv_d == 0) THEN
	#{
		CUST.DENOM.crncyname = BANCS.OUTPARAM.crncyname
		PRINT(CUST.DENOM.crncyname)
		
		CUST.DENOM.dcmldesc = BANCS.OUTPARAM.dcmldesc
		print(CUST.DENOM.dcmldesc)
		#}
	ELSE
		#{
		CUST.DENOM.crncyname = ""
		CUST.DENOM.dcmldesc = ""
	#}
	ENDIF
	
	# ----------------------------------------------------------------
	#  Writing to the lst file
	# ---------------------------------------------------------------
	sv_k = CUST.DENOM.sol_desc + "|" + CUST.DENOM.CrncyCode1 + "|"
	sv_k = sv_k + CUST.DENOM.totexchgamt + "|" + CUST.DENOM.CrncyCode2 + "|"
	sv_k = sv_k + CUST.DENOM.totamt + "|" + CUST.DENOM.rate + "|"
	sv_k = sv_k + CUST.DENOM.chrgAmount + "|" + CUST.DENOM.totamt_wrd + "|" + CUST.DENOM.Bankname + "|"
	sv_k = sv_k + CUST.DENOM.crncyname + "|" + CUST.DENOM.dcmldesc
	PRINT(sv_k)
	sv_q = " echo '" + sv_k + "' >> " + CUST.DENOM.lstFile
	PRINT(sv_q)
	sv_v = SYSTEM(sv_q)
	print(sv_v)
	
	# ----------------------------------------------------------------
	#  REPORT GENERATION
	# ----------------------------------------------------------------
	CUST.DENOM.jasperFile = "Transaction_Print.jasper"
	
	CUST.DENOM.rptTitle = "SHARE PURCHASE REPORT"
	CUST.DENOM.rptFile = "SHARE_PURCHASE_REPORT"
	
	sv_n = CUST.DENOM.rptFile + "_" + BANCS.STDIN.userId + "_" + CUST.DENOM.key
	CUST.DENOM.FILENAME = sv_n
	sv_e = "exebatch mrbx4004 $B2K_SESSION_ID " + CUST.DENOM.jasperFile + "  " + CUST.DENOM.lstFile + "   "
	sv_e = sv_e + sv_n
	PRINT(sv_e)
	sv_f = SYSTEM(sv_e)
	PRINT(sv_f)
	IF (sv_f != 0) THEN
	#{
		PRINT("RPT Generation Failed")
		GOTO STEP1
	#}
	ENDIF
	
	sv_x = "exebatch babx4040 $B2K_SESSION_ID  '" + sv_n + "'.PDF '" + CUST.DENOM.rptTitle + "' 'MANAGER' '1' 'N'"
	PRINT(sv_x)
	sv_h = SYSTEM(sv_x)
	PRINT(sv_h)
	IF (sv_h == 0) THEN
	#{
		PRINT("Report generated successfully")
		CUST.DENOM.CUSTOPTN_ID = "FIN_REPORTS_DIR"
		BANCS.INPARAM.BINDVARS = CUST.DENOM.CUSTOPTN_ID + "|" + BANCS.STDIN.contextBankId
		sv_q = "custoptn_value| SELECT custoptn_value FROM TBAADM.COMT"
		sv_q = sv_q + " WHERE custoptn_id = ?SVAR "
		sv_q = sv_q + " AND bank_id = ?SVAR "
		print(sv_q)
		sv_s = urhk_dbselectWithBind(sv_q)
		IF (sv_s == 0) THEN
		#{
			CUST.DENOM.PATH = BANCS.OUTPARAM.custoptn_value + "/" + BANCS.STDIN.userId + "/" + sv_n + ".PDF"
			print(CUST.DENOM.PATH)
			#}
		ELSE
			#{
			CUST.DENOM.PATH = "Report generation failed"
			CUST.DENOM.ErrMsg = urtn_getMsg("V200757")
			print(CUST.DENOM.ErrMsg)
			sv_u = urhk_SetOrbOut("Error_1|1^" + CUST.DENOM.ErrMsg + "^Message")
		#}
		ENDIF
		
	#}
	ENDIF
	
	
	STEP1:
END-->
