#-------------------------------------------------------------------------------------------------------
# Source Name           :   ccpmdp004.scr
# Author                :   Gokulkrishna
# Title                 :
# Description           :   This script fetches all  download Id and lists in a custom
#			   :	searcher
# Sources called        :   None
# Sources called from   :   cchqd_det_ginc.jsp
# Modification History  :
# <Serial No.>      <Date>          <Author Name>       <Description>
# 0.00a             05-03-2012       Gokulkrishna              Original version
#-------------------------------------------------------------------------------------------------------

<--START
CHECKSUM="fd820b1e45cdfa57f48d4d52482745bc04caa77563f275a199697340743bd645"
	TRACE ON
	
	#-------------------------------------------------------------------------------------------------------
	# Create repository and class
	#-------------------------------------------------------------------------------------------------------
	IF(REPEXISTS("CUST")==0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF(CLASSEXISTS("CUST","DATA")==0) THEN
	#{
		CREATECLASS("CUST","DATA",5)
	#}
	ENDIF
	
	#PRINT(BANCS.INPUT.Currency)
	#PRINT(BANCS.INPUT.roleId)
	
	#-------------------------------------------------------------------------------------------------------
	# Intialising repository variables
	#-------------------------------------------------------------------------------------------------------
	CUST.DATA.roleId = ""
	CUST.DATA.counter = ""
	
	#-------------------------------------------------------------------------------------------------------
	# Fetch all Event IDs of GCHRG event and Fixed derivation type
	#-------------------------------------------------------------------------------------------------------
	sv_s = "roleId,crncy,rcreID"
	sv_s = sv_s + "|"
	sv_s = sv_s + " SELECT role_id,crncy_code,rcre_user_id"
	sv_s = sv_s + " FROM CUSTOM.CRCL"
	sv_s = sv_s + " WHERE  del_flg = 'N' "
	sv_s = sv_s + " AND bank_id =?SVAR "
	#sv_s = sv_s + " AND role_id = ?SVAR "
	#sv_s = sv_s + " AND crncy_code = ?SVAR "
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	PRINT(sv_s)
	
	sv_c    =   urhk_dbCursorOpenWithBind(sv_s)
	#PRINT(sv_c)
	
	IF(sv_c != 0) THEN
	#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	IF (sv_c == 0) THEN
	#{
		CUST.DATA.cursorNum = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		PRINT(CUST.DATA.cursorNum)
		sv_r = urhk_dbCursorFetch(CUST.DATA.cursorNum)
		PRINT(sv_r)
		IF(sv_r == 0) THEN
		#{
			WHILE (sv_r == 0)
			#{
				CUST.DATA.counter = CINT(CUST.DATA.counter) + 1
				CUST.DATA.roleId = BANCS.OUTPARAM.roleId
				PRINT(CUST.DATA.roleId)
				CUST.DATA.crncy = BANCS.OUTPARAM.crncy
				PRINT(CUST.DATA.crncy)
				CUST.DATA.rcreID = BANCS.OUTPARAM.rcreID
				
				sv_u    =   urhk_SetOrbOut("recCount|" + CUST.DATA.counter)
				sv_y    = "roleId_"+ CUST.DATA.counter + " |" +CUST.DATA.roleId + "|"
				PRINT(sv_y)
				sv_t = urhk_SetOrbOut(sv_y)
				PRINT(sv_t)
				
				sv_y = "currency_"+ CUST.DATA.counter + " |" + CUST.DATA.crncy + "|"
				PRINT(sv_y)
				sv_t = urhk_SetOrbOut(sv_y)
				PRINT(sv_t)
				
				sv_y = "rcreID_"+ CUST.DATA.counter + " |" + CUST.DATA.rcreID + "|"
				PRINT(sv_y)
				sv_t = urhk_SetOrbOut(sv_y)
				PRINT(sv_t)
				
				
				#}
				
				PRINT(CUST.DATA.cursorNum)
				sv_r = urhk_dbCursorFetch(CUST.DATA.cursorNum)
			#}
			DO
			#}
		ELSE
			#{
			PRINT("No records fetched")
		#}
		ENDIF
		
		PRINT(CUST.DATA.cursorNum)
		sv_r = urhk_dbCursorClose(CUST.DATA.cursorNum)
	#}
	ENDIF
	
	PRINT(CUST.DATA.counter)
	
	ENDOFSCRIPT:
		EXITSCRIPT
		TRACE OFF
END-->
