#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: crejVerify.scr
# Type: NA|default
# Description: Reject Reason Code
# Author: Anshul_Singh04
# Date: 5/8/18 10:20 AM
# Modification Log:
#============================================================

<--START
CHECKSUM="a2653922d9923b6ee4097a788c8a11272edfaeb29e31f901acd73429d89e9d61"
	# Script body goes here
	TRACE ON
	
	sv_a = REPEXISTS("EQT")
	IF (sv_a == 0) THEN
	#{
		CREATEREP("EQT")
	#}
	ENDIF
	sv_a = CLASSEXISTS("EQT", "REJ")
	IF (sv_a == 0 ) THEN
	#{
		CREATECLASS("EQT", "REJ", 5)
	#}
	ENDIF
	
	PRINT(BANCS.INPUT.rejCode)
	
	EQT.REJ.msg=" "
	
	sv_j= "UPDATE CUSTOM.RRCM_MOD SET ENTITY_CRE_FLG='Y' , LCHG_USER_ID=?SVAR, LCHG_TIME=sysdate WHERE bank_id=?SVAR AND OLD_CLG_REJ_CODE =?SVAR "
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.userId  +"|"+ BANCS.STDIN.contextBankId + "|" + BANCS.INPUT.rejCode
	PRINT(BANCS.INPARAM.BINDVARS )
	sv_r = urhk_dbsqlWithBind(sv_j)
	print(sv_r)
	
	IF(sv_r!=0)THEN
	#{
		EQT.REJ.msg = "Error in Database Operation!!"
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		sv_u = urhk_SetOrbOut("Error_1|ERR^"+EQT.REJ.msg)
		GOTO ENDOFSCRIPT
	#}
	endif
	
	sv_j= "DELETE FROM CUSTOM.RRCM WHERE bank_id=?SVAR AND OLD_CLG_REJ_CODE =?SVAR "
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + BANCS.INPUT.rejCode
	
	PRINT(BANCS.INPARAM.BINDVARS )
	sv_r = urhk_dbsqlWithBind(sv_j)
	print(sv_r)
	
	sv_j= "Insert into CUSTOM.RRCM select * from CUSTOM.RRCM_MOD where bank_id=?SVAR AND OLD_CLG_REJ_CODE =?SVAR "
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + BANCS.INPUT.rejCode
	
	PRINT(BANCS.INPARAM.BINDVARS )
	sv_r = urhk_dbsqlWithBind(sv_j)
	print(sv_r)
	
	IF(sv_r!=0)THEN
	#{
		EQT.REJ.msg = "Error in Database Operation!!"
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		sv_u = urhk_SetOrbOut("Error_1|ERR^"+EQT.REJ.msg)
		GOTO ENDOFSCRIPT
	#}
	endif
	
	IF(sv_r==0) THEN
	#{
		EQT.REJ.msg = "Record has been Verified Successfully."
	#}
	endif
	
	sv_j= "DELETE FROM CUSTOM.RRCM_MOD WHERE bank_id=?SVAR AND OLD_CLG_REJ_CODE =?SVAR "
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + BANCS.INPUT.rejCode
	
	PRINT(BANCS.INPARAM.BINDVARS )
	sv_r = urhk_dbsqlWithBind(sv_j)
	print(sv_r)
	
	ENDOFSCRIPT:
		
		EXITSCRIPT
		
END-->
