#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: b_dfltTdAcct.scr
# Type: NA|default
# Description: b_dfltTdAcct.scr
# Author:
# Date:
# Modification Log:
#============================================================

#------------------------------------------------------------------------------------------------
#   Name            : b_dfltTdAcct.scr
#   Description     : This script is called from SRV_FetchDfltTDAcctDtls_post_process_data.scr
#   Date            : 28-05-2013
#   Author          : jane
#   Input           : NA
#   Output          : NA
#   Called Script   : SRV_FetchDfltTDAcctDtls_post_process_data.scr
#   Calling Script  : NA
#   Menu Option     : HOAACTD
#   Srl. No         Date            Author          	Description.
#   -------         ------          ------          	------------
#   1.0             28-05-2013      jane     		Original Version
#-----------------------------------------------------------------------------------------------
<--START
CHECKSUM="821ca98c798ae50f26cc163dda2b554213a0b5ed0ef0d96c4f12b40cb0cce423"
	TRACE ON
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	IF (CLASSEXISTS("CUST", "CDDUM") == 0) THEN
	#{
		CREATECLASS("CUST", "CDDUM", 5)
	#}
	ENDIF
	
	sv_a=urhk_B2k_PrintRepos("BANCS")
	sv_a = urhk_SRV_SetVal("dfltTdAcct.acctCommonInfo.acctInterestTax.wtaxAmountScopeFlg.code|P")
	PRINT(sv_a)

	#######################
IF (FIELDEXISTS(BANCS.STDIN.menuId) == 1) THEN
#{
	IF (BANCS.STDIN.menuId == "HOAACTD") THEN
	#{
		IF(BANCS.STDIN.contextBankId == "54") THEN
		sv_f = urhk_SRV_GetVal("dfltTdAcct.acctCommonInfo.acctGeneralInfo.acctBasic.custId.cifId")
		CUST.CDDUM.cifId = BANCS.OUTPARAM.srvValue
		PRINT(CUST.CDDUM.cifId)
			sv_s = "PIN, CORP_PIN|SELECT Cust_Swift_Code_Desc,STRUSERFIELD8  "
			sv_s = sv_s + "FROM crmuser.accounts "
			sv_s = sv_s + "WHERE bank_id  = ?SVAR "
			sv_s = sv_s + "AND orgkey = ?SVAR "
			BANCS.INPARAM.BINDVARS =  BANCS.STDIN.contextBankId + "|" + CUST.CDDUM.cifId  

			sv_r =  urhk_dbSelectWithBind(sv_s)
			print(sv_r)

			IF (sv_r ==0) THEN
			#{
				CUST.CDDUM.PIN = TRIM(BANCS.OUTPARAM.PIN)
				print (CUST.CDDUM.PIN)
				CUST.CDDUM.CORP_PIN = TRIM(BANCS.OUTPARAM.CORP_PIN)
				print (CUST.CDDUM.CORP_PIN)

				IF((CUST.CDDUM.PIN=="") AND (CUST.CDDUM.CORP_PIN==""))  THEN
				#{
					sv_u = urhk_SRV_SetErr("Please maintain KRA PIN for this Customer......" )
					BANCS.OUTPUT.successOrFailure = "F"
					EXITSCRIPT

				#}
				ENDIF
			
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
#}
ENDIF

#############################
	
	IF (BANCS.STDIN.contextBankId=="54") THEN
	#{
		sv_a = urhk_SRV_GetVal("acctCrit.schmCode.schmCode")
		sv_b=BANCS.OUTPARAM.srvValue
		PRINT(sv_b)
		IF ((sv_b!="TD400") AND (sv_b!="TD401") AND (sv_b!="TD407")) THEN
		#{
			sv_u = urhk_SRV_SetErr("Scheme Not allowed contact Head Office......" )
			BANCS.OUTPUT.successOrFailure = "F"
			EXITSCRIPT
		#}
		ENDIF
		IF (sv_b=="TD407") THEN
		#{
			IF (BANCS.STDIN.homeSolId!="000") THEN
			#{
				sv_u = urhk_SRV_SetErr("Scheme Not allowed at Branch contact Head Office......" )
				BANCS.OUTPUT.successOrFailure = "F"
				EXITSCRIPT
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	sv_u = "userid|select user_id from tbaadm.upr where user_work_class='200' AND home_bank_id= ?SVAR AND sol_id= ?SVAR "
	sv_u = sv_u + "AND del_flg='N' AND rownum<2"
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.mySolId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_f = urhk_dbSelectWithBind(sv_u)
	PRINT(sv_f)
	
	IF ( sv_f =="0" ) THEN
	#{
		CUST.CDDUM.userid = BANCS.OUTPARAM.userid
		PRINT(CUST.CDDUM.userid)
		
		sv_a = urhk_SRV_SetVal("dfltTdAcct.acctCommonInfo.acctGeneralInfo.acctCustInfo.acctMgrUserId.userId|" + CUST.CDDUM.userid)
		PRINT(sv_a)
	#}
	ENDIF
	
	
	
	sv_e = urhk_SRV_SetVal("dfltTdAcct.acctCommonInfo.acctGeneralInfo.acctBasic.acctMgrAtAcct.userId| ")
	PRINT(sv_e)
	IF (BANCS.STDIN.contextBankId!="43") THEN
		sv_e = urhk_SRV_SetVal("dfltTdAcct.acctCommonInfo.acctInterestTax.wtaxFlg.code|W")
		PRINT(sv_e)
	ENDIF
	
	sv_e = urhk_SRV_SetVal("dfltTdAcct.acctCommonInfo.acctInterestTax.wtaxLevelFlg.code|A")
	PRINT(sv_e)
	
	sv_e = urhk_SRV_SetVal("dfltTdAcct.acctCommonInfo.acctInterestTax.wtaxAmountScopeFlg.code|P")
	PRINT(sv_e)
	
	
	TRACE OFF
END-->
