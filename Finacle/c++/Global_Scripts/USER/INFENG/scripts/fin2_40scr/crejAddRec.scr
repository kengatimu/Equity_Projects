#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: crejAddRec.scr
# Type: NA|default
# Description: Reject Reason Code Script
# Author: Anshul_Singh04
# Date: 5/7/18 3:34 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="de15bc33eaca69955bf022c18a134ac58e25f9f7e83e81aa91549132c25d7666"
	# Script body goes here
	TRACE ON
	
	#sv_u = urhk_B2K_PrintRepos("BANCS")
	#print(sv_u)
	
	sv_a = REPEXISTS("EQT")
	IF (sv_a == 0) THEN
	#{
		CREATEREP("EQT")
	#}
	ENDIF
	sv_a = CLASSEXISTS("EQT", "REJ")
	IF (sv_a == 0 ) THEN
	#{
		CREATECLASS("EQT", "REJ", 5)
	#}
	ENDIF
	
	EQT.REJ.msg = " "
	
	PRINT(BANCS.INPUT.rejCode)
	EQT.REJ.rejCode=BANCS.INPUT.rejCode
	
	PRINT(BANCS.INPUT.newRejCode)
	EQT.REJ.newRejCode = BANCS.INPUT.newRejCode
	
	##############################################################################
	# Deleting Data from MOD table
	##############################################################################
	
	sv_d = "DELETE FROM CUSTOM.RRCM_MOD WHERE bank_id=?SVAR AND OLD_CLG_REJ_CODE=?SVAR"
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + EQT.REJ.rejCode
	
	PRINT(BANCS.INPARAM.BINDVARS )
	sv_r = urhk_dbsqlWithBind(sv_d)
	print(sv_r)
	
	##############################################################################
	
	sv_w= "INSERT INTO CUSTOM.RRCM_MOD (OLD_CLG_REJ_CODE, CLG_REJ_CODE, ENTITY_CRE_FLG, RCRE_USER_ID, RCRE_TIME, LCHG_USER_ID, LCHG_TIME, BANK_ID)"
	
	#-------------------------------------------------
	# Getting the record values from front end
	#-------------------------------------------------
	
	sv_v =" VALUES (?SVAR, ?SVAR, 'N', ?SVAR, sysdate, ?SVAR, sysdate, ?SVAR)"
	sv_v = sv_w+sv_v
	print(sv_v)
	
	BANCS.INPARAM.BINDVARS = EQT.REJ.rejCode + "|" + EQT.REJ.newRejCode + "|" + BANCS.STDIN.userId +  "|" + BANCS.STDIN.userId + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_e = urhk_dbsqlWithBind(sv_v)
	print(sv_e)
	
	IF(sv_e!=0)THEN
	#{
		EQT.REJ.msg = "Error in Adding Records in DB!!!"
		GOTO ENDOFSCRIPT
	#}
	endif
	
	IF(BANCS.INPUT.funcCode=="A")THEN
	#{
		EQT.REJ.msg="Record has been Added Successfully."
	#}
	endif
	
	IF(BANCS.INPUT.funcCode=="M")THEN
	#{
		EQT.REJ.msg="Record has been Modified Successfully."
	#}
	endif
	
	ENDOFSCRIPT:
		
		sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
		#sv_u = urhk_SetOrbOut("Error_1|ERR^"+EQT.REJ.msg)
		
		EXITSCRIPT
		
END-->
