<--start
trace on
        sv_r = urhk_B2k_PrintRepos("BANCS")
        if(fieldexists(BANCS.STDIN.menuId)) then
                print(BANCS.STDIN.menuId)
                IF ((BANCS.STDIN.menuId == "HTM")) THEN
                        sv_u = urhk_SRV_GetVal("tranHdr.tranIdentifier.tranId")
                        print(sv_u)
                        IF(sv_u == 0) THEN
                                sv_a = BANCS.OUTPARAM.srvValue
                                print(sv_a)
                        ELSE
                                sv_a = ""
                        ENDIF
			if(BANCS.STDIN.contextBankId =="54")then
	                        CALL("vptmamlDataOut.scr")
			endif
			callscriptifexist("TranMakerCheckerCheck.scr")	
                ENDIF
        ENDIF
exitscript
trace off
end-->

