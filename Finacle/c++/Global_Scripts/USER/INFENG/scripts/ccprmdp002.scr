#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: ccprmdp002.scr
# Type: NA|default
# Description: script used to validate parameter id
# Author: haneesha.kanumuru
# Date: 8/1/18 2:07 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="e28be04383a2c3a0dc2b499cc17d8f1314e170add563e7a9e8d0d9e579d126b7"
	# Script body goes here
	TRACE ON
	#==========================================================*
	# create repository
	#==========================================================*
	sv_a = REPEXISTS("CUST")
	IF(sv_a == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	sv_r = CLASSEXISTS("CUST","CCPRM")
	IF (sv_r == 0) THEN
	#{
		CREATECLASS("CUST", "CCPRM", 5)
	#}
	ENDIF
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	
	
	CUST.CCPRM.errorFlg = "N"
	CUST.CCPRM.errorMsg = " "
	CUST.CCPRM.toCrncy = " "
	IF(FIELDEXISTS(BANCS.INPUT.crncy)) THEN
	#{
		IF(TRIM(BANCS.INPUT.crncy) != "")THEN
		#{
			CUST.CCPRM.crncy= TOUPPER(TRIM(BANCS.INPUT.crncy))
			PRINT(CUST.CCPRM.crncy)
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.CCPRM.errorFlg = "Y"
		CUST.CCPRM.errorMsg = "Invalid Input for fnExecuteScript"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	
	
	
	sv_s = "count|"
	sv_s = sv_s + " SELECT COUNT(*)"
	sv_s = sv_s + " FROM TBAADM.GAM "
	sv_s = sv_s + " WHERE ACCT_CRNCY_CODE = ?SVAR"
	#sv_s = sv_s + " AND DEL_FLG != 'Y'"
	#sv_s = sv_s + " AND ENTITY_CRE_FLG != 'N'"
	#sv_s = sv_s + " WHERE ACCT_NUM = ?SVAR"
	sv_s = sv_s + " AND BANK_ID = ?SVAR"
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS =   CUST.CCPRM.crncy + "|"+ BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	CUST.CCPRM.count = BANCS.OUTPARAM.count
	PRINT(CUST.CCPRM.count)
	
	
	IF( CUST.CCPRM.count ==0 )THEN
	#{
		#		CUST.CCPRM.acctNum = BANCS.OUTPARAM.acctNum
		#		PRINT(CUST.CCPRM.acctNum)
		#		#}
		#	ELSE
		#		#{
		CUST.CCPRM.errorFlg = "Y"
		CUST.CCPRM.errorMsg = "Invalid Currency"
	#}
	ENDIF
	ENDOFSCRIPT:
		sv_u = urhk_SetOrbOut("errorFlg|"+CUST.CCPRM.errorFlg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("errorMsg|"+CUST.CCPRM.errorMsg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("crncy|"+CUST.CCPRM.crncy)
		
		PRINT(sv_u)
		TRACE OFF
END-->

