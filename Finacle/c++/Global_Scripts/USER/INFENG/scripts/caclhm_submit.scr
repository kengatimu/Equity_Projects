#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Type: NA|default
# Modification Log: 
#============================================================

<--START
trace on
	###########################################################################
	# Create repository and class
	###########################################################################
	
	IF(REPEXISTS("CUST")==0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF(CLASSEXISTS("CUST","DATA")==0) THEN
	#{
		CREATECLASS("CUST","DATA",5)
	#}
	ENDIF
	sv_u = urhk_B2k_PrintRepos("BANCS")
	#upload service listener port number
        sv_r = urhk_getEnv("FILEUPLOAD_JLIMO_PORTNUM")
        print(sv_r)
        if (sv_r != 0) then
        #{
                sv_z = "SuccessOrFailure|N"
                sv_u = urhk_SetOrbOut(sv_z)
                sv_z = "Error_02|ERROR^ENV FILEUPLOAD_JLIMO_PORTNUM not set.Please contact your Administrator.^upldFile"
                sv_u = urhk_SetOrbOut(sv_z)
                EXIT
                #}
        else
                #{
                #print(BANCS.OUTPARAM.envValue)
                BANCS.INPUT.uploadPortNum = BANCS.OUTPARAM.envValue
		print(BANCS.INPUT.uploadPortNum)
        #}
        endif
	lv_b = BANCS.INPUT.uploadPortNum
	
	lv_a = SYSTEM("uploadclient LIMO://" + BANCS.INPUT.uploadIPAddress + ":" + lv_b + "/\ " + BANCS.INPUT.uploadFileName)
	
	IF(lv_a != 0) THEN
	#{
		lv_a = urhk_setorbErr("Error occurred while uploading the file")
		lv_a = urhk_setorbout("SuccessOrFailure|N")
		EXITSCRIPT
	#}
	ENDIF
	
	lv_f = TRIM(BANCS.INPUT.uploadFileName)
	lv_l = STRLEN(lv_f)
	lv_c = GETPOSITION(lv_f, '/')
	
	WHILE(lv_c != 0)
	#{
		lv_l = STRLEN(lv_f)
		lv_f = MID$(lv_f, lv_c, lv_l - lv_c)
		lv_c = GETPOSITION(lv_f, '/')
	#}
	DO
	
	lv_r = ""
	lv_p = lv_f
	PRINT(lv_f)
	CUST.DATA.fileName=lv_f

	CUST.DATA.fileformat	=	MID$(CUST.DATA.fileName,STRLEN(CUST.DATA.fileName) - 3,3)
	PRINT(CUST.DATA.fileformat)
	
	############################################################################
	# Validating File Extension
	############################################################################
	
	IF((TOUPPER(CUST.DATA.fileformat) != "CSV") AND (TOUPPER(CUST.DATA.fileformat) != "TXT"))THEN
	#IF(TOUPPER(CUST.DATA.fileformat) != "CSV")THEN
	#{
		lv_a = urhk_setorbErr("File Should be uploaded in CSV format only")
		lv_a = urhk_setorbout("SuccessOrFailure|N")
		EXITSCRIPT
	#}
	ENDIF
	lv_a = urhk_getEnv("FIN_REPORTS_DIR")
	IF (lv_a == 0) THEN
	#{
		lv_r = TRIM(BANCS.OUTPARAM.envValue)
	#}
	ENDIF
	
	IF((lv_a != 0) OR (TRIM(lv_r) == "")) THEN
	#{
		lv_q = "cnt|SELECT COUNT(CUSTOPTN_ID) FROM COMT WHERE CUSTOPTN_ID = 'FIN_REPORTS_DIR' AND BANK_ID = ?SVAR"
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
		lv_a = urhk_dbSelectWithBind(lv_q)
		IF(lv_a != 0) THEN
		#{
			lv_a = urhk_setorbErr("Error occurred while uploading the file")
			lv_a = urhk_setorbout("SuccessOrFailure|N")
			EXITSCRIPT
		#}
		ENDIF
		
		IF(BANCS.OUTPARAM.cnt == "1") THEN
		#{
			lv_q = "path|SELECT CUSTOPTN_VALUE FROM COMT WHERE CUSTOPTN_ID = 'FIN_REPORTS_DIR' AND BANK_ID = ?SVAR"
			BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
			lv_a = urhk_dbSelectWithBind(lv_q)
			IF(lv_a != 0) THEN
			#{
				lv_a = urhk_setorbErr("Error occurred while uploading the file")
				lv_a = urhk_setorbout("SuccessOrFailure|N")
				EXITSCRIPT
			#}
			ENDIF
			lv_r = TRIM(BANCS.OUTPARAM.path)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(lv_r != "") THEN
	#{
		IF ('/' != (CHARAT(lv_r, STRLEN(lv_r) - 1 ))) THEN
		#{
			lv_r = lv_r + '/'
		#}
		ENDIF
		lv_p = lv_r + BANCS.STDIN.userId + "/" + lv_f
		#}
	ELSE
		#{
		lv_a = urhk_getEnv("ONS_LOG_DIR")
		IF (lv_a == 0) THEN
		#{
			lv_r = TRIM(BANCS.OUTPARAM.envValue)
		#}
		ENDIF
		
		IF((lv_a != 0) OR (TRIM(lv_r) == "")) THEN
		#{
			lv_q = "cnt|SELECT COUNT(CUSTOPTN_ID) FROM COMT WHERE CUSTOPTN_ID = 'ONS_LOG_DIR' AND BANK_ID = ?SVAR"
			BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
			lv_a = urhk_dbSelectWithBind(lv_q)
			IF(lv_a != 0) THEN
			#{
				lv_a = urhk_setorbErr("Error occurred while uploading the file")
				lv_a = urhk_setorbout("SuccessOrFailure|N")
				EXITSCRIPT
			#}
			ENDIF
			
			IF(BANCS.OUTPARAM.cnt == "1") THEN
			#{
				lv_q = "path|SELECT CUSTOPTN_VALUE FROM COMT WHERE CUSTOPTN_ID = 'ONS_LOG_DIR' AND BANK_ID = ?SVAR"
				BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
				lv_a = urhk_dbSelectWithBind(lv_q)
				IF(lv_a != 0) THEN
				#{
					lv_a = urhk_setorbErr("Error occurred while uploading the file")
					lv_a = urhk_setorbout("SuccessOrFailure|N")
					EXITSCRIPT
				#}
				ENDIF
				lv_r = TRIM(BANCS.OUTPARAM.path)
			#}
			ENDIF
		#}
		ENDIF
		IF(lv_r != "") THEN
		#{
			IF ('/' != (CHARAT(lv_r, STRLEN(lv_r) - 1 ))) THEN
	                #{
                        	lv_r = lv_r + '/'
	                #}
       	         	ENDIF
			lv_p = lv_r + BANCS.STDIN.userId + "/" + lv_f
			#}
		ELSE
			#{
			lv_a = urhk_getEnv("TBA_PROD_ROOT")
			IF (lv_a == 0) THEN
			#{
				lv_r = TRIM(BANCS.OUTPARAM.envValue)
				IF ('/' != (CHARAT(lv_r, STRLEN(lv_r) - 1 ))) THEN
                        	#{
                                	lv_r = lv_r + '/'
                        	#}
                        	ENDIF
				lv_p = lv_r + "CDCI_LOGS/" + lv_f
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF

	
	
	lv_q = "DELETE FROM PQT WHERE PRINT_FILE_NAME = ?SVAR AND BANK_ID = ?SVAR"
	BANCS.INPARAM.BINDVARS = lv_p + "|" + BANCS.STDIN.contextBankId
	lv_a = urhk_dbSQLWithBind(lv_q)
	lv_a = urhk_dbSQLWithBind("COMMIT")
	
	lv_a = SYSTEM("babx4040 $B2K_SESSION_ID '" + lv_f + "' 'CACLHM File Upload' " + BANCS.STDIN.userId + " 1 N")
	
	IF(lv_a == 0) THEN
	#{
		lv_a = urhk_setorbout("SuccessOrFailure|Y")
		lv_a = urhk_setorbout("RESULT_MSG|File uploaded Successfully and under processing. Check HPR menu to view the status of uploaded file.")
		call("parseCACLHMUpld.scr")
		#}
	ELSE
		#{
		lv_a = urhk_setorbErr("Error occurred while uploading the file")
		lv_a = urhk_setorbout("SuccessOrFailure|N")
	#}
	ENDIF
	
END-->
