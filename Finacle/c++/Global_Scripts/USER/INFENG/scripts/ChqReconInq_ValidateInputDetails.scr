#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: ChqReconInq_ValidateInputDetails.scr
# event: NA
# description: NA
#=================================================
#----------------------------------------------------------------------------------------------------------
# Filename              :       ChqReconInq_ValidateInputDetails.scr
# Author                :       Nitin Prabhakar
# Description           :       This script validates the values passed in request XMl.
# Input                 :       5 input fields fetched values.
# Output                :       Success or Failure
# Called Script         :       ChqReconInq_main.scr
# Calling Script        :       None
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----           -----           --------          ----------------        ---------
#  1           23-07-2013      Nitin Prabhakar       Original Version          1.0
#-----------------------------------------------------------------------------------------------------------
<--start
TRACE ON
#-------------------------------------------------------------------------------------------------
# Validate Zone ID
#-------------------------------------------------------------------------------------------------
IF(CUST.INTERFACE.ZoneID == "") THEN
#{
	# Zone Id is Mandatory.
	CUST.INTERFACE.successOrFailure = "F"
	CUST.INTERFACE.errorCode = "ERR096"
	GOTO ERROREXIT
#}
ENDIF
#-------------------------------------------------------------------------------------------------
# Validate Zone Date
#-------------------------------------------------------------------------------------------------
IF(CUST.INTERFACE.ZoneDate != "") THEN
#{
	sv_u = urhk_B2k_valDate(CUST.INTERFACE.ZoneDate)
	print(sv_u)
	IF(sv_u != 0) THEN
	#{
		# Invalid Date Format.
		CUST.INTERFACE.successOrFailure = "F"
		CUST.INTERFACE.errorCode = "ERR097"
		GOTO ERROREXIT
	#}
	ENDIF
#}
ELSE
#{
	# Zone Date is Mandatory.
	CUST.INTERFACE.successOrFailure = "F"
	CUST.INTERFACE.errorCode = "ERR098"
	GOTO ERROREXIT
#}
ENDIF
#-------------------------------------------------------------------------------------------------
# Validate Zone Identifier
#-------------------------------------------------------------------------------------------------
IF(CUST.INTERFACE.ZoneIdentifier != "") THEN
#{
	IF((CUST.INTERFACE.ZoneIdentifier != "O") AND (CUST.INTERFACE.ZoneIdentifier != "T")) THEN
	#{
		# Invalid Zone Identifier.
		CUST.INTERFACE.successOrFailure = "F"
		CUST.INTERFACE.errorCode = "ERR100"
		GOTO ERROREXIT
	#}
	ENDIF
#}
ELSE
#{
	# Zone Identifier is Mandatory.
	CUST.INTERFACE.successOrFailure = "F"
	CUST.INTERFACE.errorCode = "ERR099"
	GOTO ERROREXIT
#}
ENDIF
#-------------------------------------------------------------------------------------------------
# Other Validations
#-------------------------------------------------------------------------------------------------
BANCS.INPARAM.BINDVARS = CUST.INTERFACE.ZoneID + "|" + CUST.INTERFACE.bankId
print(BANCS.INPARAM.BINDVARS)
sv_s = "zoneCrncy,homeClgFlg|"
sv_s = sv_s + " SELECT ZONE_CRNCY_CODE,HOME_CLG_FLG"
sv_s = sv_s + " FROM TBAADM.OZC "
sv_s = sv_s + " WHERE CLG_ZONE_CODE = ?SVAR "
sv_s = sv_s + " AND BANK_ID = ?SVAR "
sv_d = urhk_dbSelectWithBind(sv_s)
print(sv_d)
IF(sv_d != 0)THEN
#{
	# Invalid Zone Id.
	CUST.INTERFACE.successOrFailure = "F"
	CUST.INTERFACE.errorCode = "ERR101"
	GOTO ERROREXIT
#}
ELSE
#{
	CUST.INTERFACE.zoneCrncy = BANCS.OUTPARAM.zoneCrncy
	print(CUST.INTERFACE.zoneCrncy)
	CUST.INTERFACE.homeClgFlg = BANCS.OUTPARAM.homeClgFlg
	print(CUST.INTERFACE.homeClgFlg)
#}
ENDIF
# Zone Id and Zone CYY
IF(CUST.INTERFACE.ZoneCcy != CUST.INTERFACE.zoneCrncy) THEN
#{
	# Invalid combination of ZoneID and ZoneCurrency
	CUST.INTERFACE.successOrFailure = "F"
	CUST.INTERFACE.errorCode = "ERR102"
	GOTO ERROREXIT
#}
ENDIF
# Zone Id and Zone Identifier
IF(CUST.INTERFACE.ZoneIdentifier != CUST.INTERFACE.homeClgFlg) THEN
#{
	# Invalid combination of ZoneID and ZoneIdentifier
	CUST.INTERFACE.successOrFailure = "F"
	CUST.INTERFACE.errorCode = "ERR103"
	GOTO ERROREXIT
#}
ENDIF
#-------------------------------------------------------------------------------------------------
# Validate Tran Id
#-------------------------------------------------------------------------------------------------
IF(CUST.INTERFACE.TranID == "") THEN
#{
	# Tran Id is Mandatory.
	CUST.INTERFACE.successOrFailure = "F"
	CUST.INTERFACE.errorCode = "ERR105"
	GOTO ERROREXIT
#}
ENDIF
EXITSCRIPT
end-->
