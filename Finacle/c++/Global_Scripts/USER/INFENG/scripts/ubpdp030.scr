#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: ubpdp030.scr
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START
TRACE ON
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp030.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called from ubp_det_link.js on click of get bill 
	#                               details button for KRA biller.
	# Called Script         :		
	# Calling function      :		function fnGetBillDtl()
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           16-04-2013         Swati Ganjoo      Original Version          1.0
	#  2           28-08-2013         Swati Ganjoo      Initialized the varaiable  1.1
	#-----------------------------------------------------------------------------------------------------------
	# Script body goes here
	#TRACE ON
	#========================================================
	# Check whether CUST Repository exists else create it
	#========================================================
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	#========================================================
	# Check whether UBP Class exists else create it
	#========================================================
	sv_r = CLASSEXISTS("CUST","UBP")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","UBP",5)
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST","INTERFACE") == 0) THEN
	#{
		CREATECLASS ("CUST","INTERFACE",5)
	#}
	ENDIF


	#========================================================
	# Initialize the variables
	#========================================================
	CUST.UBP.errorFlg="N"
	CUST.UBP.errorMsg=" "
	#CUST.INTERFACE.goFlg="N"
	CUST.INTERFACE.FlgType="BUTTONCLICK"
	CUST.UBP.departCode=" "
	CUST.UBP.docRefNum=" "
	CUST.UBP.regionCode=" "
	CUST.UBP.slipPayCode=" "
	CUST.UBP.stationCode=" "
	CUST.UBP.sysCode=" "
	CUST.UBP.billerValReq="N"
	CUST.UBP.tranPart=" "
	CUST.UBP.amount= " "
	CUST.UBP.rateCode = " "
	CUST.UBP.rate = " "
	CUST.INTERFACE.tranStatus = ""
	CUST.INTERFACE.customerName = ""
	CUST.INTERFACE.amount =""
	CUST.INTERFACE.billNumber = ""
CUST.INTERFACE.amount		= ""
CUST.INTERFACE.billNumber   = ""
CUST.INTERFACE.customerName = ""
CUST.UBP.tranPart           = ""
CUST.INTERFACE.departCode   = ""
CUST.INTERFACE.docRefNum    = ""
CUST.INTERFACE.regionCode   = ""
CUST.INTERFACE.slipPayCode  = ""
CUST.INTERFACE.stationCode  = ""
CUST.INTERFACE.sysCode      = ""
	
	#Added
	CUST.UBP.getServiceName1=" "
	IF(FIELDEXISTS(BANCS.INPUT.billerId))THEN
	#{
		IF(TRIM(BANCS.INPUT.billerId) != "")THEN
		#{
			CUST.UBP.billerId=BANCS.INPUT.billerId
			PRINT(CUST.UBP.billerId)

                        BANCS.INPARAM.BINDVARS = CUST.UBP.billerId+"|"+ "Y|N|" + BANCS.STDIN.contextBankId
                        sv_s = "valType,account|"
                        sv_s = sv_s + " SELECT FREE_TEXT4,free_text1"
                        sv_s = sv_s + " FROM TBAADM.BRGT "
                        sv_s = sv_s + " WHERE BILLER_ID=?SVAR"
                        sv_s = sv_s + " AND NVL(ENTITY_CRE_FLG, 'Y') = ?SVAR"
                        sv_s = sv_s + " AND NVL(DEL_FLG, 'N') = ?SVAR"
                        sv_s = sv_s + " AND BANK_ID = ?SVAR"
                        PRINT(sv_s)
                        sv_d = urhk_dbSelectWithBind(sv_s)
                        IF(sv_d==0)THEN
                        #{
                                CUST.UBP.valType = BANCS.OUTPARAM.valType
                                PRINT(CUST.UBP.valType)
				CUST.UBP.account = BANCS.OUTPARAM.account
                                PRINT(CUST.UBP.account)
                        #}
                        ELSE
                        #{
                                CUST.UBP.valType=""
				CUST.UBP.account=""
                        #}
                        ENDIF

		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.billRefNum))THEN
	#{
		IF(TRIM(BANCS.INPUT.billRefNum) != "")THEN
		#{
			CUST.UBP.billRefNum=BANCS.INPUT.billRefNum
			PRINT(CUST.UBP.billRefNum)
		#}
		ENDIF
		sv_m = mid$(BANCS.INPUT.billRefNum,2,3)
		sv_n = mid$(BANCS.INPUT.billRefNum,0,2)
		IF (BANCS.STDIN.contextBankId =="55") THEN
			IF (sv_n =="99") THEN
				IF (sv_m !=BANCS.INPUT.billerId) THEN
					CUST.UBP.errorFlg="Y"
					CUST.UBP.errorMsg= "Bill Number does not match Biller service"
					PRINT(CUST.UBP.errorMsg)
					GOTO ENDOFSCRIPT
				ENDIF
			ENDIF
		ENDIF
	#}
	ENDIF

	IF(FIELDEXISTS(BANCS.INPUT.rateCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.rateCode) != "")THEN
		#{
			CUST.UBP.rateCode=BANCS.INPUT.rateCode
			PRINT(CUST.UBP.rateCode)
		#}
		ENDIF
	#}
	ENDIF

	IF(FIELDEXISTS(BANCS.INPUT.rate))THEN
	#{
		IF(TRIM(BANCS.INPUT.rate) != "")THEN
		#{
			CUST.UBP.rate=BANCS.INPUT.rate
			PRINT(CUST.UBP.rate)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.billAmt))THEN
	#{
		IF(TRIM(BANCS.INPUT.billAmt) != "")THEN
		#{
			CUST.UBP.billAmt=BANCS.INPUT.billAmt
			PRINT(CUST.UBP.billAmt)	
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.freetext1))THEN
	#{
		IF(TRIM(BANCS.INPUT.freetext1) != "")THEN
		#{
			CUST.UBP.freetext1=BANCS.INPUT.freetext1
			PRINT(CUST.UBP.freetext1)
		#}
		ELSE
		#{
			CUST.UBP.freetext1=""
		#}
		ENDIF
	#}
	ELSE
	#{
		CUST.UBP.freetext1=""
	#}
	ENDIF
	print(CUST.UBP.freetext1)

	IF(FIELDEXISTS(BANCS.INPUT.location))THEN
	#{
		IF(TRIM(BANCS.INPUT.location) != "")THEN
		#{
			CUST.UBP.location=BANCS.INPUT.location
			PRINT(CUST.UBP.location)
		#}
		ELSE
		#{
			IF(CUST.UBP.billerId == "2587")THEN
			#{
				CUST.UBP.location=""
				CUST.UBP.errorFlg="Y"
                                CUST.UBP.errorMsg="Location is mandatory "
				CUST.UBP.billerValReq="Y"
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			CUST.UBP.location=""
		#}
		ENDIF
	#}
	ELSE
	#{
		CUST.UBP.location=""
	#}
	ENDIF
	print(CUST.UBP.location)

	IF(FIELDEXISTS(BANCS.INPUT.phoneNumber))THEN
	#{
		IF(TRIM(BANCS.INPUT.phoneNumber) != "")THEN
		#{
			CUST.UBP.phoneNumber=BANCS.INPUT.phoneNumber
			PRINT(CUST.UBP.phoneNumber)
		#}
		ELSE
		#{
			CUST.UBP.phoneNumber=""
		#}
		ENDIF
	#}
	ELSE
	#{
		CUST.UBP.phoneNumber=""
	#}
	ENDIF 
	print(CUST.UBP.phoneNumber)
	IF(FIELDEXISTS(BANCS.INPUT.billerCode))THEN
		#{
			IF(TRIM(BANCS.INPUT.billerCode) != "")THEN
			#{
				CUST.UBP.billerCode=BANCS.INPUT.billerCode
				PRINT(CUST.UBP.billerCode)
			#}
			ELSE
			#{
				CUST.UBP.billerCode=""
			#}
			ENDIF
		#}
		ELSE
		#{
			CUST.UBP.billerCode=""
		#}
	ENDIF

##----------------------------------------------------------------------------------------------------
IF (BANCS.STDIN.contextBankId == "50") THEN
#{
	BANCS.INPUT.moduleName = "UBP"
	BANCS.INPUT.parameterId = "RRAUBP"
	CALL("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.UBP.rraBiller = BANCS.OUTPUT.parameterVal
	#}
	ELSE
	#{
		sv_u = urhk_SRV_SetErr("Biller list setup not done in CPARAM")
		BANCS.OUTPUT.successOrFailure = "F"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
#}
ENDIF
##----------------------------------------------------------------------------------------------------


	IF ((CUST.UBP.billerId == "1230") AND ((BANCS.STDIN.contextBankId == "54") OR (BANCS.STDIN.contextBankId == "43"))) THEN
	#{
		PRINT(CUST.UBP.rateCode)
		PRINT(CUST.UBP.rate)
		CUST.UBP.billAmt=CDOUBLE(BANCS.INPUT.billAmt)
		PRINT(CUST.UBP.billAmt)

		BANCS.INPARAM.InputAmount = CUST.UBP.billAmt
		BANCS.INPARAM.FromCurrency = BANCS.STDIN.homeCrncyCode
		BANCS.INPARAM.ToCurrency = "UGX"
		BANCS.INPARAM.RateCode = CUST.UBP.rateCode
		BANCS.INPARAM.Rate = CUST.UBP.rate
		BANCS.INPARAM.CustId = ""
		
		sv_u = urhk_B2k_ConvertAmount("")
		PRINT(sv_u)
		
		IF(sv_u == 0)THEN
		#{
			CUST.UBP.billAmt = cint(BANCS.OUTPARAM.OutputAmount)
			PRINT(CUST.UBP.billAmt)
			CUST.UBP.billAmt =  FORMAT$(CDOUBLE(CUST.UBP.billAmt),"%.2f")
			PRINT(CUST.UBP.billAmt)

		#}
		ELSE
		#{
			CUST.UBP.errorFlg="N"
			CUST.UBP.errorMsg= "Unable to derive charge amount"
			PRINT(CUST.UBP.errorMsg)
		#}
		ENDIF
	#}
	ENDIF

	IF ((CUST.UBP.billerId == "1044") AND (BANCS.STDIN.contextBankId != "54")) THEN
	#{
		PRINT(CUST.UBP.rateCode)
		PRINT(CUST.UBP.rate)
		CUST.UBP.billAmt=CDOUBLE(BANCS.INPUT.billAmt)
		PRINT(CUST.UBP.billAmt)

		BANCS.INPARAM.InputAmount = CUST.UBP.billAmt
		BANCS.INPARAM.FromCurrency = BANCS.STDIN.homeCrncyCode
		BANCS.INPARAM.ToCurrency = "KES"
		BANCS.INPARAM.RateCode = CUST.UBP.rateCode
		BANCS.INPARAM.Rate = CUST.UBP.rate
		BANCS.INPARAM.CustId = ""
		
		sv_u = urhk_B2k_ConvertAmount("")
		PRINT(sv_u)
		
		IF(sv_u == 0)THEN
		#{
			CUST.UBP.billAmt = cint(BANCS.OUTPARAM.OutputAmount)
			PRINT(CUST.UBP.billAmt)
			CUST.UBP.billAmt =  FORMAT$(CDOUBLE(CUST.UBP.billAmt),"%.2f")
			PRINT(CUST.UBP.billAmt)

		#}
		ELSE
		#{
			CUST.UBP.errorFlg="N"
			CUST.UBP.errorMsg= "Unable to derive charge amount"
			PRINT(CUST.UBP.errorMsg)
		#}
		ENDIF
	#}
	ENDIF

	#CHANGE#
	#CHECKING IF CPARAM FOR BUTTON CLICK EXISTS OR NOT
	call("ubpdp078.scr")
	IF(CUST.UBP.cparamErrorFlg=="N")THEN
	#{
		IF(CUST.UBP.billerValReq=="Y")THEN
		#{
			CUST.UBP.notAndValFlg="N"
			CALL("FIOutbound_connection.scr")
			IF(CUST.INTERFACE.errorMsgFlg=="Y")THEN
			#{
					CUST.UBP.errorFlg="Y"
					CUST.UBP.errorMsg=CUST.INTERFACE.errorMsgDesc
					IF(CUST.UBP.errorMsg =="")THEN
					#{
						CUST.UBP.errorMsg=CUST.UBP.errorDesc
					#}
					ENDIF
					PRINT(CUST.UBP.errorMsg)
					GOTO ENDOFSCRIPT
			#}
			ELSE
			#{

					CUST.UBP.amount =  FORMAT$(CDOUBLE(CUST.INTERFACE.amount),"%.2f")
					print(CUST.UBP.amount)
					CUST.UBP.tranPart = CUST.INTERFACE.billNumber + " " +  CUST.INTERFACE.customerName
					CUST.UBP.tranPart = MID$(CUST.UBP.tranPart,0,49)	
					CUST.UBP.departCode=CUST.INTERFACE.departCode
					CUST.UBP.docRefNum=CUST.INTERFACE.docRefNum
					CUST.UBP.regionCode=CUST.INTERFACE.regionCode
					CUST.UBP.slipPayCode=CUST.INTERFACE.slipPayCode
					CUST.UBP.stationCode=CUST.INTERFACE.stationCode
					CUST.UBP.sysCode=CUST.INTERFACE.sysCode
					PRINT(CUST.UBP.departCode)
					PRINT(CUST.UBP.docRefNum)
					PRINT(CUST.UBP.regionCode)
					PRINT(CUST.UBP.slipPayCode)
					PRINT(CUST.UBP.stationCode)
					PRINT(CUST.UBP.sysCode)

					IF(CUST.UBP.departCode == "") THEN
					#{
						CUST.UBP.departCode = " "
					#}
					ENDIF
					IF(CUST.UBP.docRefNum == "") THEN
					#{
						CUST.UBP.docRefNum = " "
					#}
					ENDIF
					IF(CUST.UBP.regionCode == "") THEN
					#{
						CUST.UBP.regionCode = " "
					#}
					ENDIF
					IF(CUST.UBP.slipPayCode == "") THEN
					#{
						CUST.UBP.slipPayCode = " "
					#}
					ENDIF
					IF(CUST.UBP.stationCode == "") THEN
					#{
						CUST.UBP.stationCode = " "
					#}
					ENDIF
					IF(CUST.UBP.sysCode == "") THEN
					#{
						CUST.UBP.sysCode = " "
					#}
					ENDIF
					PRINT(CUST.UBP.docRefNum)
					PRINT(CUST.UBP.regionCode)
					PRINT(CUST.UBP.slipPayCode)
					PRINT(CUST.UBP.stationCode)
					PRINT(CUST.UBP.sysCode)

					#CUST.INTERFACE.status="success"
					IF(CUST.INTERFACE.status=="false")THEN
					#{
						CUST.UBP.errorFlg="Y"
						CUST.UBP.errorMsg=CUST.INTERFACE.message
						PRINT(CUST.UBP.errorMsg)
						GOTO ENDOFSCRIPT
					#}	
					ENDIF
					
			#}
			ENDIF
		#}
		ENDIF
		
	#}
	ENDIF

	CUST.UBP.billAmt = cdouble(CUST.UBP.billAmt)
	print(CUST.UBP.billAmt)
	CUST.UBP.amount = cdouble(CUST.UBP.amount)
	print(CUST.UBP.amount)

	IF ((CUST.UBP.billerId == "1071") OR (CUST.UBP.billerId == "1230") OR (CUST.UBP.billerId == "2560005") OR (CUST.UBP.billerId == "2560004") OR (CUST.UBP.billerId == "1002") OR (CUST.UBP.billerId == "1003") OR (CUST.UBP.billerId == "2961") OR (CUST.UBP.billerId == "101320")) THEN
	#{
		IF(CUST.UBP.billAmt != CUST.UBP.amount)THEN
		#{
		CUST.UBP.errorFlg="Y"
		CUST.UBP.errorMsg= "The Entered Bill Amount is not Equal to E-Slip Amount"
		PRINT(CUST.UBP.errorMsg)
		GOTO ENDOFSCRIPT
		#}	
		ENDIF
	#}	
	ENDIF
	
	
	#----CHG0036840 for School Pay
	IF ((CUST.UBP.billerId == "SCHPAY") AND (BANCS.STDIN.contextBankId == "56") AND (CUST.UBP.sysCode == "false")) THEN
	#{
			IF(CDOUBLE(CUST.UBP.billAmt) < CDOUBLE(CUST.UBP.amount))THEN
			#{
					CUST.UBP.errorFlg="Y"
					CUST.UBP.errorMsg= "The Entered Bill Amount is not Equal to Amount owed "
					CUST.UBP.errorMsg = CUST.UBP.errorMsg + CUST.UBP.amount
					PRINT(CUST.UBP.errorMsg)
					GOTO ENDOFSCRIPT
			#}
			ENDIF
	#}
	ENDIF


	##----CHG0031249-------------------by Jane--------------------------------
	IF ((CUST.UBP.billerId == "1230") AND (BANCS.STDIN.contextBankId == "56")) THEN
	#{

		sv_z ="count|select count(*) from tbaadm.dtd where acid in (select acid from tbaadm.gam where foracid= ?SVAR) "
		sv_z =sv_z + "and ref_num= ?SVAR and PART_TRAN_TYPE='C' and del_flg='N' and pstd_flg='Y' and bank_id= ?SVAR "
		print(sv_z)
		BANCS.INPARAM.BINDVARS = CUST.UBP.account + "|" + CUST.UBP.billRefNum + "|" + BANCS.STDIN.contextBankId
		print(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_z)
		print(sv_d)
		IF (sv_d == 0) THEN
			CUST.UBP.count = BANCS.OUTPARAM.count
			print(CUST.UBP.count)
			IF (CUST.UBP.count>0) THEN
				CUST.UBP.errorFlg="Y"
				CUST.UBP.errorMsg= "PRN ALREADY PROCESSED"
				PRINT(CUST.UBP.errorMsg)
				GOTO ENDOFSCRIPT

			ENDIF
		ELSE
			CUST.UBP.count = "0"
		ENDIF

	#}
	ENDIF

	##-------------------------------------------------------------------CHG0035285---by Jane--------------------------------
        IF ((CUST.UBP.billerId == "1071") AND (BANCS.STDIN.contextBankId == "54")) THEN
        #{

                sv_z ="count|select count(*) from tbaadm.dtd where acid in (select acid from tbaadm.gam where foracid= ?SVAR) "
                sv_z =sv_z + "and ref_num= ?SVAR and PART_TRAN_TYPE='C' and del_flg='N' and bank_id= ?SVAR "
                print(sv_z)
                BANCS.INPARAM.BINDVARS = CUST.UBP.account + "|" + CUST.UBP.billRefNum + "|" + BANCS.STDIN.contextBankId
                print(BANCS.INPARAM.BINDVARS)
                sv_d = urhk_dbSelectWithBind(sv_z)
                print(sv_d)
                IF (sv_d == 0) THEN
                        CUST.UBP.count = BANCS.OUTPARAM.count
                        print(CUST.UBP.count)
                        IF (CUST.UBP.count>0) THEN
                                CUST.UBP.errorFlg="Y"
                                CUST.UBP.errorMsg= "PRN ALREADY PROCESSED"
                                PRINT(CUST.UBP.errorMsg)
                                GOTO ENDOFSCRIPT

                        ENDIF
                ELSE
                        CUST.UBP.count = "0"
                ENDIF

        #}
        ENDIF

	##----------------------------------------------------------------------------------------------------
	IF (BANCS.STDIN.contextBankId == "50") THEN
	#{
		sv_a=getPosition(CUST.UBP.rraBiller,"," + CUST.UBP.billerId + ",")
		print(sv_a)
		IF (sv_a > 0) THEN
		#{
			IF( cint(CUST.UBP.billAmt) > cint(CUST.UBP.amount)) THEN
			#{
			CUST.UBP.errorFlg="Y"
			CUST.UBP.errorMsg= "Overpayment Amount to this Biller not allowed "
			PRINT(CUST.UBP.errorMsg)
			GOTO ENDOFSCRIPT
			#}	
			ENDIF
		#}	
		ENDIF
	#}	
	ENDIF
	##----------------------------------------------------------------------------------------------------

	IF (BANCS.INPUT.billerId=="1369") THEN
	#{
		print(BANCS.INPUT.billRefNum)
		BANCS.INPUT.billRefNum1=MID$(BANCS.INPUT.billRefNum,0,1)
		print(BANCS.INPUT.billRefNum1)

		IF(BANCS.INPUT.billRefNum1=="E") THEN
		#{
			IF(CUST.UBP.billAmt != CUST.UBP.amount)THEN
			#{
				CUST.UBP.errorFlg="Y"
				CUST.UBP.errorMsg= "The Entered Bill Amount is not Equal to E-Slip Amount"
				PRINT(CUST.UBP.errorMsg)
				GOTO ENDOFSCRIPT
			#}	
			ENDIF
		#}
		ENDIF

	#}
	ENDIF

	IF ((CUST.UBP.billerId == "1230") AND ((BANCS.STDIN.contextBankId == "54") OR (BANCS.STDIN.contextBankId == "43"))) THEN
	#{

		BANCS.INPARAM.BINDVARS =  CUST.UBP.billAmt +"|"+ CUST.UBP.billRefNum
		print(BANCS.INPARAM.BINDVARS)

		sv_s = "Counter|"
		sv_s = sv_s + "select count(*) from tbaadm.dtd where bank_id = '56' "
		sv_s = sv_s + "and TRAN_DATE = (select db_stat_date from tbaadm.gct where bank_id = '56') "
		sv_s = sv_s + "and tran_amt = ?SVAR "
		sv_s = sv_s + "AND RCRE_USER_ID = 'FIVUSR56' " 
		sv_s = sv_s + "AND REF_NUM = ?SVAR and PART_TRAN_TYPE = 'C'"
		print(sv_s)
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		IF(sv_d==0)THEN
		#{
			print(BANCS.OUTPARAM.Counter)
			CUST.UBP.Counter = CINT(BANCS.OUTPARAM.Counter)
			PRINT(CUST.UBP.Counter)
			IF(CUST.UBP.Counter > 0) THEN
			#{
				CUST.UBP.errorFlg="Y"
				CUST.UBP.errorMsg= "Transaction already created in Finacle"
				PRINT(CUST.UBP.errorMsg)
				GOTO ENDOFSCRIPT
			#}
			ENDIF

		#}
		ELSE
		#{
			CUST.UBP.Counter=""
		#}
		ENDIF
	#}
	ENDIF
		
	ENDOFSCRIPT:

		
                #------------------------------------------------------------------------
		# Removing Quotes from data from Remote billers
		#------------------------------------------------------------------------
		sv_s = "tranPart,custName|"
		#sv_s = sv_s + " select replace(?SVAR,'''','&#8217'), "
		#sv_s = sv_s + " replace(?SVAR,'''','&#8217') "
	        sv_s = sv_s + " select replace(?SVAR,'''','_'), "
                sv_s = sv_s + " replace(?SVAR,'''','_') " 	
		sv_s = sv_s + " from dual "
		print(sv_s)
		
		BANCS.INPARAM.BINDVARS = CUST.UBP.tranPart +"|"+ CUST.INTERFACE.customerName
                print(BANCS.INPARAM.BINDVARS)

		sv_d = urhk_dbSelectWithBind(sv_s)
		
		IF(sv_d==0)THEN
		#{	
			IF ((CUST.UBP.billerId == "1230") AND ((BANCS.STDIN.contextBankId == "54") OR (BANCS.STDIN.contextBankId == "43"))) THEN
			#{
			CUST.INTERFACE.customerName = BANCS.STDIN.mySolId
			PRINT(CUST.INTERFACE.customerName)
		
			ELSE
			#{
			CUST.INTERFACE.customerName = BANCS.OUTPARAM.custName
			CUST.UBP.tranPart = BANCS.OUTPARAM.tranPart
			PRINT(CUST.UBP.tranPart)
			PRINT(CUST.INTERFACE.customerName)
			#}
			ENDIF

		#}
		ELSE
		#{
			CUST.INTERFACE.customerName=""
			CUST.UBP.tranPart = ""
		#}
		ENDIF	
		
		sv_s = "errorFlg|" + CUST.UBP.errorFlg
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
           	if(CUST.INTERFACE.tranStatus == "FAILURE")THEN
                #{
                        CUST.UBP.errorMsg = CUST.INTERFACE.message
                #}
                ENDIF

		sv_s = "errorMsg|" + CUST.UBP.errorMsg
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "departCode|" + CUST.UBP.departCode
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "docRefNum|" + CUST.UBP.docRefNum
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "regionCode|" + CUST.UBP.regionCode
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "slipPayCode|" + CUST.UBP.slipPayCode
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "stationCode|" + CUST.UBP.stationCode
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		IF(CUST.UBP.billerId == "2587")THEN
		#{
			# For biller NWA syscode shoule be send as the location
			CUST.UBP.sysCode= CUST.UBP.location
			print(CUST.UBP.sysCode)
		#}
		ENDIF

		sv_s = "sysCode| " + CUST.UBP.sysCode
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "billerValReq|" + CUST.UBP.billerValReq
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "chanAmt| " + CUST.UBP.amount
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)

                sv_s = "tranPart| " + CUST.UBP.tranPart
                PRINT(sv_s)
                sv_u = urhk_SetOrbOut(sv_s)
                PRINT(sv_u)

                sv_s = "customerName| " + CUST.INTERFACE.customerName
                PRINT(sv_s)
                sv_u = urhk_SetOrbOut(sv_s)
                PRINT(sv_u)


		#TRACE OFF
		EXITSCRIPT
END-->
