#**************************************************************************************************************
# Filename              : accmdp011.scr
# Description           : The script is used to cancel the created contract which are unverifed
# Input                 :
# Output                :
# Called Script         : Common_Interface_GetErrorDesc.scr
# Calling Script        : accmmn002.scr
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----          ------           --------          ----------------        ---------
#   1          25-03-2013       Kalvin Joseph     Original Version          1.0
#   2		23-08-2013	Kalvin		 Modified Version for TO:361667	    1.1
#   3		29-08-2013	Kalvin		 Modified Version for TO:366774	    1.2
#**************************************************************************************************************


<--START
CHECKSUM="fd4a1fadaeea5f7051e7af71b5710c83e251590be33e9b5ae12d69417794c9d0"
	TRACE ON
	
	PRINT(CUST.CONTRACT.funcCode)
	PRINT(CUST.CONTRACT.acctName)
	PRINT(CUST.CONTRACT.acctNo)
	PRINT(CUST.CONTRACT.solId)
	PRINT(CUST.CONTRACT.schmCode)
	PRINT(CUST.CONTRACT.minAmt)
	PRINT(CUST.CONTRACT.maxAmt)
	PRINT(CUST.CONTRACT.contractAmt)
	PRINT(CUST.CONTRACT.contractPeriod)
	PRINT(CUST.CONTRACT.contractId)
	#PRINT(CUST.CONTRACT.charges)
	PRINT(CUST.CONTRACT.maxContractPeriod)
	CUST.CONTRACT.remarks=""
	BANCS.INPARAM.BINDVARS = CUST.CONTRACT.contractId +"|"+ CUST.CONTRACT.acctNo+ "|"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS+ "N"+"|"+"N"
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_s = "count1|"
	sv_s = sv_s + "SELECT COUNT(1) FROM CUSTOM.CATOD_SANCTION_MOD_TABLE "
	sv_s = sv_s + " WHERE REQUEST_ID=?SVAR "
	sv_s = sv_s + " AND ACCT_NUM=?SVAR "
	sv_s = sv_s + " AND DEL_FLG=?SVAR "
	sv_s = sv_s + " AND ENTITY_CRE_FLG=?SVAR"
	PRINT(sv_s)
	sv_d= urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	IF(sv_d==0) THEN
	#{
		
		#{
		CUST.CONTRACT.modCount=BANCS.OUTPARAM.count1
		PRINT(CUST.CONTRACT.modCount)
		
		IF(	CUST.CONTRACT.modCount!=0) THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CONTRACT.contractId +"|"+ BANCS.STDIN.contextBankId
			BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS+"|"+ "N"+"|"+"N"
			
			sv_s = "DELETE FROM CUSTOM.CATOD_SANCTION_MOD_TABLE"
			sv_s = sv_s + " WHERE REQUEST_ID=?SVAR"
			sv_s = sv_s + " AND BANK_ID = ?SVAR"
			sv_s = sv_s + " AND ENTITY_CRE_FLG = ?SVAR"
			sv_s = sv_s + " AND DEL_FLG = ?SVAR"
			PRINT(sv_s)
			sv_d= urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d!=0) THEN
			#{
				CUST.INTERFACE.errorCode="ACCM_ERR_021"
				CALL("Common_Interface_GetErrorDesc.scr")
				IF ( CUST.INTERFACE.SuccessFlag == "1") THEN
				#{
					sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
					CUST.CONTRACT.errorMsg = "Could not fetch Parameter value for " +  CUST.INTERFACE.errorCode
					sv_z = "Error_0|ERROR^" + CUST.CONTRACT.errorMsg + "^contrctAmt"
					sv_u = urhk_SetOrbOut(sv_z)
					CUST.CONTRACT.successFlg = "F"
					GOTO ENDOFSCRIPT
					#}
				ELSE
					#{
					CUST.CONTRACT.errorMsg=CUST.INTERFACE.errorDesc
					sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
					print(sv_u)
					sv_u = urhk_SetOrbOut("Error_0|ERROR^" + CUST.CONTRACT.errorMsg + "^contrctAmt")
					print(sv_u)
					GOTO ENDOFSCRIPT
				#}
				ENDIF
				#}
			ELSE
				#{
				CALL("accmdp025.scr")
				PRINT("success")
			#}
			ENDIF
			
			#}
		ELSE
			#{
			BANCS.INPARAM.BINDVARS = CUST.CONTRACT.contractId +"|"+ CUST.CONTRACT.acctNo+ "|"
			BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS+ "N"+"|"+"N"
			PRINT(BANCS.INPARAM.BINDVARS)
			
			sv_s = "count1|"
			sv_s = sv_s + "SELECT COUNT(1) FROM CUSTOM.CATOD_SANCTION_TABLE "
			sv_s = sv_s + " WHERE REQUEST_ID=?SVAR "
			sv_s = sv_s + " AND ACCT_NUM=?SVAR "
			sv_s = sv_s + " AND DEL_FLG=?SVAR "
			sv_s = sv_s + " AND ENTITY_CRE_FLG=?SVAR"
			PRINT(sv_s)
			sv_d= urhk_dbSelectWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d==0) THEN
			#{
				CUST.CONTRACT.mainCount=BANCS.OUTPARAM.count1
				PRINT(CUST.CONTRACT.mainCount)
				IF(CUST.CONTRACT.mainCount!=0)THEN
				#{
					
					BANCS.INPARAM.BINDVARS = CUST.CONTRACT.contractId +"|"+ BANCS.STDIN.contextBankId
					BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS+"|"+ "N"+"|"+"N"
					
					sv_s = "DELETE FROM CUSTOM.CATOD_SANCTION_TABLE"
					sv_s = sv_s + " WHERE REQUEST_ID=?SVAR"
					sv_s = sv_s + " AND BANK_ID = ?SVAR"
					sv_s = sv_s + " AND ENTITY_CRE_FLG = ?SVAR"
					sv_s = sv_s + " AND DEL_FLG = ?SVAR"
					PRINT(sv_s)
					sv_d= urhk_dbSqlWithBind(sv_s)
					PRINT(sv_d)
					
					IF(sv_d!=0) THEN
					#{
						CUST.INTERFACE.errorCode="ACCM_ERR_021"
						CALL("Common_Interface_GetErrorDesc.scr")
						IF ( CUST.INTERFACE.SuccessFlag == "1") THEN
						#{
							sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
							CUST.CONTRACT.errorMsg = "Could not fetch Parameter value for " +  CUST.INTERFACE.errorCode
							sv_z = "Error_0|ERROR^" + CUST.CONTRACT.errorMsg + "^contrctAmt"
							sv_u = urhk_SetOrbOut(sv_z)
							CUST.CONTRACT.successFlg = "F"
							GOTO ENDOFSCRIPT
							#}
						ELSE
							#{
							CUST.CONTRACT.errorMsg=CUST.INTERFACE.errorDesc
							sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
							print(sv_u)
							sv_u = urhk_SetOrbOut("Error_0|ERROR^" + CUST.CONTRACT.errorMsg + "^contrctAmt")
							print(sv_u)
							GOTO ENDOFSCRIPT
						#}
						ENDIF
						#}
					ELSE
						#{
						PRINT("SUCCESS")
					#}
					ENDIF
					#}
				ELSE
					#{
					CUST.INTERFACE.errorCode="ACCM_ERR_013"
					CALL("Common_Interface_GetErrorDesc.scr")
					IF ( CUST.INTERFACE.SuccessFlag == "1") THEN
					#{
						sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
						CUST.CONTRACT.errorMsg = "Could not fetch Parameter value for " +  CUST.INTERFACE.errorCode
						sv_z = "Error_0|ERROR^" + CUST.CONTRACT.errorMsg + "^contrctAmt"
						sv_u = urhk_SetOrbOut(sv_z)
						CUST.CONTRACT.successFlg = "F"
						GOTO ENDOFSCRIPT
						#}
					ELSE
						#{
						CUST.CONTRACT.errorMsg=CUST.INTERFACE.errorDesc
						sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
						print(sv_u)
						sv_u = urhk_SetOrbOut("Error_0|ERROR^" + CUST.CONTRACT.errorMsg + "^contrctAmt")
						print(sv_u)
						GOTO ENDOFSCRIPT
					#}
					ENDIF
				#}
				ENDIF
				#}
				
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	BANCS.INPARAM.BINDVARS = CUST.CONTRACT.contractId +"|"+ BANCS.STDIN.contextBankId
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS+"|"+ "Y"+"|"+"N|N"
	sv_s = " remCount| "
	sv_s = sv_s + " SELECT  COUNT(1) "
	sv_s = sv_s + "  FROM custom.catod_sanction_table "
	sv_s = sv_s + "   WHERE request_id=?SVAR "
	sv_s = sv_s + "   AND  REMARKS is NOT NULL "
	sv_s = sv_s + "   AND bank_id=?SVAR "
	sv_s = sv_s + "   AND entity_cre_flg=?SVAR "
	sv_s = sv_s + "   AND del_flg=?SVAR "
	sv_s = sv_s + "   AND cls_flg=?SVAR "
	PRINT(sv_s)
	sv_d= urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	IF(sv_d==0) THEN
	#{
		CUST.CONTRACT.remCount =BANCS.OUTPARAM.remCount
		#}
	ELSE
		#{
		CUST.CONTRACT.remCount ="0"
	#}
	ENDIF
	IF ( CUST.CONTRACT.remCount!="0" ) THEN
	#{
		BANCS.INPARAM.BINDVARS=CUST.CONTRACT.remarks+"|"+CUST.CONTRACT.contractId
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS +"|"+ BANCS.STDIN.contextBankId
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS+"|"+ "Y"+"|"+"N|N"
		sv_s = "UPDATE CUSTOM.CATOD_SANCTION_TABLE SET "
		sv_s = sv_s + "REMARKS =?SVAR  "
		sv_s = sv_s + " WHERE REQUEST_ID= ?SVAR "
		sv_s = sv_s + "   AND bank_id=?SVAR "
		sv_s = sv_s + "   AND entity_cre_flg=?SVAR "
		sv_s = sv_s + "   AND del_flg=?SVAR "
		sv_s = sv_s + "   AND cls_flg=?SVAR "
		PRINT(sv_s)
		sv_d= urhk_dbSqlWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d!=0) THEN
		#{
			CUST.INTERFACE.errorCode="ACCM_ERR_021"
			CALL("Common_Interface_GetErrorDesc.scr")
			IF ( CUST.INTERFACE.SuccessFlag == "1") THEN
			#{
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				CUST.CONTRACT.errorMsg = "Could not fetch Parameter value for " +  CUST.INTERFACE.errorCode
				sv_z = "Error_0|ERROR^" + CUST.CONTRACT.errorMsg + "^contrctAmt"
				sv_u = urhk_SetOrbOut(sv_z)
				CUST.CONTRACT.successFlg = "F"
				GOTO ENDOFSCRIPT
				#}
			ELSE
				#{
				CUST.CONTRACT.errorMsg=CUST.INTERFACE.errorDesc
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				print(sv_u)
				sv_u = urhk_SetOrbOut("Error_0|ERROR^" + CUST.CONTRACT.errorMsg + "^contrctAmt")
				print(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	
	sv_u = urhk_SetOrbOut("RESULT_MSG|"+"Contract ID " +CUST.CONTRACT.contractId+" has been CANCELLED  successfully")
	PRINT(sv_u)
	sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
	print(sv_u)
	
	ENDOFSCRIPT:
END-->
