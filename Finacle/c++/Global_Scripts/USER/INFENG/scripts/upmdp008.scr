#----------------------------------------------------------------------------------------------------------
# Filename              :       upmdp008.scr
# Author                :       Hari Mohan Verma
# Description           :       This is the script for cancelling last action done on unverified data and
#								setting it to backend
# Input                 :       func Code,user id
# Output                :       Details page field values
# Called Script         :       SRV_CancelUsrProfile_post_process_data.scr
# Calling Script        :       NA
#
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----        ----------         --------------    ----------------        ---------
#  1           25-06-2013         Hari MOhan Verma   original Version          1.0
#-----------------------------------------------------------------------------------------------------------

<--START
CHECKSUM="ac8260a1b12ea11760226e029c18e848093eda069530f5b1ba9d7aed13698a20"
	TRACE ON
	
	#-------------------------------------------------------------
	# Repository creation
	#-------------------------------------------------------------
	IF (REPEXISTS("CUST") == 0 ) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	IF (CLASSEXISTS("CUST","UPM") == 0) THEN
	#{
		CREATECLASS("CUST","UPM",5)
	#}
	ENDIF
	
	sv_a=urhk_SRV_GetVal("usrGenDtl.usrId")
	CUST.UPM.addUserId=BANCS.OUTPARAM.srvValue
	print(CUST.UPM.addUserId)
	
	#-------------------------------------------------------------
	# Deleting Data From Table
	#-------------------------------------------------------------
	BANCS.INPARAM.BINDVARS = CUST.UPM.addUserId
	sv_s = "count| Select count(*) from CUSTOM.C_UAD_MOD WHERE USER_ID = ?SVAR "
	sv_d = urhk_dbSelectWithBind(sv_s)
	IF(sv_d != 0) THEN
	#{
		print("Query execution failed")
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	IF(BANCS.OUTPARAM.count != 0) THEN
	#{
		BANCS.INPARAM.BINDVARS = CUST.UPM.addUserId
		sv_s="DELETE FROM CUSTOM.C_UAD_MOD WHERE USER_ID = ?SVAR "
		sv_d =urhk_dbSQLWithBind(sv_s)
		IF(sv_d != 0) THEN
		#{
			print("Query execution failed")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		#}
	ELSE
		#{
		BANCS.INPARAM.BINDVARS = CUST.UPM.addUserId
		sv_s="DELETE FROM CUSTOM.C_UAD WHERE USER_ID = ?SVAR "
		sv_s = sv_s + " AND ENTITY_CRE_FLG = 'N' "
		sv_d =urhk_dbSQLWithBind(sv_s)
		IF(sv_d != 0) THEN
		#{
			print("Query execution failed")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	ENDOFSCRIPT:
		
		#TRACE OFF
END-->
