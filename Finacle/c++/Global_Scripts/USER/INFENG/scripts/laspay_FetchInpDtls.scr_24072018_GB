#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: laspay_FetchInpDtls.scr
# event: NA
# description: NA
#=================================================
<--START
CHECKSUM="294cb2ce4233b5ec105254772a3214aefea8134cc5f9a68f25c9f4cb4c1c9631"
	TRACE ON
	
	sv_v = "LASPAY_REQ.AccountID"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{
		CUST.INTERFACE.AccountID =("BANCS").("INPUT").(sv_v)
		#}
	ELSE
		#{
		CUST.INTERFACE.AccountID = ""
	#}
	ENDIF
	
	sv_v = "LASPAY_REQ.enteredAmt"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{
		CUST.INTERFACE.enteredAmt =("BANCS").("INPUT").(sv_v)
		#}
	ELSE
		#{
		CUST.INTERFACE.enteredAmt = ""
	#}
	ENDIF
	
	sv_v = "LASPAY_REQ.enteredAmtCcy"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{
		CUST.INTERFACE.enteredAmtCcy =("BANCS").("INPUT").(sv_v)
		#}
	ELSE
		#{
		CUST.INTERFACE.enteredAmtCcy = ""
	#}
	ENDIF
	
	sv_v = "LASPAY_REQ.sourceAcctID"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{
		CUST.INTERFACE.sourceAcId =("BANCS").("INPUT").(sv_v)
		#}
	ELSE
		#{
		CUST.INTERFACE.sourceAcId = ""
	#}
	ENDIF
	
	
	sv_v = "LASPAY_REQ.Refnum"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{
		CUST.INTERFACE.Refnum =("BANCS").("INPUT").(sv_v)
		#}
	ELSE
		#{
		CUST.INTERFACE.Refnum = ""
	#}
	ENDIF
	
	
	sv_v = "LASPAY_REQ.lapaytype"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{
		CUST.INTERFACE.lapaytype =("BANCS").("INPUT").(sv_v)
		#}
	ELSE
		#{
		CUST.INTERFACE.lapaytype = ""
	#}
	ENDIF
	
	sv_v = "LASPAY_REQ.Phone"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{
		CUST.INTERFACE.Phone =("BANCS").("INPUT").(sv_v)
		#}
	ELSE
		#{
		CUST.INTERFACE.Phone = ""
	#}
	ENDIF
	
	print(CUST.INTERFACE.AccountID)
	CUST.INTERFACE.foracid = trim(CUST.INTERFACE.AccountID)
	sv_p = urhk_getAcctDetailsInRepository(CUST.INTERFACE.foracid)
	
	IF(sv_p==0) THEN
	#{
		CUST.INTERFACE.schmType = BANCS.OUTPARAM.schmType
		print(CUST.INTERFACE.schmType)
		
		IF (CUST.INTERFACE.schmType !="LAA") THEN
		#{
			#CUST.INTERFACE.srvErrFlg ="Y"
			CUST.INTERFACE.successOrFailure = "F"
			CUST.INTERFACE.errorCode = "ERR110"
			GOTO ERROREXIT
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.INTERFACE.srvErrFlg ="Y"
		CUST.INTERFACE.successOrFailure = "F"
		GOTO ERROREXIT
	#}
	ENDIF
	print(CUST.INTERFACE.enteredAmt)
	print(CUST.INTERFACE.enteredAmtCcy)
	print(CUST.INTERFACE.sourceAcId)
	print(CUST.INTERFACE.Refnum)
	print(CUST.INTERFACE.lapaytype)
	print(CUST.INTERFACE.Phone)

	####
	CUST.INTERFACE.errMsgstflg="S"
	sv_t= urhk_getAcctDetailsInRepository(CUST.INTERFACE.sourceAcId)
	print(sv_t)
	IF (sv_t == 0) THEN
		CUST.INTERFACE.solidsh = BANCS.OUTPARAM.acctSolId
		print(CUST.INTERFACE.solidsh)
		CUST.INTERFACE.schmcodesh = BANCS.OUTPARAM.schmCode
		print(CUST.INTERFACE.schmcodesh)
		CUST.INTERFACE.schmType = BANCS.OUTPARAM.schmType
		print(CUST.INTERFACE.schmType)
		CUST.INTERFACE.clearBalance = BANCS.OUTPARAM.clearBalance
		print(CUST.INTERFACE.clearBalance)
		CUST.INTERFACE.EffAvailableAmttr = BANCS.OUTPARAM.EffAvailableAmt
		print(CUST.INTERFACE.EffAvailableAmttr)
		CUST.INTERFACE.lienAmt = BANCS.OUTPARAM.lienAmt
		print(CUST.INTERFACE.lienAmt)
		IF (CUST.INTERFACE.lapaytype!="MPESA") THEN
			IF (CDOUBLE(CUST.INTERFACE.EffAvailableAmttr) < CDOUBLE(CUST.INTERFACE.enteredAmt)) THEN
				print("inin")
				CUST.INTERFACE.errMsgstflg="F"
				EXITSCRIPT
			ENDIF
		ENDIF
	ENDIF
	####
	
	#---------------------------------------------------------------------------------
	# Validation for Account ID
	#---------------------------------------------------------------------------------
	IF(CUST.INTERFACE.AccountID != "") THEN
	#{
		sv_u = urhk_valAcctNumber(CUST.INTERFACE.AccountID)
		print(sv_u)
		IF(sv_u != 0) THEN
		#{
			CUST.INTERFACE.successOrFailure = "F"
			CUST.INTERFACE.errorCode = "ERR110"
			GOTO ERROREXIT
		#}
		ENDIF
		
		#}
	ELSE
		#{
		CUST.INTERFACE.successOrFailure = "F"
		CUST.INTERFACE.errorCode = "ERR106"
		GOTO ERROREXIT
	#}
	ENDIF
	
	#---------------------------------------------------------------------------------
	# Validation for Source Account ID
	#---------------------------------------------------------------------------------
	IF(CUST.INTERFACE.sourceAcId != "") THEN
	#{
		sv_u = urhk_valAcctNumber(CUST.INTERFACE.sourceAcId)
		print(sv_u)
		IF(sv_u != 0) THEN
		#{
			CUST.INTERFACE.successOrFailure = "F"
			CUST.INTERFACE.errorCode = "ERR110"
			GOTO ERROREXIT
		#}
		ENDIF
		
		#}
	ELSE
		#{
		CUST.INTERFACE.successOrFailure = "F"
		CUST.INTERFACE.errorCode = "ERR106"
		GOTO ERROREXIT
	#}
	ENDIF
	
	#---------------------------------------------------------------------------------
	# Validation for Amount
	#---------------------------------------------------------------------------------
	IF(CUST.INTERFACE.enteredAmt != "") THEN
	#{
		
		#}
	ELSE
		#{
		CUST.INTERFACE.successOrFailure = "F"
		CUST.INTERFACE.errorCode = "UBP_ERR003"
		GOTO ERROREXIT
	#}
	ENDIF
	
	
	#---------------------------------------------------------------------------------
	# Validation for Currency
	#---------------------------------------------------------------------------------
	IF(CUST.INTERFACE.enteredAmtCcy != "") THEN
	#{
		sv_u = urhk_B2k_valCrncy(CUST.INTERFACE.enteredAmtCcy)
		IF(sv_u != 0) THEN
		#{
			CUST.INTERFACE.successOrFailure = "F"
			CUST.INTERFACE.errorCode = "DENOM_ERR_016"
			GOTO ERROREXIT
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.INTERFACE.successOrFailure = "F"
		CUST.INTERFACE.errorCode = "DENOM_ERR_016"
		GOTO ERROREXIT
	#}
	ENDIF
	
ERROREXIT:
#trace off
END-->
