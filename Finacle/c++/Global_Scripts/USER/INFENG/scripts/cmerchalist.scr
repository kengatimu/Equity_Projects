#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: ubpdp026.scr
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START
CHECKSUM="6858ef4c2f00c2e8a1c63d67a64c51e104d9153c2115507952be144e05c65e97"
	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp026.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called from crit page link file for fetching biller Id
	# Called Script         :
	# Calling Script        :	function fnBillerId()
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           16-04-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	
	sv_a = urhk_b2k_printRepos("BANCS")
	print(sv_a)
	
	#========================================================
	# Check whether CUST Repository exists else create it
	#========================================================
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	#========================================================
	# Check whether UBP Class exists else create it
	#========================================================
	sv_r = CLASSEXISTS("CUST","UBP")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","UBP",5)
	#}
	ENDIF
	#========================================================
	# Initialize the variables
	#========================================================
	
	CUST.UBP.supeffectivedate = " "
	CUST.UBP.supmiscode = " "
	CUST.UBP.supitemcode=" "
	CUST.UBP.supitemdescription=" "
	CUST.UBP.supbaseprice=" "
	CUST.UBP.bankid=" "
	CUST.UBP.dco=" "
	CUST.UBP.recordCount = "0"
	#========================================================
	# Fetching the Record count
	#========================================================
	
	IF (FIELDEXISTS(BANCS.INPUT.dco) == 1) THEN
	#{
		CUST.UBP.dco=BANCS.INPUT.dco
		print(CUST.UBP.dco)
		sv_k =CUST.UBP.dco
		print(sv_k)
		#}
	ELSE
		#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	BANCS.INPARAM.BINDVARS = sv_k + "|" + sv_k + "|" + BANCS.STDIN.contextBankId
	print(BANCS.INPARAM.BINDVARS)
	sv_s = "recordNum|"
	sv_s = sv_s + " select  count(*) from custom.supplier_price_list "
	sv_s = sv_s + " where SUP_MIS_CODE=?SVAR AND SUP_EFFECTIVE_DATE=(select max(SUP_EFFECTIVE_DATE)  from custom.supplier_price_list where SUP_MIS_CODE=?SVAR AND BANK_ID= ?SVAR)"
	PRINT(sv_s)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d == 0)THEN
	#{
		CUST.UBP.recordNum = BANCS.OUTPARAM.recordNum
		PRINT(CUST.UBP.recordNum)
	#}
	ENDIF
	sv_u = urhk_SetOrbOut("recCount|"+CUST.UBP.recordNum)
	PRINT(sv_u)
	
	IF(CUST.UBP.recordNum=="0")THEN
	#{
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		#========================================================
		# Fetching tthe values from BRGT table and populating
		# the searcher list
		#========================================================
		BANCS.INPARAM.BINDVARS = sv_k + "|" + sv_k + "|" + BANCS.STDIN.contextBankId
		print(BANCS.INPARAM.BINDVARS)
		sv_s = "supeffectivedate,supmiscode,supitemcode,supitemdescription,supbaseprice,bankid|"
		sv_s = sv_s + " select  SUP_EFFECTIVE_DATE,SUP_MIS_CODE,SUP_ITEM_CODE,SUP_ITEM_DESCRIPTION,SUP_BASE_PRICE,BANK_ID from custom.supplier_price_list "
		sv_s = sv_s + " where SUP_MIS_CODE=?SVAR  AND SUP_EFFECTIVE_DATE=(select max(SUP_EFFECTIVE_DATE)  from custom.supplier_price_list where SUP_MIS_CODE=?SVAR AND BANK_ID= ?SVAR ) "
		PRINT(sv_s)
		sv_c = urhk_dbCursorOpenWithBind(sv_s)
		PRINT(sv_c)
		IF(sv_c != 0)THEN
		#{
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		sv_n = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		PRINT(sv_n)
		sv_f = urhk_dbCursorFetch(sv_n)
		PRINT(sv_f)
		sv_i = 1
		WHILE(sv_f == 0)
		#{
			CUST.UBP.supeffectivedate = BANCS.OUTPARAM.supeffectivedate
			PRINT(CUST.UBP.supeffectivedate)
			CUST.UBP.supmiscode = BANCS.OUTPARAM.supmiscode
			PRINT(CUST.UBP.supmiscode)
			CUST.UBP.supitemcode = BANCS.OUTPARAM.supitemcode
			PRINT(CUST.UBP.supitemcode)
			CUST.UBP.supitemdescription = BANCS.OUTPARAM.supitemdescription
			PRINT(CUST.UBP.supitemdescription)
			CUST.UBP.supbaseprice = BANCS.OUTPARAM.supbaseprice
			PRINT(CUST.UBP.supbaseprice)
			CUST.UBP.bankid = BANCS.OUTPARAM.bankid
			PRINT(CUST.UBP.bankid)
			
			#			sv_s = "Supeffectivedate_" + Format$(sv_i,"%d") + "|" + CUST.UBP.supeffectivedate
			#			PRINT(sv_s)
			#			sv_u = urhk_SetOrbOut(sv_s)
			#			PRINT(sv_u)
			
			sv_s = "RefCode_" + Format$(sv_i,"%d") + "|" + CUST.UBP.supmiscode
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			
			sv_s = "ItemCode_" + Format$(sv_i,"%d") + "|" + CUST.UBP.supitemcode
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			
			sv_s = "ItemDescription_" + Format$(sv_i,"%d") + "|" + CUST.UBP.supitemdescription
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			
			sv_s = "UnitPrice_" + Format$(sv_i,"%d") + "|" + CUST.UBP.supbaseprice
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			
			#			sv_s = "Bankid_" + Format$(sv_i,"%d") + "|" + CUST.UBP.bankid
			#			PRINT(sv_s)
			#			sv_u = urhk_SetOrbOut(sv_s)
			#			PRINT(sv_u)
			#
			sv_i = sv_i + 1
			sv_f = urhk_dbCursorFetch(sv_n)
			PRINT(sv_f)
		#}
		DO
		sv_c = urhk_dbCursorClose(sv_n)
		PRINT(sv_c)
	#}
	ENDIF
	
	ENDOFSCRIPT:
END-->
