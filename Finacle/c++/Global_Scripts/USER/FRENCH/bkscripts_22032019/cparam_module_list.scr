##########################################################################################
# Source Name           : cparam_module_list.scr
# Date                  : 04-Mar-2013
# Description           :
# Author                : Mohan Raghu Arudi
# Modification History  :
# <Serial No.>  <Date>        <Author Name>             <Description>
# ------------  -----------   -----------------         --------------
#      1        04-Mar-2013   Mohan Raghu Arudi         Base Version
###########################################################################################

<--START
CHECKSUM="30b8c2d6f8bf7ef814a65244340dd01a55b72e2f649b399a8fbda25e897da91e"
	#trace on
	###########################################################################################
	# STEP : STEP 0 BEGIN
	# CREATE THE NECESSARY REPOSITORY AND CLASSES
	###########################################################################################
	
	IF(REPEXISTS("CUST") == 0 ) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF(CLASSEXISTS("CUST","DATA") == 0) THEN
	#{
		CREATECLASS("CUST","DATA",5)
	#}
	ENDIF
	
	#sv_i = urhk_B2k_printRepos("BANCS")
	#print(sv_i)
	
	CUST.DATA.recordCount	= ""
	
	################################################################################
	# STEP : STEP 0 END
	################################################################################
	
	################################################################################
	# STEP : STEP 1 BEGIN
	# Selecting the values of parameter from screen
	################################################################################
	
	CUST.DATA.module_id = ""
	CUST.DATA.module_desc = ""
	
	CUST.DATA.module_id = BANCS.INPUT.moduleName
	print(CUST.DATA.module_id)
	CUST.DATA.module_id            =   TOUPPER(TRIM(CUST.DATA.module_id))
	
	################################################################################
	#Initializing fields from front end to 'N'
	################################################################################
	
	################################################################################
	# STEP : STEP 1 END
	################################################################################
	
	################################################################################
	# STEP : STEP 2 BEGIN
	# Selecting the fields for tran id listing
	################################################################################
	
	
	#{
	sv_s = "recordCount|"
	sv_s = sv_s + " SELECT count(*) "
	sv_s = sv_s + " FROM CUSTOM.MODULE_DETAILS "
	sv_s = sv_s + " WHERE BANK_ID = ?SVAR AND DEL_FLG='N'"
	BANCS.INPARAM.BINDVARS =  BANCS.STDIN.contextBankId
	IF(CUST.DATA.module_id != "") THEN
	#{
		sv_s = sv_s + " AND MODULE_ID like ?SVAR"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.DATA.module_id + "%"
	#}
	ENDIF
	sv_s = sv_s + " AND ENTITY_CRE_FLG='Y'"
	
	#}
	
	
	print(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	
	
	IF (sv_d != 0) THEN
	#{
		CUST.DATA.recordCount = "0"
		#}
	ELSE
		#{
		CUST.DATA.recordCount = BANCS.OUTPARAM.recordCount
	#}
	ENDIF
	CUST.DATA.recordCount = "recordCount|"+CUST.DATA.recordCount
	sv_u = urhk_SetOrbOut(CUST.DATA.recordCount)
	
	
	
	sv_s = "module_id,module_desc|"
	sv_s = sv_s + " SELECT MODULE_ID,MODULE_DESC "
	sv_s = sv_s + " FROM CUSTOM.MODULE_DETAILS "
	sv_s = sv_s + " WHERE BANK_ID = ?SVAR AND DEL_FLG='N'"
	BANCS.INPARAM.BINDVARS =  BANCS.STDIN.contextBankId
	IF(CUST.DATA.module_id != "") THEN
	#{
		sv_s = sv_s + " AND MODULE_ID like ?SVAR"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.DATA.module_id + "%"
	#}
	ENDIF
	sv_s = sv_s + " AND ENTITY_CRE_FLG='Y'"
	
	
	
	
	print(BANCS.INPARAM.BINDVARS)
	sv_u = urhk_dbCursorOpenWithBind(sv_s)
	
	IF ( sv_u == 0 ) THEN
	#{
		sv_m = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		
		sv_c = urhk_dbCursorFetch(sv_m)
		sv_z = 0
		WHILE(sv_c == 0)
		#{
			#########################################################################
			#Checking for Null Values
			#########################################################################
			
			IF(BANCS.OUTPARAM.module_id == "") THEN
			#{
				BANCS.OUTPARAM.module_id = " "
				#}
			ELSE
				#{
				BANCS.OUTPARAM.module_id = TRIM(BANCS.OUTPARAM.module_id)
			#}
			ENDIF
			
			IF(BANCS.OUTPARAM.module_desc == "") THEN
			#{
				BANCS.OUTPARAM.module_desc = " "
				#}
			ELSE
				#{
				BANCS.OUTPARAM.module_desc = TRIM(BANCS.OUTPARAM.module_desc)
			#}
			ENDIF
			
			sv_z = sv_z + 1
			sv_o ="moduleName_"+FORMAT$(sv_z,"%d")+"|"+BANCS.OUTPARAM.module_id
			sv_q ="moduleDesc_"+FORMAT$(sv_z,"%d")+"|"+BANCS.OUTPARAM.module_desc
			
			sv_u = urhk_SetOrbOut(sv_o)
			
			sv_u = urhk_SetOrbOut(sv_q)
			
			sv_c = urhk_dbCursorFetch(sv_m)
		#}
		DO
		sv_u = urhk_dbCursorClose(sv_m)
	#}
	ENDIF
	
	################################################################################
	# STEP : STEP 2 END
	################################################################################
	
	################################################################################
	# Exit the script
	################################################################################
	
	EXITSCRIPT
	
	################################################################################
	# End of script
	################################################################################
	#trace off
END-->
