#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: tlrdenadp011.scr
# Type: NA|default
# Description: Custom Menu : HTLRDENA
# Author:
# Date:
# Modification Log:
#============================================================

###########################################################################################
# Source Name           : tlrdenadp011.scr
# Date                  :
# Description           : To get the list of teller id
# Input                 : none
# Output                : none
# Called Script         : none
# Calling Script        : none
############################################################################################


<--START
CHECKSUM="40ad4561a9a8cf8f98a15d073d593e5b7dd11b386c7316343315267fdd1b56d8"
	TRACE ON
	#-------------------------------------------------------------------------------
	#	Creating Repository and Class
	#-------------------------------------------------------------------------------
	sv_r = REPEXISTS("CUST")
	IF (sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	sv_r = CLASSEXISTS("CUST","DENOM")
	IF (sv_r ==0) THEN
	#{
		CREATECLASS("CUST","DENOM",5)
	#}
	ENDIF
	#sv_u=urhk_b2k_PrintRepos("BANCS")
	#print (sv_u)
	CUST.DENOM.funcCode =BANCS.INPUT.func
	PRINT(CUST.DENOM.funcCode)
	CUST.DENOM.tellrId =BANCS.INPUT.tellrId
	PRINT(CUST.DENOM.tellrId)
	
	sv_q = "cnt|SELECT count(*)  FROM"
	IF((CUST.DENOM.funcCode == "I") OR (CUST.DENOM.funcCode == "M") )THEN
	#{
		sv_q = sv_q + " custom.C_DENM"
		#}
	ELSE
		#{
		sv_q = sv_q + " custom.C_DENM_MOD "
	#}
	ENDIF
	sv_q = sv_q  + " where sol_id = ?SVAR AND teller_id = ?SVAR AND rownum<2"
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.mySolId + "|" + BANCS.INPUT.tellrId
	PRINT(sv_q)
	sv_b = urhk_dbSelectWithBind(sv_q)
	PRINT(sv_b)
	
	IF(sv_b == 0)THEN
	#{
		IF(BANCS.OUTPARAM.cnt > 0 )THEN
		#{
			sv_z = urhk_SetOrbOut("Y|V")
			#}
		ELSE
			#{
			sv_z = urhk_SetOrbOut("N|I")
			PRINT(sv_z)
		#}
		ENDIF
		#}
	ELSE
		#{
		GOTO FINISH
	#}
	ENDIF
	
	FINISH:
		EXITSCRIPT
		TRACE OFF
END-->
