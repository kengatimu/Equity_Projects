#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cmntrtcdp001.scr
# Type: NA|default
# Description: validation script for currency in CMNTRTC
# Author: haneesha.kanumuru
# Date: 5/7/18 2:04 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="ac015fcef9b49d984d0c6637853988ffb4134242fb8b460233728f3c6e172b71"
	# Script body goes here
	TRACE ON
	#==========================================================*
	# create repository
	#==========================================================*
	sv_a = REPEXISTS("CUST")
	IF(sv_a == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	sv_r = CLASSEXISTS("CUST","CMNTRTC")
	IF (sv_r == 0) THEN
	#{
		CREATECLASS("CUST", "CMNTRTC", 5)
	#}
	ENDIF
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	
	
	CUST.CMNTRTC.errorFlg = "N"
	CUST.CMNTRTC.errorMsg = " "
	CUST.CMNTRTC.fromCrncy = " "
	IF(FIELDEXISTS(BANCS.INPUT.fromCrncy)) THEN
	#{
		IF(TRIM(BANCS.INPUT.fromCrncy) != "")THEN
		#{
			CUST.CMNTRTC.fromCrncy= TOUPPER(TRIM(BANCS.INPUT.fromCrncy))
			PRINT(CUST.CMNTRTC.fromCrncy)
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.CMNTRTC.errorFlg = "Y"
		CUST.CMNTRTC.errorMsg = "Invalid Input for fnExecuteScript"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	CUST.CMNTRTC.count=""
	
	
	sv_s = "count|"
	sv_s = sv_s + " SELECT COUNT(*)"
	sv_s = sv_s + " FROM TBAADM.CNC "
	sv_s = sv_s + " WHERE CRNCY_CODE = ?SVAR"
	sv_s = sv_s + " AND DEL_FLG != 'Y'"
	sv_s = sv_s + " AND ENTITY_CRE_FLG != 'N'"
	#sv_s = sv_s + " WHERE ACCT_NUM = ?SVAR"
	sv_s = sv_s + " AND BANK_ID = ?SVAR"
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS =   CUST.CMNTRTC.fromCrncy + "|"+ BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	CUST.CMNTRTC.count = BANCS.OUTPARAM.count
	PRINT(CUST.CMNTRTC.count)
	
	
	IF( CUST.CMNTRTC.count ==0 )THEN
	#{
		#		CUST.CMNTRTC.acctNum = BANCS.OUTPARAM.acctNum
		#		PRINT(CUST.CMNTRTC.acctNum)
		#		#}
		#	ELSE
		#		#{
		CUST.CMNTRTC.errorFlg = "Y"
		CUST.CMNTRTC.errorMsg = "Invalid Currency"
	#}
	ENDIF
	ENDOFSCRIPT:
		sv_u = urhk_SetOrbOut("errorFlg|"+CUST.CMNTRTC.errorFlg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("errorMsg|"+CUST.CMNTRTC.errorMsg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("fromCrncy|"+CUST.CMNTRTC.fromCrncy)
		
		PRINT(sv_u)
		TRACE OFF
END-->
