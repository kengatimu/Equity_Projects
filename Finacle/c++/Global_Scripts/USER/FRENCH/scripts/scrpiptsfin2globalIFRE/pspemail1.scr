<--START
CHECKSUM="4ba2ade428c2258e83b9e809948bbfb78d138965c62191fc4d2a4e11532f02a1"
	TRACE ON
	sv_k=""
	sv_y=""
	sv_w=""
	
	sv_q="foracid,date_from,date_to,request_date,status,bank_id|"
	sv_q = sv_q + " SELECT FORACID,to_char((to_date(DATE_FROM, 'DD-MM-RRRR')),'DD-MM-RRRR'),to_char((to_date(DATE_TO, 'DD-MM-RRRR')),'DD-MM-RRRR'),REQUEST_DATE,STATUS,BANK_ID from CUSTOM.PSPEMIAL_TABLE where STATUS='N' AND BANK_ID='54' and DATE_FROM>'15-nov-2013' "
	print(sv_q)
	
	sv_e = urhk_dbCursorOpenWithBind(sv_q)
	
	IF(sv_e==0) THEN
	#{
		sv_m = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		sv_q = urhk_dbCursorFetch(sv_m)
		print(sv_q)
		IF(sv_q == 1) THEN
		#{
			sv_x = urhk_dbCursorClose(sv_m)
			print(sv_x)
			exitscript
		#}
		ENDIF
		print(sv_q)
		WHILE(sv_q==0)
		#{
			##################
			# Get account balance as on BOD date.
			
			IF(REPEXISTS("CUST")==0) THEN
			#{
				CREATEREP("CUST")
			#}
			ENDIF
			IF (CLASSEXISTS("CUST","DATA") == 0) THEN
			#{
				CREATECLASS("CUST","DATA",5)
			#}
			ENDIF
			CUST.DATA.foracid=BANCS.OUTPARAM.foracid
			print(CUST.DATA.foracid)
			CUST.DATA.date_from=BANCS.OUTPARAM.date_from
			print(CUST.DATA.date_from)
			CUST.DATA.date_to=BANCS.OUTPARAM.date_to
			print(CUST.DATA.date_to)
			CUST.DATA.request_date=BANCS.OUTPARAM.request_date
			print(CUST.DATA.request_date)
			CUST.DATA.status=BANCS.OUTPARAM.status
			print(CUST.DATA.status)
			
			print(BANCS.STDIN.BODDate)
			##################
			sv_h = mid$(BANCS.STDIN.BODDate,0,10)
			print(sv_h)
			
			CALL ("pspemail.scr")
			
			print(CUST.DATA.foracid)
			
			sv_s = " UPDATE CUSTOM.PSPEMIAL_TABLE "
			sv_s = sv_s + " SET STATUS ='S'"
			sv_s = sv_s + " WHERE FORACID= ?SVAR"
			sv_s = sv_s + " AND bank_id = ?SVAR"
			print(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.DATA.foracid + "|" + BANCS.STDIN.contextBankId
			print(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSQLWithBind(sv_s)
			print(sv_d)
			
			IF (sv_d == 0) THEN
			#{
				print("The statement sent successfully")
				sv_d = urhk_dbSQLWithBind("COMMIT")
				print(sv_d)
			#}
			ENDIF
			
			sv_q = urhk_dbCursorFetch(sv_m)
			print(sv_q)
		#}
		DO
		TRACE OFF
		EXITSCRIPT
		
	END-->
	
	
