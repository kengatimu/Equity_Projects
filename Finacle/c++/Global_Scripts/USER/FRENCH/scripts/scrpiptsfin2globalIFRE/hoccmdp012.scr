#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: hoccmdp012.scr
# event: NA
# description: called on OnChange event of tranId of critera page
#=================================================
#----------------------------------------------------------------------------------------------------------
# Filename              :       hoccmdp012.scr
# Author                :       Nitin Prabhakar
# Description           :       This script is called on Onchange event tranId on criteria page.
# Input                 :       zoneCode, TranId, zoneDate.
# Output                :       error message or few field value for citeria page.
# Called Script         :       None
# Calling Script        :       None
#
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----        ----------         --------------    ----------------        ---------
#  1           22-07-2013         Nitin Prabhakar   original Version          1.0
#  2           26-08-2013         Swati Ganjoo      Customization for UBP     2.0
#-----------------------------------------------------------------------------------------------------------
<--START
CHECKSUM="407c7e5b2c745a985fc93908949ae70b7a3394beb1c80345ba82a61b6dd663ce"
	TRACE ON
	#------------------------------------------------------------------------------
	# Creation of  repository and class
	#------------------------------------------------------------------------------
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	IF (CLASSEXISTS("CUST", "HOCCM") == 0) THEN
	#{
		CREATECLASS("CUST", "HOCCM", 5)
	#}
	ENDIF
	IF (CLASSEXISTS("CUST", "INTERFACE") == 0) THEN
	#{
		CREATECLASS("CUST", "INTERFACE", 5)
	#}
	ENDIF
	#--------------------------------------------------------------------------
	# Getting Input at Backend
	#--------------------------------------------------------------------------
	CUST.HOCCM.zoneCode =  TOUPPER(TRIM(BANCS.INPUT.zoneCode))
	print(CUST.HOCCM.zoneCode)
	CUST.HOCCM.critTranId =  TOUPPER(TRIM(BANCS.INPUT.critTranId))
	print(CUST.HOCCM.critTranId)
	CUST.HOCCM.zoneDate =  TOUPPER(TRIM(BANCS.INPUT.zoneDate))
	print(CUST.HOCCM.zoneDate)
	CUST.HOCCM.solId = TRIM(BANCS.STDIN.mySolId)
	print(CUST.HOCCM.solId)
	CUST.HOCCM.bankId = TRIM(BANCS.STDIN.contextBankId)
	print(CUST.HOCCM.bankId)
	###UBP ADDED##############
	CUST.HOCCM.billPayYes="N"
	###UBP ADDED##############
	#--------------------------------------------------------------------------
	# selecting noOfChq, totalCrAmt
	#--------------------------------------------------------------------------
	BANCS.INPARAM.BINDVARS = CUST.HOCCM.zoneCode + "|" + CUST.HOCCM.critTranId
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.HOCCM.solId
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.HOCCM.bankId
	print(BANCS.INPARAM.BINDVARS)
	sv_s = "setNum,totalCrAmt,freetext1,clrZoneDate|"
	sv_s = sv_s + " SELECT SET_NUM,INSTRMNT_AMT,FREETEXT1,CLG_ZONE_DATE"
	sv_s = sv_s + " FROM TBAADM.OCI "
	sv_s = sv_s + " WHERE CLG_ZONE_CODE = ?SVAR "
	sv_s = sv_s + " AND FREETEXT2 = ?SVAR "
	#sv_s = sv_s + " AND CLG_ZONE_DATE = TO_DATE(?SVAR,'DD-MM-YYYY') "
	sv_s = sv_s + " AND SOL_ID = ?SVAR "
	sv_s = sv_s + " AND BANK_ID = ?SVAR "
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	IF(sv_d != 0)THEN
	#{
		# error
		sv_w = "SUCCESSORFAILURE|N"
		sv_u = urhk_SetOrbOut(sv_w)
		sv_m = "Error in fetching cheque details for TranId."
		sv_z = "Error|" + sv_m
		sv_u = urhk_SetOrbOut(sv_z)
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		CUST.HOCCM.setNum = TRIM(BANCS.OUTPARAM.setNum)
		print(CUST.HOCCM.setNum)
		CUST.HOCCM.totalCrAmt = BANCS.OUTPARAM.totalCrAmt
		CUST.HOCCM.totalCrAmt = FORMAT$(CDOUBLE(CUST.HOCCM.totalCrAmt),"%.2f")
		print(CUST.HOCCM.totalCrAmt)
		CUST.HOCCM.critBatchNum = BANCS.OUTPARAM.freetext1
		print(CUST.HOCCM.critBatchNum)
		CUST.HOCCM.zoneDate = BANCS.OUTPARAM.clrZoneDate
	#}
	ENDIF
	
	#--------------------------------------------------------------------------
	# selecting critCrAcctNum
	#--------------------------------------------------------------------------
	BANCS.INPARAM.BINDVARS = CUST.HOCCM.zoneCode
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.HOCCM.critTranId
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.HOCCM.setNum
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.HOCCM.solId
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.HOCCM.bankId
	print(BANCS.INPARAM.BINDVARS)
	sv_s = "critCrAcctNum|"
	sv_s = sv_s + " SELECT DISTINCT(g.Foracid)"
	sv_s = sv_s + " FROM TBAADM.OCP p, TBAADM.GAM g , TBAADM.OCI i"
	sv_s = sv_s + " WHERE p.CLG_ZONE_CODE = ?SVAR "
	sv_s = sv_s + " AND p.CLG_ZONE_CODE = i.CLG_ZONE_CODE "
	sv_s = sv_s + " AND p.CLG_ZONE_DATE = i.CLG_ZONE_DATE "
	sv_s = sv_s + " AND p.SOL_ID = i.SOL_ID "
	sv_s = sv_s + " AND p.SET_NUM = i.SET_NUM "
	sv_s = sv_s + " AND i.FREETEXT2 = ?SVAR "
	sv_s = sv_s + " AND p.SET_NUM = LPAD(?SVAR,6) "
	sv_s = sv_s + " AND p.SOL_ID = ?SVAR "
	sv_s = sv_s + " AND p.BANK_ID = ?SVAR "
	sv_s = sv_s + " AND g.ACID = p.ACID "
	sv_s = sv_s + " AND g.BANK_ID = p.BANK_ID "
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	IF(sv_d != 0)THEN
	#{
		# error
		sv_w = "SUCCESSORFAILURE|N"
		sv_u = urhk_SetOrbOut(sv_w)
		sv_m = "Error in fetching Cr. Account Id for TranId."
		sv_z = "Error|" + sv_m
		sv_u = urhk_SetOrbOut(sv_z)
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		CUST.HOCCM.critCrAcctNum = BANCS.OUTPARAM.critCrAcctNum
		print(CUST.HOCCM.critCrAcctNum)
	#}
	ENDIF
	###############
	#UBP change
	###############
	call("ubpdp064.scr")
	###############
	#UBP change
	###############
	
	#--------------------------------------------------------------------------
	# sending values to FE.
	#--------------------------------------------------------------------------
	sv_w = "SuccessOrFailure|Y"
	sv_u = urhk_SetOrbOut(sv_w)
	sv_w = "critCrAcctNum|" + CUST.HOCCM.critCrAcctNum
	sv_u = urhk_SetOrbOut(sv_w)
	sv_w = "totalCrAmt|" + CUST.HOCCM.totalCrAmt
	sv_u = urhk_SetOrbOut(sv_w)
	sv_w = "critBatchNum|" + CUST.HOCCM.critBatchNum
	sv_u = urhk_SetOrbOut(sv_w)
	
	#################UBP CHANGE#############
	sv_w = "billPayYes|" + CUST.HOCCM.billPayYes
	sv_u = urhk_SetOrbOut(sv_w)
	IF(CUST.HOCCM.billPayYes=="Y")THEN
	#{
		sv_w = "billerId|" + CUST.HOCCM.billerId
		sv_u = urhk_SetOrbOut(sv_w)
		IF(CUST.HOCCM.valType=="L")THEN
		#{
			
			CUST.HOCCM.typOfServ=" "
			sv_w = "typOfServ|" + CUST.HOCCM.typOfServ
			sv_u = urhk_SetOrbOut(sv_w)
			sv_w = "valType|" + CUST.HOCCM.valType
			sv_u = urhk_SetOrbOut(sv_w)
			#}
		ELSE
			#{
			sv_w = "typOfServ|" + CUST.HOCCM.typOfServ
			sv_u = urhk_SetOrbOut(sv_w)
			sv_w = "valType|" + CUST.HOCCM.valType
			sv_u = urhk_SetOrbOut(sv_w)
		#}
		ENDIF
	#}
	ENDIF
	#################UBP CHANGE#############
	ENDOFSCRIPT:
		#----------------------------------------
		# deleting repositories and calsses.
		#----------------------------------------
		IF (CLASSEXISTS("CUST","HOCCM") != 0) THEN
		#{
			DELETECLASS ("CUST","HOCCM")
		#}
		ENDIF
		IF (CLASSEXISTS("CUST","INTERFACE") != 0) THEN
		#{
			DELETECLASS ("CUST","INTERFACE")
		#}
		ENDIF
		IF (REPEXISTS ("CUST") != 0) THEN
		#{
			DELETEREP ("CUST")
		#}
		ENDIF
		EXITSCRIPT
		TRACE OFF
END-->
