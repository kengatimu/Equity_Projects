#----------------------------------------------------------------------------------------------------------
# Filename              :       upmdp013.scr
# Author                :       Hari Mohan Verma
# Description           :       This is the script for verifying data and setting changes to backend
#
# Input                 :       func Code,user id
# Output                :       Details page field values
# Called Script         :       SRV_ModifyUsrProfile_pre_process_data.scr
# Calling Script        :       NA
#
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----        ----------         --------------    ----------------        ---------
#  1           25-06-2013         NIthya Murugesan   original Version          1.0
#-----------------------------------------------------------------------------------------------------------
<--START
CHECKSUM="beb1a532793901e5de4e7be9ec9db9f5134a2d1ef9f7578c947b6717e67bb9a1"
	TRACE ON
	
	#-------------------------------------------------------------
	# Repository creation
	#-------------------------------------------------------------
	IF (REPEXISTS("CUST") == 0 ) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	IF (CLASSEXISTS("CUST","UPM") == 0) THEN
	#{
		CREATECLASS("CUST","UPM",5)
	#}
	ENDIF
	
	sv_a = urhk_SRV_GetVal("usrGenDtl.usrId")
	CUST.UPM.addUserId = BANCS.OUTPARAM.srvValue
	print(CUST.UPM.addUserId)
	
	sv_g = GETPOSITION(ONS.CUSTOM.custDataIn,"~upmgendet")
	sv_g = sv_g-1
	sv_l = STRLEN(ONS.CUSTOM.custDataIn)
	CUST.UPM.classValue = TRIM(MID$(ONS.CUSTOM.custDataIn,sv_g,sv_l))
	print(CUST.UPM.classValue)
	sv_u = urhk_ParseONSCustData(CUST.UPM.classValue)
	print(sv_u)
	IF (FIELDEXISTS(BANCS.INPUT.upmgendet_FIELD_1) != 0) THEN
	#{
		CUST.UPM.tempRoleId = TRIM(BANCS.INPUT.upmgendet_FIELD_1)
		print(CUST.UPM.tempRoleId)
		#}
	ELSE
		#{
		CUST.UPM.tempRoleId = " "
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.upmgendet_FIELD_2) !=0)THEN
	#{
		CUST.UPM.expiryDate = TRIM(BANCS.INPUT.upmgendet_FIELD_2)
		print(CUST.UPM.expiryDate)
		#}
	ELSE
		#{
		CUST.UPM.expiryDate=" "
	#}
	ENDIF
	
	sv_e = urhk_SRV_GetVal("usrGenDtl.usrRole.id")
	print(sv_e)
	IF(sv_e == 0)THEN
	#{
		CUST.UPM.actualRID = BANCS.OUTPARAM.srvValue
		print(CUST.UPM.actualRID)
	#}
	ENDIF
	
	IF(CUST.UPM.expiryDate != "") THEN
	#{
		BANCS.INPARAM.date1 = CUST.UPM.expiryDate
		BANCS.INPARAM.date2 = BANCS.STDIN.BODDate
		
		sv_u = urhk_B2k_dateDiff("")
		IF (sv_u == 0) THEN
		#{
			IF (BANCS.OUTPARAM.dateDiff >= 0) THEN
			#{
				sv_u=urhk_SRV_SetVal("usrGenDtl.usrRole.id|"+CUST.UPM.tempRoleId)
				print(sv_u)
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	#TRACE OFF
END-->
