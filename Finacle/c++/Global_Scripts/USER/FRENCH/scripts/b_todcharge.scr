<--START
CHECKSUM="d662e997f47706b5ad74b6e19febd36c952132f2c962c1d6d8d1fa322ceeac0a"
	TRACE ON
	
	sv_a=urhk_B2k_PrintRepos("BANCS")
	
	sv_a = REPEXISTS("STDWFS")
	IF (sv_a == 0) THEN
	#{
		CREATEREP("STDWFS")
	#}
	ENDIF
	
	sv_a = CLASSEXISTS("STDWFS", "GLBDATA")
	IF (sv_a == 0) THEN
	#{
		CREATECLASS("STDWFS", "GLBDATA", 5)
	#}
	ENDIF
	
	sv_u = urhk_SRV_NumOfRecs("acctTODLL")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
		#STDWFS.GLBDATA.noOfRecords = BANCS.OUTPARAM.srvValue
		sv_f = BANCS.OUTPARAM.srvValue
		PRINT(sv_f)
		#}
	ELSE
		#{
		STDWFS.GLBDATA.errMsg = "Error:urhk_SRV_NumOfRecs failed"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	#	STDWFS.GLBDATA.myBrCode = BANCS.STDIN.myBrCode
	#	PRINT(STDWFS.GLBDATA.myBrCode)
	#
	#	sv_b=urhk_SRV_GetVal("criteria.acct.foracid")
	#	print(sv_b)
	#	STDWFS.GLBDATA.todforacid = BANCS.OUTPARAM.srvValue
	#	print(STDWFS.GLBDATA.todforacid)
	
	sv_m = 0
	sv_a = "\n"
	sv_l = 0
	PRINT(sv_l)
	WHILE(sv_l < cint(sv_f))
	#{
		sv_v = Format$(sv_l,"%d")
		sv_r = "<rec_"+sv_v+">"
		#-------------------------------------------------------------------------------------------
		# Fetch serial num
		#-------------------------------------------------------------------------------------------
		sv_a = REPEXISTS("STDWFS")
		IF (sv_a == 0) THEN
		#{
			CREATEREP("STDWFS")
		#}
		ENDIF
		
		sv_a = CLASSEXISTS("STDWFS", "GLBDATA")
		IF (sv_a == 0) THEN
		#{
			CREATECLASS("STDWFS", "GLBDATA", 5)
		#}
		ENDIF
		
		sv_u = urhk_SRV_GetVal("acctTODLL." +sv_r+ ".todBasicDtl.acct.foracid")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			STDWFS.GLBDATA.todforacid = BANCS.OUTPARAM.srvValue
			PRINT(STDWFS.GLBDATA.todforacid)
			#}
		ELSE
			#{
			STDWFS.GLBDATA.errMsg = "Error: urhk_SRV_GetVal failed for todforacid"
			BANCS.OUTPUT.successOrFailure = "F"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		sv_u = urhk_SRV_GetVal("acctTODLL." +sv_r+ ".key.serial_num")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			STDWFS.GLBDATA.serialnum = BANCS.OUTPARAM.srvValue
			PRINT(STDWFS.GLBDATA.serialnum)
			#}
		ELSE
			#{
			STDWFS.GLBDATA.errMsg = "Error: urhk_SRV_GetVal failed for serialnum"
			BANCS.OUTPUT.successOrFailure = "F"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		sv_u = urhk_SRV_GetVal("acctTODLL." +sv_r+ ".todBasicDtl.todAmtToBeGrntd")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			STDWFS.GLBDATA.AmtToBeGrntdall = BANCS.OUTPARAM.srvValue
			PRINT(STDWFS.GLBDATA.AmtToBeGrntdall)
			
			sv_m = STDWFS.GLBDATA.AmtToBeGrntdall
			print(sv_m)
			
			sv_c = GETPOSITION(sv_m,"|")
			sv_d = STRLEN(sv_m)
			IF (sv_c != 0) THEN
			#{
				sv_a = TRIM(MID$ (sv_m,0,sv_c-1))
				print(sv_a)
				
				STDWFS.GLBDATA.AmtToBeGrntd = sv_a
				print(STDWFS.GLBDATA.AmtToBeGrntd)
				
				
				
				sv_c = GETPOSITION(sv_m,"|")
				print(sv_c)
				sv_d = STRLEN(sv_m)
				print(sv_d)
				IF (sv_c != 0) THEN
				#{
					
					sv_a = TRIM(MID$ (sv_m,sv_c,sv_d))
					print(sv_a)
					
					STDWFS.GLBDATA.AmtToBeGrntdcyc = sv_a
					print(STDWFS.GLBDATA.AmtToBeGrntdcyc)
					
					CALL("b_mtttodcharge.scr")
					#CALL("charlestest.scr")
					
					#				#===============================================================
					#				# executing the script by passing argument as request file.
					#				#===============================================================
					#				sv_a = "exebatch babx4061 $B2K_SESSION_ID b_mtttodcharge.scr "
					#				sv_a = sv_a + STDWFS.GLBDATA.todforacid + " "
					#				sv_a = sv_a + STDWFS.GLBDATA.AmtToBeGrntd + " "
					#				sv_a = sv_a + STDWFS.GLBDATA.AmtToBeGrntdcyc + " "
					#				print(sv_a)
					#				sv_s = system(sv_a)
					#				print(sv_s)
					#
					#				IF ( sv_s == 0 ) THEN
					#				#{
					#					print("Charge Collected Successfully")
					#				#}
					#				ELSE
					#				#{
					#					print("transaction failed")
					#
					#				#}
					#				ENDIF
				#}
				ENDIF
			#}
			ENDIF
			
			#}
		ELSE
			#{
			STDWFS.GLBDATA.errMsg = "Error: urhk_SRV_GetVal failed for AmtToBeGrntd"
			BANCS.OUTPUT.successOrFailure = "F"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		sv_l = sv_l + 1
		PRINT(sv_l)
		print(sv_f)
	#}
	DO
	ENDOFSCRIPT:
		#TRACE OFF
		
		EXITSCRIPT
END-->
