#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: ctodadp018.scr
# event: NA
# description: NA
#=================================================
#----------------------------------------------------------------------------------------------------------
# Filename              :       ctodadp018.scr
# Author                :       Kalvin
# Description           :       This script is used for mfetching history details onclik of request id
#
# Input                 :
# Output                :
# Called Script         :
# Calling Script        :       None
#
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----        ----------         --------------    ----------------        ---------
#  1           27-08-2013         KALVIN    original Version for TO 361667        1.0
#   2		01-10-2013	Kalvin		 Modified for TO :376423	2.0
#						removing Sol constraints
#-----------------------------------------------------------------------------------------------------------
<--START
CHECKSUM="7a6145f0e05a11731f2ed2f11d645260444b3a8dfdf5eb14fc9b1a6df6468994"
	TRACE ON
	
	#============================================================================
	# Create the necessary Repositories and Classes
	#============================================================================
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
		#}
	ELSE
		#{
		DELETEREP("CUST")
		CREATEREP("CUST")
	#}
	ENDIF
	
	
	IF (CLASSEXISTS("CUST", "CATOD") == 0) THEN
	#{
		CREATECLASS("CUST", "CATOD", 5)
		#}
	ELSE
		#{
		DELETECLASS("CUST", "CATOD")
		CREATECLASS("CUST", "CATOD", 5)
	#}
	ENDIF
	
	PRINT(BANCS.INPUT.reqId)
	CUST.CATOD.reqId=	  BANCS.INPUT.reqId
	#CUST.CONTRACT.reqId=	  "27"
	CUST.CATOD.count="0"
	CUST.CATOD.status=" "
	CUST.CATOD.rmrks=" "
	CUST.CATOD.cretUser=" "
	CUST.CATOD.cretDate=" "
	CUST.CATOD.verfdUser="hfh"
	CUST.CATOD.verfdDate=" "
	CUST.CATOD.solId =BANCS.STDIN.mySolId
	CUST.CATOD.bankId=BANCS.STDIN.contextBankId
	
	#availAmt|status|remarks|cretUser|cretDate|lodgedUsr|lodgedDate|verfdUser|verfdDate"
	BANCS.INPARAM.BINDVARS = CUST.CATOD.reqId+"|"+ "N"+"|" + CUST.CATOD.bankId
	sv_q = "count|SELECT count(1) "
	sv_q = sv_q + "FROM CUSTOM.CATOD_AVAILMENT_HIST_TABLE"
	sv_q = sv_q + " WHERE REQUEST_ID=?SVAR"
	sv_q = sv_q + " AND IS_DELETED = ?SVAR"
	sv_q = sv_q + " AND BANK_ID =?SVAR "
	PRINT(sv_q )
	sv_c = urhk_dbSelectWithBind(sv_q)
	print(sv_c)
	IF(sv_c == 0)THEN
	#{
		CUST.CATOD.count=BANCS.OUTPARAM.count
	#}
	ENDIF
	
	sv_u = urhk_SetOrbOut("recordCount|"+CUST.CATOD.count)
	print(sv_u)
	
	
	
	BANCS.INPARAM.BINDVARS = CUST.CATOD.reqId+"|"+ "N"+"|" + CUST.CATOD.bankId
	
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_s = " reqAmount,"
	sv_s = sv_s +"rmrks,cretUser,cretDate,verfdUser,verfdDate|SELECT   "
	sv_s = sv_s + "  REQ_AMT,"
	sv_s = sv_s + "  REMARKS,"
	sv_s = sv_s + "  CREATED_BY,to_char(to_date(CREATED_DATE,'dd-mon-yy'),'dd-mm-yyyy'), "
	sv_s = sv_s + "  VERIFY_BY,to_char(to_date(VERIFY_DATE,'dd-mon-yy'),'dd-mm-yyyy') "
	sv_s = sv_s + "   FROM CUSTOM.CATOD_AVAILMENT_HIST_TABLE  "
	sv_s = sv_s + "   WHERE REQUEST_ID =?SVAR  "
	sv_s = sv_s + " AND IS_DELETED = ?SVAR"
	sv_s = sv_s + "   AND bank_id=?SVAR "
	
	PRINT(sv_s)
	sv_r = urhk_dbCursorOpenWithBind(sv_s)
	
	IF (sv_r != 0) THEN
	#{
		BANCS.OUTPUT.fatalErrorMsg = "Unable to open cursor. Contact system admin"
		BANCS.OUTPUT.fatalErrorFlg = "Y"
		GOTO FINISH
	#}
	ENDIF
	
	CUST.CATOD.cursorNumber = BANCS.OUTPARAM.DB_CURSOR_NUMBER
	sv_r = urhk_dbCursorFetch(CUST.CATOD.cursorNumber)
	
	sv_i = 1
	WHILE(sv_r == 0)
	#{
		PRINT(BANCS.OUTPARAM.reqAmount)
		#CUST.CATOD.reqId = BANCS.OUTPARAM.reqId
		IF (BANCS.OUTPARAM.reqAmount !="") THEN
		#{
			CUST.CATOD.reqAmount= BANCS.OUTPARAM.reqAmount
			#}
		ELSE
			#{
			CUST.CATOD.reqAmount= " "
		#}
		ENDIF
		
		CUST.CATOD.status="APPROVED"
		
		IF (BANCS.OUTPARAM.rmrks!="") THEN
		#{
			CUST.CATOD.rmrks=	  BANCS.OUTPARAM.rmrks
			#}
		ELSE
			#{
			CUST.CATOD.rmrks= " "
		#}
		ENDIF
		
		
		
		IF (BANCS.OUTPARAM.cretUser!="") THEN
		#{
			CUST.CATOD.cretUser=	  BANCS.OUTPARAM.cretUser
			CUST.CATOD.lodgedUser=	  BANCS.OUTPARAM.cretUser
			#}
		ELSE
			#{
			CUST.CATOD.cretUser= " "
			CUST.CATOD.lodgedUser =" "
		#}
		ENDIF
		
		
		IF (BANCS.OUTPARAM.cretDate!="") THEN
		#{
			CUST.CATOD.cretDate=	  BANCS.OUTPARAM.cretDate
			CUST.CATOD.lodgedDate =   BANCS.OUTPARAM.cretDate
			#}
		ELSE
			#{
			CUST.CATOD.cretDate= " "
			CUST.CATOD.lodgedDate =" "
		#}
		ENDIF
		
		
		
		IF (BANCS.OUTPARAM.verfdUser!="") THEN
		#{
			CUST.CATOD.verfdUser=  BANCS.OUTPARAM.verfdUser
			#}
		ELSE
			#{
			CUST.CATOD.verfdUser= " "
		#}
		ENDIF
		
		
		IF (BANCS.OUTPARAM.verfdDate!="") THEN
		#{
			CUST.CATOD.verfdDate=  BANCS.OUTPARAM.verfdDate
			#}
		ELSE
			#{
			CUST.CATOD.verfdDate= " "
		#}
		ENDIF
		
		
		
		
		sv_y = format$(sv_i, "%d")
		print(sv_y)
		
		#availAmt|status|remarks|cretUser|cretDate|lodgedUsr|lodgedDate|verfdUser|verfdDate"
		
		sv_s = "availAmt_" + sv_y + "|" +CUST.CATOD.reqAmount
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		
		sv_s = "status_" + sv_y + "|" +CUST.CATOD.status
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		sv_s = "remarks_" + sv_y + "|" +CUST.CATOD.rmrks
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		sv_s = "cretUser_" + sv_y + "|" +CUST.CATOD.cretUser
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		sv_s = "cretDate_" + sv_y + "|" +CUST.CATOD.cretDate
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		sv_s = "lodgedUsr_" + sv_y + "|" + CUST.CATOD.lodgedUser
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		sv_s = "lodgedDate_" + sv_y + "|" + CUST.CATOD.lodgedDate
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		PRINT(CUST.CATOD.verfdUser)
		sv_s = "verfdUser_" + sv_y + "|" +CUST.CATOD.verfdUser
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		sv_s = "verfdDate_" + sv_y + "|" +CUST.CATOD.verfdDate
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		sv_i = sv_i + 1
		
		print(sv_i)
		
		
		sv_r = urhk_dbCursorFetch(CUST.CATOD.cursorNumber)
		PRINT(sv_r)
	#}
	DO
	#--------------------------------------------------------------------------
	#Close the cursor
	#--------------------------------------------------------------------------
	sv_r = urhk_dbCursorClose(CUST.CATOD.cursorNumber)
	
	FINISH:
		EXITSCRIPT
END-->
