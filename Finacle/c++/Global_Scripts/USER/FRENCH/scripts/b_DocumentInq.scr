
################################################################
########################################################################################################
#Name               :DOC2.scr
#Date               :23-09-2013
#Author             :Marion Kinyanjui
#Description        :This script is used to fetch values from ENTITYDOCUMENT table
#Called script      :None
#Calling Script     :None
#Input              :Cust_id
#Output             :ROWNUM,DOCDESCR,STATUS,REFERENCENUMBER
########################################################################################################
<--start
trace on
print("MARION")

####################################################################
#section      :creating new repository if required
#Description  :Checks for the existence of the repository STDWFS and
#               creates a new one by that name if it does not exist
#####################################################################

IF (REPEXISTS("CUST") == 0 ) THEN
#{
    CREATEREP("CUST")
#}
ENDIF

############################################################################
#section      :creating new class if required
#Desscription :checks for the existence of the class GLBDATA in the
#repository
#               create a new one if it does not exist
##############################################################################


IF (CLASSEXISTS("CUST","DATA") == 0) THEN
#{
    CREATECLASS("CUST","DATA",5)
#}
	ELSE
	#{
		DELETECLASS("CUST","DATA")
		CREATECLASS("CUST","DATA",5)
	#}
	ENDIF

############################################
#Assigns the values to the variables
############################################
sv_a = urhk_b2k_PrintRepos("BANCS")

sv_y=0
print("MARION")
sv_a = urhk_GetChannelInfo("")
print(sv_a)
CUST.DATA.DccId =BANCS.OUTPARAM.DccId
print(CUST.DATA.DccId)

	IF (FIELDEXISTS(BANCS.INPUT.ORGKEY) == 1) THEN
			CUST.DATA.ORGKEY=trim(BANCS.INPUT.ORGKEY)
			print(CUST.DATA.ORGKEY)
			sv_b = "DOCDESCR,STATUS,REFERENCENUMBER|select DOCDESCR,STATUS,REFERENCENUMBER from CRMUSER.ENTITYDOCUMENT  WHERE ORGKEY = ?SVAR " 
			print(sv_b)
			BANCS.INPARAM.BINDVARS = CUST.DATA.ORGKEY 
			print(BANCS.INPARAM.BINDVARS)
			#sv_q= urhk_dbSelectWithBind(sv_b)
			#print(sv_q)
			sv_e = urhk_dbCursorOpenWithBind(sv_b)
	                #IF( sv_e == 0 ) THEN
			sv_m = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		         print(sv_m)
		        sv_n = urhk_dbCursorFetch(sv_m)
	ENDIF

                sv_g = 0
		CUST.DATA.number = "0"
		WHILE(sv_n == 0)
		#{
			sv_b = BANCS.OUTPARAM.REFERENCENUMBER
			sv_c = BANCS.OUTPARAM.DOCDESCR
			sv_d = BANCS.OUTPARAM.STATUS
			
			
			IF (sv_b != "") THEN
			#{
				sv_p = "ACCTLISTStruct.<rec_" + CUST.DATA.number + ">.Acct.REFERENCENUMBER|" + sv_b
				print(sv_p)
				sv_u = urhk_SetOrbOut(sv_p)

			ENDIF
			IF (sv_c != "") THEN
			#{
				sv_p = "ACCTLISTStruct.<rec_" + CUST.DATA.number + ">.Acct.DOCDESCR|" + sv_c
				print(sv_p)
				sv_u = urhk_SetOrbOut(sv_p)

			ENDIF
			IF (sv_d != "") THEN
				#{
					sv_p = "ACCTLISTStruct.<rec_" + CUST.DATA.number + ">.Acct.STATUS|" + sv_d
					print(sv_p)
					sv_u = urhk_SetOrbOut(sv_p)
				
				
			ENDIF
			
			sv_n = urhk_dbCursorFetch(sv_m)
			sv_g = sv_g + 1
			CUST.DATA.number = sv_g
			print(CUST.DATA.number)
		
		DO

		IF(sv_g ==0)THEN
			print(sv_g)
			sv_a = "count|select count(1)from CRMUSER.ENTITYDOCUMENT  WHERE ORGKEY = ?SVAR " 
			print(sv_a)
			BANCS.INPARAM.BINDVARS = CUST.DATA.ORGKEY 
			print(BANCS.INPARAM.BINDVARS)
			sv_q= urhk_dbSelectWithBind(sv_b)
			print(sv_q)
			
			IF(sv_q!=0) THEN
			
		          print("NO documents found")	 
		          sv_u=urhk_SetOrbOut("desciption|NO documents found")
			  
			 ENDIF
		ENDIF
		  sv_n = urhk_dbCursorClose(sv_m)

		exitscript

		
	#ENDIF
	
trace off
end-->
