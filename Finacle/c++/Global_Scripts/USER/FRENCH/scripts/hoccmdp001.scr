#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: hoccmdp001.scr
# event: NA
# description: On onchange event of Zone code in critera page.
#=================================================
#----------------------------------------------------------------------------------------------------------
# Filename              :       hoccmdp001.scr
# Author                :       Nitin Prabhakar
# Description           :       This script is called at onchange event of Zone code in critera page.
# Input                 :       ZoneCode.
# Output                :       Zone currency Code or error message.
# Called Script         :       None
# Calling Script        :       None
#
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----        ----------         --------------    ----------------        ---------
#  1           12-07-2013         Nitin Prabhakar   original Version          1.0
#-----------------------------------------------------------------------------------------------------------

<--START
CHECKSUM="9d8abee54370309ee6a33462f68b2f54de087a5ba7786bb86e533af7bd0e72d7"
	TRACE ON
	#------------------------------------------------------------------------------
	# Creation of  repository and class
	#------------------------------------------------------------------------------
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	IF (CLASSEXISTS("CUST", "HOCCM") == 0) THEN
	#{
		CREATECLASS("CUST", "HOCCM", 5)
	#}
	ENDIF
	IF (CLASSEXISTS("CUST", "INTERFACE") == 0) THEN
	#{
		CREATECLASS("CUST", "INTERFACE", 5)
	#}
	ENDIF
	#--------------------------------------------------------------------------
	# Getting Input at Backend
	#--------------------------------------------------------------------------
	CUST.HOCCM.zoneCode =  TOUPPER(TRIM(BANCS.INPUT.zoneCode))
	print(CUST.HOCCM.zoneCode)
	CUST.HOCCM.bankId = TRIM(BANCS.STDIN.contextBankId)
	print(CUST.HOCCM.bankId)
	#--------------------------------------------------------------------------
	# selecting zone CCY code
	#--------------------------------------------------------------------------
	BANCS.INPARAM.BINDVARS = CUST.HOCCM.zoneCode + "|" + CUST.HOCCM.bankId
	print(BANCS.INPARAM.BINDVARS)
	sv_s = "zoneCCY,zoneIdentifier|"
	sv_s = sv_s + " SELECT ZONE_CRNCY_CODE,HOME_CLG_FLG"
	sv_s = sv_s + " FROM TBAADM.OZC "
	sv_s = sv_s + " WHERE CLG_ZONE_CODE = ?SVAR "
	sv_s = sv_s + " AND BANK_ID = ?SVAR "
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	IF(sv_d != 0)THEN
	#{
		# error
		sv_w = "SUCCESSORFAILURE|N"
		sv_u = urhk_SetOrbOut(sv_w)
		sv_m = "Enter valid Zone code."
		sv_z = "Error|" + sv_m
		sv_u = urhk_SetOrbOut(sv_z)
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		CUST.HOCCM.zoneCCY = BANCS.OUTPARAM.zoneCCY
		print(CUST.HOCCM.zoneCCY)
		CUST.HOCCM.zoneIdentifier = BANCS.OUTPARAM.zoneIdentifier
		print(CUST.HOCCM.zoneIdentifier)
	#}
	ENDIF
	
	
	#--------------------------------------------------------------------------
	# sending values to FE.
	#--------------------------------------------------------------------------
	sv_w = "SuccessOrFailure|Y"
	sv_u = urhk_SetOrbOut(sv_w)
	sv_w = "zoneCCY|" + CUST.HOCCM.zoneCCY
	sv_u = urhk_SetOrbOut(sv_w)
	sv_w = "zoneIdentifier|" + CUST.HOCCM.zoneIdentifier
	sv_u = urhk_SetOrbOut(sv_w)
	
	ENDOFSCRIPT:
		#----------------------------------------
		# deleting repositories and calsses.
		#----------------------------------------
		IF (CLASSEXISTS("CUST","HOCCM") != 0) THEN
		#{
			DELETECLASS ("CUST","HOCCM")
		#}
		ENDIF
		IF (CLASSEXISTS("CUST","INTERFACE") != 0) THEN
		#{
			DELETECLASS ("CUST","INTERFACE")
		#}
		ENDIF
		IF (REPEXISTS ("CUST") != 0) THEN
		#{
			DELETEREP ("CUST")
		#}
		ENDIF
		EXITSCRIPT
		TRACE OFF
END-->
