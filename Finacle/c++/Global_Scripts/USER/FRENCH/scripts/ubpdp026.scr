#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: ubpdp026.scr
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START
CHECKSUM="3090a60c937d7019ad72ce225a1066de9574b80fb4403f9aff04da75b0421d32"
	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp026.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called from crit page link file for fetching biller Id
	# Called Script         :
	# Calling Script        :	function fnBillerId()
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----			 --------          ----------------		 ---------
	#  1           16-04-2013         Swati Ganjoo      Original Version          1.0
	#  2           18-10-2013         Vijaya V		 to display biller name in searcher
	#  3          25-10-2013         Vijaya V		 condition modified to show only context bank id billers in searcher
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	
	#========================================================
	# Check whether CUST Repository exists else create it
	#========================================================
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	#========================================================
	# Check whether UBP Class exists else create it
	#========================================================
	sv_r = CLASSEXISTS("CUST","UBP")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","UBP",5)
	#}
	ENDIF
	#========================================================
	# Initialize the variables
	#========================================================
	
	CUST.UBP.billerId = " "
	CUST.UBP.billAcctId = " "
	CUST.UBP.billAcName = " "
	CUST.UBP.bankId=" "
	CUST.UBP.valType=" "
	CUST.UBP.recordCount = "0"
	#========================================================
	# Fetching the Record count
	#========================================================
	BANCS.INPARAM.BINDVARS = "N|Y|"+ BANCS.STDIN.contextBankId+ "|" + BANCS.STDIN.contextBankId
	sv_s = "recordNum|"
	sv_s = sv_s + " SELECT COUNT(*)"
	sv_s = sv_s + " FROM TBAADM.BRGT "
	sv_s = sv_s + " WHERE NVL(DEL_FLG, 'N') = ?SVAR"
	sv_s = sv_s + " AND NVL(ENTITY_CRE_FLG, 'Y') = ?SVAR"
	sv_s = sv_s + " AND FREE_TEXT1 IS NOT NULL"
	sv_s = sv_s + " AND FREE_TEXT2 = ?SVAR"
	sv_s = sv_s + " AND FREE_TEXT2 IS NOT NULL"
	sv_s = sv_s + " AND FREE_TEXT4 IS NOT NULL"
	sv_s = sv_s + " AND BANK_ID = ?SVAR"
	PRINT(sv_s)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d == 0)THEN
	#{
		CUST.UBP.recordNum = BANCS.OUTPARAM.recordNum
		PRINT(CUST.UBP.recordNum)
	#}
	ENDIF
	sv_u = urhk_SetOrbOut("recCount|"+CUST.UBP.recordNum)
	PRINT(sv_u)
	
	IF(CUST.UBP.recordNum=="0")THEN
	#{
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		#========================================================
		# Fetching tthe values from BRGT table and populating
		# the searcher list
		#========================================================
		
		BANCS.INPARAM.BINDVARS = ""
		BANCS.INPARAM.BINDVARS = "Y|N|"+ BANCS.STDIN.contextBankId+ "|" + BANCS.STDIN.contextBankId
		sv_s = "billerId,billAcctId,billAcName,bankId,chgEvetId,valType|"
		sv_s = sv_s + " SELECT BILLER_ID,FREE_TEXT1,LPAD (BILLER_NAME, 35, ' '),FREE_TEXT2,FREE_TEXT3,FREE_TEXT4"
		#sv_s = sv_s + " SELECT BILLER_ID,FREE_TEXT1,replace(lpad(BILLER_NAME,35,' '),'''','&#8217'),FREE_TEXT2,FREE_TEXT3,FREE_TEXT4"
		sv_s = sv_s + " FROM TBAADM.BRGT "
		sv_s = sv_s + " WHERE NVL(ENTITY_CRE_FLG, 'Y') = ?SVAR"
		sv_s = sv_s + " AND NVL(DEL_FLG, 'N') = ?SVAR"
		sv_s = sv_s + " AND FREE_TEXT1 IS NOT NULL"
		sv_s = sv_s + " AND FREE_TEXT2 = ?SVAR"
		sv_s = sv_s + " AND FREE_TEXT2 IS NOT NULL"
		sv_s = sv_s + " AND FREE_TEXT4 IS NOT NULL"
		sv_s = sv_s + " AND BANK_ID = ?SVAR"
		sv_s = sv_s + " ORDER BY BILLER_ID"
		PRINT(sv_s)
		sv_c = urhk_dbCursorOpenWithBind(sv_s)
		PRINT(sv_c)
		IF(sv_c != 0)THEN
		#{
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		sv_n = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		PRINT(sv_n)
		sv_f = urhk_dbCursorFetch(sv_n)
		PRINT(sv_f)
		sv_i = 1
		WHILE(sv_f == 0)
		#{
			CUST.UBP.billerId = BANCS.OUTPARAM.billerId
			PRINT(CUST.UBP.billerId)
			CUST.UBP.billAcctId = BANCS.OUTPARAM.billAcctId
			PRINT(CUST.UBP.billAcctId)
			CUST.UBP.billAcName = BANCS.OUTPARAM.billAcName
			PRINT(CUST.UBP.billAcName)
			CUST.UBP.bankId = BANCS.OUTPARAM.bankId
			PRINT(CUST.UBP.bankId)
			CUST.UBP.chgEvetId = BANCS.OUTPARAM.chgEvetId
			PRINT(CUST.UBP.chgEvetId)
			CUST.UBP.valType = BANCS.OUTPARAM.valType
			PRINT(CUST.UBP.valType)
			
			sv_s = "billerId_" + Format$(sv_i,"%d") + "|" + CUST.UBP.billerId
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			
			sv_s = "billAcctId_" + Format$(sv_i,"%d") + "|" + CUST.UBP.billAcctId
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			
			sv_s = "billAcName_" + Format$(sv_i,"%d") + "|" + CUST.UBP.billAcName
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			
			sv_s = "bankId_" + Format$(sv_i,"%d") + "|" + CUST.UBP.bankId
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			
			sv_s = "valType_" + Format$(sv_i,"%d") + "|" + CUST.UBP.valType
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			
			sv_i = sv_i + 1
			sv_f = urhk_dbCursorFetch(sv_n)
			PRINT(sv_f)
			
		#}
		DO
		sv_c = urhk_dbCursorClose(sv_n)
		PRINT(sv_c)
	#}
	ENDIF
	
	ENDOFSCRIPT:
END-->
