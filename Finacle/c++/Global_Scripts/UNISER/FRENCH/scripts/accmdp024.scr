#**************************************************************************************************************
# Filename              : accmdp024.scr
# Description           : The script is used to fetch the data on click of req id in inquiry scree
# Input                 :
# Output                :
# Called Script         :
# Calling Script        :
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----          ------           --------          ----------------        ---------
#   1          25-03-2013       Kalvin Joseph     Original Version 361667        1.0
#   2		01-10-2013	Kalvin		 Modified for TO :376423	2.0
#						removing Sol constraints
#**************************************************************************************************************
<--START
CHECKSUM="cf05e10c470af55ccd79811df55cce64caa415195fe1fd881c943424688e7cdc"
	TRACE ON
	
	#============================================================================
	# Create the necessary Repositories and Classes
	#============================================================================
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
		#}
	ELSE
		#{
		DELETEREP("CUST")
		CREATEREP("CUST")
	#}
	ENDIF
	
	
	IF (CLASSEXISTS("CUST", "CONTRACT") == 0) THEN
	#{
		CREATECLASS("CUST", "CONTRACT", 5)
		#}
	ELSE
		#{
		DELETECLASS("CUST", "CONTRACT")
		CREATECLASS("CUST", "CONTRACT", 5)
	#}
	ENDIF
	
	PRINT(BANCS.INPUT.reqId)
	CUST.CONTRACT.reqId=	  BANCS.INPUT.reqId
	#CUST.CONTRACT.reqId=	  "27"
	CUST.CONTRACT.count="0"
	CUST.CONTRACT.reqType=" "
	CUST.CONTRACT.reqAmount=" "
	CUST.CONTRACT.reqPeriod=" "
	CUST.CONTRACT.apprvdAmt=" "
	CUST.CONTRACT.apprvdPerd=" "
	CUST.CONTRACT.status=" "
	CUST.CONTRACT.rmrks=" "
	CUST.CONTRACT.charge=" "
	CUST.CONTRACT.chargeTrn=""
	CUST.CONTRACT.cretUser=" "
	CUST.CONTRACT.cretDate=" "
	CUST.CONTRACT.verfdUser="hfh"
	CUST.CONTRACT.verfdDate=" "
	CUST.CONTRACT.solId =BANCS.STDIN.mySolId
	CUST.CONTRACT.bankId=BANCS.STDIN.contextBankId
	
	
	BANCS.INPARAM.BINDVARS = CUST.CONTRACT.reqId+"|"+ "N"+"|"+ CUST.CONTRACT.bankId
	sv_q = "count|SELECT count(1) "
	sv_q = sv_q + "FROM CUSTOM.CATOD_SANCTION_HIST_TABLE"
	sv_q = sv_q + " WHERE REQUEST_ID=?SVAR"
	sv_q = sv_q + " AND DEL_FLG =?SVAR "
	sv_q = sv_q + " AND BANK_ID =?SVAR "
	PRINT(sv_q )
	sv_c = urhk_dbSelectWithBind(sv_q)
	print(sv_c)
	IF(sv_c == 0)THEN
	#{
		CUST.CONTRACT.count=BANCS.OUTPARAM.count
	#}
	ENDIF
	
	sv_u = urhk_SetOrbOut("recordCount|"+CUST.CONTRACT.count)
	print(sv_u)
	
	
	
	BANCS.INPARAM.BINDVARS = CUST.CONTRACT.reqId+"|"+ "N"+"|"+ CUST.CONTRACT.bankId
	
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_s = " reqType,reqAmount,reqPeriod,apprvdAmt,apprvdPerd,"
	sv_s = sv_s +"status,rmrks,chargeTrn,cretUser,cretDate,verfdUser,verfdDate,entityCreFlg|SELECT   "
	sv_s = sv_s + "  REQ_TYPE,REQ_AMT,REQ_PERIOD,SANCT_AMT,SANCT_PERIOD,"
	sv_s = sv_s + "  STATUS_FLG,REMARKS,FEE_TRAN_ID,"
	sv_s = sv_s + "  CREATED_BY,to_char(to_date(CREATED_DATE,'dd-mon-yy'),'dd-mm-yyyy'), "
	sv_s = sv_s + "  VERIFY_BY,to_char(to_date(VERIFY_DATE,'dd-mon-yy'),'dd-mm-yyyy'),ENTITY_CRE_FLG "
	sv_s = sv_s + "   FROM custom.catod_sanction_HIST_table  "
	sv_s = sv_s + "   WHERE REQUEST_ID =?SVAR  "
	sv_s = sv_s + "   AND del_flg=?SVAR "
	sv_s = sv_s + "   AND bank_id=?SVAR "
	
	PRINT(sv_s)
	sv_r = urhk_dbCursorOpenWithBind(sv_s)
	
	IF (sv_r != 0) THEN
	#{
		BANCS.OUTPUT.fatalErrorMsg = "Unable to open cursor. Contact system admin"
		BANCS.OUTPUT.fatalErrorFlg = "Y"
		GOTO FINISH
	#}
	ENDIF
	
	CUST.CONTRACT.cursorNumber = BANCS.OUTPARAM.DB_CURSOR_NUMBER
	sv_r = urhk_dbCursorFetch(CUST.CONTRACT.cursorNumber)
	
	sv_i = 1
	WHILE(sv_r == 0)
	#{
		PRINT(BANCS.OUTPARAM.reqAmount)
		#CUST.CONTRACT.reqId = BANCS.OUTPARAM.reqId
		IF (BANCS.OUTPARAM.reqAmount !="") THEN
		#{
			CUST.CONTRACT.reqAmount= BANCS.OUTPARAM.reqAmount
			#}
		ELSE
			#{
			CUST.CONTRACT.reqAmount= " "
		#}
		ENDIF
		
		IF (BANCS.OUTPARAM.reqPeriod!="") THEN
		#{
			CUST.CONTRACT.reqPeriod=  BANCS.OUTPARAM.reqPeriod+"MONTHS"
			#}
		ELSE
			#{
			CUST.CONTRACT.reqPeriod= " "
		#}
		ENDIF
		
		IF (BANCS.OUTPARAM.apprvdAmt!="") THEN
		#{
			CUST.CONTRACT.apprvdAmt=  BANCS.OUTPARAM.apprvdAmt
			#}
		ELSE
			#{
			CUST.CONTRACT.apprvdAmt= " "
			#}
			
		#}
		ENDIF
		
		
		IF (BANCS.OUTPARAM.apprvdPerd!="") THEN
		#{
			CUST.CONTRACT.apprvdPerd= BANCS.OUTPARAM.apprvdPerd+"MONTHS"
			#}
		ELSE
			#{
			CUST.CONTRACT.apprvdPerd= " "
		#}
		ENDIF
		
		
		IF (BANCS.OUTPARAM.entityCreFlg!="") THEN
		#{
			CUST.CONTRACT.entityCreFlg =BANCS.OUTPARAM.entityCreFlg
			#}
		ELSE
			#{
			CUST.CONTRACT.entityCreFlg= " "
		#}
		ENDIF
		
		
		IF (BANCS.OUTPARAM.entityCreFlg=="Y") THEN
		#{
			CUST.CONTRACT.status="APPROVED"
			#}
		ELSE
			#{
			CUST.CONTRACT.status="PENDING"
		#}
		ENDIF
		
		IF (BANCS.OUTPARAM.rmrks!="") THEN
		#{
			CUST.CONTRACT.rmrks=	  BANCS.OUTPARAM.rmrks
			#}
		ELSE
			#{
			CUST.CONTRACT.rmrks= " "
		#}
		ENDIF
		
		
		PRINT(BANCS.OUTPARAM.chargeTrn)
		IF (BANCS.OUTPARAM.chargeTrn!="") THEN
		#{
			CUST.CONTRACT.chargeTrn=  BANCS.OUTPARAM.chargeTrn
			#}
		ELSE
			#{
			CUST.CONTRACT.chargeTrn= " "
		#}
		ENDIF
		
		IF((CUST.CONTRACT.chargeTrn=="") OR (CUST.CONTRACT.chargeTrn==" "))THEN
		#{
			CUST.CONTRACT.charge="NO"
			#}
		ELSE
			#{
			CUST.CONTRACT.charge="YES"
		#}
		ENDIF
		
		IF (BANCS.OUTPARAM.cretUser!="") THEN
		#{
			CUST.CONTRACT.cretUser=	  BANCS.OUTPARAM.cretUser
			#}
		ELSE
			#{
			CUST.CONTRACT.cretUser= " "
		#}
		ENDIF
		
		
		IF (BANCS.OUTPARAM.cretDate!="") THEN
		#{
			CUST.CONTRACT.cretDate=	  BANCS.OUTPARAM.cretDate
			#}
		ELSE
			#{
			CUST.CONTRACT.cretDate= " "
		#}
		ENDIF
		
		
		IF (BANCS.OUTPARAM.verfdUser!="") THEN
		#{
			CUST.CONTRACT.verfdUser=  BANCS.OUTPARAM.verfdUser
			#}
		ELSE
			#{
			CUST.CONTRACT.verfdUser= " "
		#}
		ENDIF
		
		
		IF (BANCS.OUTPARAM.verfdDate!="") THEN
		#{
			CUST.CONTRACT.verfdDate=  BANCS.OUTPARAM.verfdDate
			#}
		ELSE
			#{
			CUST.CONTRACT.verfdDate= " "
		#}
		ENDIF
		
		
		IF (BANCS.OUTPARAM.reqType =="F") THEN
		#{
			CUST.CONTRACT.reqType="FRESH"
		#}
		ENDIF
		
		#}
		sv_y = format$(sv_i, "%d")
		print(sv_y)
		
		sv_s = "reqType_" + sv_y + "|" +CUST.CONTRACT.reqType
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		sv_s = "reqAmount_" + sv_y + "|" +CUST.CONTRACT.reqAmount
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		sv_s = "reqPeriod_" + sv_y + "|" +CUST.CONTRACT.reqPeriod
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		sv_s = "apprvdAmt_" + sv_y + "|" +CUST.CONTRACT.apprvdAmt
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		sv_s = "apprvdPerd_" + sv_y + "|" +CUST.CONTRACT.apprvdPerd
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		sv_s = "status_" + sv_y + "|" +CUST.CONTRACT.status
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		sv_s = "rmrks_" + sv_y + "|" +CUST.CONTRACT.rmrks
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		sv_s = "charge_" + sv_y + "|" +CUST.CONTRACT.charge
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		sv_s = "chargeTrn_" + sv_y + "|" +CUST.CONTRACT.chargeTrn
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		sv_s = "cretUser_" + sv_y + "|" +CUST.CONTRACT.cretUser
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		sv_s = "cretDate_" + sv_y + "|" +CUST.CONTRACT.cretDate
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		PRINT(CUST.CONTRACT.verfdUser)
		sv_s = "verfdUser_" + sv_y + "|" +CUST.CONTRACT.verfdUser
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		sv_s = "verfdDate_" + sv_y + "|" +CUST.CONTRACT.verfdDate
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		sv_i = sv_i + 1
		
		print(sv_i)
		
		
		sv_r = urhk_dbCursorFetch(CUST.CONTRACT.cursorNumber)
		PRINT(sv_r)
	#}
	DO
	#--------------------------------------------------------------------------
	#Close the cursor
	#--------------------------------------------------------------------------
	sv_r = urhk_dbCursorClose(CUST.CONTRACT.cursorNumber)
	
	FINISH:
		EXITSCRIPT
END-->
