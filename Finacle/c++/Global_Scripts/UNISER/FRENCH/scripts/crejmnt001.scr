#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: crejmnt001.scr
# Type: NA|default
# Description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
# Author: Anshul_Singh04
# Date: 7/5/18 3:54
# Modification Log:
#============================================================

<--START
CHECKSUM="3d682ee3fd9a5aa91dab1200024c6521ce78af0bae8b213157b3304ff37b5dc3"
	
	TRACE ON
	
	sv_u = urhk_B2K_PrintRepos("BANCS")
	print(sv_u)
	
	sv_a = REPEXISTS("EQT")
	IF (sv_a == 0) THEN
	#{
		CREATEREP("EQT")
	#}
	ENDIF
	sv_a = CLASSEXISTS("EQT", "REJ")
	IF (sv_a == 0 ) THEN
	#{
		CREATECLASS("EQT", "REJ", 5)
	#}
	ENDIF
	EQT.REJ.successOrFailure=""
	EQT.REJ.queryTbl= " "
	EQT.REJ.funcCode=TRIM(BANCS.INPUT.funcCode)
	PRINT(EQT.REJ.funcCode)
	PRINT(BANCS.INPUT.rejCode)
	EQT.REJ.rejCode=BANCS.INPUT.rejCode
	
	sv_a=0
	sv_b=0
	sv_c=" "
	sv_d=0
	
	sv_m = "recCountMod|SELECT count(1)"
	sv_m= sv_m +" FROM CUSTOM.RRCM_MOD WHERE bank_id=?SVAR and OLD_CLG_REJ_CODE=?SVAR "
	
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + EQT.REJ.rejCode
	
	PRINT(sv_m)
	sv_r = urhk_dbselectWithBind(sv_m)
	
	IF (sv_r != 0) THEN
	#{
		sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
		sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		sv_d=BANCS.OUTPARAM.recCountMod
		print(sv_d)
	#}
	ENDIF
	
	
	IF((sv_d==0) AND (EQT.REJ.funcCode=="V"))THEN
	#{
		sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
		sv_t= urhk_SetOrbOut("Error_1|ERR^No Record Pending for Verification for the Selected Reason Code ^Go")
		GOTO ENDOFSCRIPT
	#}
	endif
	
	IF((sv_d==0) AND (EQT.REJ.funcCode=="X"))THEN
	#{
		sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
		sv_t= urhk_SetOrbOut("Error_1|ERR^No Record Modified for the Selected Reason Code ^Go")
		GOTO ENDOFSCRIPT
	#}
	endif
	
	sv_m = "recCount|SELECT count(1)"
	sv_m= sv_m +" FROM CUSTOM.RRCM WHERE bank_id=?SVAR and OLD_CLG_REJ_CODE=?SVAR "
	
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + EQT.REJ.rejCode
	
	PRINT(sv_m)
	sv_r = urhk_dbselectWithBind(sv_m)
	
	IF (sv_r != 0) THEN
	#{
		sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
		sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		sv_a= BANCS.OUTPARAM.recCount
		print(sv_a)
	#}
	ENDIF
	
	
	IF( (EQT.REJ.funcCode=="M") OR (EQT.REJ.funcCode=="X") OR (EQT.REJ.funcCode=="V") OR (EQT.REJ.funcCode=="D"))THEN
	#{
		IF((sv_a==0) AND (sv_d== 0))THEN
		#{
			print(sv_d)
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^No Record Exists for the Selected Reason Code^Go")
			GOTO ENDOFSCRIPT
		#}
		endif
		
		IF((sv_a==0) AND (sv_d!= 0) AND (EQT.REJ.funcCode=="D"))THEN
		#{
			print(sv_d)
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Cannot Delete the Record. Verification Pending !!!^Go")
			GOTO ENDOFSCRIPT
		#}
		endif
		
		IF((sv_a!=0) AND (sv_d== 0))THEN
		#{
			GOTO FETCHDATA
		#}
		endif
		
		sv_k = "lchgUSerID|SELECT distinct LCHG_USER_ID"
		sv_k= sv_k +" FROM CUSTOM.RRCM_MOD WHERE bank_id=?SVAR and OLD_CLG_REJ_CODE=?SVAR "
		
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + EQT.REJ.rejCode
		
		PRINT(sv_k)
		sv_r = urhk_dbselectWithBind(sv_k)
		
		IF (sv_r != 0) THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			EQT.REJ.lchgUSerID= BANCS.OUTPARAM.lchgUSerID
			print(EQT.REJ.lchgUSerID)
			
			IF((sv_d!=0) AND (EQT.REJ.funcCode=="V") AND (EQT.REJ.lchgUSerID==BANCS.STDIN.userId))THEN
			#{
				sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
				sv_t= urhk_SetOrbOut("Error_1|ERR^Enterer cannot verify the Record !!!^Go")
				GOTO ENDOFSCRIPT
			#}
			endif
			
			IF((sv_d!=0)AND (EQT.REJ.funcCode!="V")) THEN
			#{
				IF(EQT.REJ.lchgUSerID!=BANCS.STDIN.userId)THEN
				#{
					sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
					sv_t= urhk_SetOrbOut("Error_1|ERR^Only Enterer can Modify/Cancel the Record^Go")
					GOTO ENDOFSCRIPT
					#}
				ELSE
					#{
					EQT.REJ.queryTbl="_MOD"
					GOTO FETCHDATA
				#}
				endif
			#}
			endif
		#}
		ENDIF
	#}
	endif
	
	IF((sv_a==0) AND (EQT.REJ.funcCode=="M") AND (sv_d!=0))THEN
	#{
		EQT.REJ.queryTbl="_MOD"
	#}
	endif
	
	IF( (sv_a==0)) THEN
	#{
		IF((EQT.REJ.funcCode=="I")) THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^No Record Exists for the Selected Reason Code^Go")
			GOTO ENDOFSCRIPT
		#}
		endif
	#}
	endif
	
	IF(EQT.REJ.funcCode=="A")THEN
	#{
		
		IF(sv_a!= 0)THEN
		#{
			print(sv_a)
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Record Already Exists for the Selected Reason Code^Go")
			GOTO ENDOFSCRIPT
		#}
		endif
		
		IF(sv_d!= 0)THEN
		#{
			print(sv_d)
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Record Already Present. Verification Pending !!!^Go")
			GOTO ENDOFSCRIPT
		#}
		endif
		
		sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	FETCHDATA:
		CALL("crejFetchData.scr")
		IF (EQT.REJ.successOrFailure!= "F") THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
			#}
		ELSE
			#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			sv_u = urhk_SetOrbOut("Error_1|ERR^"+EQT.REJ.scrMsgFields)
		#}
		ENDIF
		#TRACE OFF
		
	ENDOFSCRIPT:
		DELETECLASS("EQT", "REJ")
		DELETEREP("EQT")
		
		EXITSCRIPT
		TRACE OFF
END-->
