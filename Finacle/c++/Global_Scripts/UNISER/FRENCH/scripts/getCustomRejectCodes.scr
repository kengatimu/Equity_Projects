#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: getEQTomRejectCodes.scr
# Type: NA|default
# Description: EQTom Reject Reason Codes
# Author: Anshul_Singh04
# Date: 5/8/18 3:38 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="a6ab4303dc22618b61744afea1bd4a5c20c6fd9643c8fdd4a313275af41dfe9e"
	# Script body goes here
	
	TRACE ON
	
	###########################################################################################
	# Check whether EQT Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("EQT")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("EQT")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether REJ Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("EQT","REJ")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("EQT","REJ",5)
	#}
	ENDIF
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	EQT.REJ.oldRejCode = ""
	EQT.REJ.rejCodeDesc = " "
	EQT.REJ.newRejCode = ""
	EQT.REJ.delFlg = "N"
	EQT.REJ.recordCount ="0"
	
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
	
	sv_s = "recordCount|SELECT count(1) FROM tbaadm.crc WHERE del_flg='N' AND bank_id = ?SVAR"
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	IF(sv_d == 0)THEN
	#{
		EQT.REJ.recordCount = BANCS.OUTPARAM.recordCount
		print(EQT.REJ.recordCount)
	#}
	ENDIF
	
	sv_u = urhk_SetOrbOut("recordCount|"+EQT.REJ.recordCount)
	print(sv_u)
	
	sv_s = "oldRejCode,newRejCode,rejCodeDesc|"
	sv_s = sv_s + " SELECT crc.CLG_REJ_CODE, nvl(rrcm.CLG_REJ_CODE, crc.CLG_REJ_CODE), CLG_REJ_DESC,DEL_FLG FROM tbaadm.crc LEFT JOIN custom.rrcm on  crc.CLG_REJ_CODE = rrcm.OLD_CLG_REJ_CODE "
	sv_s = sv_s + " AND crc.bank_id= rrcm.bank_id WHERE CRC.DEL_FLG='N'  AND CRC.bank_id = ?SVAR ORDER BY CRC.CLG_REJ_CODE"
	print(sv_s)
	
	sv_c = urhk_dbCursorOpenWithBind(sv_s)
	print(sv_c)
	
	IF(sv_c != 0)THEN
	#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_n = BANCS.OUTPARAM.DB_CURSOR_NUMBER
	print(sv_n)
	
	sv_f = urhk_dbCursorFetch(sv_n)
	print(sv_f)
	
	sv_i = 1
	
	WHILE(sv_f == 0)
	#{
		EQT.REJ.oldRejCode = BANCS.OUTPARAM.oldRejCode
		EQT.REJ.rejCodeDesc = BANCS.OUTPARAM.rejCodeDesc
		EQT.REJ.newRejCode = BANCS.OUTPARAM.newRejCode
		#EQT.REJ.delFlg = BANCS.OUTPARAM.delFlg
		
		PRINT(EQT.REJ.oldRejCode)
		PRINT(EQT.REJ.rejCodeDesc)
		PRINT(EQT.REJ.newRejCode)
		#PRINT(EQT.REJ.delFlg)
		
		#####################################################################
		# Code To Replace Special Character ' before setting value in Searcher
		#####################################################################
		
		lv_a = EQT.REJ.rejCodeDesc
		
		lv_l = STRLEN(lv_a)
		lv_n = 0
		lv_s = ""
		WHILE(lv_n < lv_l)
		#{
			PRINT(lv_a)
			PRINT(lv_n)
			lv_c = ""
			lv_c = MID$(lv_a,lv_n,1)
			PRINT(lv_c)
			
			IF(lv_c == "'")THEN
			#{
				PRINT("Found One")
				lv_s = lv_s + ""
				PRINT(lv_s)
				GOTO NEXT_CHAR1
			#}
			ENDIF
			lv_s = lv_s + lv_c
			PRINT(lv_s)
			
			NEXT_CHAR1:
				
				lv_n = lv_n + 1
			#}
			DO
			
			PRINT(lv_a)
			PRINT(lv_s)
			
			EQT.REJ.rejCodeDesc=lv_s
			
			#####################################################################
			
			sv_s = "oldRejCode_" + Format$(sv_i,"%d") + "|" + EQT.REJ.oldRejCode
			print(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			print(sv_u)
			
			sv_s = "newRejCode_" + Format$(sv_i,"%d") + "|" + EQT.REJ.newRejCode
			print(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			print(sv_u)
			
			sv_s = "rejCodeDesc_" + Format$(sv_i,"%d") + "|" + EQT.REJ.rejCodeDesc
			print(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			print(sv_u)
			
			#			sv_s = "delFlg_" + Format$(sv_i,"%d") + "|" + EQT.REJ.delFlg
			#			print(sv_s)
			#			sv_u = urhk_SetOrbOut(sv_s)
			#			print(sv_u)
			
			sv_i = sv_i + 1
			print(sv_i)
			
			print(sv_n)
			sv_f = urhk_dbCursorFetch(sv_n)
			print(sv_f)
			
		#}
		DO
		
		sv_c = urhk_dbCursorClose(sv_n)
		print(sv_c)
		
		
	ENDOFSCRIPT:
		DELETECLASS("EQT", "REJ")
		DELETEREP("EQT")
		
		EXITSCRIPT
		TRACE OFF
		
END-->
