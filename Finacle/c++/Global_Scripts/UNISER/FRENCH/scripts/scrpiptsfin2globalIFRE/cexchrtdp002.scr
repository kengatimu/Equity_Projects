#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cexchrtdp002.scr
# Type: NA|default
# Description: cexchrtdp002.scr
# Author: shivakrishna.d
# Date: 8/27/18 4:46 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="31bfab325e29f32358dba4cd862c5a28f37fc6d6528ef374851de1b22d835496"
	# Script body goes here
	TRACE ON
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CEXCH Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CEXCH")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CEXCH",5)
	#}
	ENDIF
	#==========================================================*
	# Intilializing the Variables
	#==========================================================*
	CUST.CEXCH.count="0"
	CUST.CEXCH.errorFlg = "N"
	CUST.CEXCH.errorMsg = " "
	CUST.CEXCH.ccyCode=" "
	CUST.CEXCH.ccyDesc=" "
	IF(FIELDEXISTS(BANCS.INPUT.ccyCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.ccyCode) != "")THEN
		#{
			CUST.CEXCH.ccyCode = TOUPPER(TRIM(BANCS.INPUT.ccyCode))
			PRINT(CUST.CEXCH.ccyCode)
		#}
		ELSE
		#{
			GOTO ENDOFSCRIPT
		ENDIF
		#}
	ELSE
		#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	sv_s="count|SELECT COUNT(1) FROM TBAADM.CNC WHERE BANK_ID=?SVAR AND CRNCY_CODE=?SVAR AND ENTITY_CRE_FLG='Y' AND DEL_FLG='N'"
	BANCS.INPARAM.BINDVARS=BANCS.STDIN.contextBankId + "|"+ CUST.CEXCH.ccyCode
	PRINT(sv_s)
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d != 0)THEN
	#{
		
		#sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
		#sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
		CUST.CEXCH.errorFlg = "Y"
		CUST.CEXCH.errorMsg = "Error in Fetching Data"
		
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	CUST.CEXCH.count =BANCS.OUTPARAM.count
	IF(CUST.CEXCH.count == "0") THEN
	#{
		#sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
		#sv_t= urhk_SetOrbOut("Invalid Currency Code")
		CUST.CEXCH.errorFlg = "Y"
		CUST.CEXCH.errorMsg = "Invalid Currency Code"
		GOTO ENDOFSCRIPT
		
	#}
	ENDIF

	IF(CUST.CEXCH.count > "0") THEN
	#{
        sv_s="ccyDesc|SELECT CRNCY_NAME FROM TBAADM.CNC WHERE BANK_ID=?SVAR AND CRNCY_CODE=?SVAR AND ENTITY_CRE_FLG='Y' AND DEL_FLG='N'"
        BANCS.INPARAM.BINDVARS=BANCS.STDIN.contextBankId + "|"+ CUST.CEXCH.ccyCode
        PRINT(sv_s)
        PRINT(BANCS.INPARAM.BINDVARS)
        sv_d = urhk_dbSelectWithBind(sv_s)
        PRINT(sv_d)
        IF(sv_d != 0)THEN
        #{

                #sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
                #sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
                CUST.CEXCH.errorFlg = "Y"
                CUST.CEXCH.errorMsg = "Error in Fetching Data"

                GOTO ENDOFSCRIPT
        #}
        ENDIF
        CUST.CEXCH.ccyDesc =BANCS.OUTPARAM.ccyDesc
	#}
	ENDIF
	#sv_s=urhk_SetOrbOut("SuccessOrFailure|Y")
	ENDOFSCRIPT:
		sv_s=urhk_SetOrbOut("errorFlg|"+CUST.CEXCH.errorFlg)
		sv_s=urhk_SetOrbOut("errorMsg|"+CUST.CEXCH.errorMsg)
		sv_s=urhk_SetOrbOut("ccyCode|"+CUST.CEXCH.ccyCode)
		sv_s=urhk_SetOrbOut("ccyDesc|"+CUST.CEXCH.ccyDesc)
		TRACE OFF
END-->
