#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: TEST878.scr
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START
CHECKSUM="86b5d50086b2a6114b30e5e21202dff82f4afd2b8c00cd68665efd85c1daa6d1"
	# Script body goes here
	TRACE ON
	##############################################
	# Creation of Repository and Class and Initialization
	##############################################
	
	sv_r = REPEXISTS("CUST")
	IF (sv_r == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	sv_r = CLASSEXISTS("CUST","FIINBOUND")
	IF (sv_r == 0) THEN
	#{
		CREATECLASS("CUST","FIINBOUND",5)
	#}
	ENDIF
	
	sv_r = CLASSEXISTS("CUST","INTERFACE")
	IF (sv_r == 0) THEN
	#{
		CREATECLASS("CUST","INTERFACE",5)
	#}
	ENDIF
	
	CUST.INTERFACE.successOrFailure="S"
	CUST.INTERFACE.Message="Success"
	CUST.INTERFACE.AcctInpInd="Y"
	CUST.INTERFACE.CustInpInd="Y"
	CUST.INTERFACE.CustValInd="N"
	CUST.INTERFACE.AcctValInd="N"
	#sv_b = "DUMMYCIF7"
	#sv_d = "0016000100009"
	#############################################
	# Getting Inputs from signatureAdd API
	#############################################
	
	#sv_a = "ACCOUNT_ID"
	
	
	
	
	
	
	
	IF((FIELDEXISTS(BANCS.INPUT.ACCOUNT_ID)) == 0)THEN
	#{
		CUST.INTERFACE.Message="AccountId not provided"
		CUST.INTERFACE.AcctInpInd="N"
		
		#}
	ELSE
		#{
		CUST.INTERFACE.AcctInpInd="Y"
		#CUST.FIINBOUND.acctNum = sv_d
		CUST.FIINBOUND.acctNum = BANCS.INPUT.ACCOUNT_ID
	#}
	ENDIF
	#sv_c = "CUST_ID"
	
	IF((FIELDEXISTS(BANCS.INPUT.CUST_ID))== 0)THEN
	#{
		CUST.INTERFACE.Message="CustId not provided"
		CUST.INTERFACE.CustInpInd="N"
		#}
	ELSE
		#{
		print("4")
		CUST.INTERFACE.CustInpInd="Y"
		CUST.FIINBOUND.custId = BANCS.INPUT.CUST_ID
		#CUST.FIINBOUND.custId = sv_b
	#}
	ENDIF
	
	IF((CUST.INTERFACE.AcctInpInd == "N") AND (CUST.INTERFACE.CustInpInd == "N"))THEN
	#{
		CUST.INTERFACE.successOrFailure="F"
		CUST.INTERFACE.Message="Inputs not provided"
		PRINT("failure")
		GOTO END
	#}
	ENDIF
	IF(CUST.INTERFACE.CustInpInd =="Y")THEN
	#{
		
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" +  CUST.FIINBOUND.custId+"|Y|N"
		print(BANCS.INPARAM.BINDVARS)
		sv_s = "count|"
		sv_s = sv_s + " SELECT count(1)"
		sv_s = sv_s + " FROM TBAADM.GAM"
		sv_s = sv_s + " WHERE BANK_ID =?SVAR"
		sv_s = sv_s + " AND CUST_ID =?SVAR"
		sv_s = sv_s + " AND ENTITY_CRE_FLG =?SVAR"
		sv_s = sv_s + " AND NVL(DEL_FLG, 'N') = ?SVAR"
		print(sv_s)
		sv_d =  urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		IF (sv_d == 0 ) THEN
		#{
			CUST.FIINBOUND.count = BANCS.OUTPARAM.count
			print(CUST.FIINBOUND.count)
			IF(CUST.FIINBOUND.count=="0")THEN
			#{
				CUST.INTERFACE.CustValInd="N"
				CUST.INTERFACE.successOrFailure="F"
				CUST.INTERFACE.Message="Invalid CIF ID"
				GOTO END
				#}
			ELSE
				#{
				CUST.INTERFACE.CustValInd="Y"
				CUST.INTERFACE.successOrFailure="S"
				CUST.INTERFACE.Message="Valid CustId"
			#}
			ENDIF
			#}
		ELSE
			#{
			CUST.INTERFACE.CustValInd="N"
			CUST.INTERFACE.successOrFailure="F"
			CUST.INTERFACE.Message="Error in fetching cif id"
			GOTO END
			#}
		#}
		ENDIF
		#}
	#}
	ENDIF
	#############################################
	# If acctId is provided, validating it and comparing custId.
	#############################################
	IF(CUST.INTERFACE.AcctInpInd == "Y")THEN
	#{
		print(CUST.FIINBOUND.acctNum)
		sv_b = urhk_getAcctDetailsInRepository(CUST.FIINBOUND.acctNum)
		print(sv_b)
		IF(sv_b == 1) THEN
		#{
			CUST.INTERFACE.AcctValInd="N"
			CUST.INTERFACE.successOrFailure="F"
			CUST.INTERFACE.Message="Invalid account Id"
			GOTO END
		#}
		ENDIF
		
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" +  CUST.FIINBOUND.acctNum+"|Y|N"
		print(BANCS.INPARAM.BINDVARS)
		sv_s = "custId|"
		sv_s = sv_s + " SELECT CUST_ID"
		sv_s = sv_s + " FROM TBAADM.GAM"
		sv_s = sv_s + " WHERE BANK_ID =?SVAR"
		sv_s = sv_s + " AND FORACID =?SVAR"
		sv_s = sv_s + " AND ENTITY_CRE_FLG =?SVAR"
		sv_s = sv_s + " AND NVL(DEL_FLG, 'N') = ?SVAR"
		print(sv_s)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		IF (sv_d == 0 ) THEN
		#{
			CUST.FIINBOUND.customerId = BANCS.OUTPARAM.custId
			PRINT(CUST.FIINBOUND.customerId)
			IF(CUST.FIINBOUND.customerId=="")THEN
			#{
				CUST.INTERFACE.AcctValInd="S"
				IF((CUST.INTERFACE.CustInpInd == "Y"))THEN
				#{
					
					CUST.INTERFACE.successOrFailure="F"
					CUST.INTERFACE.Message="Account provided is office account. custId should not be provided."
					PRINT(CUST.INTERFACE.Message)
					GOTO END
					#}
				ELSE
					#{
					CUST.INTERFACE.successOrFailure="S"
					CUST.INTERFACE.Message="Office account."
				#}
				ENDIF
				#}
			ELSE
				#{
				IF(CUST.INTERFACE.CustValInd == "Y")THEN
				#{
					IF(CUST.FIINBOUND.customerId != CUST.FIINBOUND.custId)THEN
					#{
						print("14")
						CUST.INTERFACE.successOrFailure="F"
						CUST.INTERFACE.Message="Account does not belong to provided cust Id"
						GOTO END
						#}
					ELSE
						#{
						
						CUST.INTERFACE.successOrFailure="S"
						CUST.INTERFACE.Message="Success - valid account AND custId"
					#}
					ENDIF
					#}
				ELSE
					#{
					CUST.INTERFACE.successOrFailure="S"
					CUST.INTERFACE.Message="Success - valid account"
				#}
				ENDIF
				
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	END:
		print(CUST.INTERFACE.successOrFailure)
		print(CUST.INTERFACE.Message)
		
		sv_u=urhk_SetOrbOut("RESPONSE|" + CUST.INTERFACE.successOrFailure)
		sv_u=urhk_SetOrbOut("MESSAGE|" + CUST.INTERFACE.Message)
		TRACE OFF
		exitscript
		
END-->
