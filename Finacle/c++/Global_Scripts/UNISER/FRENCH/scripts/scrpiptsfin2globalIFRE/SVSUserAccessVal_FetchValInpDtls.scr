#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: SVSUserAccessVal_FetchValInpDtls.scr
# event: NA
# description: NA
#=================================================
#----------------------------------------------------------------------------------------------------------
# Filename              :       SVSUserAccessVal_FetchValInpDtls.scr
# Author                :       
# Description           :       This script fetches and validates the values passed in request xml
# Input                 :       Request XML
# Output                :       Response XML
# Called Script         :       None
# Calling Script        :       Common_Interface_CreateRepos.scr
#								
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----           -----           --------          ----------------        ---------
#  1           07-03-2013          			          Original Version          1.0
#-----------------------------------------------------------------------------------------------------------
<--start
trace on
	
#----------------------------------------------------------------
# Fetching Account ID from request XML
#----------------------------------------------------------------
sv_v = "SVSUserAccessVal_REQ.AccountID"
IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
#{

	CUST.INTERFACE.accountID =("BANCS").("INPUT").(sv_v)
#}
ELSE
#{
    CUST.INTERFACE.accountID = ""
#}
ENDIF

#----------------------------------------------------------------
# Fetching User ID from request XML
#----------------------------------------------------------------
sv_v = "SVSUserAccessVal_REQ.UserID"
IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
#{

	CUST.INTERFACE.userID =("BANCS").("INPUT").(sv_v)
#}
ELSE
#{
    CUST.INTERFACE.userID = ""
#}
ENDIF

#----------------------------------------------------------------
# Fetching Bank ID from request XML
#----------------------------------------------------------------
sv_v = "SVSUserAccessVal_REQ.BankID"
IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
#{

	CUST.INTERFACE.bankID =("BANCS").("INPUT").(sv_v)
#}
ELSE
#{
    CUST.INTERFACE.bankID = ""
#}
ENDIF

#----------------------------------------------------------------
# Validation for Account ID, User ID and Bank ID
#----------------------------------------------------------------
print(CUST.INTERFACE.accountID)
print(CUST.INTERFACE.userID)
print(CUST.INTERFACE.bankID)

#------------------------------------------------------------
# If no inputs are passed then error to be thrown in response
#------------------------------------------------------------
IF((CUST.INTERFACE.accountID == "") AND (CUST.INTERFACE.userID == "") AND (CUST.INTERFACE.bankID == "")) THEN
#{
    CUST.INTERFACE.successOrFailure = "F"
	CUST.INTERFACE.errorCode = "ERR001"
    GOTO ERROREXIT
#}
ENDIF

#----------------------------------------------
# Validate Account ID
#----------------------------------------------
IF(CUST.INTERFACE.accountID == "") THEN
#{
	CUST.INTERFACE.successOrFailure = "F"
	CUST.INTERFACE.errorCode = "ERR023"
	GOTO ERROREXIT

#}
ELSE
#{
	#Validation for account id is done along with the GAM query in next script
#}
ENDIF
	
	
#-------------------------------------------------------------------------------------------------
# Validating if the given BankId is valid one or not
#--------------------------------------------------------------------------------------------------
IF(CUST.INTERFACE.bankID == "") THEN		
#{
	CUST.INTERFACE.successOrFailure = "F"
	CUST.INTERFACE.errorCode = "ERR032"
	GOTO ERROREXIT
#}
ELSE
#{
	BANCS.INPARAM.BINDVARS = CUST.INTERFACE.bankID + "|" + "N"
	sv_s= "count|"
	sv_s = sv_s + "SELECT  COUNT(*) FROM  TBAADM.GCT "
	sv_s = sv_s + "WHERE BANK_ID = ?SVAR "
	sv_s = sv_s + "AND DEL_FLG = ?SVAR "
	print(sv_s)
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_s)
	IF (sv_d == 0) THEN
	#{
		CUST.INTERFACE.count=BANCS.OUTPARAM.count
		IF (CUST.INTERFACE.count == 0) THEN
		#{
			CUST.INTERFACE.successOrFailure = "F"
			CUST.INTERFACE.errorCode = "ERR033"
			GOTO ERROREXIT
		#}
		ENDIF
	#}
	ENDIF
#}
ENDIF
			
#--------------------------------------------------------------------------------
# Validating User ID
#--------------------------------------------------------------------------------
IF(CUST.INTERFACE.userID ==" ")THEN
#{
	CUST.INTERFACE.successOrFailure = "F"
	CUST.INTERFACE.errorCode = "ERRoo06"
	GOTO ERROREXIT
#}
ELSE
#{
	BANCS.INPARAM.BINDVARS=CUST.INTERFACE.userID+"|Y|N"
	print(BANCS.INPARAM.BINDVARS)
	sv_s = "count|"
	sv_s = sv_s + " SELECT count(*)"
	sv_s = sv_s + " FROM TBAADM.UPR"
	sv_s = sv_s + " WHERE USER_ID =?SVAR"
	sv_s = sv_s + " AND ENTITY_CRE_FLG =?SVAR"
	sv_s = sv_s + " AND NVL(DEL_FLG, 'N') = ?SVAR"
	print(sv_s)
	sv_d =  urhk_dbSelectWithBind(sv_s)
    print(sv_d)
    IF(sv_d == 0) THEN
    #{
    	CUST.INTERFACE.count=BANCS.OUTPARAM.count	
	IF(CUST.INTERFACE.count == 0) THEN
    	#{
	    	CUST.INTERFACE.successOrFailure="F"
			CUST.INTERFACE.errorCode="ERR009"
			GOTO ERROREXIT
		#}
		ENDIF
	#}
	ENDIF
#}
ENDIF
	
                	
#trace off                
end-->

