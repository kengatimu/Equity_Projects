#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: Actual_sanction_accs_custval.scr
# Type: NA|default
# Description: Actual_sanction_accs_custval.scr
# Author:
# Date:
# Modification Log:
#============================================================

<--START
CHECKSUM="1471047a5995973aba9bd1e8c8386c4be429fd421ff176e0c5512c536c9aa80d"
	TRACE ON
	sv_i = urhk_b2k_printRepos("BANCS")
	PRINT(BANCS.FINRPTDEF.SOL_ID)
	#print(BANCS.FINRPTDEF.REQUEST_ID)
	
	IF(FIELDEXISTS(BANCS.FINRPTDEF.ACCT_NUM))THEN
	#{
		PRINT(BANCS.FINRPTDEF.ACCT_NUM)
	#}
	ELSE
	#{
		BANCS.FINRPTDEF.ACCT_NUM = ""
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.FINRPTDEF.FROM_DATE))THEN
	#{
		PRINT(BANCS.FINRPTDEF.FROM_DATE)
	#}
	ELSE
	#{
		BANCS.FINRPTDEF.FROM_DATE = ""
		IF(FIELDEXISTS(BANCS.FINRPTDEF.TO_DATE))THEN
		#{
			PRINT(BANCS.FINRPTDEF.TO_DATE)
		#}
		ELSE
		#{
			BANCS.FINRPTDEF.TO_DATE = ""
		#}
		ENDIF
		IF(FIELDEXISTS(BANCS.FINRPTDEF.STATUS_FLG))THEN
		#{
			PRINT(BANCS.FINRPTDEF.STATUS_FLG)
		#}
		ELSE
		#{
			BANCS.FINRPTDEF.STATUS_FLG = ""
		#}
		ENDIF
		IF(BANCS.FINRPTDEF.SOL_ID == "") THEN
		#{
			sv_u = Urhk_FINRPT_SetErr("SOL_ID|SOL_ID must be entered")
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			IF(BANCS.FINRPTDEF.SOL_ID != "ALL") THEN
			#{
				sv_u = urhk_B2k_valSolId(BANCS.FINRPTDEF.SOL_ID)
				IF(sv_u != 0) THEN
				#{
					sv_u = Urhk_FINRPT_SetErr("SOL_ID|Invalid SOL_ID")
					GOTO ENDOFSCRIPT
				#}
				ENDIF
				IF(BANCS.STDIN.homeSolId != BANCS.FINRPTDEF.SOL_ID) THEN
				#{
					IF(BANCS.STDIN.homeSolId != "000") THEN
					#{
						sv_u = Urhk_FINRPT_SetErr("SOL_ID|Report can be generated only for  users Home Sol ID")
						GOTO ENDOFSCRIPT
					#}
					ENDIF
				#}
				ENDIF
			#}
			ENDIF
			IF(BANCS.STDIN.homeSolId != "000") THEN
			#{
				IF(BANCS.FINRPTDEF.SOL_ID == "ALL") THEN
				#{
					sv_u = Urhk_FINRPT_SetErr("SOL_ID|Report can be generated only for users Sol ID")
					GOTO ENDOFSCRIPT
				#}
				ENDIF
				
			#}
			ENDIF
			
		#}
		ENDIF
		
		
		IF(BANCS.FINRPTDEF.ACCT_NUM != "") THEN
		#{
			sv_u = urhk_valAcctNumber(BANCS.FINRPTDEF.ACCT_NUM)
			IF(sv_u != 0) THEN
			#{
				sv_u = Urhk_FINRPT_SetErr("ACCT_NUM|Invalid ACCT_NUM")
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		
		IF(BANCS.FINRPTDEF.STATUS_FLG != "") THEN
		#{
			IF((BANCS.FINRPTDEF.STATUS_FLG == "A") OR (BANCS.FINRPTDEF.STATUS_FLG == "C") OR (BANCS.FINRPTDEF.STATUS_FLG == "X")) THEN
			#{
				PRINT("successful")
				#}
			ELSE
				#{
				sv_u = Urhk_FINRPT_SetErr("STATUS_FLG|Invalid STATUS FLAG. Possible values are A-ACTIVE, C-CLOSED, X-EXPIRED")
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		
		
		IF(BANCS.FINRPTDEF.FROM_DATE != "") THEN
		#{
			sv_u = urhk_B2k_valDate(BANCS.FINRPTDEF.FROM_DATE)
			IF(sv_u !=0) THEN
			#{
				sv_u = Urhk_FINRPT_SetErr("FROM_DATE|Invalid FROM DATE")
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		
		
		IF(BANCS.FINRPTDEF.TO_DATE != "") THEN
		#{
			sv_u = urhk_B2k_valDate(BANCS.FINRPTDEF.TO_DATE)
			IF(sv_u !=0) THEN
			#{
				sv_u = Urhk_FINRPT_SetErr("TO_DATE|Invalid TO DATE")
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		ENDOFSCRIPT:
			
	END-->
	
