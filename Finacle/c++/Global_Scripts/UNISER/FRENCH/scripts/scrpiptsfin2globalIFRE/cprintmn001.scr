#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cprintmn001.scr
# Type: NA|default
# Description: script on click of go
# Author: haneesha.kanumuru
# Date: 8/22/18 2:10 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="21a2123ef626bc2135ee7354e6b25c3bbef8f701240d1373e7077d93aad9a138"
	# Script body goes here
	# Script body goes here
	TRACE ON
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CPRINT Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CPRINT")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CPRINT",5)
	#}
	ENDIF
	#==========================================================*
	# Intilializing the Variables
	#==========================================================*
	CUST.CPRINT.recCount =""
	CUST.CPRINT.count =""
	CUST.CPRINT.paramVal=""
	IF(FIELDEXISTS(BANCS.INPUT.funcCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.funcCode) != "")THEN
		#{
			CUST.CPRINT.funcCode = TOUPPER(TRIM(BANCS.INPUT.funcCode))
			PRINT(CUST.CPRINT.funcCode)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.roleId))THEN
	#{
		IF(TRIM(BANCS.INPUT.roleId) != "")THEN
		#{
			CUST.CPRINT.roleId = TOUPPER(TRIM(BANCS.INPUT.roleId))
			PRINT(CUST.CPRINT.roleId)
		#}
		ENDIF
	#}
	ENDIF
	
	
	
	IF(CUST.CPRINT.funcCode == "A") THEN
	#{
		sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.c_prefint"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d != 0)THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
		
		CUST.CPRINT.recCount = BANCS.OUTPARAM.recCount
		PRINT(CUST.CPRINT.recCount)
		
		sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.c_prefint_mod"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		
		IF(sv_d != 0)THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		CUST.CPRINT.count=BANCS.OUTPARAM.recCount
		PRINT(CUST.CPRINT.count)
		IF(CINT(CUST.CPRINT.count) >0)THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Error_1|ERR^Record already exists ,Verification pending^Go")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		IF(CINT(CUST.CPRINT.recCount) > 0)THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Error_1|ERR^Record already exists^Go")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		
	#}
	ENDIF
	sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
	PRINT(sv_u)
	
	#==========================================================*
	#For M-MODIFY
	#==========================================================*
	IF(CUST.CPRINT.funcCode == "M") THEN
	#{
		sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.c_prefint"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d != 0)THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		CUST.CPRINT.recCount = BANCS.OUTPARAM.recCount
		PRINT(CUST.CPRINT.recCount)
		
		
		sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.c_prefint_mod"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		
		IF(sv_d != 0)THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		CUST.CPRINT.count=BANCS.OUTPARAM.recCount
		PRINT(CUST.CPRINT.count)
		
		IF((CUST.CPRINT.recCount == 0) AND (CUST.CPRINT.count == 0))THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Error_1|ERR^Record does not exists^Go")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.c_prefint_mod"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		
		IF(sv_d == 0)THEN
		#{
			CUST.CPRINT.count=BANCS.OUTPARAM.recCount
		#}
		ENDIF
		
		
		sv_s = "lchg_user,flg,del_flg| SELECT LCHG_USER_ID,ENTITY_CRE_FLG,DEL_FLG "
		IF(CUST.CPRINT.count > 0) THEN
		#{
			sv_s = sv_s + "FROM CUSTOM.c_prefint_mod"
			
			#}
		ELSE
			#{
			sv_s = sv_s + "FROM CUSTOM.c_prefint"
		#}
		ENDIF
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d==0) THEN
		#{
			
			CUST.CPRINT.lchg_user = BANCS.OUTPARAM.lchg_user
			CUST.CPRINT.flg = BANCS.OUTPARAM.flg
			CUST.CPRINT.delFlg= BANCS.OUTPARAM.del_flg
			PRINT(CUST.CPRINT.delFlg)
			PRINT(CUST.CPRINT.lchg_user)
		#}
		ENDIF
		
		IF((CUST.CPRINT.flg == "N") AND (CUST.CPRINT.lchg_user != BANCS.STDIN.userId))THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Error_1|ERR^Last changes are not verified only same user can modify^Go")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
		
		sv_s = "roleId,intrst| select ROLE_ID,PREF_INTEREST "
		IF(CUST.CPRINT.count > 0) THEN
		#{
			sv_s = sv_s + "FROM CUSTOM.c_prefint_mod"
			
			#}
		ELSE
			#{
			sv_s = sv_s + "FROM CUSTOM.c_prefint"
		#}
		ENDIF
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		CUST.CPRINT.roleId =BANCS.OUTPARAM.roleId
		PRINT(CUST.CPRINT.roleId)
		CUST.CPRINT.prIntrst =BANCS.OUTPARAM.intrst
		PRINT(CUST.CPRINT.prIntrst)
		
		
		
		sv_u = urhk_SetOrbOut("roleId|"+ CUST.CPRINT.roleId)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("prIntrst|"+ CUST.CPRINT.prIntrst)
		PRINT(sv_u)
		
	#}
	ENDIF
	
	#==========================================================*
	#For I-INQUIRE
	#==========================================================*
	IF(CUST.CPRINT.funcCode == "I") THEN
	#{
		sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.c_prefint"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d != 0)THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		CUST.CPRINT.recCount = BANCS.OUTPARAM.recCount
		PRINT(CUST.CPRINT.recCount)
		
		
		sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.c_prefint_mod"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		
		IF(sv_d != 0)THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		CUST.CPRINT.count=BANCS.OUTPARAM.recCount
		PRINT(CUST.CPRINT.count)
		
		IF((CUST.CPRINT.recCount == 0) AND (CUST.CPRINT.count == 0))THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Error_1|ERR^Record does not exists^Go")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.c_prefint_mod"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		
		IF(sv_d == 0)THEN
		#{
			CUST.CPRINT.count=BANCS.OUTPARAM.recCount
		#}
		ENDIF
		
		
		sv_s = "lchg_user,flg,del_flg| SELECT LCHG_USER_ID,ENTITY_CRE_FLG,DEL_FLG "
		IF(CUST.CPRINT.count > 0) THEN
		#{
			sv_s = sv_s + "FROM CUSTOM.c_prefint_mod"
			
			#}
		ELSE
			#{
			sv_s = sv_s + "FROM CUSTOM.c_prefint"
		#}
		ENDIF
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d==0) THEN
		#{
			
			CUST.CPRINT.lchg_user = BANCS.OUTPARAM.lchg_user
			CUST.CPRINT.flg = BANCS.OUTPARAM.flg
			CUST.CPRINT.delFlg= BANCS.OUTPARAM.del_flg
			PRINT(CUST.CPRINT.delFlg)
			PRINT(CUST.CPRINT.lchg_user)
		#}
		ENDIF
		
		IF((CUST.CPRINT.flg == "N") AND (CUST.CPRINT.lchg_user != BANCS.STDIN.userId))THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Error_1|ERR^Last changes are not verified only same user can Inquire^Go")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
		
		sv_s = "roleId,intrst| select ROLE_ID,PREF_INTEREST "
		IF(CUST.CPRINT.count > 0) THEN
		#{
			sv_s = sv_s + "FROM CUSTOM.c_prefint_mod"
			
			#}
		ELSE
			#{
			sv_s = sv_s + "FROM CUSTOM.c_prefint"
		#}
		ENDIF
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		CUST.CPRINT.roleId =BANCS.OUTPARAM.roleId
		PRINT(CUST.CPRINT.roleId)
		CUST.CPRINT.prIntrst =BANCS.OUTPARAM.intrst
		PRINT(CUST.CPRINT.prIntrst)
		
		
		
		sv_u = urhk_SetOrbOut("roleId|"+ CUST.CPRINT.roleId)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("prIntrst|"+ CUST.CPRINT.prIntrst)
		PRINT(sv_u)
	#}
	ENDIF
	
	#==========================================================*
	#For V-VERIFY
	#==========================================================*
	IF(CUST.CPRINT.funcCode == "V") THEN
	#{
		sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.c_prefint"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d != 0)THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		CUST.CPRINT.recCount = BANCS.OUTPARAM.recCount
		PRINT(CUST.CPRINT.recCount)
		sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.c_prefint_mod"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		
		IF(sv_d != 0)THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		CUST.CPRINT.count=BANCS.OUTPARAM.recCount
		PRINT(CUST.CPRINT.count)
		
		IF((CUST.CPRINT.recCount == 0) AND (CUST.CPRINT.count == 0))THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Error_1|ERR^Record does not exists^Go")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.c_prefint_mod"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		
		IF(sv_d == 0)THEN
		#{
			CUST.CPRINT.count=BANCS.OUTPARAM.recCount
		#}
		ENDIF
		
		
		sv_s = "lchg_user,flg,del_flg| SELECT LCHG_USER_ID,ENTITY_CRE_FLG,DEL_FLG "
		IF(CUST.CPRINT.count > 0) THEN
		#{
			sv_s = sv_s + "FROM CUSTOM.c_prefint_mod"
			
			#}
		ELSE
			#{
			sv_s = sv_s + "FROM CUSTOM.c_prefint"
		#}
		ENDIF
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d==0) THEN
		#{
			
			CUST.CPRINT.lchg_user = BANCS.OUTPARAM.lchg_user
			CUST.CPRINT.flg = BANCS.OUTPARAM.flg
			CUST.CPRINT.delFlg= BANCS.OUTPARAM.del_flg
			PRINT(CUST.CPRINT.delFlg)
			PRINT(CUST.CPRINT.lchg_user)
		#}
		ENDIF
		IF(CUST.CPRINT.flg == "Y") THEN
		#{
			#			sv_u = urhk_SetOrbOut("Error|Y")
			#			sv_a = urhk_SetOrbOut("Error_01|Error :^Nothing to verify^acctno")
			sv_a =urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_a)
			sv_u = urhk_SetOrbErr("Error_1|ERR^There is nothing to verify^Go")
			PRINT(sv_u)
			
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		
		IF(CUST.CPRINT.lchg_user == BANCS.STDIN.userId) THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Error_1|ERR^Same user cannot verify^Go")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
		sv_s = "roleId,intrst| select ROLE_ID,PREF_INTEREST "
		IF(CUST.CPRINT.count > 0) THEN
		#{
			sv_s = sv_s + "FROM CUSTOM.c_prefint_mod"
			
			#}
		ELSE
			#{
			sv_s = sv_s + "FROM CUSTOM.c_prefint"
		#}
		ENDIF
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		CUST.CPRINT.roleId =BANCS.OUTPARAM.roleId
		PRINT(CUST.CPRINT.roleId)
		CUST.CPRINT.prIntrst =BANCS.OUTPARAM.intrst
		PRINT(CUST.CPRINT.prIntrst)
		
		
		
		sv_u = urhk_SetOrbOut("roleId|"+ CUST.CPRINT.roleId)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("prIntrst|"+ CUST.CPRINT.prIntrst)
		PRINT(sv_u)
	#}
	ENDIF
	#==========================================================*
	#For X-CANCEL
	#==========================================================*
	IF(CUST.CPRINT.funcCode == "X") THEN
	#{
		sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.c_prefint"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d != 0)THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		CUST.CPRINT.recCount = BANCS.OUTPARAM.recCount
		PRINT(CUST.CPRINT.recCount)
		sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.c_prefint_mod"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		
		IF(sv_d != 0)THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		CUST.CPRINT.count=BANCS.OUTPARAM.recCount
		PRINT(CUST.CPRINT.count)
		
		IF((CUST.CPRINT.recCount == 0) AND (CUST.CPRINT.count == 0))THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Error_1|ERR^Record does not exists^Go")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.c_prefint_mod"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		
		IF(sv_d == 0)THEN
		#{
			CUST.CPRINT.count=BANCS.OUTPARAM.recCount
		#}
		ENDIF
		
		
		sv_s = "lchg_user,flg,del_flg| SELECT LCHG_USER_ID,ENTITY_CRE_FLG,DEL_FLG "
		IF(CUST.CPRINT.count > 0) THEN
		#{
			sv_s = sv_s + "FROM CUSTOM.c_prefint_mod"
			
			#}
		ELSE
			#{
			sv_s = sv_s + "FROM CUSTOM.c_prefint"
		#}
		ENDIF
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d==0) THEN
		#{
			
			CUST.CPRINT.lchg_user = BANCS.OUTPARAM.lchg_user
			CUST.CPRINT.flg = BANCS.OUTPARAM.flg
			CUST.CPRINT.delFlg= BANCS.OUTPARAM.del_flg
			PRINT(CUST.CPRINT.delFlg)
			PRINT(CUST.CPRINT.lchg_user)
		#}
		ENDIF
		IF((CUST.CPRINT.flg !="N")) THEN
		#{
			sv_m = urhk_SetOrbErr("Error_1|ERR^Noting to Cancel^Go")
			sv_z = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		IF((CUST.CPRINT.lchg_user != BANCS.STDIN.userId)) THEN
		#{
			sv_m = urhk_SetOrbErr("Error_1|ERR^Last changes are not verified only same user can Cancel^Go")
			sv_z = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		sv_s = "roleId,intrst| select ROLE_ID,PREF_INTEREST "
		IF(CUST.CPRINT.count > 0) THEN
		#{
			sv_s = sv_s + "FROM CUSTOM.c_prefint_mod"
			
			#}
		ELSE
			#{
			sv_s = sv_s + "FROM CUSTOM.c_prefint"
		#}
		ENDIF
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		CUST.CPRINT.roleId =BANCS.OUTPARAM.roleId
		PRINT(CUST.CPRINT.roleId)
		CUST.CPRINT.prIntrst =BANCS.OUTPARAM.intrst
		PRINT(CUST.CPRINT.prIntrst)
		
		
		
		sv_u = urhk_SetOrbOut("roleId|"+ CUST.CPRINT.roleId)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("prIntrst|"+ CUST.CPRINT.prIntrst)
		PRINT(sv_u)
	#}
	ENDIF
	
	#==========================================================*
	#ForD-DELETE
	#==========================================================*
	IF(CUST.CPRINT.funcCode == "D") THEN
	#{
		sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.c_prefint"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d != 0)THEN
		#{
			
		#}
		ENDIF
		
		CUST.CPRINT.recCount = BANCS.OUTPARAM.recCount
		PRINT(CUST.CPRINT.recCount)
		sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.c_prefint_mod"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		
		IF(sv_d != 0)THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		CUST.CPRINT.count=BANCS.OUTPARAM.recCount
		PRINT(CUST.CPRINT.count)
		
		IF((CUST.CPRINT.recCount == 0) AND (CUST.CPRINT.count == 0))THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Error_1|ERR^Record does not exists^Go")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.c_prefint_mod"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		
		IF(sv_d == 0)THEN
		#{
			CUST.CPRINT.count=BANCS.OUTPARAM.recCount
		#}
		ENDIF
		
		
		sv_s = "lchg_user,flg,del_flg| SELECT LCHG_USER_ID,ENTITY_CRE_FLG,DEL_FLG "
		IF(CUST.CPRINT.count > 0) THEN
		#{
			sv_s = sv_s + "FROM CUSTOM.c_prefint_mod"
			
			#}
		ELSE
			#{
			sv_s = sv_s + "FROM CUSTOM.c_prefint"
		#}
		ENDIF
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d==0) THEN
		#{
			
			CUST.CPRINT.lchg_user = BANCS.OUTPARAM.lchg_user
			CUST.CPRINT.flg = BANCS.OUTPARAM.flg
			CUST.CPRINT.delFlg= BANCS.OUTPARAM.del_flg
			PRINT(CUST.CPRINT.delFlg)
			PRINT(CUST.CPRINT.lchg_user)
		#}
		ENDIF
		IF((CUST.CPRINT.flg == "N"))THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Error_1|ERR^Last changes are not verified^Go")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
		sv_s = "roleId,intrst| select ROLE_ID,PREF_INTEREST "
		sv_s = sv_s + "FROM CUSTOM.c_prefint"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		CUST.CPRINT.roleId =BANCS.OUTPARAM.roleId
		PRINT(CUST.CPRINT.roleId)
		CUST.CPRINT.prIntrst =BANCS.OUTPARAM.intrst
		PRINT(CUST.CPRINT.prIntrst)
		
		
		
		sv_u = urhk_SetOrbOut("roleId|"+ CUST.CPRINT.roleId)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("prIntrst|"+ CUST.CPRINT.prIntrst)
		PRINT(sv_u)
	#}
	ENDIF
	sv_z = urhk_SetOrbOut("SuccessOrFailure|Y")
	PRINT(sv_z)
	TRACE OFF
	ENDOFSCRIPT:
		
END-->
