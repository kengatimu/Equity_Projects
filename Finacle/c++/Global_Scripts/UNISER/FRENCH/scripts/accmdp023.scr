#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: accmdp006.scr
# event: NA
# description:
#=================================================

#**************************************************************************************************************
# Filename              : accmdp023.scr
# Description           : The script is used to fetch the data to the front end for Inquiry mode
# Input                 :
# Output                :
# Called Script         : Common_Interface_GetErrorDesc.scr
# Calling Script        : accmmn001.scr
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----          ------           --------          ----------------        ---------
#   1          25-03-2013       Kalvin Joseph     Original Version          1.0
#   2		23-08-2013	Kalvin		 Modified Version for TO:361667	    1.1
#**************************************************************************************************************
<--START
CHECKSUM="2da22778888361f3a582a13a4956f3270fd7ce4e8500affff688cec30383ae44"
	TRACE ON
	CUST.CONTRACT.contractId=BANCS.INPUT.contractId
	PRINT(BANCS.STDIN.contextBankId)
	#	IF ((CUST.CONTRACT.acctNum !="")AND (CUST.CONTRACT.contractId !="")) THEN
	#	#{
	#		call("accmdp023.scr")
	#	#}
	#	ENDIF
	IF(CUST.CONTRACT.acctNum=="") THEN
	#{
		BANCS.INPARAM.BINDVARS= CUST.CONTRACT.contractId+ "|" + BANCS.STDIN.contextBankId + "|"
		BANCS.INPARAM.BINDVARS= BANCS.INPARAM.BINDVARS + "Y" + "|"  + "N"
		sv_s = "  acctNum|SELECT   "
		sv_s = sv_s + "  acct_num "
		sv_s = sv_s + "   FROM custom.catod_sanction_table  "
		sv_s = sv_s + "   WHERE REQUEST_ID =?SVAR  "
		sv_s = sv_s + "   AND bank_id=?SVAR "
		sv_s = sv_s + "   AND entity_cre_flg=?SVAR "
		sv_s = sv_s + "   AND del_flg=?SVAR "
		PRINT(sv_s)
		sv_d=urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF (sv_d!=0) THEN
		#{
			
			#{
			CUST.INTERFACE.errorCode="ACCM_ERR_012"
			CALL("Common_Interface_GetErrorDesc.scr")
			IF ( CUST.INTERFACE.SuccessFlag == "1") THEN
			#{
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				CUST.CONTRACT.errorMsg = "Could not fetch Parameter value for " +  CUST.INTERFACE.errorCode
				sv_z = "Error_0|ERROR^" + CUST.CONTRACT.errorMsg + "^acctNum"
				sv_u = urhk_SetOrbOut(sv_z)
				CUST.CONTRACT.successFlg = "F"
				GOTO ENDOFSCRIPT
				#}
			ELSE
				#{
				CUST.CONTRACT.errorMsg=CUST.INTERFACE.errorDesc
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				print(sv_u)
				sv_u = urhk_SetOrbOut("Error_0|ERROR^" + CUST.CONTRACT.errorMsg + "^acctNum")
				print(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		CUST.CONTRACT.acctNum=BANCS.OUTPARAM.acctNum
	#}
	ENDIF
	
	BANCS.INPARAM.BINDVARS =CUST.CONTRACT.acctNum + "|" + "N" + "|" + "Y"
	
	sv_s ="schmType,crncyCode,custId,clrBalAmt,acctOpnDate,lastTranDate,acctName|"
	sv_s = sv_s + " SELECT SCHM_TYPE,ACCT_CRNCY_CODE,CIF_ID,CLR_BAL_AMT,to_char(to_date(acct_opn_date,'dd-mon-yy'),'dd-mm-yyyy'),to_char(to_date(last_tran_date,'dd-mon-yy'),'dd-mm-yyyy'),acct_name FROM TBAADM.GAM "
	sv_s = sv_s + " WHERE  FORACID = ?SVAR "
	sv_s = sv_s + " AND DEL_FLG = ?SVAR "
	sv_s = sv_s + " AND ENTITY_CRE_FLG = ?SVAR"
	
	PRINT(sv_s)
	sv_u= urhk_dbSelectWithBind(sv_s)
	PRINT(sv_u)
	
	IF (sv_u == 0) THEN
	#{
		CUST.CONTRACT.schmType =BANCS.OUTPARAM.schmType
		CUST.CONTRACT.crncyCode=BANCS.OUTPARAM.crncyCode
		CUST.CONTRACT.custId=BANCS.OUTPARAM.custId
		CUST.CONTRACT.clrBalAmt=BANCS.OUTPARAM.clrBalAmt
		CUST.CONTRACT.acctOpnDate=BANCS.OUTPARAM.acctOpnDate
		CUST.CONTRACT.lastTranDate=BANCS.OUTPARAM.lastTranDate
		CUST.CONTRACT.acctName=BANCS.OUTPARAM.acctName
	#}
	ENDIF
	
	IF(CUST.CONTRACT.acctNum!="")THEN
	#{
		sv_u = urhk_SetOrbOut("acctNo| "+ CUST.CONTRACT.acctNum)
	#}
	ENDIF
	IF(CUST.CONTRACT.acctName!="")THEN
	#{
		sv_u = urhk_SetOrbOut("acctname| "+ CUST.CONTRACT.acctName)
	#}
	ENDIF
	IF(CUST.CONTRACT.custId!="")THEN
	#{
		sv_u = urhk_SetOrbOut("custid| "+ CUST.CONTRACT.custId)
	#}
	ENDIF
	IF(CUST.CONTRACT.schmType!="")THEN
	#{
		sv_u = urhk_SetOrbOut("schmType| "+CUST.CONTRACT.schmType)
	#}
	ENDIF
	IF(CUST.CONTRACT.clrBalAmt!="")THEN
	#{
		sv_u = urhk_SetOrbOut("clrBal| "+ CUST.CONTRACT.clrBalAmt)
	#}
	ENDIF
	IF(CUST.CONTRACT.acctOpnDate!="")THEN
	#{
		sv_u = urhk_SetOrbOut("acctOpnDate| "+ CUST.CONTRACT.acctOpnDate)
	#}
	ENDIF
	IF(CUST.CONTRACT.lastTranDate!="")THEN
	#{
		sv_u = urhk_SetOrbOut("lastTranDate| "+ CUST.CONTRACT.lastTranDate)
	#}
	ENDIF
	
	BANCS.INPARAM.BINDVARS= CUST.CONTRACT.contractId+ "|" + BANCS.STDIN.contextBankId + "|"
	BANCS.INPARAM.BINDVARS= BANCS.INPARAM.BINDVARS + "Y" + "|"  + "N"
	sv_s ="count|"
	sv_s = sv_s + " SELECT COUNT (1) "
	sv_s = sv_s + " FROM CUSTOM.CATOD_SANCTION_TABLE "
	sv_s = sv_s + " WHERE REQUEST_ID=?SVAR"
	sv_s = sv_s + " AND BANK_ID=?SVAR "
	sv_s = sv_s + " AND ENTITY_CRE_FLG=?SVAR "
	sv_s = sv_s + " AND DEL_FLG=?SVAR"
	PRINT(sv_s)
	sv_d=urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF (sv_d!=0) THEN
	#{
		
		#{
		CUST.INTERFACE.errorCode="ACCM_ERR_012"
		CALL("Common_Interface_GetErrorDesc.scr")
		IF ( CUST.INTERFACE.SuccessFlag == "1") THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			CUST.CONTRACT.errorMsg = "Could not fetch Parameter value for " +  CUST.INTERFACE.errorCode
			sv_z = "Error_0|ERROR^" + CUST.CONTRACT.errorMsg + "^acctNum"
			sv_u = urhk_SetOrbOut(sv_z)
			CUST.CONTRACT.successFlg = "F"
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			CUST.CONTRACT.errorMsg=CUST.INTERFACE.errorDesc
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			print(sv_u)
			sv_u = urhk_SetOrbOut("Error_0|ERROR^" + CUST.CONTRACT.errorMsg + "^acctNum")
			print(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	CUST.CONTRACT.count = BANCS.OUTPARAM.count
	
	#--------------------------------------------------------------------------
	# Create cursor and open it
	#--------------------------------------------------------------------------
	
	BANCS.INPARAM.BINDVARS= CUST.CONTRACT.contractId+ "|" + BANCS.STDIN.contextBankId + "|"
	BANCS.INPARAM.BINDVARS= BANCS.INPARAM.BINDVARS + "Y" + "|"  + "N"
	sv_s = "  reqId,acctName,reqType,clsFlg,sanctAmt,sanctPerd,cretUser,cretDate,remarks|SELECT   "
	sv_s = sv_s + "  REQUEST_ID, ACCT_NAME,REQ_TYPE,CLS_FLG, "
	sv_s = sv_s + "  SANCT_AMT,SANCT_PERIOD,CREATED_BY,to_char(to_date(CREATED_DATE,'dd-mon-yy'),'dd-mm-yyyy'),REMARKS"
	sv_s = sv_s + "   FROM custom.catod_sanction_table  "
	sv_s = sv_s + "   WHERE REQUEST_ID =?SVAR  "
	sv_s = sv_s + "   AND bank_id=?SVAR "
	sv_s = sv_s + "   AND entity_cre_flg=?SVAR "
	sv_s = sv_s + "   AND del_flg=?SVAR "
	
	
	sv_r = urhk_dbCursorOpenWithBind(sv_s)
	
	IF (sv_r != 0) THEN
	#{
		BANCS.OUTPUT.fatalErrorMsg = "Unable to open cursor. Contact system admin"
		BANCS.OUTPUT.fatalErrorFlg = "Y"
		GOTO FINISH
	#}
	ENDIF
	CUST.CONTRACT.cursorNumber = BANCS.OUTPARAM.DB_CURSOR_NUMBER
	sv_r = urhk_dbCursorFetch(CUST.CONTRACT.cursorNumber)
	
	sv_i = 0
	WHILE(sv_r == 0)
	#{
		CUST.CONTRACT.reqId = BANCS.OUTPARAM.reqId
		CUST.CONTRACT.acctName	 = BANCS.OUTPARAM.acctName
		CUST.CONTRACT.remarks	= BANCS.OUTPARAM.remarks
		IF (BANCS.OUTPARAM.reqType =="F") THEN
		#{
			CUST.CONTRACT.reqType="FRESH"
			#}
		ELSE
			#{
			IF(BANCS.OUTPARAM.reqType == "C")THEN
			#{
				CUST.CONTRACT.reqType="TERMINATED"
				#}
			ELSE
				#{
				CUST.CONTRACT.reqType="ENHANCEMENT"
			#}
			ENDIF
			
		#}
		ENDIF
		IF (BANCS.OUTPARAM.clsFlg =="Y") THEN
		#{
			IF(CUST.CONTRACT.remarks != "")  THEN
			#{
				CUST.CONTRACT.status = "TERMINATED"
				#}
			ELSE
				#{
				CUST.CONTRACT.status="EXPIRED"
			#}
			ENDIF
			#}
		ELSE
			#{
			CUST.CONTRACT.status="APPROVED"
		#}
		ENDIF
		CUST.CONTRACT.sanctAmt=BANCS.OUTPARAM.sanctAmt
		CUST.CONTRACT.sanctPerd=BANCS.OUTPARAM.sanctPerd
		CUST.CONTRACT.cretUser=BANCS.OUTPARAM.cretUser
		CUST.CONTRACT.cretDate=BANCS.OUTPARAM.cretDate
		CUST.CONTRACT.remarks="Sanction Request Approved"
		#		var reqId = new Array(iListMaxDisplay);
		#	var acctNum = new Array(iListMaxDisplay);
		#	var acctName = new Array(iListMaxDisplay);
		#	var reqType = new Array(iListMaxDisplay);
		#	var status = new Array(iListMaxDisplay);
		#	var remrks = new Array(iListMaxDisplay);
		#	var apprvdAmt = new Array(iListMaxDisplay);
		#	var apprvdPrd = new Array(iListMaxDisplay);
		#	var cretUser = new Array(iListMaxDisplay);
		#	var cretDate
		sv_y = format$(sv_i, "%d")
		print(sv_y)
		sv_s = "reqId_" + sv_y + "| " +CUST.CONTRACT.reqId
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		sv_s = "acctNum_" + sv_y + "| " +CUST.CONTRACT.acctNum
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		sv_s = "acctName_" + sv_y + "| " +CUST.CONTRACT.acctName
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		sv_s = "reqType_" + sv_y + "| " +CUST.CONTRACT.reqType
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		sv_s = "status_" + sv_y + "| " +CUST.CONTRACT.status
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		sv_s = "remrks_" + sv_y + "| " +CUST.CONTRACT.remarks
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		sv_s = "apprvdAmt_" + sv_y + "| " +CUST.CONTRACT.sanctAmt
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		sv_s = "apprvdPrd_" + sv_y + "| " +CUST.CONTRACT.sanctPerd
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		sv_s = "cretUser_" + sv_y + "| " +CUST.CONTRACT.cretUser
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		PRINT(sv_r)
		
		sv_s = "cretDate_" + sv_y + "| " + CUST.CONTRACT.cretDate
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		
		
		sv_i = sv_i + 1
		print(sv_i)
		
		
		
		#----------------------------------------------------------------------
		# Fetch next record
		#----------------------------------------------------------------------
		
		sv_r = urhk_dbCursorFetch(CUST.CONTRACT.cursorNumber)
		
	#}
	DO
	#--------------------------------------------------------------------------
	#Close the cursor
	#--------------------------------------------------------------------------
	sv_r = urhk_dbCursorClose(CUST.CONTRACT.cursorNumber)
	
	
	
	sv_a = urhk_SetOrbOut("setSize|1")
	sv_a = urhk_SetOrbOut("MaxRecsPerPage|"+CUST.CONTRACT.count)
	sv_a = urhk_SetOrbOut("currentPage|1")
	sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
	
	FINISH:
		
	ENDOFSCRIPT:
		
END-->
