#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: ubpdp051.scr
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START
CHECKSUM="fedeca8150f5718087dcec211dac7b224804fb3621b1374858402db7b8566377"
	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp051.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called from HBLRG for validation type searcher from cparam
	#
	# Called Script         :
	# Calling script        :		ubpdp039.scr
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           16-04-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	
	#========================================================
	# Check whether CUST Repository exists else create it
	#========================================================
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	#========================================================
	# Check whether UBP Class exists else create it
	#========================================================
	sv_r = CLASSEXISTS("CUST","UBP")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","UBP",5)
	#}
	ENDIF
	#========================================================
	# Initialize the variables
	#========================================================
	IF(FIELDEXISTS(BANCS.INPUT.BankId))THEN
	#{
		IF(TRIM(BANCS.INPUT.BankId) != "")THEN
		#{
			CUST.UBP.BankId = TOUPPER(TRIM(BANCS.INPUT.BankId))
			PRINT(CUST.UBP.BankId)
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.errorFlg = "Y"
		CUST.UBP.errorMsg = "Unable to get Bank Id"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	CUST.UBP.valType=" "
	CUST.UBP.valDesc=" "
	
	
	#========================================================
	# Fetching the Record count
	#========================================================
	#BANCS.INPARAM.BINDVARS = CUST.UBP.BankId+"|N|Y"
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId+"|N|Y"
	sv_s = "recordCount|"
	sv_s = sv_s + " SELECT COUNT(*)"
	sv_s = sv_s + " FROM CUSTOM.C_CPVALUE "
	sv_s = sv_s + " WHERE BANK_ID = ?SVAR"
	sv_s = sv_s + " AND NVL(DEL_FLG, 'N') = ?SVAR"
	sv_s = sv_s + " AND NVL(ENTITY_CRE_FLG, 'Y') = ?SVAR"
	sv_s = sv_s + " AND MODULE_NAME = 'HBLRG'"
	#sv_s = sv_s + " GROUP BY EVENT_ID"
	#sv_s = sv_s + " AND AMT_DERV_SRL_NUM IS NOT NULL"
	PRINT(sv_s)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d == 0)THEN
	#{
		CUST.UBP.recordCount = BANCS.OUTPARAM.recordCount
		PRINT(CUST.UBP.recordCount)
	#}
	ENDIF
	sv_u = urhk_SetOrbOut("recCount|"+CUST.UBP.recordCount)
	print("recCount|"+CUST.UBP.recordCount)
	PRINT(sv_u)
	
	IF(CUST.UBP.recordCount=="0")THEN
	#{
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		#========================================================
		# Fetching the values from C_CPVALUE table and populating
		# the searcher list
		#========================================================
		#BANCS.INPARAM.BINDVARS = CUST.UBP.BankId+"|N|Y"
		BANCS.INPARAM.BINDVARS =BANCS.STDIN.contextBankId+"|N|Y"
		
		sv_s = "valType,valDesc|"
		sv_s = sv_s + " SELECT PARAMETER_ID,PARAMETER_VALUE"
		sv_s = sv_s + " FROM CUSTOM.C_CPVALUE "
		sv_s = sv_s + " WHERE BANK_ID = ?SVAR"
		sv_s = sv_s + " AND NVL(DEL_FLG, 'N') = ?SVAR"
		sv_s = sv_s + " AND NVL(ENTITY_CRE_FLG, 'Y') = ?SVAR"
		sv_s = sv_s + " AND MODULE_NAME = 'HBLRG'"
		#sv_s = sv_s + " GROUP BY PARAMETER_ID"
		#sv_s = sv_s + " AND AMT_DERV_SRL_NUM IS NOT NULL"
		PRINT(sv_s)
		sv_c = urhk_dbCursorOpenWithBind(sv_s)
		PRINT(sv_c)
		IF(sv_c != 0)THEN
		#{
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		sv_n = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		PRINT(sv_n)
		sv_f = urhk_dbCursorFetch(sv_n)
		PRINT(sv_f)
		sv_i = 1
		WHILE(sv_f == 0)
		#{
			
			CUST.UBP.valType = BANCS.OUTPARAM.valType
			PRINT(CUST.UBP.valType)
			CUST.UBP.valDesc = BANCS.OUTPARAM.valDesc
			PRINT(CUST.UBP.valDesc)
			
			sv_s = "valType_" + Format$(sv_i,"%d") + "|" + CUST.UBP.valType
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			
			sv_s = "valDesc_" + Format$(sv_i,"%d") + "|" + CUST.UBP.valDesc
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			
			sv_i = sv_i + 1
			sv_f = urhk_dbCursorFetch(sv_n)
			PRINT(sv_f)
			
		#}
		DO
		sv_c = urhk_dbCursorClose(sv_n)
		PRINT(sv_c)
	#}
	ENDIF
	
	ENDOFSCRIPT:
END-->
