#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: cshasal_det_submit.scr
# event: NA
# description: NA
#=================================================

<--START
CHECKSUM="b6cd5cf340b28b2429a9b21657b28bc13db5d4a3e9cb6239c99863a69691e8d1"
	
	TRACE ON
	
	sv_a = urhk_b2k_printRepos("BANCS")
	sv_a = urhk_SetOrbOut("SuccessOrFailure|Y")
	
	
	IF (REPEXISTS("TRAD") == 0) THEN
	#{
		CREATEREP("TRAD")
	#}
	ENDIF
	
	IF (CLASSEXISTS("TRAD","INTERFACE") == 0) THEN
	#{
		CREATECLASS ("TRAD","INTERFACE",5)
	#}
	ENDIF
	
	
	IF(BANCS.INPUT.funcCode=="A")  THEN
	#{
		sv_a="Record  " + BANCS.INPUT.txtdetorderno +" Sucessfully Posted"
		sv_b = urhk_SetOrbOut("RESULT_MSG|" + sv_a)
	#}
	ENDIF
	
	
	IF(BANCS.INPUT.funcCode=="X")  THEN
	#{
		
		
		sv_s = "UPDATE custom.sale_details  SET"
		sv_s = sv_s + " TRANS_STATUS = 'D',"
		sv_s = sv_s + " DELETED_BY =?SVAR"
		sv_s = sv_s + " WHERE"
		sv_s = sv_s + " REF_NUM = ?SVAR"
		
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.userId + "|" + BANCS.INPUT.txtorderid
		
		sv_d = urhk_dbSqlWithBind(sv_s)
		
		IF(sv_d != 0)THEN
		#{
			#ERROR
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			
			sv_b = urhk_SetOrbOut("RESULT_MSG| Database Error")
			print(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		sv_a="Record  Deleted Succesfully"
		sv_b = urhk_SetOrbOut("RESULT_MSG|" + sv_a)
	#}
	ENDIF
	
	
	############################################
	# Is it a Verification Transaction
	###########################################
	
	IF(BANCS.INPUT.funcCode=="V")  THEN
	#{
		
		print(BANCS.INPUT.txtorderid)
		BANCS.INPARAM.BINDVARS=BANCS.INPUT.txtorderid
		############################################
		# Check to see if Verifier is still the maker
		###########################################
		
		sv_s = "ENTERED_BY"
		sv_s = sv_s + " | "
		sv_s = sv_s + " select distinct(ENTERED_BY) from custom.sale_details where REF_NUM=?SVAR"
		
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(BANCS.OUTPARAM.ENTERED_BY)
		
		# IF(BANCS.OUTPARAM.ENTERED_BY==BANCS.STDIN.EmployeeId)THEN
		IF (BANCS.OUTPARAM.ENTERED_BY==BANCS.STDIN.userId) THEN
		#{
			sv_a ="Error : Maker Cannot Verify Same Record"
			sv_b = urhk_SetOrbOut("RESULT_MSG|" + sv_a)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		############################################
		# Verifier is not Maker Continue  to update
		###########################################
		CALL("cshare_post_custodial.scr")
		
		IF (TRAD.INTERFACE.errorStatus == "Y") THEN
		#{
			sv_a=TRAD.INTERFACE.errorMsg
			sv_b = urhk_SetOrbOut("RESULT_MSG|" + sv_a)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		sv_s = "UPDATE custom.sale_details  SET"
		sv_s = sv_s + " TRANS_STATUS = 'V',"
		sv_s = sv_s + " VERIFIED_BY =?SVAR"
		sv_s = sv_s + " WHERE"
		sv_s = sv_s + " REF_NUM = ?SVAR"
		
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.userId + "|" + BANCS.INPUT.txtorderid
		
		sv_d = urhk_dbSqlWithBind(sv_s)
		
		IF(sv_d != 0)THEN
		#{
			#ERROR
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			
			sv_b = urhk_SetOrbOut("RESULT_MSG| Database Error")
			print(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		sv_a ="Record  " + BANCS.INPUT.txtdetorderno +" Sucessfully Verified"
		sv_b = urhk_SetOrbOut("RESULT_MSG|" + sv_a)
	#}
	ENDIF
	
	
	
	
	
	ENDOFSCRIPT :
		
		TRACE OFF
END-->
