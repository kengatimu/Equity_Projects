#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: ChqReconInq_FetchInputDetails.scr
# event: NA
# description: NA
#=================================================
#----------------------------------------------------------------------------------------------------------
# Filename              :       ChqReconInq_FetchInputDetails.scr
# Author                :       Nitin Prabhakar
# Description           :       This script fetches the values passed in request XML.
# Input                 :       Request XML
# Output                :       5 input fields
# Called Script         :       CollectChequeCharges_main.scr
# Calling Script        :       None
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----           -----           --------          ----------------        ---------
#  1           23-07-2013      Nitin Prabhakar       Original Version          1.0
#-----------------------------------------------------------------------------------------------------------
<--start
TRACE ON
#-------------------------------------------------------------------------------
# Getting input data from xml
#-------------------------------------------------------------------------------
# ZoneID
sv_v = "ChqReconInq_REQ.ZoneID"
IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
#{
	CUST.INTERFACE.ZoneID =("BANCS").("INPUT").(sv_v)
#}
ELSE
#{
	CUST.INTERFACE.ZoneID = ""
#}
ENDIF
print(CUST.INTERFACE.ZoneID)

# ZoneIdentifier
sv_v = "ChqReconInq_REQ.ZoneIdentifier"
IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
#{
	CUST.INTERFACE.ZoneIdentifier = ("BANCS").("INPUT").(sv_v)
#}
ELSE
#{
	CUST.INTERFACE.ZoneIdentifier = ""
#}
ENDIF
print(CUST.INTERFACE.ZoneIdentifier)

# ZoneDate
sv_v = "ChqReconInq_REQ.ZoneDate"
IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
#{
	CUST.INTERFACE.ZoneDate = ("BANCS").("INPUT").(sv_v)
#}
ELSE
#{
	CUST.INTERFACE.ZoneDate = ""
#}
ENDIF
print(CUST.INTERFACE.ZoneDate)

# ZoneCcy
sv_v = "ChqReconInq_REQ.ZoneCcy"
IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
#{
	CUST.INTERFACE.ZoneCcy = ("BANCS").("INPUT").(sv_v)
#}
ELSE
#{
	CUST.INTERFACE.ZoneCcy = ""
#}
ENDIF
print(CUST.INTERFACE.ZoneCcy)

# TranID
sv_v = "ChqReconInq_REQ.TranID"
IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
#{
	CUST.INTERFACE.TranID = ("BANCS").("INPUT").(sv_v)
#}
ELSE
#{
	CUST.INTERFACE.TranID = ""
#}
ENDIF
print(CUST.INTERFACE.TranID)

EXITSCRIPT
end-->
