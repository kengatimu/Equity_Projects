##########################################################################################
# Source Name           : cierevdp004.scr
# Date                  : 04-Mar-2013
# Description           : Show Tran Id List
# Author                : Mohan Raghu Arudi
# Modification History  :
# <Serial No.>  <Date>        <Author Name>             <Description>
# ------------  -----------   -----------------         --------------
#      1        04-Mar-2013   Mohan Raghu Arudi         Base Version
###########################################################################################

<--START
CHECKSUM="aaae4b4b54be7881631def3b1af9e2592d076c785df0589b3f9de0dcbb88a338"
	TRACE ON
	
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CIEREV Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CIEREV")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CIEREV",5)
	#}
	ENDIF
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	
	CUST.CIEREV.tranId = ""
	CUST.CIEREV.tranDate = ""
	CUST.CIEREV.recordCount = "0"
	
	IF(FIELDEXISTS(BANCS.INPUT.tranId))THEN
	#{
		IF(TRIM(BANCS.INPUT.tranId) != "")THEN
		#{
			CUST.CIEREV.tranId = TOUPPER(TRIM(BANCS.INPUT.tranId))
			print(CUST.CIEREV.tranId)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.tranDate))THEN
	#{
		IF(TRIM(BANCS.INPUT.tranDate) != "")THEN
		#{
			CUST.CIEREV.tranDate = TOUPPER(TRIM(BANCS.INPUT.tranDate))
			print(CUST.CIEREV.tranDate)
		#}
		ENDIF
	#}
	ENDIF
	
	BANCS.INPARAM.BINDVARS = ""
	sv_s = "recordCount|"
	sv_s = sv_s + " SELECT COUNT(*)"
	sv_s = sv_s + " FROM CUSTOM.IETD "
	sv_s = sv_s + " WHERE REVERSE_FLG != 'Y'"
	sv_s = sv_s + " AND INIT_ENTITY_ID = '"+BANCS.STDIN.contextBankId+"'"
	
	IF(CUST.CIEREV.tranId != "")THEN
	#{
		sv_s = sv_s + " AND TRIM(ENTITY1_TRAN_ID) LIKE '"+CUST.CIEREV.tranId+"%'"
	#}
	ENDIF
	
	IF(CUST.CIEREV.tranDate != "")THEN
	#{
		sv_s = sv_s + " AND ENTITY1_TRAN_DATE = to_date('"+CUST.CIEREV.tranDate+"','DD-MM-YYYY')"
	#}
	ENDIF
	
	print(sv_s)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	IF(sv_d == 0)THEN
	#{
		CUST.CIEREV.recordCount = BANCS.OUTPARAM.recordCount
		print(CUST.CIEREV.recordCount)
	#}
	ENDIF
	
	sv_u = urhk_SetOrbOut("recordCount|"+CUST.CIEREV.recordCount)
	print(sv_u)
	
	BANCS.INPARAM.BINDVARS = ""
	sv_s = "tranId,tranDate|"
	sv_s = sv_s + " SELECT ENTITY1_TRAN_ID,to_char(ENTITY1_TRAN_DATE,'DD-MM-YYYY')"
	sv_s = sv_s + " FROM CUSTOM.IETD"
	sv_s = sv_s + " WHERE REVERSE_FLG != 'Y'"
	sv_s = sv_s + " AND INIT_ENTITY_ID = '"+BANCS.STDIN.contextBankId+"'"
	
	IF(CUST.CIEREV.tranId != "")THEN
	#{
		sv_s = sv_s + " AND TRIM(ENTITY1_TRAN_ID) LIKE '"+CUST.CIEREV.tranId+"%'"
	#}
	ENDIF
	
	IF(CUST.CIEREV.tranDate != "")THEN
	#{
		sv_s = sv_s + " AND ENTITY1_TRAN_DATE = to_date('"+CUST.CIEREV.tranDate+"','DD-MM-YYYY')"
	#}
	ENDIF
	sv_s = sv_s + " ORDER BY 1,2"
	
	print(sv_s)
	
	sv_c = urhk_dbCursorOpenWithBind(sv_s)
	print(sv_c)
	
	IF(sv_c != 0)THEN
	#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_n = BANCS.OUTPARAM.DB_CURSOR_NUMBER
	print(sv_n)
	
	sv_f = urhk_dbCursorFetch(sv_n)
	print(sv_f)
	
	sv_i = 1
	
	WHILE(sv_f == 0)
	#{
		CUST.CIEREV.tranId = BANCS.OUTPARAM.tranId
		print(CUST.CIEREV.tranId)
		
		CUST.CIEREV.tranDate = BANCS.OUTPARAM.tranDate
		print(CUST.CIEREV.tranDate)
		
		sv_s = "tranId_" + Format$(sv_i,"%d") + "|" + CUST.CIEREV.tranId
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_s = "tranDate_" + Format$(sv_i,"%d") + "|" + CUST.CIEREV.tranDate
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_i = sv_i + 1
		
		sv_f = urhk_dbCursorFetch(sv_n)
		print(sv_f)
		
	#}
	DO
	
	sv_c = urhk_dbCursorClose(sv_n)
	print(sv_c)
	
	
	ENDOFSCRIPT:
		
END-->
