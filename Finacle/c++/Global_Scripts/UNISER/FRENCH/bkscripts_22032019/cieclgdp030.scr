##########################################################################################
# Source Name           : cieclgdp030.scr
# Date                  : 04-Mar-2013
# Description           : show Bank Code
# Author                : Mohan Raghu Arudi
# Modification History  :
# <Serial No.>  <Date>        <Author Name>             <Description>
# ------------  -----------   -----------------         --------------
#      1        04-Mar-2013   Mohan Raghu Arudi         Base Version
###########################################################################################

<--START
CHECKSUM="b3fa69b1f81c83a4e35d265f2fd5860a0ef34a5c8aa0c86e5b419648d6ea5669"
	TRACE ON
	
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CIECLG Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CIECLG")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CIECLG",5)
	#}
	ENDIF
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	
	CUST.CIECLG.instrumentBankCode = ""
	CUST.CIECLG.shortName = ""
	CUST.CIECLG.instrumentBankName = ""
	CUST.CIECLG.delFlg = ""
	CUST.CIECLG.instrumentEntity = ""
	CUST.CIECLG.recordCount = "0"
	
	IF(FIELDEXISTS(BANCS.INPUT.instrumentBankCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.instrumentBankCode) != "")THEN
		#{
			CUST.CIECLG.instrumentBankCode = BANCS.INPUT.instrumentBankCode
			print(CUST.CIECLG.instrumentBankCode)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.instrumentEntity))THEN
	#{
		IF(TRIM(BANCS.INPUT.instrumentEntity) != "")THEN
		#{
			CUST.CIECLG.instrumentEntity = BANCS.INPUT.instrumentEntity
			print(CUST.CIECLG.instrumentEntity)
		#}
		ENDIF
	#}
	ENDIF
	
	CUST.CIECLG.instrumentBankCode = CUST.CIECLG.instrumentBankCode + "%"
	
	BANCS.INPARAM.BINDVARS = CUST.CIECLG.instrumentEntity + "|" + CUST.CIECLG.instrumentBankCode
	print(BANCS.INPARAM.BINDVARS)
	
	sv_s = "recordCount|"
	sv_s = sv_s + " SELECT COUNT(*)"
	sv_s = sv_s + " FROM TBAADM.BANK_CODE_TABLE"
	sv_s = sv_s + " WHERE BANK_ID = ?SVAR"
	sv_s = sv_s + " AND BANK_CODE LIKE ?SVAR"
	print(sv_s)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	IF(sv_d == 0)THEN
	#{
		CUST.CIECLG.recordCount = BANCS.OUTPARAM.recordCount
		print(CUST.CIECLG.recordCount)
	#}
	ENDIF
	
	sv_u = urhk_SetOrbOut("recordCount|"+CUST.CIECLG.recordCount)
	print(sv_u)
	
	BANCS.INPARAM.BINDVARS = CUST.CIECLG.instrumentEntity + "|" + CUST.CIECLG.instrumentBankCode
	print(BANCS.INPARAM.BINDVARS)
	
	sv_s = "instrumentBankCode,shortName,instrumentBankName,delFlg|"
	sv_s = sv_s + " SELECT BANK_CODE,NVL(BANK_SHORT_NAME,'-'),NVL(BANK_NAME,'-'),DEL_FLG"
	sv_s = sv_s + " FROM TBAADM.BANK_CODE_TABLE"
	sv_s = sv_s + " WHERE BANK_ID = ?SVAR"
	sv_s = sv_s + " AND BANK_CODE LIKE ?SVAR"
	sv_s = sv_s + " ORDER BY BANK_CODE"
	print(sv_s)
	
	sv_c = urhk_dbCursorOpenWithBind(sv_s)
	print(sv_c)
	
	IF(sv_c != 0)THEN
	#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_n = BANCS.OUTPARAM.DB_CURSOR_NUMBER
	print(sv_n)
	
	sv_f = urhk_dbCursorFetch(sv_n)
	print(sv_f)
	
	sv_i = 1
	
	WHILE(sv_f == 0)
	#{
		CUST.CIECLG.instrumentBankCode = BANCS.OUTPARAM.instrumentBankCode
		print(CUST.CIECLG.instrumentBankCode)
		
		CUST.CIECLG.shortName = BANCS.OUTPARAM.shortName
		print(CUST.CIECLG.shortName)
		
		CUST.CIECLG.instrumentBankName = BANCS.OUTPARAM.instrumentBankName
		print(CUST.CIECLG.instrumentBankName)
		
		CUST.CIECLG.delFlg = BANCS.OUTPARAM.delFlg
		print(CUST.CIECLG.delFlg)
		
		sv_s = "instrumentBankCode_" + Format$(sv_i,"%d") + "|" + CUST.CIECLG.instrumentBankCode
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_s = "shortName_" + Format$(sv_i,"%d") + "|" + CUST.CIECLG.shortName
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_s = "instrumentBankName_" + Format$(sv_i,"%d") + "|" + CUST.CIECLG.instrumentBankName
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_s = "delFlg_" + Format$(sv_i,"%d") + "|" + CUST.CIECLG.delFlg
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_i = sv_i + 1
		
		sv_f = urhk_dbCursorFetch(sv_n)
		print(sv_f)
		
	#}
	DO
	
	sv_c = urhk_dbCursorClose(sv_n)
	print(sv_c)
	
	
	ENDOFSCRIPT:
		
END-->
