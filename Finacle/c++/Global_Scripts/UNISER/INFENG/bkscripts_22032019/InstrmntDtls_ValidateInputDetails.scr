#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: InstrmntDtls_ValidateInputDetails.scr
# event: NA
# description: NA
#=================================================
#----------------------------------------------------------------------------------------------------------
# Filename              :       InstrmntDtls_ValidateInputDetails.scr
# Author                :       Nitin Prabhakar
# Description           :       This script validates the values passed in request XMl.
# Input                 :       4 input fields fetched values.
# Output                :       Success or Failure
# Called Script         :       BankersChq_main.scr
# Calling Script        :       None
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----           -----           --------          ----------------        ---------
#  1           26-07-2013      Nitin Prabhakar       Original Version          1.0
#-----------------------------------------------------------------------------------------------------------
<--start
TRACE ON
#-------------------------------------------------------------------------------------------------
# Validate AcctNum
#-------------------------------------------------------------------------------------------------
IF(CUST.INTERFACE.AcctNum == "") THEN
#{
	# Account Number is Mandatory.
	CUST.INTERFACE.successOrFailure = "F"
	CUST.INTERFACE.errorCode = "ERR106"
	GOTO ERROREXIT
#}
ELSE
#{
	sv_u = urhk_valAcctNumber(CUST.INTERFACE.AcctNum)
	print(sv_u)
	IF(sv_u != 0) THEN
	#{
		# Invalid Account ID
		CUST.INTERFACE.successOrFailure = "F"
		CUST.INTERFACE.errorCode = "ERR110"
		GOTO ERROREXIT
	#}
	ENDIF
	CUST.INTERFACE.acACID = BANCS.OUTPARAM.Acid
	print(CUST.INTERFACE.acACID)
#}
ENDIF
#-------------------------------------------------------------------------------------------------
# Validate ChequeNum
#-------------------------------------------------------------------------------------------------
IF(CUST.INTERFACE.ChequeNum == "") THEN
#{
	# Cheque Number is Mandatory.
	CUST.INTERFACE.successOrFailure = "F"
	CUST.INTERFACE.errorCode = "ERR107"
	GOTO ERROREXIT
#}
ENDIF
#-------------------------------------------------------------------------------------------------
# Validate SolId
#-------------------------------------------------------------------------------------------------
IF(CUST.INTERFACE.SolId == "") THEN
#{
	# Sol Id is Mandatory.
	CUST.INTERFACE.successOrFailure = "F"
	CUST.INTERFACE.errorCode = "ERR108"
	GOTO ERROREXIT
#}
ENDIF
#-------------------------------------------------------------------------------------------------
# Validate InstrmntType
#-------------------------------------------------------------------------------------------------
IF(CUST.INTERFACE.InstrmntType == "") THEN
#{
	# Instrument Type is Mandatory.
	CUST.INTERFACE.successOrFailure = "F"
	CUST.INTERFACE.errorCode = "ERR109"
	GOTO ERROREXIT
#}
ENDIF

EXITSCRIPT
end-->
