	
<--START
CHECKSUM="c9787b87ea084508ccf525d0b277bc53d5661f1c2db3234e4049b5389a233ea8"
	TRACE ON
	
	####################################################################
	#section      :creating new repository if required
	#Description  :Checks for the existence of the repository STDWFS and
	#               creates a new one by that name if it does not exist
	#####################################################################
	
	IF (REPEXISTS("CUST") == 0 ) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	############################################################################
	#section      :creating new class if required
	#Desscription :checks for the existence of the class GLBDATA in the
	#repository
	#               create a new one if it does not exist
	##############################################################################
	
	
	IF (CLASSEXISTS("CUST","DATA") == 0) THEN
	#{
		CREATECLASS("CUST","DATA",5)
	#}
	ENDIF
	
	############################################
	#Assigns the values to the variables
	############################################
	
	sv_a = urhk_b2k_PrintRepos("BANCS")
	print(sv_a)
	sv_y=0
	
	IF( (FIELDEXISTS(("BANCS").("INPUT").("BankID"))== 0) ) THEN
	#{
		CUST.DATA.errMsg="Kindly Supply Bank id"
		sv_e=urhk_SetOrbOut("Error|"+CUST.DATA.errMsg)
		GOTO End
		#}
	ELSE
		#{
		CUST.DATA.BankID=BANCS.INPUT.BankID
		print(CUST.DATA.BankID)
	#}
	ENDIF
	
	IF( (FIELDEXISTS(("BANCS").("INPUT").("BillerID"))== 0) ) THEN
	#{
		CUST.DATA.errMsg="Kindly Supply BillerID "
		sv_e=urhk_SetOrbOut("Error|"+CUST.DATA.errMsg)
		GOTO End
		#}
	ELSE
		#{
		CUST.DATA.BillerID=BANCS.INPUT.BillerID
		print(CUST.DATA.BillerID)
	#}
	ENDIF
	
	IF( (FIELDEXISTS(("BANCS").("INPUT").("BillID"))== 0) ) THEN
	#{
		CUST.DATA.errMsg="Kindly Supply BillID "
		sv_e=urhk_SetOrbOut("Error|"+CUST.DATA.errMsg)
		GOTO End
		#}
	ELSE
		#{
		CUST.DATA.BillID=BANCS.INPUT.BillID
		print(CUST.DATA.BillID)
	#}
	ENDIF
	
	
	
	############################################
	# Fetching the value from table
	############################################
	
	sv_q = "biller_code,foracid| "
	sv_q = sv_q + " select FREE_TEXT5,FREE_TEXT1 from tbaadm.brgt where FREE_TEXT5=?SVAR AND bank_id=?SVAR and del_flg='N'  "
	print(sv_q)
	BANCS.INPARAM.BINDVARS = CUST.DATA.BillerID + "|" + CUST.DATA.BankID
	
	sv_p= urhk_dbSelectWithBind(sv_q)
	print(sv_p)
	
	IF (sv_p==0) THEN
	#{
		
		CUST.DATA.biller_code = BANCS.OUTPARAM.biller_code
		print (CUST.DATA.biller_code)
		CUST.DATA.foracid = BANCS.OUTPARAM.foracid
		print (CUST.DATA.foracid)
		sv_q = "refnum,name| "
		sv_q = sv_q + " select refnum,name from custom.c_cic where bank_id=?SVAR AND acctnum=?SVAR AND refnum=?SVAR  "
		print(sv_q)
		BANCS.INPARAM.BINDVARS = CUST.DATA.BankID + "|" + CUST.DATA.foracid + "|" + CUST.DATA.BillID
		
		sv_p= urhk_dbSelectWithBind(sv_q)
		print(sv_p)
		
		IF (sv_p==0) THEN
		#{
			
			CUST.DATA.refnum = BANCS.OUTPARAM.refnum
			print (CUST.DATA.refnum)
			CUST.DATA.name = BANCS.OUTPARAM.name
			print (CUST.DATA.name)
			sv_z = urhk_SetOrbOut("Biller_ID|"+CUST.DATA.biller_code)
			print(sv_z)
			sv_z = urhk_SetOrbOut("BILLER_ACCOUNT|"+CUST.DATA.foracid)
			print(sv_z)
			sv_z = urhk_SetOrbOut("Bill_ID|"+CUST.DATA.BillID)
			print(sv_z)
			sv_z = urhk_SetOrbOut("Bill_ID_NAME|"+CUST.DATA.name)
			print(sv_z)
			sv_e=urhk_SetOrbOut("StattusCode|1")
			#}
		ELSE
			#{
			CUST.DATA.errMsg="BillID Does Not Exist"
			sv_e=urhk_SetOrbOut("StattusCode|2")
			sv_e=urhk_SetOrbOut("Error|"+CUST.DATA.errMsg)
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.DATA.errMsg="BillerID Does Not Exist"
		sv_e=urhk_SetOrbOut("StattusCode|2")
		sv_e=urhk_SetOrbOut("Error|"+CUST.DATA.errMsg)
	#}
	ENDIF
	
	End:
		EXITSCRIPT
		TRACE OFF
END-->
