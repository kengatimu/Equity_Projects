#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: accmdp021.scr
# event: NA
# description: Script for Maker checker validation
#
#=================================================
#**************************************************************************************************************
# Filename              : accmdp021.scr
# Description           : The script isused for checking the maker checker category of the user id
# Input                 :
# Output                :
# Called Script         :
# Calling Script        : accmdp001.scr,accmdp002.scr,accmdp003.scr,accmdp005.scr,accmdp006.scr,accmdp007.scr,accmdp008.scr
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----          ------           --------          ----------------        ---------

#  1         25-04-2013       Kalvin Joseph		 original		  1.0
#**************************************************************************************************************

<--START
CHECKSUM="c1ef4cc14d813cc0844cb855ef8f24a847d4a845bb61cb55f46210adc957f586"
	TRACE ON
	print(CUST.CONTRACT.userId)
	#CUST.CONTRACT.workClass=""
	PRINT(CUST.CONTRACT.workClass)
	
	#------------------------------------------------------------------------------
	# fetching user Id classes allowed on basis of funcCode.
	#------------------------------------------------------------------------------
	IF ((CUST.CONTRACT.funcCode == "A")OR(CUST.CONTRACT.funcCode == "M")OR(CUST.CONTRACT.funcCode == "T")OR(CUST.CONTRACT.funcCode == "E")OR(CUST.CONTRACT.funcCode == "Z")) THEN
	#{
		# CATOD Maker users class fetch
		CUST.INTERFACE.errorCode = "CATOD_MAKER"
		CALL("CATOD_GetParameterDesc.scr")
		IF ( CUST.INTERFACE.SuccessFlag == "1") THEN
		#{
			CUST.CONTRACT.errorMsg = "Could not fetch Parameter value for " +  CUST.INTERFACE.errorCode
			CUST.CONTRACT.fieldName = "abc"
			CUST.CONTRACT.successFlg = "F"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		CUST.CONTRACT.userClasses = CUST.INTERFACE.errorDesc
		print(CUST.CONTRACT.userClasses)
		GOTO USERCHECK
	#}
	ENDIF
	IF ((CUST.CONTRACT.funcCode == "V") OR (CUST.CONTRACT.funcCode == "X")) THEN
	#{
		# CATOD Checker users class fetch
		CUST.INTERFACE.errorCode = "CATOD_CHECKER"
		CALL("CATOD_GetParameterDesc.scr")
		IF ( CUST.INTERFACE.SuccessFlag == "1") THEN
		#{
			CUST.CONTRACT.errorMsg = "Could not fetch Parameter value for " +  CUST.INTERFACE.errorCode
			CUST.CONTRACT.fieldName = "abc"
			CUST.CONTRACT.successFlg = "F"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		CUST.CONTRACT.userClasses = CUST.INTERFACE.errorDesc
		print(CUST.CONTRACT.userClasses)
		GOTO USERCHECK
	#}
	ENDIF
	
	USERCHECK:
		
		# CPARAM value cant be blank.
		IF ( TRIM(CUST.CONTRACT.userClasses) == "") THEN
		#{
			sv_w = "SUCCESSORFAILURE|N"
			sv_u = urhk_SetOrbOut(sv_w)
			CUST.CONTRACT.errorMsg = "Parameter value for " +  CUST.CONTRACT.parameterId + " is blank."
			sv_z = "Error_0|ERROR^" + CUST.CONTRACT.errorMsg + "^abc"
			sv_u = urhk_SetOrbOut(sv_z)
			CUST.CONTRACT.successFlg = "F"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		print(CUST.CONTRACT.userClasses)
		#sv_a = 1
		sv_l = STRLEN(CUST.CONTRACT.userClasses)
		print(sv_l)
		WHILE(sv_l > 0)
		#{
			sv_g = GETPOSITION(CUST.CONTRACT.userClasses,",")
			print(sv_g)
			IF (sv_g == 0) THEN
			#{
				CUST.CONTRACT.classValue = TRIM(CUST.CONTRACT.userClasses)
				print(CUST.CONTRACT.classValue)
				CUST.CONTRACT.userClasses = ""
				#}
			ELSE
				#{
				CUST.CONTRACT.classValue = TRIM(MID$(CUST.CONTRACT.userClasses,0,sv_g - 1))
				CUST.CONTRACT.userClasses = MID$(CUST.CONTRACT.userClasses,sv_g,sv_l)
			#}
			ENDIF
			print(CUST.CONTRACT.classValue)
			IF (CUST.CONTRACT.workClass == CUST.CONTRACT.classValue) THEN
			#{
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			print(CUST.CONTRACT.userClasses)
			sv_l = STRLEN(CUST.CONTRACT.userClasses)
			print(sv_l)
			IF (sv_l == 0) THEN
			#{
				CUST.INTERFACE.errorCode="CTODA_ERR_013"
				CALL("Common_Interface_GetErrorDesc.scr")
				IF ( CUST.INTERFACE.SuccessFlag == "1") THEN
				#{
					sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
					CUST.CONTRACT.errorMsg = "Could not fetch Parameter value for " +  CUST.INTERFACE.errorCode
					sv_z = "Error_0|ERROR^" + CUST.CONTRACT.errorMsg + "^contrctAmt"
					sv_u = urhk_SetOrbOut(sv_z)
					CUST.CONTRACT.successFlg = "F"
					GOTO ENDOFSCRIPT
					#}
				ELSE
					#{
					CUST.CONTRACT.errorMsg=CUST.INTERFACE.errorDesc
					sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
					print(sv_u)
					sv_u = urhk_SetOrbOut("Error_0|ERROR^" + CUST.CONTRACT.errorMsg + "^contrctAmt")
					print(sv_u)
					GOTO ENDOFSCRIPT
				#}
				ENDIF
			#}
			ENDIF
		#}
		DO
		
		
	ENDOFSCRIPT:
		EXITSCRIPT
END-->
