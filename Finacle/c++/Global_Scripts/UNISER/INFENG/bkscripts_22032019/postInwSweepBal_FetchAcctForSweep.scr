#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: postInwSweepBal_FetchAcctForSweep.scr
# Type: NA|default
# Description: postInwSweepBal_FetchAcctForSweep.scr
# Author:
# Date:
# Modification Log:
#============================================================

#----------------------------------------------------------------------------------------------------------
# Filename              :       postInwSweepBal_FetchDrAccts.scr
# Description           :
# Input                 :       None
# Output                :       None
# Called Script         :       None
# Calling Script        :
#
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----           -----           --------          ----------------        ---------
#  1           15-11-2013         Venkatesh         Original Version         1.0
#-----------------------------------------------------------------------------------------------------------
<--START
CHECKSUM="2ac45a49cf17820412e476a0a48f61e2a7bb904790f75a949ec9773dc5ed23bc"
	TRACE ON
	
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + "VERSWEEP" + "|Y|N|"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "%_" + CUST.INTERFACE.crAcctId
	sv_s = "drAcct|SELECT PARAMETER_VALUE FROM CUSTOM.C_CPVALUE WHERE"
	sv_s = sv_s + " BANK_ID = ?SVAR AND MODULE_NAME = ?SVAR AND ENTITY_CRE_FLG = ?SVAR"
	sv_s = sv_s + " AND DEL_FLG = ?SVAR AND PARAMETER_ID like ?SVAR"
	sv_d = urhk_dbCursorOpenWithBind(sv_s)
	IF(sv_d != 0) THEN
	#{
		CUST.INTERFACE.errMsg = "No sweeping required for for " + CUST.INTERFACE.crAcctId + " in entity id " + BANCS.STDIN.contextBankId
		CUST.INTERFACE.successOrFailure = "S"
		sv_w = SYSTEM("echo " + CUST.INTERFACE.errMsg + ">>" + CUST.INTERFACE.failLog)
		GOTO FETCHNEXTSWEEPREC
	#}
	ENDIF
	
	sv_n = BANCS.OUTPARAM.DB_CURSOR_NUMBER
	PRINT(sv_n)
	sv_d = urhk_dbCursorFetch(sv_n)
	PRINT(sv_d)
	IF(sv_d != 0) THEN
	#{
		CUST.INTERFACE.errMsg = "No sweeping required for for " + CUST.INTERFACE.crAcctId + " in entity id " + BANCS.STDIN.contextBankId
		CUST.INTERFACE.successOrFailure = "S"
		sv_w = SYSTEM("echo " + CUST.INTERFACE.errMsg + ">>" + CUST.INTERFACE.failLog)
		GOTO FETCHNEXTSWEEPREC
	#}
	ENDIF
	sv_i = 1
	WHILE(sv_d == 0)
	#{
		sv_v = "drAcctId_" + FORMAT$(sv_i,"%d")
		("CUST").("INTERFACE").(sv_v) = BANCS.OUTPARAM.drAcct
		PRINT(("CUST").("INTERFACE").(sv_v))
		
		sv_i = sv_i + 1
		sv_d = urhk_dbCursorFetch(sv_n)
		PRINT(sv_d)
	#}
	DO
	
	sv_d = urhk_dbCursorClose(sv_n)
	
	CUST.INTERFACE.recCount = sv_i - 1
	PRINT(CUST.INTERFACE.recCount)
	
	#trace off
END-->
