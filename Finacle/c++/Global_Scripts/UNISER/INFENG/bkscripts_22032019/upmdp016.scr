#----------------------------------------------------------------------------------------------------------
# Filename              :       upmdp016.scr
# Author                :       Hari Mohan Verma
# Description           :       This is the script for validating temporary role id
#
# Input                 :       temporary role id,bank id
# Output                :       validation of temporary role id
# Called Script         :       None
# Calling Script        :       None
#
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----        ----------         --------------    ----------------        ---------
#  1           25-06-2013         Hari MOhan Verma   original Version          1.0
#                                 Nithya Murugesan
#-----------------------------------------------------------------------------------------------------------
<--START
CHECKSUM="836e8c8256be9016bd97e4e036dedaeeb481315fc346b832fcfc0a47f9c95bb8"
	TRACE ON
	#-------------------------------------------------------------
	# Repository creation
	#-------------------------------------------------------------
	IF (REPEXISTS("CUST") == 0 ) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	IF (CLASSEXISTS("CUST","UPM") == 0) THEN
	#{
		CREATECLASS("CUST","UPM",5)
	#}
	ENDIF
	
	
	sv_u = urhk_B2K_PrintRepos("BANCS")
	CUST.UPM.tempRoleId = BANCS.INPUT.tempRoleId
	CUST.UPM.contextBankId = BANCS.STDIN.contextBankId
	CUST.UPM.dataVal = ""
	#-------------------------------------------------------------
	# checking the role id in RPT table
	#-------------------------------------------------------------
	
	CUST.UPM.expiryDate = BANCS.INPUT.expiryDate
	BANCS.INPARAM.date1 = CUST.UPM.expiryDate
	BANCS.INPARAM.date2 = BANCS.STDIN.BODDate
	
	sv_u = urhk_B2k_dateDiff("")
	IF (sv_u == 0) THEN
	#{
		IF (BANCS.OUTPARAM.dateDiff <  0) THEN
		#{
			print(BANCS.OUTPARAM.dateDiff)
			CUST.UPM.dateVal = "S"
			
			#}
		ELSE
			#{
			print(BANCS.OUTPARAM.dateDiff)
			CUST.UPM.dateVal = "N"
		#}
		ENDIF
	#}
	ENDIF
	
	BANCS.INPARAM.BINDVARS = CUST.UPM.tempRoleId
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.UPM.contextBankId
	
	print(BANCS.INPARAM.BINDVARS)
	sv_s = "count| Select count(*) from RPT WHERE ROLE_ID = ?SVAR AND BANK_ID = ?SVAR AND DEL_FLG = 'N' "
	sv_d = urhk_dbSelectWithBind(sv_s)
	IF(sv_d != 0) THEN
	#{
		print("Query execution failed")
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		IF(BANCS.OUTPARAM.count != 0) THEN
		#{
			sv_w = "SUCCESSORFAILURE|S"
			print(sv_w)
			sv_u = urhk_SetOrbOut(sv_w)
			print(sv_u)
			sv_u =urhk_SetOrbOut("errorFlg|"+CUST.UPM.dateVal)
			print(sv_u)
			#}
		ELSE
			#{
			sv_w = "SUCCESSORFAILURE|F"
			print(sv_w)
			sv_u = urhk_SetOrbOut(sv_w)
			print(sv_u)
			sv_u =urhk_SetOrbOut("errorFlg|"+CUST.UPM.dateVal)
			print(sv_u)
		#}
		ENDIF
	#}
	ENDIF
	ENDOFSCRIPT:
		#TRACE OFF
END-->
