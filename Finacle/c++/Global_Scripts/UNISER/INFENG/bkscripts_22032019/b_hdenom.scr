<--START
CHECKSUM="78c848ac1ed53da55ea0b3d41e84560a4a719d39bb2025de6bec0f298230491d"
	TRACE ON
	# Script body goes here
	
	sv_a = urhk_B2k_PrintRepos("BANCS")
	print(sv_a)
	
	sv_z = urhk_SetOrbOut("output|"+ BANCS.STDIN.userWorkClass)
	print(sv_z)
	
	
	sv_r = REPEXISTS("STDWFS")
	IF (sv_r == 0) THEN
	#{
		CREATEREP("STDWFS")
	#}
	ENDIF
	
	sv_r = CLASSEXISTS("STDWFS", "GLBDATA")
	IF (sv_r == 0) THEN
	#{
		CREATECLASS("STDWFS", "GLBDATA", 5)
	#}
	ENDIF
	#IF (BANCS.STDIN.mySolId=="017") THEN
	IF (BANCS.STDIN.userId=="XXXXX") THEN
	#{
		STDWFS.GLBDATA.denomstatus="Y"
		sv_z = urhk_SetOrbOut("output2|"+ STDWFS.GLBDATA.denomstatus)
		print(sv_z)
		#}
	ELSE
		#{
		STDWFS.GLBDATA.denomstatus="N"
		sv_z = urhk_SetOrbOut("output2|"+ STDWFS.GLBDATA.denomstatus)
		print(sv_z)
	#}
	ENDIF
	
	TRACE OFF
END-->
