#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: s_shdwbalchrgmn001.scr
# Type: NA|default
# Description: s_shdwbalchrgmn001.scr
# Author:
# Date:
# Modification Log:
#============================================================

#-------------------------------------------------------------------------------------------------------------
#    Name			 : s_shdwbalchrgmn001.scr
#    Description     : This script is used for the validation  find effective balance before transaction
#    Date            : 15-11-2013
#    Author          : Bharath
#    Input           : NA
#    Output			 : NA
#    Called Script	 : Tran SRV pre process
#    Calling Script	 : NA
#    Menu Option	 : HTM
#    Srl. No			Date			Author			Description.
#    ------			----------			-------			---------------
#    1.0			15-11-2013			Prakash			Original Version
#---------------------------------------------------------------------------------------------------------------
<--START
CHECKSUM="f432dab6b21369c40f1ee173621f01c797d20863e3a93bf716ed25d3220ccfee"
	#TRACE ON
	
	#------------------------------------------------------------------------------
	#	Creating repository and class
	#	Initializing the variables
	# 	Getting all the values from CPARAM
	#------------------------------------------------------------------------------
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST","RATE") == 0) THEN
	#{
		CREATECLASS("CUST","RATE",5)
	#}
	ENDIF
	
	
	#-------------------------------------------------------------------------------------------------------
	# Initializing
	#-------------------------------------------------------------------------------------------------------
	CUST.RATE.errMsg      = ""
	
	#-------------------------------------------------------------------------------------------------------
	# Get the number of accounts entered from HTM menu option
	#-------------------------------------------------------------------------------------------------------
	sv_u = urhk_SRV_NumOfRecs("tranDtl.partTranDetailLL")
	PRINT(sv_u)
	IF(sv_u == 0) THEN
	#{
		CUST.RATE.noOfRecords = CINT(BANCS.OUTPARAM.srvValue)
		PRINT(CUST.RATE.noOfRecords)
		#}
	ELSE
		#{
		CUST.RATE.errMsg = "Error:urhk_SRV_NumOfRecs failed"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_l = 0
	
	WHILE(sv_l < CUST.RATE.noOfRecords)
	#{
		sv_v = Format$(sv_l,"%d")
		sv_r = "<rec_"+sv_v+">"
		
		#-------------------------------------------------------------------------------------------
		# Fetch A/c - foracid
		#-------------------------------------------------------------------------------------------
		sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".acctId.foracid")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			CUST.RATE.foracid = BANCS.OUTPARAM.srvValue
			PRINT(CUST.RATE.foracid)
			#}
		ELSE
			#{
			CUST.RATE.errMsg = "Error: urhk_SRV_GetVal failed for Account Number"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		sv_u = urhk_getAcctDetailsInRepository(CUST.RATE.foracid)
		PRINT(sv_u)
		IF (sv_u ==0) THEN
		#{
			("CUST").("RATE").("EB"+CUST.RATE.foracid) = BANCS.OUTPARAM.EffAvailableAmt
			CUST.RATE.preEffAvailableAmt = ("CUST").("RATE").("EB"+CUST.RATE.foracid)
			PRINT(CUST.RATE.preEffAvailableAmt)
		#}
		ENDIF
		
		sv_l = sv_l + 1
		PRINT (sv_l)
	#}
	DO
	
	ENDOFSCRIPT:
		IF (CUST.RATE.errMsg != "") THEN
		#{
			sv_u = urhk_SRV_SetErr(CUST.RATE.errMsg)
			BANCS.OUTPUT.successOrFailure="F"
		#}
		ENDIF
		
		EXITSCRIPT
END-->
