#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: comsnAddRec.scr
# Type: NA|default
# Description: comsnAddRec.scr
# Author: shivakrishna.d
# Date: 6/18/18 7:14 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="7da1e09bb88cc29f4be72a4ecb50641fdc5665e4ce2f1c3f54d6f51c687eb01c"
	# Script body goes here
	TRACE ON
	
	sv_a = REPEXISTS("EQT")
	IF (sv_a == 0) THEN
	#{
		CREATEREP("EQT")
	#}
	ENDIF
	sv_a = CLASSEXISTS("EQT", "COMM")
	IF (sv_a == 0 ) THEN
	#{
		CREATECLASS("EQT", "COMM", 5)
	#}
	ENDIF
	
	EQT.COMM.msg = " "
	EQT.COMM.chkMOd = "N"
	
	PRINT(BANCS.INPUT.commCode)
	PRINT(BANCS.INPUT.commDesc)
	PRINT(BANCS.INPUT.commCrncy)
	PRINT(BANCS.INPUT.commpcnt)
	PRINT(BANCS.INPUT.commVat)
	
	EQT.COMM.commCode=BANCS.INPUT.commCode
	EQT.COMM.commCrncy=BANCS.INPUT.commCrncy
	EQT.COMM.commDesc=MID$(BANCS.INPUT.commDesc,0,100)
	PRINT(EQT.COMM.commDesc)
	EQT.COMM.commpcnt=BANCS.INPUT.commpcnt
	EQT.COMM.mincomm=BANCS.INPUT.mincomm
	EQT.COMM.commVat=BANCS.INPUT.commVat
	
	
	##############################################################################
	# Counting Rows from MOD table
	##############################################################################
	
	sv_d = "recCount|SELECT count(1) FROM CUSTOM.C_COMM_MOD WHERE bank_id=?SVAR AND COMM_CODE=?SVAR"
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + EQT.COMM.commCode
	
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_r = urhk_dbSelectWithBind(sv_d)
	PRINT(sv_r)
	
	IF(sv_r!=0)THEN
	#{
		EQT.COMM.msg = "Error in Fetching Records from DB!!!"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	PRINT(BANCS.OUTPARAM.recCount)
	
	IF((BANCS.INPUT.funcCode=="M") AND (BANCS.OUTPARAM.recCount!=0)) THEN
	#{
		EQT.COMM.chkMOd = "Y"
		GOTO UPDATE_MOD
	#}
	ENDIF
	##############################################################################
	
	sv_w= "INSERT INTO CUSTOM.C_COMM_MOD (COMM_CODE, COMM_DESC, CRNCY_CODE, VAT_RATE, COMM_PCNT, MIN_COMM, RCRE_USER_ID, RCRE_TIME, LCHG_USER_ID, LCHG_TIME, BANK_ID)"
	
	#-------------------------------------------------
	# Getting the record values from front end
	#-------------------------------------------------
	
	sv_v =" VALUES (?SVAR, ?SVAR, ?SVAR, ?SVAR, ?SVAR, ?SVAR,?SVAR, sysdate, ?SVAR, sysdate, ?SVAR)"
	sv_v = sv_w+sv_v
	PRINT(sv_v)
	
	BANCS.INPARAM.BINDVARS = EQT.COMM.commCode + "|" + EQT.COMM.commDesc + "|" + EQT.COMM.commCrncy + "|" + EQT.COMM.commVat + "|" +  EQT.COMM.commpcnt + "|" +  EQT.COMM.mincomm + "|" +  BANCS.STDIN.userId +  "|" + BANCS.STDIN.userId + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_e = urhk_dbsqlWithBind(sv_v)
	PRINT(sv_e)
	
	UPDATE_MOD:
		
		IF(EQT.COMM.chkMOd == "Y") THEN
		#{
			sv_w = "UPDATE CUSTOM.C_COMM_MOD set COMM_DESC= ?SVAR, CRNCY_CODE= ?SVAR, VAT_RATE= ?SVAR, COMM_PCNT= ?SVAR,MIN_COMM= ?SVAR, LCHG_USER_ID= ?SVAR, LCHG_TIME= sysdate"
			sv_w = sv_w + " WHERE COMM_CODE = ?SVAR AND BANK_ID = ?SVAR"
			
			PRINT(sv_w)
			
			BANCS.INPARAM.BINDVARS = EQT.COMM.commDesc + "|" + EQT.COMM.commCrncy + "|" + EQT.COMM.commVat + "|" + EQT.COMM.commpcnt  + "|" + EQT.COMM.mincomm + "|" +  BANCS.STDIN.userId  +  "|" +  EQT.COMM.commCode + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			
			sv_e = urhk_dbsqlWithBind(sv_w)
			PRINT(sv_e)
		#}
		ENDIF
		
		IF(sv_e!=0)THEN
		#{
			EQT.COMM.msg = "Error in Adding Records in DB!!!"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		IF(BANCS.INPUT.funcCode=="A")THEN
		#{
			EQT.COMM.msg="Record has been Added Successfully."
		#}
		ENDIF
		
		IF(BANCS.INPUT.funcCode=="M")THEN
		#{
			EQT.COMM.msg="Record has been Modified Successfully."
		#}
		ENDIF
		
	ENDOFSCRIPT:
		
		sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
		
		EXITSCRIPT
END-->
