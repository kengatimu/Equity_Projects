#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: hoccmmn003.scr
# event: NA
# description: NA
#=================================================
#----------------------------------------------------------------------------------------------------------
# Filename              :       hoccmmn003.scr
# Author                :       Nitin Prabhakar
# Description           :       This is the main script that gets triggered on Go button click of Release mode.
# Input                 :       Front End fields of Criteria Pages
# Output                :       values for details page FE fields value.
# Called Script         :       hoccmdp002.scr, hoccmdp006.scr, hoccmdp007.scr
# Calling Script        :       None
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----           -----           --------          ----------------        ---------
#  1           03-07-2013      Nitin Prabhakar       Original Version          1.0
#-----------------------------------------------------------------------------------------------------------
<--START
CHECKSUM="524d170417f056f096412bc4f198cdf1e1dac82eaac76ec65655aeabc7ef4e1f"
	TRACE ON
	sv_u = urhk_B2k_PrintRepos("BANCS")
	print(sv_u)
	#------------------------------------------------------------------------------
	# Creation of  repository and class
	#------------------------------------------------------------------------------
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	IF (CLASSEXISTS("CUST", "HOCCM") == 0) THEN
	#{
		CREATECLASS("CUST", "HOCCM", 5)
	#}
	ENDIF
	IF (CLASSEXISTS("CUST", "INTERFACE") == 0) THEN
	#{
		CREATECLASS("CUST", "INTERFACE", 5)
	#}
	ENDIF
	#------------------------------------------------------------------------------
	#  Declaration of variables used in script
	#------------------------------------------------------------------------------
	CUST.HOCCM.userId = TRIM(BANCS.STDIN.userId)
	print(CUST.HOCCM.userId)
	CUST.HOCCM.bankId = TRIM(BANCS.STDIN.contextBankId)
	print(CUST.HOCCM.bankId)
	CUST.HOCCM.solID = TRIM(BANCS.STDIN.mySolId)
	print(CUST.HOCCM.solID)
	CUST.HOCCM.funcCode = TRIM(BANCS.INPUT.funcCode)
	print(CUST.HOCCM.funcCode)
	
	CUST.HOCCM.critCrAcctNum = TRIM(BANCS.INPUT.critCrAcctNum)
	print(CUST.HOCCM.critCrAcctNum)
	CUST.HOCCM.zoneCode = TRIM(BANCS.INPUT.zoneCode)
	print(CUST.HOCCM.zoneCode)
	CUST.HOCCM.zoneDate = TRIM(BANCS.INPUT.zoneDate)
	print(CUST.HOCCM.zoneDate)
	CUST.HOCCM.zoneCCY = TRIM(BANCS.INPUT.zoneCCY)
	print(CUST.HOCCM.zoneCCY)
	CUST.HOCCM.smInstrument = TRIM(BANCS.INPUT.smInstrument)
	print(CUST.HOCCM.smInstrument)
	CUST.HOCCM.noOfChq = TRIM(BANCS.INPUT.noOfChq)
	print(CUST.HOCCM.noOfChq)
	CUST.HOCCM.totalCrAmt = TRIM(BANCS.INPUT.totalCrAmt)
	print(CUST.HOCCM.totalCrAmt)
	CUST.HOCCM.setNum = TRIM(BANCS.INPUT.setNum)
	print(CUST.HOCCM.setNum)
	CUST.HOCCM.critBatchNum = TRIM(BANCS.INPUT.critBatchNum)
	print(CUST.HOCCM.critBatchNum)
	CUST.HOCCM.critTranId = TRIM(BANCS.INPUT.critTranId)
	print(CUST.HOCCM.critTranId)
	CUST.HOCCM.zoneIdentifier = TRIM(BANCS.INPUT.zoneIdentifier)
	print(CUST.HOCCM.zoneIdentifier)
	##UBP Variables--START
	IF(FIELDEXISTS(BANCS.INPUT.billerId) == 1)THEN
	#{
		CUST.HOCCM.billerId = TRIM(BANCS.INPUT.billerId)
		print(CUST.HOCCM.billerId)
	#}
	ENDIF
	
	
	CUST.HOCCM.successFlg = "S"
	CUST.INTERFACE.srvErrFlg ="N"
	CUST.HOCCM.entry_user_id = ""
if(BANCS.STDIN.contextBankId=="54")then
                if(CUST.HOCCM.zoneCode!="CTSINHSE")then
                        CUST.HOCCM.successFlg = "F"
                        CUST.INTERFACE.srvErrFlg = "N"
                        CUST.HOCCM.errorMsg = "Clearing Not Allowed Finacle2!!!!!! Other than CTSINHSE"
                        CUST.HOCCM.fieldName = "zoneCode"
                        GOTO ENDOFSCRIPT
                endif
endif

	
	#------------------------------------------------------------------------------
	# Checking maker-checker concept
	#------------------------------------------------------------------------------
	IF(CUST.HOCCM.funcCode == "R") THEN
	#{
		BANCS.INPARAM.BINDVARS = CUST.HOCCM.solID + "|" + CUST.HOCCM.zoneDate
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.HOCCM.zoneCode
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.HOCCM.bankId
		print(BANCS.INPARAM.BINDVARS)
		sv_s = ""
		sv_s = sv_s + "entry_user_id|"
		sv_s = sv_s + " SELECT ENTRY_USER_ID"
		sv_s = sv_s + " FROM TBAADM.OCI "
		sv_s = sv_s + " WHERE SOL_ID = ?SVAR "
		sv_s = sv_s + " AND CLG_ZONE_DATE = TO_DATE(?SVAR,'DD-MM-YYYY') "
		sv_s = sv_s + " AND CLG_ZONE_CODE = ?SVAR "
		sv_s = sv_s + " AND trim(SET_NUM) = '" + CUST.HOCCM.setNum + "' "
		sv_s = sv_s + " AND STATUS_FLG = ' ' "
		sv_s = sv_s + " AND BANK_ID = ?SVAR AND ROWNUM = 1"
		print(sv_s)
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		IF(sv_d != 0)THEN
		#{
			# error
			sv_w = "SUCCESSORFAILURE|N"
			sv_u = urhk_SetOrbOut(sv_w)
			sv_m = "Error in fetching entry user id"
			sv_z = "Error|" + sv_m
			sv_u = urhk_SetOrbOut(sv_z)
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			CUST.HOCCM.entry_user_id = BANCS.OUTPARAM.entry_user_id
			print(CUST.HOCCM.entry_user_id)
			print(CUST.HOCCM.userId)
			IF(TRIM(CUST.HOCCM.entry_user_id)==TRIM(CUST.HOCCM.userId)) THEN
			#{
				#print("same user")
				CUST.HOCCM.errorMsg = "Same user cannot release the Set"
				CUST.HOCCM.fieldName = "abc"
				CUST.HOCCM.successFlg = "F"
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	
	
	#------------------------------------------------------------------------------
	# calling script to validate criteria page
	#------------------------------------------------------------------------------
	CALL("hoccmdp002.scr")
	IF (CUST.HOCCM.successFlg == "F") THEN
	#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	#------------------------------------------------------------------------------
	# calling script to fetch
	#------------------------------------------------------------------------------
	IF (CUST.HOCCM.smInstrument == "M") THEN
	#{
		call("hoccmdp006.scr")
		#}
	ELSE
		#{
		call("hoccmdp007.scr")
	#}
	ENDIF
	IF (CUST.HOCCM.successFlg == "F") THEN
	#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	
	
	BANCS.OUTPUT.SuccessOrFailure = "S"
	sv_w = ""
	sv_w = "SuccessOrFailure|Y"
	sv_u = urhk_SetOrbOut(sv_w)
	ENDOFSCRIPT:
		IF ((CUST.HOCCM.successFlg == "F") AND (CUST.INTERFACE.srvErrFlg !="Y")) THEN
		#{
			sv_w = "SUCCESSORFAILURE|N"
			sv_u = urhk_SetOrbOut(sv_w)
			sv_z = "Error_0|ERROR^" + CUST.HOCCM.errorMsg + "^" + CUST.HOCCM.fieldName
			sv_u = urhk_SetOrbOut(sv_z)
			CUST.HOCCM.successFlg = "F"
		#}
		ENDIF
		#----------------------------------------
		# deleting repositories and calsses.
		#----------------------------------------
		IF (CLASSEXISTS("CUST","HOCCM") != 0) THEN
		#{
			DELETECLASS ("CUST","HOCCM")
		#}
		ENDIF
		IF (CLASSEXISTS("CUST","INTERFACE") != 0) THEN
		#{
			DELETECLASS ("CUST","INTERFACE")
		#}
		ENDIF
		IF (REPEXISTS ("CUST") != 0) THEN
		#{
			DELETEREP ("CUST")
		#}
		ENDIF
		EXITSCRIPT
		TRACE OFF
END-->
