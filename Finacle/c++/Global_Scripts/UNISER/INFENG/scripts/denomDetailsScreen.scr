#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: denomDetailsScreen.scr
# event: NA
# description: NA
#=================================================

<--START
CHECKSUM="67fd09b155a6b9f356b6104282a59455c082578d91687bcb5b246198df7a924b"
	TRACE ON
	sv_r = REPEXISTS("CUST")
	IF (sv_r == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	sv_r = CLASSEXISTS("CUST", "DENOM")
	IF (sv_r == 0) THEN
	#{
		CREATECLASS("CUST", "DENOM", 5)
	#}
	ENDIF
	
	sv_q = urhk_B2k_PrintRepos("BANCS")
	print(sv_q)
	
	
	print(BANCS.INPUT.tranId)
	CUST.DENOM.tranId = BANCS.INPUT.tranId
	print(CUST.DENOM.tranId)
	CUST.DENOM.delFlg = "N"
	CUST.DENOM.EntityCreFlg = "Y"
	print(BANCS.STDIN.EmployeeId)
	
	#---------------------------------------------------------------------#
	# Code fetching the bank Name from BKC table     #
	#---------------------------------------------------------------------#
	
	BANCS.INPARAM.BINDVARS = ""
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.myBankCode+ "|" +BANCS.STDIN.contextBankId + "|" + CUST.DENOM.delFlg
	print(BANCS.INPARAM.BINDVARS)
	sv_s = ""
	sv_s = "bankName|SELECT BANK_NAME from TBAADM.BKC where BANK_CODE= ?SVAR AND BANK_ID = ?SVAR "
	sv_s = sv_s + " AND NVL(DEL_FLG,'N') = ?SVAR "
	print(sv_s)
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	IF(sv_d == 0) THEN
	#{
		CUST.DENOM.bankName = BANCS.OUTPARAM.bankName
		#}
	ELSE
		#{
		CUST.DENOM.bankName = ""
	#}
	ENDIF
	print(CUST.DENOM.bankName)
	
	#---------------------------------------------------------------------#
	# Code fetching the branch Name from BCT table     #
	#---------------------------------------------------------------------#
	
	BANCS.INPARAM.BINDVARS = ""
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.myBankCode+ "|" +BANCS.STDIN.contextBankId+ "|" +BANCS.STDIN.myBrCode + "|" + CUST.DENOM.delFlg
	print(BANCS.INPARAM.BINDVARS)
	sv_s = ""
	sv_s = "branchName|SELECT BR_NAME from TBAADM.BCT where BANK_CODE= ?SVAR AND BANK_ID = ?SVAR "
	sv_s = sv_s + " AND BR_CODE = ?SVAR AND  NVL(DEL_FLG,'N') = ?SVAR "
	print(sv_s)
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	IF(sv_d == 0) THEN
	#{
		CUST.DENOM.branchName = BANCS.OUTPARAM.branchName
		#}
	ELSE
		#{
		CUST.DENOM.branchName = ""
	#}
	ENDIF
	print(CUST.DENOM.branchName)
	
	#---------------------------------------------------------------------#
	# Code fetching the bank Name from BKC table     #
	#---------------------------------------------------------------------#
	
	BANCS.INPARAM.BINDVARS = ""
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.EmployeeId+ "|" + CUST.DENOM.EntityCreFlg + "|" + CUST.DENOM.delFlg
	print(BANCS.INPARAM.BINDVARS)
	sv_s = ""
	sv_s = "empName|SELECT EMP_NAME from TBAADM.GET where EMP_ID= ?SVAR AND ENTITY_CRE_FLG = ?SVAR "
	sv_s = sv_s + " AND NVL(DEL_FLG,'N') = ?SVAR "
	print(sv_s)
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	IF(sv_d == 0) THEN
	#{
		CUST.DENOM.empName = BANCS.OUTPARAM.empName
		#}
	ELSE
		#{
		CUST.DENOM.empName = ""
	#}
	ENDIF
	print(CUST.DENOM.empName)
	
	
	#---------------------------------------------------------------------#
	# fetching data from the cashdx  table     #
	#---------------------------------------------------------------------#
	
	CUST.DENOM.sysdate = BANCS.STDIN.sysDate
	print(CUST.DENOM.sysdate)
	
	CUST.DENOM.empId = BANCS.STDIN.userId
	print (CUST.DENOM.empId)
	
	
	sv_s = "custName,idNum,phoneNo,docDetails,empId,nationDet|SELECT CUSTOMER_NAME,ID_NUM,PHONE_NUM,SUPPORTING_DETAILS,USER_ID,NATIONALITY from("
	sv_s = sv_s + "SELECT CUSTOMER_NAME,ID_NUM,PHONE_NUM,SUPPORTING_DETAILS,USER_ID,NATIONALITY "
	sv_s = sv_s + "FROM custom.cashdx_custom "
	sv_s = sv_s + "WHERE BANK_ID = ?SVAR "
	sv_s = sv_s + "AND USER_ID = ?SVAR "
	sv_s = sv_s + "AND CUSTOMER_NAME IS NOT NULL "
	sv_s = sv_s + "AND PHONE_NUM IS NOT NULL "
	sv_s = sv_s + "order by RCRE_TIME desc) "
	sv_s = sv_s + "where rownum <2 "
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + CUST.DENOM.empId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF (sv_d == 0) THEN
	#{
		CUST.DENOM.custName = BANCS.OUTPARAM.custName
		print(CUST.DENOM.custName)
		CUST.DENOM.idNum = BANCS.OUTPARAM.idNum
		print(CUST.DENOM.idNum)
		CUST.DENOM.phoneNo = BANCS.OUTPARAM.phoneNo
		print(CUST.DENOM.phoneNo)
		CUST.DENOM.docDetails = BANCS.OUTPARAM.docDetails
		print(CUST.DENOM.docDetails)
		CUST.DENOM.empId = BANCS.OUTPARAM.empId
		print(CUST.DENOM.empId)
		CUST.DENOM.nationDet = BANCS.OUTPARAM.nationDet
		print(CUST.DENOM.nationDet)
		
		#}
	ELSE
		#{
		
		CUST.DENOM.custName = ""
		CUST.DENOM.idNum = ""
		CUST.DENOM.phoneNo = ""
		CUST.DENOM.docDetails = ""
		CUST.DENOM.empId = ""
		CUST.DENOM.nationDet = ""
	#}
	ENDIF
	
	IF (CUST.DENOM.docDetails == "") THEN
	#{
		CUST.DENOM.docDetails = "__________"
	#}
	ENDIF
	IF (CUST.DENOM.nationDet == "") THEN
	#{
		CUST.DENOM.nationDet = "__________"
	#}
	ENDIF
	
	
	
	
	BANCS.INPARAM.BINDVARS = ""
	BANCS.INPARAM.BINDVARS = CUST.DENOM.tranId + "|" +  BANCS.STDIN.BODDate  + "|" +BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_s = "partTranType,crncy_code,denom_ind,denom_value,denom_count|"
	sv_s = sv_s + "SELECT PTRAN_TYPE,CRNCY_CODE,DENOM_IND,DENOM_VALUE,DENOM_COUNT"
	sv_s = sv_s + " FROM CUSTOM.C_DETD"
	sv_s = sv_s + " WHERE  DXFER_ID = ?SVAR"
	sv_s = sv_s + " AND tran_date = to_date(?SVAR,'DD-MM-YYYY HH24:MI:SS') "
	sv_s = sv_s + " AND  BANK_ID = ?SVAR AND TYPE is null ORDER BY PTRAN_TYPE,DENOM_IND"
	
	print(sv_s)
	sv_c = urhk_dbCursorOpenWithBind(sv_s)
	IF (sv_c != 0)THEN
	#{
		CUST.DENOM.errorMesg = "NOT ABLE FETCH DENOM DETAILS"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	CUST.DENOM.curNum = BANCS.OUTPARAM.DB_CURSOR_NUMBER
	
	sv_c = urhk_dbCursorFetch(CUST.DENOM.curNum)
	#print(sv_c)
	IF (sv_c != 0)THEN
	#{
		CUST.DENOM.errorMesg = "NOT ABLE FETCH DENOM DETAIL"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	WHILE(sv_c == 0)
	#{
		CUST.DENOM.partTranType = BANCS.OUTPARAM.partTranType
		print(CUST.DENOM.partTranType)
		CUST.DENOM.crncy_code = BANCS.OUTPARAM.crncy_code
		print(CUST.DENOM.crncy_code)
		CUST.DENOM.denom_ind = BANCS.OUTPARAM.denom_ind
		print(CUST.DENOM.denom_ind)
		CUST.DENOM.denom_value = BANCS.OUTPARAM.denom_value
		print(CUST.DENOM.denom_value)
		CUST.DENOM.denom_count = BANCS.OUTPARAM.denom_count
		print(CUST.DENOM.denom_count)
		
		IF((CUST.DENOM.partTranType == "C") AND (CUST.DENOM.denom_ind == "C")) THEN
		#{
			sv_u = urhk_SetOrbOut("creditCoins|"+CUST.DENOM.denom_count)
			print(sv_u)
			sv_u = urhk_SetOrbOut("creditCoinsValue|"+CUST.DENOM.denom_value)
			print(sv_u)
		#}
		ENDIF
		IF((CUST.DENOM.partTranType == "C") AND (CUST.DENOM.denom_ind == "N")) THEN
		#{
			sv_u = urhk_SetOrbOut("creditNotes|"+CUST.DENOM.denom_count)
			print(sv_u)
			sv_u = urhk_SetOrbOut("creditNotesValue|"+CUST.DENOM.denom_value)
			print(sv_u)
			
		#}
		ENDIF
		IF((CUST.DENOM.partTranType == "D") AND (CUST.DENOM.denom_ind == "C")) THEN
		#{
			sv_u = urhk_SetOrbOut("debitCoins|"+CUST.DENOM.denom_count)
			print(sv_u)
			sv_u = urhk_SetOrbOut("debitCoinsValue|"+CUST.DENOM.denom_value)
			print(sv_u)
		#}
		ENDIF
		IF((CUST.DENOM.partTranType == "D") AND (CUST.DENOM.denom_ind == "N")) THEN
		#{
			sv_u = urhk_SetOrbOut("debitNotes|"+CUST.DENOM.denom_count)
			print(sv_u)
			sv_u = urhk_SetOrbOut("debitNotesValue|"+CUST.DENOM.denom_value)
			print(sv_u)
			
		#}
		ENDIF
		
		IF(CUST.DENOM.partTranType == "C") THEN
		#{
			CUST.DENOM.creditCrncy = CUST.DENOM.crncy_code
		#}
		ENDIF
		IF(CUST.DENOM.partTranType == "D") THEN
		#{
			CUST.DENOM.debitCrncy = CUST.DENOM.crncy_code
		#}
		ENDIF
		
		
		
		sv_c = urhk_dbCursorFetch(CUST.DENOM.curNum)
	#}
	DO
	sv_c = urhk_dbCursorClose(CUST.DENOM.curNum)
	
	IF(CUST.DENOM.bankName != "")  THEN
	#{
		sv_u = urhk_SetOrbOut("bankName|"+CUST.DENOM.bankName)
		print(sv_u)
	#}
	ENDIF
	
	IF(CUST.DENOM.branchName != "")  THEN
	#{
		sv_u = urhk_SetOrbOut("branchName|"+CUST.DENOM.branchName)
		print(sv_u)
	#}
	ENDIF
	
	IF(CUST.DENOM.empName != "")  THEN
	#{
		sv_u = urhk_SetOrbOut("empName|"+CUST.DENOM.empName)
		print(sv_u)
	#}
	ENDIF
	
	IF(CUST.DENOM.creditCrncy != "")  THEN
	#{
		sv_u = urhk_SetOrbOut("creditCrncy|"+CUST.DENOM.creditCrncy)
		print(sv_u)
	#}
	ENDIF
	
	IF(CUST.DENOM.debitCrncy != "")  THEN
	#{
		sv_u = urhk_SetOrbOut("debitCrncy|"+CUST.DENOM.debitCrncy)
		print(sv_u)
	#}
	ENDIF
	
	IF(BANCS.STDIN.homeCrncyCode != "")  THEN
	#{
		sv_u = urhk_SetOrbOut("homeCrncy|"+BANCS.STDIN.homeCrncyCode)
		print(sv_u)
	#}
	ENDIF
	
	
	IF(BANCS.STDIN.userId != "")  THEN
	#{
		sv_u = urhk_SetOrbOut("userId|"+BANCS.STDIN.userId)
	#}
	ENDIF
	
	IF(BANCS.STDIN.BODDate!= "")  THEN
	#{
		sv_u = urhk_SetOrbOut("bodDate|"+BANCS.STDIN.BODDate)
	#}
	ENDIF
	
	
	IF(CUST.DENOM.custName != "")  THEN
	#{
		sv_u = urhk_SetOrbOut("custName|"+CUST.DENOM.custName)
		print(sv_u)
	#}
	ENDIF
	IF(CUST.DENOM.idNum != "")  THEN
	#{
		sv_u = urhk_SetOrbOut("idNum|"+CUST.DENOM.idNum)
		print(sv_u)
	#}
	ENDIF
	IF(CUST.DENOM.phoneNo != "")  THEN
	#{
		sv_u = urhk_SetOrbOut("phoneNo|"+CUST.DENOM.phoneNo)
		print(sv_u)
	#}
	ENDIF
	IF(CUST.DENOM.docDetails != "")  THEN
	#{
		sv_u = urhk_SetOrbOut("docDetails|"+CUST.DENOM.docDetails)
		print(sv_u)
	#}
	ENDIF
	
	IF(CUST.DENOM.nationDet != "")  THEN
	#{
		sv_u = urhk_SetOrbOut("nationDet|"+CUST.DENOM.nationDet)
		print(sv_u)
	#}
	ENDIF
	
	
	
	
	ENDOFSCRIPT:
		TRACE OFF
		
END-->
