#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: upmdp017.scr
# event: NA
# description: NA
#=================================================

#--------------------------------------------------------------------------------------------------------------------------------------
# Filename              :       upmdp017.scr
# Author                :       Nithya Murugesan
# Description           :       This is the script for validating user id
#
# Input                 :       usrId
# Output                :
# Called Script         :       None
# Calling Script        :       None
#
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----        ----------         --------------    ----------------        ---------
#  1           20-08-2013         Nithya Murugesan   original Version          1.0
#   2		  21-08-2013		Vijaya V		modifications done for TOL - 366250
#---------------------------------------------------------------------------------------------------------------------------------------
<--START
CHECKSUM="54567a6e15a879125dc3a707c5e62f2e824a3fb0832738468182e913bc2ffe43"
	TRACE ON
	#=====Repository creation=========#
	IF (REPEXISTS("CUST") == 0 ) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	IF (CLASSEXISTS("CUST","UPM") == 0) THEN
	#{
		CREATECLASS("CUST","UPM",5)
	#}
	ENDIF
	#=================================#
	sv_u = urhk_B2K_PrintRepos("BANCS")
	CUST.UPM.contextBankId = BANCS.STDIN.contextBankId
	CUST.UPM.userId = BANCS.STDIN.userId
	CUST.UPM.upmUserId = BANCS.INPUT.userId
	CUST.UPM.mySolId = ""
	
	BANCS.INPARAM.BINDVARS = CUST.UPM.userId
	print(BANCS.INPARAM.BINDVARS)
	sv_s = "roleId,solId| Select role_id,sol_id from TBAADM.UPR WHERE USER_ID = ?SVAR AND DEL_FLG = 'N' "
	sv_d = urhk_dbSelectWithBind(sv_s)
	IF(sv_d != 0) THEN
	#{
		print("Query execution failed")
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		IF((BANCS.OUTPARAM.roleId != "") AND (BANCS.OUTPARAM.solId != "")) THEN
		#{
			CUST.UPM.roleId = BANCS.OUTPARAM.roleId
			print(CUST.UPM.roleId)
			CUST.UPM.mySolId = BANCS.OUTPARAM.solId
			print(CUST.UPM.mySolId)
			sv_u =urhk_SetOrbOut("roleId|"+CUST.UPM.roleId)
			print(sv_u)
		#}
		ENDIF
	#}
	ENDIF
	
	BANCS.INPARAM.BINDVARS = CUST.UPM.upmUserId
	#BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.UPM.contextBankId
	print(BANCS.INPARAM.BINDVARS)
	sv_s = "solId| Select sol_id from TBAADM.UPR WHERE USER_ID = ?SVAR  AND DEL_FLG = 'N' "
	sv_d = urhk_dbSelectWithBind(sv_s)
	IF(sv_d != 0) THEN
	#{
		print("Query execution failed")
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		IF(BANCS.OUTPARAM.solId != "") THEN
		#{
			IF(BANCS.OUTPARAM.solId != CUST.UPM.mySolId) THEN
			#{
				CUST.UPM.solIdFlag = "F"
				#}
			ELSE
				#{
				CUST.UPM.solIdFlag = "S"
			#}
			ENDIF
			print(CUST.UPM.solIdFlag)
			
			sv_u =urhk_SetOrbOut("solId|"+CUST.UPM.solIdFlag)
			print(sv_u)
		#}
		ENDIF
	#}
	ENDIF
	
END-->
