#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: trading003.scr
# event: NA
# description: This script is used to fetch the names of a customer using an acct number
#=================================================
#It is called jsp page to populate the account name fields
<--START
CHECKSUM="ab343af75f517c48fe0f3d383a8a3de0be8aae033f9f9b5b15f9dada9fd831b8"
	
	TRACE ON
	
	print(BANCS.INPUT.foracid)
	
	sv_x=urhk_B2k_PrintRepos("BANCS")
	
	
	#query to select names using CDSC no
	
	sv_s = "FORACID,ACCT_NAME,ACCT_CRNCY_CODE,SOL_ID"
	sv_s = sv_s + " | "
	sv_s = sv_s + " SELECT FORACID,ACCT_NAME,ACCT_CRNCY_CODE,SOL_ID "
	sv_s = sv_s + " FROM tbaadm.gam"
	sv_s = sv_s + " WHERE FORACID= ?SVAR"
	sv_s = sv_s + " AND ROWNUM=1"
	
	
	BANCS.INPARAM.BINDVARS = BANCS.INPUT.foracid
	sv_d = urhk_dbSelectWithBind(sv_s )
	print(sv_d)
	
	IF(sv_d==0) THEN
	#{
		
		#{
		
		sv_v=BANCS.OUTPARAM.FORACID
		sv_w=BANCS.OUTPARAM.ACCT_NAME
		sv_a=BANCS.OUTPARAM.ACCT_CRNCY_CODE
		sv_b=BANCS.OUTPARAM.SOL_ID
		sv_c=sv_v+"!"+sv_w+"!"+sv_a+"!"+sv_b
		
		PRINT(sv_c)
		
		IF(sv_d == 0) THEN
		#{
			sv_z = urhk_SetOrbOut("RESULTS|"+sv_c)
			print(sv_z)
			#}
		ELSE
			#{
			BANCS.OUTPUT.RESULT = ""
			sv_z = urhk_SetOrbOut("RESULTS|"+BANCS.OUTPARAM.RESULT)
			print(sv_z)
		#}
		ENDIF
		
		#}
	ELSE
		#{
		sv_y="NULL"
		sv_z = urhk_SetOrbOut("RESULTS|"+sv_y)
		print(sv_z)
		
	#}
	ENDIF
	
	
	
END-->
