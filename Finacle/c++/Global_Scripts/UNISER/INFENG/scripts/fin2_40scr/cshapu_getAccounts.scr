#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: Fetch_Trading_id.scr
# event: NA
# description: for Fetching from trading System using id number
#=================================================

<--START
CHECKSUM="14ec9ef8ad8f241cada42f4925b1a99102222d3f2683ad5884cc26d5e3fc9946"
	TRACE ON
	IF (REPEXISTS("TRAD") == 0) THEN
	#{
		CREATEREP("TRAD")
	#}
	ENDIF
	
	IF (CLASSEXISTS("TRAD","INTERFACE") == 0) THEN
	#{
		CREATECLASS ("TRAD","INTERFACE",5)
		#}
		
	#}
	ENDIF
	
	
	sv_a = urhk_b2k_printRepos("BANCS")
	
	
	sv_a=BANCS.INPUT.cdsno
	
	#Gsv_a="0000012219610"
	
	TRAD.INTERFACE.RequestParam="getBankAccountsByCdsNo:CDSNO:"+sv_a
	
	TRAD.INTERFACE.ReqType="getBankAccountsByCdsNo"
	
	PRINT(TRAD.INTERFACE.RequestParam)
	TRAD.INTERFACE.Response=""
	
	CALL("TradingOutbound_Request.scr")
	PRINT(TRAD.INTERFACE.Response)
	TRAD.INTERFACE.count=1
	sv_u = urhk_SetOrbOut("recCount|"+TRAD.INTERFACE.count)
	
	
	sv_z = "acctno_" + TRAD.INTERFACE.count
	IF(TRAD.INTERFACE.Response == "")THEN
		TRAD.INTERFACE.Response = " "
	ENDIF	
	sv_z = sv_z + "|" + TRAD.INTERFACE.Response
	
	PRINT(sv_z)
	sv_u = urhk_SetOrbOut(sv_z)
	
	
	ENDOFSCRIPT:
		EXITSCRIPT
		TRACE OFF
END-->
