#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: laupay_FetchInpDtls.scr
# event: NA
# description: NA
#=================================================

<--START
CHECKSUM="513b1ec714c9840a804e8f757f59d464d380e479db288b032ceafe8216aa1bd4"
	TRACE ON
	sv_v = "LAUPAY_REQ.AccountID"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{
		
		CUST.INTERFACE.accountID =("BANCS").("INPUT").(sv_v)
		#}
	ELSE
		#{
		CUST.INTERFACE.accountID = ""
	#}
	ENDIF
	
	
	sv_v = "LAUPAY_REQ.enteredAmt"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{
		
		CUST.INTERFACE.enteredAmt =("BANCS").("INPUT").(sv_v)
		#}
	ELSE
		#{
		CUST.INTERFACE.enteredAmt = ""
	#}
	ENDIF
	
	sv_v = "LAUPAY_REQ.enteredAmtCcy"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{
		
		CUST.INTERFACE.enteredAmtCcy =("BANCS").("INPUT").(sv_v)
		#}
	ELSE
		#{
		CUST.INTERFACE.enteredAmtCcy = ""
	#}
	ENDIF
	
	sv_v = "LAUPAY_REQ.sourceAcctID"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{
		CUST.INTERFACE.sourceAcId=("BANCS").("INPUT").(sv_v)
		#}
	ELSE
		#{
		CUST.INTERFACE.sourceAcId = ""
	#}
	ENDIF
	
	
	sv_v = "LAUPAY_REQ.tranRmks"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{
		CUST.INTERFACE.tranRmks=("BANCS").("INPUT").(sv_v)
		#}
	ELSE
		#{
		CUST.INTERFACE.tranRmks = ""
	#}
	ENDIF
	
	sv_v = "LAUPAY_REQ.rateCode"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{
		
		CUST.INTERFACE.rateCode=("BANCS").("INPUT").(sv_v)
		#}
	ELSE
		#{
		CUST.INTERFACE.rateCode = ""
	#}
	ENDIF
	
	print(CUST.INTERFACE.accountID)
	print(CUST.INTERFACE.enteredAmt)
	print(CUST.INTERFACE.enteredAmtCcy)
	print(CUST.INTERFACE.rateCode)
	#---------------------------------------------------------------------------------
	# Validation for Account ID
	#---------------------------------------------------------------------------------
	IF(CUST.INTERFACE.accountID != "") THEN
	#{
		sv_u = urhk_getAcctDetailsInRepository(CUST.INTERFACE.accountID)
		print(sv_u)
		IF(sv_u != 0) THEN
		#{
			CUST.INTERFACE.successOrFailure = "F"
			CUST.INTERFACE.errorCode = "ERR110"
			GOTO ERROREXIT
			#}
		ELSE
			#{
			CUST.INTERFACE.loanAcctCrncy = BANCS.OUTPARAM.acctCrncyCode
		#}
		ENDIF
		
		#}
	ELSE
		#{
		CUST.INTERFACE.successOrFailure = "F"
		CUST.INTERFACE.errorCode = "ERR106"
		GOTO ERROREXIT
	#}
	ENDIF
	
	#---------------------------------------------------------------------------------
	# Validation for Account ID
	#---------------------------------------------------------------------------------
	IF(CUST.INTERFACE.sourceAcId!= "") THEN
	#{
		sv_u = urhk_valAcctNumber(CUST.INTERFACE.sourceAcId)
		print(sv_u)
		IF(sv_u != 0) THEN
		#{
			CUST.INTERFACE.successOrFailure = "F"
			CUST.INTERFACE.errorCode = "ERR110"
			GOTO ERROREXIT
		#}
		ENDIF
		
		#}
	ELSE
		#{
		CUST.INTERFACE.successOrFailure = "F"
		CUST.INTERFACE.errorCode = "ERR106"
		GOTO ERROREXIT
	#}
	ENDIF
	
	
	
	#---------------------------------------------------------------------------------
	# Validation for Amount
	#---------------------------------------------------------------------------------
	IF(CUST.INTERFACE.enteredAmt != "") THEN
	#{
		
		
		#}
	ELSE
		#{
		CUST.INTERFACE.successOrFailure = "F"
		CUST.INTERFACE.errorCode = "UBP_ERR003"
		GOTO ERROREXIT
	#}
	ENDIF
	
	
	#---------------------------------------------------------------------------------
	# Validation for Currency
	#---------------------------------------------------------------------------------
	IF(CUST.INTERFACE.enteredAmtCcy != "") THEN
	#{
		sv_u = urhk_B2k_valCrncy(CUST.INTERFACE.enteredAmtCcy)
		IF(sv_u != 0) THEN
		#{
			CUST.INTERFACE.successOrFailure = "F"
			CUST.INTERFACE.errorCode = "DENOM_ERR_016"
			GOTO ERROREXIT
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.INTERFACE.successOrFailure = "F"
		CUST.INTERFACE.errorCode = "DENOM_ERR_016"
		GOTO ERROREXIT
	#}
	ENDIF
	
	#---------------------------------------------------------------------------------
	# Validation for Rate Code
	#---------------------------------------------------------------------------------
	IF(CUST.INTERFACE.rateCode != "") THEN
	#{
		sv_y = "67" + "|" + CUST.INTERFACE.rateCode
		sv_r = urhk_B2k_valRefCode(sv_y)
		IF (sv_r != 0) THEN
		#{
			CUST.INTERFACE.successOrFailure = "F"
			CUST.INTERFACE.errorCode = "UBP_ERR035"
			GOTO ERROREXIT
		#}
		ENDIF
		BANCS.INPARAM.ToCurrency   = CUST.INTERFACE.loanAcctCrncy
		BANCS.INPARAM.InputAmount  = CDOUBLE(CUST.INTERFACE.enteredAmt)
		BANCS.INPARAM.FromCurrency = CUST.INTERFACE.enteredAmtCcy
		BANCS.INPARAM.RateCode     = CUST.INTERFACE.rateCode
		BANCS.INPARAM.Rate         = ""
		sv_u = urhk_B2k_ConvertAmount("")
		IF(sv_u == 0) THEN
		#{
			print(BANCS.OUTPARAM.Rate)
			CUST.INTERFACE.conversionRate = BANCS.OUTPARAM.Rate
		#}
		ENDIF
		
		#}
	ELSE
		#{
		CUST.INTERFACE.conversionRate = "1"
	#}
	ENDIF
	
	
	#trace off
END-->
