#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: DDacctCheck.scr
# Type: NA|default
# Description: DDacctCheck.scr
# Author:
# Date:
# Modification Log:
#============================================================

#-------------------------------------------------------------------------------------
#    Name				: DDacctCheck.scr
#    Description     	: for checking for DDA accounts
#    Date            	: 21-08-2013
#    Author          	: Suhasini
#    Called Script		: NA
#    Calling Script		: NA
#    Menu Option		: TM related menus
#    Srl. No		Date			Author			Description.
#    -------		----			------			----------------
#-------------------------------------------------------------------------------------
<--START
CHECKSUM="d2f04873334671ca70fb080d5676cb9a91998d4761b7a9a264737e7fb3ac0aa9"
	TRACE ON
	
	#------------------------------------------------------------------------------
	#       Creating repository and class
	#       Initializing the variables
	#       Getting all the values from CPARAM
	#------------------------------------------------------------------------------
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST","FOAT") == 0) THEN
	#{
		CREATECLASS("CUST","FOAT",5)
	#}
	ENDIF
	
	PRINT(BANCS.INPUT.acctNum)
	IF (FIELDEXISTS(BANCS.INPUT.acctNum)) THEN
	#{
		CUST.FOAT.uAccount = BANCS.INPUT.acctNum
		PRINT(CUST.FOAT.uAccount)
		#}
	ELSE
		#{
		sv_u = urhk_SetOrbOut("F| ")
		PRINT(sv_u)
		EXITSCRIPT
	#}
	ENDIF
	
	
	sv_s = "schm_type|"
	sv_s = sv_s + "SELECT schm_type "
	sv_s = sv_s + "FROM TBAADM.GAM "
	sv_s = sv_s + "WHERE foracid = ?SVAR AND bank_id = ?SVAR "
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.FOAT.uAccount + "|" +  BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	IF (sv_d != 0) THEN
	#{
		sv_u = urhk_SetOrbOut("F| ")
		#}
	ELSE
		#{
		sv_u = urhk_SetOrbOut("S|"+BANCS.OUTPARAM.schm_type)
		PRINT(sv_u)
	#}
	ENDIF
	
	
	TRACE OFF
	EXITSCRIPT
END-->
