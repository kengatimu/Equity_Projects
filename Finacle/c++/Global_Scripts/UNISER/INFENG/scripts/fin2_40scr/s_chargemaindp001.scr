#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: s_chargemaindp001.scr
# Type: NA|default
# Description: s_chargemaindp001.scr
# Author:
# Date:
# Modification Log:
#============================================================

#------------------------------------------------------------------------------------------------
#   Name			: s_chargemaindp001.scr
#   Description		: This file should reside in INFENG/scripts folder.
#   Date			: 17-03-2013
#   Author			: Farhan
#   Input			: NA
#   Output			: NA
#   Called Script	:
#   Calling Script	: getcparamvalue.scr
#   Menu Option		:
#	Srl. No         Date            Author          	Description.
#	-------         ------          ------          	------------
#   1.0             17-03-2013      Farhan		      Original Version
#-----------------------------------------------------------------------------------------------
<--START
CHECKSUM="a4af0fbd2344c9542020e14aad60fc3aeb790d53c7abdfdbfef87d0817da639e"
	#TRACE ON
	
	IF (BANCS.STDIN.onlineOrBatch == "B") THEN
	#{
		EXITSCRIPT
	#}
	ENDIF
	sv_a = REPEXISTS("COLL")
	IF (sv_a == 0) THEN
	#{
		CREATEREP("COLL")
	#}
	ENDIF
	
	sv_a = CLASSEXISTS("COLL", "GLBDATA")
	IF (sv_a == 0) THEN
	#{
		CREATECLASS("COLL", "GLBDATA", 5)
	#}
	ENDIF
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST","TMCHRG") == 0) THEN
	#{
		CREATECLASS("CUST","TMCHRG",5)
	#}
	ENDIF
	
	sv_a = REPEXISTS("CASH")
	IF (sv_a == 0) THEN
	#{
		CREATEREP("CASH")
	#}
	ENDIF
	
	sv_a = CLASSEXISTS("CASH","WITH")
	IF (sv_a == 0) THEN
	#{
		CREATECLASS("CASH","WITH",5)
	#}
	ENDIF
	
	IF (FIELDEXISTS(CASH.WITH.ok)) THEN
	#{
		PRINT(CASH.WITH.ok)
		IF (CASH.WITH.ok == "N") THEN
		#{
			PRINT("its no")
			#}
		ELSE
			#{
			CASH.WITH.ok = "Y"
			PRINT(CASH.WITH.ok)
		#}
		ENDIF
		#}
	ELSE
		#{
		CASH.WITH.ok = "N"
		PRINT(CASH.WITH.ok)
	#}
	ENDIF
	
	#-----------------------------------------------------------
	#Initializing required variables
	#----------------------------------------------------------
	CUST.TMCHRG.errMsg = ""
	CUST.TMCHRG.stepVal = "2"
	CALL("cparamsetup.scr")
	
	#----------------------------------------------------
	
	sv_z = BANCS.STDIN.mySolDesc
	PRINT(sv_z)
	
	sv_u = urhk_SRV_GetVal("tranHdr.tranTypeSubType.tranType")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
		COLL.GLBDATA.tran_type = BANCS.OUTPARAM.srvValue
		sv_t = COLL.GLBDATA.tran_type
		PRINT(COLL.GLBDATA.tran_type)
		#}
	ELSE
		#{
		CUST.TMCHRG.errMsg = "Error: urhk_SRV_GetVal failed for tran type"
		BANCS.OUTPUT.successOrFailure="F"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_u = urhk_SRV_GetVal("tranHdr.addlTranCritPtr.acctId.crncyCode")
	IF (sv_u == 0) THEN
	#{
		COLL.GLBDATA.crncyCode = BANCS.OUTPARAM.srvValue
		sv_c = COLL.GLBDATA.crncyCode
		PRINT(COLL.GLBDATA.crncyCode)
		#}
	ELSE
		#{
		CUST.TMCHRG.errMsg = "Error: urhk_SRV_GetVal failed for Currency Code"
		BANCS.OUTPUT.successOrFailure="F"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_u = urhk_SRV_GetVal("tranHdr.addlTranCritPtr.refCrncy.crncyCode")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
		COLL.GLBDATA.refCrncyCode = BANCS.OUTPARAM.srvValue
		sv_j = COLL.GLBDATA.refCrncyCode
		PRINT(COLL.GLBDATA.refCrncyCode)
		#}
	ELSE
		#{
		CUST.TMCHRG.errMsg = "Error: urhk_SRV_GetVal failed for  Ref Currency Code"
		BANCS.OUTPUT.successOrFailure="F"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_u = urhk_SRV_GetVal("tranHdr.tranTypeSubType.tranSubType")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
		COLL.GLBDATA.sub_type = BANCS.OUTPARAM.srvValue
		sv_s = COLL.GLBDATA.sub_type
		PRINT(COLL.GLBDATA.sub_type)
		#}
	ELSE
		#{
		CUST.TMCHRG.errMsg = "Error: urhk_SRV_GetVal failed for part tran type"
		BANCS.OUTPUT.successOrFailure="F"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_u = urhk_SRV_GetVal("tranHdr.addlTranCritPtr.acctId.acctName")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
		COLL.GLBDATA.acctName = BANCS.OUTPARAM.srvValue
		sv_m = COLL.GLBDATA.acctName
		PRINT(COLL.GLBDATA.acctName)
		#}
	ELSE
		#{
		CUST.TMCHRG.errMsg = "Error: urhk_SRV_GetVal failed for account Name"
		BANCS.OUTPUT.successOrFailure="F"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_u = urhk_SRV_NumOfRecs("tranDtl.partTranDetailLL")
	PRINT(sv_u)
	IF(sv_u == 0) THEN
	#{
		COLL.GLBDATA.noOfRecords = BANCS.OUTPARAM.srvValue
		PRINT(COLL.GLBDATA.noOfRecords)
		#}
	ELSE
		#{
		CUST.TMCHRG.errMsg = "Error:urhk_SRV_NumOfRecs failed"
		BANCS.OUTPUT.successOrFailure="F"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_k = 0
	PRINT(sv_k)
	WHILE(sv_k < COLL.GLBDATA.noOfRecords)
	#{
		sv_v = Format$(sv_k,"%d")
		sv_r = "<rec_"+sv_v+">"
		#-------------------------------------------------------------------------------------------
		# Fetch serial num
		#-------------------------------------------------------------------------------------------
		
		sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".key.serial_num")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			COLL.GLBDATA.serialnum = BANCS.OUTPARAM.srvValue
			PRINT(COLL.GLBDATA.serialnum)
			#}
		ELSE
			#{
			CUST.TMCHRG.errMsg = "Error: urhk_SRV_GetVal failed for serialnum"
			BANCS.OUTPUT.successOrFailure="F"
			GOTO NEXTREC
		#}
		ENDIF
		
		#-------------------------------------------------------------------------------------------
		# Fetch Tran Particulars
		#-------------------------------------------------------------------------------------------
		sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".tranParticulars")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			COLL.GLBDATA.tranParticulars = BANCS.OUTPARAM.srvValue
			sv_p = COLL.GLBDATA.tranParticulars
			PRINT(COLL.GLBDATA.tranParticulars)
			#}
		ELSE
			#{
			CUST.TMCHRG.errMsg = "Error: urhk_SRV_GetVal failed for Tran Particulars"
			BANCS.OUTPUT.successOrFailure="F"
			GOTO NEXTREC
		#}
		ENDIF
		
		#-------------------------------------------------------------------------------------------
		# Fetch Rate code
		#-------------------------------------------------------------------------------------------
		sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".rateCode.refCode")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			COLL.GLBDATA.rateCode = BANCS.OUTPARAM.srvValue
			sv_b = COLL.GLBDATA.rateCode
			PRINT(COLL.GLBDATA.rateCode)
			#}
		ELSE
			#{
			CUST.TMCHRG.errMsg = "Error: urhk_SRV_GetVal failed for Rate Code"
			BANCS.OUTPUT.successOrFailure="F"
			GOTO NEXTREC
		#}
		ENDIF
		
		#-------------------------------------------------------------------------------------------
		# Fetch Rate
		#-------------------------------------------------------------------------------------------
		sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".rate")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			COLL.GLBDATA.rate = BANCS.OUTPARAM.srvValue
			sv_e = COLL.GLBDATA.rate
			PRINT(COLL.GLBDATA.rate)
			#}
		ELSE
			#{
			CUST.TMCHRG.errMsg = "Error: urhk_SRV_GetVal failed for Rate"
			BANCS.OUTPUT.successOrFailure="F"
			GOTO NEXTREC
		#}
		ENDIF
		
		#-------------------------------------------------------------------------------------------
		# Fetch Tran Remarks
		#-------------------------------------------------------------------------------------------
		sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".tranRmks")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			COLL.GLBDATA.tranRmks = BANCS.OUTPARAM.srvValue
			sv_g = COLL.GLBDATA.tranRmks
			PRINT(COLL.GLBDATA.tranRmks)
			#}
		ELSE
			#{
			CUST.TMCHRG.errMsg = "Error: urhk_SRV_GetVal failed for Tran Remarks"
			BANCS.OUTPUT.successOrFailure="F"
			GOTO NEXTREC
		#}
		ENDIF
		
		#-------------------------------------------------------------------------------------------
		# Fetch A/c - foracid
		#-------------------------------------------------------------------------------------------
		sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".acctId.foracid")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			COLL.GLBDATA.account = BANCS.OUTPARAM.srvValue
			sv_n = COLL.GLBDATA.account
			PRINT(COLL.GLBDATA.account)
			#}
		ELSE
			#{
			CUST.TMCHRG.errMsg = "Error: urhk_SRV_GetVal failed for Account Number"
			BANCS.OUTPUT.successOrFailure="F"
			GOTO NEXTREC
		#}
		ENDIF
		
		#-------------------------------------------------------------------------------------------
		# Fetch PartTranType
		#-------------------------------------------------------------------------------------------
		sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".partTranType.code")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			COLL.GLBDATA.partTrantype = BANCS.OUTPARAM.srvValue
			PRINT(COLL.GLBDATA.partTrantype)
			#}
		ELSE
			#{
			CUST.TMCHRG.errMsg = "Error: urhk_SRV_GetVal failed for PartTranType"
			BANCS.OUTPUT.successOrFailure="F"
			GOTO NEXTREC
		#}
		ENDIF
		
		#-------------------------------------------------------------------------------------------
		# Fetch Tran Amount
		#-------------------------------------------------------------------------------------------
		sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".tranAmt")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			COLL.GLBDATA.tran_amt = BANCS.OUTPARAM.srvValue
			sv_o = COLL.GLBDATA.tran_amt
			PRINT(COLL.GLBDATA.tran_amt)
			#}
		ELSE
			#{
			CUST.TMCHRG.errMsg = "Error: urhk_SRV_GetVal failed for Tran Amount"
			BANCS.OUTPUT.successOrFailure="F"
			GOTO NEXTREC
		#}
		ENDIF
		
		#-------------------------------------------------------------------------------------------
		# Fetch Reference Amount
		#-------------------------------------------------------------------------------------------
		sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".refAmt")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			COLL.GLBDATA.refAmt = BANCS.OUTPARAM.srvValue
			sv_h = COLL.GLBDATA.refAmt
			PRINT(COLL.GLBDATA.refAmt)
			#}
		ELSE
			#{
			CUST.TMCHRG.errMsg = "Error: urhk_SRV_GetVal failed for Ref Amount"
			BANCS.OUTPUT.successOrFailure="F"
			GOTO NEXTREC
		#}
		ENDIF
		
		#-------------------------------------------------------------------------------------------
		# Fetch instrmnt type
		#-------------------------------------------------------------------------------------------
		sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".instrmnt.instrmntType")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			COLL.GLBDATA.instrmnt = BANCS.OUTPARAM.srvValue
			PRINT(COLL.GLBDATA.instrmnt)
			#}
		ELSE
			#{
			CUST.TMCHRG.errMsg = "Error: urhk_SRV_GetVal failed for Instrmnt Type"
			BANCS.OUTPUT.successOrFailure="F"
			GOTO NEXTREC
		#}
		ENDIF
		
		#-------------------------------------------------------------------------------------------
		# Fetch instrmnt num
		#-------------------------------------------------------------------------------------------
		
		sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".instrmnt.instrmntNum")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			COLL.GLBDATA.instrmntNum = BANCS.OUTPARAM.srvValue
			sv_y = COLL.GLBDATA.instrmntNum
			PRINT(COLL.GLBDATA.instrmntNum)
			#}
		ELSE
			#{
			CUST.TMCHRG.errMsg = "Error: urhk_SRV_GetVal failed for Instr Number"
			BANCS.OUTPUT.successOrFailure="F"
			GOTO NEXTREC
		#}
		ENDIF
		
		#-------------------------------------------------------------------------------------------
		# Fetch instrmnt date
		#-------------------------------------------------------------------------------------------
		
		sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".instrmnt.instrmntDate")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			COLL.GLBDATA.instrmntDate = BANCS.OUTPARAM.srvValue
			PRINT(COLL.GLBDATA.instrmntDate)
			#}
		ELSE
			#{
			CUST.TMCHRG.errMsg = "Error: urhk_SRV_GetVal failed for Instr Date"
			BANCS.OUTPUT.successOrFailure="F"
			GOTO NEXTREC
		#}
		ENDIF
		
		IF ((COLL.GLBDATA.tran_type == "T") AND (COLL.GLBDATA.partTrantype == "C")) THEN
		#{
			GOTO NEXTREC
		#}
		ENDIF
		
		
		IF (((COLL.GLBDATA.sub_type == "CR") AND (COLL.GLBDATA.partTrantype == "D")) OR ((COLL.GLBDATA.sub_type == "CP") AND (COLL.GLBDATA.partTrantype == "D")))  THEN
		#{
			GOTO NEXTREC
		#}
		ENDIF
		
		IF ((COLL.GLBDATA.account == "") OR (COLL.GLBDATA.tran_amt == "") OR (COLL.GLBDATA.partTrantype == "") ) THEN
		#{
			GOTO NEXTREC
		#}
		ENDIF
		
		
		IF (COLL.GLBDATA.instrmnt != "") THEN
		#{
			IF ((COLL.GLBDATA.instrmntNum == "") OR (COLL.GLBDATA.instrmntDate == "")) THEN
			#{
				GOTO NEXTREC
			#}
			ENDIF
		#}
		ENDIF
		
		PRINT(sv_n)
		PRINT(sv_c)
		PRINT(sv_t)
		PRINT(sv_s)
		PRINT(sv_m)
		PRINT(sv_y)
		PRINT(sv_o)
		sv_o = LEFT$(sv_o,GETPOSITION(sv_o,"|")-1)
		PRINT(sv_p)
		PRINT(sv_z)
		PRINT(sv_b)
		PRINT(sv_e)
		PRINT(sv_g)
		PRINT(sv_h)
		sv_h = LEFT$(sv_h,GETPOSITION(sv_h,"|")-1)
		PRINT(sv_j)
		sv_x = sv_n + "|" + sv_c + "|" + sv_t + "|" + sv_s + "|" + sv_m + "|" + BANCS.STDIN.BODDate + "|" + sv_y + "|" + BANCS.STDIN.userId
		sv_x = sv_x + "|" + sv_o + "|" + sv_p + "|" + sv_z + "|" + sv_b + "|" + sv_e +"|" + sv_g + "|" + sv_h + "|" + sv_j
		#sv_x = sv_x + "|" + sv_o + "|" + sv_p + "|" + sv_z + "|" + sv_b + "|" + sv_e +"|" + sv_g + "|" + sv_h
		PRINT(sv_x)
		
		#---Generating File for mrt
		
		sv_f = TRIM(sv_n) + ".lst"
		PRINT(sv_f)
		
		sv_a = "test -f " + sv_f
		sv_u = SYSTEM(sv_a)
		PRINT(sv_u)
		
		IF (sv_u == 0) THEN
		#{
			sv_u = SYSTEM("rm -f " + sv_f)
		#}
		ENDIF
		
		sv_u = "echo '" + sv_x + "' >> " + sv_f
		PRINT(sv_u)
		
		sv_r = SYSTEM(sv_u)
		PRINT(sv_r)
		
		sv_x = urhk_getFileLocation("COM|ofti.com")
		PRINT(sv_x)
		
		sv_l = BANCS.OUTPARAM.fileLocation
		PRINT(sv_l)
		
		sv_t    =   sv_l + "ofti.com"
		PRINT(sv_t)
		
		sv_t = sv_t + " " + sv_f
		PRINT(sv_t)
		
		sv_u = SYSTEM(sv_t)
		PRINT(sv_u)
		
		NEXTREC:
			sv_k = sv_k + 1
			PRINT(sv_k)
		#}
		DO
		
		
	ENDOFSCRIPT:
		
		#TRACE OFF
		
		EXITSCRIPT
END-->
