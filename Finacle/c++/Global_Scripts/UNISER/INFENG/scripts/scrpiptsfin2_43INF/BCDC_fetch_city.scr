  <--START
	TRACE ON
	#BCDC_fetch_setid.scr
	#City code fetch script
  
	sv_s =""
	sv_s = sv_s + " CITY_ID | select count(distinct(city_code)) from custom.SOL_CITY_BCDC where sol_id in ('"+BANCS.STDIN.mySolId+"','"+BANCS.INPUT.CUSTSOLID+"') "
	print(sv_s)
	sv_u = urhk_dbselectwithBind(sv_s)
	print(sv_u)
	IF (sv_u == 0) THEN
	#{
			BANCS.INPUT.CITY_ID=BANCS.OUTPARAM.CITY_ID
			
		else

			BANCS.INPUT.CITY_ID=0

	#}
	ENDIF

	     print(BANCS.INPUT.CITY_ID)
	
TRACE OFF
END-->	
