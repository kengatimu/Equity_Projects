#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: checkRecCount.scr
# Type: NA|default
# Description: This script is used to get the number of records in the 'Fee details' page, based on which the fields will be disabled.
# Author:
# Date:
# Modification Log:
#============================================================

<--START
CHECKSUM="a6e3f87502093f5b2dca55cc836dff4a64fdcd8b628fe74ce5c2ccb752928f13"
	TRACE ON
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST","LADISB") == 0) THEN
	#{
		CREATECLASS("CUST","LADISB",5)
	#}
	ENDIF
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	sv_u = urhk_b2k_printrepos("BANCS")
	PRINT(BANCS.INPUT.srlNofrmPage)
	
	sv_l = STRLEN(BANCS.INPUT.srlNofrmPage)
	sv_m = MID$(BANCS.INPUT.srlNofrmPage,(sv_l-1),sv_l)
	PRINT(sv_m)
	
	sv_m = 0
	sv_g = 0
	sv_v = BANCS.INPUT.srlNofrmPage
	
	WHILE (sv_g == 0)
	#{
		sv_p = GETPOSITION(sv_v,",")
		PRINT(sv_p)
		IF(sv_p == 0) THEN
		#{
			sv_g = 1
			#}
		ELSE
			#{
			sv_l = STRLEN(BANCS.INPUT.srlNofrmPage)
			sv_v = MID$(sv_v,sv_p,sv_l)
			sv_g = 0
			sv_m = sv_m+1
		#}
		ENDIF
		PRINT(sv_v)
		PRINT(sv_m)
		PRINT(sv_g)
	#}
	DO
	
	sv_m = sv_m +1
	CUST.LADISB.recCount = sv_m
	
	sv_u = urhk_SetOrbOut("totalCount|"+CUST.LADISB.recCount)
	PRINT(sv_u)
	#-------------------------------------------------------------------------------
	# Deleting custom repositories and classes created
	#-------------------------------------------------------------------------------
	IF(CLASSEXISTS("CUST","LADISB") == 1) THEN
	#{
		DELETECLASS("CUST","LADISB")
	#}
	ENDIF
	
	IF(REPEXISTS("CUST") == 1) THEN
	#{
		DELETEREP("CUST")
	#}
	ENDIF
	
	TRACE OFF
END-->
