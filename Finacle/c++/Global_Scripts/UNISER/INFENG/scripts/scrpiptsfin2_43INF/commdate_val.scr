#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: commdate_val.scr
# Type: NA|default
# Description: This script is for validating the commission start date
# Author: shivakrishna.d
# Date: 7/4/18 3:15 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="394c51fda9fd2d601f841d878d05699bd3aca5baa9f5a933e246bb1bfc87d436"
	# Script body goes here
	
	# Script body goes here
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	###########################################################################################
	# Check whether DATA Calss exists else create it
	###########################################################################################
	sv_r = CLASSEXISTS("CUST","DATA")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","DATA",5)
	#}
	ENDIF
	
	###########################################################################################
	#Initialize the variables
	###########################################################################################
	CUST.DATA.datediff = "0"
	CUST.DATA.errorFlg = "N"
	CUST.DATA.errorMsg = " "
	CUST.DATA.commdate = " "
	CUST.DATA.commdate1 = " "
	CUST.DATA.BODdate = " "
	CUST.DATA.BODdate1 = " "
	IF(FIELDEXISTS(BANCS.INPUT.commdate))THEN
	#{
		IF(TRIM(BANCS.INPUT.commdate) != "")THEN
		#{
			CUST.DATA.commdate = TOUPPER(BANCS.INPUT.commdate)
			PRINT(CUST.DATA.commdate)
		#}
		ENDIF
	#}
	ENDIF
	CUST.DATA.BODdate = MID$(BANCS.STDIN.BODDate,0,10)
	
	sv_s = "commdate1|select to_char((to_date(?SVAR,'DD-MM-YY')),'DD-MM-YYYY') from dual"
	BANCS.INPARAM.BINDVARS = CUST.DATA.commdate
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	IF(sv_d == 0) THEN
	#{
		CUST.DATA.commdate1 = BANCS.OUTPARAM.commdate1
	#}
	ENDIF
	
	# BOD date
	sv_s = "BODDate1|select to_char((to_date(?SVAR,'DD-MM-YY')),'DD-MM-YYYY') from dual"
	BANCS.INPARAM.BINDVARS = CUST.DATA.BODdate
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	IF(sv_d == 0) THEN
	#{
		CUST.DATA.BODDate1 = BANCS.OUTPARAM.BODDate1
	#}
	ENDIF
	BANCS.INPARAM.date1 = CUST.DATA.commdate1
	BANCS.INPARAM.date2 = CUST.DATA.BODDate1
	#
	sv_a = urhk_B2k_dateDiff("")
	PRINT(sv_a)
	IF(sv_a == 0) THEN
	#{
		
		CUST.DATA.datediff=BANCS.OUTPARAM.dateDiff
	#}
	ENDIF
	
	IF(CUST.DATA.datediff < 0) THEN
	#{
		CUST.DATA.errorFlg = "Y"
		CUST.DATA.errorMsg = CUST.DATA.errorMsg + "  " + "Commission START Date should not be less than BOD"
		GOTO ENDOFSCRIPT
		
	#}
	ENDIF
	
	ENDOFSCRIPT:
		sv_u = urhk_SetOrbOut("errFlg|"+CUST.DATA.errorFlg)
		sv_u = urhk_SetOrbOut("errMsg|"+CUST.DATA.errorMsg)
		sv_u = urhk_SetOrbOut("commdate|"+CUST.DATA.commdate)
		TRACE OFF
END-->
