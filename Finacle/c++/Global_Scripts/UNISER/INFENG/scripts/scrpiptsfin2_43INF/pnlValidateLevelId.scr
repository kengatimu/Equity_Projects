#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: pnlValidateLevelId.scr
# Type: NA|default
# Description: Validation Script
# Author: Anshul_Singh04
# Date: 12/3/18 4:56 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="e6e553c99424d859125957ca5c039fbf621d957a6950e9dec1fcadfd307ac203"
	# Script body goes here
	TRACE ON
	sv_a = REPEXISTS("EQT")
	IF (sv_a == 0) THEN
	#{
		CREATEREP("EQT")
	#}
	ENDIF
	sv_a = CLASSEXISTS("EQT", "ODP")
	IF (sv_a == 0 ) THEN
	#{
		CREATECLASS("EQT", "ODP", 5)
	#}
	ENDIF
	
	EQT.ODP.errorMsg =" "
	EQT.ODP.errorFlg = "N"
	
	IF(FIELDEXISTS(BANCS.INPUT.levelType))THEN
	#{
		
		IF(TRIM(BANCS.INPUT.levelType) != "")THEN
		#{
			EQT.ODP.levelType = TOUPPER(BANCS.INPUT.levelType)
			PRINT(EQT.ODP.levelType)
			#}
		ELSE
			#{
			EQT.ODP.errorFlg ="Y"
			EQT.ODP.errorMsg ="Level Type not passed!"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		#}
	ELSE
		#{
		EQT.ODP.errorFlg ="Y"
		EQT.ODP.errorMsg ="Level Type not passed!"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.levelId))THEN
	#{
		
		IF(TRIM(BANCS.INPUT.levelId) != "")THEN
		#{
			EQT.ODP.levelId = TOUPPER(BANCS.INPUT.levelId)
			PRINT(EQT.ODP.levelId)
			#}
		ELSE
			#{
			EQT.ODP.errorFlg ="Y"
			EQT.ODP.errorMsg ="Level Id not passed!"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		#}
	ELSE
		#{
		EQT.ODP.errorFlg ="Y"
		EQT.ODP.errorMsg ="Level Id not passed!"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	IF(EQT.ODP.levelType == "A")THEN
	#{
		sv_s = "recCount |select count(*) from tbaadm.gam where foracid = ?SVAR and bank_id = ?SVAR and del_flg ='N' and entity_cre_flg ='Y' and schm_type = 'ODA'"
		BANCS.INPARAM.BINDVARS = EQT.ODP.levelId  + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d != 0)THEN
		#{
			EQT.ODP.errorFlg ="Y"
			EQT.ODP.errorMsg ="Error in Database Operation!!"
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			EQT.ODP.recCount =  BANCS.OUTPARAM.recCount
			PRINT(EQT.ODP.recCount)
			
			IF(EQT.ODP.recCount == "0")THEN
			#{
				EQT.ODP.errorFlg ="Y"
				EQT.ODP.errorMsg ="Invalid Account Id: "+EQT.ODP.levelId + " or not an Overdraft Account."
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	IF(EQT.ODP.levelType == "C")THEN
	#{
		sv_s = "recCount|select count(*) from tbaadm.cmg where cif_id = ?SVAR and bank_id = ?SVAR and del_flg ='N' and entity_cre_flg ='Y'"
		BANCS.INPARAM.BINDVARS = EQT.ODP.levelId  + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d != 0)THEN
		#{
			EQT.ODP.errorFlg ="Y"
			EQT.ODP.errorMsg ="Error in Database Operation!!"
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			EQT.ODP.recCount =  BANCS.OUTPARAM.recCount
			PRINT(EQT.ODP.recCount)
			
			IF(EQT.ODP.recCount == "0")THEN
			#{
				EQT.ODP.errorFlg ="Y"
				EQT.ODP.errorMsg ="Invalid CIF Id: "+EQT.ODP.levelId
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	IF(EQT.ODP.levelType == "S")THEN
	#{
		sv_s = "recCount|select count(*) from tbaadm.gsp where schm_code = ?SVAR and schm_type = 'ODA' and bank_id = ?SVAR and del_flg ='N'"
		BANCS.INPARAM.BINDVARS = EQT.ODP.levelId  + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d != 0)THEN
		#{
			EQT.ODP.errorFlg ="Y"
			EQT.ODP.errorMsg ="Error in Database Operation!!"
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			EQT.ODP.recCount =  BANCS.OUTPARAM.recCount
			PRINT(EQT.ODP.recCount)
			
			IF(EQT.ODP.recCount == "0")THEN
			#{
				EQT.ODP.errorFlg ="Y"
				EQT.ODP.errorMsg ="Invalid Scheme Code: "+EQT.ODP.levelId + " or not an ODA Scheme."
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	ENDOFSCRIPT:
		sv_u = urhk_SetOrbOut("errFlag|" + EQT.ODP.errorFlg)
		sv_u = urhk_SetOrbOut("errMsg|" + EQT.ODP.errorMsg)
		
END-->

