##########################################################################################
#    Name                       :   b_purpose.scr
#    Date                       :   14-05-2013
#    Description                :   This script provides listing of Modules
#    Input                      :   NA
#    Output                     :   NA
#    Author                     :   jane ndirangu
#    Called From                :  limxfrmain_cglink.js
#    Calling Script             :   NA
#    Reference		       	:
#    Modification History       :
#    Menu			 :HLIMXFR
# <Serial No>     <Date>      <Author>        	   <Description>
#   0001        14-05-2013  jane ndirangu      Initial Draft
##########################################################################################

<--START
CHECKSUM="2be1093555cbdf9dc86d4eb73a2281ee41e447b6203d5baad079875592ad899f"
	TRACE ON
	
	###########################################################################################
	# STEP : STEP 0 BEGIN
	# CREATE THE NECESSARY REPOSITORY AND CLASSES
	###########################################################################################
	
	IF(REPEXISTS("CUST") == 0 ) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF(CLASSEXISTS("CUST","DATA") == 0) THEN
	#{
		CREATECLASS("CUST","DATA",5)
	#}
	ENDIF
	CUST.DATA.recordCount	= ""
	BANCS.INPARAM.BINDVARS = ""
	
	CUST.DATA.REFCODE=BANCS.INPUT.REFCODE
	print(CUST.DATA.REFCODE)
	
	######################################################################################
	lv_s = "recordCount|"
	lv_s = lv_s + " SELECT count(*) "
	lv_s = lv_s + " FROM TBAADM.RCT "
	lv_s = lv_s + " WHERE BANK_ID = ?SVAR AND DEL_FLG='N'"
	lv_s = lv_s + " AND REF_REC_TYPE='41'"
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
	print(BANCS.INPARAM.BINDVARS)
	
	
	lv_d = urhk_dbSelectWithBind(lv_s)
	
	IF (lv_d != 0) THEN
	#{
		CUST.DATA.recordCount = "0"
		#}
	ELSE
		#{
		CUST.DATA.recordCount = BANCS.OUTPARAM.recordCount
	#}
	ENDIF
	CUST.DATA.recordCount = "recordCount|"+CUST.DATA.recordCount
	lv_u = urhk_SetOrbOut(CUST.DATA.recordCount)
	
	######################################################################################
	lv_s = "REFCODE,REFDESC|"
	lv_s = lv_s + " SELECT REF_CODE,REF_DESC "
	lv_s = lv_s + " FROM TBAADM.RCT "
	lv_s = lv_s + " WHERE BANK_ID = ?SVAR AND DEL_FLG='N'"
	lv_s = lv_s + " AND REF_REC_TYPE='41'"
	print(lv_s)
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
	print(BANCS.INPARAM.BINDVARS)
	print(lv_s)
	
	
	lv_u = urhk_dbCursorOpenWithBind(lv_s)
	
	
	IF ( lv_u == 0 ) THEN
	#{
		lv_m = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		
		lv_c = urhk_dbCursorFetch(lv_m)
		lv_z = 0
		WHILE(lv_c == 0)
		#{
			#########################################################################
			#Checking for Null Values
			#########################################################################
			
			IF(BANCS.OUTPARAM.REFCODE == "") THEN
			#{
				BANCS.OUTPARAM.REFCODE = " "
				#}
			ELSE
				#{
				BANCS.OUTPARAM.REFCODE = TRIM(BANCS.OUTPARAM.REFCODE)
			#}
			ENDIF
			
			IF(BANCS.OUTPARAM.REFDESC == "") THEN
			#{
				BANCS.OUTPARAM.REFDESC = " "
				#}
			ELSE
				#{
				BANCS.OUTPARAM.REFDESC = TRIM(BANCS.OUTPARAM.REFDESC)
			#}
			ENDIF
			
			lv_z = lv_z + 1
			print(BANCS.OUTPARAM.REFCODE)
			print(BANCS.OUTPARAM.REFDESC)
			lv_o ="REFCODE_"+FORMAT$(lv_z,"%d")+"|"+BANCS.OUTPARAM.REFCODE
			lv_q ="REFDESC_"+FORMAT$(lv_z,"%d")+"|"+BANCS.OUTPARAM.REFDESC
			
			lv_u = urhk_SetOrbOut(lv_o)
			
			
			lv_u = urhk_SetOrbOut(lv_q)
			
			lv_c = urhk_dbCursorFetch(lv_m)
		#}
		DO
		lv_u = urhk_dbCursorClose(lv_m)
	#}
	ENDIF
	
	
	
	
	
	EXITSCRIPT
	TRACE OFF
	################################################################################
	# End of script
	################################################################################
END-->
