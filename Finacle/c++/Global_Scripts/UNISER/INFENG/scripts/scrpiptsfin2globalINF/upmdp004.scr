#----------------------------------------------------------------------------------------------------------
# Filename              :       upmdp004.scr
# Author                :       Hari Mohan Verma
# Description           :       This is the script for getting data from DB and setting it to details page
#								in verify mode
# Input                 :       func Code,user id
# Output                :       Details page field values
# Called Script         :       SRV_FetchUsrProfileForCancel_post_process_data.scr,
#                               SRV_FetchUsrProfileForVerify_post_process_data.scr
# Calling Script        :       NA
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----        ----------         --------------    ----------------        ---------
#  1           25-06-2013         Hari MOhan Verma   original Version          1.0
#-----------------------------------------------------------------------------------------------------------

<--START
CHECKSUM="e4e3d6a9dbb2cc8f7809289014c78af49c7bcfa5a2cb2dd3a7ae0631f3c1810c"
	TRACE ON
	
	#-------------------------------------------------------------
	# Repository creation
	#-------------------------------------------------------------
	IF (REPEXISTS("CUST") == 0 ) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	IF (CLASSEXISTS("CUST","UPM") == 0) THEN
	#{
		CREATECLASS("CUST","UPM",5)
	#}
	ENDIF
	
	sv_a = urhk_SRV_GetVal("usrGenDtl.usrId")
	CUST.UPM.addUserId = BANCS.OUTPARAM.srvValue
	print(CUST.UPM.addUserId)
	
	#-------------------------------------------------------------
	# Getting Data From Table for VerIFy
	#-------------------------------------------------------------
	BANCS.INPARAM.BINDVARS = CUST.UPM.addUserId
	sv_s = "count| Select count(*) from CUSTOM.C_UAD_MOD WHERE USER_ID = ?SVAR "
	sv_d = urhk_dbSelectWithBind(sv_s)
	IF(sv_d != 0) THEN
	#{
		print("Query execution failed")
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	IF(BANCS.OUTPARAM.count != 0) THEN
	#{
		BANCS.INPARAM.BINDVARS= CUST.UPM.addUserId
		sv_s = "tempRoleId,expiryDate| "
		sv_s = sv_s + "SELECT TEMPORARY_ROLE_ID,TO_CHAR((TO_DATE(EXPIRY_DATE,'DD-MON-YY HH:MI:SS')), 'DD-MM-YYYY') "
		sv_s = sv_s + "FROM CUSTOM.C_UAD_MOD "
		sv_s = sv_s + "WHERE USER_ID =?SVAR "
		print(sv_s)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		IF (sv_d != 0) THEN
		#{
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		#}
	ELSE
		#{
		BANCS.INPARAM.BINDVARS= CUST.UPM.addUserId
		sv_s = "tempRoleId,expiryDate| "
		sv_s = sv_s + "SELECT TEMPORARY_ROLE_ID,TO_CHAR((TO_DATE(EXPIRY_DATE,'DD-MON-YY HH:MI:SS')), 'DD-MM-YYYY') "
		sv_s = sv_s + "FROM CUSTOM.C_UAD "
		sv_s = sv_s + "WHERE USER_ID =?SVAR"
		sv_s = sv_s + " AND ENTITY_CRE_FLG = 'N' "
		sv_s = sv_s + " AND DEL_FLG = 'N' "
		print(sv_s)
		sv_d = urhk_dbSelectWithBind(sv_s)
		IF (sv_d != 0) THEN
		#{
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
	#}
	ENDIF
	
	CUST.UPM.tempRoleId = BANCS.OUTPARAM.tempRoleId
	CUST.UPM.expiryDate = BANCS.OUTPARAM.expiryDate
	print(CUST.UPM.tempRoleId)
	print(CUST.UPM.expiryDate)
	
	sv_d = ""
	sv_d = sv_d + "~upmgendet|"
	sv_d = sv_d + CUST.UPM.tempRoleId
	sv_d = sv_d + "|"
	sv_d = sv_d +  CUST.UPM.expiryDate
	sv_d = sv_d + "|"
	
	ONS.CUSTOM.custDataOut = ONS.CUSTOM.custDataOut + "|" + sv_d
	print(ONS.CUSTOM.custDataOut)
	
	ENDOFSCRIPT:
		
		#TRACE OFF
END-->
