#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: catod_maxcontract_update.scr
# Type: NA|default
# Description: catod_maxcontract_update.scr
# Author:
# Date:
# Modification Log:
#============================================================

#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: catod_maxcontract_update.scr
# event: NA
# description: NA
#=================================================
<--START
CHECKSUM="cd704ff823d2590a53093e945c0e1c1135de309ba1a7212b624f59b5b4f47b88"
	
	TRACE ON
	
	#----------------------------------------------------------------------
	# Create the necessary repositories and Classes
	#----------------------------------------------------------------------
	IF(REPEXISTS("CUST") == 0 ) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF(CLASSEXISTS("CUST","CATOD") == 0) THEN
	#{
		CREATECLASS("CUST","CATOD",5)
	#}
	ENDIF
	
	sv_s = "acctNum,cntrctAmt,maxCntrtAmt|"
	sv_s = sv_s + "SELECT ACCT_NUM,CONTRACT_AMT,MAX_CNTRT_AMT FROM CUSTOM.CATOD_MASTER_TABLE WHERE ENTITY_CRE_FLG='Y' AND DEL_FLG = 'N'"
	sv_s = sv_s + " AND CONTRACT_AMT >=MAX_CNTRT_AMT AND BANK_ID = ?SVAR"
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
	sv_d = urhk_dbCursorOpenWithBind(sv_s)
	PRINT(sv_d)
	sv_n = BANCS.OUTPARAM.DB_CURSOR_NUMBER
	PRINT(sv_n)
	sv_d = urhk_dbCursorFetch(sv_n)
	PRINT(sv_d)
	sv_c = 0
	WHILE(sv_d == 0)
	#{
		
		sv_c = sv_c + 1
		PRINT(sv_c)
		CUST.CATOD.acctNum = BANCS.OUTPARAM.acctNum
		PRINT(CUST.CATOD.acctNum)
		CUST.CATOD.cntrctAmt = BANCS.OUTPARAM.cntrctAmt
		PRINT(CUST.CATOD.cntrctAmt)
		CUST.CATOD.maxCntrtAmt = BANCS.OUTPARAM.maxCntrtAmt
		PRINT(CUST.CATOD.maxCntrtAmt)
		
		#-------------------------------------------------------------------------
		# CALCULATING THE MAX CONTRACT AMOUNT
		# MAX_CNTRT_AMT  will be the sum of CONTRACT_AMT and the following charges
		# 2% of CONTRACT_AMT or 200 KES (whichever is higher)  -- Contract Fees
		# 10% of CONTRACT_AMT or 200 KES (whichever is higher) . Availment fees
		# 50 KES . Withdrawal charge
		#---------------------------------------------------------------------------
		lv_t = CDOUBLE(CUST.CATOD.cntrctAmt) * 2/100
		lv_u = CDOUBLE(CUST.CATOD.cntrctAmt) * 10/100
		lv_w = 50
		
		IF ( lv_t < 200 ) THEN
		#{
			lv_t = 200
		#}
		ENDIF
		IF ( lv_u < 200 ) THEN
		#{
			lv_u = 200
		#}
		ENDIF
		PRINT(lv_t)
		PRINT(lv_u)
		
		CUST.CATOD.maxCntrctAmt = CDOUBLE(CUST.CATOD.cntrctAmt) + lv_t + lv_u + lv_w
		PRINT(CUST.CATOD.maxCntrctAmt)
		BANCS.INPARAM.BINDVARS = CUST.CATOD.maxCntrctAmt + "|" + BANCS.STDIN.contextBankId + "|" + CUST.CATOD.acctNum
		sv_s = "UPDATE CUSTOM.CATOD_MASTER_TABLE SET MAX_CNTRT_AMT = ?SVAR WHERE BANK_ID=?SVAR AND ACCT_NUM = ?SVAR"
		sv_d = urhk_dbSqlWithBind(sv_s)
		PRINT(sv_d)
		
		sv_d = urhk_dbCursorFetch(sv_n)
		PRINT(sv_d)
	#}
	DO
	PRINT(sv_c)
	sv_d = urhk_dbCursorClose(sv_n)
	PRINT(sv_d)
	
	TRACE OFF
END-->
