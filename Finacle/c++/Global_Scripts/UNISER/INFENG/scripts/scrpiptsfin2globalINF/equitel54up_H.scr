<--START
CHECKSUM="15b20c09cafec961b24d83f662dbd7d72d3fd238ea53237ab640f8f9a5a197df"
	TRACE ON
	sv_k=""
	sv_y=""
	sv_w=""
	
	sv_q="foracid,tran_amt |"
	sv_q = sv_q + " select  FORACID,TRAN_AMT "
	sv_q = sv_q + " from CUSTOM.INT_CREDIT where CLASSFROM='H' "
	print(sv_q)
	
	sv_e = urhk_dbCursorOpenWithBind(sv_q)
	
	IF(sv_e==0) THEN
	#{
		sv_m = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		sv_q = urhk_dbCursorFetch(sv_m)
		print(sv_q)
		IF(sv_q == 1) THEN
		#{
			sv_x = urhk_dbCursorClose(sv_m)
			print(sv_x)
			exitscript
		#}
		ENDIF
		print(sv_q)
		WHILE(sv_q==0)
		#{
			##################
			# Get account balance as on BOD date.
			
			#-----------------------------------------------------------------------------------------------
			# CREATING NEW REPOSITORY AND CLASS
			#-----------------------------------------------------------------------------------------------
			
			IF (REPEXISTS("CUST") == 0) THEN
			#{
				CREATEREP("CUST")
			#}
			ENDIF
			
			IF (CLASSEXISTS("CUST", "DATA") == 0) THEN
			#{
				CREATECLASS("CUST", "DATA", 5)
			#}
			ENDIF
			
			CUST.DATA.foracid=BANCS.OUTPARAM.foracid
			print(CUST.DATA.foracid)
			CUST.DATA.tran_amt=BANCS.OUTPARAM.tran_amt
			print(CUST.DATA.tran_amt)
			
			
			
			##################
			sv_h = mid$(BANCS.STDIN.BODDate,0,10)
			print(sv_h)
			sv_s = "getboddate|select to_char((to_date(?SVAR,'DD-MM-YYYY')),'DD-MON-YYYY') from dual"
			BANCS.INPARAM.BINDVARS =  sv_h
			sv_d = urhk_dbSelectWithBind(sv_s)
			print(sv_d)
			IF (sv_d =="0") THEN
			#{
				print(BANCS.OUTPARAM.getboddate)
				
				
				
				sv_s = "update crmuser.phoneemail set PHONENO=?SVAR,PHONENOLOCALCODE=?SVAR where PHONEOREMAIL='PHONE'  "
				sv_s = sv_s + " AND PHONEEMAILTYPE='COMMPH2' AND bank_id='54' AND orgkey=?SVAR  "
				print(sv_s)
				BANCS.INPARAM.BINDVARS = CUST.DATA.tran_amt + "|" + CUST.DATA.tran_amt + "|" + CUST.DATA.foracid
				print(BANCS.INPARAM.BINDVARS)
				
				sv_d = urhk_dbSQLWithBind(sv_s)
				IF (sv_d == 0) THEN
				#{
					sv_c = urhk_dbSQLWithBind("commit")
					print(sv_c)
				#}
				ENDIF
				
				print(CUST.DATA.foracid)
				print(CUST.DATA.tran_amt)
				
				sv_s = "UPDATE CUSTOM.INT_CREDIT "
				sv_s = sv_s + " SET CLASSFROM = 'P'  "
				sv_s = sv_s + " WHERE FORACID = ?SVAR "
				sv_s = sv_s + " AND CLASSFROM = 'H' AND TRAN_AMT=?SVAR  "
				print(sv_s)
				BANCS.INPARAM.BINDVARS = CUST.DATA.foracid + "|" + CUST.DATA.tran_amt
				print(BANCS.INPARAM.BINDVARS)
				
				sv_d = urhk_dbSQLWithBind(sv_s)
				IF (sv_d == 0) THEN
				#{
					sv_c = urhk_dbSQLWithBind("commit")
					print(sv_c)
				#}
				ENDIF
				
			#}
			ENDIF
			
			sv_q = urhk_dbCursorFetch(sv_m)
			print(sv_q)
		#}
		DO
		TRACE OFF
		EXITSCRIPT
		
	END-->
	
	
