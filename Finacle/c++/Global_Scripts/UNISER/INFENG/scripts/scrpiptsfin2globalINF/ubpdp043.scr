#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: ubpdp043.scr
# event: NA
# description: NA
#=================================================

<--START
CHECKSUM="7dffca2ababff802651c89d9c8da5402fb6f828fe14113be207bb87863ea884b"
	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp043.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called from ubp_det_link.js for Pay ref Code-->Ref code desc.
	#
	# Called Script         :
	# Calling Function      :		function fnPayForRefCode
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           16-04-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	
	
	# Script body goes here
	TRACE ON
	#========================================================
	# Check whether CUST Repository exists else create it
	#========================================================
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	#========================================================
	# Check whether UBP Class exists else create it
	#========================================================
	sv_r = CLASSEXISTS("CUST","UBP")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","UBP",5)
	#}
	ENDIF
	CUST.UBP.refCode=""
	CUST.UBP.refDesc=" "
	CUST.UBP.errorFlg="N"
	CUST.UBP.errorMsg=" "
	#========================================================
	# Check whether UBP Class exists else create it
	#========================================================
	IF(FIELDEXISTS(BANCS.INPUT.freetext1))THEN
	#{
		IF(TRIM(BANCS.INPUT.freetext1) != "")THEN
		#{
			CUST.UBP.freetext1 = TOUPPER(TRIM(BANCS.INPUT.freetext1))
			PRINT(CUST.UBP.freetext1)
			#========================================================
			# Fetching the Record count
			#========================================================
			BANCS.INPARAM.BINDVARS = "N|"+BANCS.STDIN.contextBankId+"|C0|"+CUST.UBP.freetext1
			sv_s = "recCount|"
			sv_s = sv_s + " SELECT count(1)"
			sv_s = sv_s + " FROM TBAADM.RCT "
			sv_s = sv_s + " WHERE NVL(DEL_FLG, 'N') = ?SVAR"
			sv_s = sv_s + " AND BANK_ID = ?SVAR"
			sv_s = sv_s + " AND REF_REC_TYPE = ?SVAR"
			sv_s = sv_s + " AND REF_CODE = ?SVAR"
			sv_d = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_d)
			IF(sv_d ==0)THEN
			#{
				CUST.UBP.recCount = BANCS.OUTPARAM.recCount
				IF(CUST.UBP.recCount==0)THEN
				#{
					CUST.UBP.errorFlg = "Y"
					#CUST.UBP.errorMsg = "Enter a Valid Payment Id."
					
					CUST.UBP.errorCode = "UBP_ERR036"
					CALL("ubpdp044.scr")
					CUST.UBP.errorMsg =CUST.UBP.errorDesc
					CUST.UBP.errorFocus="freeText1"
					GOTO ENDOFSCRIPT
					#}
				ELSE
					#{
					BANCS.INPARAM.BINDVARS = "N|"+BANCS.STDIN.contextBankId+"|C0|"+CUST.UBP.freetext1
					sv_s = "refDesc|"
					sv_s = sv_s + " SELECT REF_DESC"
					sv_s = sv_s + " FROM TBAADM.RCT "
					sv_s = sv_s + " WHERE NVL(DEL_FLG, 'N') = ?SVAR"
					sv_s = sv_s + " AND BANK_ID = ?SVAR"
					sv_s = sv_s + " AND REF_REC_TYPE = ?SVAR"
					sv_s = sv_s + " AND REF_CODE = ?SVAR"
					sv_d = urhk_dbSelectWithBind(sv_s)
					PRINT(sv_d)
					IF(sv_d ==0)THEN
					#{
						CUST.UBP.refDesc = BANCS.OUTPARAM.refDesc
						PRINT(CUST.UBP.refDesc)
						#}
					ELSE
						#{
						CUST.UBP.errorFlg = "Y"
						#CUST.UBP.errorMsg = "Enter a Valid Payment Id."
						CUST.UBP.errorCode = "UBP_ERR036"
						CALL("ubpdp044.scr")
						CUST.UBP.errorMsg =CUST.UBP.errorDesc
						CUST.UBP.errorFocus="freeText1"
						GOTO ENDOFSCRIPT
					#}
					ENDIF
				#}
				ENDIF
			#}
			ENDIF
			#}
		ELSE
			#{
			CUST.UBP.freetext1=""
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.freetext1=""
	#}
	ENDIF
	ENDOFSCRIPT:
		sv_s = "errorFlg|" + CUST.UBP.errorFlg
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "errorMsg|" + CUST.UBP.errorMsg
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "refDesc|" + CUST.UBP.refDesc
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
END-->
