#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: trading006.scr
# event: NA
# description: Test Script for validating an account number
#=================================================

<--START
CHECKSUM="4931e20fc403de975b12623d55a6f59f330edf76a748d97e71a7e2d63ddd7489"
	TRACE ON
	
	#------------------------------------------------------------------------------
	#       Creating repository and class
	#       Initializing the variables
	#------------------------------------------------------------------------------
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST", "TRADE") == 0) THEN
	#{
		CREATECLASS("CUST", "TRADE", 5)
	#}
	ENDIF
	
	
	
	PRINT(BANCS.INPUT.operAcctNo)
	sv_s ="forAcct|SELECT foracid FROM TBAADM.GAM "
	sv_s = sv_s + "WHERE foracid = ?SVAR "
	sv_s = sv_s + "AND GAM.bank_id = ?SVAR "
	
	BANCS.INPARAM.BINDVARS = BANCS.INPUT.operAcctNo + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF (sv_d == 0) THEN
	#{
		
		
		
		IF(FIELDEXISTS(BANCS.OUTPARAM.forAcct)) THEN
		#{
			
			sv_z = "Accnt| " + BANCS.INPUT.operAcctNo
			sv_u = urhk_SetOrbOut(sv_z)
			
			#}
		ELSE
			#{
			
			#looking for the old account number
			
			sv_s = "fin_acct_num|SELECT foracid FROM CUSTOM.OLD_ACCT_MAP "
			sv_s = sv_s + "WHERE old_num = ?SVAR "
			sv_s = sv_s + "AND bank_id = ?SVAR "
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = BANCS.INPUT.operAcctNo + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSelectWithBind(sv_s)
			
			#if there is a match
			
			IF(FIELDEXISTS(BANCS.OUTPARAM.fin_acct_num)) THEN
			#{
				
				sv_z = "Accnt| " + BANCS.OUTPARAM.fin_acct_num
				sv_u = urhk_SetOrbOut(sv_z)
				
				
				
				
				#}
			ELSE
				#{
				
				sv_z="Error|"+"Error"
				sv_u=urhk_SetOrbOut(sv_z)
				
			#}
			ENDIF
			
			
		#}
		ENDIF
		
		
		#}
	ELSE
		#{
		
		sv_z="Error|"+"Error"
		sv_u=urhk_SetOrbOut(sv_z)
		
	#}
	ENDIF
	
	
	#TRACE OFF
	EXITSCRIPT
END-->
