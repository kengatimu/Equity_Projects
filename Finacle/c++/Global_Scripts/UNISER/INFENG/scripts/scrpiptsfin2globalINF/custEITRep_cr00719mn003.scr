#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: custEITRep_cr00719mn003.scr
# Type: NA|default
# Description: custEITRep_cr00719mn003.scr
# Author:
# Date:
# Modification Log:
#============================================================

#############################################################################################
# Source Name          : custEITRep_cr00719mn003.scr
# Date                 : 21-10-2012
# Description          : Delete data from c_eit
# Input Values         : NA
# Output Values        : NA
# Called Script        : NA
# Calling Script       : NA
# Author               : Parikshith V
# Bank                 : EQUITY BANK
# Modification History :
# <Version No.>    <Date>     <Author Name>         <Description>
#   1            21-10-2012   Parikshith V	Original Version
#############################################################################################

<--START
CHECKSUM="3a18afdf9f49e4b8e8ff12040a6a414d25bcbcdb06fe7114b955011953e5485e"
	TRACE ON
	
	#------------------------------------------------------------------------------
	#       Creating repository and class
	#       Initializing the variables
	#       Getting all the values from CPARAM
	#------------------------------------------------------------------------------
	IF(REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF(CLASSEXISTS("CUST","EIT") == 0) THEN
	#{
		CREATECLASS("CUST","EIT",5)
	#}
	ENDIF
	
	CUST.EIT.errMsg    = ""
	
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
	sv_s = " BODDate|select to_char(dc_cls_date,'dd-mm-yyyy') from tbaadm.gct  "
	sv_s = sv_s + " WHERE bank_id = ?SVAR "
	sv_u = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_u)
	
	sv_b = BANCS.OUTPARAM.BODDate
	
	
	#Deleting Entries older than 2 days from C_EIT_BACKUP
	BANCS.INPARAM.BINDVARS =  sv_b + "|" + BANCS.STDIN.contextBankId
	sv_s = " delete from custom.c_eit_backup WHERE ((to_date(?SVAR,'dd-mm-yyyy') - eit_date) > 1) "
	sv_s = sv_s + " AND bank_id=?SVAR"
	PRINT(sv_s)
	sv_u = urhk_dbSQLWithBind(sv_s)
	PRINT(sv_u)
	#Selecting limit value from setvar
	#BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
	#sv_s = " limit|SELECT variable_value "
	#sv_s = sv_s + "   FROM c_svrsetvar "
	#sv_s = sv_s + "  WHERE module_name = 'GL_REPORTS' "
	#sv_s = sv_s + "    AND sub_module_name = 'EIT_RPT' "
	#sv_s = sv_s + "    AND variable_name = 'C_EIT_LIMIT' "
	#sv_s = sv_s + "    AND del_flg != 'Y' "
	#sv_s = sv_s + " AND bank_id = ?SVAR "
	#sv_u = urhk_dbSelectWithBind(sv_s)
	#print(sv_u)
	
	BANCS.INPUT.moduleName = "GL_REPORTS"
	BANCS.INPUT.parameterId = "C_EIT_LIMIT"
	CALL ("getcparamvalue.scr")
	IF(BANCS.OUTPUT.parameterVal != "")THEN
	#{
		sv_u = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		CUST.EIT.errMsg = "Limit setup not done in CPARAM"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_l="13"
	IF(sv_u == 0) THEN
	#{
		sv_l = BANCS.OUTPARAM.limit
	#}
	ENDIF
	PRINT(sv_l)
	
	#Deleting Entries older than 13 months from C_EIT
	BANCS.INPARAM.BINDVARS = MID$(BANCS.STDIN.BODDate,0,10) + "|" + sv_l + "|" + BANCS.STDIN.contextBankId
	sv_s = " DELETE FROM custom.c_eit "
	sv_s = sv_s + "       WHERE MONTHS_BETWEEN (TO_DATE (?SVAR, 'dd-mm-yyyy'), tran_date) >= ?SVAR "
	sv_s = sv_s + " AND bank_id = ?SVAR "
	sv_u = urhk_dbSqlWithBind(sv_s)
	PRINT(sv_u)
	BANCS.INPARAM.BINDVARS = ""
	sv_u = urhk_dbSqlWithBind("COMMIT")
	PRINT(sv_u)
	
	ENDOFSCRIPT:
		IF (CUST.EIT.errMsg != "") THEN
		#{
			PRINT(CUST.EIT.errMsg)
			sv_u = SYSTEM("echo '"+CUST.EIT.errMsg+"' >>EIT.log")
		#}
		ENDIF
		
		TRACE OFF
END-->
