##########################################################################################
# Source Name           : cparam_module_list.scr
# Date                  : 04-Mar-2013
# Description           :
# Author                : Mohan Raghu Arudi
# Modification History  :
# <Serial No.>  <Date>        <Author Name>             <Description>
# ------------  -----------   -----------------         --------------
#      1        04-Mar-2013   Mohan Raghu Arudi         Base Version
###########################################################################################
#Modify Parameter Value
<--START
CHECKSUM="fafae0d2638ccec0a9054d406c234fb2257b3418722f7e666f94f70253e1e6ca"
	TRACE ON
	
	
	print(CUST.CPARAM.funcCode)
	print(CUST.CPARAM.moduleName)
	print(CUST.CPARAM.parameterLevel)
	print(CUST.CPARAM.parameterId)
	print(CUST.CPARAM.schemeCode)
	print(CUST.CPARAM.currencyCode)
	print(CUST.CPARAM.genericCode)
	print(CUST.CPARAM.genericId)
	print(CUST.CPARAM.solId)
	
	CUST.CPARAM.recCount = ""
	
	sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS(SELECT 1 FROM CUSTOM.MODULE"
	sv_s = sv_s + " WHERE MODULE_ID = ?SVAR"
	sv_s = sv_s + " AND BANK_ID = ?SVAR"
	sv_s = sv_s + " AND ENTITY_CRE_FLG = 'Y'"
	sv_s = sv_s + " AND DEL_FLG = 'N')"
	
	print(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.CPARAM.moduleName + "|" + BANCS.STDIN.contextBankId
	print(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	IF(sv_d != 0)THEN
	#{
		#ERROR
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		print(sv_u)
		sv_u = urhk_SetOrbOut("Error_1|Error^Invalid Module Name^moduleName")
		print(sv_u)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.C_CPMASTER"
	sv_s = sv_s + " WHERE MODULE_NAME = ?SVAR"
	sv_s = sv_s + " AND PARAMETER_LEVEL = ?SVAR"
	sv_s = sv_s + " AND PARAMETER_ID = ?SVAR"
	sv_s = sv_s + " AND DEL_FLG = 'N'"
	sv_s = sv_s + " AND BANK_ID = ?SVAR"
	
	print(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.CPARAM.moduleName + "|" + CUST.CPARAM.parameterLevel + "|" + CUST.CPARAM.parameterId + "|" + BANCS.STDIN.contextBankId
	print(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	IF(sv_d != 0)THEN
	#{
		#ERROR
	#}
	ENDIF
	
	CUST.CPARAM.recCount = BANCS.OUTPARAM.recCount
	print(CUST.CPARAM.recCount)
	
	IF(CINT(CUST.CPARAM.recCount) == 0)THEN
	#{
		#ERROR
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		print(sv_u)
		sv_u = urhk_SetOrbOut("Error_1|Error^Record does not exists^parameterId")
		print(sv_u)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.C_CPMASTER_MOD"
	sv_s = sv_s + " WHERE MODULE_NAME = ?SVAR"
	sv_s = sv_s + " AND PARAMETER_LEVEL = ?SVAR"
	sv_s = sv_s + " AND PARAMETER_ID = ?SVAR"
	sv_s = sv_s + " AND DEL_FLG = 'N'"
	sv_s = sv_s + " AND BANK_ID = ?SVAR"
	
	print(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.CPARAM.moduleName + "|" + CUST.CPARAM.parameterLevel + "|" + CUST.CPARAM.parameterId + "|" + BANCS.STDIN.contextBankId
	print(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	IF(sv_d != 0)THEN
	#{
		#ERROR
	#}
	ENDIF
	
	CUST.CPARAM.recCount = BANCS.OUTPARAM.recCount
	print(CUST.CPARAM.recCount)
	
	IF(CINT(CUST.CPARAM.recCount) == 0)THEN
	#{
		CUST.CPARAM.mainTable = "Y"
		#}
	ELSE
		#{
		CUST.CPARAM.mainTable = "N"
	#}
	ENDIF
	
	CUST.CPARAM.parameterDesc = " "
	CUST.CPARAM.parameterType = " "
	CUST.CPARAM.listType = " "
	CUST.CPARAM.queryId = " "
	CUST.CPARAM.queryName = " "
	CUST.CPARAM.queryDetails = " "
	CUST.CPARAM.entityCreFlg = " "
	
	sv_s = "parameterDesc,parameterType,listType,queryId,entityCreFlg|SELECT PARAMETER_DESC,PARAMETER_TYPE,LIST_TYPE,QUERY_ID,ENTITY_CRE_FLG"
	sv_s = sv_s + " FROM CUSTOM.C_CPMASTER "
	sv_s = sv_s + " WHERE MODULE_NAME = ?SVAR"
	sv_s = sv_s + " AND PARAMETER_LEVEL = ?SVAR"
	sv_s = sv_s + " AND PARAMETER_ID = ?SVAR"
	sv_s = sv_s + " AND DEL_FLG = 'N'"
	sv_s = sv_s + " AND BANK_ID = ?SVAR"
	
	print(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.CPARAM.moduleName + "|" + CUST.CPARAM.parameterLevel + "|" + CUST.CPARAM.parameterId + "|" + BANCS.STDIN.contextBankId
	print(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	IF(sv_d != 0)THEN
	#{
		#ERROR
		
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		print(sv_u)
		sv_u = urhk_SetOrbOut("Error_1|Error^Unable to get data from database^parameterId")
		print(sv_u)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.parameterDesc != "")THEN
	#{
		CUST.CPARAM.parameterDesc = BANCS.OUTPARAM.parameterDesc
		print(CUST.CPARAM.parameterDesc)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.parameterType != "")THEN
	#{
		CUST.CPARAM.parameterType = BANCS.OUTPARAM.parameterType
		print(CUST.CPARAM.parameterType)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.listType != "")THEN
	#{
		CUST.CPARAM.listType = BANCS.OUTPARAM.listType
		print(CUST.CPARAM.listType)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.queryId != "")THEN
	#{
		CUST.CPARAM.queryId = BANCS.OUTPARAM.queryId
		print(CUST.CPARAM.queryId)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.entityCreFlg != "")THEN
	#{
		CUST.CPARAM.entityCreFlg = BANCS.OUTPARAM.entityCreFlg
		print(CUST.CPARAM.entityCreFlg)
	#}
	ENDIF
	
	IF(CUST.CPARAM.entityCreFlg != "Y")THEN
	#{
		#ERROR
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		print(sv_u)
		sv_u = urhk_SetOrbOut("Error_1|Error^Record not yet Authorised^parameterId")
		print(sv_u)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	IF(CUST.CPARAM.listType == "LS")THEN
	#{
		
		
	#}
	ENDIF
	
	IF(CUST.CPARAM.listType == "LD")THEN
	#{
		
		
	#}
	ENDIF
	
	#To Get Parameter Value
	
	sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.C_CPVALUE_MOD"
	sv_s = sv_s + " WHERE MODULE_NAME = ?SVAR"
	sv_s = sv_s + " AND PARAMETER_ID = ?SVAR"
	sv_s = sv_s + " AND DEL_FLG = 'N'"
	sv_s = sv_s + " AND BANK_ID = ?SVAR"
	
	print(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.CPARAM.moduleName + "|" + CUST.CPARAM.parameterId + "|" + BANCS.STDIN.contextBankId
	print(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	IF(sv_d != 0)THEN
	#{
		#ERROR
	#}
	ENDIF
	
	CUST.CPARAM.recCount = BANCS.OUTPARAM.recCount
	print(CUST.CPARAM.recCount)
	
	IF(CINT(CUST.CPARAM.recCount) == 0)THEN
	#{
		CUST.CPARAM.mainTable = "Y"
	#}
	ELSE
		#{
		CUST.CPARAM.mainTable = "N"
	#}
	ENDIF
	
	
	CUST.CPARAM.lchgUserId = ""
	sv_s = "parameterValue,lchgUserId|SELECT PARAMETER_VALUE,LCHG_USER_ID"
	IF(CUST.CPARAM.mainTable == "Y")THEN
	#{
		sv_s = sv_s + " FROM CUSTOM.C_CPVALUE "
		#}
	ELSE
		#{
		sv_s = sv_s + " FROM CUSTOM.C_CPVALUE_MOD "
	#}
	ENDIF
	sv_s = sv_s + " WHERE MODULE_NAME = ?SVAR"
	sv_s = sv_s + " AND PARAMETER_ID = ?SVAR"
	sv_s = sv_s + " AND DEL_FLG = 'N'"
	sv_s = sv_s + " AND BANK_ID = ?SVAR"
	
	print(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.CPARAM.moduleName + "|" + CUST.CPARAM.parameterId + "|" + BANCS.STDIN.contextBankId
	print(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	IF(sv_d != 0)THEN
	#{
		CUST.CPARAM.parameterValue = " "
		#}
	ELSE
		#{
		IF(BANCS.OUTPARAM.parameterValue != "")THEN
		#{
			CUST.CPARAM.parameterValue = BANCS.OUTPARAM.parameterValue
			print(CUST.CPARAM.parameterValue)
		#}
		ENDIF
		
		IF(BANCS.OUTPARAM.lchgUserId != "")THEN
		#{
			CUST.CPARAM.lchgUserId = BANCS.OUTPARAM.lchgUserId
			print(CUST.CPARAM.lchgUserId)
		#}
		ENDIF
		
		IF((CUST.CPARAM.mainTable == "N") AND (CUST.CPARAM.lchgUserId != BANCS.STDIN.userId))THEN
		#{
			#ERROR
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			print(sv_u)
			sv_u = urhk_SetOrbOut("Error_1|Error^Unverified record exists. Only same user can modify^parameterId")
			print(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	
	sv_u = urhk_SetOrbOut("parameterValue|"+CUST.CPARAM.parameterValue)
	print(sv_u)
	
	sv_u = urhk_SetOrbOut("parameterDesc|"+CUST.CPARAM.parameterDesc)
	print(sv_u)
	sv_u = urhk_SetOrbOut("parameterType|"+CUST.CPARAM.parameterType)
	print(sv_u)
	sv_u = urhk_SetOrbOut("listType|"+CUST.CPARAM.listType)
	print(sv_u)
	sv_u = urhk_SetOrbOut("queryId|"+CUST.CPARAM.queryId)
	print(sv_u)
	sv_u = urhk_SetOrbOut("queryName|"+CUST.CPARAM.queryName)
	print(sv_u)
	sv_u = urhk_SetOrbOut("queryDetails|"+CUST.CPARAM.queryDetails)
	print(sv_u)
	
	sv_u = urhk_SetOrbOut("parameterLevel|"+CUST.CPARAM.parameterLevel)
	print(sv_u)
	sv_u = urhk_SetOrbOut("parameterId|"+CUST.CPARAM.parameterId)
	print(sv_u)
	sv_u = urhk_SetOrbOut("moduleName|"+CUST.CPARAM.moduleName)
	print(sv_u)
	sv_u = urhk_SetOrbOut("solId|"+CUST.CPARAM.solId)
	print(sv_u)
	sv_u = urhk_SetOrbOut("schemeCode|"+CUST.CPARAM.schemeCode)
	print(sv_u)
	sv_u = urhk_SetOrbOut("currencyCode|"+CUST.CPARAM.currencyCode)
	print(sv_u)
	sv_u = urhk_SetOrbOut("genericCode|"+CUST.CPARAM.genericCode)
	print(sv_u)
	sv_u = urhk_SetOrbOut("genericId|"+CUST.CPARAM.genericId)
	print(sv_u)
	sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
	print(sv_u)
	
	ENDOFSCRIPT:
END-->
