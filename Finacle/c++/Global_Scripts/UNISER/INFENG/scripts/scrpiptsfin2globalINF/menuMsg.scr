#----------------------------------------------------------------------------------------------------------------------------- 
#    Name			 : menuMsg.scr
#    Description		: to show msg based on Menu.
#    Date			: 11-07-2013
#    Author			  : Vijaya V
#    Called Script		 : NA
#    Calling Script	 : header_link.js
#    Menu Option		 : Home page
#    Srl. No			Date			Author			Description.
#    ------				----------			-------			---------------
#    1.0			12-09-2013			Vijaya V		Original Version
#----------------------------------------------------------------------------------------------------------------------------
<--START
CHECKSUM="c6c964867889a08b8865571f8659ab7ed544d7822634db72d0cd937bae971e00"
	TRACE ON
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST", "MENU") == 0) THEN
	#{
		CREATECLASS("CUST", "MENU", 5)
	#}
	ENDIF
	
	CUST.MENU.valid	=	" "
	CUST.MENU.mopId	=	" "
	CUST.MENU.solId	=	BANCS.STDIN.mySolId
	
	#########################################################
	#Inputs from jsp
	#########################################################
	#print(BANCS.INPUT.menu_Name)
	
	CUST.MENU.menu_Name = trim(BANCS.INPUT.menu_Name)
	
	##################################################
	#Validation for spl characters  & '
	##################################################
	
	sv_p = GETPOSITION(CUST.MENU.menu_Name, "'")
	print(sv_p)
	IF(sv_p!=0 ) THEN
	#{
		sv_u = urhk_SetOrbOut("err|"+CUST.MENU.mopId)
		GOTO ENDSCRIPT
	#}
	ENDIF
	
	sv_p = GETPOSITION(CUST.MENU.menu_Name, "&")
	print(sv_p)
	IF(sv_p!=0 ) THEN
	#{
		sv_u = urhk_SetOrbOut("err|"+CUST.MENU.mopId)
		GOTO ENDSCRIPT
	#}
	ENDIF
	
	sv_q = urhk_B2k_PrintRepos("BANCS")
	print(sv_q)
	
	print(BANCS.STDIN.contextBankId)
	
	sv_s = "mopId|SELECT MOP_ID "
	sv_s = sv_s + " FROM TBAADM.MOD "
	sv_s = sv_s + " WHERE DEL_FLG ='N' "
	sv_s = sv_s + " AND ENTITY_CRE_FLG= 'Y' "
	sv_s = sv_s + " AND MOP_TYPE = 'U' "
	sv_s = sv_s + " AND MOP_ID =  ?SVAR "
	sv_s = sv_s + " AND bank_id = ?SVAR "
	BANCS.INPARAM.BINDVARS = CUST.MENU.menu_Name  + "|"  + BANCS.STDIN.contextBankId
	PRINT(sv_s)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	IF (sv_d == 0) THEN
	#{
		CUST.MENU.mopId = BANCS.OUTPARAM.mopId
		print(CUST.MENU.mopId)
		
		IF(CUST.MENU.mopId !="") THEN
		#{
			CUST.MENU.valid = "Y"
		#}
		ENDIF
		
		sv_u = urhk_SetOrbOut("paramV1|"+CUST.MENU.valid)
		sv_u = urhk_SetOrbOut("paramV2|"+CUST.MENU.solId)
		#}
	ELSE
		#{
		sv_u = urhk_SetOrbOut("err|"+CUST.MENU.mopId)
		GOTO ENDSCRIPT
	#}
	ENDIF
	
	
	ENDOFSCRIPT:
		TRACE OFF
		EXITSCRIPT
END-->
