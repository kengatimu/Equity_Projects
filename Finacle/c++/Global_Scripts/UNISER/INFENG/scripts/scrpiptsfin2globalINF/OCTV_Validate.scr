#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: OCTV_Validate.scr
# Type: NA|default
# Description: Equity Finacle 10.2.13 to 10.2.18 Poring
# Author:
# Date:
# Modification Log:
#============================================================

<--START
CHECKSUM="bf8701e23017d4aebf47b52f6a43b1cc61c2eb8255f5af1afd4ed9c24d2c2805"
	TRACE ON
	#----------------------------------------------
	# Executing the validation function ExecVal
	#----------------------------------------------
	sv_a=urhk_ExecVal("valDate|dateStr=&clg_zone_date|MANDATORY")
	
	sv_b=urhk_ExecVal("validateClgZone|code=&clg_zone_code|setId=&sol_id|date=&clg_zone_date|desc=&clg_zone_desc|MANDATORY|IGNORE_ERROR")
	
	IF(sv_b==1) THEN
	#{
		sv_z=urhk_VAL_SetErr("FLD_NAME=clg_zone_code|ERR_TYPE=B|ERR_CODE=755")
		GOTO Finish
	#}
	ENDIF
	
	IF(sv_b==0) THEN
	#{
		sv_p=urhk_VAL_GetVal("sol_id")
		sv_q=BANCS.OUTPARAM.fldValue
		
		sv_r=urhk_VAL_GetVal("clg_zone_date")
		sv_s=BANCS.OUTPARAM.fldValue
		
		sv_t=urhk_VAL_GetVal("clg_zone_code")
		sv_u=BANCS.OUTPARAM.fldValue
		
		sv_v = "count | select count(*) from TBAADM.OZH "
		sv_v = sv_v + " where "
		sv_v = sv_v + " OZH.BANK_ID =?SVAR AND "
		sv_v = sv_v + " sol_id =?SVAR"
		sv_v = sv_v + " AND clg_zone_date = to_date(?SVAR, 'DD-MM-YYYY HH24:MI:SS')"
		sv_v = sv_v + " AND clg_zone_code =?SVAR"
		BANCS.INPARAM.BINDVARS= BANCS.STDIN.contextBankId + "|" +sv_q + "|" +sv_s+ "|" +sv_u
		sv_w = urhk_dbSelectWithBind(sv_v)
		
		IF(BANCS.OUTPARAM.count > 0) THEN
		#{
			sv_v = "zoneStatus | select zone_stat from TBAADM.OZH "
			sv_v = sv_v + " where "
			sv_v = sv_v + " OZH.BANK_ID = ?SVAR AND "
			sv_v = sv_v + " sol_id =?SVAR"
			sv_v = sv_v + " AND clg_zone_date = to_date(?SVAR, 'DD-MM-YYYY HH24:MI:SS')"
			sv_v = sv_v + " AND clg_zone_code =?SVAR"
			
			BANCS.INPARAM.BINDVARS= BANCS.STDIN.contextBankId + "|" +sv_q + "|" +sv_s+ "|" +sv_u
			sv_w = urhk_dbSelectWithBind(sv_v)
			sv_x=BANCS.OUTPARAM.zoneStatus
			
			IF (sv_x != "S") THEN
			#{
				sv_y=urhk_VAL_SetErr("FLD_NAME=clg_zone_code|ERR_TYPE=B|ERR_CODE=A17")
				GOTO Finish
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	sv_c=urhk_ExecVal("validateSetNum|from_num=&set_num_from")
	
	sv_d=urhk_ExecVal("validateSetNum|to_num=&set_num_to")
	
	IF((sv_c == 0 ) AND (sv_d == 0))THEN
	#{
		sv_e=urhk_VAL_GetVal("set_num_from")
		sv_f=BANCS.OUTPARAM.fldValue
		
		sv_g=urhk_VAL_GetVal("set_num_to")
		sv_h=BANCS.OUTPARAM.fldValue
		
		IF ((sv_f != "") AND (sv_h != "")) THEN
		#{
			IF (CDOUBLE(sv_f) > CDOUBLE(sv_h)) THEN
			#{
				sv_i=urhk_VAL_SetErr("FLD_NAME=set_num_from|ERR_TYPE=B|ERR_CODE=740")
				GOTO Finish
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	#------------------------------------------------------------
	# This script is for restrictinng use of menu option OCTV unless for custodial Batch Verification
	#------------------------------------------------------------
	#-----------------------------------------------
	#Data from C_CPARAM
	#-------------------------------------
	BANCS.INPUT.moduleName = "CLEARING"
	BANCS.INPUT.parameterId = "ZONE_CUSTOZONE"
	CALL("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		PRINT(BANCS.OUTPUT.parameterVal)
		sv_a=BANCS.OUTPUT.parameterVal
		PRINT(sv_a)
		#}
	ELSE
		#{
		sv_g = "RESULT_MSG|Parameter value not set in C_CPARAM_VALUE_TABLE"
		sv_z = urhk_SetOrbOut(sv_g)
	#}
	ENDIF
	
	#-----------------------------------------------
	#Data from C_CPARAM
	#-------------------------------------
	BANCS.INPUT.moduleName = "CLEARING"
	BANCS.INPUT.parameterId = "ZONE_PROMPTZONE"
	CALL("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		PRINT(BANCS.OUTPUT.parameterVal)
		sv_b=BANCS.OUTPUT.parameterVal
		PRINT(sv_b)
		#}
	ELSE
		#{
		sv_g = "RESULT_MSG|Parameter value not set in C_CPARAM_VALUE_TABLE"
		sv_z = urhk_SetOrbOut(sv_g)
	#}
	ENDIF
	
	IF ( BANCS.STDIN.contextBankId =="54") THEN
	#{
		#-----------------------------------------------
		#Data from C_CPARAM
		#-------------------------------------
		BANCS.INPUT.moduleName = "CLEARING"
		BANCS.INPUT.parameterId = "ZONE_PAYBILL"
		CALL("getcparamvalue.scr")
		IF (BANCS.OUTPUT.parameterVal != "") THEN
		#{
			PRINT(BANCS.OUTPUT.parameterVal)
			sv_c=BANCS.OUTPUT.parameterVal
			PRINT(sv_c)
			#}
		ELSE
			#{
			sv_g = "RESULT_MSG|Parameter value not set in C_CPARAM_VALUE_TABLE"
			sv_z = urhk_SetOrbOut(sv_g)
		#}
		ENDIF
		
		IF (sv_u!=sv_a) THEN
		#{
			IF (sv_u!=sv_b) THEN
			#{
				IF (sv_u!=sv_c) THEN
				#{
					PRINT("Invalid Zone for this menu option")
					sv_s = "FLD_NAME=clg_zone_code|ERR_TYPE=B|ERR_CODE=208"
					sv_u = urhk_VAL_SetErr(sv_s)
					sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
					GOTO Finish
				#}
				ENDIF
			#}
			ENDIF
		#}
		ENDIF
		#}
	ELSE
		#{
		IF (sv_u!=sv_a) THEN
		#{
			IF (sv_u!=sv_b) THEN
			#{
				PRINT("Invalid Zone for this menu option")
				sv_s = "FLD_NAME=clg_zone_code|ERR_TYPE=B|ERR_CODE=208"
				sv_u = urhk_VAL_SetErr(sv_s)
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				GOTO Finish
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	Finish:
		EXITSCRIPT
		TRACE OFF
END-->
