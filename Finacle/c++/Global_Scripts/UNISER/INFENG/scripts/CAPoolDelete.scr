<--START
TRACE ON
        IF(FIELDEXISTS(BANCS.INPUT.acctNum))THEN
        #{
                IF(TRIM(BANCS.INPUT.acctNum) != "")THEN
                #{
                        sv_a = TOUPPER(BANCS.INPUT.acctNum)
                        PRINT(sv_a)
                        #}
                ELSE
                        #{
                        GOTO ENDOFSCRIPT
                #}
                ENDIF
                #}
        ELSE
                #{
                GOTO ENDOFSCRIPT
        #}
        ENDIF

        sv_s = "UPDATE TBAADM.GAM SET POOL_ID=NULL WHERE BANK_ID=?SVAR AND FORACID=?SVAR"
        BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId +"|"+ sv_a
        PRINT(sv_s)
        PRINT(BANCS.INPARAM.BINDVARS)
        sv_q = urhk_dbSQLWithBind(sv_s)
        PRINT(sv_q)
        sv_q = urhk_dbSQLWithBind("COMMIT")
        PRINT(sv_q)
ENDOFSCRIPT:
TRACE OFF
END-->
