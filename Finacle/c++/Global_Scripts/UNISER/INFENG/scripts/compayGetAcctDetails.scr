#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: compayGetAcctDetails.scr
# Type: NA|default
# Description: Commission Payment Script for Getting Account Details
# Author: Anshul_Singh04
# Date: 6/4/18 5:00 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="d31746424c483682002342d9d11362ae16d513cdadc093ebcb289f9da4ca6e5c"
	TRACE ON
	
	###########################################################################################
	# Check whether EQT Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("EQT")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("EQT")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether COMM Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("EQT","COMM")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("EQT","COMM",5)
	#}
	ENDIF
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	
	EQT.COMM.acctNum = " "
	EQT.COMM.errorFlg = "N"
	EQT.COMM.errorMsg = " "
	EQT.COMM.acctEntityId = " "
	EQT.COMM.acctSolId = " "
	EQT.COMM.acctCrncy = " "
	EQT.COMM.acctName = " "
	EQT.COMM.entityCreFlg = " "
	EQT.COMM.acctClsFlg = " "
	
	#sv_u = urhk_b2k_printrepos("BANCS")
	#print(sv_u)
	
	IF(FIELDEXISTS(BANCS.INPUT.acctNum))THEN
	#{
		IF(TRIM(BANCS.INPUT.acctNum) != "")THEN
		#{
			EQT.COMM.acctNum = TOUPPER(TRIM(BANCS.INPUT.acctNum))
			PRINT(EQT.COMM.acctNum)
		#}
		ENDIF
		#}
	ELSE
		#{
		EQT.COMM.errorFlg = "Y"
		EQT.COMM.errorMsg = "Invalid Input for fnExecuteScript"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_s = "acctEntityId,acctSolId,acctCrncy,acctName,entityCreFlg,acctClsFlg,schmType|"
	sv_s = sv_s + " SELECT BANK_ID,SOL_ID,ACCT_CRNCY_CODE,ACCT_NAME,ENTITY_CRE_FLG,ACCT_CLS_FLG,SCHM_TYPE"
	sv_s = sv_s + " FROM TBAADM.GAM "
	sv_s = sv_s + " WHERE FORACID = ?SVAR AND BANK_ID= ?SVAR"
	sv_s = sv_s + " AND DEL_FLG != 'Y'"
	PRINT(sv_s)
	
	BANCS.INPARAM.BINDVARS = EQT.COMM.acctNum + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	IF(sv_d == 0)THEN
	#{
		EQT.COMM.acctEntityId = BANCS.OUTPARAM.acctEntityId
		PRINT(EQT.COMM.acctEntityId)
		EQT.COMM.acctSolId = BANCS.OUTPARAM.acctSolId
		PRINT(EQT.COMM.acctSolId)
		EQT.COMM.acctCrncy = BANCS.OUTPARAM.acctCrncy
		PRINT(EQT.COMM.acctCrncy)
		EQT.COMM.acctName = BANCS.OUTPARAM.acctName
		PRINT(EQT.COMM.acctName)
		EQT.COMM.entityCreFlg = BANCS.OUTPARAM.entityCreFlg
		PRINT(EQT.COMM.entityCreFlg)
		EQT.COMM.acctClsFlg = BANCS.OUTPARAM.acctClsFlg
		PRINT(EQT.COMM.acctClsFlg)
		EQT.COMM.schmType = BANCS.OUTPARAM.schmType
		PRINT(EQT.COMM.schmType)
		
		IF(EQT.COMM.entityCreFlg == "N")THEN
		#{
			EQT.COMM.errorFlg = "Y"
			EQT.COMM.errorMsg = "Account Creation is Not yet Authorised"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		IF(EQT.COMM.acctClsFlg == "Y")THEN
		#{
			EQT.COMM.errorFlg = "Y"
			EQT.COMM.errorMsg = "Account is closed"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		IF((EQT.COMM.schmType !="SBA") AND  (EQT.COMM.schmType !="CAA") AND (EQT.COMM.schmType !="ODA")) THEN
		#{
			EQT.COMM.errorFlg = "Y"
			EQT.COMM.errorMsg = "Only SBA,CAA OR ODA type of Account should be entered."
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		#}
	ELSE
		#{
		EQT.COMM.errorFlg = "Y"
		EQT.COMM.errorMsg = "Invalid Account ID"
	#}
	ENDIF
	
	ENDOFSCRIPT:
		
		sv_u = urhk_SetOrbOut("errorFlg|"+EQT.COMM.errorFlg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("errorMsg|"+EQT.COMM.errorMsg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("acctEntityId|"+EQT.COMM.acctEntityId)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("acctSolId|"+EQT.COMM.acctSolId)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("acctCrncy|"+EQT.COMM.acctCrncy)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("acctName|"+EQT.COMM.acctName)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("acctNum|"+EQT.COMM.acctNum)
		PRINT(sv_u)
		
END-->
