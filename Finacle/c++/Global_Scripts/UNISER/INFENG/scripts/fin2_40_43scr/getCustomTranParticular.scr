#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: getCustomTranPart.scr
# Type: NA|default
# Description: getCustomTranPart.scr
# Author: Anshul_Singh04
# Date: 11/14/18 6:10 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="b5369af2db58fd7f307b3382656585d3321c24de5083006e4c60e0caebca393c"
	# Script body goes here
	TRACE ON
	sv_r=urhk_B2k_PrintRepos("BANCS")
	
	BANCS.OUTPUT.description = " "
	
	IF (REPEXISTS("CUST") == 1) THEN
	#{
		IF (CLASSEXISTS("CUST","TRAN") == 1) THEN
		#{
			IF(FIELDEXISTS(CUST.TRAN.origTranId))THEN
			#{
				IF(TRIM(CUST.TRAN.origTranId) != "")THEN
				#{
					PRINT(CUST.TRAN.origTranId)
					PRINT(CUST.TRAN.origTranDate)
					
					IF(CUST.TRAN.origTranId!="")THEN
					#{
						sv_s = "cRefCrncy,cRefAmt|select  REF_CRNCY_CODE,REF_AMT from tbaadm.dtd "
						sv_s = sv_s + "where tran_id = ?SVAR and tran_date = TO_DATE(?SVAR,'DD-MM-YYYY') and bank_id = ?SVAR "
						sv_s = sv_s + "and TRAN_SUB_TYPE in ('NP','CP','PI') and cust_id is not null and rownum = 1 "
						
						BANCS.INPARAM.BINDVARS = CUST.TRAN.origTranId + "|" + MID$(CUST.TRAN.origTranDate,0,10) + "|" + BANCS.STDIN.contextBankId
						PRINT(BANCS.INPARAM.BINDVARS)
						
						sv_d = urhk_dbSelectWithBind(sv_s)
						PRINT(sv_d)
						
						IF(sv_d == 0) THEN
						#{
							print(BANCS.OUTPARAM.cRefCrncy)
							print(BANCS.OUTPARAM.cRefAmt)
							IF(FIELDEXISTS(BANCS.INPUT.eventId))THEN
							#{							
								IF(BANCS.INPUT.eventId == "VAT")THEN
								#{
								sv_s = "VAT FOR TRANID:"+ TRIM(CUST.TRAN.origTranId) +" WITH TRAN AMOUNT: "+BANCS.OUTPARAM.cRefAmt+""+BANCS.OUTPARAM.cRefCrncy
								#}
								ELSE
								#{		
								sv_s = "CHARGES ON TRANID:"+ TRIM(CUST.TRAN.origTranId) +" WITH TRAN AMOUNT: "+BANCS.OUTPARAM.cRefAmt+""+BANCS.OUTPARAM.cRefCrncy
								#}
								ENDIF
								print(sv_s)
							
								BANCS.OUTPUT.description = sv_s
							#}
							ENDIF
						#}
                                      		ELSE
                                      		#{
                                                        IF(FIELDEXISTS(BANCS.INPUT.eventId))THEN
                                                        #{

                                              		IF(BANCS.INPUT.eventId == "VAT")THEN
                                              		#{
                                                      		BANCS.OUTPUT.description = "VAT ON CHARGES"
                                              		#}
                                              		ELSE
                                              		#{
                                                      		BANCS.OUTPUT.description = "CHARGE COLLECTION"
                                              		#}
                                              		ENDIF
							#}
							ENDIF
						#}
						ENDIF
                                      #}
                                      ELSE
                                      #{
                                              IF(FIELDEXISTS(BANCS.INPUT.eventId))THEN
                                              #{

                                              IF(BANCS.INPUT.eventId == "VAT")THEN
                                              #{
                                                      BANCS.OUTPUT.description = "VAT ON CHARGES"
                                              #}
                                              ELSE
                                              #{
                                                      BANCS.OUTPUT.description = "CHARGE COLLECTION"
                                              #}
                                              ENDIF
						#}
						ENDIF

					#}
					ENDIF
				#}
				ELSE
				#{
                                       IF(FIELDEXISTS(BANCS.INPUT.eventId))THEN
                                       #{

                			IF(BANCS.INPUT.eventId == "VAT")THEN
                			#{
		        			BANCS.OUTPUT.description = "VAT ON CHARGES"
					#}
					ELSE
					#{
		        			BANCS.OUTPUT.description = "CHARGE COLLECTION"
					#}
					ENDIF
					#}
					ENDIF
				#}
				ENDIF
			#}
			ELSE
			#{
                                IF(FIELDEXISTS(BANCS.INPUT.eventId))THEN
                                #{

                		IF(BANCS.INPUT.eventId == "VAT")THEN
                		#{
		        		BANCS.OUTPUT.description = "VAT ON CHARGES"
				#}
				ELSE
				#{
		        		BANCS.OUTPUT.description = "CHARGE COLLECTION"
				#}
				ENDIF
				#}
				ENDIF
			#}
			ENDIF
		#}
		ELSE
		#{
                       IF(FIELDEXISTS(BANCS.INPUT.eventId))THEN
                       #{

                	IF(BANCS.INPUT.eventId == "VAT")THEN
                	#{
		        	BANCS.OUTPUT.description = "VAT ON CHARGES"
			#}
			ELSE
			#{
		        	BANCS.OUTPUT.description = "CHARGE COLLECTION"
			#}
			ENDIF
			#}
			ENDIF
		#}
		ENDIF
	#}
	ELSE
	#{
                IF(FIELDEXISTS(BANCS.INPUT.eventId))THEN
                #{

                IF(BANCS.INPUT.eventId == "VAT")THEN
                #{
		        BANCS.OUTPUT.description = "VAT ON CHARGES"
		#}
		ELSE
		#{
		        BANCS.OUTPUT.description = "CHARGE COLLECTION"
		#}
		ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
END-->
