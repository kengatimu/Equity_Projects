#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: test2.scr
# event: NA
# description: This is a simple script
#=================================================

<--START
CHECKSUM="e0afbab294ada351162001df2f6c302eee132b2b2af32c8bcce49227bb9abe0d"
	
	TRACE ON
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST","TMCHRG") == 0) THEN
	#{
		CREATECLASS("CUST","TMCHRG",5)
	#}
	ENDIF
	
	CUST.TMCHRG.acctNo ="1"
	print(CUST.TMCHRG.acctNo)
	sv_s = "count|"
	sv_s = sv_s + " select count(1) from gv$session  "
	sv_s = sv_s + " where sid in ( select session_id from dba_dml_locks) "
	print(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.TMCHRG.acctNo
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	IF(sv_d != 0)THEN
	#{
		
		CUST.TMCHRG.todAcctStatus = "Error in finding maximum number "
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		CUST.TMCHRG.count = BANCS.OUTPARAM.count
		print(CUST.TMCHRG.count)
	#}
	ENDIF
	
	
	CUST.TMCHRG.acctNo ="2"
	print(CUST.TMCHRG.acctNo)
	sv_s = "sid,serial,INST_ID|"
	sv_s = sv_s + " select sid, serial#,INST_ID from gv$session  "
	sv_s = sv_s + " where sid in ( select session_id from dba_dml_locks) AND rownum< ?SVAR "
	print(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.TMCHRG.acctNo
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	IF(sv_d != 0)THEN
	#{
		
		CUST.TMCHRG.todAcctStatus = "Error in finding maximum number"
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		CUST.TMCHRG.sid = BANCS.OUTPARAM.sid
		print(CUST.TMCHRG.sid)
		CUST.TMCHRG.serial = BANCS.OUTPARAM.serial
		print(CUST.TMCHRG.serial)
		CUST.TMCHRG.INST_ID = BANCS.OUTPARAM.INST_ID
		print(CUST.TMCHRG.INST_ID)
		
	#}
	ENDIF
	##select sid, serial#,INST_ID from gv$session where sid in ( select session_id from dba_dml_locks);
	##alter system kill session '916,5851';
	#                            sid ,serial
	ENDOFSCRIPT:
		
END-->
