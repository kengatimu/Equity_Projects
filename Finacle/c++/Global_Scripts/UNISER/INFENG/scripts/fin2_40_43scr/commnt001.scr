#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: commnt001.scr
# Type: NA|default
# Description: Commision Maintenance Script
# Author: Anshul_Singh04
# Date: 6/1/18 11:07 AM
# Modification Log:
#============================================================

<--START
CHECKSUM="4d6e9b2782251a70b0bf2ea0047285fe4330b53b25c105d8e16d8622ce72a9e4"
	# Script body goes here
	
	TRACE ON
	
	sv_u = urhk_B2K_PrintRepos("BANCS")
	PRINT(sv_u)
	
	sv_a = REPEXISTS("EQT")
	IF (sv_a == 0) THEN
	#{
		CREATEREP("EQT")
	#}
	ENDIF
	sv_a = CLASSEXISTS("EQT", "COMM")
	IF (sv_a == 0 ) THEN
	#{
		CREATECLASS("EQT", "COMM", 5)
	#}
	ENDIF
	EQT.COMM.successOrFailure=""
	EQT.COMM.queryTbl= " "
	EQT.COMM.delFlg = "N"
	EQT.COMM.funcCode=TRIM(BANCS.INPUT.funcCode)
	PRINT(EQT.COMM.funcCode)
	PRINT(BANCS.INPUT.commCode)
	BANCS.INPUT.commCode = TOUPPER(TRIM(BANCS.INPUT.commCode))
	EQT.COMM.commCode =  BANCS.INPUT.commCode
	
	EQT.COMM.bacid = TOUPPER(TRIM(BANCS.INPUT.placeHolder))
	print(EQT.COMM.bacid)
	
	sv_a=0
	sv_b=0
	sv_c=" "
	sv_d=0
	sv_f=0
	
	sv_m = "recCountMod|SELECT count(1)"
	sv_m= sv_m +" FROM CUSTOM.C_CPM_MOD WHERE bank_id=?SVAR AND COMM_CODE=?SVAR "
	
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + EQT.COMM.commCode
	
	PRINT(sv_m)
	sv_r = urhk_dbSelectWithBind(sv_m)
	
	IF (sv_r != 0) THEN
	#{
		sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
		sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		sv_d=BANCS.OUTPARAM.recCountMod
		PRINT(sv_d)
		IF(sv_d!=0)THEN
		#{
			sv_f=1
		#}
		endif
	#}
	ENDIF
	
	
	IF((sv_d==0) AND (EQT.COMM.funcCode=="V"))THEN
	#{
		sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
		sv_t= urhk_SetOrbOut("Error_1|ERR^No Record Pending for Verification for the Selected Commission Code.^Go")
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	IF((sv_d==0) AND (EQT.COMM.funcCode=="X"))THEN
	#{
		sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
		sv_t= urhk_SetOrbOut("Error_1|ERR^No Record Modified for the Selected Commission Code!^Go")
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	IF( (EQT.COMM.funcCode=="M") OR (EQT.COMM.funcCode=="D") )THEN
	#{
		sv_m = "delFlg|SELECT nvl(max(DEL_FLG),0)"
		sv_m= sv_m +" FROM CUSTOM.C_CPM WHERE bank_id=?SVAR AND COMM_CODE=?SVAR "
		
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + EQT.COMM.commCode
		
		PRINT(sv_m)
		sv_r = urhk_dbselectWithBind(sv_m)
		
		IF (sv_r != 0) THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			EQT.COMM.delFlg= BANCS.OUTPARAM.delFlg
			PRINT(EQT.COMM.delFlg)
		#}
		ENDIF
		
		IF(EQT.COMM.delFlg=="Y")THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Record is Deleted! Only Inquiry is Allowed.^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		IF(EQT.COMM.delFlg=="0")THEN
		#{
			IF(sv_f==0)THEN
			#{
				sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
				sv_t= urhk_SetOrbOut("Error_1|ERR^No Record Exists for the Selected Commission Code.^Go")
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	sv_m = "recCount|SELECT count(1)"
	sv_m= sv_m +" FROM CUSTOM.C_CPM WHERE bank_id=?SVAR AND COMM_CODE=?SVAR "
	
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + EQT.COMM.commCode
	
	PRINT(sv_m)
	sv_r = urhk_dbselectWithBind(sv_m)
	
	IF (sv_r != 0) THEN
	#{
		sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
		sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		sv_a= BANCS.OUTPARAM.recCount
		PRINT(sv_a)
	#}
	ENDIF
	
	
	IF( (EQT.COMM.funcCode=="M") OR (EQT.COMM.funcCode=="X") OR (EQT.COMM.funcCode=="V") OR (EQT.COMM.funcCode=="D"))THEN
	#{
		IF((sv_a==0) AND (sv_d== 0))THEN
		#{
			PRINT(sv_d)
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^No Record Exists for the Selected Commission Code.^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		IF((sv_a==0) AND (sv_d!= 0) AND (EQT.COMM.funcCode=="D"))THEN
		#{
			PRINT(sv_d)
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Cannot Delete the Record. Verification is Pending !!!^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		IF((sv_a!=0) AND (sv_d== 0))THEN
		#{
			GOTO FETCHDATA
		#}
		ENDIF
		
		sv_k = "lchgUserID|SELECT distinct LCHG_USER_ID"
		sv_k= sv_k +" FROM CUSTOM.C_CPM_MOD WHERE bank_id=?SVAR AND COMM_CODE=?SVAR "
		
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + EQT.COMM.commCode
		
		PRINT(sv_k)
		sv_r = urhk_dbselectWithBind(sv_k)
		
		IF (sv_r != 0) THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data!^Go")
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			EQT.COMM.lchgUserID= BANCS.OUTPARAM.lchgUserID
			PRINT(EQT.COMM.lchgUserID)
			
			IF((sv_d!=0) AND (EQT.COMM.funcCode=="V") AND (EQT.COMM.lchgUserID==BANCS.STDIN.userId))THEN
			#{
				sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
				sv_t= urhk_SetOrbOut("Error_1|ERR^Enterer cannot verify the Record !!!^Go")
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			
			IF((sv_d!=0)AND (EQT.COMM.funcCode!="V")) THEN
			#{
				IF(EQT.COMM.lchgUserID!=BANCS.STDIN.userId)THEN
				#{
					sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
					sv_t= urhk_SetOrbOut("Error_1|ERR^Only Enterer can Modify/Cancel the Record.^Go")
					GOTO ENDOFSCRIPT
					#}
				ELSE
					#{
					EQT.COMM.queryTbl="_MOD"
					GOTO FETCHDATA
				#}
				ENDIF
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	IF((sv_a==0) AND (EQT.COMM.funcCode=="M") AND (sv_d!=0))THEN
	#{
		EQT.COMM.queryTbl="_MOD"
	#}
	ENDIF
	
	IF( (sv_a==0)) THEN
	#{
		IF((EQT.COMM.funcCode=="I")) THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^No Record Exists for the Selected Commission Code!^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	IF(EQT.COMM.funcCode=="A")THEN
	#{
		
		IF(sv_a!= 0)THEN
		#{
			PRINT(sv_a)
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Record Already Exists for the Selected Commission Code!^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		IF(sv_d!= 0)THEN
		#{
			PRINT(sv_d)
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Record Already Present. Verification Pending !!!^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	FETCHDATA:
		CALL("commntFetchData.scr")
		IF (EQT.COMM.successOrFailure!= "F") THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
			#}
		ELSE
			#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			sv_u = urhk_SetOrbOut("Error_1|ERR^"+EQT.COMM.scrMsgFields)
		#}
		ENDIF
		#TRACE OFF
		
	ENDOFSCRIPT:
		DELETECLASS("EQT", "COMM")
		DELETEREP("EQT")
		
		EXITSCRIPT
		TRACE OFF
END-->
