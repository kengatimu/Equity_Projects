#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: ctodadp014.scr
# Type: NA|default
# Description: ctodadp014.scr
# Author:
# Date:
# Modification Log:
#============================================================

#----------------------------------------------------------------------------------------------------------
# Filename              : ctodadp014.scr
# Author                : Bharath Reddy B
# Description           : This script is used to find amount available for TOD
# Input                 :
# Output                : None
# Called Script         : inquirydetails_custom.jsp
# Calling Script        : None
#
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----        ----------         --------------    ----------------        ---------
#  1           07-08-2013          Bharath            Original Version          1.0
#-----------------------------------------------------------------------------------------------------------

<--START
CHECKSUM="595615a34e682cbd8efa34bfb16c18f68daadf4983721f966138ef93021cfb7e"
	TRACE ON
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST", "CTODA") == 0) THEN
	#{
		CREATECLASS("CUST", "CTODA", 5)
	#}
	ENDIF
	
	#-----------------------------------------------------------------------
	#Getting values from frondend
	#-------------------------------------------------------------------------
	CUST.CTODA.errorMsg = ""
	CUST.CTODA.opt		= "01"
	CUST.CTODA.acctId 	= TRIM(BANCS.INPUT.acctId)
	PRINT(CUST.CTODA.acctId)
	
	#-----------------------------------------------------------------------
	#Getting tod available amount
	#-------------------------------------------------------------------------
	sv_s = "message|SELECT CUSTOM.CHK_CONTRACT(?SVAR,?SVAR,?SVAR) "
	sv_s = sv_s + " FROM  DUAL "
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.CTODA.acctId + "|" + CUST.CTODA.opt + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_d= urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF (sv_d == 0) THEN
	#{
		CUST.CTODA.message = BANCS.OUTPARAM.message
		PRINT(CUST.CTODA.message)
		IF (CUST.CTODA.message != "") THEN
		#{
			sv_u = urhk_SetOrbOut("Message|" + CUST.CTODA.message)
			#}
		ELSE
			#{
			CUST.CTODA.errorMsg = "CHK_CONTRACT FUNCTION returned null"
			PRINT(CUST.CTODA.errorMsg)
			sv_u = urhk_SetOrbOut("Message| ")
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.CTODA.errorMsg = "Error in finding TOD amount"
		PRINT(CUST.CTODA.errorMsg)
		sv_u = urhk_SetOrbOut("Message| ")
	#}
	ENDIF
	
	EXITSCRIPT
END-->
