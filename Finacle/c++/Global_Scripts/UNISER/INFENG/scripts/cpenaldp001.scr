#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cpenaldp001.scr
# Type: NA|default
# Description: validate SCRIPT OF CPENAL MENU
# Author: Sridevi
# Date: 7/16/19 16:27 PM
# Modification Log:
#============================================================

<--START

	TRACE ON
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CPNL Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CPNL")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CPNL",5)
	#}
	ENDIF
	
	#==========================================================*
	# Intilializing the Variables
	#==========================================================*
	

	IF(FIELDEXISTS(BANCS.INPUT.funcCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.funcCode) != "")THEN
		#{
			CUST.CPNL.funcCode = TOUPPER(TRIM(BANCS.INPUT.funcCode))
			PRINT(CUST.CPNL.funcCode)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.acctId))THEN
	#{
		IF(TRIM(BANCS.INPUT.acctId) != "")THEN
		#{
			CUST.CPNL.acctId = TOUPPER(TRIM(BANCS.INPUT.acctId))
			PRINT(CUST.CPNL.acctId)
		#}
		ENDIF
	#}
	ENDIF


	#############################################################
	#Account Id validation
	#############################################################

		sv_s = "recCount |select count(*) from tbaadm.gam where foracid = ?SVAR and bank_id = ?SVAR and del_flg ='N' and entity_cre_flg ='Y' and schm_type IN  ('ODA','CAA')"
		BANCS.INPARAM.BINDVARS = CUST.CPNL.acctId  + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)

		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)

		IF(sv_d != 0)THEN
		#{
			CUST.CPNL.errorFlg ="Y"
			CUST.CPNL.errorMsg ="Error in Database Operation!!"
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			sv_u = urhk_SetOrbOut("Error_1|Error^"+CUST.CPNL.errorMsg+"^acctId")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT1
		#}
		ELSE
		#{
			CUST.CPNL.recCount =  BANCS.OUTPARAM.recCount
			PRINT(CUST.CPNL.recCount)

			IF(CUST.CPNL.recCount == "0")THEN
			#{
					CUST.CPNL.errorFlg ="Y"
					CUST.CPNL.errorMsg ="Invalid Account Id: "+CUST.CPNL.acctId + " or not an Overdraft Account."
					sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
					sv_u = urhk_SetOrbOut("Error_1|Error^"+CUST.CPNL.errorMsg+"^acctId")
					PRINT(sv_u)
					GOTO ENDOFSCRIPT1
			#}
			ENDIF
		#}
		ENDIF
		
	ENDOFSCRIPT1:
		
END-->