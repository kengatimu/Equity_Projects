##########################################################################################
# Source Name           : cieclgdp006.scr
# Date                  : 04-Mar-2013
# Description           : Set Num Searcher
# Author                : Mohan Raghu Arudi
# Modification History  :
# <Serial No.>  <Date>        <Author Name>             <Description>
# ------------  -----------   -----------------         --------------
#      1        04-Mar-2013   Mohan Raghu Arudi         Base Version
###########################################################################################

<--START
CHECKSUM="96b1cb9ae4c4bcc684098d7d55501e6800ee4153f63b14f0ffb3874aa9afc851"
	TRACE ON
	###########################################################################################
	# CREATE THE NECESSARY REPOSITORY AND CLASSES
	###########################################################################################
	
	IF(REPEXISTS("CUST") == 0 ) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF(CLASSEXISTS("CUST","CIECLG") == 0) THEN
	#{
		CREATECLASS("CUST","CIECLG",5)
	#}
	ENDIF
	
	CUST.CIECLG.recordCount	= "0"
	CUST.CIECLG.funcCode = ""
	CUST.CIECLG.zoneCode = ""
	CUST.CIECLG.zoneDate = ""
	CUST.CIECLG.setNum = ""
	
	###########################################################################################
	# Get the input values from front end
	###########################################################################################
	
	IF(FIELDEXISTS(BANCS.INPUT.funcCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.funcCode) != "")THEN
		#{
			CUST.CIECLG.funcCode = TOUPPER(TRIM(BANCS.INPUT.funcCode))
			print(CUST.CIECLG.funcCode)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.zoneCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.zoneCode) != "")THEN
		#{
			CUST.CIECLG.zoneCode = TOUPPER(TRIM(BANCS.INPUT.zoneCode))
			print(CUST.CIECLG.zoneCode)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.zoneDate))THEN
	#{
		IF(TRIM(BANCS.INPUT.zoneDate) != "")THEN
		#{
			CUST.CIECLG.zoneDate = TOUPPER(TRIM(BANCS.INPUT.zoneDate))
			print(CUST.CIECLG.zoneDate)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.setNum))THEN
	#{
		IF(TRIM(BANCS.INPUT.setNum) != "")THEN
		#{
			CUST.CIECLG.setNum = TOUPPER(TRIM(BANCS.INPUT.setNum))
			print(CUST.CIECLG.setNum)
		#}
		ENDIF
	#}
	ENDIF
	
	###########################################################################################
	# Record Count
	###########################################################################################
	
	IF(CUST.CIECLG.funcCode == "M")THEN
	#{
		sv_s = "recordCount|SELECT COUNT(*) FROM CUSTOM.IECD"
		sv_s = sv_s + " WHERE ZONE_CODE LIKE ?SVAR"
		sv_s = sv_s + " AND ZONE_DATE = TO_DATE(?SVAR,'DD-MM-YYYY')"
		sv_s = sv_s + " AND SOL_ID = ?SVAR"
		sv_s = sv_s + " AND TRIM(SET_NUM) LIKE ?SVAR"
		sv_s = sv_s + " AND BANK_ID = ?SVAR"
		sv_s = sv_s + " AND STATUS_FLG IN ('A')"
		sv_s = sv_s + " AND REVERSE_FLG = 'N'"
		print(sv_s)
		
		BANCS.INPARAM.BINDVARS = ""
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneCode + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneDate + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.mySolId + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.setNum + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.contextBankId
		print(BANCS.INPARAM.BINDVARS)
	#}
	ENDIF
	
	IF(CUST.CIECLG.funcCode == "V")THEN
	#{
		sv_s = "recordCount|SELECT COUNT(*) FROM CUSTOM.IECD"
		sv_s = sv_s + " WHERE ZONE_CODE LIKE ?SVAR"
		sv_s = sv_s + " AND ZONE_DATE = TO_DATE(?SVAR,'DD-MM-YYYY')"
		sv_s = sv_s + " AND SOL_ID = ?SVAR"
		sv_s = sv_s + " AND TRIM(SET_NUM) LIKE ?SVAR"
		sv_s = sv_s + " AND BANK_ID = ?SVAR"
		sv_s = sv_s + " AND STATUS_FLG IN ('A')"
		sv_s = sv_s + " AND REVERSE_FLG = 'N'"
		print(sv_s)
		
		BANCS.INPARAM.BINDVARS = ""
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneCode + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneDate + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.mySolId + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.setNum + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.contextBankId
		print(BANCS.INPARAM.BINDVARS)
	#}
	ENDIF
	
	IF(CUST.CIECLG.funcCode == "D")THEN
	#{
		sv_s = "recordCount|SELECT COUNT(*) FROM CUSTOM.IECD"
		sv_s = sv_s + " WHERE ZONE_CODE LIKE ?SVAR"
		sv_s = sv_s + " AND ZONE_DATE = TO_DATE(?SVAR,'DD-MM-YYYY')"
		sv_s = sv_s + " AND SOL_ID = ?SVAR"
		sv_s = sv_s + " AND TRIM(SET_NUM) LIKE ?SVAR"
		sv_s = sv_s + " AND BANK_ID = ?SVAR"
		sv_s = sv_s + " AND STATUS_FLG IN ('A')"
		sv_s = sv_s + " AND REVERSE_FLG = 'N'"
		print(sv_s)
		
		BANCS.INPARAM.BINDVARS = ""
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneCode + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneDate + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.mySolId + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.setNum + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.contextBankId
		print(BANCS.INPARAM.BINDVARS)
	#}
	ENDIF
	
	IF(CUST.CIECLG.funcCode == "R")THEN
	#{
		sv_s = "recordCount|SELECT COUNT(*) FROM CUSTOM.IECD"
		sv_s = sv_s + " WHERE ZONE_CODE LIKE ?SVAR"
		sv_s = sv_s + " AND ZONE_DATE = TO_DATE(?SVAR,'DD-MM-YYYY')"
		sv_s = sv_s + " AND SOL_ID = ?SVAR"
		sv_s = sv_s + " AND TRIM(SET_NUM) LIKE ?SVAR"
		sv_s = sv_s + " AND BANK_ID = ?SVAR"
		sv_s = sv_s + " AND STATUS_FLG IN ('V')"
		sv_s = sv_s + " AND REVERSE_FLG = 'N'"
		print(sv_s)
		
		BANCS.INPARAM.BINDVARS = ""
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneCode + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneDate + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.mySolId + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.setNum + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.contextBankId
		print(BANCS.INPARAM.BINDVARS)
	#}
	ENDIF
	
	IF(CUST.CIECLG.funcCode == "I")THEN
	#{
		sv_s = "recordCount|SELECT COUNT(*) FROM CUSTOM.IECD"
		sv_s = sv_s + " WHERE ZONE_CODE LIKE ?SVAR"
		sv_s = sv_s + " AND ZONE_DATE = TO_DATE(?SVAR,'DD-MM-YYYY')"
		sv_s = sv_s + " AND SOL_ID = ?SVAR"
		sv_s = sv_s + " AND TRIM(SET_NUM) LIKE ?SVAR"
		sv_s = sv_s + " AND BANK_ID = ?SVAR"
		sv_s = sv_s + " AND STATUS_FLG IN ('A','V','R','O')"
		sv_s = sv_s + " AND REVERSE_FLG = 'N'"
		print(sv_s)
		
		BANCS.INPARAM.BINDVARS = ""
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneCode + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneDate + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.mySolId + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.setNum + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.contextBankId
		print(BANCS.INPARAM.BINDVARS)
	#}
	ENDIF
	
	IF(CUST.CIECLG.funcCode == "O")THEN
	#{
		sv_s = "recordCount|SELECT COUNT(*) FROM CUSTOM.IECD"
		sv_s = sv_s + " WHERE ZONE_CODE LIKE ?SVAR"
		sv_s = sv_s + " AND ZONE_DATE = TO_DATE(?SVAR,'DD-MM-YYYY')"
		sv_s = sv_s + " AND SOL_ID = ?SVAR"
		sv_s = sv_s + " AND TRIM(SET_NUM) LIKE ?SVAR"
		sv_s = sv_s + " AND BANK_ID = ?SVAR"
		sv_s = sv_s + " AND STATUS_FLG IN ('R')"
		sv_s = sv_s + " AND REVERSE_FLG = 'N'"
		print(sv_s)
		
		BANCS.INPARAM.BINDVARS = ""
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneCode + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneDate + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.mySolId + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.setNum + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.contextBankId
		print(BANCS.INPARAM.BINDVARS)
	#}
	ENDIF
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	IF(sv_d == 0)THEN
	#{
		CUST.CIECLG.recordCount = BANCS.OUTPARAM.recordCount
		PRINT(CUST.CIECLG.recordCount)
	#}
	ENDIF
	
	sv_u = urhk_SetOrbOut("recordCount|"+CUST.CIECLG.recordCount)
	print(sv_u)
	
	###########################################################################################
	# Records - Cursor
	###########################################################################################
	
	IF(CUST.CIECLG.funcCode == "M")THEN
	#{
		sv_s = "setNum,zoneCode,zoneDate,status|SELECT SET_NUM,ZONE_CODE,TO_CHAR(ZONE_DATE,'DD-MM-YYYY'),STATUS_FLG FROM CUSTOM.IECD"
		sv_s = sv_s + " WHERE ZONE_CODE LIKE ?SVAR"
		sv_s = sv_s + " AND ZONE_DATE = TO_DATE(?SVAR,'DD-MM-YYYY')"
		sv_s = sv_s + " AND SOL_ID = ?SVAR"
		sv_s = sv_s + " AND TRIM(SET_NUM) LIKE ?SVAR"
		sv_s = sv_s + " AND BANK_ID = ?SVAR"
		sv_s = sv_s + " AND STATUS_FLG IN ('A')"
		sv_s = sv_s + " AND REVERSE_FLG = 'N'"
		sv_s = sv_s + " ORDER BY 1"
		print(sv_s)
		
		BANCS.INPARAM.BINDVARS = ""
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneCode + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneDate + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.mySolId + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.setNum + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.contextBankId
		print(BANCS.INPARAM.BINDVARS)
	#}
	ENDIF
	
	IF(CUST.CIECLG.funcCode == "V")THEN
	#{
		sv_s = "setNum,zoneCode,zoneDate,status|SELECT SET_NUM,ZONE_CODE,TO_CHAR(ZONE_DATE,'DD-MM-YYYY'),STATUS_FLG FROM CUSTOM.IECD"
		sv_s = sv_s + " WHERE ZONE_CODE LIKE ?SVAR"
		sv_s = sv_s + " AND ZONE_DATE = TO_DATE(?SVAR,'DD-MM-YYYY')"
		sv_s = sv_s + " AND SOL_ID = ?SVAR"
		sv_s = sv_s + " AND TRIM(SET_NUM) LIKE ?SVAR"
		sv_s = sv_s + " AND BANK_ID = ?SVAR"
		sv_s = sv_s + " AND STATUS_FLG IN ('A')"
		sv_s = sv_s + " AND REVERSE_FLG = 'N'"
		sv_s = sv_s + " ORDER BY 1"
		print(sv_s)
		
		BANCS.INPARAM.BINDVARS = ""
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneCode + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneDate + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.mySolId + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.setNum + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.contextBankId
		print(BANCS.INPARAM.BINDVARS)
	#}
	ENDIF
	
	IF(CUST.CIECLG.funcCode == "D")THEN
	#{
		sv_s = "setNum,zoneCode,zoneDate,status|SELECT SET_NUM,ZONE_CODE,TO_CHAR(ZONE_DATE,'DD-MM-YYYY'),STATUS_FLG FROM CUSTOM.IECD"
		sv_s = sv_s + " WHERE ZONE_CODE LIKE ?SVAR"
		sv_s = sv_s + " AND ZONE_DATE = TO_DATE(?SVAR,'DD-MM-YYYY')"
		sv_s = sv_s + " AND SOL_ID = ?SVAR"
		sv_s = sv_s + " AND TRIM(SET_NUM) LIKE ?SVAR"
		sv_s = sv_s + " AND BANK_ID = ?SVAR"
		sv_s = sv_s + " AND STATUS_FLG IN ('A')"
		sv_s = sv_s + " AND REVERSE_FLG = 'N'"
		sv_s = sv_s + " ORDER BY 1"
		print(sv_s)
		
		BANCS.INPARAM.BINDVARS = ""
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneCode + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneDate + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.mySolId + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.setNum + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.contextBankId
		print(BANCS.INPARAM.BINDVARS)
	#}
	ENDIF
	
	IF(CUST.CIECLG.funcCode == "R")THEN
	#{
		sv_s = "setNum,zoneCode,zoneDate,status|SELECT SET_NUM,ZONE_CODE,TO_CHAR(ZONE_DATE,'DD-MM-YYYY'),STATUS_FLG FROM CUSTOM.IECD"
		sv_s = sv_s + " WHERE ZONE_CODE LIKE ?SVAR"
		sv_s = sv_s + " AND ZONE_DATE = TO_DATE(?SVAR,'DD-MM-YYYY')"
		sv_s = sv_s + " AND SOL_ID = ?SVAR"
		sv_s = sv_s + " AND TRIM(SET_NUM) LIKE ?SVAR"
		sv_s = sv_s + " AND BANK_ID = ?SVAR"
		sv_s = sv_s + " AND STATUS_FLG IN ('V')"
		sv_s = sv_s + " AND REVERSE_FLG = 'N'"
		sv_s = sv_s + " ORDER BY 1"
		print(sv_s)
		
		BANCS.INPARAM.BINDVARS = ""
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneCode + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneDate + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.mySolId + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.setNum + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.contextBankId
		print(BANCS.INPARAM.BINDVARS)
		
	#}
	ENDIF
	
	IF(CUST.CIECLG.funcCode == "I")THEN
	#{
		sv_s = "setNum,zoneCode,zoneDate,status|SELECT SET_NUM,ZONE_CODE,TO_CHAR(ZONE_DATE,'DD-MM-YYYY'),STATUS_FLG FROM CUSTOM.IECD"
		sv_s = sv_s + " WHERE ZONE_CODE LIKE ?SVAR"
		sv_s = sv_s + " AND ZONE_DATE = TO_DATE(?SVAR,'DD-MM-YYYY')"
		sv_s = sv_s + " AND SOL_ID = ?SVAR"
		sv_s = sv_s + " AND TRIM(SET_NUM) LIKE ?SVAR"
		sv_s = sv_s + " AND BANK_ID = ?SVAR"
		sv_s = sv_s + " AND STATUS_FLG IN ('A','V','R','O')"
		sv_s = sv_s + " AND REVERSE_FLG = 'N'"
		sv_s = sv_s + " ORDER BY 1"
		print(sv_s)
		
		BANCS.INPARAM.BINDVARS = ""
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneCode + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneDate + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.mySolId + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.setNum + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.contextBankId
		print(BANCS.INPARAM.BINDVARS)
		
	#}
	ENDIF
	
	IF(CUST.CIECLG.funcCode == "O")THEN
	#{
		sv_s = "setNum,zoneCode,zoneDate,status|SELECT SET_NUM,ZONE_CODE,TO_CHAR(ZONE_DATE,'DD-MM-YYYY'),STATUS_FLG FROM CUSTOM.IECD"
		sv_s = sv_s + " WHERE ZONE_CODE LIKE ?SVAR"
		sv_s = sv_s + " AND ZONE_DATE = TO_DATE(?SVAR,'DD-MM-YYYY')"
		sv_s = sv_s + " AND SOL_ID = ?SVAR"
		sv_s = sv_s + " AND TRIM(SET_NUM) LIKE ?SVAR"
		sv_s = sv_s + " AND BANK_ID = ?SVAR"
		sv_s = sv_s + " AND STATUS_FLG IN ('R')"
		sv_s = sv_s + " AND REVERSE_FLG = 'N'"
		sv_s = sv_s + " ORDER BY 1"
		print(sv_s)
		
		BANCS.INPARAM.BINDVARS = ""
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneCode + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.zoneDate + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.mySolId + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.CIECLG.setNum + "%" + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.contextBankId
		print(BANCS.INPARAM.BINDVARS)
		
	#}
	ENDIF
	
	sv_c = urhk_dbCursorOpenWithBind(sv_s)
	print(sv_c)
	
	IF(sv_c != 0)THEN
	#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_n = BANCS.OUTPARAM.DB_CURSOR_NUMBER
	print(sv_n)
	
	sv_f = urhk_dbCursorFetch(sv_n)
	print(sv_f)
	
	sv_i = 1
	
	WHILE(sv_f == 0)
	#{
		CUST.CIECLG.setNum = BANCS.OUTPARAM.setNum
		PRINT(CUST.CIECLG.setNum)
		CUST.CIECLG.zoneCode = BANCS.OUTPARAM.zoneCode
		PRINT(CUST.CIECLG.zoneCode)
		CUST.CIECLG.zoneDate = BANCS.OUTPARAM.zoneDate
		PRINT(CUST.CIECLG.zoneDate)
		CUST.CIECLG.status = BANCS.OUTPARAM.status
		PRINT(CUST.CIECLG.status)
		
		sv_s = "setNum_" + Format$(sv_i,"%d") + "|" + CUST.CIECLG.setNum
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_s = "zoneCode_" + Format$(sv_i,"%d") + "|" + CUST.CIECLG.zoneCode
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_s = "zoneDate_" + Format$(sv_i,"%d") + "|" + CUST.CIECLG.zoneDate
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_s = "status_" + Format$(sv_i,"%d") + "|" + CUST.CIECLG.status
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_i = sv_i + 1
		
		sv_f = urhk_dbCursorFetch(sv_n)
		print(sv_f)
	#}
	DO
	
	sv_c = urhk_dbCursorClose(sv_n)
	print(sv_c)
	
	
	################################################################################
	# End of script
	################################################################################
	
	ENDOFSCRIPT:
		TRACE OFF
END-->
