#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: ubpdp045.scr
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START
CHECKSUM="797890ed6f092b87b23ad6e3fd8615fd246204056b57ce2ba498575151b8748a"
	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp045.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called for HBLRG menu from hblrgdtl_custom.js
	# Called Script         :
	# Calling script        :
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           24-07-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	#========================================================
	# Check whether CUST Repository exists else create it
	#========================================================
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	#========================================================
	# Check whether CBLREG Class exists else create it
	#========================================================
	IF(CLASSEXISTS("CUST","CBLREG") == 0) THEN
	#{
		CREATECLASS ("CUST","CBLREG",5)
	#}
	ENDIF
	
	CUST.CBLREG.errorFlg="N"
	CUST.CBLREG.errorMsg=" "
	CUST.CBLREG.billerName=" "
	IF(FIELDEXISTS(BANCS.INPUT.cifId))THEN
	#{
		IF(TRIM(BANCS.INPUT.cifId) != "")THEN
		#{
			CUST.CBLREG.cifId=BANCS.INPUT.cifId
			PRINT(CUST.CBLREG.cifId)
			#}
			
		#}
		ENDIF
	#}
	ENDIF
	BANCS.INPARAM.BINDVARS=CUST.CBLREG.cifId+"|Y|N|"+BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_s = "count1|"
	sv_s = sv_s + " SELECT COUNT(1)"
	sv_s = sv_s + " FROM TBAADM.CMG"
	#sv_s = sv_s + " WHERE CUST_ID =?SVAR"
	sv_s = sv_s + " WHERE CIF_ID =?SVAR"
	sv_s = sv_s + " AND ENTITY_CRE_FLG =?SVAR"
	sv_s = sv_s + " AND DEL_FLG =?SVAR"
	sv_s = sv_s + " AND BANK_ID =?SVAR"
	PRINT(sv_s)
	sv_d =  urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF (sv_d == 0 ) THEN
	#{
		CUST.CBLREG.count1 = BANCS.OUTPARAM.count1
		IF(CUST.CBLREG.count1=="0")THEN
		#{
			CUST.CBLREG.errorFlg="Y"
			CUST.CBLREG.errorMsg="Enter a valid CIF ID"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	BANCS.INPARAM.BINDVARS=CUST.CBLREG.cifId+"|Y|N|"+ BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_s = "billerName|"
	sv_s = sv_s + " SELECT CUST_NAME"
	sv_s = sv_s + " FROM TBAADM.CMG"
	#sv_s = sv_s + " WHERE CUST_ID =?SVAR"
	sv_s = sv_s + " WHERE CIF_ID =?SVAR"
	sv_s = sv_s + " AND ENTITY_CRE_FLG =?SVAR"
	sv_s = sv_s + " AND DEL_FLG =?SVAR"
	sv_s = sv_s + " AND BANK_ID =?SVAR"
	PRINT(sv_s)
	sv_d =  urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF (sv_d == 0 ) THEN
	#{
		CUST.CBLREG.billerName = BANCS.OUTPARAM.billerName
	#}
	ENDIF
	
	ENDOFSCRIPT:
		
		sv_s = "errorFlg|" + CUST.CBLREG.errorFlg
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "errorMsg|" + CUST.CBLREG.errorMsg
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "departCode|" + CUST.CBLREG.billerName
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
END-->
