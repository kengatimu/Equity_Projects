#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: getEndPoint.scr
# Type: NA|default
# Description: InterEntity - To Get EndPoint URL
# Author: Balaji_P05
# Date: 3/25/20 4:09 PM
# Modification Log:
#============================================================

<--START
	# Script body goes here
	sv_s = ""
	sv_s = "boxName|"
	sv_s = sv_s + " SELECT BOX_NAME"
	sv_s = sv_s + " FROM custom.Inter_entity "
	sv_s = sv_s + " WHERE ENTITY_ID = ?SVAR"
	sv_s = sv_s + " AND DEL_FLG != 'Y'"
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d == 0)THEN
	#{
		PRINT(BANCS.OUTPARAM.boxName)
		sv_b= BANCS.OUTPARAM.boxName
		
		sv_t = ""
		IF(FIELDEXISTS(CUST.CIETXN.bankId ))THEN
		#{
			IF(TRIM(CUST.CIETXN.bankId ) != "")THEN
			#{
				sv_t=CUST.CIETXN.bankId
			#}
			ENDIF
		#}
		ENDIF
		
		IF(FIELDEXISTS(CUST.INTERFACE.bankID ))THEN
		#{
			IF(TRIM(CUST.INTERFACE.bankID ) != "")THEN
			#{
				sv_t = CUST.INTERFACE.bankID
			#}
			ENDIF
		#}
		ENDIF
		
		IF(sv_t !="" OR sv_t!=" ")THEN
		#{
			sv_s = "endPointUrl|"
			sv_s = sv_s + " SELECT END_POINT_URL"
			sv_s = sv_s + " FROM custom.Inter_entity "
			sv_s = sv_s + " WHERE ENTITY_ID = ?SVAR"
			sv_s = sv_s + " AND DEL_FLG != 'Y'"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = sv_t
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d == 0)THEN
			#{
				CUST.CIETXN.endPointUrl = BANCS.OUTPARAM.endPointUrl
				PRINT(CUST.CIETXN.endPointUrl)
				#}
			ELSE
				#{
				CUST.CIETXN.errorFlg = "Y"
				CUST.CIETXN.errorMsg = "Invalid Entity ID"
			#}
			ENDIF
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.CIETXN.errorFlg = "Y"
		CUST.CIETXN.errorMsg = "Invalid Entity ID"
	#}
	ENDIF
	ENDOFSCRIPT:
END-->
