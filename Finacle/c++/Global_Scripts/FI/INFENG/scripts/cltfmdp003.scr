#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cltfmdp003.scr
# Type: NA|default
# Description: validation script for customer Id in CLTFM
# Author: haneesha.kanumuru
# Date: 2/7/18 5:18 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="31f83e3c4943ed090c90b57b9e0a46fac5ea3e5cd19cdb8d6066d2596d73628d"
	# Script body goes here
	TRACE ON
	#==========================================================*
	# create repository
	#==========================================================*
	sv_a = REPEXISTS("CUST")
	IF(sv_a == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	sv_r = CLASSEXISTS("CUST","CLTFM")
	IF (sv_r == 0) THEN
	#{
		CREATECLASS("CUST", "CLTFM", 5)
	#}
	ENDIF
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	
	CUST.CLTFM.acctNum = " "
	CUST.CLTFM.errorFlg = "N"
	CUST.CLTFM.errorMsg = " "
	CUST.CLTFM.cifId = " "
	CUST.CLTFM.count = " "
	IF(FIELDEXISTS(BANCS.INPUT.cifId)) THEN
	#{
		IF(TRIM(BANCS.INPUT.cifId) != "")THEN
		#{
			CUST.CLTFM.cifId= TOUPPER(TRIM(BANCS.INPUT.cifId))
			PRINT(CUST.CLTFM.cifId)
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.CLTFM.errorFlg = "Y"
		CUST.CLTFM.errorMsg = "Invalid Input for fnExecuteScript"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	#	IF(FIELDEXISTS(BANCS.INPUT.acctNum)) THEN
	#	#{
	#		IF(TRIM(BANCS.INPUT.acctNum) != "")THEN
	#		#{
	#			CUST.CLTFM.acctNum= TOUPPER(TRIM(BANCS.INPUT.acctNum))
	#			PRINT(CUST.CLTFM.acctNum)
	#		#}
	#		ENDIF
	#		#}
	#	ELSE
	#		#{
	#		CUST.CLTFM.errorFlg = "Y"
	#		CUST.CLTFM.errorMsg = "Invalid Input for fnExecuteScript"
	#		GOTO ENDOFSCRIPT
	#	#}
	#	ENDIF
	
	
	sv_s = "count|"
	sv_s = sv_s + " SELECT COUNT(*)"
	sv_s = sv_s + " FROM TBAADM.GAM "
	sv_s = sv_s + " WHERE CIF_ID = ?SVAR"
	sv_s = sv_s + " AND BANK_ID = ?SVAR"
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	CUST.CLTFM.count = BANCS.OUTPARAM.count
	PRINT(CUST.CLTFM.count)
	IF( CUST.CLTFM.count ==0 )THEN
	#{
		
		#		CUST.UBAIEREP.setId = BANCS.OUTPARAM.setId
		#		PRINT(CUST.UBAIEREP.setId)
		#		#}
		#	ELSE
		#		#{
		CUST.CLTFM.errorFlg = "Y"
		CUST.CLTFM.errorMsg = "Invalid CIF ID"
		
		
		#}
		
	#}
	ENDIF
	ENDOFSCRIPT:
		sv_u = urhk_SetOrbOut("errorFlg|"+CUST.CLTFM.errorFlg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("errorMsg|"+CUST.CLTFM.errorMsg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("cifId|"+CUST.CLTFM.cifId)
		
		PRINT(sv_u)
		TRACE OFF
END-->
