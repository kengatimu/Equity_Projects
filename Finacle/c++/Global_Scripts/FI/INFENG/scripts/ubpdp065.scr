#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: ubpdp065.scr
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START
CHECKSUM="2073e42814e2966d2d5f960b7dfe621fc576fd56929139bba534efe4eaf00b15"
	# Script body goes here
	TRACE ON
	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp065.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called from hoccm_det1_link.js for fetching set num in
	#                               representment mode
	# Called Script         :
	# Calling Function      :		function fngetSetNum()
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           27-08-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	#========================================================
	# Check whether CUST Repository exists else create it
	#========================================================
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	#========================================================
	# Check whether UBP Class exists else create it
	#========================================================
	sv_r = CLASSEXISTS("CUST","UBP")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","UBP",5)
	#}
	ENDIF
	#========================================================
	# Initialize the variables
	#========================================================
	
	
	CUST.UBP.errorFlg="N"
	CUST.UBP.errorMsg=" "
	
	CUST.UBP.repModSetNum=" "
	CUST.UBP.solId = TRIM(BANCS.STDIN.mySolId)
	print(CUST.UBP.solId)
	CUST.UBP.bankId = TRIM(BANCS.STDIN.contextBankId)
	print(CUST.UBP.bankId)
	#========================================================
	# Validating Ref. Num
	#=======================================================
	
	IF(FIELDEXISTS(BANCS.INPUT.billAcctId))THEN
	#{
		IF(TRIM(BANCS.INPUT.billAcctId) != "")THEN
		#{
			CUST.UBP.critCrAcctNum = TOUPPER(TRIM(BANCS.INPUT.billAcctId))
			PRINT(CUST.UBP.critCrAcctNum)
			
			#}
		ELSE
			#{
			CUST.UBP.critCrAcctNum=" "
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.critCrAcctNum=" "
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.zoneCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.zoneCode) != "")THEN
		#{
			CUST.UBP.zoneCode = TOUPPER(TRIM(BANCS.INPUT.zoneCode))
			PRINT(CUST.UBP.zoneCode)
			
			#}
		ELSE
			#{
			CUST.UBP.zoneCode=" "
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.zoneCode=" "
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.zoneDate))THEN
	#{
		IF(TRIM(BANCS.INPUT.zoneDate) != "")THEN
		#{
			CUST.UBP.zoneDate = TOUPPER(TRIM(BANCS.INPUT.zoneDate))
			PRINT(CUST.UBP.zoneDate)
			
			#}
		ELSE
			#{
			CUST.UBP.zoneDate=" "
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.zoneDate=" "
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.critTranId))THEN
	#{
		IF(TRIM(BANCS.INPUT.critTranId) != "")THEN
		#{
			CUST.UBP.critTranId = TOUPPER(TRIM(BANCS.INPUT.critTranId))
			PRINT(CUST.UBP.critTranId)
			
			#}
		ELSE
			#{
			CUST.UBP.critTranId=" "
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.critTranId=" "
	#}
	ENDIF
	#--------------------------------------------------------------------------
	# selecting noOfChq, totalCrAmt
	#--------------------------------------------------------------------------
	BANCS.INPARAM.BINDVARS = CUST.UBP.zoneCode + "|" + CUST.UBP.critTranId+ "|" + CUST.UBP.zoneDate
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.UBP.solId
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.UBP.bankId
	print(BANCS.INPARAM.BINDVARS)
	sv_s = "repModSetNum|"
	sv_s = sv_s + " SELECT SET_NUM"
	sv_s = sv_s + " FROM TBAADM.OCI "
	sv_s = sv_s + " WHERE CLG_ZONE_CODE = ?SVAR "
	sv_s = sv_s + " AND FREETEXT2 = ?SVAR "
	sv_s = sv_s + " AND CLG_ZONE_DATE = TO_DATE(?SVAR,'DD-MM-YYYY') "
	sv_s = sv_s + " AND SOL_ID = ?SVAR "
	sv_s = sv_s + " AND BANK_ID = ?SVAR "
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	IF(sv_d != 0)THEN
	#{
		# error
		CUST.UBP.errorFlg = "Y"
		CUST.UBP.errorMsg ="Unable to fetch UBP details"
		
		GOTO ENDOFSCRIPT
		
		#}
	ELSE
		#{
		CUST.UBP.repModSetNum = TRIM(BANCS.OUTPARAM.repModSetNum)
		print(CUST.UBP.repModSetNum)
	#}
	ENDIF
	
	ENDOFSCRIPT:
		sv_s = "errorFlg|" + CUST.UBP.errorFlg
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "errorMsg|" + CUST.UBP.errorMsg
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		IF(CUST.UBP.repModSetNum=="")THEN
		#{
			CUST.UBP.repModSetNum=" "
		#}
		ENDIF
		sv_s = "repModSetNum|" + CUST.UBP.repModSetNum
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		
END-->
