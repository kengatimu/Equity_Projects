#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: ubpdp044.scr
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START
CHECKSUM="f86cc52ff34057a797aa0de959bd2531d2072002c89709cd1a96c6456ac72be9"
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp044.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called for fetching error code from CPARAM
	#
	# Called Script         :
	# Calling script        :		ubpdp039.scr
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           16-04-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	PRINT(CUST.UBP.errorCode)
	BANCS.INPARAM.BINDVARS = CUST.UBP.errorCode + "|" + "Y" + "|" + "N"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + "ALL|"+BANCS.STDIN.contextBankId+"|UBP"
	sv_s = "errorDesc|SELECT PARAMETER_VALUE FROM CUSTOM.C_CPARAM_VALUE_TABLE"
	sv_s = sv_s + " WHERE PARAMETER_ID = ?SVAR AND ENTITY_CRE_FLG = ?SVAR"
	sv_s = sv_s + " AND DEL_FLG = ?SVAR AND LEVELCODE_1 = ?SVAR AND BANK_ID=?SVAR AND MODULE_NAME=?SVAR"
	PRINT(sv_s)
	sv_d = urhk_dbselectWithBind(sv_s)
	IF(sv_d == 0) THEN
	#{
		CUST.UBP.errorDesc = BANCS.OUTPARAM.errorDesc
		#}
	ELSE
		#{
		CUST.UBP.errorDesc = "Could not fetch error description: " + CUST.UBP.errorCode
	#}
	ENDIF
	PRINT(CUST.UBP.errorDesc)
	#trace off
END-->
