##########################################################################################
# Source Name           : cierevdp003.scr
# Date                  : 04-Mar-2013
# Description           : Account Balance Details - On click of Explode Icon
# Author                : Mohan Raghu Arudi
# Modification History  :
# <Serial No.>  <Date>        <Author Name>             <Description>
# ------------  -----------   -----------------         --------------
#      1        04-Mar-2013   Mohan Raghu Arudi         Base Version
###########################################################################################
<--START
CHECKSUM="bb81f35230c205f189ecfed3506adbb603546720b3ff4b90cc69b0ade3fd6881"
	TRACE ON
	
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CIEREV Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CIEREV")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CIEREV",5)
	#}
	ENDIF
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	
	CUST.CIEREV.errorFlg = "N"
	CUST.CIEREV.errorMsg = " "
	CUST.CIEREV.acctId = " "
	CUST.CIEREV.entityId = " "
	CUST.CIEREV.solId = " "
	CUST.CIEREV.crncyCode = " "
	CUST.CIEREV.acctName = " "
	CUST.CIEREV.ledgerBal = " "
	CUST.CIEREV.availableBal = " "
	CUST.CIEREV.effAvailableBal = " "
	CUST.CIEREV.freezeStatus = " "
	CUST.CIEREV.freezeReasonCode = " "
	CUST.CIEREV.acctClosed = " "
	CUST.CIEREV.shadowBal = " "
	CUST.CIEREV.acctStatus = " "
	
	IF(FIELDEXISTS(BANCS.INPUT.acctId))THEN
	#{
		IF(TRIM(BANCS.INPUT.acctId) != "")THEN
		#{
			CUST.CIEREV.acctId = TOUPPER(TRIM(BANCS.INPUT.acctId))
			print(CUST.CIEREV.acctId)
			#}
		ELSE
			#{
			CUST.CIEREV.errorFlg = "Y"
			CUST.CIEREV.errorMsg = "Account Id is blank"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.CIEREV.errorFlg = "Y"
		CUST.CIEREV.errorMsg = "Invalid Input for fnExecuteScript"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.entityId))THEN
	#{
		IF(TRIM(BANCS.INPUT.entityId) != "")THEN
		#{
			CUST.CIEREV.entityId = TOUPPER(TRIM(BANCS.INPUT.entityId))
			print(CUST.CIEREV.entityId)
			#}
		ELSE
			#{
			CUST.CIEREV.errorFlg = "Y"
			CUST.CIEREV.errorMsg = "Entity Id is blank"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.CIEREV.errorFlg = "Y"
		CUST.CIEREV.errorMsg = "Invalid Input for fnExecuteScript"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	IF(CUST.CIEREV.entityId != BANCS.STDIN.contextBankId)THEN
	#{
		CALL("cietxndp010.scr")
	#}
	ENDIF
	
	sv_s = CUST.CIEREV.acctId
	print(sv_s)
	sv_u = urhk_getAcctDetailsInRepository(sv_s)
	print(sv_u)
	
	IF(sv_u != 0)THEN
	#{
		CUST.CIEREV.errorFlg = "Y"
		CUST.CIEREV.errorMsg = "Unable to get Account Balance Details"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.AcctId != "")THEN
	#{
		CUST.CIEREV.acctId = BANCS.OUTPARAM.AcctId
		print(CUST.CIEREV.acctId)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.acctSolId != "")THEN
	#{
		CUST.CIEREV.solId = BANCS.OUTPARAM.acctSolId
		print(CUST.CIEREV.solId)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.acctCrncyCode != "")THEN
	#{
		CUST.CIEREV.crncyCode = BANCS.OUTPARAM.acctCrncyCode
		print(CUST.CIEREV.crncyCode)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.acctName != "")THEN
	#{
		CUST.CIEREV.acctName = BANCS.OUTPARAM.acctName
		print(CUST.CIEREV.acctName)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.clearBalance != "")THEN
	#{
		CUST.CIEREV.ledgerBal = BANCS.OUTPARAM.clearBalance
		print(CUST.CIEREV.ledgerBal)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.AvailableAmt != "")THEN
	#{
		CUST.CIEREV.availableBal = BANCS.OUTPARAM.AvailableAmt
		print(CUST.CIEREV.availableBal)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.EffAvailableAmt != "")THEN
	#{
		CUST.CIEREV.effAvailableBal = BANCS.OUTPARAM.EffAvailableAmt
		print(CUST.CIEREV.effAvailableBal)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.freezeCode != "")THEN
	#{
		CUST.CIEREV.freezeStatus = BANCS.OUTPARAM.freezeCode
		print(CUST.CIEREV.freezeStatus)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.freezeReasonCode != "")THEN
	#{
		CUST.CIEREV.freezeReasonCode = BANCS.OUTPARAM.freezeReasonCode
		print(CUST.CIEREV.freezeReasonCode)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.acctCloseflg != "")THEN
	#{
		CUST.CIEREV.acctClosed = BANCS.OUTPARAM.acctCloseflg
		print(CUST.CIEREV.acctClosed)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.unclearBalance != "")THEN
	#{
		CUST.CIEREV.shadowBal = BANCS.OUTPARAM.unclearBalance
		print(CUST.CIEREV.shadowBal)
	#}
	ENDIF
	
	CUST.CIEREV.acid = ""
	sv_s = CUST.CIEREV.acctId
	sv_u = urhk_B2k_ForacidToAcid(sv_s)
	print(sv_u)
	
	IF(sv_u == 0)THEN
	#{
		PRINT(BANCS.OUTPARAM.Acid)
		CUST.CIEREV.acid= BANCS.OUTPARAM.Acid
		PRINT(CUST.CIEREV.acid)
	#}
	ENDIF
	
	IF(CUST.CIEREV.acid != "")THEN
	#{
		BANCS.INPARAM.acid = CUST.CIEREV.acid
		sv_u = urhk_getAcctStatus("")
		print(sv_u)
		
		IF(sv_u == 0)THEN
		#{
			print(BANCS.OUTPARAM.acctStatus)
			CUST.CIEREV.acctStatus = BANCS.OUTPARAM.acctStatus
			PRINT(CUST.CIEREV.acctStatus)
			
			IF(CUST.CIEREV.acctStatus == "A")THEN
			#{
				CUST.CIEREV.acctStatus = "A - Active"
			#}
			ENDIF
			
			IF(CUST.CIEREV.acctStatus == "D")THEN
			#{
				CUST.CIEREV.acctStatus = "D - Dormant"
			#}
			ENDIF
			
			IF(CUST.CIEREV.acctStatus == "I")THEN
			#{
				CUST.CIEREV.acctStatus = "I - Inactive"
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	IF(CUST.CIEREV.entityId != BANCS.STDIN.contextBankId)THEN
	#{
		CUST.CIEREV.entityId = BANCS.STDIN.contextBankId
		CALL("cietxndp010.scr")
	#}
	ENDIF
	
	ENDOFSCRIPT:
		
		sv_u = urhk_SetOrbOut("errorFlg|"+CUST.CIEREV.errorFlg)
		print(sv_u)
		sv_u = urhk_SetOrbOut("errorMsg|"+CUST.CIEREV.errorMsg)
		print(sv_u)
		sv_u = urhk_SetOrbOut("acctId|"+CUST.CIEREV.acctId)
		print(sv_u)
		sv_u = urhk_SetOrbOut("solId|"+CUST.CIEREV.solId)
		print(sv_u)
		sv_u = urhk_SetOrbOut("crncyCode|"+CUST.CIEREV.crncyCode)
		print(sv_u)
		sv_u = urhk_SetOrbOut("acctName|"+CUST.CIEREV.acctName)
		print(sv_u)
		sv_u = urhk_SetOrbOut("ledgerBal|"+CUST.CIEREV.ledgerBal)
		print(sv_u)
		sv_u = urhk_SetOrbOut("availableBal|"+CUST.CIEREV.availableBal)
		print(sv_u)
		sv_u = urhk_SetOrbOut("effAvailableBal|"+CUST.CIEREV.effAvailableBal)
		print(sv_u)
		sv_u = urhk_SetOrbOut("freezeStatus|"+CUST.CIEREV.freezeStatus)
		print(sv_u)
		sv_u = urhk_SetOrbOut("freezeReasonCode|"+CUST.CIEREV.freezeReasonCode)
		print(sv_u)
		sv_u = urhk_SetOrbOut("acctClosed|"+CUST.CIEREV.acctClosed)
		print(sv_u)
		sv_u = urhk_SetOrbOut("shadowBal|"+CUST.CIEREV.shadowBal)
		print(sv_u)
		sv_u = urhk_SetOrbOut("acctStatus|"+CUST.CIEREV.acctStatus)
		print(sv_u)
		
		sv_u = urhk_b2k_printrepos("BANCS")
		print(sv_u)
END-->
