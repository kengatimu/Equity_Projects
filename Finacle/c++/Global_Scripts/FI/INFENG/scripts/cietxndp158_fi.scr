#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cietxndp158_fi.scr
# Type: NA|default
# Description: a
# Author: Balaji_P05
# Date: 4/9/20 2:34 PM
# Modification Log:
#============================================================
<--START
	# Script body goes here
	TRACE ON
	
	CALL("cietxnFiWebService.scr")
	IF(CUST.INTERFACE.errorMsgFlg == "Y")THEN
	#{
		CUST.INTERFACE.recordCount=0
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	PRINT(CUST.INTERFACE.outputData)
	PRINT(CUST.INTERFACE.outputField)
	CUST.INTERFACE.recordCount=CUST.INTERFACE.outputField
	IF(CUST.INTERFACE.outputField != 0)THEN
	#{
		sv_l=CUST.INTERFACE.outputData
		sv_h = GETPOSITION(sv_l,"~")
		sv_i=0
		PRINT(sv_h)
		WHILE(sv_h > 0)
		#{
			sv_i=sv_i+CINT("1")
			sv_j = MID$(sv_l,0,sv_h-1)
			PRINT(sv_j)
			
			("CUST").("INTERFACE").("outputData_" + FORMAT$(sv_i,"%d"))=sv_j
			PRINT(("CUST").("INTERFACE").("outputData_" + FORMAT$(sv_i,"%d")))
			sv_y = STRLEN(sv_l)
			PRINT(sv_y)
			
			sv_l = MID$(sv_l,sv_h,sv_y)
			PRINT(sv_l)
			sv_h = GETPOSITION(sv_l,"~")
			PRINT(sv_h)
		#}
		DO
	#}
	ENDIF
	
	
	ENDOFSCRIPT:
END-->
