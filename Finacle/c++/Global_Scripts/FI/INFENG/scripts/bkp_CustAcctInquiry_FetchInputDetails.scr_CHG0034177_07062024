#----------------------------------------------------------------------------------------------------------
# Filename              :       CustAcctInquiry_FetchInputDetails.scr
# Author                :       Venkatesh T R
# Description           :       This script fetches the values passed in Request XML
# Input                 :       Customer ID, Account ID, National ID, Passport No,User ID
# Output                :       
# Called Script         :       CustAcctInquiry_main.scr
# Calling Script        :       None
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----           -----           --------          ----------------        ---------
#  1           04-03-2012         Venkatesh          Original Version          1.0
#-----------------------------------------------------------------------------------------------------------
<--start
#trace on
	
	
	
	sv_v = "CustAcctInquiry_REQ.AccountID"
	CUST.INTERFACE.Coge = ""
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{
		CUST.INTERFACE.accountID =("BANCS").("INPUT").(sv_v)
		print(CUST.INTERFACE.accountID)
if (BANCS.STDIN.contextBankId == "50")  then
        IF (fieldexists(CUST.INTERFACE.accountID) == 1) THEN
                IF  ( STRLEN(CUST.INTERFACE.accountID) > 16 ) THEN
                        BANCS.INPARAM.iban_number =CUST.INTERFACE.accountID
                        sv_u = urhk_getForacidFromIBANnumber("")
                        PRINT(sv_u)
                        IF(sv_u !=0) THEN
                                CUST.INTERFACE.accountID =""
                        ELSE
                                CUST.INTERFACE.accountID = BANCS.OUTPARAM.foracid
				CUST.INTERFACE.Coge = "Y"
                        ENDIF
                ENDIF
        ENDIF
ENDIF
print(CUST.INTERFACE.accountID)

	#}
	ELSE
	#{
		CUST.INTERFACE.accountID = ""
	#}
        ENDIF
        
        
        sv_v = "CustAcctInquiry_REQ.CustomerID"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{

		CUST.INTERFACE.customerID =("BANCS").("INPUT").(sv_v)
	#}
	ELSE
	#{
		CUST.INTERFACE.customerID = ""
	#}
        ENDIF
        
        sv_v = "CustAcctInquiry_REQ.NationalID"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{

		CUST.INTERFACE.nationalID =("BANCS").("INPUT").(sv_v)
	#}
	ELSE
	#{
		CUST.INTERFACE.nationalID = ""
	#}
        ENDIF
        
        sv_v = "CustAcctInquiry_REQ.OthDocNo"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{

		CUST.INTERFACE.drivingLicNo =("BANCS").("INPUT").(sv_v)
		print(CUST.INTERFACE.drivingLicNo)
	#}
	ELSE
	#{
		CUST.INTERFACE.drivingLicNo = ""
	#}
        ENDIF
	
        
        sv_v = "CustAcctInquiry_REQ.PassportNo"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{

		CUST.INTERFACE.passportNo =("BANCS").("INPUT").(sv_v)
	#}
	ELSE
	#{
		CUST.INTERFACE.passportNo = ""
	#}
        ENDIF
	sv_v = "CustAcctInquiry_REQ.UserID"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{
		
		CUST.INTERFACE.userID =("BANCS").("INPUT").(sv_v)
	#}
	ELSE
	#{
		CUST.INTERFACE.userID = ""
	#}
        ENDIF

	sv_v = "CustAcctInquiry_REQ.BankID"
        IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
        #{
		print("1")
                CUST.INTERFACE.bankID =("BANCS").("INPUT").(sv_v)
        #}
        ELSE
        #{
		print("2")
                CUST.INTERFACE.successOrFailure = "F"
                CUST.INTERFACE.errorCode = "ERR032"
                GOTO ERROREXIT 
        #}
        ENDIF
        sv_v = "CustAcctInquiry_REQ.PhoneNo"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{
		
		CUST.INTERFACE.PhoneNo =("BANCS").("INPUT").(sv_v)
	#}
	ELSE
	#{
		CUST.INTERFACE.PhoneNo = ""
	#}
        ENDIF

        sv_v = "CustAcctInquiry_REQ.ALienID"
        IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
        #{

                CUST.INTERFACE.ALienID =("BANCS").("INPUT").(sv_v)
        #}
        ELSE
        #{
                CUST.INTERFACE.ALienID = ""
        #}
        ENDIF


        
	
	print(CUST.INTERFACE.accountID)
	print(CUST.INTERFACE.customerID)
	print(CUST.INTERFACE.nationalID)
	print(CUST.INTERFACE.passportNo)
	print(CUST.INTERFACE.bankID)
        print(CUST.INTERFACE.PhoneNo)	
	print(CUST.INTERFACE.ALienID)
print(CUST.INTERFACE.drivingLicNo)

IF (CUST.INTERFACE.bankID=="43") THEN
	sv_a = urhk_GetChannelInfo("")
	print(sv_a)
	CUST.INTERFACE.DccId =BANCS.OUTPARAM.DccId
	print(CUST.INTERFACE.DccId)
	IF (CUST.INTERFACE.DccId=="POL") THEN
		CUST.INTERFACE.accountID =trim(CUST.INTERFACE.accountID)
		print(CUST.INTERFACE.accountID)
		CUST.INTERFACE.accountIDlen = STRLEN(CUST.INTERFACE.accountID)
		print(CUST.INTERFACE.accountIDlen)
		IF (cint(CUST.INTERFACE.accountIDlen) >15) THEN
			IF (cint(CUST.INTERFACE.accountIDlen) !=23) THEN
				GOTO ENDOFSCRIPT
			ENDIF
			CUST.INTERFACE.accountIDIBAN=CUST.INTERFACE.accountID
			print(CUST.INTERFACE.accountIDIBAN)
			BANCS.INPARAM.iban_number = CUST.INTERFACE.accountID
			sv_u = urhk_getForacidFromIBANnumber("") 
			PRINT(sv_u)
			IF (sv_u==0) THEN
				PRINT(BANCS.OUTPARAM.foracid)
				IF(BANCS.OUTPARAM.foracid != "") THEN
					CUST.INTERFACE.accountID = BANCS.OUTPARAM.foracid
					print(CUST.INTERFACE.accountID)
					GOTO ENDOFSCRIPT
				ENDIF
			ENDIF
		ENDIF
		####
		IF (FIELDEXISTS(CUST.INTERFACE.accountID) == 1) THEN
			IF (CUST.INTERFACE.accountID != "") THEN
				CUST.INTERFACE.accountID =trim(CUST.INTERFACE.accountID)
				print(CUST.INTERFACE.accountID)
				sv_u = urhk_getAcctDetailsInRepository(CUST.INTERFACE.accountID)
				print(sv_u)
				IF (sv_u == 0) THEN
					CUST.INTERFACE.acctOwnerShip = BANCS.OUTPARAM.acctOwnerShip
					print(CUST.INTERFACE.acctOwnerShip)
					IF (CUST.INTERFACE.acctOwnerShip=="O") THEN
						#-------------------------------------------------------------------------------
						# Fetching all the accounts and its details
						#-------------------------------------------------------------------------------
						CALL("CustAcctInquiry_FetchAccountDetailsGL.scr")
					
						#-------------------------------------------------------------------------------
						# Send Response
						#-------------------------------------------------------------------------------
						CALL("CustAcctInquiry_SendResponseGL.scr")
						CUST.INTERFACE.checkGL = "Y"
						print(CUST.INTERFACE.checkGL)
					ENDIF
				ENDIF
			ENDIF
		ENDIF
		####
	ENDIF
ENDIF
ENDOFSCRIPT:
##trace off
end-->

