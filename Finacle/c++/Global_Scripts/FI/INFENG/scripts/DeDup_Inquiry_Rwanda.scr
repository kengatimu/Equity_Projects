#######################################################################
# Test
#
##################################################################################

<--START
TRACE ON

  #---------------------------------------------------------
        # Create the necessary repos and classes
        #---------------------------------------------------------

        IF (REPEXISTS("CUST") == 0 ) THEN
        #{
                CREATEREP("CUST")
        #}
        ENDIF

        IF (CLASSEXISTS("CUST","DATA") == 0) THEN
        #{
                CREATECLASS("CUST","DATA",5)
        #}
        ENDIF

        #---------------------------------------------------------
        #       Bancs Repository Printing
        #---------------------------------------------------------
        sv_x = urhk_B2k_PrintRepos("BANCS")
        PRINT(sv_x)

        CUST.DATA.succFail="F"
        CUST.DATA.count = "0"
        CUST.DATA.tranId = ""

        IF(FIELDEXISTS(BANCS.INPUT.nationalId)==0) THEN
        #{
                CUST.DATA.nationalId = ""
        #}
        ELSE
        #{
                CUST.DATA.nationalId = BANCS.INPUT.nationalId
                PRINT(CUST.DATA.nationalId)
        #}
        ENDIF

        IF(FIELDEXISTS(BANCS.INPUT.passportId)==0) THEN
        #{
                CUST.DATA.passportId = ""
        #}
        ELSE
        #{
                CUST.DATA.passportId = BANCS.INPUT.passportId
                PRINT(CUST.DATA.passportId)
        #}
        ENDIF

        IF(FIELDEXISTS(BANCS.INPUT.birthCertificate)==0) THEN
        #{
                CUST.DATA.birthCertificate = ""
        #}
        ELSE
        #{
                CUST.DATA.birthCertificate = BANCS.INPUT.birthCertificate
                PRINT(CUST.DATA.birthCertificate)
        #}
        ENDIF

        IF(FIELDEXISTS(BANCS.INPUT.militId)==0) THEN
        #{
                CUST.DATA.militId = ""
        #}
        ELSE
        #{
                CUST.DATA.militId = BANCS.INPUT.militId
                PRINT(CUST.DATA.militId)
        #}
        ENDIF

        IF(FIELDEXISTS(BANCS.INPUT.drvId)==0) THEN
        #{
                CUST.DATA.drvId = ""
        #}
        ELSE
        #{
                CUST.DATA.drvId = BANCS.INPUT.drvId
                PRINT(CUST.DATA.drvId)
        #}
        ENDIF

        IF(FIELDEXISTS(BANCS.INPUT.alnId)==0) THEN
        #{
                CUST.DATA.alnId = ""
        #}
        ELSE
        #{
                CUST.DATA.alnId = BANCS.INPUT.alnId
                PRINT(CUST.DATA.alnId)
        #}
        ENDIF

        IF(FIELDEXISTS(BANCS.INPUT.permitId)==0) THEN
        #{
                CUST.DATA.permitId = ""
        #}
        ELSE
        #{
                CUST.DATA.permitId = BANCS.INPUT.permitId
                PRINT(CUST.DATA.permitId)
        #}
        ENDIF

        IF(FIELDEXISTS(BANCS.INPUT.refId)==0) THEN
        #{
                CUST.DATA.refId = ""
        #}
        ELSE
        #{
                CUST.DATA.refId = BANCS.INPUT.refId
                PRINT(CUST.DATA.refId)
        #}
        ENDIF

        IF(FIELDEXISTS(BANCS.INPUT.unchrId)==0) THEN
        #{
                CUST.DATA.unchrId = ""
        #}
        ELSE
        #{
                CUST.DATA.unchrId = BANCS.INPUT.unchrId
                PRINT(CUST.DATA.unchrId)
        #}
        ENDIF

        IF(FIELDEXISTS(BANCS.INPUT.nsfcrId)==0) THEN
        #{
                CUST.DATA.nsfcrId = ""
        #}
        ELSE
        #{
                CUST.DATA.nsfcrId = BANCS.INPUT.nsfcrId
                PRINT(CUST.DATA.nsfcrId)
        #}
        ENDIF

        IF(FIELDEXISTS(BANCS.INPUT.empId)==0) THEN
        #{
                CUST.DATA.empId = ""
        #}
        ELSE
        #{
                CUST.DATA.empId = BANCS.INPUT.empId
                PRINT(CUST.DATA.empId)
        #}
        ENDIF

        IF(FIELDEXISTS(BANCS.INPUT.maisc)==0) THEN
        #{
                CUST.DATA.maisc = ""
        #}
        ELSE
        #{
                CUST.DATA.maisc = BANCS.INPUT.maisc
                PRINT(CUST.DATA.maisc)
        #}
        ENDIF

        IF((CUST.DATA.nationalId == "") and  (CUST.DATA.passportId == "") and (CUST.DATA.birthCertificate == "") and (CUST.DATA.militId == "") and (CUST.DATA.drvId == "") and (CUST.DATA.alnId == "") and (CUST.DATA.permitId == "") and (CUST.DATA.refId == "") and (CUST.DATA.unchrId == "") and (CUST.DATA.nsfcrId == "") and (CUST.DATA.empId == "") and (CUST.DATA.maisc == ""))THEN
        #{
                sv_u = urhk_SetOrbOut("Failed| Please enter Id's")
                print(sv_u)

                EXITSCRIPT
        #}
        ENDIF

		###############National ID#######################
		
        IF(CUST.DATA.nationalId != "")THEN
        #{
                BANCS.INPARAM.BINDVARS = CUST.DATA.nationalId+ "|" +BANCS.STDIN.contextBankId
                print(BANCS.INPARAM.BINDVARS)

                sv_a = "count|select count(1) from crmuser.entitydocument where referencenumber=?SVAR and "
                sv_a = sv_a + " bank_id =?SVAR and doccode = 'DOC03' "
                print(sv_a)

                sv_d = urhk_dbSelectWithBind(sv_a)
                print(sv_d)

                IF(sv_d == "0")THEN
                #{
                        CUST.DATA.count = BANCS.OUTPARAM.count
                        print(CUST.DATA.count)
                #}
                ELSE
                #{
                        CUST.DATA.count = 0
                #}
                ENDIF

                IF(CINT(CUST.DATA.count) == 0)THEN
                #{
                        BANCS.INPARAM.BINDVARS = MID$(CUST.DATA.nationalId,0,13)+ "|"  +BANCS.STDIN.contextBankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_a = "count|select count(1) from crmuser.entitydocument where referencenumber=?SVAR and "
                        sv_a = sv_a + " bank_id =?SVAR and doccode = 'DOC03' "
                        print(sv_a)

                        sv_d = urhk_dbSelectWithBind(sv_a)
                        print(sv_d)

                        IF(sv_d == "0")THEN
                        #{
                                CUST.DATA.count1 = BANCS.OUTPARAM.count
                                print(CUST.DATA.count)
                        #}
                        ELSE
                        #{
                                CUST.DATA.count1 = 0
                        #}
                        ENDIF

                        IF(CUST.DATA.count1 == 0) THEN
                        #{
                                CUST.DATA.nationalId = MID$(CUST.DATA.nationalId,0,13)
                                print(CUST.DATA.nationalId)

                                BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
                                print(BANCS.INPARAM.BINDVARS)

                                sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                                sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                                sv_a = sv_a + " where referencenumber like '"+CUST.DATA.nationalId+"%' and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='DOC03' "
                                sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                                print(sv_a)
                        #}
                        ELSE
                        #{
                                CUST.DATA.nationalId = MID$(CUST.DATA.nationalId,0,13)
                                print(CUST.DATA.nationalId)

                                BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
                                print(BANCS.INPARAM.BINDVARS)

                                sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                                sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                                sv_a = sv_a + " where referencenumber like '"+CUST.DATA.nationalId+"%'  and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='DOC03' "
                                sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                                print(sv_a)
                        #}
                        ENDIF
                #}
                ELSE
                #{

                        BANCS.INPARAM.BINDVARS = CUST.DATA.nationalId+ "|"  +BANCS.STDIN.contextBankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                        sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                        sv_a = sv_a + " where referencenumber=?SVAR and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='DOC03' "
                        sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                        print(sv_a)
                #}
                ENDIF
        #}
        ENDIF
		################################################
		
		#############Passport###########################
		
		IF(CUST.DATA.passportId != "")THEN
        #{
                BANCS.INPARAM.BINDVARS = CUST.DATA.passportId+ "|" +BANCS.STDIN.contextBankId
                print(BANCS.INPARAM.BINDVARS)

                sv_a = "count|select count(1) from crmuser.entitydocument where referencenumber=?SVAR and "
                sv_a = sv_a + " bank_id =?SVAR and doccode = 'PSSPR' "
                print(sv_a)

                sv_d = urhk_dbSelectWithBind(sv_a)
                print(sv_d)

                IF(sv_d == "0")THEN
                #{
                        CUST.DATA.count = BANCS.OUTPARAM.count
                        print(CUST.DATA.count)
                #}
                ELSE
                #{
                        CUST.DATA.count = 0
                #}
                ENDIF

                IF(CINT(CUST.DATA.count) == 0)THEN
                #{
                        BANCS.INPARAM.BINDVARS = MID$(CUST.DATA.passportId,0,13)+ "|"  +BANCS.STDIN.contextBankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_a = "count|select count(1) from crmuser.entitydocument where referencenumber=?SVAR and "
                        sv_a = sv_a + " bank_id =?SVAR and doccode = 'PSSPR' "
                        print(sv_a)

                        sv_d = urhk_dbSelectWithBind(sv_a)
                        print(sv_d)

                        IF(sv_d == "0")THEN
                        #{
                                CUST.DATA.count1 = BANCS.OUTPARAM.count
                                print(CUST.DATA.count)
                        #}
                        ELSE
                        #{
                                CUST.DATA.count1 = 0
                        #}
                        ENDIF

                        IF(CUST.DATA.count1 == 0) THEN
                        #{
                                CUST.DATA.passportId = MID$(CUST.DATA.passportId,0,13)
                                print(CUST.DATA.passportId)

                                BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
                                print(BANCS.INPARAM.BINDVARS)

                                sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                                sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                                sv_a = sv_a + " where referencenumber like '"+CUST.DATA.passportId+"%' and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='PSSPR' "
                                sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                                print(sv_a)
                        #}
                        ELSE
                        #{
                                CUST.DATA.passportId = MID$(CUST.DATA.passportId,0,13)
                                print(CUST.DATA.passportId)

                                BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
                                print(BANCS.INPARAM.BINDVARS)

                                sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                                sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                                sv_a = sv_a + " where referencenumber like '"+CUST.DATA.passportId+"%'  and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='PSSPR' "
                                sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                                print(sv_a)
                        #}
                        ENDIF
                #}
                ELSE
                #{

                        BANCS.INPARAM.BINDVARS = CUST.DATA.passportId+ "|"  +BANCS.STDIN.contextBankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                        sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                        sv_a = sv_a + " where referencenumber=?SVAR and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='PSSPR' "
                        sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                        print(sv_a)
                #}
                ENDIF
        #}
        ENDIF
		################################################

		##########Birth Certificate#####################
		
				IF(CUST.DATA.birthCertificate != "")THEN
        #{
                BANCS.INPARAM.BINDVARS = CUST.DATA.birthCertificate+ "|" +BANCS.STDIN.contextBankId
                print(BANCS.INPARAM.BINDVARS)

                sv_a = "count|select count(1) from crmuser.entitydocument where referencenumber=?SVAR and "
                sv_a = sv_a + " bank_id =?SVAR and doccode = 'BIRCT' "
                print(sv_a)

                sv_d = urhk_dbSelectWithBind(sv_a)
                print(sv_d)

                IF(sv_d == "0")THEN
                #{
                        CUST.DATA.count = BANCS.OUTPARAM.count
                        print(CUST.DATA.count)
                #}
                ELSE
                #{
                        CUST.DATA.count = 0
                #}
                ENDIF

                IF(CINT(CUST.DATA.count) == 0)THEN
                #{
                        BANCS.INPARAM.BINDVARS = MID$(CUST.DATA.birthCertificate,0,13)+ "|"  +BANCS.STDIN.contextBankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_a = "count|select count(1) from crmuser.entitydocument where referencenumber=?SVAR and "
                        sv_a = sv_a + " bank_id =?SVAR and doccode = 'BIRCT' "
                        print(sv_a)

                        sv_d = urhk_dbSelectWithBind(sv_a)
                        print(sv_d)

                        IF(sv_d == "0")THEN
                        #{
                                CUST.DATA.count1 = BANCS.OUTPARAM.count
                                print(CUST.DATA.count)
                        #}
                        ELSE
                        #{
                                CUST.DATA.count1 = 0
                        #}
                        ENDIF

                        IF(CUST.DATA.count1 == 0) THEN
                        #{
                                CUST.DATA.birthCertificate = MID$(CUST.DATA.birthCertificate,0,13)
                                print(CUST.DATA.birthCertificate)

                                BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
                                print(BANCS.INPARAM.BINDVARS)

                                sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                                sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                                sv_a = sv_a + " where referencenumber like '"+CUST.DATA.birthCertificate+"%' and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='BIRCT' "
                                sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                                print(sv_a)
                        #}
                        ELSE
                        #{
                                CUST.DATA.birthCertificate = MID$(CUST.DATA.birthCertificate,0,13)
                                print(CUST.DATA.birthCertificate)

                                BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
                                print(BANCS.INPARAM.BINDVARS)

                                sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                                sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                                sv_a = sv_a + " where referencenumber like '"+CUST.DATA.birthCertificate+"%'  and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='BIRCT' "
                                sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                                print(sv_a)
                        #}
                        ENDIF
                #}
                ELSE
                #{

                        BANCS.INPARAM.BINDVARS = CUST.DATA.birthCertificate+ "|"  +BANCS.STDIN.contextBankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                        sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                        sv_a = sv_a + " where referencenumber=?SVAR and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='BIRCT' "
                        sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                        print(sv_a)
                #}
                ENDIF
        #}
        ENDIF
		
		##################################################
		
		#################Military Id#######################
		
				IF(CUST.DATA.militId != "")THEN
        #{
                BANCS.INPARAM.BINDVARS = CUST.DATA.militId+ "|" +BANCS.STDIN.contextBankId
                print(BANCS.INPARAM.BINDVARS)

                sv_a = "count|select count(1) from crmuser.entitydocument where referencenumber=?SVAR and "
                sv_a = sv_a + " bank_id =?SVAR and doccode = 'MILTID' "
                print(sv_a)

                sv_d = urhk_dbSelectWithBind(sv_a)
                print(sv_d)

                IF(sv_d == "0")THEN
                #{
                        CUST.DATA.count = BANCS.OUTPARAM.count
                        print(CUST.DATA.count)
                #}
                ELSE
                #{
                        CUST.DATA.count = 0
                #}
                ENDIF

                IF(CINT(CUST.DATA.count) == 0)THEN
                #{
                        BANCS.INPARAM.BINDVARS = MID$(CUST.DATA.militId,0,13)+ "|"  +BANCS.STDIN.contextBankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_a = "count|select count(1) from crmuser.entitydocument where referencenumber=?SVAR and "
                        sv_a = sv_a + " bank_id =?SVAR and doccode = 'MILTID' "
                        print(sv_a)

                        sv_d = urhk_dbSelectWithBind(sv_a)
                        print(sv_d)

                        IF(sv_d == "0")THEN
                        #{
                                CUST.DATA.count1 = BANCS.OUTPARAM.count
                                print(CUST.DATA.count)
                        #}
                        ELSE
                        #{
                                CUST.DATA.count1 = 0
                        #}
                        ENDIF

                        IF(CUST.DATA.count1 == 0) THEN
                        #{
                                CUST.DATA.militId = MID$(CUST.DATA.militId,0,13)
                                print(CUST.DATA.militId)

                                BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
                                print(BANCS.INPARAM.BINDVARS)

                                sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                                sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                                sv_a = sv_a + " where referencenumber like '"+CUST.DATA.militId+"%' and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='MILTID' "
                                sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                                print(sv_a)
                        #}
                        ELSE
                        #{
                                CUST.DATA.militId = MID$(CUST.DATA.militId,0,13)
                                print(CUST.DATA.militId)

                                BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
                                print(BANCS.INPARAM.BINDVARS)

                                sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                                sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                                sv_a = sv_a + " where referencenumber like '"+CUST.DATA.militId+"%'  and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='MILTID' "
                                sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                                print(sv_a)
                        #}
                        ENDIF
                #}
                ELSE
                #{

                        BANCS.INPARAM.BINDVARS = CUST.DATA.militId+ "|"  +BANCS.STDIN.contextBankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                        sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                        sv_a = sv_a + " where referencenumber=?SVAR and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='MILTID' "
                        sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                        print(sv_a)
                #}
                ENDIF
        #}
        ENDIF
		
		#############################################
		
		###############Driving Licence###############
		
				IF(CUST.DATA.drvId != "")THEN
        #{
                BANCS.INPARAM.BINDVARS = CUST.DATA.drvId+ "|" +BANCS.STDIN.contextBankId
                print(BANCS.INPARAM.BINDVARS)

                sv_a = "count|select count(1) from crmuser.entitydocument where referencenumber=?SVAR and "
                sv_a = sv_a + " bank_id =?SVAR and doccode = 'DRVLC' "
                print(sv_a)

                sv_d = urhk_dbSelectWithBind(sv_a)
                print(sv_d)

                IF(sv_d == "0")THEN
                #{
                        CUST.DATA.count = BANCS.OUTPARAM.count
                        print(CUST.DATA.count)
                #}
                ELSE
                #{
                        CUST.DATA.count = 0
                #}
                ENDIF

                IF(CINT(CUST.DATA.count) == 0)THEN
                #{
                        BANCS.INPARAM.BINDVARS = MID$(CUST.DATA.drvId,0,13)+ "|"  +BANCS.STDIN.contextBankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_a = "count|select count(1) from crmuser.entitydocument where referencenumber=?SVAR and "
                        sv_a = sv_a + " bank_id =?SVAR and doccode = 'DRVLC' "
                        print(sv_a)

                        sv_d = urhk_dbSelectWithBind(sv_a)
                        print(sv_d)

                        IF(sv_d == "0")THEN
                        #{
                                CUST.DATA.count1 = BANCS.OUTPARAM.count
                                print(CUST.DATA.count)
                        #}
                        ELSE
                        #{
                                CUST.DATA.count1 = 0
                        #}
                        ENDIF

                        IF(CUST.DATA.count1 == 0) THEN
                        #{
                                CUST.DATA.drvId = MID$(CUST.DATA.drvId,0,13)
                                print(CUST.DATA.drvId)

                                BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
                                print(BANCS.INPARAM.BINDVARS)

                                sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                                sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                                sv_a = sv_a + " where referencenumber like '"+CUST.DATA.drvId+"%' and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='DRVLC' "
                                sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                                print(sv_a)
                        #}
                        ELSE
                        #{
                                CUST.DATA.drvId = MID$(CUST.DATA.drvId,0,13)
                                print(CUST.DATA.drvId)

                                BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
                                print(BANCS.INPARAM.BINDVARS)

                                sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                                sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                                sv_a = sv_a + " where referencenumber like '"+CUST.DATA.drvId+"%'  and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='DRVLC' "
                                sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                                print(sv_a)
                        #}
                        ENDIF
                #}
                ELSE
                #{

                        BANCS.INPARAM.BINDVARS = CUST.DATA.drvId+ "|"  +BANCS.STDIN.contextBankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                        sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                        sv_a = sv_a + " where referencenumber=?SVAR and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='DRVLC' "
                        sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                        print(sv_a)
                #}
                ENDIF
        #}
        ENDIF
		
		###########################################
		
		######Alian ID#######################
			
				IF(CUST.DATA.alnId != "")THEN
        #{
                BANCS.INPARAM.BINDVARS = CUST.DATA.alnId+ "|" +BANCS.STDIN.contextBankId
                print(BANCS.INPARAM.BINDVARS)

                sv_a = "count|select count(1) from crmuser.entitydocument where referencenumber=?SVAR and "
                sv_a = sv_a + " bank_id =?SVAR and doccode = 'ALNID' "
                print(sv_a)

                sv_d = urhk_dbSelectWithBind(sv_a)
                print(sv_d)

                IF(sv_d == "0")THEN
                #{
                        CUST.DATA.count = BANCS.OUTPARAM.count
                        print(CUST.DATA.count)
                #}
                ELSE
                #{
                        CUST.DATA.count = 0
                #}
                ENDIF

                IF(CINT(CUST.DATA.count) == 0)THEN
                #{
                        BANCS.INPARAM.BINDVARS = MID$(CUST.DATA.alnId,0,13)+ "|"  +BANCS.STDIN.contextBankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_a = "count|select count(1) from crmuser.entitydocument where referencenumber=?SVAR and "
                        sv_a = sv_a + " bank_id =?SVAR and doccode = 'ALNID' "
                        print(sv_a)

                        sv_d = urhk_dbSelectWithBind(sv_a)
                        print(sv_d)

                        IF(sv_d == "0")THEN
                        #{
                                CUST.DATA.count1 = BANCS.OUTPARAM.count
                                print(CUST.DATA.count)
                        #}
                        ELSE
                        #{
                                CUST.DATA.count1 = 0
                        #}
                        ENDIF

                        IF(CUST.DATA.count1 == 0) THEN
                        #{
                                CUST.DATA.alnId = MID$(CUST.DATA.alnId,0,13)
                                print(CUST.DATA.alnId)

                                BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
                                print(BANCS.INPARAM.BINDVARS)

                                sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                                sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                                sv_a = sv_a + " where referencenumber like '"+CUST.DATA.alnId+"%' and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='ALNID' "
                                sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                                print(sv_a)
                        #}
                        ELSE
                        #{
                                CUST.DATA.alnId = MID$(CUST.DATA.alnId,0,13)
                                print(CUST.DATA.alnId)

                                BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
                                print(BANCS.INPARAM.BINDVARS)

                                sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                                sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                                sv_a = sv_a + " where referencenumber like '"+CUST.DATA.alnId+"%'  and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='ALNID' "
                                sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                                print(sv_a)
                        #}
                        ENDIF
                #}
                ELSE
                #{

                        BANCS.INPARAM.BINDVARS = CUST.DATA.alnId+ "|"  +BANCS.STDIN.contextBankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                        sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                        sv_a = sv_a + " where referencenumber=?SVAR and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='ALNID' "
                        sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                        print(sv_a)
                #}
                ENDIF
        #}
        ENDIF
		
		##########################
		
		##############Permit ID#######################
		
				IF(CUST.DATA.permitId != "")THEN
        #{
                BANCS.INPARAM.BINDVARS = CUST.DATA.permitId+ "|" +BANCS.STDIN.contextBankId
                print(BANCS.INPARAM.BINDVARS)

                sv_a = "count|select count(1) from crmuser.entitydocument where referencenumber=?SVAR and "
                sv_a = sv_a + " bank_id =?SVAR and doccode = 'PERMT' "
                print(sv_a)

                sv_d = urhk_dbSelectWithBind(sv_a)
                print(sv_d)

                IF(sv_d == "0")THEN
                #{
                        CUST.DATA.count = BANCS.OUTPARAM.count
                        print(CUST.DATA.count)
                #}
                ELSE
                #{
                        CUST.DATA.count = 0
                #}
                ENDIF

                IF(CINT(CUST.DATA.count) == 0)THEN
                #{
                        BANCS.INPARAM.BINDVARS = MID$(CUST.DATA.permitId,0,13)+ "|"  +BANCS.STDIN.contextBankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_a = "count|select count(1) from crmuser.entitydocument where referencenumber=?SVAR and "
                        sv_a = sv_a + " bank_id =?SVAR and doccode = 'PERMT' "
                        print(sv_a)

                        sv_d = urhk_dbSelectWithBind(sv_a)
                        print(sv_d)

                        IF(sv_d == "0")THEN
                        #{
                                CUST.DATA.count1 = BANCS.OUTPARAM.count
                                print(CUST.DATA.count)
                        #}
                        ELSE
                        #{
                                CUST.DATA.count1 = 0
                        #}
                        ENDIF

                        IF(CUST.DATA.count1 == 0) THEN
                        #{
                                CUST.DATA.permitId = MID$(CUST.DATA.permitId,0,13)
                                print(CUST.DATA.permitId)

                                BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
                                print(BANCS.INPARAM.BINDVARS)

                                sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                                sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                                sv_a = sv_a + " where referencenumber like '"+CUST.DATA.permitId+"%' and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='PERMT' "
                                sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                                print(sv_a)
                        #}
                        ELSE
                        #{
                                CUST.DATA.permitId = MID$(CUST.DATA.permitId,0,13)
                                print(CUST.DATA.permitId)

                                BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
                                print(BANCS.INPARAM.BINDVARS)

                                sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                                sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                                sv_a = sv_a + " where referencenumber like '"+CUST.DATA.permitId+"%'  and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='PERMT' "
                                sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                                print(sv_a)
                        #}
                        ENDIF
                #}
                ELSE
                #{

                        BANCS.INPARAM.BINDVARS = CUST.DATA.permitId+ "|"  +BANCS.STDIN.contextBankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                        sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                        sv_a = sv_a + " where referencenumber=?SVAR and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='PERMT' "
                        sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                        print(sv_a)
                #}
                ENDIF
        #}
        ENDIF
		
		#########################
		
		#########Ref ID#######################
		
				IF(CUST.DATA.refId != "")THEN
        #{
                BANCS.INPARAM.BINDVARS = CUST.DATA.refId+ "|" +BANCS.STDIN.contextBankId
                print(BANCS.INPARAM.BINDVARS)

                sv_a = "count|select count(1) from crmuser.entitydocument where referencenumber=?SVAR and "
                sv_a = sv_a + " bank_id =?SVAR and doccode = 'REFID' "
                print(sv_a)

                sv_d = urhk_dbSelectWithBind(sv_a)
                print(sv_d)

                IF(sv_d == "0")THEN
                #{
                        CUST.DATA.count = BANCS.OUTPARAM.count
                        print(CUST.DATA.count)
                #}
                ELSE
                #{
                        CUST.DATA.count = 0
                #}
                ENDIF

                IF(CINT(CUST.DATA.count) == 0)THEN
                #{
                        BANCS.INPARAM.BINDVARS = MID$(CUST.DATA.refId,0,13)+ "|"  +BANCS.STDIN.contextBankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_a = "count|select count(1) from crmuser.entitydocument where referencenumber=?SVAR and "
                        sv_a = sv_a + " bank_id =?SVAR and doccode = 'REFID' "
                        print(sv_a)

                        sv_d = urhk_dbSelectWithBind(sv_a)
                        print(sv_d)

                        IF(sv_d == "0")THEN
                        #{
                                CUST.DATA.count1 = BANCS.OUTPARAM.count
                                print(CUST.DATA.count)
                        #}
                        ELSE
                        #{
                                CUST.DATA.count1 = 0
                        #}
                        ENDIF

                        IF(CUST.DATA.count1 == 0) THEN
                        #{
                                CUST.DATA.refId = MID$(CUST.DATA.refId,0,13)
                                print(CUST.DATA.refId)

                                BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
                                print(BANCS.INPARAM.BINDVARS)

                                sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                                sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                                sv_a = sv_a + " where referencenumber like '"+CUST.DATA.refId+"%' and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='REFID' "
                                sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                                print(sv_a)
                        #}
                        ELSE
                        #{
                                CUST.DATA.refId = MID$(CUST.DATA.refId,0,13)
                                print(CUST.DATA.refId)

                                BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
                                print(BANCS.INPARAM.BINDVARS)

                                sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                                sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                                sv_a = sv_a + " where referencenumber like '"+CUST.DATA.refId+"%'  and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='REFID' "
                                sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                                print(sv_a)
                        #}
                        ENDIF
                #}
                ELSE
                #{

                        BANCS.INPARAM.BINDVARS = CUST.DATA.refId+ "|"  +BANCS.STDIN.contextBankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                        sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                        sv_a = sv_a + " where referencenumber=?SVAR and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='REFID' "
                        sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                        print(sv_a)
                #}
                ENDIF
        #}
        ENDIF
		
		#######################################
		
		######################Unchir ID#######################
		
				IF(CUST.DATA.unchrId != "")THEN
        #{
                BANCS.INPARAM.BINDVARS = CUST.DATA.unchrId+ "|" +BANCS.STDIN.contextBankId
                print(BANCS.INPARAM.BINDVARS)

                sv_a = "count|select count(1) from crmuser.entitydocument where referencenumber=?SVAR and "
                sv_a = sv_a + " bank_id =?SVAR and doccode = 'UNHCR' "
                print(sv_a)

                sv_d = urhk_dbSelectWithBind(sv_a)
                print(sv_d)

                IF(sv_d == "0")THEN
                #{
                        CUST.DATA.count = BANCS.OUTPARAM.count
                        print(CUST.DATA.count)
                #}
                ELSE
                #{
                        CUST.DATA.count = 0
                #}
                ENDIF

                IF(CINT(CUST.DATA.count) == 0)THEN
                #{
                        BANCS.INPARAM.BINDVARS = MID$(CUST.DATA.unchrId,0,13)+ "|"  +BANCS.STDIN.contextBankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_a = "count|select count(1) from crmuser.entitydocument where referencenumber=?SVAR and "
                        sv_a = sv_a + " bank_id =?SVAR and doccode = 'UNHCR' "
                        print(sv_a)

                        sv_d = urhk_dbSelectWithBind(sv_a)
                        print(sv_d)

                        IF(sv_d == "0")THEN
                        #{
                                CUST.DATA.count1 = BANCS.OUTPARAM.count
                                print(CUST.DATA.count)
                        #}
                        ELSE
                        #{
                                CUST.DATA.count1 = 0
                        #}
                        ENDIF

                        IF(CUST.DATA.count1 == 0) THEN
                        #{
                                CUST.DATA.unchrId = MID$(CUST.DATA.unchrId,0,13)
                                print(CUST.DATA.unchrId)

                                BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
                                print(BANCS.INPARAM.BINDVARS)

                                sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                                sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                                sv_a = sv_a + " where referencenumber like '"+CUST.DATA.unchrId+"%' and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='UNHCR' "
                                sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                                print(sv_a)
                        #}
                        ELSE
                        #{
                                CUST.DATA.unchrId = MID$(CUST.DATA.unchrId,0,13)
                                print(CUST.DATA.unchrId)

                                BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
                                print(BANCS.INPARAM.BINDVARS)

                                sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                                sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                                sv_a = sv_a + " where referencenumber like '"+CUST.DATA.unchrId+"%'  and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='UNHCR' "
                                sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                                print(sv_a)
                        #}
                        ENDIF
                #}
                ELSE
                #{

                        BANCS.INPARAM.BINDVARS = CUST.DATA.unchrId+ "|"  +BANCS.STDIN.contextBankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_a = "cifId,custFirstName,custLastName,custPoneNo,suspendedFlg|select accounts.orgkey,accounts.cust_first_name,accounts.cust_last_name,accounts.preferredphone,accounts.suspended"
                        sv_a = sv_a + " from crmuser.accounts,crmuser.entitydocument "
                        sv_a = sv_a + " where referencenumber=?SVAR and accounts.orgkey=entitydocument.orgkey and accounts.bank_id=?SVAR and doccode='UNHCR' "
                        sv_a = sv_a + " and accounts.bank_id=entitydocument.bank_id and rownum<20"
                        print(sv_a)
                #}
                ENDIF
        #}
        ENDIF
		
		########################
		
        sv_d = urhk_dbCursorOpenWithBind(sv_a)
        print(sv_d)

        IF(sv_d!=0) THEN
        #{
                sv_u = urhk_dbCursorClose(sv_a)
                print(sv_u)

                sv_u = urhk_SetOrbOut("Status| No record found")
                print(sv_u)
        #}
        ELSE
        #{
                CUST.DATA.DB_CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
                print(CUST.DATA.DB_CURSOR_NUMBER)


                sv_c = urhk_dbCursorFetch(CUST.DATA.DB_CURSOR_NUMBER)
                print(sv_c)

                IF(sv_c != "0")THEN
                #{
                        #sv_e = urhk_SetOrbOut("SuccessorFailure|Sucess")
                        #sv_u = urhk_SetOrbOut("CifId| "+CUST.DATA.cifId)
                        #sv_u = urhk_SetOrbOut("CustFirstName| "+CUST.DATA.custFirstName)
                        #sv_u = urhk_SetOrbOut("CustLastName| "+CUST.DATA.custLastName)
                        #sv_u = urhk_SetOrbOut("CustPhoneNo| "+CUST.DATA.custPoneNo)
                        sv_e = urhk_SetOrbOut("SuccessorFailure|Failed")
                        sv_u = urhk_SetOrbOut("Failure| No record found")
                        print(sv_u)
                #}
                ENDIF

                while(sv_c ==0)
                #{
                        CUST.DATA.cifId = BANCS.OUTPARAM.cifId
                        print(CUST.DATA.cifId)
                        CUST.DATA.custFirstName = BANCS.OUTPARAM.custFirstName
                        print(CUST.DATA.custFirstName)
                        CUST.DATA.custLastName = BANCS.OUTPARAM.custLastName
                        print(CUST.DATA.custLastName)
                        CUST.DATA.custPoneNo = BANCS.OUTPARAM.custPoneNo
                        print(CUST.DATA.custPoneNo)
                        CUST.DATA.suspendedFlg = BANCS.OUTPARAM.suspendedFlg
                        print(CUST.DATA.suspendedFlg)

                        CUST.DATA.count = CINT(CUST.DATA.count)+ 1
                        print(CUST.DATA.count)

                        #sv_c = urhk_SetOrbOut("recordCount|" +CUST.DATA.count)
                        #print(sv_c)

                        sv_c = urhk_SetOrbOut("CustomerDetails.<rec_"+CUST.DATA.count+">.CifId"+"| "+CUST.DATA.cifId)
                        print(sv_c)

                        sv_c = urhk_SetOrbOut("CustomerDetails.<rec_"+CUST.DATA.count+">.CustFirstName"+"| "+CUST.DATA.custFirstName)
                        print(sv_c)

                        sv_c = urhk_SetOrbOut("CustomerDetails.<rec_"+CUST.DATA.count+">.CustLastName"+"| "+CUST.DATA.custLastName)
                        print(sv_c)

                        sv_c = urhk_SetOrbOut("CustomerDetails.<rec_"+CUST.DATA.count+">.CustPhoneNo"+"| "+CUST.DATA.custPoneNo)
                        print(sv_c)

                        sv_c = urhk_SetOrbOut("CustomerDetails.<rec_"+CUST.DATA.count+">.SuspendedFlg"+"| "+CUST.DATA.suspendedFlg)
                        print(sv_c)

                        sv_c = urhk_dbCursorFetch(CUST.DATA.DB_CURSOR_NUMBER)
                        print(sv_c)

                        #CUST.DATA.count = CINT(CUST.DATA.count)+ 1
                #}
                do
                sv_c = urhk_dbCursorClose(CUST.DATA.DB_CURSOR_NUMBER)
                print(sv_c)
        #}
        ENDIF

EXITSCRIPT

