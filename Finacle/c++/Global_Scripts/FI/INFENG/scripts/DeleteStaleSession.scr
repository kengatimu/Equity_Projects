<--START
	TRACE ON
	
	sv_r = REPEXISTS("CUST")
	IF (sv_r == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	sv_r = CLASSEXISTS("CUST", "EOD")
	IF (sv_r == 0) THEN
	#{
		CREATECLASS("CUST", "EOD", 5)
	#}
	ENDIF
	print(MTT.TranDetails.InitSolId)
	print(BANCS.STDIN.contextBankId)
	
	#-----------------------------------------------------------------------------------------------------------------
	#Start of changes for Deleting the Stale Sessions
	#-----------------------------------------------------------------------------------------------------------------

		BANCS.INPARAM.BINDVARS = MTT.TranDetails.InitSolId +"|"+ BANCS.STDIN.contextBankId
		PRINT (BANCS.INPARAM.BINDVARS)
		sv_s = ""
		sv_s = sv_s + "DELETE FROM TBAADM.LGI WHERE USER_ID NOT IN "
		sv_s = sv_s + " (SELECT USER_ID FROM TBAADM.UPR WHERE VIRTUAL_FLG='Y') AND EXE_NAME IN ('mrbx4004','ofti') "
		sv_s = sv_s + " AND USER_ID NOT IN (SELECT USER_ID FROM TBAADM.UPR WHERE USER_LOGGED_ON_FLG='Y') "
		sv_s = sv_s + " AND CONTEXT_SOL_ID=?SVAR AND  CONTEXT_BANK_ID=?SVAR AND PID='-1' "

		sv_d = urhk_dbSQLWithBind(sv_s)
		sv_s = urhk_dbSqlWithBind("Commit")
	
	#-----------------------------------------------------------------------------------------------------------------
	#End of the changes
	#-----------------------------------------------------------------------------------------------------------------
	
	EXITSCRIPT
	
	TRACE OFF
	
END-->
