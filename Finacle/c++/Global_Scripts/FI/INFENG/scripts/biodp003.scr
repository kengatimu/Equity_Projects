#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: biodp003.scr
# event: NA
# description: NA
#=================================================
<--START
CHECKSUM="723b530ccaab80bb5ffe0c10b914877d22af6be013fbc6f651a958361dbc9e4f"
	#TRACE ON
	
	BANCS.OUTPUT.doValidation = "N"
	
	print(BANCS.INPUT.buttonIdDtls)
	
	sv_s = "doValidation|select  instr((select listagg(parameter_value,',') "
	sv_s = sv_s + " within group (order by parameter_id) from custom.c_cpvalue "
	sv_s = sv_s + " where parameter_id like ?SVAR AND module_name = ?SVAR "
	sv_s = sv_s + " AND bank_id = ?SVAR ),?SVAR ) from dual"
	BANCS.INPARAM.BINDVARS = "BUTTONIDVAL%" + "|" + "BIOM"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + BANCS.INPUT.buttonIdDtls
	print(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	IF(sv_d == 0) THEN
	#{
		print(BANCS.OUTPARAM.doValidation)
		IF(BANCS.OUTPARAM.doValidation != 0) THEN
		#{
			BANCS.OUTPUT.doValidation = "Y"
		#}
		ENDIF
	#}
	ENDIF
	print(BANCS.OUTPUT.doValidation)
	sv_v = urhk_SetOrbOut("doValidation| " + BANCS.OUTPUT.doValidation)
	
	
	#trace off
END-->
