#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: ubpdp058.scr
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START
CHECKSUM="66ec5ef3ce3dcf4253dfdc1685b8ff0c4fcb3685015a3d7b8996484aaeb93012"
	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp058.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called from hoccm_crit_link.js for fetching type of service.
	# Called Script         :
	# Calling function      :		function fnTypOfServ()
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           23-08-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	
	#========================================================
	# Check whether CUST Repository exists else create it
	#========================================================
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	#========================================================
	# Check whether UBP Class exists else create it
	#========================================================
	sv_r = CLASSEXISTS("CUST","UBP")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","UBP",5)
	#}
	ENDIF
	#========================================================
	# Initialize the variables
	#========================================================
	
	CUST.UBP.billerId = " "
	CUST.UBP.billAcctId = " "
	CUST.UBP.bankId=" "
	CUST.UBP.valType=" "
	CUST.UBP.recordCount = "0"
	
	#========================================================
	# Validating Ref. Num
	#========================================================
	IF(FIELDEXISTS(BANCS.INPUT.billerId))THEN
	#{
		IF(TRIM(BANCS.INPUT.billerId) != "")THEN
		#{
			CUST.UBP.billerId = TOUPPER(TRIM(BANCS.INPUT.billerId))
			PRINT(CUST.UBP.billerId)
			
			#}
		ELSE
			#{
			CUST.UBP.billerId=""
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.billerId=""
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.valType))THEN
	#{
		IF(TRIM(BANCS.INPUT.valType) != "")THEN
		#{
			CUST.UBP.valType = TOUPPER(TRIM(BANCS.INPUT.valType))
			PRINT(CUST.UBP.valType)
			
			#}
		ELSE
			#{
			CUST.UBP.valType=""
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.valType=""
	#}
	ENDIF
	#========================================================
	#Checking if the biller id entered is correct
	#========================================================
	BANCS.INPARAM.BINDVARS = CUST.UBP.billerId+"|N|Y|"+ BANCS.STDIN.contextBankId
	print(BANCS.INPARAM.BINDVARS)
	sv_s = "recordNum|"
	sv_s = sv_s + " SELECT COUNT(*)"
	sv_s = sv_s + " FROM TBAADM.BSRT "
	sv_s = sv_s + " WHERE BILLER_ID = ?SVAR"
	sv_s = sv_s + " AND NVL(DEL_FLG, 'N') = ?SVAR"
	sv_s = sv_s + " AND NVL(ENTITY_CRE_FLG, 'Y') = ?SVAR"
	sv_s = sv_s + " AND BANK_ID =?SVAR"
	PRINT(sv_s)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d == 0)THEN
	#{
		CUST.UBP.recordNum = BANCS.OUTPARAM.recordNum
		PRINT(CUST.UBP.recordNum)
		IF(CUST.UBP.recordNum=="0")THEN
		#{
			GOTO ENDOFSCRIPT
			
			
		#}
		ENDIF
	#}
	ENDIF
	
	sv_u = urhk_SetOrbOut("recCount|"+CUST.UBP.recordNum)
	PRINT(sv_u)
	
	IF(CUST.UBP.recordNum=="0")THEN
	#{
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		#========================================================
		# Fetching tthe values from BRGT table and populating
		# the searcher list
		#========================================================
		BANCS.INPARAM.BINDVARS = CUST.UBP.billerId+"|N|Y|"+ BANCS.STDIN.contextBankId
		print(BANCS.INPARAM.BINDVARS)
		sv_s = "typOfServ|"
		sv_s = sv_s + " SELECT BILLER_SERVICE_ID"
		sv_s = sv_s + " FROM TBAADM.BSRT "
		sv_s = sv_s + " WHERE BILLER_ID = ?SVAR"
		sv_s = sv_s + " AND NVL(DEL_FLG, 'N') = ?SVAR"
		sv_s = sv_s + " AND NVL(ENTITY_CRE_FLG, 'Y') = ?SVAR"
		sv_s = sv_s + " AND BANK_ID =?SVAR"
		PRINT(sv_s)
		sv_c = urhk_dbCursorOpenWithBind(sv_s)
		PRINT(sv_c)
		IF(sv_c != 0)THEN
		#{
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		sv_n = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		PRINT(sv_n)
		sv_f = urhk_dbCursorFetch(sv_n)
		PRINT(sv_f)
		sv_i = 1
		WHILE(sv_f == 0)
		#{
			CUST.UBP.typOfServ = BANCS.OUTPARAM.typOfServ
			PRINT(CUST.UBP.typOfServ)
			
			sv_s = "typOfServ_" + Format$(sv_i,"%d") + "|" + CUST.UBP.typOfServ
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			
			sv_i = sv_i + 1
			sv_f = urhk_dbCursorFetch(sv_n)
			PRINT(sv_f)
			
		#}
		DO
		sv_c = urhk_dbCursorClose(sv_n)
		PRINT(sv_c)
	#}
	ENDIF
	
	ENDOFSCRIPT:
END-->
