##########################################################################################
# Source Name           : s_siexgendp003.scr
# Date                  : 06-Sep-2013
# Description           : This script is called from SRV's to restrict verification
# Author                : Bharath
# Modification History  :
# <Serial No.>  <Date>        <Author Name>             <Description>
# ------------  -----------   -----------------         --------------
#      1        06-Sep-2013    Bharath 			Base Version
###########################################################################################
<--START
CHECKSUM="c4f782c9524f7cd96e89ecd9a6eb74e66fba20781bdbc5a343022add594dce75"
	# Script body goes here
	TRACE ON
	
	#---------------------------------------------------------------------
	#Check whether CUST Repository exists else create it
	#---------------------------------------------------------------------
	
	IF(REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	#---------------------------------------------------------------------
	#Check whether CSIIET Class exists else create it
	#---------------------------------------------------------------------
	
	IF(CLASSEXISTS("CUST","CSIIET") == 0) THEN
	#{
		CREATECLASS ("CUST","CSIIET",5)
	#}
	ENDIF


	IF(BANCS.STDIN.moduleIdentity == "FI")THEN
	#{
		GOTO ENDSCRIPT

	#}
	ELSE
	#{
		IF (BANCS.STDIN.contextBankId =="54") THEN
		#{
			IF ((BANCS.STDIN.userWorkClass !="201") AND (BANCS.STDIN.userWorkClass !="650") AND (BANCS.STDIN.userWorkClass !="600") AND (BANCS.STDIN.userWorkClass !="110") AND (BANCS.STDIN.userWorkClass !="150") AND (BANCS.STDIN.userWorkClass !="200")) THEN
			#{
				sv_u = urhk_SRV_SetErr("Insufficent Workclass 201 for Verification......")
				BANCS.OUTPUT.successOrFailure = "F"
				EXITSCRIPT
			#}
			ENDIF
		#}
		ENDIF

	#}
	ENDIF
	

ENDSCRIPT:
	
END-->

