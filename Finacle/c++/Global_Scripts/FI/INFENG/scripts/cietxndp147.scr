#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cietxndp147.scr
# Type: NA|default
# Description: cietxndp147.scr _ staff check
# Author: Balaji_P05
# Date: 3/25/20 5:48 PM
# Modification Log:
#============================================================

<--START
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CIETXN Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CIETXN")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CIETXN",5)
	#}
	ENDIF
	# Script body goes here
	PRINT(BANCS.INPUT.inputData)
	BANCS.OUTPUT.staffCount=0
	sv_l=BANCS.INPUT.inputData
	sv_h = GETPOSITION(sv_l,"|")
	sv_i=0
	PRINT(sv_h)
	WHILE(sv_h > 0)
	#{
		sv_i=sv_i+CINT("1")
		sv_j = MID$(sv_l,0,sv_h-1)
		PRINT(sv_j)
		
		("CUST").("CIETXN").("inputData_" + FORMAT$(sv_i,"%d"))=sv_j
		sv_y = STRLEN(sv_l)
		PRINT(sv_y)
		
		sv_l = MID$(sv_l,sv_h,sv_y)
		PRINT(sv_l)
		sv_h = GETPOSITION(sv_l,"|")
		PRINT(sv_h)
	#}
	DO
	BANCS.INPUT.foracid=CUST.CIETXN.inputData_1
	BANCS.INPUT.txnType=CUST.CIETXN.inputData_2
	
	PRINT(BANCS.INPUT.foracid)
	PRINT(BANCS.INPUT.txnType)
	
	IF(BANCS.INPUT.txnType == "CD")THEN
	#{
		sv_s = "staffCount|"
		sv_s = sv_s + " SELECT count(1) "
		sv_s = sv_s + " FROM TBAADM.GAM "
		sv_s = sv_s + " WHERE FORACID = ?SVAR"
		sv_s = sv_s + " AND BANK_ID = ?SVAR"
		sv_s = sv_s + " AND SCHM_TYPE = 'SBA'"
		sv_s = sv_s + " AND SCHM_CODE = 'SB190'"
		sv_s = sv_s + " AND DEL_FLG != 'Y'"
		PRINT(sv_s)
		
		BANCS.INPARAM.BINDVARS = BANCS.INPUT.foracid + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
	#}
	ENDIF
	
	IF((BANCS.INPUT.txnType == "TF") OR(BANCS.INPUT.txnType == "PA_TT"))THEN
	#{
		sv_s = "staffCount|"
		sv_s = sv_s + " SELECT count(1) "
		sv_s = sv_s + " FROM TBAADM.GAM "
		sv_s = sv_s + " WHERE FORACID = ?SVAR"
		sv_s = sv_s + " AND BANK_ID = ?SVAR"
		#sv_s = sv_s + " AND SCHM_TYPE = 'SBA'"
		#sv_s = sv_s + " AND SCHM_CODE = 'SB190'"
		sv_s = sv_s + " AND ((schm_type ='OAB') OR ((schm_type ='SBA') AND (schm_code ='SB190')))"
		sv_s = sv_s + " AND DEL_FLG != 'Y'"
		PRINT(sv_s)
		
		BANCS.INPARAM.BINDVARS = BANCS.INPUT.foracid + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
	#}
	ENDIF
	
	
	IF (FIELDEXISTS(sv_s)) THEN
	#{
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d == 0)THEN
		#{
			BANCS.OUTPUT.staffCount = BANCS.OUTPARAM.staffCount
			PRINT(BANCS.OUTPUT.staffCount)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(MID$(BANCS.STDIN.userId,0,6) == "FIVUSR")THEN
	#{
		sv_b=urhk_setOrbOut("outputData|"+BANCS.OUTPUT.staffCount)
		PRINT(sv_b)
	#}
	ENDIF
END-->
