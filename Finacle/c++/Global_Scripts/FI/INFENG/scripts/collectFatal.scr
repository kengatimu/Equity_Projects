#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: collectFatal.scr
# Type: NA|default
# Description: This script is used to Collect Fatal from EOD user directory.
# Author:
# Date:
# Modification Log:
#============================================================

<--START
CHECKSUM="76a50d59a1ffd8ac210e156d2e90fcf4959507917768ab5aa45de60fb178d36b"
	# Script body goes here
	
	TRACE ON
	
	sv_d = urhk_getEnv("CDCI_LOGS")
	print(sv_d)
	sv_t = "/finacle/EQPRODFI/BackEnd/Finacle/FC/app/CDCI_LOGS"
	IF(sv_t != "") THEN
	#{
		sv_d = sv_t + "/" + BANCS.STDIN.userId
		PRINT(sv_d)
		#}
	ELSE
		#{
		PRINT("Could not get CDCI path!!")
		EXITSCRIPT
	#}
	ENDIF
	
	sv_b = urhk_getFileLocation("COM|collectFatal.com")
	PRINT(sv_b)
	
	IF(sv_b !=0 ) THEN
	#{
		PRINT("Could not get com file path ")
		EXITSCRIPT
	#}
	ENDIF
	
	sv_j = BANCS.OUTPARAM.fileLocation
	PRINT(sv_j)
	
	sv_j = sv_j + "collectFatal.com" + " "
	sv_j = sv_j + sv_d
	sv_j = sv_j + " " + BANCS.STDIN.contextBankId
	sv_j = sv_j + " " + BANCS.STDIN.BODDate
	
	print(sv_j)
	
	sv_f = "ksh " + sv_j + "&"
	PRINT(sv_f)
	
	sv_g = SYSTEM(sv_f)
	PRINT(sv_g)
	
	IF (sv_g != 0) THEN
	#{
		PRINT("Processsing of records failed ")
		EXITSCRIPT
	#}
	ENDIF
	TRACE OFF
	EXITSCRIPT
END-->
