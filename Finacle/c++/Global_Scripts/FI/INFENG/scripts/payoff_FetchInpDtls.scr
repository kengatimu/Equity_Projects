#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: payoff_FetchInpDtls.scr
# event: NA
# description: NA
#=================================================
<--START
CHECKSUM="1a43e9539832506915d2778e12fbcd0b2bf7c0a041b82847c42131c819907074"
	#TRACE ON
	sv_v = "PAYOFF_REQ.Inquiry"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{
		
		CUST.INTERFACE.Inquiry =("BANCS").("INPUT").(sv_v)
		#}
	ELSE
		#{
		CUST.INTERFACE.Inquiry = "N"
	#}
	ENDIF
	
	sv_v = "PAYOFF_REQ.AccountID"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{
		
		CUST.INTERFACE.accountID =("BANCS").("INPUT").(sv_v)
		#}
	ELSE
		#{
		CUST.INTERFACE.accountID = ""
	#}
	ENDIF
	
	sv_v = "PAYOFF_REQ.collAcctID"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
	#{
		
		CUST.INTERFACE.collAcctID =("BANCS").("INPUT").(sv_v)
		#}
	ELSE
		#{
		CUST.INTERFACE.collAcctID = ""
	#}
	ENDIF
	
	print(CUST.INTERFACE.accountID)
	print(CUST.INTERFACE.collAcctID)
	
	#---------------------------------------------------------------------------------
	# Validation for Account ID
	#---------------------------------------------------------------------------------
	IF(CUST.INTERFACE.accountID != "") THEN
	#{
		sv_u = urhk_valAcctNumber(CUST.INTERFACE.accountID)
		print(sv_u)
		IF(sv_u != 0) THEN
		#{
			CUST.INTERFACE.successOrFailure = "F"
			CUST.INTERFACE.errorCode = "ERR110"
			GOTO ERROREXIT
		#}
		ENDIF
		
		#}
	ELSE
		#{
		CUST.INTERFACE.successOrFailure = "F"
		CUST.INTERFACE.errorCode = "ERR106"
		GOTO ERROREXIT
	#}
	ENDIF
	
	#---------------------------------------------------------------------------------
	# Validation for Account ID
	#---------------------------------------------------------------------------------
	IF(CUST.INTERFACE.collAcctID!= "") THEN
	#{
		sv_u = urhk_valAcctNumber(CUST.INTERFACE.collAcctID)
		print(sv_u)
		IF(sv_u != 0) THEN
		#{
			CUST.INTERFACE.successOrFailure = "F"
			CUST.INTERFACE.errorCode = "ERR110"
			GOTO ERROREXIT
		#}
		ENDIF
		
		#}
	ELSE
	#{
	    IF(CUST.INTERFACE.Inquiry == "Y") THEN
	    #{
		####Pick operative account of the loan from LAM for inquiry purpose incase not passed.
		lv_j = "collAcctID|select foracid from tbaadm.gam where bank_id =?SVAR and acid in "
		lv_j = lv_j + " (select op_acid from tbaadm.lam where bank_id =?SVAR and acid in(select acid from tbaadm.gam where foracid=?SVAR and bank_id=?SVAR)) "
		print(lv_j)

                BANCS.INPARAM.BINDVARS =  BANCS.STDIN.contextBankId +"|"+ BANCS.STDIN.contextBankId + "|" + CUST.INTERFACE.accountID +"|"+ BANCS.STDIN.contextBankId
                print(BANCS.INPARAM.BINDVARS)

                lv_u = urhk_dbSelectWithBind(lv_j)
                if(lv_u == 0) then
			CUST.INTERFACE.collAcctID = BANCS.OUTPARAM.collAcctID
			print(CUST.INTERFACE.collAcctID)
			sv_u = urhk_valAcctNumber(CUST.INTERFACE.collAcctID)
			print(sv_u)
			IF(sv_u != 0) THEN
			#{
				CUST.INTERFACE.successOrFailure = "F"
				CUST.INTERFACE.errorCode = "ERR110"
				GOTO ERROREXIT
			#}
			ENDIF
		#}
		else
		#{
				CUST.INTERFACE.successOrFailure = "F"
				CUST.INTERFACE.errorCode = "ERR106"
				GOTO ERROREXIT		
		#}
		endif
	    #}
	    ELSE
	    #{
				CUST.INTERFACE.successOrFailure = "F"
				CUST.INTERFACE.errorCode = "ERR106"
				GOTO ERROREXIT
	    #}
	    ENDIF
	#}
	ENDIF
	
	
	#trace off
END-->
