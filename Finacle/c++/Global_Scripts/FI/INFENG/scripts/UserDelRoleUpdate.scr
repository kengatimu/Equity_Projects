<--START
TRACE ON

        sv_s = "UserSol|SELECT SOL_ID FROM TBAADM.UPR WHERE USER_ID=?SVAR"
        BANCS.INPARAM.BINDVARS = CUST.INTERFACE.userId

        PRINT(BANCS.INPARAM.BINDVARS)
        sv_d = urhk_dbSelectWithBind(sv_s)
        IF(sv_d == 0) THEN
        #{
			CUST.INTERFACE.userSolId = BANCS.OUTPARAM.UserSol
			PRINT(CUST.INTERFACE.userSolId)
	#}
	ENDIF
       
	IF(BANCS.STDIN.mySolId != CUST.INTERFACE.userSolId)THEN
        #{
                BANCS.INPARAM.FlagType  = "S"
                BANCS.INPARAM.SolId     = CUST.INTERFACE.userSolId
                sv_u = urhk_ContextSOLUpdate("")
                PRINT(sv_u)
                IF (sv_u == 0) THEN
                #{
                                PRINT("Context SOL change success")
                ELSE
                                PRINT("Context SOL change failed")
                #}
                ENDIF
        #}
        ENDIF

        sv_a=urhk_SetUrhkInp("usrGenDtl.usrId|"+CUST.INTERFACE.userId )
        print(sv_a)


        sv_u = urhk_ExecSrvNoCommit("SRV_FetchUsrProfileForModify|same_user_verify = Y|retain_all_output = Y")
        print(sv_u)

        IF(sv_u != 0)THEN
        #{
                sv_u = urhk_CopyOutToIn("")
                print(sv_u)

                sv_u = urhk_ExecSrvNoCommit("SRV_FetchUsrProfileForModify|same_user_verify = Y|retain_all_output = Y")
                print(sv_u)
                IF(sv_u != 0)THEN
                #{
                        sv_c = BANCS.OUTPUT.successOrFailure
                        sv_d = 1
                        sv_c = CINT(BANCS.OUTPARAM.Error_num)
                        print(sv_c)
                        WHILE(sv_d <= sv_c)
                        #{
                                sv_f = FORMAT$(sv_d, "%d")
                                sv_e = ("BANCS").("OUTPARAM").("ErrorMesg_" + sv_f)
                                PRINT(sv_e)
                                sv_d = sv_d + 1
                        #}
                        DO
                        sv_d = 1
                        sv_c = CINT(BANCS.OUTPARAM.Xcpn_num)
                        WHILE(sv_d <= sv_c)
                        #{
                                sv_f = FORMAT$(sv_d, "%d")
                                sv_e = ("BANCS").("OUTPARAM").("XcpnDesc_" + sv_f)
                                sv_d = sv_d + 1
                        #}
                        DO
                        sv_d = 1
                        sv_c = CINT(BANCS.OUTPARAM.Warning_num)
                        WHILE(sv_d <= sv_c)
                        #{
                                sv_f = FORMAT$(sv_d, "%d")
                                sv_e = ("BANCS").("OUTPARAM").("WarningMesg" + sv_f)
                                sv_d = sv_d + 1
                        #}
                        DO
						GOTO ENDOFSCRIPT
                #}
                ENDIF

        #}
        ENDIF

        PRINT(sv_u)

        sv_u = urhk_CopyOutToIn("")
        print(sv_u)
        #sv_a=urhk_SetUrhkInp("usrGenDtl.usrId|"+CUST.INTERFACE.userId )
        #print(sv_a)
        sv_u=urhk_SetUrhkInp("usrGenDtl.usrRole.id|DEL")
        print(sv_u)
        sv_u=urhk_SetUrhkInp("usrGenDtl.workClass.refCode|011")
        print(sv_u)

        #sv_u = urhk_ExecSrvNoCommit("SRV_ModifyUsrProfileAndVerify|same_user_verify = Y|retain_all_output = Y")
        sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|same_user_verify = Y|retain_all_output = Y")
        print(sv_u)

        IF(sv_u != 0)THEN
        #{
                sv_u = urhk_CopyOutToIn("")
                print(sv_u)

                #sv_u = urhk_ExecSrvNoCommit("SRV_ModifyUsrProfileAndVerify|same_user_verify = Y|retain_all_output = Y")
                sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|same_user_verify = Y|retain_all_output = Y")
                print(sv_u)

                IF(sv_u != 0)THEN
                #{
						CUST.INTERFACE.uprUpdateFlg = "F"
                        sv_c = BANCS.OUTPUT.successOrFailure
                        sv_d = 1
                        sv_c = CINT(BANCS.OUTPARAM.Error_num)
                        print(sv_c)
                        WHILE(sv_d <= sv_c)
                        #{
                                sv_f = FORMAT$(sv_d, "%d")
                                sv_e = ("BANCS").("OUTPARAM").("ErrorMesg_" + sv_f)
                                PRINT(sv_e)
                                sv_d = sv_d + 1
                                #GOTO ENDOFSCRIPT
                        #}
                        DO
                        sv_d = 1
                        sv_c = CINT(BANCS.OUTPARAM.Xcpn_num)
                        WHILE(sv_d <= sv_c)
                        #{
                                sv_f = FORMAT$(sv_d, "%d")
                                sv_e = ("BANCS").("OUTPARAM").("XcpnDesc_" + sv_f)
                                sv_d = sv_d + 1
                        #}
                        DO
                        sv_d = 1
                        sv_c = CINT(BANCS.OUTPARAM.Warning_num)
                        WHILE(sv_d <= sv_c)
                        #{
                                sv_f = FORMAT$(sv_d, "%d")
                                sv_e = ("BANCS").("OUTPARAM").("WarningMesg" + sv_f)
                                sv_d = sv_d + 1
                        #}
                        DO

                #}
                ENDIF

        #}
        ENDIF
sv_u = urhk_dbSqlWithBind("COMMIT")
ENDOFSCRIPT:
END-->

