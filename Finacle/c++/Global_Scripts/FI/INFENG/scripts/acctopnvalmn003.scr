#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: acctopnvalmn003.scr
# Type: NA|default
# Description: acctopnvalmn003.scr
# Author:
# Date:
# Modification Log:
#============================================================

#-------------------------------------------------------------------------------------------------------------------------
#    Name			 : acctopnvalmn003.scr
#    Description     : This script is used to make fields mandatory
#    Date            : 06-09-2013
#    Author          : Bharath
#    Input           : NA
#    Output			 : NA
#    Called Script	 : NA
#    Calling Script	 : mis_codes_custom.jsp
#    Menu Option	 : HOAACSB
#    Srl. No			Date				Author			Description.
#    ------			----------			-------			---------------
#    1.0			13-08-2013			Priya K			Original Version
#	 2.0			21-08-2013			Vijaya V		changes for TOL - 368012
#	 3.0			06-09-2013			Bharath			Modified for new porting items
#---------------------------------------------------------------------------------------------------------------------------
<--START
CHECKSUM="1dcf239a468db60529f40b1f2d1058f85d6fe6a2024467d1a9520cf48a9551a2"
	TRACE ON
	#-----------------------------------------------------------------
	#Creating repository and class
	#-----------------------------------------------------------------
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST", "ACCTOPEN") == 0) THEN
	#{
		CREATECLASS("CUST", "ACCTOPEN", 5)
	#}
	ENDIF
	
	#--------------------------------------------------------------------
	# Initilization variables
	#--------------------------------------------------------------------
	CUST.ACCTOPEN.retMsg			= " "
	CUST.ACCTOPEN.sucFail			= "S"
	CUST.ACCTOPEN.fieldId			= " "
	CUST.ACCTOPEN.schmCode			= ""
	CUST.ACCTOPEN.glSubHeadCode		= ""
	CUST.ACCTOPEN.freeText10		= ""
	
	IF(BANCS.STDIN.contextBankId == "56" ) THEN
	#{
		IF(FIELDEXISTS(BANCS.INPUT.schmCode))THEN
		#{
			IF(TRIM(BANCS.INPUT.schmCode) != "")THEN
			#{
				CUST.ACCTOPEN.schmCode=BANCS.INPUT.schmCode
				PRINT(CUST.ACCTOPEN.schmCode)
			#}
			ENDIF
		#}
		ENDIF
		
		IF(FIELDEXISTS(BANCS.INPUT.glSubHeadCode))THEN
		#{
			IF(TRIM(BANCS.INPUT.glSubHeadCode) != "")THEN
			#{
				CUST.ACCTOPEN.glSubHeadCode=BANCS.INPUT.glSubHeadCode
				PRINT(CUST.ACCTOPEN.glSubHeadCode)
			#}
			ENDIF
		#}
		ENDIF
		
		IF(FIELDEXISTS(BANCS.INPUT.freeText10))THEN
		#{
			IF(TRIM(BANCS.INPUT.freeText10) != "")THEN
			#{
				CUST.ACCTOPEN.freeText10=BANCS.INPUT.freeText10
				PRINT(CUST.ACCTOPEN.freeText10)
			#}
			ENDIF
		#}
		ENDIF
		
		#---------------------------------------------------------------------------
		# Check if glsubheadcode is null and get glsubheadcode from GSS table
		#---------------------------------------------------------------------------
		IF (CUST.ACCTOPEN.glSubHeadCode == "" ) THEN
		#{
			sv_s = "cnt|SELECT count(1) "
			sv_s = sv_s + "FROM TBAADM.GSS "
			sv_s = sv_s + "WHERE  del_flg ='N' "
			sv_s = sv_s + "AND schm_code=?SVAR "
			sv_s = sv_s + "AND bank_id = ?SVAR "
			
			BANCS.INPARAM.BINDVARS = CUST.ACCTOPEN.schmCode + "|"  + BANCS.STDIN.contextBankId
			PRINT(sv_s)
			sv_d = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_d)
			
			IF (sv_d == 0) THEN
			#{
				CUST.ACCTOPEN.cnt = BANCS.OUTPARAM.cnt
				PRINT(CUST.ACCTOPEN.cnt)
				
				sv_s = "glSubHeadCode|SELECT gl_sub_head_code "
				sv_s = sv_s + "FROM TBAADM.GSS "
				sv_s = sv_s + "WHERE  del_flg ='N' "
				sv_s = sv_s + "AND schm_code=?SVAR "
				sv_s = sv_s + "AND bank_id = ?SVAR "
				
				IF (CINT(CUST.ACCTOPEN.cnt) > 1) THEN
				#{
					sv_s = sv_s + "AND default_flg = 'Y' "
				#}
				ENDIF
				
				BANCS.INPARAM.BINDVARS = CUST.ACCTOPEN.schmCode + "|"  + BANCS.STDIN.contextBankId
				PRINT(sv_s)
				sv_d = urhk_dbSelectWithBind(sv_s)
				PRINT(sv_d)
				
				IF (sv_d == 0) THEN
				#{
					CUST.ACCTOPEN.glSubHeadCode = BANCS.OUTPARAM.glSubHeadCode
					PRINT(CUST.ACCTOPEN.glSubHeadCode)
				#}
				ENDIF
			#}
			ENDIF
		#}
		ENDIF
		
		IF ((CUST.ACCTOPEN.glSubHeadCode == "35001") OR (CUST.ACCTOPEN.glSubHeadCode == "35002")) THEN
		#{
			IF(CUST.ACCTOPEN.freeText10 == "") THEN
			#{
				CUST.ACCTOPEN.retMsg = "STAFF ID is mandatory - Free Text 10"
				CUST.ACCTOPEN.sucFail = "F"
				CUST.ACCTOPEN.fieldId = "misFreeText10"
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	ENDOFSCRIPT:
		PRINT(CUST.ACCTOPEN.retMsg)
		PRINT(CUST.ACCTOPEN.sucFail)
		PRINT(CUST.ACCTOPEN.fieldId)
		
		sv_u = urhk_SetOrbOut("sucFail|" + CUST.ACCTOPEN.sucFail)
		sv_u = urhk_SetOrbOut("Error|" + CUST.ACCTOPEN.retMsg)
		sv_u = urhk_SetOrbOut("fieldId|" + CUST.ACCTOPEN.fieldId)
		
		EXITSCRIPT
END-->
