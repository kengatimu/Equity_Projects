#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: s_tranvaldp001.scr
# Type: NA|default
# Description: s_tranvaldp001.scr
# Author:
# Date:
# Modification Log:
#============================================================

#---------------------------------------------------------------------------------
#	Name			: s_tranvaldp001.scr
#	Description		: This script is used for blocking transactions when the
#				      tran sub type is BI and account is dormant or inactive
#					  account in TM menu.
#	Date			: 16-01-2013
#	Author			: GOKULKRISHNA
#	Input			: NA
#	Output			: NA
#	Called Script	: SRV_AddAndPostXferTran_validate_data.scr
#	Calling Scrip	: NA
#	Menu Option		: HTM
#	Srl. No			Date			Author			Description.
#	-------			---------		-------			---------------
#	1.0				16-01-2013		GOKULKRISHNA			Original Version
#   1.1				06-03-2013      GOKULKRISHNA			Added the onlineOrBatch Check
#   1.2				01-08-2013      Bharath			Validation added for deleted part trans
#	2.0			29-Aug-2013	Mohan Raghu Arudi		Commented Delete Rep and Class
#----------------------------------------------------------------------------------

<--START
CHECKSUM="ba752508d4a7a46385113de7ecc07eaaf1d33b6b76e0a27d8f235de53bc99ad8"
	#TRACE ON
	
	#------------------------------------------------------------------------------
	#       Creating repository and class
	#       Initializing the variables
	#------------------------------------------------------------------------------
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST","DATA") == 0) THEN
	#{
		CREATECLASS("CUST","DATA",5)
	#}
	ENDIF
	
	IF (BANCS.STDIN.onlineOrBatch == "B") THEN
	#{
		EXITSCRIPT
	#}
	ENDIF
        #sv_q = urhk_B2k_PrintRepos("BANCS")
        #print(sv_q)
	CUST.DATA.debitdormantskip="N"
	print(CUST.DATA.debitdormantskip)
	sv_u = urhk_SRV_GetVal("tranHdr.tranTypeSubType.tranType")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
		CUST.DATA.tranType = BANCS.OUTPARAM.srvValue
		PRINT(CUST.DATA.tranType)
		#}
	ELSE
		#{
		sv_u = urhk_SRV_SetErr("Error: urhk_SRV_GetVal failed for Tran Type")
		BANCS.OUTPUT.successOrFailure = "F"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_u = urhk_SRV_GetVal("tranHdr.tranTypeSubType.tranSubType")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
		CUST.DATA.tranSubType = BANCS.OUTPARAM.srvValue
		PRINT(CUST.DATA.tranSubType)
		#}
	ELSE
		#{
		sv_u = urhk_SRV_SetErr("Error: urhk_SRV_GetVal failed for Tran SUB Type")
		BANCS.OUTPUT.successOrFailure = "F"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_u = urhk_SRV_NumOfRecs("tranDtl.partTranDetailLL")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
		CUST.DATA.noOfRecords = CINT(BANCS.OUTPARAM.srvValue)
		PRINT(CUST.DATA.noOfRecords)
		#}
	ELSE
		#{
		sv_u = urhk_SRV_SetErr("Error:urhk_SRV_NumOfRecs failed")
		BANCS.OUTPUT.successOrFailure = "F"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_l = 0
	sv_m = ""
	sv_p = ""
	sv_n = 0
	sv_t = 0
	
	WHILE(sv_l < CUST.DATA.noOfRecords)
	#{
		sv_v = Format$(sv_l,"%d")
		sv_r = "<rec_"+sv_v+">"
		#-------------------------------------------------------------------------------------------
		# Fetch A/c - foracid
		#-------------------------------------------------------------------------------------------
		sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".acctId.foracid")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			#("CUST").("DATA").("foracid_"+sv_r) = BANCS.OUTPARAM.srvValue
			#CUST.DATA.foracid = ("CUST").("DATA").("foracid_"+sv_r)
			CUST.DATA.foracid = BANCS.OUTPARAM.srvValue
			PRINT(CUST.DATA.foracid)
			#}
		ELSE
			#{
			sv_u = urhk_SRV_SetErr("Error: urhk_SRV_GetVal failed for foracid")
			BANCS.OUTPUT.successOrFailure="F"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".partTranType.code")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			("CUST").("DATA").("partTran _"+sv_r) = BANCS.OUTPARAM.srvValue
			CUST.DATA.partTran = ("CUST").("DATA").("partTran _"+sv_r)
			PRINT(CUST.DATA.partTran)
			#}
		ELSE
			#{
			sv_u = urhk_SRV_SetErr("Error: urhk_SRV_GetVal failed for part Tran")
			BANCS.OUTPUT.successOrFailure="F"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".delFlg")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			CUST.DATA.delFlg = BANCS.OUTPARAM.srvValue
			PRINT(CUST.DATA.delFlg)
			#}
		ELSE
			#{
			sv_u = urhk_SRV_SetErr("Error: urhk_SRV_GetVal failed for delFlg")
			BANCS.OUTPUT.successOrFailure="F"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		##### start validate charge dormant
		CUST.DATA.debitdormantskip="N"
		
		sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".tranParticulars")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			CUST.DATA.tranParticulars = BANCS.OUTPARAM.srvValue
			PRINT(CUST.DATA.tranParticulars)
			IF (CUST.DATA.tranParticulars=="COMM ON INWD REM") THEN
				CUST.DATA.debitdormantskip="Y"
				print(CUST.DATA.debitdormantskip)
			ENDIF
			#}
		ELSE
			#{
			#sv_u = urhk_SRV_SetErr("Error: urhk_SRV_GetVal failed for tranParticulars")
			#BANCS.OUTPUT.successOrFailure="F"
			#GOTO ENDOFSCRIPT
		#}
		ENDIF
		sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".tranRmks")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			CUST.DATA.tranRmks = BANCS.OUTPARAM.srvValue
			PRINT(CUST.DATA.tranRmks)
			#}
		ELSE
			#{
			#sv_u = urhk_SRV_SetErr("Error: urhk_SRV_GetVal failed for tranRmks")
			#BANCS.OUTPUT.successOrFailure="F"
			#GOTO ENDOFSCRIPT
		#}
		ENDIF
		sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".tranParticulars2")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			CUST.DATA.tranParticulars2 = BANCS.OUTPARAM.srvValue
			PRINT(CUST.DATA.tranParticulars2)
			#}
		ELSE
			#{
			#sv_u = urhk_SRV_SetErr("Error: urhk_SRV_GetVal failed for tranParticulars2")
			#BANCS.OUTPUT.successOrFailure="F"
			#GOTO ENDOFSCRIPT
		#}
		ENDIF
		sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".refNum")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			CUST.DATA.refNum = BANCS.OUTPARAM.srvValue
			PRINT(CUST.DATA.refNum)
			#}
		ELSE
			#{
			#sv_u = urhk_SRV_SetErr("Error: urhk_SRV_GetVal failed for refNum")
			#BANCS.OUTPUT.successOrFailure="F"
			#GOTO ENDOFSCRIPT
		#}
		ENDIF
		##### end validate charge dormant 
		#sv_l = sv_l + 1
		#PRINT(sv_l)
		
		IF (CUST.DATA.delFlg == "Y") THEN
		#{
			GOTO NEXTREC
		#}
		ENDIF
		
		PRINT(CUST.DATA.foracid)
		IF (CUST.DATA.foracid != "") THEN
		#{
			# Get its ACID and chk for the Account Status Flag
			sv_s = "acctStat|SELECT acct_status "
			sv_s = sv_s + "FROM TBAADM.SMT "
			sv_s = sv_s + "WHERE acid = "
			sv_s = sv_s + "( "
			sv_s = sv_s + "SELECT acid "
			sv_s = sv_s + "FROM TBAADM.GAM "
			sv_s = sv_s + "WHERE foracid = ?SVAR "
			sv_s = sv_s + "AND bank_id = ?SVAR "
			sv_s = sv_s + "AND del_flg = 'N' "
			sv_s = sv_s + "AND entity_cre_flg = 'Y' "
			sv_s = sv_s + "AND acct_cls_flg = 'N' "
			sv_s = sv_s + ") "
			sv_s = sv_s + " AND bank_id = ?SVAR"
			BANCS.INPARAM.BINDVARS = CUST.DATA.foracid  + "|" + BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			PRINT(sv_s)
			sv_d = urhk_dbselectWithBind(sv_s)
			print(sv_d)
			IF (sv_d != 0) THEN
			#{
				##GOTO ENDOFSCRIPT
				print("not in SMT")
				CUST.DATA.acctStat =""
				print(CUST.DATA.acctStat)
			ELSE
				CUST.DATA.acctStat = BANCS.OUTPARAM.acctStat
				PRINT(CUST.DATA.acctStat)
			#}
			ENDIF

		#}
		ENDIF
		
		IF ((CUST.DATA.acctStat == "D") AND (CUST.DATA.tranSubType == "BI")) THEN
		#{
			IF (CUST.DATA.partTran == "D") THEN
			#{
				sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".key.serial_num")
				PRINT(sv_u)
				IF (sv_u == 0) THEN
				#{
					sv_m = sv_m  + BANCS.OUTPARAM.srvValue + ","
				#}
				ENDIF
				PRINT(sv_m)
				sv_n = sv_n + 1
			#}
			ENDIF
		#}
		ENDIF
		
		IF ((CUST.DATA.acctStat == "I") AND (CUST.DATA.tranSubType == "BI") AND (BANCS.STDIN.contextBankId!="54")) THEN
		#{
			IF (CUST.DATA.partTran == "D") THEN
			#{
				sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".key.serial_num")
				PRINT(sv_u)
				IF (sv_u == 0) THEN
				#{
					sv_p = sv_p + BANCS.OUTPARAM.srvValue + ","
				#}
				ENDIF
				PRINT(sv_p)
				sv_t = sv_t + 1
			#}
			ENDIF
		#}
		ENDIF
		NEXTREC:
			sv_l = sv_l + 1
			PRINT(sv_l)
		#}
		DO
		
		IF (sv_n > 0) THEN
		#{
			IF (CUST.DATA.debitdormantskip=="N") THEN
				BANCS.OUTPUT.successOrFailure="F"
				sv_m = RTRIM(sv_m ,',')
				sv_m = "Transaction not allowed for Dormant Accounts with part tran serial num:"+  sv_m
				sv_u = urhk_SRV_SetErr(sv_m)
			ENDIF
		#}
		ENDIF
		
		IF (sv_t > 0) THEN
		#{
			IF (CUST.DATA.debitdormantskip=="N") THEN
				BANCS.OUTPUT.successOrFailure="F"
				sv_p = RTRIM(sv_p ,',')
				sv_p = "Transaction not allowed for Inactive Accounts with part tran serial num:"+  sv_p
				sv_u = urhk_SRV_SetErr(sv_p)
			ENDIF
		#}
		ENDIF
		
	ENDOFSCRIPT:
		#-------------------------------------------------------------------------------------------------------
		# Deleting the Class and Repository
		#-------------------------------------------------------------------------------------------------------
		IF (CLASSEXISTS("CUST","DATA") == 1) THEN
		#{
			#DELETECLASS("CUST","DATA")
		#}
		ENDIF
		
		IF (REPEXISTS("CUST") == 1) THEN
		#{
			#DELETEREP("CUST")
		#}
		ENDIF
		
		EXITSCRIPT
		#TRACE OFF
END-->
