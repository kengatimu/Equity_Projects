#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: denomdp019.scr
# Type: NA|default
# Description: denomdp019.scr
# Author:
# Date:
# Modification Log:
#============================================================

################################################################################################
#  Name			:  denomdp019.scr
#  Date			:  19-Jan-2007
#  Description		:  This script fetches the denomination details entered for the input tran id
#				and sends it to the front end
#  Input		:  Tran Id, Tran Date
#  Output		:  Denomination details
#  Author		:  Mandar
#  CallingScript	:  SRV_ModifyAndPostTrans_Post_Process.scr
#  Called JSP		:  -
#  Menu Option		:
#  MODULE CODE          :  DENOM
#
#  Modification History	:
#  ====================
#  Serial No.	DATE		Author			Description
#	0.2		26-AUG-2011	Arathilakshmi		Edited to include HGCHRG/HTUTM menu
################################################################################################

<--START
CHECKSUM="fa180cb90b6b30a41a6faca28ee0a581333ab5e629f354bb3e552d004babeb98"
	TRACE ON
	BANCS.INPARAM.BINDVARS = CUST.DENOM.tranId + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_l = 0
	sv_l = "DELETE FROM custom.c_detd "
	sv_l = sv_l + "WHERE dxfer_id = LPAD(?SVAR,9) AND bank_id=?SVAR "
	PRINT(sv_l)
	sv_v = urhk_dbsqlWithBind(sv_l)
	PRINT(sv_v)
	IF (sv_v != 0) THEN
	#{
		sv_u = urhk_setOrbOut("Message|N")
		PRINT(sv_u)
		sv_u = urhk_setOrbOut("Details|DataBase Error")
		PRINT(sv_u)
		GOTO FINISH
	#}
	ENDIF
	BANCS.INPARAM.BINDVARS = CUST.DENOM.tranId  + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_l = 0
	sv_l = "DELETE FROM custom.c_derd "
	sv_l = sv_l + "WHERE dxfer_id = LPAD(?SVAR,9) AND bank_id=?SVAR "
	PRINT(sv_l)
	sv_v = urhk_dbsqlWithBind(sv_l)
	PRINT(sv_v)
	IF (sv_v != 0) THEN
	#{
		sv_l = 0
	#}
	ENDIF
END-->
