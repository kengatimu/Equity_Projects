#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: SRV_AddAndPostXferTran_post_process_data.scr
# Type: NA|default
# Description: SRV_AddAndPostXferTran_post_process_data.scr
# Author:
# Date:
# Modification Log:
#============================================================

#-------------------------------------------------------------------------------------
#    Name				: SRV_AddAndPostXferTran_post_process_data.scr
#    Description     	: The script is used to execute through Standing Instruction Maintainence
#    Date            	: 15-03-2013
#    Author          	: Divya
#    Input           	: NA
#    Output				: NA
#    Called Script		: s_chqopndp001.scr
#    Calling Script		: NA
#    Menu Option		: HTM
#    Srl. No		Date			Author			Description.
#    -------		----			------			----------------
#    1.0			15-03-2013		Divya	 		Original Version
#    2.0            12-09-2013      Swati Ganjoo    Bypass script for CUBP
#-------------------------------------------------------------------------------------
<--START
CHECKSUM="f685c5dc07e4847be9f8476aab59a597b40ffebda2cfe3910e0cfc57e550f83b"
	#TRACE ON
	
	IF(FIELDEXISTS(BANCS.STDIN.menuId))THEN
	#{
		IF(BANCS.STDIN.menuId == "CIETXN")THEN
		#{
			EXITSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.STDIN.menuId))THEN
	#{
		IF(BANCS.STDIN.menuId == "CUBP")THEN
		#{
			EXITSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.STDIN.moduleIdentity))THEN
	#{
		IF(BANCS.STDIN.moduleIdentity == "FI")THEN
		#{
                        #Vino Palani 31/12/2019
			#+++++++++++++++++++++++
                        sv_u = urhk_GetChannelInfo("")
                        if(sv_u==0)then
                                BANCS.INPUT.dccIdd = BANCS.OUTPARAM.DccId
                        else
                                BANCS.INPUT.dccIdd = ""
                        endif
			if(BANCS.INPUT.dccIdd=="VER")then
				#############################callscriptifexist("ebTmValVer.scr")
			endif
			#14/02/2020 
			#+++++++++++++
			if(BANCS.INPUT.dccIdd=="MYS")then
                        if((BANCS.STDIN.contextBankId=="54") or (BANCS.STDIN.contextBankId=="56") or (BANCS.STDIN.contextBankId=="50") or (BANCS.STDIN.contextBankId=="55"))then
                                sv_u = urhk_SRV_GetVal("tranHdr.tranIdentifier.tranId")
                                BANCS.INPUT.intranId = TRIM(BANCS.OUTPARAM.srvValue)
                                print(BANCS.INPUT.intranId)
                                if(BANCS.INPUT.intranId !="")then
					sv_a = "nohup babx4061 $B2K_SESSION_ID ebFinFinLt.scr  "+trim(BANCS.INPUT.intranId)+" &"
					sv_a = system(sv_a)
					exitscript
                                endif
                        endif
                        endif
                        if(BANCS.INPUT.dccIdd=="OMN")then
                        if(BANCS.STDIN.contextBankId=="54")then
                                sv_u = urhk_SRV_GetVal("tranHdr.tranIdentifier.tranId")
                                BANCS.INPUT.intranId = trim(BANCS.OUTPARAM.srvValue)
                                print(BANCS.INPUT.intranId)
                                if(BANCS.INPUT.intranId !="")then
					sv_a = "nohup babx4061 $B2K_SESSION_ID ebInwardRemitLimit.scr  "+trim(BANCS.INPUT.intranId)+" &"
					sv_s = system(sv_a)
					print(sv_s)
					exitscript
                                endif

                        endif
                        endif
			EXITSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	IF(BANCS.STDIN.onlineOrBatch == "O") THEN
	#{
		CALL("s_chargedp001.scr")
		CALL ("s_chqopndp001.scr")
		CALL("s_odchrgcalcmn001.scr")
	#}
	ENDIF
        if(fieldexists(BANCS.STDIN.menuId))then
                if(BANCS.STDIN.menuId == "HTM")then
			if(BANCS.STDIN.contextBankId=="54")then
				#OD recovery Reconcillation
				sv_u = urhk_SRV_GetVal("tranHdr.tranIdentifier.tranId")
				print(sv_u)
				BANCS.INPUT.intranId = TRIM(BANCS.OUTPARAM.srvValue)
				print(BANCS.INPUT.intranId)
				if(BANCS.INPUT.intranId !="")then
					sv_a = "nohup babx4061 $B2K_SESSION_ID ebOdaAgentMnRec.scr  "+trim(BANCS.INPUT.intranId)+" &"
					print(sv_a)
					sv_a = system(sv_a)
					print(sv_a)
				endif
				print(BANCS.INPUT.intranId)
				if(BANCS.INPUT.intranId !="")then
				#	sv_a = "nohup babx4061 $B2K_SESSION_ID ebSafaricomFloatTran.scr  "+trim(BANCS.INPUT.intranId)+" &"
				#	print(sv_a)
				#	sv_a = system(sv_a)
				#	print(sv_a)
				endif
				callscriptifexist("ebSafFloatTran.scr")
			endif
                endif
        endif
        if(fieldexists(BANCS.STDIN.menuId))then
                if(BANCS.STDIN.contextBankId=="54")then
                        if(BANCS.STDIN.menuId=="HTM")then
                                callscriptifexist("vptmamlDataIn.scr")
                        endif
                endif
        endif

	
TRACE OFF	
END-->
