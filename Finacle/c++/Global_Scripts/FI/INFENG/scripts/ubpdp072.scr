#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: ubpdp072.scr
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START
CHECKSUM="a1fe58a294e609cb5325d4b29dad659d842ccbaa597381ff794c5ecf8a11878c"
	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp072.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets on click of submit of delete mode.This script deletes the
	#                               entry from custom.c_ubp table.
	# Called Script         :
	# Calling Function      :		function fngetSetNum()
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           10-09-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	CUST.CIECLG.scriptSuccess = "Y"
	BANCS.INPARAM.BINDVARS = CUST.CIECLG.zoneCode + "|" + CUST.CIECLG.zoneDate
	#BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.CIECLG.crAcctNum
	print(BANCS.INPARAM.BINDVARS)
	sv_s = "DELETE  FROM CUSTOM.C_UBP "
	sv_s = sv_s + " WHERE CLG_ZONE_CODE = ?SVAR "
	sv_s = sv_s + " AND CLG_ZONE_DATE = TO_DATE(?SVAR,'DD-MM-YYYY') "
	#sv_s = sv_s + " AND BILLER_BNK_ID = ?SVAR "
	sv_s = sv_s + " AND trim(SET_NUM) = '" + CUST.CIECLG.setNum + "' "
	sv_s = sv_s + " AND BILLER_ACCT_ID = ?SVAR "
	sv_d = urhk_dbSQLWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d!=0)THEN
	#{
		CUST.CIECLG.scriptSuccess = "N"
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		print(sv_u)
		sv_u = urhk_SetOrbOut("Error_1|Error^Database Delete Operation Failed ON Custom Table^funcCode")
		print(sv_u)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	ENDOFSCRIPT:
		exitscript
END-->
