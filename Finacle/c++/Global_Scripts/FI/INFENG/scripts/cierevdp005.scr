##########################################################################################
# Source Name           : cierevdp005.scr
# Date                  : 13-Mar-2013
# Description           : Txn Reverse Script
# Author                :  Priyanka T02
# Modification History  :
# <Serial No.>  <Date>        <Author Name>             <Description>
# ------------  -----------   -----------------         --------------
#      1        13-Mar-2013    Priyanka T02               Base Version
###########################################################################################

<--START
CHECKSUM="971453894cd741d694e03cce8b2ee5e33eebcf6ebe677a4244ba6e5d346414ff"
	TRACE ON
	
	print(CUST.CIEREV.tranId)
	print(CUST.CIEREV.tranDate)
	
	CUST.CIEREV.scriptSuccess = "Y"
	
	###########################################################################################
	# Transaction Reversal Service
	###########################################################################################
	
	sv_a=urhk_SetUrhkInp("revTranCrit.tran.tranId|" + CUST.CIEREV.tranId)
	print(sv_a)
	
	sv_a=urhk_SetUrhkInp("revTranCrit.tran.tranDate|" + CUST.CIEREV.tranDate)
	print(sv_a)
	
	###########################################################################################
	# Fetch Reverse Tran Request Service
	###########################################################################################
	
	IF(CUST.CIEREV.srvcallmode == "E")THEN
	#{
		sv_u = urhk_ExecSrvNoCommit("SRV_FetchDataForTranRev|same_user_verify = Y|retain_all_output = Y")
		print(sv_u)
		#}
	ELSE
		#{
		sv_u = urhk_ExecSrvNoCommit("SRV_FetchDataForTranRev|same_user_verify = Y|retain_all_output = Y")
		print(sv_u)
		
		IF(sv_u != 0)THEN
		#{
			sv_u = urhk_CopyOutToIn("")
			print(sv_u)
			
			sv_u = urhk_ExecSrvNoCommit("SRV_FetchDataForTranRev|same_user_verify = Y|retain_all_output = Y")
			print(sv_u)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(sv_u != 0)THEN
	#{
		CALL("cierevdp006.scr")
		
		IF(CUST.CIEREV.srvError == "Y")THEN
		#{
			CUST.CIEREV.scriptSuccess = "N"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	sv_u = urhk_CopyOutToIn("")
	print(sv_u)
	
	###########################################################################################
	# Reverse Tran Request Service
	###########################################################################################
	
	sv_u = urhk_SetUrhkOut("revTranCrit.refNum|refNum")
	print(sv_u)
	sv_u=urhk_SetUrhkInp("revPTranLL.<rec_0>.selRec.code|Y")
	print(sv_u)
	sv_u=urhk_SetUrhkInp("revTran.tranDetail.pstFlg|Y")
	print(sv_u)
	
	IF(CUST.CIEREV.srvcallmode == "E")THEN
	#{
		sv_u = urhk_ExecSrvNoCommit("SRV_RevTranRequest|same_user_verify = Y|retain_all_output = Y")
		print(sv_u)
		#}
	ELSE
		#{
		sv_u = urhk_ExecSrvNoCommit("SRV_RevTranRequest|same_user_verify = Y|retain_all_output = Y")
		print(sv_u)
		
		IF(sv_u != 0)THEN
		#{
			sv_u = urhk_CopyOutToIn("")
			print(sv_u)
			
			sv_u = urhk_ExecSrvNoCommit("SRV_RevTranRequest|same_user_verify = Y|retain_all_output = Y")
			print(sv_u)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(sv_u != 0)THEN
	#{
		CALL("cierevdp006.scr")
		
		IF(CUST.CIEREV.srvError == "Y")THEN
		#{
			CUST.CIEREV.scriptSuccess = "N"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	print(BANCS.OUTPARAM.refNum)
	
	CUST.CIEREV.refNum = BANCS.OUTPARAM.refNum
	print(CUST.CIEREV.refNum)
	
	sv_u = urhk_CopyOutToIn("")
	PRINT(sv_u)
	
	###########################################################################################
	# Fetch Reverse Tran For Verify Service
	###########################################################################################
	
	IF(CUST.CIEREV.srvcallmode == "E")THEN
	#{
		sv_u = urhk_ExecSrvNoCommit("SRV_FetchDataForTranRevVerify|same_user_verify = Y|retain_all_output = Y")
		print(sv_u)
		#}
	ELSE
		#{
		sv_u = urhk_ExecSrvNoCommit("SRV_FetchDataForTranRevVerify|same_user_verify = Y|retain_all_output = Y")
		print(sv_u)
		
		IF(sv_u != 0)THEN
		#{
			sv_u = urhk_CopyOutToIn("")
			print(sv_u)
			
			sv_u = urhk_ExecSrvNoCommit("SRV_FetchDataForTranRevVerify|same_user_verify = Y|retain_all_output = Y")
			print(sv_u)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(sv_u != 0)THEN
	#{
		CALL("cierevdp006.scr")
		
		IF(CUST.CIEREV.srvError == "Y")THEN
		#{
			CUST.CIEREV.scriptSuccess = "N"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	sv_u = urhk_CopyOutToIn("")
	PRINT(sv_u)
	
	###########################################################################################
	# Reverse Tran Verify Service
	###########################################################################################
	
	sv_u = urhk_SetUrhkOut("revTran.tranDetail.tranIdentifier.tranDate|revTranDate")
	sv_u = urhk_SetUrhkOut("revTran.tranDetail.tranIdentifier.tranId|revTranId")
	
	IF(CUST.CIEREV.srvcallmode == "E")THEN
	#{
		sv_u = urhk_ExecSrvNoCommit("SRV_RevTran|same_user_verify = Y|retain_all_output = Y")
		print(sv_u)
		#}
	ELSE
		#{
		sv_u = urhk_ExecSrvNoCommit("SRV_RevTran|same_user_verify = Y|retain_all_output = Y")
		print(sv_u)
		
		IF(sv_u != 0)THEN
		#{
			sv_u = urhk_CopyOutToIn("")
			print(sv_u)
			
			sv_u = urhk_ExecSrvNoCommit("SRV_RevTran|same_user_verify = Y|retain_all_output = Y")
			print(sv_u)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(sv_u != 0)THEN
	#{
		CALL("cierevdp006.scr")
		
		IF(CUST.CIEREV.srvError == "Y")THEN
		#{
			CUST.CIEREV.scriptSuccess = "N"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	PRINT(BANCS.OUTPARAM.revTranId)
	PRINT(BANCS.OUTPARAM.revTranDate)
	
	CUST.CIEREV.revTranId = BANCS.OUTPARAM.revTranId
	CUST.CIEREV.revTranDate = MID$(BANCS.OUTPARAM.revTranDate,0,10)
	CUST.CIEREV.revTranDate1 = BANCS.OUTPARAM.revTranDate
	
	sv_u = urhk_SetUrhkInp("tranHdr.tranIdentifier.tranId|"+CUST.CIEREV.revTranId)
	sv_u = urhk_SetUrhkInp("tranHdr.tranIdentifier.tranDate|"+CUST.CIEREV.revTranDate1)
	
	IF(CUST.CIEREV.srvcallmode == "E")THEN
	#{
		sv_u = urhk_ExecSrvNoCommit("SRV_FetchTranDtlsForPost|same_user_verify = Y|retain_all_output = Y")
		print(sv_u)
		#}
	ELSE
		#{
		sv_u = urhk_ExecSrvNoCommit("SRV_FetchTranDtlsForPost|same_user_verify = Y|retain_all_output = Y")
		print(sv_u)
		
		IF(sv_u != 0)THEN
		#{
			sv_u = urhk_CopyOutToIn("")
			print(sv_u)
			
			sv_u = urhk_ExecSrvNoCommit("SRV_FetchTranDtlsForPost|same_user_verify = Y|retain_all_output = Y")
			print(sv_u)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(sv_u != 0)THEN
	#{
		CALL("cierevdp006.scr")
		
		IF(CUST.CIEREV.srvError == "Y")THEN
		#{
			CUST.CIEREV.scriptSuccess = "N"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	sv_u = urhk_CopyOutToIn("")
	print(sv_u)
	
	IF(CUST.CIEREV.srvcallmode == "E")THEN
	#{
		sv_u = urhk_ExecSrvNoCommit("SRV_PostTran|same_user_verify = Y|retain_all_output = Y")
		print(sv_u)
		#}
	ELSE
		#{
		sv_u = urhk_ExecSrvNoCommit("SRV_PostTran|same_user_verify = Y|retain_all_output = Y")
		print(sv_u)
		
		IF(sv_u != 0)THEN
		#{
			sv_u = urhk_CopyOutToIn("")
			print(sv_u)
			
			sv_u = urhk_ExecSrvNoCommit("SRV_PostTran|same_user_verify = Y|retain_all_output = Y")
			print(sv_u)
		#}
		ENDIF
	#}
	ENDIF
	
	
	
	
	ENDOFSCRIPT:
		
		EXITSCRIPT
		
END-->
