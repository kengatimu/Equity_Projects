#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: accmmn001.scr
# event: NA
# description: NA
#=================================================
#**************************************************************************************************************
# Filename              : accmmn001.scr
# Description           : The script is used fetching various data for the frontend for various functions
# Input                 :
# Output                :
# Called Script         :
# Calling Script        : accmdp001.scr,accmdp002.scr,accmdp003.scr,accmdp005.scr,accmdp004.scr
#			  accmdp006.scr,accmdp007.scr,accmdp008.scr
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----          ------           --------          ----------------        ---------

#  1         25-04-2013       Kalvin Joseph		 original		  1.0
#  2         24-07-2013       Bharath		 Modified for TOL 361668		  2.0
#  3		03-09-2013	  Kalvin		MODIFIED for 369221
#**************************************************************************************************************


<--START
CHECKSUM="0f396518abf3b0587c8724b8d5966fba3a8bcd4a9f66281531cee0f201c3d5f7"
	# Script body goes here
	#TRACE ON
	
	CALL("createCustRepos.scr")
	
	#sv_u = urhk_B2k_PrintRepos("BANCS")
	
	CUST.CONTRACT.funcCode			=""
	CUST.CONTRACT.acctName			=""
	CUST.CONTRACT.acctNum			=""
	CUST.CONTRACT.solId			=""
	CUST.CONTRACT.schmCode			=""
	CUST.CONTRACT.minAmt			=""
	CUST.CONTRACT.maxAmt			=""
	CUST.CONTRACT.contractAmt		=""
	CUST.CONTRACT.contractPeriod		=""
	CUST.CONTRACT.contractId		=""
	CUST.CONTRACT.charges			=""
	
	
	#---------------------------------------------------------------------------
	# Fetching the values for the details page
	#---------------------------------------------------------------------------
	
	IF(FIELDEXISTS(BANCS.INPUT.funcCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.funcCode) != "")THEN
		#{
			CUST.CONTRACT.funcCode = TOUPPER(TRIM(BANCS.INPUT.funcCode))
			print(CUST.CONTRACT.funcCode)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.acctName))THEN
	#{
		IF(TRIM(BANCS.INPUT.acctName) != "")THEN
		#{
			CUST.CONTRACT.acctName = TOUPPER(TRIM(BANCS.INPUT.acctName))
			print(CUST.CONTRACT.acctName)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.acctNum))THEN
	#{
		IF(TRIM(BANCS.INPUT.acctNum) != "")THEN
		#{
			CUST.CONTRACT.acctNum = TOUPPER(TRIM(BANCS.INPUT.acctNum))
			print(CUST.CONTRACT.acctNum)
			#------------------------------------------------------------------------------
			#  Validating Account Status
			#------------------------------------------------------------------------------
			sv_s = "acid|"
			sv_s = sv_s + " SELECT GAM.acid"
			sv_s = sv_s + " FROM TBAADM.GAM"
			sv_s = sv_s + " WHERE GAM.foracid = ?SVAR"
			sv_s = sv_s + " AND GAM.bank_id = ?SVAR"
			sv_s = sv_s + " AND GAM.entity_cre_flg = 'Y'"
			sv_s = sv_s + " AND GAM.del_flg = 'N'"
			sv_s = sv_s + " AND GAM.acct_cls_flg = 'N'"
			print(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.CONTRACT.acctNum + "|" + BANCS.STDIN.contextBankId
			sv_d=urhk_dbSelectWithBind(sv_s)
			print(sv_d)
			
			IF (sv_d == 0) THEN
			#{
				CUST.CONTRACT.acid = BANCS.OUTPARAM.acid
				print(CUST.CONTRACT.acid)
				#}
			ELSE
				#{
				CUST.CONTRACT.errorMsg = "Account is Invalid"
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				print(sv_u)
				sv_u = urhk_SetOrbOut("Error_0|ERROR^" + CUST.CONTRACT.errorMsg + "^acctNum")
				print(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			
			#---------------------------------------------------------------
			#Checking the status of the account using userhook
			#---------------------------------------------------------------
			BANCS.INPARAM.acid = CUST.CONTRACT.acid
			IF(CUST.CONTRACT.acid != "") THEN
			#{
				sv_e = urhk_getAcctStatus(BANCS.INPARAM.acid)
				PRINT(sv_e)
				IF(sv_e == 0) THEN
				#{
					CUST.CONTRACT.acctStatus = BANCS.OUTPARAM.acctStatus
					#}
				ELSE
					#{
					CUST.CONTRACT.acctStatus = ""
				#}
				ENDIF
				#}
			ELSE
				#{
				CUST.CONTRACT.acctStatus = ""
			#}
			ENDIF
			
			PRINT(CUST.CONTRACT.acctStatus)
			IF (BANCS.STDIN.contextBankId=="54") THEN
			#{
				IF (CUST.CONTRACT.acctStatus=="D") THEN
				#{
					#CUST.CONTRACT.errorMsg = "Account is Dormant/Inactive"
					CUST.CONTRACT.errorMsg = "Account is Dormant"
					sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
					print(sv_u)
					sv_u = urhk_SetOrbOut("Error_0|ERROR^" + CUST.CONTRACT.errorMsg + "^acctNum")
					print(sv_u)
					GOTO ENDOFSCRIPT
				#}
				ENDIF
			#}
			ELSE
			#{
				IF ((CUST.CONTRACT.acctStatus=="I") OR (CUST.CONTRACT.acctStatus=="D")) THEN
				#{
					CUST.CONTRACT.errorMsg = "Account is Dormant/Inactive"
					sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
					print(sv_u)
					sv_u = urhk_SetOrbOut("Error_0|ERROR^" + CUST.CONTRACT.errorMsg + "^acctNum")
					print(sv_u)
					GOTO ENDOFSCRIPT
				#}
				ENDIF

			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.solId))THEN
	#{
		IF(TRIM(BANCS.INPUT.solId) != "")THEN
		#{
			CUST.CONTRACT.solId = TOUPPER(TRIM(BANCS.INPUT.solId))
			print(CUST.CONTRACT.solId)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.schmCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.schmCode) != "")THEN
		#{
			CUST.CONTRACT.schmCode = TOUPPER(TRIM(BANCS.INPUT.schmCode))
			print(CUST.CONTRACT.schmCode)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.minAmt))THEN
	#{
		IF(TRIM(BANCS.INPUT.minAmt) != "")THEN
		#{
			CUST.CONTRACT.minAmt = TOUPPER(TRIM(BANCS.INPUT.minAmt))
			print(CUST.CONTRACT.minAmt)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.maxAmt))THEN
	#{
		IF(TRIM(BANCS.INPUT.maxAmt) != "")THEN
		#{
			CUST.CONTRACT.maxAmt = TOUPPER(TRIM(BANCS.INPUT.maxAmt))
			print(CUST.CONTRACT.maxAmt)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.contractAmt))THEN
	#{
		IF(TRIM(BANCS.INPUT.contractAmt) != "")THEN
		#{
			CUST.CONTRACT.contractAmt = TOUPPER(TRIM(BANCS.INPUT.contractAmt))
			print(CUST.CONTRACT.contractAmt)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.contractPeriod))THEN
	#{
		IF(TRIM(BANCS.INPUT.contractPeriod) != "")THEN
		#{
			CUST.CONTRACT.contractPeriod = TOUPPER(TRIM(BANCS.INPUT.contractPeriod))
			print(CUST.CONTRACT.contractPeriod)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.contractId))THEN
	#{
		IF(TRIM(BANCS.INPUT.contractId) != "")THEN
		#{
			CUST.CONTRACT.contractId = TOUPPER(TRIM(BANCS.INPUT.contractId))
			print(CUST.CONTRACT.contractId)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.charges))THEN
	#{
		IF(TRIM(BANCS.INPUT.charges) != "")THEN
		#{
			CUST.CONTRACT.charges = TOUPPER(TRIM(BANCS.INPUT.charges))
			print(CUST.CONTRACT.charges)
		#}
		ENDIF
	#}
	ENDIF
	
	#sv_u = urhk_b2k_printrepos("BANCS")
	#print(sv_u)
	
	#-----------------------------------Modified for TOL 369221---------------------------#
	BANCS.INPARAM.BINDVARS =CUST.CONTRACT.acctNum	 + "|" + "N" + "|" + "Y"
	
	sv_s ="schmType,schmCode|"
	sv_s = sv_s + " SELECT SCHM_TYPE,SCHM_CODE FROM TBAADM.GAM "
	sv_s = sv_s + " WHERE  FORACID = ?SVAR "
	sv_s = sv_s + " AND DEL_FLG = ?SVAR "
	sv_s = sv_s + " AND ENTITY_CRE_FLG = ?SVAR"
	
	PRINT(sv_s)
	sv_u= urhk_dbSelectWithBind(sv_s)
	PRINT(sv_u)
	
	IF (sv_u == 0) THEN
	#{
		CUST.CONTRACT.schmType=BANCS.OUTPARAM.schmType
		CUST.CONTRACT.schmCode=BANCS.OUTPARAM.schmCode
		#}
	ELSE
		#{
		CUST.CONTRACT.errorMsg="UNABLE TO FETCH SCHM TYPE FROM TABLE"
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		print(sv_u)
		sv_u = urhk_SetOrbOut("Error_0|ERROR^" + CUST.CONTRACT.errorMsg + "^acctNum")
		print(sv_u)
		GOTO  ENDOFSCRIPT
		#}
		
	#}
	ENDIF
	
	IF((CUST.CONTRACT.funcCode=="A") OR (CUST.CONTRACT.funcCode=="M") OR (CUST.CONTRACT.funcCode=="E") ) THEN
	#{
		PRINT(CUST.CONTRACT.schmType)
		#-----------------------------------------------------------
		#               Fetching valid schmecode:
		#-----------------------------------------------------------
		BANCS.INPUT.moduleName = "CATODSCORE"
		BANCS.INPUT.parameterId = "ALWDSCHM"
		CALL("getcparamvalue.scr")
		
		IF (BANCS.OUTPUT.parameterVal != "")THEN
		#{
			print(BANCS.OUTPUT.parameterVal)
			sv_l = STRLEN(BANCS.OUTPUT.parameterVal)
			print(sv_l)
			CUST.CONTRACT.sType=BANCS.OUTPUT.parameterVal
			print(CUST.CONTRACT.sType)
			WHILE(sv_l > 0)
			#{
				sv_g = GETPOSITION(CUST.CONTRACT.sType,",")
				print(sv_g)
				IF (sv_g == 0) THEN
				#{
					CUST.CONTRACT.alwSchmType = CUST.CONTRACT.sType
					print(CUST.CONTRACT.alwSchmType)
					CUST.CONTRACT.sType = ""
					#}
				ELSE
					#{
					CUST.CONTRACT.alwSchmType = TRIM(MID$(CUST.CONTRACT.sType,0,sv_g - 1))
					CUST.CONTRACT.sType = MID$(CUST.CONTRACT.sType,sv_g,sv_l)
				#}
				ENDIF
				print(CUST.CONTRACT.alwSchmType)
				IF (CUST.CONTRACT.schmCode ==CUST.CONTRACT.alwSchmType) THEN
				#{
					# {
					GOTO LABEL1
				#}
				ENDIF
				print(CUST.CONTRACT.sType)
				sv_l = STRLEN(CUST.CONTRACT.sType)
				print(sv_l)
				IF (sv_l == 0) THEN
				#{
					CUST.CONTRACT.errMsg = "Scheme Code not allowed for this menu"
					sv_e = CUST.CONTRACT.errMsg
					sv_m = "funcCode"
					sv_e = "Error_1|ERROR:^"+ sv_e +".^"+ sv_m +""
					sv_u = urhk_SetOrbOut(sv_e)
					sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
					GOTO ENDOFSCRIPT
				#}
				ENDIF
				# }
			#}
			DO
		#}
		ELSE
			#{
			CUST.CONTRACT.errMsg = "ALLOWED SCHMECODE SETUP NOT DONE IN CPARAM"
			GOTO ENDOFSCRIPT
			# }
		#}
		ENDIF
		
	#}
	ENDIF
	
	LABEL1:
		#}
		
		
		
		
		#-----------------------------------Modified for TOL 369221---------------------------#
		
		
		
		#------------------------------------------------------------------------------------
		#Calling the corresponding scripts with respect to the function for various operation
		#-------------------------------------------------------------------------------------
		IF (CUST.CONTRACT.funcCode=="A") THEN
		#{
			CALL("accmdp001.scr")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		IF (CUST.CONTRACT.funcCode=="M") THEN
		#{
			CALL("accmdp002.scr")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		IF (CUST.CONTRACT.funcCode=="X") THEN
		#{
			CALL("accmdp003.scr")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		IF (CUST.CONTRACT.funcCode=="T") THEN
		#{
			CALL("accmdp004.scr")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		IF (CUST.CONTRACT.funcCode=="V") THEN
		#{
			CALL("accmdp005.scr")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		IF (CUST.CONTRACT.funcCode=="I") THEN
		#{
			CALL("accmdp006.scr")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		IF (CUST.CONTRACT.funcCode=="E") THEN
		#{
			CALL("accmdp007.scr")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		IF (CUST.CONTRACT.funcCode=="Z") THEN
		#{
			CALL("accmdp008.scr")
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
		
		
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		print(sv_u)
		
	ENDOFSCRIPT:
		
END-->
