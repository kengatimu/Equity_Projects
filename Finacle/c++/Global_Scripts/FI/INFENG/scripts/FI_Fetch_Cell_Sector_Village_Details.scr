######################################################################
#
#
######################################################################

<--START
TRACE ON

  #---------------------------------------------------------
        # Create the necessary repos and classes
        #---------------------------------------------------------

        IF (REPEXISTS("CUST") == 0 ) THEN
        #{
                CREATEREP("CUST")
        #}
        ENDIF

        IF (CLASSEXISTS("CUST","DATA") == 0) THEN
        #{
                CREATECLASS("CUST","DATA",5)
        #}
        ENDIF

        #---------------------------------------------------------
        #       Bancs Repository Printing
        #---------------------------------------------------------

        sv_x = urhk_B2k_PrintRepos("BANCS")
        PRINT(sv_x)

        CUST.DATA.succFail="F"
        CUST.DATA.count = "0"

        IF(FIELDEXISTS(BANCS.INPUT.bankId)==0) THEN
        #{
                CUST.DATA.bankId = ""
        #}
        ELSE
        #{
                CUST.DATA.bankId = BANCS.INPUT.bankId
                PRINT(CUST.DATA.bankId)
        #}
        ENDIF

        IF(FIELDEXISTS(BANCS.INPUT.sector)==0) THEN
        #{
                CUST.DATA.sector = ""
        #}
        ELSE
        #{
                CUST.DATA.sector = BANCS.INPUT.sector
                PRINT(CUST.DATA.sector)
        #}
        ENDIF

        IF(FIELDEXISTS(BANCS.INPUT.cell)==0) THEN
        #{
                CUST.DATA.cell = ""
        #}
        ELSE
        #{
                CUST.DATA.cell = BANCS.INPUT.cell
                PRINT(CUST.DATA.cell)
        #}
        ENDIF

                IF(FIELDEXISTS(BANCS.INPUT.village)==0) THEN
        #{
                CUST.DATA.village = ""
        #}
        ELSE
        #{
                CUST.DATA.village = BANCS.INPUT.village
                PRINT(CUST.DATA.village)
        #}
        ENDIF

        IF(CUST.DATA.bankId == "") THEN
        #{
                sv_u = urhk_SetOrbOut("Failed| Please enter Bank Id")
                print(sv_u)

                EXITSCRIPT
        #}
        ENDIF

        IF(CUST.DATA.sector != "")THEN
        #{
                IF(CUST.DATA.sector != "SECTOR")THEN
                #{
                        sv_u = urhk_SetOrbOut("Failed| Kindly pass the correct value as 'sector'")
                        print(sv_u)

                        EXITSCRIPT
                #}
                ENDIF
        #}
        ENDIF

        IF(CUST.DATA.cell != "")THEN
        #{
                IF(CUST.DATA.cell != "CELL")THEN
                #{
                        sv_u = urhk_SetOrbOut("Failed| Kindly pass the correct value as 'cell'")
                        print(sv_u)

                        EXITSCRIPT
                #}
                ENDIF
        #}
        ENDIF

        IF(CUST.DATA.village != "")THEN
        #{
                IF(CUST.DATA.village != "VILLAGE")THEN
                #{
                        sv_u = urhk_SetOrbOut("Failed| Kindly pass the correct value as 'Village'")
                        print(sv_u)

                        EXITSCRIPT
                #}
                ENDIF
        #}
        ENDIF

        IF((CUST.DATA.bankId != "") and (CUST.DATA.sector != ""))THEN
        #{
                IF(CUST.DATA.sector == "SECTOR")THEN
                #{
                        BANCS.INPARAM.BINDVARS = CUST.DATA.bankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_a = "sectorCode,sectorDesc|select unique value, localetext"
                        sv_a = sv_a + " from crmuser.categories c, crmuser.category_lang l"
                        sv_a = sv_a + " where c.bank_id=?SVAR and l.bank_id=c.bank_id and categorytype='NIDA_SECTOR' and c.categoryid=l.categoryid"
                        print(sv_a)

                        sv_d = urhk_dbCursorOpenWithBind(sv_a)
                        print(sv_d)

                        IF(sv_d!=0) THEN
                        #{
                                sv_u = urhk_dbCursorClose(sv_a)
                                print(sv_u)

                                sv_u = urhk_SetOrbOut("Status| No record found")
                                print(sv_u)
                        #}
                        ELSE
                        #{
                                CUST.DATA.DB_CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
                                print(CUST.DATA.DB_CURSOR_NUMBER)

                                sv_c = urhk_dbCursorFetch(CUST.DATA.DB_CURSOR_NUMBER)
                                print(sv_c)

                                IF(sv_c != "0")THEN
                                #{
                                        sv_e = urhk_SetOrbOut("SuccessorFailure|Failed")
                                        sv_u = urhk_SetOrbOut("Failure| No record found")
                                        print(sv_u)
                                #}
                                ENDIF

                                while(sv_c ==0)
                                #{
                                        CUST.DATA.sectorCode = BANCS.OUTPARAM.sectorCode
                                        print(CUST.DATA.sectorCode)
                                        CUST.DATA.sectorDesc = BANCS.OUTPARAM.sectorDesc
                                        print(CUST.DATA.sectorDesc)

                                        CUST.DATA.count = CINT(CUST.DATA.count)+ 1
                                        print(CUST.DATA.count)

                                        sv_c = urhk_SetOrbOut("sectorDetails.<rec_"+CUST.DATA.count+">.sectorCode"+"| "+CUST.DATA.sectorCode)
                                        print(sv_c)

                                        sv_c = urhk_SetOrbOut("sectorDetails.<rec_"+CUST.DATA.count+">.sectorDesc"+"| "+CUST.DATA.sectorDesc)
                                        print(sv_c)

                                        sv_c = urhk_dbCursorFetch(CUST.DATA.DB_CURSOR_NUMBER)
                                        print(sv_c)
                                #}
                                do
                                sv_c = urhk_dbCursorClose(CUST.DATA.DB_CURSOR_NUMBER)
                                print(sv_c)
                        #}
                        ENDIF
                #}
                ELSE
                #{
                        sv_u = urhk_SetOrbOut("Failed| Kindly pass the correct value as 'sector'")
                        print(sv_u)

                        EXITSCRIPT
                #}
                ENDIF
        #}
        ENDIF

        IF((CUST.DATA.bankId != "") and (CUST.DATA.cell != ""))THEN
        #{
                IF(CUST.DATA.cell == "CELL")THEN
                #{
                        BANCS.INPARAM.BINDVARS = CUST.DATA.bankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_a = "cellCode,cellDesc|select unique value, localetext"
                        sv_a = sv_a + " from crmuser.categories c, crmuser.category_lang l"
                        sv_a = sv_a + " where c.bank_id=?SVAR and l.bank_id=c.bank_id and categorytype='NIDA_CELL' and c.categoryid=l.categoryid"
                        print(sv_a)

                        sv_d = urhk_dbCursorOpenWithBind(sv_a)
                        print(sv_d)

                        IF(sv_d!=0) THEN
                        #{
                                sv_u = urhk_dbCursorClose(sv_a)
                                print(sv_u)

                                sv_u = urhk_SetOrbOut("Status| No record found")
                                print(sv_u)
                        #}
                        ELSE
                        #{
                                CUST.DATA.DB_CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
                                print(CUST.DATA.DB_CURSOR_NUMBER)

                                sv_c = urhk_dbCursorFetch(CUST.DATA.DB_CURSOR_NUMBER)
                                print(sv_c)

                                IF(sv_c != "0")THEN
                                #{
                                        sv_e = urhk_SetOrbOut("SuccessorFailure|Failed")
                                        sv_u = urhk_SetOrbOut("Failure| No record found")
                                        print(sv_u)
                                #}
                                ENDIF

                                                                 while(sv_c ==0)
                                #{
                                        CUST.DATA.cellCode = BANCS.OUTPARAM.cellCode
                                        print(CUST.DATA.cellCode)
                                        CUST.DATA.cellDesc = BANCS.OUTPARAM.cellDesc
                                        print(CUST.DATA.cellDesc)

                                        CUST.DATA.count = CINT(CUST.DATA.count)+ 1
                                        print(CUST.DATA.count)

                                        sv_c = urhk_SetOrbOut("cellDetails.<rec_"+CUST.DATA.count+">.cellCode"+"| "+CUST.DATA.cellCode)
                                        print(sv_c)

                                        sv_c = urhk_SetOrbOut("cellDetails.<rec_"+CUST.DATA.count+">.cellDesc"+"| "+CUST.DATA.cellDesc)
                                        print(sv_c)

                                        sv_c = urhk_dbCursorFetch(CUST.DATA.DB_CURSOR_NUMBER)
                                        print(sv_c)
                                #}
                                do
                                sv_c = urhk_dbCursorClose(CUST.DATA.DB_CURSOR_NUMBER)
                                print(sv_c)
                        #}
                        ENDIF
                #}
                ELSE
                #{
                        sv_u = urhk_SetOrbOut("Failed| Kindly pass the correct value as 'cell'")
                        print(sv_u)

                        EXITSCRIPT
                #}
                ENDIF
        #}
        ENDIF

        IF((CUST.DATA.bankId != "") and (CUST.DATA.village != ""))THEN
        #{
                IF(CUST.DATA.village == "VILLAGE")THEN
                #{
                        BANCS.INPARAM.BINDVARS = CUST.DATA.bankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_a = "villageCode,villageDesc|select unique value, localetext"
                        sv_a = sv_a + " from crmuser.categories c, crmuser.category_lang l"
                        sv_a = sv_a + " where c.bank_id=?SVAR and l.bank_id=c.bank_id and categorytype='VILLAGE' and c.categoryid=l.categoryid"
                        print(sv_a)

                        sv_d = urhk_dbCursorOpenWithBind(sv_a)
                        print(sv_d)

                        IF(sv_d!=0) THEN
                        #{
                                sv_u = urhk_dbCursorClose(sv_a)
                                print(sv_u)

                                sv_u = urhk_SetOrbOut("Status| No record found")
                                print(sv_u)
                        #}
                        ELSE
                        #{
                                CUST.DATA.DB_CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
                                print(CUST.DATA.DB_CURSOR_NUMBER)

                                sv_c = urhk_dbCursorFetch(CUST.DATA.DB_CURSOR_NUMBER)
                                print(sv_c)

                                IF(sv_c != "0")THEN
                                #{
                                        sv_e = urhk_SetOrbOut("SuccessorFailure|Failed")
                                        sv_u = urhk_SetOrbOut("Failure| No record found")
                                        print(sv_u)
                                #}
                                ENDIF

                                while(sv_c ==0)
                                #{
                                        CUST.DATA.villageCode = BANCS.OUTPARAM.villageCode
                                        print(CUST.DATA.villageCode)
                                        CUST.DATA.villageDesc = BANCS.OUTPARAM.villageDesc
                                        print(CUST.DATA.villageDesc)

                                        CUST.DATA.count = CINT(CUST.DATA.count)+ 1
                                        print(CUST.DATA.count)

                                        sv_c = urhk_SetOrbOut("villageDetails.<rec_"+CUST.DATA.count+">.villageCode"+"| "+CUST.DATA.villageCode)
                                        print(sv_c)

                                        sv_c = urhk_SetOrbOut("villageDetails.<rec_"+CUST.DATA.count+">.villageDesc"+"| "+CUST.DATA.villageDesc)
                                        print(sv_c)

                                        sv_c = urhk_dbCursorFetch(CUST.DATA.DB_CURSOR_NUMBER)
                                        print(sv_c)
                                #}
                                do
                                sv_c = urhk_dbCursorClose(CUST.DATA.DB_CURSOR_NUMBER)
                                print(sv_c)
                        #}
                        ENDIF
                #}
                ELSE
                #{
                        sv_u = urhk_SetOrbOut("Failed| Kindly pass the correct value as 'village'")
                        print(sv_u)

                        EXITSCRIPT
                #}
                ENDIF
        #}
        ENDIF

EXITSCRIPT
