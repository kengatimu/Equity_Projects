<--START
trace on

	sv_r=urhk_B2k_PrintRepos("BANCS")
	print (sv_r)   


	sv_r = REPEXISTS("CUST")
	IF (sv_r == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	endif

	sv_r = CLASSEXISTS("CUST", "EZB")
	IF (sv_r == 0) THEN
	#{
		CREATECLASS("CUST", "EZB", 5)
	#}
	ENDIF

	sv_u=urhk_B2k_PrintRepos("BANCS")
	print(sv_u)

	CUST.EZB.errMsg =""

	IF (FIELDEXISTS(BANCS.INPUT.inputAmount)) THEN
		print(BANCS.INPUT.inputAmount)
		sv_h = BANCS.INPUT.inputAmount

		BANCS.OUTPUT.collAmount =CDOUBLE(sv_h)
		print(BANCS.OUTPUT.collAmount)

		BANCS.OUTPUT.AmountWithNoDiscount = CDOUBLE(sv_h)
		print(BANCS.OUTPUT.AmountWithNoDiscount)

		BANCS.OUTPUT.collCrncy = BANCS.INPUT.inputCrncy
		print(BANCS.OUTPUT.collCrncy)
	ENDIF

ENDOFSCRIPT :

TRACE OFF
EXITSCRIPT
END-->
