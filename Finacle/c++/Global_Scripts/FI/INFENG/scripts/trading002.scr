#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script:
# event: NA
# description: g
#=================================================

############################################################################################################

# Source Name                       :
# Date                              : 30-07-2013
# Description                       : This script is called from the custom share purchase page to display a list of active securities
# Input                             : None
# Output                            : None
# Called Script                     : None
# Calling Script                    :
# Author                            :
# Modification History              :

############################################################################################################
<--START
CHECKSUM="e276b0962be41b8d36c55aa061ceebb1ffbb7adbd86770872600491e613f27eb"
	TRACE ON
	
	#==========================================================*
	# CREATING NEW REPOSITORY AND CLASS
	#==========================================================*
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	IF (CLASSEXISTS("CUST", "CUDDS") == 0) THEN
	#{
		CREATECLASS("CUST", "CUDDS", 5)
	#}
	ENDIF
	sv_a = 1
	
	sv_x=urhk_B2k_PrintRepos("BANCS")
	print(BANCS.INPUT.Originator)
	
	CUST.CUDDS.deleted="0"
	
	sv_s = "recCount"
	sv_s = sv_s + " | "
	sv_s = sv_s + " SELECT COUNT(*) "
	sv_s = sv_s + " FROM CUSTOM.SECURITY "
	sv_s = sv_s + " WHERE DELETED= ?SVAR"
	#sv_s = sv_s + " WHERE bank_id = ?SVAR "
	#sv_s = sv_s + " AND rownum < 50 "
	
	
	BANCS.INPARAM.BINDVARS = CUST.CUDDS.deleted
	print(sv_s)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	CUST.CUDDS.recCount = BANCS.OUTPARAM.recCount
	print(CUST.CUDDS.recCount)
	
	sv_q = urhk_SetOrbOut("recCount|"+CUST.CUDDS.recCount)
	
	sv_s = "empId,empName"
	sv_s = sv_s + " | "
	sv_s = sv_s + " SELECT SECURITYCODE,SECURITYNAME "
	sv_s = sv_s + " FROM CUSTOM.SECURITY"
	sv_s = sv_s + " WHERE DELETED= ?SVAR order by SECURITYCODE"
	#sv_s = sv_s + " AND bank_id = ?SVAR "
	#sv_s = sv_s + " AND rownum < 50 "
	
	BANCS.INPARAM.BINDVARS = CUST.CUDDS.deleted
	
	print(sv_s)
	sv_c = urhk_dbCursorOpenWithBind(sv_s)
	
	IF( sv_c != 0) THEN
	#{
		sv_u = urhk_SRV_SetErr("No recods fetched for the entered Account Number")
		BANCS.OUTPUT.SuccessOrFailure = "F"
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		sv_l = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		print(sv_l)
		sv_c = urhk_dbCursorFetch(sv_l)
		print(sv_c)
		
		WHILE( sv_c == 0)
		#{
			CUST.CUDDS.count = sv_a
			print(CUST.CUDDS.count)
			
			CUST.CUDDS.empId= BANCS.OUTPARAM.empId
			print(CUST.CUDDS.empId)
			
			CUST.CUDDS.empName= BANCS.OUTPARAM.empName
			print(CUST.CUDDS.empName)
			
			sv_z = "txtfldname25_" + CUST.CUDDS.count
			sv_z = sv_z + "|" + CUST.CUDDS.empId
			sv_u = urhk_SetOrbOut(sv_z)
			
			sv_z = "txtfldname28_" + CUST.CUDDS.count
			sv_z = sv_z + "|" + CUST.CUDDS.empName
			sv_u = urhk_SetOrbOut(sv_z)
			
			sv_a = sv_a + 1
			sv_c = urhk_dbCursorFetch(sv_l)
			print(sv_c)
		#}
		DO
		sv_c = urhk_dbCursorClose(sv_l)
		print(sv_c)
	#}
	ENDIF
	
	ENDOFSCRIPT:
		exitscript
		
END-->
