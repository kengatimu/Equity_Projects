#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: ubpdp029.scr
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START
CHECKSUM="76b51025307d0199d31980b2af7e2a5ee9336bb9c972be25e513e58cc95e418b"
	# Script body goes here
	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp029.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called from ubp_det_link.js on click of get bill
	#                               details button for IPO biller.
	# Called Script         :
	# Calling function      :		function fnGetBillDtl()
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           16-04-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	#========================================================
	# Check whether CUST Repository exists else create it
	#========================================================
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	#========================================================
	# Check whether UBP Class exists else create it
	#========================================================
	sv_r = CLASSEXISTS("CUST","UBP")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","UBP",5)
	#}
	ENDIF
	
	#========================================================
	# Initialize the variables
	#========================================================
	CUST.UBP.errorFlg="N"
	CUST.UBP.errorMsg=" "
	CUST.UBP.billRefNum=" "
	CUST.UBP.name=" "
	
	IF(FIELDEXISTS(BANCS.INPUT.billerId))THEN
	#{
		IF(TRIM(BANCS.INPUT.billerId) != "")THEN
		#{
			CUST.UBP.billerId=BANCS.INPUT.billerId
			PRINT(CUST.UBP.billerId)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.billRefNum))THEN
	#{
		IF(TRIM(BANCS.INPUT.billRefNum) != "")THEN
		#{
			CUST.UBP.billRefNum=BANCS.INPUT.billRefNum
			PRINT(CUST.UBP.billRefNum)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.billAmt))THEN
	#{
		IF(TRIM(BANCS.INPUT.billAmt) != "")THEN
		#{
			CUST.UBP.billAmt=BANCS.INPUT.billAmt
			PRINT(CUST.UBP.billAmt)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.billAcctId))THEN
	#{
		IF(TRIM(BANCS.INPUT.billAcctId) != "")THEN
		#{
			CUST.UBP.billAcctId=BANCS.INPUT.billAcctId
			PRINT(CUST.UBP.billAcctId)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.appCatgry))THEN
	#{
		IF(TRIM(BANCS.INPUT.appCatgry) != "")THEN
		#{
			CUST.UBP.appCatgry=BANCS.INPUT.appCatgry
			PRINT(CUST.UBP.appCatgry)
		#}
		ENDIF
	#}
	ENDIF
	
	BANCS.INPARAM.BINDVARS =CUST.UBP.billRefNum+"|"+CUST.UBP.billAcctId+"|"+CUST.UBP.billAmt
	sv_s = "response|"
	sv_s = sv_s + " SELECT CUSTOM.VALIDATE_IPO_APPLICATION"
	sv_s = sv_s + " (?SVAR,?SVAR,?SVAR)"
	sv_s = sv_s + " FROM DUAL"
	PRINT(sv_s)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d==0)THEN
	#{
		PRINT(BANCS.OUTPARAM.response)
		CUST.UBP.cdsc = MID$(BANCS.OUTPARAM.response,0,1)
		PRINT(CUST.UBP.cdsc)
		CUST.UBP.response = MID$(BANCS.OUTPARAM.response,2,2)
		PRINT(CUST.UBP.response)
		CUST.UBP.iponame= MID$(BANCS.OUTPARAM.response,5,50)
		PRINT(CUST.UBP.iponame)
		CUST.UBP.name=CUST.UBP.iponame
		IF (CUST.UBP.response != "00") THEN
		#{
			CUST.UBP.errorFlg="Y"
			#CUST.UBP.errorMsg="FAILED........"+CUST.UBP.iponame
			CUST.UBP.errorCode = "UBP_ERR045"
			CALL("ubpdp044.scr")
			CUST.UBP.errorMsg =CUST.UBP.errorDesc+CUST.UBP.iponame
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			IF ((CUST.UBP.cdsc=="C") OR (CUST.UBP.cdsc =="T")) THEN
			#{
				IF (CUST.UBP.cdsc =="C") THEN
				#{
					CUST.UBP.billRefNum=CUST.UBP.appCatgry + "/EQBC/C-" + CUST.UBP.billRefNum
					#}
				ELSE
					#{
					CUST.UBP.billRefNum=CUST.UBP.appCatgry +"/EQT/" + CUST.UBP.billRefNum
				#}
				ENDIF
			#}
			ENDIF
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.errorFlg="Y"
		#CUST.UBP.errorMsg="An error has occured. Please contact Support."
		CUST.UBP.errorCode = "UBP_ERR046"
		CALL("ubpdp044.scr")
		CUST.UBP.errorMsg =CUST.UBP.errorDesc
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	ENDOFSCRIPT:
		
		sv_s = "errorFlg|" + CUST.UBP.errorFlg
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "errorMsg|" + CUST.UBP.errorMsg
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "billRefNum|" + CUST.UBP.billRefNum
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "name|" + CUST.UBP.iponame
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		#TRACE OFF
		EXITSCRIPT
		
END-->
