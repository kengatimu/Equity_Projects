#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: ccprmdp004.scr
# Type: NA|default
# Description: validation script for event type
# Author: haneesha.kanumuru
# Date: 8/13/18 12:51 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="546287c2f8ef190d8911d9060630e1d2bd90b61c3419f405cb08357282004734"
	# Script body goes here
	TRACE ON
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CCPRM Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CCPRM")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CCPRM",5)
	#}
	ENDIF
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	CUST.CCPRM.evntType =" "
	CUST.CCPRM.errorFlg ="N"
	CUST.CCPRM.errorMsg = " "
	CUST.CCPRM.count = " "
	IF(FIELDEXISTS(BANCS.INPUT.evntType)) THEN
	#{
		IF(TRIM(BANCS.INPUT.evntType) != "")THEN
		#{
			CUST.CCPRM.evntType= TOUPPER(TRIM(BANCS.INPUT.evntType))
			PRINT(CUST.CCPRM.evntType)
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.CCPRM.errorFlg = "Y"
		CUST.CCPRM.errorMsg = "Invalid Input for fnExecuteScript"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_s = "schm|"
	sv_s = sv_s + " SELECT COUNT(*)"
	sv_s = sv_s + " FROM tbaadm.ptt "
	sv_s = sv_s + " WHERE EVENT_TYPE = ?SVAR"
	sv_s = sv_s + " AND DEL_FLG != 'Y'"
	#sv_s = sv_s + " AND entity_cre_flg !='N'"
	#sv_s = sv_s + " AND schm_type='LAA'"
	sv_s = sv_s + " AND bank_id=?SVAR"
	
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.CCPRM.evntType + "|" +BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	CUST.CCPRM.count = BANCS.OUTPARAM.schm
	PRINT(CUST.CCPRM.count)
	
	
	IF( CUST.CCPRM.count ==0 )THEN
	#{
		
		CUST.CCPRM.errorFlg = "Y"
		CUST.CCPRM.errorMsg = "Invalid Event Type"
	#}
	ENDIF
	
	ENDOFSCRIPT:
		sv_u = urhk_SetOrbOut("errorFlg|"+CUST.CCPRM.errorFlg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("errorMsg|"+CUST.CCPRM.errorMsg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("evntType|"+CUST.CCPRM.evntType)
		
		PRINT(sv_u)
END-->

