#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: ubpdp073.scr
# Type: NA|default
# Description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
# Author: vikas_p
# Date: 7/24/14 9:23 AM
# Modification Log:
#============================================================

#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: ubpdp073.scr
# event: NA
# description: na
#=================================================

##########################################################################################
# Source Name           : ubpdp073.scr
# Date                  : 11-09-2013
# Description           : CUBP OFTI Receipt Generation
# Author                : Nitin Prabhakar
# Modification History  :
# <Serial No.>  <Date>        <Author Name>             <Description>
# ------------  -----------   -----------------         --------------
#      1         11-09-2013    Nitin Prabhakar          Original file
###########################################################################################
<--START
	TRACE ON
	
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	###########################################################################################
	# Check whether CIETXN Class exists else create it
	###########################################################################################
	sv_r = CLASSEXISTS("CUST","CUBP")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CUBP",5)
	#}
	ENDIF
	
	#========================================================
	# Check whether UBP Class exists else create it
	#========================================================
	sv_r = CLASSEXISTS("CUST","UBP")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","UBP",5)
	#}
	ENDIF
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	sv_u = urhk_b2k_printrepos("BANCS")
	CUST.CUBP.billPayRefNum = " "
	CUST.CUBP.errorFlg = "N"
	CUST.CUBP.errorMsg = " "
	CUST.CUBP.billerBnkID = " "
	CUST.CUBP.bilName = " "
	CUST.CUBP.field1 = " "
	CUST.CUBP.field2 = " "
	CUST.CUBP.part_tran_srl_num = " "
	CUST.CUBP.BODDate = MID$(BANCS.STDIN.BODDate,0,10)
	PRINT(CUST.CUBP.BODDate)
	BANCS.OUTPUT.lv_units = ""	
	IF(FIELDEXISTS(BANCS.INPUT.billPayRefNum))THEN
	#{
		IF(TRIM(BANCS.INPUT.billPayRefNum) != "")THEN
		#{
			CUST.CUBP.billPayRefNum = TOUPPER(TRIM(BANCS.INPUT.billPayRefNum))
			PRINT(CUST.CUBP.billPayRefNum)
			CUST.CUBP.prntRevFlg = BANCS.INPUT.prntRevFlg
			PRINT(CUST.CUBP.prntRevFlg)
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.CUBP.errorFlg = "Y"
		CUST.CUBP.errorMsg = "Unable to generate Receipt"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_s = "timeStamp|select to_char(sysdate,'ddmmyyyyhhmiss') from dual" 
	print(sv_s)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	IF(sv_d == 0)THEN
	#{
		CUST.CUBP.timeStamp = BANCS.OUTPARAM.timeStamp
		print(CUST.CUBP.timeStamp)
	#}
	ENDIF
	
	sv_p = urhk_B2k_PID("")
	IF(sv_p == 0 ) THEN
	#{
		CUST.CUBP.Rpid = "cubp_receipt_"+TRIM(BANCS.OUTPARAM.PID)
		CUST.CUBP.Rpid = CUST.CUBP.Rpid + CUST.CUBP.timeStamp
		PRINT(CUST.CUBP.Rpid)
		#}
	ELSE
		#{
		CUST.CUBP.Rpid = "123"
		PRINT(CUST.CUBP.Rpid)
	#}
	ENDIF
	
	CUST.CUBP.lst = "cubp_receipt_"+TRIM(BANCS.OUTPARAM.PID)
	CUST.CUBP.lst = CUST.CUBP.lst + CUST.CUBP.timeStamp +".lst"
	print(CUST.CUBP.lst)
	CUST.CUBP.sucRptName = "cubp_receipt_"+TRIM(BANCS.OUTPARAM.PID)
	CUST.CUBP.sucRptName = CUST.CUBP.sucRptName + CUST.CUBP.timeStamp
	print(CUST.CUBP.sucRptName)
	sv_s = "touch "+ CUST.CUBP.lst
	PRINT(sv_s)
	sv_o = SYSTEM(sv_s)
	PRINT(sv_o)
	
	IF(sv_o !=0) THEN
	#{
		sv_o = SYSTEM(sv_s)
		PRINT(sv_o)
	#}
	ENDIF
	
	IF(sv_o !=0) THEN
	#{
		sv_o = SYSTEM(sv_s)
		PRINT(sv_o)
	#}
	ENDIF
	
	IF(sv_o !=0) THEN
	#{
		sv_o = SYSTEM(sv_s)
		PRINT(sv_o)
	#}
	ENDIF
	
	IF(sv_o !=0) THEN
	#{
		sv_o = SYSTEM(sv_s)
		PRINT(sv_o)
	#}
	ENDIF
	
	##############################################################################
	# Fetch Data from custom table using ieTxnRefNo
	##############################################################################
	
	BANCS.INPARAM.BINDVARS = CUST.CUBP.billPayRefNum
	#BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + MID$(BANCS.STDIN.BODDate,0,10)
	#BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_s = "billerId,tranType,BillerAcctId,PayerAcctId,refCrncy,"
	sv_s = sv_s + " refAmt,tranDate,tranId,revTranId,revTranDate,CHARGE_TRAN_ID,CHARGE_CRNCY,CHARGE_AMT,CHARGE_TRAN_DATE,billerBnkID,bilName,tranRemarks|"
	sv_s = sv_s + " SELECT BILLER_ID,MODE_OF_PAY,BILLER_ACCT_ID,PAYER_ACCT_ID,REF_CRNCY,"
	sv_s = sv_s + " BILL_AMT,TO_CHAR(TRAN_DATE,'DD-MM-YYYY'),TRAN_ID,REV_TRAN_ID,TO_CHAR(REV_TRAN_DATE,'DD-MM-YYYY'),"
	sv_s = sv_s + " CHARGE_TRAN_ID,CHARGE_CRNCY,CHARGE_AMT,TO_CHAR(CHARGE_TRAN_DATE,'DD-MM-YYYY'),BILLER_BNK_ID,NAME,TRAN_RMKS "
	sv_s = sv_s + " FROM CUSTOM.C_UBP "
	sv_s = sv_s + " WHERE BILL_PAY_REF_NUM = ?SVAR "
	#sv_s = sv_s + " AND NVL(entity1_tran_date,charge_Tran_Date) = to_date(?SVAR,'dd-mm-yyyy') "
	#sv_s = sv_s + " AND init_entity_id = ?SVAR"
	
	PRINT(sv_s)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	IF(sv_d == 0)THEN
	#{
		CUST.CUBP.refAmt = BANCS.OUTPARAM.refAmt
		PRINT(CUST.CUBP.refAmt)
		CUST.CUBP.tranRemarks = BANCS.OUTPARAM.tranRemarks
		PRINT(CUST.CUBP.tranRemarks)
		CUST.CUBP.billerId = BANCS.OUTPARAM.billerId
		PRINT(CUST.CUBP.billerId)
		CUST.CUBP.billerBnkID = BANCS.OUTPARAM.billerBnkID
		PRINT(CUST.CUBP.billerBnkID)
		CUST.CUBP.bilName = BANCS.OUTPARAM.bilName
		PRINT(CUST.CUBP.bilName)
		CUST.CUBP.tranType = BANCS.OUTPARAM.tranType
		PRINT(CUST.CUBP.tranType)
		
		CUST.CUBP.tranAcId=BANCS.OUTPARAM.PayerAcctId
		PRINT(CUST.CUBP.tranAcId)
		
		IF (CUST.CUBP.prntRevFlg == "N") THEN
		#{
			CUST.CUBP.crAcctId = BANCS.OUTPARAM.BillerAcctId
			PRINT(CUST.CUBP.crAcctId)
			CUST.CUBP.drAcctId = BANCS.OUTPARAM.PayerAcctId
			PRINT(CUST.CUBP.drAcctId)
			CUST.CUBP.chargeTranid = BANCS.OUTPARAM.CHARGE_TRAN_ID
			CUST.CUBP.chargeCrncy = BANCS.OUTPARAM.CHARGE_CRNCY
			CUST.CUBP.chargeAmt = BANCS.OUTPARAM.CHARGE_AMT
			CUST.CUBP.chargetrandate = BANCS.OUTPARAM.CHARGE_TRAN_DATE
			PRINT(CUST.CUBP.chargeTranid)
			PRINT(CUST.CUBP.chargeCrncy)
			PRINT(CUST.CUBP.chargeAmt)
			PRINT(CUST.CUBP.chargetrandate)
			
			#}
		ELSE
			#{
			IF (CUST.CUBP.tranType == "C") THEN
			#{
				CUST.CUBP.drAcctId = BANCS.OUTPARAM.BillerAcctId
				PRINT(CUST.CUBP.drAcctId)
				#}
			ELSE
				#{
				CUST.CUBP.crAcctId = BANCS.OUTPARAM.PayerAcctId
				PRINT(CUST.CUBP.crAcctId)
				CUST.CUBP.drAcctId = BANCS.OUTPARAM.BillerAcctId
				PRINT(CUST.CUBP.drAcctId)
			#}
			ENDIF
		#}
		ENDIF
		CUST.CUBP.refCrncy = BANCS.OUTPARAM.refCrncy
		PRINT(CUST.CUBP.refCrncy)
		CUST.CUBP.refAmt = BANCS.OUTPARAM.refAmt
		print(CUST.CUBP.refAmt)
		
		
		
		
		IF (CUST.CUBP.prntRevFlg == "N") THEN
		#{
			#CUST.CUBP.cubpTranId = TRIM(BANCS.OUTPARAM.tranId)
			CUST.CUBP.cubpTranId = BANCS.OUTPARAM.tranId
			PRINT(CUST.CUBP.cubpTranId)
			CUST.CUBP.tranDate = BANCS.OUTPARAM.tranDate
			PRINT(CUST.CUBP.tranDate)
			#}
		ELSE
			#{
			#CUST.CUBP.cubpTranId = TRIM(BANCS.OUTPARAM.revTranId)
			CUST.CUBP.cubpTranId = BANCS.OUTPARAM.revTranId
			PRINT(CUST.CUBP.cubpTranId)
			CUST.CUBP.tranDate = BANCS.OUTPARAM.revTranDate
			PRINT(CUST.CUBP.tranDate)
		#}
		ENDIF
	#}
	ENDIF
	###########################################################################################
	# Create lst File - Data Fields in sync with ofti.mri
	###########################################################################################
	
	print(CUST.CUBP.prntRevFlg)
	IF(CUST.CUBP.tranType == "C")THEN
	#{
		IF(CUST.CUBP.prntRevFlg == "N")THEN
		#{
			print("NOT reversal")	
			CUST.CUBP.foracid = CUST.CUBP.crAcctId
			
			PRINT(CUST.CUBP.tranDate)
			#		CUST.CUBP.tranDate = CUST.CUBP.entity1TranDate
			CUST.CUBP.tranAmt = FORMAT$(CDOUBLE(CUST.CUBP.refAmt),"%.2f")
			CUST.CUBP.tranCrncyCode = CUST.CUBP.refCrncy
			#CUST.CUBP.tran_id = CUST.CUBP.billPayRefNum
			CUST.CUBP.tran_id = CUST.CUBP.cubpTranId
			CUST.CUBP.acct_name = ""
			CUST.CUBP.acid = ""
			sv_s = "acct_name,acid|"
			sv_s = sv_s + " SELECT ACCT_NAME,ACID"
			sv_s = sv_s + " FROM TBAADM.GAM "
			sv_s = sv_s + " WHERE FORACID = ?SVAR"
			sv_s = sv_s + " AND DEL_FLG != 'Y'"
			#sv_s = sv_s + " AND BANK_ID = ?SVAR"
			sv_s = sv_s + " AND BANK_ID IS NOT NULL"
			PRINT(sv_s)
			
			#BANCS.INPARAM.BINDVARS = CUST.CUBP.crAcctId+"|"+BANCS.STDIN.contextBankId
			BANCS.INPARAM.BINDVARS = CUST.CUBP.crAcctId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_d)
			IF(sv_d == 0)THEN
			#{
				CUST.CUBP.acct_name = BANCS.OUTPARAM.acct_name
				PRINT(CUST.CUBP.acct_name)
				CUST.CUBP.crAcid = BANCS.OUTPARAM.acid
				PRINT(CUST.CUBP.crAcid)
			#}
			ENDIF
			
			CUST.CUBP.tranType = "C"
			CUST.CUBP.tranSubType = "NR"
			CUST.CUBP.partTranType = "C"
			
			CUST.CUBP.tranparticulars = ""
			CUST.CUBP.posted_user_id = ""
			sv_s = "tranparticulars,posted_user_id,part_tran_srl_num|"
			sv_s = sv_s + " SELECT TRAN_PARTICULAR,PSTD_USER_ID,PART_TRAN_SRL_NUM"
			sv_s = sv_s + " FROM TBAADM.DTD "
			#sv_s = sv_s + " WHERE ACID = ?SVAR"
			#sv_s = sv_s + " AND TRIM(TRAN_ID) = ?SVAR"
			sv_s = sv_s + " WHERE PART_TRAN_TYPE = ?SVAR"
			sv_s = sv_s + " AND TRAN_ID = ?SVAR"
			sv_s = sv_s + " AND TRAN_DATE = TO_DATE(?SVAR,'DD-MM-YYYY')"
			sv_s = sv_s + " AND BANK_ID = ?SVAR"
			sv_s = sv_s + " AND ROWNUM = 1"
			
			PRINT(sv_s)
			#BANCS.INPARAM.BINDVARS = CUST.CUBP.crAcid + "|" + CUST.CUBP.cubpTranId + "|" + CUST.CUBP.tranDate+"|"+BANCS.STDIN.contextBankId
			BANCS.INPARAM.BINDVARS = CUST.CUBP.partTranType + "|" + CUST.CUBP.cubpTranId + "|" + CUST.CUBP.tranDate+"|"+BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_d)
			IF(sv_d == 0)THEN
			#{
				CUST.CUBP.tranparticulars = BANCS.OUTPARAM.tranparticulars
				PRINT(CUST.CUBP.tranparticulars)
				CUST.CUBP.posted_user_id = BANCS.OUTPARAM.posted_user_id
				PRINT(CUST.CUBP.posted_user_id)
				CUST.CUBP.part_tran_srl_num = BANCS.OUTPARAM.part_tran_srl_num
				PRINT(CUST.CUBP.part_tran_srl_num)
			#}
			ENDIF
			
			IF(BANCS.STDIN.contextBankId == CUST.CUBP.billerBnkID)THEN
			#{
				CUST.CUBP.field1 = CUST.CUBP.tran_id
				CUST.CUBP.field2 = " "
				#}
			ELSE
				#{
				CUST.CUBP.field1 = CUST.CUBP.billPayRefNum
				CUST.CUBP.field2 = CUST.CUBP.bilName
			#}
			ENDIF
			
			CALL("ubpdp074.scr")
			sv_s = "echo '" + CUST.CUBP.outRecord + "'  >> " + CUST.CUBP.lst
			sv_o = SYSTEM(sv_s)
			PRINT(sv_s)
			PRINT(sv_o)
			
			IF(sv_o !=0) THEN
			#{
				sv_o = SYSTEM(sv_s)
				PRINT(sv_o)
			#}
			ENDIF
			
			IF(sv_o !=0) THEN
			#{
				sv_o = SYSTEM(sv_s)
				PRINT(sv_o)
			#}
			ENDIF
			
			IF(sv_o !=0) THEN
			#{
				sv_o = SYSTEM(sv_s)
				PRINT(sv_o)
			#}
			ENDIF
			
			IF(sv_o !=0) THEN
			#{
				sv_o = SYSTEM(sv_s)
				PRINT(sv_o)
			#}
			ENDIF
			
			CUST.CUBP.BODDate = MID$(BANCS.STDIN.BODDate,0,10)
			PRINT(CUST.CUBP.BODDate)
			
			
			#------------------------------------------------------------------------
			# Charge Tran Id
			#------------------------------------------------------------------------
			IF (CUST.CUBP.chargeTranid != "")THEN
			#{
				
				
				
				BANCS.INPARAM.BINDVARS = CUST.CUBP.chargeTranid + "|" + BANCS.STDIN.contextBankId+ "|" + CUST.CUBP.BODDate
				
				PRINT(BANCS.INPARAM.BINDVARS)
				
				sv_s = " chrgAcct,chrgAcctName,chrgAcid,chrgTranpart,chrgPstdUsr,part_tran_srl_num"
				sv_s = sv_s + " |select foracid,acct_name,b.acid,TRAN_PARTICULAR,PSTD_USER_ID,PART_TRAN_SRL_NUM "
				sv_s = sv_s + " from tbaadm.dtd a, tbaadm.gam b "
				sv_s = sv_s + " where tran_id  = ?SVAR "
				sv_s = sv_s + " AND a.acid  = b.acid "
				sv_s = sv_s + " AND a.bank_id = b.bank_id "
				sv_s = sv_s + " AND a.bank_id  = ?SVAR "
				sv_s = sv_s + " AND a.TRAN_DATE = to_date(?SVAR,'dd-mm-yyyy')"
				sv_s = sv_s + " AND part_tran_type = 'C' AND PSTD_USER_ID != 'SYSTEM'"
				
				PRINT(sv_s)
				sv_d = urhk_dbSelectWithBind(sv_s)
				
				IF(sv_d == 0) THEN
				#{
					CUST.UBP.chrgAcct       = BANCS.OUTPARAM.chrgAcct
					CUST.UBP.chrgAcctName   = BANCS.OUTPARAM.chrgAcctName
					CUST.UBP.chrgTranpart   = BANCS.OUTPARAM.chrgTranpart
					CUST.UBP.chrgPstdUsr    = BANCS.OUTPARAM.chrgPstdUsr
					CUST.UBP.parttransrlnum = BANCS.OUTPARAM.part_tran_srl_num
					
					PRINT(CUST.UBP.parttransrlnum)
					PRINT(CUST.UBP.chrgAcct )
					PRINT(CUST.UBP.chrgTranpart)
					PRINT(CUST.UBP.chrgAcct )
					
					CUST.CUBP.tranAmt = FORMAT$(CDOUBLE(CUST.CUBP.chargeAmt),"%.2f")
					CUST.CUBP.foracid = CUST.UBP.chrgAcct
					CUST.CUBP.tranDate =CUST.CUBP.chargetrandate
					CUST.CUBP.tranCrncyCode = CUST.CUBP.chargeCrncy
					CUST.CUBP.tran_id = CUST.CUBP.chargeTranid
					CUST.CUBP.acct_name = CUST.UBP.chrgAcctName
					CUST.CUBP.tranType = "C"
					CUST.CUBP.tranSubType = "CR"
					CUST.CUBP.partTranType = "C"
					CUST.CUBP.tranparticulars = CUST.UBP.chrgTranpart
					CUST.CUBP.posted_user_id = CUST.UBP.chrgPstdUsr
					
					PRINT(CUST.CUBP.foracid)
					PRINT(CUST.CUBP.tranDate)
					PRINT(CUST.CUBP.tranAmt)
					PRINT(CUST.CUBP.tranCrncyCode)
					PRINT(CUST.CUBP.tran_id)
					PRINT(CUST.CUBP.acct_name)
					PRINT(CUST.CUBP.tranType)
					PRINT(CUST.CUBP.tranSubType)
					PRINT(CUST.CUBP.partTranType)
					PRINT(CUST.CUBP.tranparticulars)
					PRINT(CUST.CUBP.posted_user_id)
					IF(BANCS.STDIN.contextBankId == CUST.CUBP.billerBnkID)THEN
					#{
						CUST.CUBP.field1 = CUST.CUBP.tran_id
						CUST.CUBP.field2 = " "
						#}
					ELSE
						#{
						CUST.CUBP.field1 = CUST.CUBP.billPayRefNum
						CUST.CUBP.field2 = CUST.CUBP.bilName
					#}
					ENDIF
					
					CALL("ubpdp074.scr")
					sv_s = "echo '" + CUST.CUBP.outRecord + "'  >> " + CUST.CUBP.lst
					sv_o = SYSTEM(sv_s)
					print(sv_s)
					print(sv_o)

					IF(sv_o !=0) THEN
					#{
						sv_o = SYSTEM(sv_s)
						print(sv_o)
					#}
					ENDIF
					
					IF(sv_o !=0) THEN
					#{
						sv_o = SYSTEM(sv_s)
						PRINT(sv_o)
					#}
					ENDIF
					
					IF(sv_o !=0) THEN
					#{
						sv_o = SYSTEM(sv_s)
						PRINT(sv_o)
					#}
					ENDIF
					
					IF(sv_o !=0) THEN
					#{
						sv_o = SYSTEM(sv_s)
						PRINT(sv_o)
					#}
					ENDIF
					
					IF(sv_o !=0) THEN
					#{
						sv_o = SYSTEM(sv_s)
						PRINT(sv_o)
					#}
					ENDIF
					
					GOTO PRINT_RECEIPT
					
					
				#}
				ENDIF
				
				
				
				
				
			#}
			ENDIF
			
			
			
			
			
			
			#}
		ELSE
			#{
			print("IN REVERSAL")
			CUST.CUBP.foracid = CUST.CUBP.drAcctId
			PRINT(CUST.CUBP.tranDate)
			#		CUST.CUBP.tranDate = CUST.CUBP.entity1TranDate
			CUST.CUBP.tranAmt = FORMAT$(CDOUBLE(CUST.CUBP.refAmt),"%.2f")
			CUST.CUBP.tranCrncyCode = CUST.CUBP.refCrncy
			#CUST.CUBP.tran_id = CUST.CUBP.billPayRefNum
			CUST.CUBP.tran_id = CUST.CUBP.cubpTranId
			CUST.CUBP.acct_name = ""
			CUST.CUBP.acid = ""
			sv_s = "acct_name,acid|"
			sv_s = sv_s + " SELECT ACCT_NAME,ACID"
			sv_s = sv_s + " FROM TBAADM.GAM "
			sv_s = sv_s + " WHERE FORACID = ?SVAR"
			sv_s = sv_s + " AND DEL_FLG != 'Y'"
			#sv_s = sv_s + " AND BANK_ID = ?SVAR"
			sv_s = sv_s + " AND BANK_ID IS NOT NULL"
			PRINT(sv_s)
			
			#BANCS.INPARAM.BINDVARS = CUST.CUBP.drAcctId+"|"+BANCS.STDIN.contextBankId
			BANCS.INPARAM.BINDVARS = CUST.CUBP.drAcctId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_d)
			IF(sv_d == 0)THEN
			#{
				CUST.CUBP.acct_name = BANCS.OUTPARAM.acct_name
				PRINT(CUST.CUBP.acct_name)
				CUST.CUBP.drAcid = BANCS.OUTPARAM.acid
				PRINT(CUST.CUBP.drAcid)
			#}
			ENDIF
			
			CUST.CUBP.tranType = "C"
			CUST.CUBP.tranSubType = "NR"
			CUST.CUBP.partTranType = "D"
			
			CUST.CUBP.tranparticulars = ""
			CUST.CUBP.posted_user_id = ""
			sv_s = "tranparticulars,posted_user_id,part_tran_srl_num|"
			sv_s = sv_s + " SELECT TRAN_PARTICULAR,PSTD_USER_ID,PART_TRAN_SRL_NUM"
			sv_s = sv_s + " FROM TBAADM.DTD "
			#------------Modified by Vikas----------------
			#sv_s = sv_s + " WHERE ACID = ?SVAR"	
			#sv_s = sv_s + " AND TRAN_ID = ?SVAR"
			sv_s = sv_s + " WHERE TRAN_ID = ?SVAR"
			#----------------------------------------------
			sv_s = sv_s + " AND TRAN_DATE = TO_DATE(?SVAR,'DD-MM-YYYY')"
			sv_s = sv_s + " AND BANK_ID = ?SVAR"
			sv_s = sv_s + " AND ROWNUM = 1"
			
			PRINT(sv_s)
			#BANCS.INPARAM.BINDVARS = CUST.CUBP.drAcid + "|" + CUST.CUBP.cubpTranId + "|" + CUST.CUBP.tranDate+"|"+BANCS.STDIN.contextBankId
			BANCS.INPARAM.BINDVARS =  CUST.CUBP.cubpTranId + "|" + CUST.CUBP.tranDate+"|"+BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_d)
			IF(sv_d == 0)THEN
			#{
				CUST.CUBP.tranparticulars = BANCS.OUTPARAM.tranparticulars
				PRINT(CUST.CUBP.tranparticulars)
				CUST.CUBP.posted_user_id = BANCS.OUTPARAM.posted_user_id
				PRINT(CUST.CUBP.posted_user_id)
				CUST.CUBP.part_tran_srl_num = BANCS.OUTPARAM.part_tran_srl_num
				PRINT(CUST.CUBP.part_tran_srl_num)
			#}
			ENDIF
			IF(BANCS.STDIN.contextBankId == CUST.CUBP.billerBnkID)THEN
			#{
				CUST.CUBP.field1 = CUST.CUBP.tran_id
				CUST.CUBP.field2 = " "
				#}
			ELSE
				#{
				CUST.CUBP.field1 = CUST.CUBP.billPayRefNum
				CUST.CUBP.field2 = CUST.CUBP.bilName
			#}
			ENDIF
			CALL("ubpdp074.scr")
			sv_s = "echo '" + CUST.CUBP.outRecord + "'  >> " + CUST.CUBP.lst
			PRINT(sv_s)
			sv_o = SYSTEM(sv_s)
			PRINT(sv_o)
			GOTO PRINT_RECEIPT
		#}
		ENDIF
	#}
	ENDIF
	
	IF(CUST.CUBP.tranType == "T")THEN
	#{
		CUST.CUBP.foracid = CUST.CUBP.crAcctId
		PRINT(CUST.CUBP.tranDate)
		#	CUST.CUBP.tranDate = CUST.CUBP.entity1TranDate
		CUST.CUBP.tranAmt = FORMAT$(CDOUBLE(CUST.CUBP.refAmt),"%.2f")
		CUST.CUBP.tranCrncyCode = CUST.CUBP.refCrncy
		#CUST.CUBP.tran_id = CUST.CUBP.billPayRefNum
		CUST.CUBP.tran_id = CUST.CUBP.cubpTranId
		CUST.CUBP.acct_name = ""
		CUST.CUBP.acid = ""
		sv_s = "acct_name,acid|"
		sv_s = sv_s + " SELECT ACCT_NAME,ACID"
		sv_s = sv_s + " FROM TBAADM.GAM "
		sv_s = sv_s + " WHERE FORACID = ?SVAR"
		sv_s = sv_s + " AND DEL_FLG != 'Y'"
		#sv_s = sv_s + " AND BANK_ID = ?SVAR"
		sv_s = sv_s + " AND BANK_ID IS NOT NULL"
		PRINT(sv_s)
		
		#BANCS.INPARAM.BINDVARS = CUST.CUBP.crAcctId+"|"+BANCS.STDIN.contextBankId
		BANCS.INPARAM.BINDVARS = CUST.CUBP.crAcctId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d == 0)THEN
		#{
			CUST.CUBP.acct_name = BANCS.OUTPARAM.acct_name
			PRINT(CUST.CUBP.acct_name)
			CUST.CUBP.crAcid = BANCS.OUTPARAM.acid
			PRINT(CUST.CUBP.crAcid)
		#}
		ENDIF
		
		CUST.CUBP.tranType = "C"
		CUST.CUBP.tranSubType = "NR"
		CUST.CUBP.partTranType = "C"
		
		CUST.CUBP.tranparticulars = ""
		CUST.CUBP.posted_user_id = ""
		sv_s = "tranparticulars,posted_user_id,part_tran_srl_num|"
		sv_s = sv_s + " SELECT TRAN_PARTICULAR,PSTD_USER_ID,PART_TRAN_SRL_NUM"
		sv_s = sv_s + " FROM TBAADM.DTD "
		sv_s = sv_s + " WHERE ACID = ?SVAR"
		#sv_s = sv_s + " AND TRIM(TRAN_ID) = ?SVAR"
		sv_s = sv_s + " AND TRAN_ID = ?SVAR"
		sv_s = sv_s + " AND TRAN_DATE = TO_DATE(?SVAR,'DD-MM-YYYY')"
		sv_s = sv_s + " AND BANK_ID = ?SVAR"
		sv_s = sv_s + " AND ROWNUM = 1"
		
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CUBP.crAcid + "|" + CUST.CUBP.cubpTranId + "|" + CUST.CUBP.tranDate+"|"+BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d == 0)THEN
		#{
			CUST.CUBP.tranparticulars = BANCS.OUTPARAM.tranparticulars
			PRINT(CUST.CUBP.tranparticulars)
			CUST.CUBP.posted_user_id = BANCS.OUTPARAM.posted_user_id
			PRINT(CUST.CUBP.posted_user_id)
			CUST.CUBP.part_tran_srl_num = BANCS.OUTPARAM.part_tran_srl_num
			PRINT(CUST.CUBP.part_tran_srl_num)
			#}
		ELSE
			#{
			CUST.CUBP.tranparticulars = "CREDIT TRANSFER THRU " + MID$(BANCS.STDIN.homeCrncyCode,0,2) + " - " + BANCS.STDIN.mySolDesc
		#}
		ENDIF
		
		IF(BANCS.STDIN.contextBankId == CUST.CUBP.billerBnkID)THEN
		#{
			CUST.CUBP.field1 = CUST.CUBP.tran_id
			CUST.CUBP.field2 = " "
			#}
		ELSE
			#{
			CUST.CUBP.field1 = CUST.CUBP.billPayRefNum
			CUST.CUBP.field2 = CUST.CUBP.bilName
		#}
		ENDIF
		
		CALL("ubpdp074.scr")
		sv_s = "echo '" + CUST.CUBP.outRecord + "'  >> " + CUST.CUBP.lst
		PRINT(sv_s)
		sv_o = SYSTEM(sv_s)
		PRINT(sv_o)
		IF(sv_o !=0) THEN
		#{
			sv_o = SYSTEM(sv_s)
			PRINT(sv_o)
		#}
		ENDIF
		
		IF(sv_o !=0) THEN
		#{
			sv_o = SYSTEM(sv_s)
			PRINT(sv_o)
		#}
		ENDIF
		
		IF(sv_o !=0) THEN
		#{
			sv_o = SYSTEM(sv_s)
			PRINT(sv_o)
		#}
		ENDIF

		IF(sv_o !=0) THEN
		#{
			sv_o = SYSTEM(sv_s)
			print(sv_o)
		#}
		ENDIF

		CUST.CUBP.BODDate = MID$(BANCS.STDIN.BODDate,0,10)
		print(CUST.CUBP.BODDate)
		
		
		#------------------------------------------------------------------------
		# Charge Tran Id
		#------------------------------------------------------------------------
		IF (CUST.CUBP.chargeTranid != "")THEN
		#{
			
			
			
			BANCS.INPARAM.BINDVARS = CUST.CUBP.chargeTranid + "|" + BANCS.STDIN.contextBankId+ "|" + CUST.CUBP.BODDate

			print(BANCS.INPARAM.BINDVARS)

			sv_s = " chrgAcct,chrgAcctName,chrgAcid,chrgTranpart,chrgPstdUsr,part_tran_srl_num"
			sv_s = sv_s + " |select foracid,acct_name,b.acid,TRAN_PARTICULAR,PSTD_USER_ID,PART_TRAN_SRL_NUM "
			sv_s = sv_s + " from tbaadm.dtd a, tbaadm.gam b "
			sv_s = sv_s + " where tran_id  = ?SVAR "
			sv_s = sv_s + " and a.acid  = b.acid "
			sv_s = sv_s + " and a.bank_id = b.bank_id "
			sv_s = sv_s + " and a.bank_id  = ?SVAR "
			sv_s = sv_s + " AND a.TRAN_DATE = to_date(?SVAR,'dd-mm-yyyy')"
			sv_s = sv_s + " and part_tran_type = 'C' and PSTD_USER_ID != 'SYSTEM'"

			print(sv_s)
			sv_d = urhk_dbSelectWithBind(sv_s)

			IF(sv_d == 0) THEN
			#{
				CUST.UBP.chrgAcct       = BANCS.OUTPARAM.chrgAcct
				CUST.UBP.chrgAcctName   = BANCS.OUTPARAM.chrgAcctName
				CUST.UBP.chrgTranpart   = BANCS.OUTPARAM.chrgTranpart
				CUST.UBP.chrgPstdUsr    = BANCS.OUTPARAM.chrgPstdUsr
				CUST.UBP.parttransrlnum = BANCS.OUTPARAM.part_tran_srl_num
				
				print(CUST.UBP.parttransrlnum)
				print(CUST.UBP.chrgAcct )
				print(CUST.UBP.chrgTranpart)
				print(CUST.UBP.chrgAcct )

				CUST.CUBP.tranAmt = FORMAT$(CDOUBLE(CUST.CUBP.chargeAmt),"%.2f")
				CUST.CUBP.foracid = CUST.UBP.chrgAcct
				CUST.CUBP.tranDate =CUST.CUBP.chargetrandate
				CUST.CUBP.tranCrncyCode = CUST.CUBP.chargeCrncy
				CUST.CUBP.tran_id = CUST.CUBP.chargeTranid
				CUST.CUBP.acct_name = CUST.UBP.chrgAcctName
				CUST.CUBP.tranType = "C"
				CUST.CUBP.tranSubType = "CR"
				CUST.CUBP.partTranType = "C"
				CUST.CUBP.tranparticulars = CUST.UBP.chrgTranpart
				CUST.CUBP.posted_user_id = CUST.UBP.chrgPstdUsr   

				PRINT(CUST.CUBP.foracid)
				PRINT(CUST.CUBP.tranDate)
				PRINT(CUST.CUBP.tranAmt)
				PRINT(CUST.CUBP.tranCrncyCode)
				PRINT(CUST.CUBP.tran_id)
				PRINT(CUST.CUBP.acct_name)
				PRINT(CUST.CUBP.tranType)
				PRINT(CUST.CUBP.tranSubType)
				PRINT(CUST.CUBP.partTranType)
				PRINT(CUST.CUBP.tranparticulars)
				PRINT(CUST.CUBP.posted_user_id)
			

				CALL("ubpdp074.scr")
				sv_s = "echo '" + CUST.CUBP.outRecord + "'  >> " + CUST.CUBP.lst
				sv_o = SYSTEM(sv_s)
				print(sv_s)
				print(sv_o)
		
				IF(sv_o !=0) THEN
				#{
					sv_o = SYSTEM(sv_s)
					print(sv_o)
				#}
				ENDIF
				
				IF(sv_o !=0) THEN
				#{
					sv_o = SYSTEM(sv_s)
					print(sv_o)
				#}
				ENDIF

				IF(sv_o !=0) THEN
				#{
					sv_o = SYSTEM(sv_s)
					print(sv_o)
				#}
				ENDIF
			
				GOTO PRINT_RECEIPT

			
			#}
			ENDIF
			
		#}
		ELSE
		#{
		
		CUST.CUBP.foracid = CUST.CUBP.drAcctId
		PRINT(CUST.CUBP.tranDate)
		#	CUST.CUBP.tranDate = CUST.CUBP.entity1TranDate
		CUST.CUBP.tranAmt = FORMAT$(CDOUBLE(CUST.CUBP.refAmt),"%.2f")
		CUST.CUBP.tranCrncyCode = CUST.CUBP.refCrncy
		#CUST.CUBP.tran_id = CUST.CUBP.billPayRefNum
		CUST.CUBP.tran_id = CUST.CUBP.cubpTranId
		
		CUST.CUBP.acct_name = ""
		CUST.CUBP.acid = ""
		sv_s = "acct_name,acid|"
		sv_s = sv_s + " SELECT ACCT_NAME,ACID"
		sv_s = sv_s + " FROM TBAADM.GAM "
		sv_s = sv_s + " WHERE FORACID = ?SVAR"
		sv_s = sv_s + " AND DEL_FLG != 'Y'"
		#sv_s = sv_s + " AND BANK_ID = ?SVAR"
		sv_s = sv_s + " AND BANK_ID IS NOT NULL"
		PRINT(sv_s)
		#BANCS.INPARAM.BINDVARS = CUST.CUBP.drAcctId+"|"+BANCS.STDIN.contextBankId
		BANCS.INPARAM.BINDVARS = CUST.CUBP.drAcctId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d == 0)THEN
		#{
			CUST.CUBP.acct_name = BANCS.OUTPARAM.acct_name
			PRINT(CUST.CUBP.acct_name)
			CUST.CUBP.drAcid = BANCS.OUTPARAM.acid
			PRINT(CUST.CUBP.drAcid)
		#}
		ENDIF
		
		CUST.CUBP.tranType = "C"
		CUST.CUBP.tranSubType = "NR"
		CUST.CUBP.partTranType = "D"
		
		CUST.CUBP.tranparticulars = ""
		CUST.CUBP.posted_user_id = ""
		sv_s = "tranparticulars,posted_user_id,part_tran_srl_num|"
		sv_s = sv_s + " SELECT TRAN_PARTICULAR,PSTD_USER_ID,PART_TRAN_SRL_NUM"
		sv_s = sv_s + " FROM TBAADM.DTD "
		sv_s = sv_s + " WHERE ACID = ?SVAR"
		#sv_s = sv_s + " AND TRIM(TRAN_ID) = ?SVAR"
		sv_s = sv_s + " AND TRAN_ID = ?SVAR"
		sv_s = sv_s + " AND TRAN_DATE = TO_DATE(?SVAR,'DD-MM-YYYY')"
		sv_s = sv_s + " AND BANK_ID = ?SVAR"
		sv_s = sv_s + " AND ROWNUM = 1"
		
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CUBP.drAcid + "|" + CUST.CUBP.cubpTranId + "|" + CUST.CUBP.tranDate+"|"+BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d == 0)THEN
		#{
			CUST.CUBP.tranparticulars = BANCS.OUTPARAM.tranparticulars
			PRINT(CUST.CUBP.tranparticulars)
			CUST.CUBP.posted_user_id = BANCS.OUTPARAM.posted_user_id
			PRINT(CUST.CUBP.posted_user_id)
			CUST.CUBP.part_tran_srl_num = BANCS.OUTPARAM.part_tran_srl_num
			PRINT(CUST.CUBP.part_tran_srl_num)
			#}

		ENDIF
		
		CALL("ubpdp074.scr")
		sv_s = "echo '" + CUST.CUBP.outRecord + "'  >> " + CUST.CUBP.lst
		print(sv_s)
		sv_o = SYSTEM(sv_s)
		print(sv_o)
		GOTO PRINT_RECEIPT	
	#}
	ENDIF
#}
ENDIF

IF(CUST.CUBP.tranType == "T")THEN
#{
	CUST.CUBP.foracid = CUST.CUBP.crAcctId
	print(CUST.CUBP.tranDate)
#	CUST.CUBP.tranDate = CUST.CUBP.entity1TranDate
	CUST.CUBP.tranAmt = FORMAT$(CDOUBLE(CUST.CUBP.refAmt),"%.2f")
	CUST.CUBP.tranCrncyCode = CUST.CUBP.refCrncy
	#CUST.CUBP.tran_id = CUST.CUBP.billPayRefNum
	CUST.CUBP.tran_id = CUST.CUBP.cubpTranId	
	CUST.CUBP.acct_name = ""
	CUST.CUBP.acid = ""
	sv_s = "acct_name,acid|"
	sv_s = sv_s + " SELECT ACCT_NAME,ACID"
	sv_s = sv_s + " FROM TBAADM.GAM "
	sv_s = sv_s + " WHERE FORACID = ?SVAR"
	sv_s = sv_s + " AND DEL_FLG != 'Y'"
	sv_s = sv_s + " AND BANK_ID = ?SVAR"
	print(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.CUBP.crAcctId+"|"+BANCS.STDIN.contextBankId
	print(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	IF(sv_d == 0)THEN
	#{
		CUST.CUBP.acct_name = BANCS.OUTPARAM.acct_name
		print(CUST.CUBP.acct_name)
		CUST.CUBP.crAcid = BANCS.OUTPARAM.acid
		print(CUST.CUBP.crAcid)
	#}
	ENDIF

	CUST.CUBP.tranType = "T"
	CUST.CUBP.tranSubType = "CI"
	CUST.CUBP.partTranType = "C"

	CUST.CUBP.tranparticulars = ""
	CUST.CUBP.posted_user_id = ""
	sv_s = "tranparticulars,posted_user_id,part_tran_srl_num|"
	sv_s = sv_s + " SELECT TRAN_PARTICULAR,PSTD_USER_ID,PART_TRAN_SRL_NUM"
	sv_s = sv_s + " FROM TBAADM.DTD "
	sv_s = sv_s + " WHERE ACID = ?SVAR"
	#sv_s = sv_s + " AND TRIM(TRAN_ID) = ?SVAR"
	sv_s = sv_s + " AND TRAN_ID = ?SVAR"
	sv_s = sv_s + " AND TRAN_DATE = TO_DATE(?SVAR,'DD-MM-YYYY')"
	sv_s = sv_s + " AND BANK_ID = ?SVAR"
	sv_s = sv_s + " AND ROWNUM = 1"
	
	print(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.CUBP.crAcid + "|" + CUST.CUBP.cubpTranId + "|" + CUST.CUBP.tranDate+"|"+BANCS.STDIN.contextBankId
	print(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	IF(sv_d == 0)THEN
	#{
		CUST.CUBP.tranparticulars = BANCS.OUTPARAM.tranparticulars
		PRINT(CUST.CUBP.tranparticulars)
		CUST.CUBP.posted_user_id = BANCS.OUTPARAM.posted_user_id
		PRINT(CUST.CUBP.posted_user_id)
		CUST.CUBP.part_tran_srl_num = BANCS.OUTPARAM.part_tran_srl_num
                print(CUST.CUBP.part_tran_srl_num)
	#}
	ELSE
	#{
		CUST.CUBP.tranparticulars = "CREDIT TRANSFER THRU " + MID$(BANCS.STDIN.homeCrncyCode,0,2) + " - " + BANCS.STDIN.mySolDesc
	#}
	ENDIF

	CALL("ubpdp074.scr")
	sv_s = "echo '" + CUST.CUBP.outRecord + "'  >> " + CUST.CUBP.lst
	print(sv_s)
	sv_o = SYSTEM(sv_s)
	print(sv_o)
	IF(sv_o !=0) THEN
	#{
		sv_o = SYSTEM(sv_s)
		print(sv_o)
	#}
	ENDIF

	IF(sv_o !=0) THEN
	#{
		sv_o = SYSTEM(sv_s)
		print(sv_o)
	#}
	ENDIF
	
	IF(sv_o !=0) THEN
	#{
		sv_o = SYSTEM(sv_s)
		print(sv_o)
	#}
	ENDIF

	IF(sv_o !=0) THEN
	#{
		sv_o = SYSTEM(sv_s)
		print(sv_o)
	#}
	ENDIF

	CUST.CUBP.foracid = CUST.CUBP.drAcctId
	print(CUST.CUBP.tranDate)
#	CUST.CUBP.tranDate = CUST.CUBP.entity1TranDate
	CUST.CUBP.tranAmt = FORMAT$(CDOUBLE(CUST.CUBP.refAmt),"%.2f")
	CUST.CUBP.tranCrncyCode = CUST.CUBP.refCrncy
	#CUST.CUBP.tran_id = CUST.CUBP.billPayRefNum
	CUST.CUBP.tran_id = CUST.CUBP.cubpTranId
	
	CUST.CUBP.acct_name = ""
	CUST.CUBP.acid = ""
	sv_s = "acct_name,acid|"
	sv_s = sv_s + " SELECT ACCT_NAME,ACID"
	sv_s = sv_s + " FROM TBAADM.GAM "
	sv_s = sv_s + " WHERE FORACID = ?SVAR"
	sv_s = sv_s + " AND DEL_FLG != 'Y'"
	sv_s = sv_s + " AND BANK_ID = ?SVAR"
	print(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.CUBP.drAcctId+"|"+BANCS.STDIN.contextBankId
	print(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	IF(sv_d == 0)THEN
	#{
		CUST.CUBP.acct_name = BANCS.OUTPARAM.acct_name
		print(CUST.CUBP.acct_name)
		CUST.CUBP.drAcid = BANCS.OUTPARAM.acid
		print(CUST.CUBP.drAcid)
	#}
	ENDIF

	CUST.CUBP.tranType = "T"
	CUST.CUBP.tranSubType = "CI"
	CUST.CUBP.partTranType = "D"

	CUST.CUBP.tranparticulars = ""	
	CUST.CUBP.posted_user_id = ""
	sv_s = "tranparticulars,posted_user_id,part_tran_srl_num|"
	sv_s = sv_s + " SELECT TRAN_PARTICULAR,PSTD_USER_ID,PART_TRAN_SRL_NUM"
	sv_s = sv_s + " FROM TBAADM.DTD "
	sv_s = sv_s + " WHERE ACID = ?SVAR"
	#sv_s = sv_s + " AND TRIM(TRAN_ID) = ?SVAR"
	sv_s = sv_s + " AND TRAN_ID = ?SVAR"
	sv_s = sv_s + " AND TRAN_DATE = TO_DATE(?SVAR,'DD-MM-YYYY')"
	sv_s = sv_s + " AND BANK_ID = ?SVAR"
	sv_s = sv_s + " AND ROWNUM = 1"
	
	print(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.CUBP.drAcid + "|" + CUST.CUBP.cubpTranId + "|" + CUST.CUBP.tranDate+"|"+BANCS.STDIN.contextBankId
	print(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	IF(sv_d == 0)THEN
	#{
		CUST.CUBP.tranparticulars = BANCS.OUTPARAM.tranparticulars
		PRINT(CUST.CUBP.tranparticulars)
		CUST.CUBP.posted_user_id = BANCS.OUTPARAM.posted_user_id
		PRINT(CUST.CUBP.posted_user_id)
		CUST.CUBP.part_tran_srl_num = BANCS.OUTPARAM.part_tran_srl_num
                print(CUST.CUBP.part_tran_srl_num)
	#}
	ELSE
	#{
		CUST.CUBP.tranparticulars = "CREDIT TRANSFER THRU " + MID$(BANCS.STDIN.homeCrncyCode,0,2) + " - " + BANCS.STDIN.mySolDesc
	#}
	ENDIF

	IF(BANCS.STDIN.contextBankId == CUST.CUBP.billerBnkID)THEN
	#{
		CUST.CUBP.field1 = CUST.CUBP.tran_id
		CUST.CUBP.field2 = " "
		#}
	ELSE
		#{
		CUST.CUBP.field1 = CUST.CUBP.billPayRefNum
		CUST.CUBP.field2 = CUST.CUBP.bilName
	#}
	ENDIF
	
		
		CALL("ubpdp074.scr")
		sv_s = "echo '" + CUST.CUBP.outRecord + "'  >> " + CUST.CUBP.lst
		PRINT(sv_s)
		sv_o = SYSTEM(sv_s)
		PRINT(sv_s)
		PRINT(sv_o)
		
		IF(sv_o !=0) THEN
		#{
			sv_o = SYSTEM(sv_s)
			PRINT(sv_o)
		#}
		ENDIF
		
		IF(sv_o !=0) THEN
		#{
			sv_o = SYSTEM(sv_s)
			PRINT(sv_o)
		#}
		ENDIF
		
		IF(sv_o !=0) THEN
		#{
			sv_o = SYSTEM(sv_s)
			PRINT(sv_o)
		#}
		ENDIF
		
		IF(sv_o !=0) THEN
		#{
			sv_o = SYSTEM(sv_s)
			PRINT(sv_o)
		#}
		ENDIF
		
		
		#------------------------------------------------------------------------
		# Charge Tran Id
		#------------------------------------------------------------------------
		IF (CUST.CUBP.chargeTranid != "")THEN
		#{
			
			
			
			BANCS.INPARAM.BINDVARS = CUST.CUBP.chargeTranid + "|" + BANCS.STDIN.contextBankId + "|" + CUST.CUBP.BODDate
			
			PRINT(BANCS.INPARAM.BINDVARS)
			
			sv_s = " chrgAcct,chrgAcctName,chrgAcid,chrgTranpart,chrgPstdUsr,part_tran_srl_num"
			sv_s = sv_s + " |select foracid,acct_name,b.acid,TRAN_PARTICULAR,PSTD_USER_ID,PART_TRAN_SRL_NUM "
			sv_s = sv_s + " from tbaadm.dtd a, tbaadm.gam b "
			sv_s = sv_s + " where tran_id  = ?SVAR "
			sv_s = sv_s + " AND a.acid  = b.acid "
			sv_s = sv_s + " AND a.bank_id = b.bank_id "
			sv_s = sv_s + " AND a.bank_id  = ?SVAR "
			sv_s = sv_s + " AND a.TRAN_DATE = to_date(?SVAR,'dd-mm-yyyy')"
			sv_s = sv_s + " AND part_tran_type = 'C' AND PSTD_USER_ID != 'SYSTEM'"
			
			PRINT(sv_s)
			sv_d = urhk_dbSelectWithBind(sv_s)
			
			IF(sv_d == 0) THEN
			#{
				CUST.UBP.chrgAcct       = BANCS.OUTPARAM.chrgAcct
				CUST.UBP.chrgAcctName   = BANCS.OUTPARAM.chrgAcctName
				CUST.UBP.chrgTranpart   = BANCS.OUTPARAM.chrgTranpart
				CUST.UBP.chrgPstdUsr    = BANCS.OUTPARAM.chrgPstdUsr
				CUST.UBP.parttransrlnum = BANCS.OUTPARAM.part_tran_srl_num
				
				PRINT(CUST.UBP.parttransrlnum)
				PRINT(CUST.UBP.chrgAcct )
				PRINT(CUST.UBP.chrgTranpart)
				PRINT(CUST.UBP.chrgAcct )
				
				CUST.CUBP.tranAmt = FORMAT$(CDOUBLE(CUST.CUBP.chargeAmt),"%.2f")
				CUST.CUBP.foracid = CUST.UBP.chrgAcct
				CUST.CUBP.tranDate =CUST.CUBP.chargetrandate
				CUST.CUBP.tranCrncyCode = CUST.CUBP.chargeCrncy
				CUST.CUBP.tran_id = CUST.CUBP.chargeTranid
				CUST.CUBP.acct_name = CUST.UBP.chrgAcctName
				CUST.CUBP.tranType = "T"
				CUST.CUBP.tranSubType = "BI"
				CUST.CUBP.partTranType = "C"
				CUST.CUBP.tranparticulars = CUST.UBP.chrgTranpart
				CUST.CUBP.posted_user_id = CUST.UBP.chrgPstdUsr
				
				PRINT(CUST.CUBP.foracid)
				PRINT(CUST.CUBP.tranDate)
				PRINT(CUST.CUBP.tranAmt)
				PRINT(CUST.CUBP.tranCrncyCode)
				PRINT(CUST.CUBP.tran_id)
				PRINT(CUST.CUBP.acct_name)
				PRINT(CUST.CUBP.tranType)
				PRINT(CUST.CUBP.tranSubType)
				PRINT(CUST.CUBP.partTranType)
				PRINT(CUST.CUBP.tranparticulars)
				PRINT(CUST.CUBP.posted_user_id)
				
				IF(BANCS.STDIN.contextBankId == CUST.CUBP.billerBnkID)THEN
				#{
					CUST.CUBP.field1 = CUST.CUBP.tran_id
					CUST.CUBP.field2 = " "
					#}
				ELSE
					#{
					CUST.CUBP.field1 = CUST.CUBP.billPayRefNum
					CUST.CUBP.field2 = CUST.CUBP.bilName
				#}
				ENDIF
				CALL("ubpdp074.scr")
				sv_s = "echo '" + CUST.CUBP.outRecord + "'  >> " + CUST.CUBP.lst
				sv_o = SYSTEM(sv_s)
				PRINT(sv_s)
				PRINT(sv_o)
				
				IF(sv_o !=0) THEN
				#{
					sv_o = SYSTEM(sv_s)
					PRINT(sv_o)
				#}
				ENDIF
				
				IF(sv_o !=0) THEN
				#{
					sv_o = SYSTEM(sv_s)
					PRINT(sv_o)
				#}
				ENDIF
				
				IF(sv_o !=0) THEN
				#{
					sv_o = SYSTEM(sv_s)
					PRINT(sv_o)
				#}
				ENDIF
				
				IF(sv_o !=0) THEN
				#{
					sv_o = SYSTEM(sv_s)
					PRINT(sv_o)
				#}
				ENDIF
				
				
				GOTO PRINT_RECEIPT
				
				
			#}
			ENDIF
			
			
			
			
			
		#}
		ENDIF
		
		
		
		GOTO PRINT_RECEIPT
		
	#}
	ENDIF
	
	PRINT_RECEIPT:

		sv_j = " sleep 3 "
                PRINT(sv_j)
                sv_g = SYSTEM(sv_j)
                PRINT(sv_g)
	
		#sv_s = "exebatch mrbx4004 $B2K_SESSION_ID ubp_ofti1.mrt "+CUST.CUBP.lst+" CUBP_RECEIPT"
		sv_s = "exebatch mrbx4004 $B2K_SESSION_ID ubp_ofti1.mrt "+CUST.CUBP.lst+" "+CUST.CUBP.sucRptName
		PRINT(sv_s)
		sv_o = SYSTEM(sv_s)
		PRINT(sv_o)
		
		IF(sv_o !=0) THEN
		#{
			sv_o = SYSTEM(sv_s)
			PRINT(sv_o)
		#}
		ENDIF
		
		IF(sv_o !=0) THEN
		#{
			sv_o = SYSTEM(sv_s)
			PRINT(sv_o)
		#}
		ENDIF
		
		
		#sv_s = "rm -f cubp_receipt.lst"
		
		#print(sv_s)l
		#sv_o = system(sv_s)
		#print(sv_o)
		
		sv_e = GETENV("FIN_REPORTS_DIR")
		PRINT(sv_e)
		#CUST.CUBP.rptFilePath = sv_e + "/" + BANCS.STDIN.userId + "/" + "CUBP_RECEIPT.rpt"
		CUST.CUBP.rptFilePath = sv_e + "/" + BANCS.STDIN.userId + "/" + CUST.CUBP.sucRptName + ".rpt"
		PRINT(CUST.CUBP.rptFilePath)
		
	ENDOFSCRIPT:
		
		sv_u = urhk_SetOrbOut("errorFlg|"+CUST.CUBP.errorFlg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("errorMsg|"+CUST.CUBP.errorMsg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("rptFilePath|"+CUST.CUBP.rptFilePath)
		PRINT(sv_u)
		
		
END-->
