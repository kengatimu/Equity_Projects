<--START
CHECKSUM="93e44c0ea8622e0d985c4c6a0c80d8319f3586d09622e9b02aabb2dee59a1e90"
	TRACE ON
	
	####################################################################
	#section      :creating new repository if required
	#Description  :Checks for the existence of the repository STDWFS and
	#               creates a new one by that name if it does not exist
	#####################################################################
	
	IF (REPEXISTS("CUST") == 0 ) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	############################################################################
	#section      :creating new class if required
	#Desscription :checks for the existence of the class GLBDATA in the
	#repository
	#               create a new one if it does not exist
	##############################################################################
	
	
	IF (CLASSEXISTS("CUST","DATA") == 0) THEN
	#{
		CREATECLASS("CUST","DATA",5)
	#}
	ENDIF
	
	############################################
	#Assigns the values to the variables
	############################################
	
	sv_a = urhk_b2k_PrintRepos("BANCS")
	print(sv_a)
	sv_y=0
	
	IF( (FIELDEXISTS(("BANCS").("INPUT").("Bankid"))== 0) ) THEN
	#{
		CUST.DATA.errMsg="Kindly Supply Bank id"
		sv_e=urhk_SetOrbOut("Error|"+CUST.DATA.errMsg)
		GOTO End
		#}
	ELSE
		#{
		CUST.DATA.Bankid=BANCS.INPUT.Bankid
		print(CUST.DATA.Bankid)
	#}
	ENDIF
	IF( (FIELDEXISTS(("BANCS").("INPUT").("Deviceid"))== 0) ) THEN
	#{
		CUST.DATA.errMsg="Kindly Supply Deviceid"
		sv_e=urhk_SetOrbOut("Error|"+CUST.DATA.errMsg)
		GOTO End
		#}
	ELSE
		#{
		CUST.DATA.Deviceid=BANCS.INPUT.Deviceid
		print(CUST.DATA.Deviceid)
	#}
	ENDIF
	############################################
	# Fetching the value from table
	############################################
	sv_s = "contraAcount,acctnamegl|"
	sv_s = sv_s + " SELECT foracid,acct_name "
	sv_s = sv_s + " FROM tbaadm.GAM "
	sv_s = sv_s + " WHERE acct_short_name = ?SVAR "
	sv_s = sv_s + " AND gl_sub_head_code='10001' "
	sv_s = sv_s + " AND schm_code='CASH' "
	sv_s = sv_s + " AND acct_ownership='O' "
	sv_s = sv_s + " AND DEL_FLG ='N' "
	sv_s = sv_s + " AND bank_id = ?SVAR and rownum<2 "
	BANCS.INPARAM.BINDVARS = CUST.DATA.Deviceid + "|" + CUST.DATA.Bankid
	print(BANCS.INPARAM.BINDVARS)
	sv_d =urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	IF (sv_d==0) THEN
	#{
		CUST.DATA.varValue1 = BANCS.OUTPARAM.contraAcount
		print (CUST.DATA.varValue1)
		CUST.DATA.varValue2 = BANCS.OUTPARAM.acctnamegl
		print(CUST.DATA.varValue2)
		sv_z = urhk_SetOrbOut("GL_ACCOUNT|"+CUST.DATA.varValue1)
		print(sv_z)
		sv_z = urhk_SetOrbOut("GL_NAME|"+CUST.DATA.varValue2)
		print(sv_z)
		sv_e=urhk_SetOrbOut("StattusCode|1")
		#}
	ELSE
		#{
		sv_e=urhk_SetOrbOut("StattusCode|2")
		CUST.DATA.errMsg="Invalid Bank id"
		sv_e=urhk_SetOrbOut("Error|"+CUST.DATA.errMsg)
	#}
	ENDIF
	
	End:
		EXITSCRIPT
		TRACE OFF
END-->
