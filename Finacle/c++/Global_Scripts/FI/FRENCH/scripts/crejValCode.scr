#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: crejValCode.scr
# Type: NA|default
# Description: Validating Reason Code
# Author: Anshul_Singh04
# Date: 5/7/18 4:28 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="7f1e50008b18cee65d31c0831f3e6b74d8edf53fb171c008863347b74ec592a9"
	# Script body goes here
	TRACE ON
	
	sv_a = REPEXISTS("EQT")
	IF (sv_a == 0) THEN
	#{
		CREATEREP("EQT")
	#}
	ENDIF
	sv_a = CLASSEXISTS("EQT", "REJ")
	IF (sv_a == 0 ) THEN
	#{
		CREATECLASS("EQT", "REJ", 5)
	#}
	ENDIF
	
	EQT.REJ.errorMsg =" "
	EQT.REJ.errorFlg = "N"
	EQT.REJ.desc = "0"
	
	#sv_r=urhk_B2k_PrintRepos("BANCS")
	
	IF(FIELDEXISTS(BANCS.INPUT.rejCode))THEN
	#{
		
		IF(TRIM(BANCS.INPUT.rejCode) != "")THEN
		#{
			EQT.REJ.rejCode = BANCS.INPUT.rejCode
			PRINT(EQT.REJ.rejCode)
			#}
		ELSE
			#{
			EQT.REJ.errorFlg ="Y"
			EQT.REJ.errorMsg =" Reject Code not entered!"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		#}
	ELSE
		#{
		EQT.REJ.errorFlg ="Y"
		EQT.REJ.errorMsg ="Reject Code not entered!"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_s = "desc|select nvl(max(CLG_REJ_DESC),0) from tbaadm.crc where del_flg='N'  and CLG_REJ_CODE = ?SVAR and bank_id =?SVAR"
	BANCS.INPARAM.BINDVARS = EQT.REJ.rejCode  + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	IF(sv_d != 0)THEN
	#{
		EQT.REJ.errorFlg ="Y"
		EQT.REJ.errorMsg ="Error in Database Operation!!"
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		EQT.REJ.desc =  BANCS.OUTPARAM.desc
		print(EQT.REJ.desc)
	#}
	ENDIF
	
	IF(EQT.REJ.desc=="0") THEN
	#{
		EQT.REJ.errorFlg ="Y"
		EQT.REJ.errorMsg ="Invalid Reason Code!!"
	#}
	endif
	
	ENDOFSCRIPT:
		sv_u = urhk_SetOrbOut("errFlag|" + EQT.REJ.errorFlg)
		sv_u = urhk_SetOrbOut("errMsg|" + EQT.REJ.errorMsg)
		sv_u = urhk_SetOrbOut("desc|" + EQT.REJ.desc)
		
END-->
