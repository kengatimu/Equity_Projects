#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: s_formatacctdp002.scr
# event: NA
# description: #
#=================================================

#-------------------------------------------------------------------------------------
#    Name               : s_formatacctdp002.scr
#    Description        : 
#    Date               : 11-12-2013 
#    Author             : Prakash
#    Called Script      : NA
#    Calling Script     : NA
#    Menu Option        : HTM
#    Srl. No            Date                  Author              Description.
#    -------            ----                  ------              ----------------
#    1.0               25-04-2543             Prakash              Original Version
#    2.0		  10-05-2013		    Prakash		    Error condition part of userhook getAcctDetailsInRepository Changed.
#-------------------------------------------------------------------------------------

<--START
TRACE ON

#------------------------------------------------------------------------------
#       Creating repository and class
#       Initializing the variables
#       Getting all the values from CPARAM
#------------------------------------------------------------------------------


IF (REPEXISTS("CUST") == 0) THEN
#{
        CREATEREP("CUST")
#}
ENDIF

IF (CLASSEXISTS("CUST","FOAT") == 0) THEN
#{
        CREATECLASS("CUST","FOAT",5)
#}
ENDIF


CUST.FOAT.errMsg = ""

BANCS.INPUT.moduleName = "NEWFORMACCT"
PRINT(BANCS.INPUT.moduleName)

sv_u=urhk_b2k_PrintRepos("BANCS")
print(sv_u)
CUST.FOAT.bodDate1 = BANCS.STDIN.BODDate
print(CUST.FOAT.bodDate1)

BANCS.INPUT.parameterId = "SCHM1"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.schm1 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Scheme code 1 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Scheme code 1 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

BANCS.INPUT.parameterId = "SCHM2"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.schm2 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Scheme code 1 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Scheme code 2 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

BANCS.INPUT.parameterId = "SCHM4"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.schm4 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Scheme code 4 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Scheme code 4 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

BANCS.INPUT.parameterId = "SCHM5"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.schm5 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Scheme code 5 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Scheme code 5 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

BANCS.INPUT.parameterId = "ACCT1"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.acct1 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Account 1 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Account 1 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

BANCS.INPUT.parameterId = "ACCT2"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.acct2 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Account 2 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Account 2 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

BANCS.INPUT.parameterId = "ACCT3"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.acct3 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Account 3 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Account 3 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

BANCS.INPUT.parameterId = "ACCT4"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.acct4 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Account 4 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Account 4 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

BANCS.INPUT.parameterId = "ACCT5"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.acct5 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Account 5 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Account 5 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF
BANCS.INPUT.parameterId = "ACCT6"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.acct6 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Account 6 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Account 6 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

BANCS.INPUT.parameterId = "ACCT7"
CALL("getcparamvaluelong.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.acct7 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Account 7 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Account 7 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

IF(BANCS.STDIN.contextBankId == "54") THEN
#{

	BANCS.INPUT.parameterId = "ACCT8"
	CALL("getcparamvaluelong.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.FOAT.acct8 = BANCS.STDIN.mySolId + BANCS.OUTPUT.parameterVal
	#}
	ELSE
	#{
		#CUST.FOAT.errMsg = "Account 7 setup not done in CPARAM"
		sv_u = urhk_SRV_SetErr("Account 8 setup not done in CPARAM")
		BANCS.OUTPUT.successOrFailure = "F"
		GOTO ENDOFSCRIPT
	#}
	ENDIF

#}
ENDIF


IF(BANCS.STDIN.contextBankId == "54") THEN
#{

	BANCS.INPUT.parameterId = "ACCT9"
	CALL("getcparamvaluelong.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.FOAT.acct9 = BANCS.OUTPUT.parameterVal
		print(CUST.FOAT.acct9)
	#}
	ELSE
	#{
		sv_u = urhk_SRV_SetErr("Account 9 setup not done in CPARAM")
		BANCS.OUTPUT.successOrFailure = "F"
		GOTO ENDOFSCRIPT
	#}
	ENDIF

#}
ENDIF

#################################################################
IF(BANCS.STDIN.contextBankId == "54") THEN
#{

	BANCS.INPUT.parameterId = "ACCT10"
	CALL("getcparamvaluelong.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.FOAT.acct10 = BANCS.OUTPUT.parameterVal
		print(CUST.FOAT.acct10)
	#}
	ELSE
	#{
		sv_u = urhk_SRV_SetErr("Account 10 setup not done in CPARAM")
		BANCS.OUTPUT.successOrFailure = "F"
		GOTO ENDOFSCRIPT
	#}
	ENDIF

#}
ENDIF

IF(BANCS.STDIN.contextBankId == "54") THEN
#{

	BANCS.INPUT.parameterId = "ACCT11"
	CALL("getcparamvaluelong.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.FOAT.acct11 = BANCS.OUTPUT.parameterVal
		print(CUST.FOAT.acct11)
	#}
	ELSE
	#{
		sv_u = urhk_SRV_SetErr("Account 11 setup not done in CPARAM")
		BANCS.OUTPUT.successOrFailure = "F"
		GOTO ENDOFSCRIPT
	#}
	ENDIF

#}
ENDIF
###################################################################

#################################################################
IF(BANCS.STDIN.contextBankId == "54") THEN
#{

	BANCS.INPUT.parameterId = "ACCT12"
	CALL("getcparamvaluelong.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.FOAT.acct12 = BANCS.OUTPUT.parameterVal
		print(CUST.FOAT.acct12)
	#}
	ELSE
	#{
		sv_u = urhk_SRV_SetErr("Account 12 setup not done in CPARAM")
		BANCS.OUTPUT.successOrFailure = "F"
		GOTO ENDOFSCRIPT
	#}
	ENDIF

#}
ENDIF

IF(BANCS.STDIN.contextBankId == "54") THEN
#{

	BANCS.INPUT.parameterId = "ACCT13"
	CALL("getcparamvaluelong.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.FOAT.acct13 = BANCS.OUTPUT.parameterVal
		print(CUST.FOAT.acct13)
	#}
	ELSE
	#{
		sv_u = urhk_SRV_SetErr("Account 13 setup not done in CPARAM")
		BANCS.OUTPUT.successOrFailure = "F"
		GOTO ENDOFSCRIPT
	#}
	ENDIF

#}
ENDIF
###################################################################

IF(BANCS.STDIN.contextBankId == "54") THEN
#{

	BANCS.INPUT.parameterId = "PERIOD"
	CALL("getcparamvaluelong.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.FOAT.period = BANCS.OUTPUT.parameterVal
		print(CUST.FOAT.period)
	#}
	ELSE
	#{
		sv_u = urhk_SRV_SetErr("Period setup not done in CPARAM")
		BANCS.OUTPUT.successOrFailure = "F"
		GOTO ENDOFSCRIPT
	#}
	ENDIF

#}
ENDIF


BANCS.INPUT.parameterId = "BACID2"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.bacid2 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Bacid 2 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Bacid 2 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

BANCS.INPUT.parameterId = "BACID3"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.bacid3 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Bacid 3 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Bacid 3 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

BANCS.INPUT.parameterId = "BACID4"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.bacid4 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Bacid 4 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Bacid 4 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

BANCS.INPUT.parameterId = "BACID5"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.bacid5 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Bacid 5 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Bacid 5 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

BANCS.INPUT.parameterId = "BACID6"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.bacid6 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Bacid 6 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Bacid 6 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPTRIPT
#}
ENDIF
BANCS.INPUT.parameterId = "BACID9"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.bacid9 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Bacid 9 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Bacid 9 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPTRIPT
#}
ENDIF


BANCS.INPUT.parameterId = "MYSOL1"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.mysol1 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Mysol1 1 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Mysol1 1 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

BANCS.INPUT.parameterId = "MYSOL2"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.mysol2 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Mysol1 2 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Mysol2 2 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

BANCS.INPUT.parameterId = "ROLE1"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.role1 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Role ID 1 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Role ID 1 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

BANCS.INPUT.parameterId = "ROLE2"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.role2 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Role ID 2 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Role ID 2 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

BANCS.INPUT.parameterId = "ROLE3"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.role3 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Role ID 3 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Role ID 3 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

BANCS.INPUT.parameterId = "ROLE4"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.role4 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Role ID 4 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Role ID 4 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

BANCS.INPUT.parameterId = "ROLE5"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.role5 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Role ID 5 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Role ID 4 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

BANCS.INPUT.parameterId = "ROLE6"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.role6 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Role ID 6 setup not done in CPARAM"
        sv_u = urhk_SRV_SetErr("Role ID 6 setup not done in CPARAM")
        BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

BANCS.INPUT.parameterId = "ROLE7"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.role7 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "Role ID  7 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("Role ID 7 setup not done in CPARAM")
        BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF


BANCS.INPUT.parameterId = "UWORK1"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.uwork1 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "UWORK1 1 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("UWORK1 1 setup not done in CPARAM")
        BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF

IF(BANCS.STDIN.contextBankId == "54") THEN

BANCS.INPUT.parameterId = "UWORK5"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.uwork5 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "UWORK5 1 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("UWORK5 5 setup not done in CPARAM")
        BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF
ENDIF

BANCS.INPUT.parameterId = "UWORK2"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.uwork2 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "UWORK1 2 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("UWORK1 2 setup not done in CPARAM")
	BANCS.OUTPUT.successOrFailure = "F"	
	GOTO ENDOFSCRIPT
#}
ENDIF

BANCS.INPUT.parameterId = "UWORK4"
CALL("getcparamvalue.scr")
IF (BANCS.OUTPUT.parameterVal != "") THEN
#{
  	CUST.FOAT.uwork4 = BANCS.OUTPUT.parameterVal
#}
ELSE
#{
	#CUST.FOAT.errMsg = "UWORK 4 setup not done in CPARAM"
	sv_u = urhk_SRV_SetErr("4 setup not done in CPARAM")
        BANCS.OUTPUT.successOrFailure = "F"
	GOTO ENDOFSCRIPT
#}
ENDIF
IF (BANCS.STDIN.contextBankId == "56") THEN
	#Cparam for Ug collection accounts by kate starts##
	BANCS.INPUT.moduleName = "FORMACCT"
	PRINT(BANCS.INPUT.moduleName)

	BANCS.INPUT.parameterId = "COLLECTACCT1"
	CALL("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.FOAT.COLLECTACCT1 = BANCS.OUTPUT.parameterVal
	#}
	ELSE
	#{
		#CUST.FOAT.errMsg = "COLLECTACCT1 2 setup not done in CPARAM"
		sv_u = urhk_SRV_SetErr("2 setup not done in CPARAM")
		BANCS.OUTPUT.successOrFailure = "F"	
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	BANCS.INPUT.parameterId = "COLLECTACCT2"
	CALL("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.FOAT.COLLECTACCT2 = BANCS.OUTPUT.parameterVal
	#}
	ELSE
	#{
		#CUST.FOAT.errMsg = "COLLECTACCT2 2 setup not done in CPARAM"
		sv_u = urhk_SRV_SetErr("2 setup not done in CPARAM")
		BANCS.OUTPUT.successOrFailure = "F"	
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	BANCS.INPUT.parameterId = "COLLECTACCT3"
	CALL("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.FOAT.COLLECTACCT3 = BANCS.OUTPUT.parameterVal
	#}
	ELSE
	#{
		#CUST.FOAT.errMsg = "COLLECTACCT3 2 setup not done in CPARAM"
		sv_u = urhk_SRV_SetErr("2 setup not done in CPARAM")
		BANCS.OUTPUT.successOrFailure = "F"	
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	BANCS.INPUT.parameterId = "COLLECTACCT4"
	CALL("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.FOAT.COLLECTACCT4 = BANCS.OUTPUT.parameterVal
	#}
	ELSE
	#{
		#CUST.FOAT.errMsg = "COLLECTACCT4 2 setup not done in CPARAM"
		sv_u = urhk_SRV_SetErr("2 setup not done in CPARAM")
		BANCS.OUTPUT.successOrFailure = "F"	
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	BANCS.INPUT.parameterId = "COLLECTACCT5"
	CALL("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.FOAT.COLLECTACCT5 = BANCS.OUTPUT.parameterVal
	#}
	ELSE
	#{
		#CUST.FOAT.errMsg = "COLLECTACCT5 2 setup not done in CPARAM"
		sv_u = urhk_SRV_SetErr("2 setup not done in CPARAM")
		BANCS.OUTPUT.successOrFailure = "F"	
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	BANCS.INPUT.parameterId = "COLLECTACCT6"
	CALL("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.FOAT.COLLECTACCT6 = BANCS.OUTPUT.parameterVal
	#}
	ELSE
	#{
		#CUST.FOAT.errMsg = "COLLECTACCT6 2 setup not done in CPARAM"
		sv_u = urhk_SRV_SetErr("2 setup not done in CPARAM")
		BANCS.OUTPUT.successOrFailure = "F"	
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	BANCS.INPUT.parameterId = "COLLECTACCT7"
	CALL("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.FOAT.COLLECTACCT7 = BANCS.OUTPUT.parameterVal
	#}
	ELSE
	#{
		#CUST.FOAT.errMsg = "COLLECTACCT7 2 setup not done in CPARAM"
		sv_u = urhk_SRV_SetErr("2 setup not done in CPARAM")
		BANCS.OUTPUT.successOrFailure = "F"	
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	BANCS.INPUT.parameterId = "COLLECTACCT8"
	CALL("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.FOAT.COLLECTACCT8 = BANCS.OUTPUT.parameterVal
	#}
	ELSE
	#{
		#CUST.FOAT.errMsg = "COLLECTACCT8 2 setup not done in CPARAM"
		sv_u = urhk_SRV_SetErr("2 setup not done in CPARAM")
		BANCS.OUTPUT.successOrFailure = "F"	
		GOTO ENDOFSCRIPT
	#}
	ENDIF
ENDIF

	CUST.BILL.parttrnNum = sv_l + 1
	print(CUST.BILL.parttrnNum)
	CUST.FOAT.tranType = CUST.BILL.tranType
	print(CUST.FOAT.tranType)
	CUST.FOAT.subType = CUST.BILL.tranSubType
	print(CUST.FOAT.subType)
	CUST.FOAT.partTran = CUST.BILL.partTran
	print(CUST.FOAT.partTran)
		
	CUST.FOAT.account = CUST.BILL.foracid
	CUST.FOAT.foracid = CUST.BILL.foracid
IF (BANCS.STDIN.contextBankId == "54") THEN
	CUST.FOAT.tranParticulars = CUST.BILL.tranParticulars
	print(CUST.FOAT.tranParticulars)
ENDIF
	IF (CUST.FOAT.account != "") THEN
	#{

		sv_s = "bacid,solid,schmCode,glheadcode,acctowner,custid,acctcrncycode,schmType,AcctopnDate|"
		sv_s = sv_s + "SELECT bacid,sol_id,schm_Code,gl_sub_head_code,acct_ownership,cif_id,acct_crncy_code,schm_Type,to_char(ACCT_OPN_DATE,'DD-MM-YYYY') "
		sv_s = sv_s + "FROM TBAADM.GAM "
		sv_s = sv_s + "WHERE foracid = ?SVAR " 
		sv_s = sv_s + "AND acct_cls_flg ='N' "
		sv_s = sv_s + "AND bank_id = ?SVAR "
		sv_s = sv_s + "AND del_flg = 'N' "
		BANCS.INPARAM.BINDVARS = CUST.FOAT.foracid + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		print(sv_s)
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)

		IF(sv_d == 0) THEN
		#{
			CUST.FOAT.glheadcode = BANCS.OUTPARAM.glheadcode
			print(CUST.FOAT.glheadcode)

			CUST.FOAT.bacid = BANCS.OUTPARAM.bacid
			print(CUST.FOAT.bacid)

			CUST.FOAT.solid = BANCS.OUTPARAM.solid
			print(CUST.FOAT.solid)

			CUST.FOAT.acctownership = BANCS.OUTPARAM.acctowner
			print(CUST.FOAT.acctownership)

			CUST.FOAT.schmCode = BANCS.OUTPARAM.schmCode
			print(CUST.FOAT.schmCode)

			CUST.FOAT.schmType = BANCS.OUTPARAM.schmType
			print(CUST.FOAT.schmType)
			print(BANCS.STDIN.BODDate)

			CUST.FOAT.AcctopnDate = BANCS.OUTPARAM.AcctopnDate
			print(CUST.FOAT.AcctopnDate)

			#CUST.FOAT.accnt = MID$(CUST.FOAT.account,3,13)
			#print (CUST.FOAT.accnt)
			
			
			#****************Start H/O EXPENDITURE DEPT CODES*******************
			IF(BANCS.STDIN.contextBankId == "54") THEN
			#{
			
				IF ((BANCS.OUTPARAM.schmCode == CUST.FOAT.schm1) OR (BANCS.OUTPARAM.schmCode == CUST.FOAT.schm2)) THEN
				#{
					IF ((BANCS.STDIN.userWorkClass != CINT(CUST.FOAT.uwork1)) AND (BANCS.STDIN.userWorkClass != CINT(CUST.FOAT.uwork2))) THEN
					#{
						#CUST.FOAT.errMsg = "Kindly Use IGLS for Group Txns"
						sv_u = urhk_SRV_SetErr(" Only WorkClass 260 or 310 allowed Kindly Use IGLS for Group Txns - parttran srlnum:" +CUST.BILL.parttrnNum)		
						print(sv_u)
                                		BANCS.OUTPUT.successOrFailure = "F"	
						GOTO ENDOFSCRIPT 
					#}
					ENDIF
				#}
				ENDIF

				### Start card center accounts restriction ###
				print(CUST.FOAT.account)

				IF ((CUST.FOAT.account == CUST.FOAT.acct1) OR (CUST.FOAT.account == CUST.FOAT.acct2) OR (CUST.FOAT.account == CUST.FOAT.acct3) OR (CUST.FOAT.account == CUST.FOAT.acct4) OR (CUST.FOAT.account == CUST.FOAT.acct5)) THEN
				#{
					IF (BANCS.STDIN.userWorkClass != CINT(CUST.FOAT.uwork2)) THEN
					#{
						#CUST.FOAT.errMsg = "Insufficent Workclass 260........." 
						sv_u = urhk_SRV_SetErr("Insufficent Workclass 260.........- parttran srlnum:"+CUST.BILL.parttrnNum)	
						print(sv_u)
                                                BANCS.OUTPUT.successOrFailure = "F"
						GOTO ENDOFSCRIPT 
					#}
					ENDIF
				#}
				ENDIF

				IF (CUST.FOAT.account ==CUST.FOAT.acct8) THEN
				#{

					IF (BANCS.STDIN.userWorkClass != "400") THEN
					#{
						 
						sv_u = urhk_SRV_SetErr("Insufficent Workclass 400.........- parttran srlnum:"+CUST.BILL.parttrnNum)	
						print(sv_u)
                                                BANCS.OUTPUT.successOrFailure = "F"
						GOTO ENDOFSCRIPT 
					#}
					ENDIF

				#}
				ENDIF

				CUST.FOAT.accnt9 = MID$(CUST.FOAT.account,3,13)
				print(CUST.FOAT.accnt9)
				sv_v = getPosition(CUST.FOAT.acct9,"," +  CUST.FOAT.accnt9 + ",")
				print(sv_v)
				IF (sv_v!=0)  THEN
				#{

					IF (((BANCS.STDIN.userWorkClass != CINT(CUST.FOAT.uwork5)) OR (BANCS.STDIN.userWorkClass != "310"))  AND (CUST.FOAT.partTran == "D"))  THEN
					#{
						 
						sv_u = urhk_SRV_SetErr("Insufficent Workclass "+CUST.FOAT.uwork5+"/310.........- parttran srlnum:"+CUST.BILL.parttrnNum)	
						print(sv_u)
                                                BANCS.OUTPUT.successOrFailure = "F"
						GOTO ENDOFSCRIPT 
					#}
					ENDIF

				#}
				ENDIF
		### End card center accounts restriction ###



			##-------Blocking Cash transactions  in GLs under OAP and OAB Scheme types---------####

				CUST.FOAT.accnt10 = MID$(CUST.FOAT.account,3,13)
				print(CUST.FOAT.accnt10)

				sv_v = getPosition(CUST.FOAT.acct10,"," +  CUST.FOAT.accnt10 + ",")
				print(sv_v)

				IF (CUST.FOAT.schmType == "OAB") THEN 
				#{
					IF ((sv_v != 0) AND (CUST.FOAT.tranType == "C")) THEN
					#{
					
						sv_u = urhk_SRV_SetErr("Cash Transactions not allowed in xxx"+CUST.FOAT.accnt10+"..- Use Transfer to transact in this GL")	
						print(sv_u)
						BANCS.OUTPUT.successOrFailure = "F"
						GOTO ENDOFSCRIPT 
					#}
					ENDIF
				#}
				ENDIF


				CUST.FOAT.accnt11 = MID$(CUST.FOAT.account,3,13)
				print(CUST.FOAT.accnt11)

				sv_u = getPosition(CUST.FOAT.acct11,"," +  CUST.FOAT.accnt11 + ",")
				print(sv_u)

				IF (CUST.FOAT.schmType == "OAP") THEN 
				#{
					IF ((sv_u == 0) AND (CUST.FOAT.tranType == "C")) THEN
					#{
					
						sv_u = urhk_SRV_SetErr("Cash Transactions not allowed in xxx"+CUST.FOAT.accnt10+"..- Use Transfer to transact in this GL")	
						print(sv_u)
						BANCS.OUTPUT.successOrFailure = "F"
						GOTO ENDOFSCRIPT 
					#}
					ENDIF
				#}
				ENDIF

			##--End of Blocking Cash transactions  in GLs under OAP and OAB Scheme types---------####


		##-------Validating Tran particulars field in Western Union/Moneygram Transactions---------####

				CUST.FOAT.accnt12 = MID$(CUST.FOAT.account,3,13)
				print(CUST.FOAT.accnt12)
				
				sv_v = getPosition(CUST.FOAT.acct12,"," +  CUST.FOAT.accnt12 + ",")
				print(sv_v)

				IF ((sv_v != 0) AND (CUST.FOAT.schmType == "OAB")) THEN
					IF(CUST.FOAT.tranParticulars == "") THEN
					#{			
					sv_u = urhk_SRV_SetErr("Transaction Particulars Field is Mandatory in this transaction")	
					print(sv_u)
					BANCS.OUTPUT.successOrFailure = "F"
					GOTO ENDOFSCRIPT 
					#}
					ENDIF
				
					IF((STRLEN(TRIM(CUST.FOAT.tranParticulars))) != 10) THEN
					#{
					sv_u = urhk_SRV_SetErr("Transaction Particulars Field to have 10 numberic characters for Western Union Transactions")	
					print(sv_u)
					BANCS.OUTPUT.successOrFailure = "F"
					GOTO ENDOFSCRIPT 
					#}
					ENDIF				
				#}
				ENDIF

				CUST.FOAT.accnt13 = MID$(CUST.FOAT.account,3,13)
				print(CUST.FOAT.accnt13)
				
				sv_v = getPosition(CUST.FOAT.acct13,"," +  CUST.FOAT.accnt13 + ",")
				print(sv_v)

				IF ((sv_v != 0) AND (CUST.FOAT.schmType == "OAB")) THEN
					IF(CUST.FOAT.tranParticulars == "") THEN
					#{			
					sv_u = urhk_SRV_SetErr("Transaction Particulars Field is Mandatory in this transaction")	
					print(sv_u)
					BANCS.OUTPUT.successOrFailure = "F"
					GOTO ENDOFSCRIPT 
					#}
					ENDIF
				
					IF((STRLEN(TRIM(CUST.FOAT.tranParticulars))) != 8) THEN
					#{
					sv_u = urhk_SRV_SetErr("Transaction Particulars Field to have 8 numberic characters for MoneyGram Transactions")	
					print(sv_u)
					BANCS.OUTPUT.successOrFailure = "F"
					GOTO ENDOFSCRIPT 
					#}
					ENDIF				
				#}
				ENDIF

		##-------End of Validating Tran particulars field in Western Union/Moneygram Transactions---------####
		

			      #####Cash in Transit restriction starts###
				IF ((CUST.FOAT.bacid  == CUST.FOAT.bacid9)) THEN
				#{
					sv_a = CUST.FOAT.solid
					print(sv_a)
					sv_b = BANCS.STDIN.mySolId
					print(sv_b)		
						
					IF(BANCS.STDIN.userWorkClass < CINT(CUST.FOAT.uwork4))THEN
					#{
						sv_u = urhk_SRV_SetErr("Insufficent Workclass 110.........- parttran srlnum:"+CUST.BILL.parttrnNum)	
						print(sv_u)
						BANCS.OUTPUT.successOrFailure = "F"
						GOTO ENDOFSCRIPT 
					#}
					ENDIF
				#}
				ENDIF
			      #####Cash in TransitGLS restriction ends###

			      IF ((CUST.FOAT.schmCode=="SB135") OR (CUST.FOAT.schmCode=="SB136")) THEN
			      #{
				sv_n = "natid,cifid|"
				sv_n = sv_n + "SELECT nat_id_card_num,cif_id "
				sv_n = sv_n + "FROM TBAADM.cmg "
				sv_n = sv_n + "WHERE cif_id in (select cif_id from tbaadm.gam where foracid= ?SVAR " 
				sv_n = sv_n + "AND acct_cls_flg ='N') "
				sv_n = sv_n + "AND bank_id = ?SVAR "
				BANCS.INPARAM.BINDVARS = CUST.FOAT.foracid + "|" + BANCS.STDIN.contextBankId
				PRINT(BANCS.INPARAM.BINDVARS)
				print(sv_n)
				sv_e = urhk_dbSelectWithBind(sv_n)
				print(sv_e)
					IF(sv_d == 0) THEN
					#{
						CUST.FOAT.natid = BANCS.OUTPARAM.natid
						print(CUST.FOAT.natid)

						CUST.FOAT.natid1=MID$(CUST.FOAT.natid,0,4)
						print(CUST.FOAT.natid1)

						CUST.FOAT.cifid = BANCS.OUTPARAM.cifid
						print(CUST.FOAT.cifid)

						IF(CUST.FOAT.natid1 == "HSNP") THEN
						#{ 
							sv_u = urhk_SRV_SetErr("Kindly put the correct national id " + CUST.FOAT.natid)	
							print(sv_u)
							BANCS.OUTPUT.successOrFailure = "F"
							GOTO ENDOFSCRIPT
						#}
						ENDIF

					#}
					ENDIF

				#}
				ENDIF

				##----------------SB127 compulsory lock for 2 years without debiting---------####

				IF ((CUST.FOAT.partTran == "D") AND (CUST.FOAT.schmCode == "SB127")) THEN
								
				BANCS.INPARAM.date1 = LEFT$(BANCS.STDIN.BODDate,10)
				print(BANCS.INPARAM.date1)

				BANCS.INPARAM.date2 = CUST.FOAT.AcctopnDate
				print(BANCS.INPARAM.date2)

				sv_e = urhk_B2k_dateDiff("")
				PRINT(BANCS.OUTPARAM.dateDiff)
      
					IF(CINT(BANCS.OUTPARAM.dateDiff) < CUST.FOAT.period) THEN
					#{
						sv_u = urhk_SRV_SetErr("Account in the scheme " +CUST.FOAT.schmCode+ " cannot be debited before maturity (2years)")		
						print(sv_u)
						BANCS.OUTPUT.successOrFailure = "F"	
						GOTO ENDOFSCRIPT

					#}
					ENDIF
				#}
				ENDIF
				##----------------SB127 compulsory lock for 2 years without debiting Ends HERE---------####

			#}
			ENDIF
			### Start collection  accounts UG restriction by kate ###

			IF (BANCS.STDIN.contextBankId == "56") THEN
				print(CUST.FOAT.COLLECTACCT1)
				print(CUST.FOAT.account)
				print(CUST.FOAT.partTran)
				IF ((CUST.FOAT.account == CUST.FOAT.COLLECTACCT1) OR (CUST.FOAT.account == CUST.FOAT.COLLECTACCT2)OR (CUST.FOAT.account == CUST.FOAT.COLLECTACCT3)OR (CUST.FOAT.account == CUST.FOAT.COLLECTACCT4)OR (CUST.FOAT.account == CUST.FOAT.COLLECTACCT5)OR (CUST.FOAT.account == CUST.FOAT.COLLECTACCT6)OR (CUST.FOAT.account == CUST.FOAT.COLLECTACCT7)OR (CUST.FOAT.account == CUST.FOAT.COLLECTACCT8)AND (CUST.FOAT.partTran=="D")) THEN
				PRINT("TRUE")
					IF (BANCS.STDIN.userWorkClass < CINT(CUST.FOAT.uwork2)) THEN
						#{
							#CUST.FOAT.errMsg = "Insufficent Workclass 260........." 
							sv_u = urhk_SRV_SetErr("Insufficent Workclass 260.........- parttran srlnum:"+CUST.BILL.parttrnNum)	
							print(sv_u)
							BANCS.OUTPUT.successOrFailure = "F"
							GOTO ENDOFSCRIPT 
						#}
					ENDIF
				ENDIF

				IF (CUST.FOAT.acctownership!="O") THEN
				#{
					sv_t = "status|"
					sv_t = sv_t + "select acct_status from tbaadm.smt where bank_id= ?SVAR and  "
					sv_t = sv_t + " acid in (select acid from tbaadm.gam where foracid= ?SVAR and bank_id= ?SVAR) "
					BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + CUST.FOAT.foracid + "|" + BANCS.STDIN.contextBankId
					PRINT(BANCS.INPARAM.BINDVARS)
					print(sv_t)
					sv_e = urhk_dbSelectWithBind(sv_t)
					print(sv_e)
					IF(sv_e == 0) THEN
					#{
						CUST.FOAT.status = BANCS.OUTPARAM.status
						print(CUST.FOAT.status)
					ELSE
						CUST.FOAT.status = ""
						print(CUST.FOAT.status)
					#}
					ENDIF
				
				
					IF ((CUST.FOAT.status=="I") AND (CUST.FOAT.partTran=="D")) THEN
					
						sv_u = urhk_SRV_SetErr("DEBIT TO INACTIVE ACCOUNT NOT ALLOWED:"+CUST.BILL.parttrnNum)	
						print(sv_u)

					ENDIF
				#}
				ENDIF

			ENDIF
			
			
			
			###collection  accounts Endrestriction ###

				
			IF((BANCS.STDIN.contextBankId == "50") OR (BANCS.STDIN.contextBankId == "55" )OR (BANCS.STDIN.contextBankId == "56"))THEN 
			#{
					
					#*********************START RESTRICT MIGRATION A/C TO 600****************************************************
					
					IF ((CUST.FOAT.account == CUST.FOAT.acct1) OR (CUST.FOAT.account == CUST.FOAT.acct2) OR (CUST.FOAT.account == CUST.FOAT.acct3)) THEN
						
						IF (BANCS.STDIN.userWorkClass < CUST.FOAT.uwork1) THEN
								print ("not okay")
								#CUST.FOAT.errMsg = " Insufficent Workclass For ATM GLs( >600 ) "
								sv_u = urhk_SRV_SetErr("Insufficent Workclass For ATM GLs ( >600 )- parttran srlnum :" +CUST.BILL.parttrnNum) 	
								print(sv_u)
								BANCS.OUTPUT.successOrFailure="F"
								GOTO ENDOFSCRIPT	
						ENDIF
						
					 ENDIF
					 
					#*********************START RESTRICT MIGRATION A/C TO 600****************************************************
			#}
			ENDIF
	
			
			IF(BANCS.STDIN.contextBankId == "54" OR BANCS.STDIN.contextBankId == "11") THEN
			#{
				IF ((CUST.FOAT.bacid  == CUST.FOAT.bacid2)) THEN
				#{
					sv_a = CUST.FOAT.solid  
					sv_b = BANCS.STDIN.mySolId
					#mySolId1
					IF (sv_b != CUST.FOAT.mysol1) THEN
					#{
						IF (sv_a != sv_b) THEN
						#{
							print ("not same branch")
							#CUST.FOAT.errMsg = "Intersol Transaction into this account not allowed "
							sv_u = urhk_SRV_SetErr("Intersol Transaction into this account not allowed- parttran srlnum:"+CUST.BILL.parttrnNum)	
							print(sv_u)
							BANCS.OUTPUT.successOrFailure="F"
							GOTO  ENDOFSCRIPT
						#}
						ENDIF
					#}
					ENDIF
				#}
				ENDIF
			#}
			ENDIF
		
			IF((BANCS.STDIN.contextBankId == "54") OR (BANCS.STDIN.contextBankId == "50") OR (BANCS.STDIN.contextBankId == "11")) THEN
			#{
				IF ((CUST.FOAT.schmCode == CUST.FOAT.schm5) OR ( CUST.FOAT.schmCode == CUST.FOAT.schm4)) THEN
				#{
						sv_a = CUST.FOAT.solid
						sv_b = BANCS.STDIN.mySolId
						
					IF ((sv_a != sv_b) AND (sv_b != CUST.FOAT.mysol2)) THEN
					#{
						sv_w = getPosition(CUST.FOAT.acct6,"," +  CUST.FOAT.account + ",")
						print(sv_w)
						
						IF (sv_w<=0) THEN
							print ("not same branch")
							#CUST.FOAT.errMsg = "Intersol Transaction into this account not allowed " 
							sv_u = urhk_SRV_SetErr("Intersol Transaction into this account not allowed- parttran srlnum:"+CUST.BILL.parttrnNum)
							print(sv_u)
                                                        BANCS.OUTPUT.successOrFailure="F"
							GOTO ENDOFSCRIPT
						ELSE
							IF ((sv_w >0) AND (CUST.FOAT.partTran=="D")) THEN
							sv_u = urhk_SRV_SetErr("Intersol Transaction into this account not allowed- parttran srlnum:"+CUST.BILL.parttrnNum)
							print(sv_u)
                                                        BANCS.OUTPUT.successOrFailure="F"
							GOTO ENDOFSCRIPT

							ENDIF
						ENDIF
					#}	
					ENDIF
				#}
				ENDIF
			
			#}
			ENDIF
		
			IF((BANCS.STDIN.contextBankId == "54") OR (BANCS.STDIN.contextBankId == "11")) THEN
			#{
			
				IF (( CUST.FOAT.bacid == CUST.FOAT.bacid3) OR ( CUST.FOAT.bacid == CUST.FOAT.bacid4) OR (CUST.FOAT.bacid == CUST.FOAT.bacid5)) THEN
				#{		
						IF (BANCS.STDIN.userWorkClass < CUST.FOAT.uwork4) THEN
						#{
							print ("not okay")
							#CUST.FOAT.errMsg = "Insufficent Workclass For ATM GLs( >110 ) " 
							sv_u = urhk_SRV_SetErr("Insufficent Workclass For ATM GLs( >110 )- parttran srlnum:"+CUST.BILL.parttrnNum)
							print(sv_u)
                                                        BANCS.OUTPUT.successOrFailure="F"
							GOTO ENDOFSCRIPT
						#}
						ENDIF
				#}
				ENDIF
			#}
			ENDIF


			
		
		
			IF((BANCS.STDIN.contextBankId == "50") OR (BANCS.STDIN.contextBankId == "55") OR (BANCS.STDIN.contextBankId == "56")) THEN
			#{
					#*********************UNMATCHED RECEIPTS ONLY TO BE HIT FROM HO****************************************************
					 IF ((CUST.FOAT.account == CUST.FOAT.acct1)) THEN
						sv_a =	CUST.FOAT.solid 
						sv_b = BANCS.STDIN.mySolId
						
						
						IF(sv_b != CUST.FOAT.mysol2) THEN
							#CUST.FOAT.errMsg = " THIS IS A HEAD OFFICE ACCOUNT !" 
							sv_u = urhk_SRV_SetErr("THIS IS A HEAD OFFICE ACCOUNT !- parttran srlnum:"+CUST.BILL.parttrnNum)
                                                        print(sv_u)
                                                        BANCS.OUTPUT.successOrFailure="F"
							GOTO ENDOFSCRIPT 
						ENDIF
					 ENDIF
			#}
			ENDIF

			IF (BANCS.STDIN.contextBankId == "50") THEN
			#{
					IF ((CUST.FOAT.bacid == CUST.FOAT.bacid2) OR (CUST.FOAT.bacid == CUST.FOAT.bacid3)) THEN
						sv_a = CUST.FOAT.solid
						sv_b = BANCS.STDIN.mySolId
						
						
						IF((sv_b != CUST.FOAT.mysol1 ) AND (CUST.FOAT.partTran=="D")) THEN
							#CUST.FOAT.errMsg = "THIS IS A HEAD OFFICE ACCOUNT.CONTACT H/O! ........." 
							sv_u = urhk_SRV_SetErr("THIS IS A HEAD OFFICE ACCOUNT.CONTACT H/O!- parttran srlnum :"+CUST.BILL.parttrnNum)
                                                        print(sv_u)
                                                        BANCS.OUTPUT.successOrFailure="F"
							GOTO ENDOFSCRIPT 
						ENDIF
					 ENDIF
					#*********************END UNMATCHED RECEIPTS TO BE HIT FROM HO****************************************************

			#}
			ENDIF

			######REstriction for Nostro Accounts#####

			IF (BANCS.STDIN.contextBankId == "54") THEN
			#{

				IF ((CUST.FOAT.partTran=="D")) THEN
						sv_w = getPosition(CUST.FOAT.acct7,"!" +  CUST.FOAT.account + "!")
						print(sv_w)
						sv_a =	CUST.FOAT.solid 
						sv_b = BANCS.STDIN.mySolId
						
						
						#IF ((sv_b != CUST.FOAT.mysol2) AND (sv_w >0) ) THEN
						#	
						#		sv_u = urhk_SRV_SetErr("THIS IS A HEAD OFFICE ACCOUNT..CONTACT TREASURY !- parttran srlnum:"+CUST.BILL.parttrnNum)
						#		print(sv_u)
						#		BANCS.OUTPUT.successOrFailure="F"
						#		GOTO ENDOFSCRIPT 
						#							
						#ENDIF
							
						IF (sv_w >0) THEN

							IF  ((BANCS.STDIN.userWorkClass != "240" ) AND (BANCS.STDIN.userWorkClass != "084") AND (BANCS.STDIN.userWorkClass != "131")) THEN
								sv_u = urhk_SRV_SetErr("Insufficent Workclass For Nostro Accounts(240 or 084)  !- parttran srlnum:"+CUST.BILL.parttrnNum)
								print(sv_u)
								BANCS.OUTPUT.successOrFailure="F"
								GOTO ENDOFSCRIPT 
							ENDIF

						ENDIF
				#}
				ENDIF

				IF ((CUST.FOAT.solid =="777")  AND (CUST.FOAT.schmCode=="SB777") AND (CUST.FOAT.partTran=="D")) THEN
				#{
					sv_u = urhk_SRV_SetErr("Transactions not allowed in this account!")
					print(sv_u)
					BANCS.OUTPUT.successOrFailure="F"
					GOTO ENDOFSCRIPT 
				#}
				ENDIF

			#}
			ENDIF
		
			IF( BANCS.STDIN.contextBankId == "54" ) THEN
			#{
				IF (CUST.FOAT.bacid == CUST.FOAT.bacid6) THEN
				#{
					IF ((BANCS.STDIN.roleId == CUST.FOAT.role1) OR (BANCS.STDIN.roleId == CUST.FOAT.role2) OR (BANCS.STDIN.roleId == CUST.FOAT.role3) OR (BANCS.STDIN.roleId == CUST.FOAT.role4) OR (BANCS.STDIN.roleId == CUST.FOAT.role5) OR (BANCS.STDIN.roleId == CUST.FOAT.role6)) THEN	
					#{
						sv_a = CUST.FOAT.solid
						sv_b = BANCS.STDIN.mySolId
						IF (sv_a != sv_b) THEN
						#{
						    print ("not same branch")
							#CUST.FOAT.errMsg = "Intersol Transcation into this account not allowed " 
							sv_u = urhk_SRV_SetErr("Intersol Transcation into this account not allowed- parttran srlnum:"+CUST.BILL.parttrnNum)
                                                        print(sv_u)
                                                        BANCS.OUTPUT.successOrFailure="F"
							GOTO ENDOFSCRIPT 
						 #}
						ENDIF
					#}
					ELSE
					#{
						IF (BANCS.STDIN.userWorkClass < CUST.FOAT.uwork4) THEN
						#{
							print ("not okay")
							#sv_a = urhk_TBAF_DispMesg(" Insufficent Workclass ( >110 ) ........." )
							sv_u = urhk_SRV_SetErr("Insufficent Workclass ( >110 )- parttran srlnum:"+CUST.BILL.parttrnNum)
                                                        print(sv_u)
                                                        BANCS.OUTPUT.successOrFailure="F"
							GOTO ENDOFSCRIPT 
						#}
						ENDIF
					#}
					ENDIF
				#}
				ENDIF
					#----Collections customization moved to collect.scr Eric Karobia
			#}
			ENDIF
			IF((BANCS.STDIN.contextBankId == "11") OR (BANCS.STDIN.contextBankId == "55")) THEN
			#{
					IF (CUST.FOAT.bacid == "RESERVE") THEN
					#{
						IF ((BANCS.STDIN.roleId == CUST.FOAT.role7) OR (BANCS.STDIN.roleId == CUST.FOAT.role1) OR (BANCS.STDIN.roleId == CUST.FOAT.role2) OR (BANCS.STDIN.roleId == CUST.FOAT.role3) OR (BANCS.STDIN.roleId == CUST.FOAT.role4) OR (BANCS.STDIN.roleId == CUST.FOAT.role5)OR (BANCS.STDIN.roleId == CUST.FOAT.role6)) THEN	
						 #{
							sv_a = CUST.FOAT.solid
							sv_b = BANCS.STDIN.mySolId
							IF (sv_a != sv_b) THEN
							#{
								print ("not same branch")
								#CUST.FOAT.errMsg = "Intersol Transcation into this account not allowed " 
								sv_u = urhk_SRV_SetErr("Intersol Transcation into this account not allowed- parttran srlnum:"+CUST.BILL.parttrnNum)
                                                        	print(sv_u)
                                                        	BANCS.OUTPUT.successOrFailure="F"
								GOTO ENDOFSCRIPT 
							 #}
							ENDIF
						#}
						ELSE
						#{
							IF (BANCS.STDIN.userWorkClass < 110) THEN
							#{
								print ("not okay")
								#CUST.FOAT.errMsg = " Insufficent Workclass ( >110 )" 
								sv_u = urhk_SRV_SetErr("Insufficent Workclass ( >110 )- parttran srlnum:"+CUST.BILL.parttrnNum)
                                                                print(sv_u)
                                                                BANCS.OUTPUT.successOrFailure="F"
								GOTO ENDOFSCRIPT 
							#}
							ENDIF
						#}	
						ENDIF
					#}
					ENDIF
			#}
			ENDIF
	
			
			IF ((CUST.FOAT.subType == "CP") AND (CUST.FOAT.acctownership!="O")) THEN
			#{
					  sv_u = urhk_SRV_SetErr("This option is not allowed for This Account:")
					  print(sv_u)
					  BANCS.OUTPUT.successOrFailure="F"
					  GOTO ENDOFSCRIPT 

			#}
			ENDIF

	
			#Tm Charges Part moved here---charge_off.scr

			IF ((CUST.FOAT.subType == "NR") OR (CUST.FOAT.subType == "CI") OR (CUST.FOAT.subType == "BI") OR (CUST.FOAT.subType == "NP")) THEN
			#{

				sv_s = "chrge_off_flg|SELECT chrge_off_flg FROM TBAADM.GAC "
				sv_s = sv_s + "WHERE acid = (SELECT acid "
				sv_s = sv_s + "FROM TBAADM.GAM "
				sv_s = sv_s + "WHERE foracid = ?SVAR "
				sv_s = sv_s + "AND bank_id = ?SVAR "
				sv_s = sv_s + "AND del_flg = 'N' and schm_type ='LAA' "
				sv_s = sv_s + ")"
				sv_s = sv_s + "AND bank_id = ?SVAR "
				BANCS.INPARAM.BINDVARS = CUST.FOAT.account + "|" + BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.contextBankId
				PRINT(BANCS.INPARAM.BINDVARS)
				PRINT(sv_s)
				sv_d = urhk_dbSelectWithBind(sv_s)
			  
				PRINT(sv_d) 
				IF (sv_d != 0) THEN
				#{
					GOTO ENDOFSCRIPT1
				#}
				ENDIF
			
				sv_y = BANCS.OUTPARAM.chrge_off_flg
				print(sv_y)

				IF (sv_y == "Y") THEN
				#{
					#CUST.FOAT.errMsg = "Charged off Account use menu option RACO"
					sv_u = urhk_SRV_SetErr("Charged off Account use menu option RACO - parttran srlnum:"+CUST.BILL.parttrnNum)
                                        print(sv_u)
                                        BANCS.OUTPUT.successOrFailure="F"
					GOTO ENDOFSCRIPT
				#}
				ENDIF
			#}
			ENDIF


ENDOFSCRIPT1:
			IF ((CUST.FOAT.subType == "NP")OR (CUST.FOAT.subType == "CI") OR (CUST.FOAT.subType == "BI")) THEN
			#{
				sv_s = "chrge_off_flg|SELECT chrge_off_flg FROM TBAADM.GAC "
				sv_s = sv_s + "WHERE acid = (SELECT acid "
				sv_s = sv_s + "FROM TBAADM.GAM "
				sv_s = sv_s + "WHERE foracid = ?SVAR "
				sv_s = sv_s + "AND bank_id = ?SVAR "
				sv_s = sv_s + "AND del_flg = 'N' and schm_type !='LAA' "
				sv_s = sv_s + ")"
				sv_s = sv_s + "AND bank_id = ?SVAR "
				BANCS.INPARAM.BINDVARS = CUST.FOAT.account + "|" + BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.contextBankId
				PRINT(BANCS.INPARAM.BINDVARS)
				PRINT(sv_s)
				sv_d = urhk_dbSelectWithBind(sv_s)
			  
				PRINT(sv_d) 
				IF (sv_d != 0) THEN
				#{
					GOTO ENDOFSCRIPT
				#}
				ENDIF
			
				sv_y = BANCS.OUTPARAM.chrge_off_flg
				print(sv_y)

				IF (sv_y == "Y") THEN
				#{
					IF ((BANCS.STDIN.userWorkClass < 310) AND (CUST.FOAT.partTran=="D")) THEN
						sv_u = urhk_SRV_SetErr("Charged off Account use menu option HTARACO. Contact H/O Admin Workclass 310 - parttran srlnum:"+CUST.BILL.parttrnNum)
						print(sv_u)
						BANCS.OUTPUT.successOrFailure="F"
						GOTO ENDOFSCRIPT
					ENDIF
				#}
				ENDIF
			#}
			ENDIF
			
		#}
		ENDIF



	#}
	ENDIF

	
ENDOFSCRIPT:
#IF(CUST.FOAT.errMsg != "" )THEN
#{
#	sv_u = urhk_SRV_SetErr(CUST.FOAT.errMsg)
#	print(sv_u)
#}
#ENDIF

TRACE OFF
END-->
