#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: ubpdp009.scr
# event: NA
# description: NA
#=================================================

<--START
#----------------------------------------------------------------------------------------------------------
# Filename              :       ubpdp009.scr
# Author                :       Swati Ganjoo
# Description           :       This script is used to calculate the Rate.
# Called Script         :		
# Calling Script        :		ubpdp008.scr
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----           -----           --------          ----------------        ---------
#  1           16-04-2013         Swati Ganjoo      Original Version          1.0
#-----------------------------------------------------------------------------------------------------------
	# Script body goes here
	# Script body goes here
	TRACE ON
	CUST.UBP.errorFlg = "N"
	BANCS.INPARAM.InputAmount = CUST.UBP.billAmt
	PRINT(BANCS.INPARAM.InputAmount)
	BANCS.INPARAM.FromCurrency = CUST.UBP.dbCrncyCode
	PRINT(BANCS.INPARAM.FromCurrency)
	BANCS.INPARAM.ToCurrency = CUST.UBP.crCrncyCode
	PRINT(BANCS.INPARAM.ToCurrency)
	BANCS.INPARAM.RateCode = CUST.UBP.rateCode
	PRINT(BANCS.INPARAM.RateCode)
	BANCS.INPARAM.Rate = ""
	
	#***********************
	# Converting the amount
	#***********************
	sv_r = urhk_B2k_ConvertAmount("")
	PRINT(sv_r)
	IF(sv_r == 0) THEN
	#{
		PRINT(BANCS.OUTPARAM.Rate)
		CUST.UBP.OutputAmount = TRIM(BANCS.OUTPARAM.OutputAmount)
		PRINT(CUST.UBP.OutputAmount)
		CUST.UBP.OutputAmount=cint(CUST.UBP.OutputAmount)
		PRINT(CUST.UBP.OutputAmount)

		CUST.UBP.OutputAmount=cdouble(CUST.UBP.OutputAmount)
		PRINT(CUST.UBP.OutputAmount)

		#CUST.UBP.DerivedRate = BANCS.OUTPARAM.Rate
		#}
	ELSE
		#{
		CUST.UBP.errorFlg = "Y"
		#CUST.UBP.errorMsg = "Error in fetching amount"
		CUST.UBP.errorCode = "UBP_ERR023"
		CALL("ubpdp044.scr")
		CUST.UBP.errorMsg =CUST.UBP.errorDesc
		CUST.UBP.errorFocus="rateCode"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	#	IF(CUST.UBP.Rate != "") THEN
	#{
	#		IF(CDOUBLE(CUST.UBP.Rate) != CDOUBLE(CUST.UBP.DerivedRate)) THEN
	#{
	#			CUST.UBP.successOrFailure = "F"
	#			BANCS.INPUT.ErrorCode = "IMT1030"
	#			GOTO ERROREXIT
	#}
	#		ENDIF
	#}
	#	ENDIF
END-->
