#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: ubpdp052.scr
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START
CHECKSUM="354728a08502701d64d845cb1da8ba16c722c3fad686da2d5aa60d425ca54aa6"
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp052.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called for remote outbound calls(general billers)
	# Called Script         :
	# Calling script        :		ubpdp052.scr
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           24-07-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	#========================================================
	# Check whether CUST Repository exists else create it
	#========================================================
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	#========================================================
	# Check whether UBP Class exists else create it
	#========================================================
	sv_r = CLASSEXISTS("CUST","UBP")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","UBP",5)
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST","INTERFACE") == 0) THEN
	#{
		CREATECLASS ("CUST","INTERFACE",5)
	#}
	ENDIF
	CUST.UBP.errorFlg="N"
	CUST.UBP.errorMsg=" "
	CUST.UBP.valSuccMsg=" "
	CUST.UBP.remTranPart=" "
	CUST.UBP.remtranRem=" "
	CUST.UBP.getServiceName1="VAL"
	CUST.UBP.remName=" "
	CUST.UBP.retStrngVal=" "
	CUST.UBP.refCrncy=" "
	CUST.INTERFACE.tranStatus = " "
	IF(FIELDEXISTS(BANCS.INPUT.billRefNum))THEN
	#{
		IF(TRIM(BANCS.INPUT.billRefNum) != "")THEN
		#{
			CUST.UBP.billRefNum=BANCS.INPUT.billRefNum
			PRINT(CUST.UBP.billRefNum)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.billAcctId))THEN
	#{
		IF(TRIM(BANCS.INPUT.billAcctId) != "")THEN
		#{
			CUST.UBP.billAcctId=BANCS.INPUT.billAcctId
			PRINT(CUST.UBP.billAcctId)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.billerId))THEN
	#{
		IF(TRIM(BANCS.INPUT.billerId) != "")THEN
		#{
			CUST.UBP.billerId=BANCS.INPUT.billerId
			PRINT(CUST.UBP.billerId)
			
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.billAmt))THEN
	#{
		IF(TRIM(BANCS.INPUT.billAmt) != "")THEN
		#{
			CUST.UBP.billAmt=BANCS.INPUT.billAmt
			PRINT(CUST.UBP.billAmt)
			
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.insertTableFlg))THEN
	#{
		IF(TRIM(BANCS.INPUT.insertTableFlg) != "")THEN
		#{
			CUST.UBP.insertTableFlg=BANCS.INPUT.insertTableFlg
			PRINT(CUST.UBP.insertTableFlg)
			
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.typOfServ))THEN
	#{
		IF(TRIM(BANCS.INPUT.typOfServ) != "")THEN
		#{
			CUST.UBP.typOfServ=BANCS.INPUT.typOfServ
			PRINT(CUST.UBP.typOfServ)
			
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.refCrncy))THEN
	#{
		IF(TRIM(BANCS.INPUT.typOfServ) != "")THEN
		#{
			CUST.UBP.refCrncy=BANCS.INPUT.refCrncy
			PRINT(CUST.UBP.refCrncy)
			
		#}
		ENDIF
	#}
	ENDIF
	CALL("FIOutbound_connection.scr")
	IF(CUST.INTERFACE.errorMsgFlg=="Y")THEN
	#{
		CUST.UBP.errorFlg="Y"
		CUST.UBP.errorMsg=CUST.INTERFACE.errorMsgDesc
		CUST.UBP.insertTableFlg="N"
		PRINT(CUST.UBP.errorMsg)
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		CUST.UBP.valSuccMsg="Validation done successfully!"
		IF(FIELDEXISTS(CUST.UBP.remTranPart))THEN
		#{
			PRINT(CUST.UBP.remTranPart)
		#}
		ENDIF
		
		IF(FIELDEXISTS(CUST.UBP.remName))THEN
		#{
			PRINT(CUST.UBP.remName)
		#}
		ENDIF
		IF(FIELDEXISTS(CUST.UBP.remtranRem))THEN
		#{
			PRINT(CUST.UBP.remtranRem)
		#}
		ENDIF
		IF(FIELDEXISTS(CUST.UBP.retStrngVal))THEN
		#{
			PRINT(CUST.UBP.retStrngVal)
		#}
		ENDIF
	#}
	ENDIF
	ENDOFSCRIPT:
		
		sv_s = "errorFlg|" + CUST.UBP.errorFlg
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		IF(CUST.INTERFACE.tranStatus == "FAILURE")THEN
		#{
			CUST.UBP.errorMsg = CUST.INTERFACE.message
		#}
		ENDIF
		
		sv_s = "errorMsg|" + CUST.UBP.errorMsg
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "valSuccMsg|" + CUST.UBP.valSuccMsg
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "insertTableFlg|" + CUST.UBP.insertTableFlg
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "remTranPart|" + CUST.UBP.remTranPart
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "remName|" + CUST.UBP.remName
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "remtranRem|" + CUST.UBP.remtranRem
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "retStrngVal|" + CUST.UBP.retStrngVal
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		#TRACE OFF
		EXITSCRIPT
END-->
