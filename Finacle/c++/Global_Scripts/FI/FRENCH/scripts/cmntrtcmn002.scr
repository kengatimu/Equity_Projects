#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cmntrtcmn002.scr
# Type: NA|default
# Description: script on click of submit in CMNTRTC
# Author: haneesha.kanumuru
# Date: 5/4/18 11:14 AM
# Modification Log:
#============================================================

<--START
CHECKSUM="cc4769e3248b9adc52765482405c45ac72ec401f431996b16859a3953aecbc49"
	
	# Script body goes here
	TRACE ON
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CMNTRTC Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CMNTRTC")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CMNTRTC",5)
	#}
	ENDIF
	
	#==========================================================*
	# Intilializing the Variables
	#==========================================================*
	CUST.CMNTRTC.recCount =""
	IF(FIELDEXISTS(BANCS.INPUT.funcCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.funcCode) != "")THEN
		#{
			CUST.CMNTRTC.funcCode = TOUPPER(TRIM(BANCS.INPUT.funcCode))
			PRINT(CUST.CMNTRTC.funcCode)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.fromCrncy)) THEN
	#{
		IF(TRIM(BANCS.INPUT.fromCrncy) != "")THEN
		#{
			CUST.CMNTRTC.fromCrncy= TOUPPER(TRIM(BANCS.INPUT.fromCrncy))
			PRINT(CUST.CMNTRTC.fromCrncy)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.rateCodeCash)) THEN
	#{
		IF(TRIM(BANCS.INPUT.rateCodeCash) != "")THEN
		#{
			CUST.CMNTRTC.rateCodeCash= TOUPPER(TRIM(BANCS.INPUT.rateCodeCash))
			PRINT(CUST.CMNTRTC.rateCodeCash)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.rateCodeTransfer)) THEN
	#{
		IF(TRIM(BANCS.INPUT.rateCodeTransfer) != "")THEN
		#{
			CUST.CMNTRTC.rateCodeTransfer= TOUPPER(TRIM(BANCS.INPUT.rateCodeTransfer))
			PRINT(CUST.CMNTRTC.rateCodeTransfer)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.toCrncy)) THEN
	#{
		IF(TRIM(BANCS.INPUT.toCrncy) != "")THEN
		#{
			CUST.CMNTRTC.toCrncy= TOUPPER(TRIM(BANCS.INPUT.toCrncy))
			PRINT(CUST.CMNTRTC.toCrncy)
		#}
		ENDIF
	#}
	ENDIF
	
	#==========================================================*
	#For A-ADD
	#==========================================================*
	IF(CUST.CMNTRTC.funcCode == "A") THEN
	#{
		sv_s = "insert into custom.c_ratecode_mod(FROM_CURRENCY,TO_CURRENCY,RATECODE_CASH,RATECODE_TRAN,DEL_FLG,ENTITY_CRE_FLG,RCRE_USER_ID,RCRE_TIME,LCHG_USER_ID,LCHG_TIME,BANK_ID)"
		sv_s = sv_s + " values(?SVAR,?SVAR,?SVAR,?SVAR,'N','N',?SVAR,sysdate,?SVAR,sysdate,?SVAR)"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS=CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + CUST.CMNTRTC.rateCodeCash + "|" + CUST.CMNTRTC.rateCodeTransfer + "|" + BANCS.STDIN.userId + "|"  + BANCS.STDIN.userId + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSqlWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d != 0)THEN
		#{
			#ERROR
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbOut("Error|Database Insertion failed")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		CUST.CMNTRTC.resultMsg = "Record has been Added successfully"
		sv_u = urhk_SetOrbOut("RESULT_MSG|"+CUST.CMNTRTC.resultMsg)
		PRINT(sv_u)
		#}
	#}
	ENDIF
	#==========================================================*
	#For M-MODIFY
	#==========================================================*
	IF(CUST.CMNTRTC.funcCode == "M") THEN
	#{
		sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
		sv_s = sv_s + " SELECT 1 FROM custom.c_ratecode_mod"
		sv_s = sv_s + " WHERE"
		sv_s = sv_s + " FROM_CURRENCY = ?SVAR  AND TO_CURRENCY =?SVAR AND BANK_ID = ?SVAR)"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d == 0) THEN
		#{
			sv_s = "update  custom.c_ratecode_mod set "
			sv_s = sv_s + " RATECODE_CASH=?SVAR,"
			sv_s = sv_s + " RATECODE_TRAN = ?SVAR,"
			sv_s = sv_s + " LCHG_USER_ID=?SVAR,"
			sv_s = sv_s + " LCHG_TIME = sysdate,"
			sv_s = sv_s + " ENTITY_CRE_FLG = 'N'"
			sv_s = sv_s + " where FROM_CURRENCY = ?SVAR  AND TO_CURRENCY =?SVAR AND BANK_ID = ?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.rateCodeCash + "|" + CUST.CMNTRTC.rateCodeTransfer +"|" + BANCS.STDIN.userId +"|" + CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0)THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error_1|Error^Database Updation Failed^settlccy")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			#}
		ELSE
			#{
			sv_s = "insert into custom.c_ratecode_mod(FROM_CURRENCY,TO_CURRENCY,RATECODE_CASH,RATECODE_TRAN,DEL_FLG,ENTITY_CRE_FLG,RCRE_USER_ID,RCRE_TIME,LCHG_USER_ID,LCHG_TIME,BANK_ID)"
			sv_s = sv_s + " values(?SVAR,?SVAR,?SVAR,?SVAR,'N','N',?SVAR,sysdate,?SVAR,sysdate,?SVAR)"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS=CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + CUST.CMNTRTC.rateCodeCash + "|" + CUST.CMNTRTC.rateCodeTransfer + "|" + BANCS.STDIN.userId + "|"  + BANCS.STDIN.userId + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0)THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database Insertion failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		CUST.CMNTRTC.resultMsg = "Record has been Modified successfully"
		sv_u = urhk_SetOrbOut("RESULT_MSG|"+CUST.CMNTRTC.resultMsg)
		PRINT(sv_u)
	#}
	ENDIF
	
	#==========================================================*
	#For V- VERIFY
	#==========================================================*
	IF(CUST.CMNTRTC.funcCode == "V") THEN
	#{
		sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
		sv_s = sv_s + " SELECT 1 FROM custom.c_ratecode"
		sv_s = sv_s + " WHERE"
		sv_s = sv_s + " FROM_CURRENCY = ?SVAR  AND TO_CURRENCY =?SVAR AND BANK_ID = ?SVAR)"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d == 0) THEN
		#{
			CUST.CMNTRTC.recCount = BANCS.OUTPARAM.recCount
			PRINT(BANCS.OUTPARAM.recCount)
			
		#}
		ENDIF
		
		IF(CUST.CMNTRTC.recCount == 1) THEN
		#{
			sv_s = "update  custom.c_ratecode set "
			sv_s = sv_s + " LCHG_USER_ID=?SVAR,"
			sv_s = sv_s + " LCHG_TIME = sysdate,"
			sv_s = sv_s + " ENTITY_CRE_FLG = 'Y',"
			sv_s = sv_s + " RATECODE_CASH = ?SVAR,"
			sv_s = sv_s + " RATECODE_TRAN = ?SVAR"
			sv_s = sv_s + " where FROM_CURRENCY = ?SVAR  AND TO_CURRENCY =?SVAR AND BANK_ID = ?SVAR"
			PRINT(sv_s)
			#BANCS.INPARAM.BINDVARS =  BANCS.STDIN.userId +"|" + CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
			BANCS.INPARAM.BINDVARS =  BANCS.STDIN.userId +"|" + CUST.CMNTRTC.rateCodeCash + "|" + CUST.CMNTRTC.rateCodeTransfer
			BANCS.INPARAM.BINDVARS =  BANCS.INPARAM.BINDVARS +"|" + CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database update failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			
			sv_s = "delete from custom.c_ratecode_mod where FROM_CURRENCY=?SVAR  AND TO_CURRENCY= ?SVAR  AND BANK_ID=?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database delete failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			#}
		ELSE
			#{
			sv_s = "update  custom.c_ratecode_mod set "
			sv_s = sv_s + " ENTITY_CRE_FLG = 'Y'"
			sv_s = sv_s + " where FROM_CURRENCY = ?SVAR  AND TO_CURRENCY =?SVAR AND BANK_ID = ?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS =   CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database update failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF

                        sv_s = "insert into custom.c_ratecode select * from custom.c_ratecode_mod where FROM_CURRENCY=?SVAR AND TO_CURRENCY=?SVAR  AND BANK_ID=?SVAR"
                        PRINT(sv_s)
                        BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
                        PRINT(BANCS.INPARAM.BINDVARS)
                        sv_d = urhk_dbSqlWithBind(sv_s)
                        PRINT(sv_d)
                        IF(sv_d != 0) THEN
                        #{
                                #ERROR
                                sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
                                PRINT(sv_u)
                                sv_u = urhk_SetOrbOut("Error|Database insert failed")
                                PRINT(sv_u)
                                GOTO ENDOFSCRIPT
                        #}
                        ENDIF

                        sv_s = "delete from custom.c_ratecode_mod where FROM_CURRENCY=?SVAR  AND TO_CURRENCY= ?SVAR  AND BANK_ID=?SVAR"
                        PRINT(sv_s)
                        BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
                        PRINT(BANCS.INPARAM.BINDVARS)
                        sv_d = urhk_dbSqlWithBind(sv_s)
                        PRINT(sv_d)

                        IF(sv_d != 0) THEN
                        #{
                                #ERROR
                                sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
                                PRINT(sv_u)
                                sv_u = urhk_SetOrbOut("Error|Database delete failed")
                                PRINT(sv_u)
                                GOTO ENDOFSCRIPT
                        #}
                        ENDIF
		#}
		ENDIF
		CUST.CMNTRTC.resultMsg = "Record has been Verified successfully"
		sv_u = urhk_SetOrbOut("RESULT_MSG|"+CUST.CMNTRTC.resultMsg)
		PRINT(sv_u)
	#}
	ENDIF
	#==========================================================*
	#For X-CANCEL
	#==========================================================*
	IF(CUST.CMNTRTC.funcCode == "X") THEN
	#{
		sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
		sv_s = sv_s + " SELECT 1 FROM custom.c_ratecode_mod"
		sv_s = sv_s + " WHERE"
		sv_s = sv_s + " FROM_CURRENCY = ?SVAR  AND TO_CURRENCY =?SVAR AND BANK_ID = ?SVAR)"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d == 0) THEN
		#{
			sv_s = "delete from custom.c_ratecode_mod where FROM_CURRENCY=?SVAR  AND TO_CURRENCY= ?SVAR  AND BANK_ID=?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database delete failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			#}
		ELSE
			#{
			sv_s = "delete from custom.c_ratecode where FROM_CURRENCY=?SVAR  AND TO_CURRENCY= ?SVAR  AND BANK_ID=?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database delete failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		CUST.CMNTRTC.resultMsg = "Record has been cancelled successfully"
		sv_u = urhk_SetOrbOut("RESULT_MSG|"+CUST.CMNTRTC.resultMsg)
		PRINT(sv_u)
	#}
	ENDIF
	#==========================================================*
	#For D- DELETE
	#==========================================================*
	IF(CUST.CMNTRTC.funcCode == "D") THEN
	#{
		sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
		sv_s = sv_s + " SELECT 1 FROM custom.c_ratecode_mod"
		sv_s = sv_s + " WHERE"
		sv_s = sv_s + " FROM_CURRENCY = ?SVAR  AND TO_CURRENCY =?SVAR AND BANK_ID = ?SVAR)"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d == 0) THEN
		#{
			sv_s = "delete from custom.c_ratecode_mod where FROM_CURRENCY=?SVAR  AND TO_CURRENCY= ?SVAR  AND BANK_ID=?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database delete failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			#}
		ELSE
			#{
			sv_s = "delete from custom.c_ratecode where FROM_CURRENCY=?SVAR  AND TO_CURRENCY= ?SVAR  AND BANK_ID=?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database delete failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		
		CUST.CMNTRTC.resultMsg = "Record has been deleted successfully"
		sv_u = urhk_SetOrbOut("RESULT_MSG|"+CUST.CMNTRTC.resultMsg)
		PRINT(sv_u)
		
	#}
	ENDIF
	sv_z = urhk_SetOrbOut("SuccessOrFailure|Y")
	PRINT(sv_z)
	
	ENDOFSCRIPT:
END-->
