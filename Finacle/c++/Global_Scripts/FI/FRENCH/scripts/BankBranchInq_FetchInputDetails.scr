#----------------------------------------------------------------------------------------------------------
# Filename              :       BankBranchInq_FetchInputDetails.scr
# Author                :       Kalvin Joseph
# Description           :       This script fetches the values passed in Request XML
# Input                 :       PaySysId, LocalClearingFlg
# Output                :       BankBranchInq_FetchInputAndAssign.scr
# Called Script         :       BankBranchInq_Main.scr
# Calling Script        :       None
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----           -----           --------          ----------------        ---------
#  1           07-03-2013  	  Kalvin            Original Version          1.0
#-----------------------------------------------------------------------------------------------------------
<--START
TRACE ON

	sv_v = "BankBranchInq_REQ.PaySysID"	
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN 
	#{
		CUST.INTERFACE.paySysID = ("BANCS").("INPUT").(sv_v)
	#}
	ELSE 
	#{	
		CUST.INTERFACE.paySysID = ""
	#}
	ENDIF

	sv_v = "BankBranchInq_REQ.ClearingFlg"	
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN 
	#{
		CUST.INTERFACE.localClearingFlg = ("BANCS").("INPUT").(sv_v)
	#}
	ELSE 
	#{	
		CUST.INTERFACE.localClearingFlg = ""
	#}
	ENDIF

	sv_v = "BankBranchInq_REQ.BankCode"	
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN 
	#{
		CUST.INTERFACE.bankCode = ("BANCS").("INPUT").(sv_v)
	#}
	ELSE 
	#{	
		CUST.INTERFACE.bankCode = ""
	#}
	ENDIF	

	sv_v = "BankBranchInq_REQ.BankID"
	IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
        #{
                CUST.INTERFACE.bankId= ("BANCS").("INPUT").(sv_v)
        #}
        ELSE
        #{
                CUST.INTERFACE.bankId = ""
        #}
	ENDIF

	PRINT(CUST.INTERFACE.paySysID)
	PRINT(CUST.INTERFACE.localClearingFlg)
	PRINT(CUST.INTERFACE.bankCode)
	PRINT(CUST.INTERFACE.bankId)



#TRACE OFF
END-->
