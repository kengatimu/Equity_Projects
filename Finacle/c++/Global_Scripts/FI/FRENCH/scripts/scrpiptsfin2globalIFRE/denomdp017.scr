#--------------------------------------------------------------------------------------------------
#     NAME            : denomdp017.scr
#     DATE            : 27-JUL-2010
#     DESCRIPTION     : This script file performs the validation for teller id if it is a valid one
#			or not and sends the result to denommn004.jsp
#     MENU OPTION     : HDENRO
#     CALLING JSP     : denommn004.jsp
#     AUTHOR NAME     : Sandeep Patil
#     MODULE CODE     :	DENOM
#     MODIFICATION
#     HISTORY         :
#                       Serial.No       Date	   AuthorName      Description
#----------------------------------------------------------------------------------------------------

<--START
CHECKSUM="d67c962923368a0113e808e83e293097fdbf98f160628cdde06653cab6a6ffde"
	TRACE ON
	
	sv_r = urhk_B2k_PrintRepos("BANCS")
	#print(sv_r)
	
	#------------------------------------------------------------------------------------------------
	# CREATION OF REPOSITORIES
	#------------------------------------------------------------------------------------------------
	
	IF(REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF(CLASSEXISTS("CUST", "DENOM") == 0) THEN
	#{
		CREATECLASS("CUST","DENOM",5)
	#}
	ENDIF
	
	#----------------------------------------------------------------------------------------------
	# INITIALIZE THE VARIABLES
	#-----------------------------------------------------------------------------------------------
	
	CUST.DENOM.teller_id = BANCS.STDIN.EmployeeId
	#print(CUST.DENOM.teller_id)
	
	#-----------VALIDATE THE TELLER ID  : START ----------------------------------------------------
	
	BANCS.INPARAM.BINDVARS = CUST.DENOM.teller_id + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_a = "count | SELECT count(*) "
	sv_a = sv_a + "FROM gec "
	sv_a = sv_a + "WHERE emp_id = ?SVAR AND bank_id = ?SVAR AND entity_cre_flg = 'Y' AND del_flg != 'Y' "
	print(sv_a)
	
	sv_q = urhk_dbSelectwithBind(sv_a)
	print(sv_q)
	
	IF(sv_q == 0) THEN
	#{
		CUST.DENOM.count = BANCS.OUTPARAM.count
		#print(CUST.DENOM.count)
		IF(CUST.DENOM.count == 0) THEN
		#{
			CUST.DENOM.ErrMsg = "User is not a valid teller"
			sv_u = urhk_setOrbOut("Failure|"+CUST.DENOM.ErrMsg)
			#print(sv_u)
			GOTO FINISH
			#}
		ELSE
			#{
			CUST.DENOM.ErrMsg = "User is a valid teller"
			sv_u = urhk_setOrbOut("Success|"+CUST.DENOM.ErrMsg)
			#print(sv_u)
			GOTO FINISH
		#}
		ENDIF
	#}
	ENDIF
	
	#-----------VALIDATE THE TELLER ID  : END --------------------------------------------------------
	
	#--------------------------------------------------------------------------------------------------
	#	THIS STEP IS TO EXIT FROM THE SCRIPT
	#--------------------------------------------------------------------------------------------------
	
	FINISH:
		TRACE OFF
		exitscript
END-->
