##########################################################################################
# Source Name           : siInterEntitydp006.scr
# Date                  : 29-Apr-2013
# Description           : This script is called from SRV's to veridy record in Verify mode.
# Author                : Priyanka T02
# Modification History  :
# <Serial No.>  <Date>        <Author Name>             <Description>
# ------------  -----------   -----------------         --------------
#      1        29-Apr-2013    Priyanka T02               Base Version
###########################################################################################

<--START
	trace on
	# Script body goes here
	#---------------------------------------------------------------------
	#Check whether CUST Repository exists else create it
	#---------------------------------------------------------------------
	
	IF(REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	#---------------------------------------------------------------------
	#Check whether CSIIET Class exists else create it
	#---------------------------------------------------------------------
	
	IF(CLASSEXISTS("CUST","CSIIET") == 0) THEN
	#{
		CREATECLASS ("CUST","CSIIET",5)
	#}
	ENDIF
	
	#---------------------------------------------------------------------
	#Initialize the variables
	#---------------------------------------------------------------------
	CUST.CSIIET.siSrlNum = ""
	CUST.CSIIET.siInterEntityFlg = ""
	CUST.CSIIET.targetEntity = ""
	CUST.CSIIET.targetEntityAcctId = ""
	CUST.CSIIET.entityCreFlg = ""
	CUST.CSIIET.delFlg = ""
	CUST.CSIIET.bankId = BANCS.STDIN.contextBankId
	
	#---------------------------------------------------------------------
	#Obtaining the value of si srl num
	#---------------------------------------------------------------------
	sv_u = urhk_SRV_GetVal("siHeader.siSrlNum")
	PRINT(sv_u)
	IF(sv_u != 0)THEN
	#{
		CUST.CSIIET.successOrFailure = "F"
		print("Couldn fetch siSrlNum")
		GOTO ENDOFSCRIPT
	#}
	ELSE
	#{
		CUST.CSIIET.siSrlNum = BANCS.OUTPARAM.srvValue
		print(CUST.CSIIET.siSrlNum)
	#}
	ENDIF

	BANCS.INPARAM.BINDVARS = CUST.CSIIET.siSrlNum + "|" + CUST.CSIIET.bankId
        print(BANCS.INPARAM.BINDVARS)

        sv_s = "targetEntity,targetEntityAcctId,siInterEntityFlg,entityCreFlg,delFlg|"
        sv_s = sv_s + "SELECT target_entity,target_entity_account_id,inter_entity_si_flg,entity_cre_flg,del_flg "
        sv_s = sv_s + "FROM CUSTOM.IESID "
        sv_s = sv_s + "WHERE si_srl_num = ?SVAR "
        sv_s = sv_s + "AND bank_id = ?SVAR"
        print(sv_s)

        sv_d = urhk_dbSelectWithBind(sv_s)
        print(sv_d)

        IF(sv_d == 0)THEN
        #{
                CUST.CSIIET.targetEntity = BANCS.OUTPARAM.targetEntity
                CUST.CSIIET.targetEntityAcctId = BANCS.OUTPARAM.targetEntityAcctId
                CUST.CSIIET.siInterEntityFlg = BANCS.OUTPARAM.siInterEntityFlg
                CUST.CSIIET.entityCreFlg = BANCS.OUTPARAM.entityCreFlg
                CUST.CSIIET.delFlg = BANCS.OUTPARAM.delFlg
                print(CUST.CSIIET.targetEntity)
                print(CUST.CSIIET.targetEntityAcctId)
                print(CUST.CSIIET.siInterEntityFlg)
                print(CUST.CSIIET.entityCreFlg)
                print(CUST.CSIIET.delFlg)

                IF((CUST.CSIIET.entityCreFlg == "Y") AND (CUST.CSIIET.delFlg == "Y"))THEN
                #{
                        BANCS.INPARAM.BINDVARS = CUST.CSIIET.siSrlNum + "|" + CUST.CSIIET.bankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_s = "UPDATE CUSTOM.IESID "
                        sv_s = sv_s + "SET DEL_FLG = 'N'"
                        sv_s = sv_s + "WHERE SI_SRL_NUM = ?SVAR "
                        sv_s = sv_s + "AND BANK_ID = ?SVAR"
                        print(sv_s)

                        sv_d = urhk_dbSQLWithBind(sv_s)
                        print(sv_d)

                        IF(sv_d != 0)THEN
                        #{
                                print("Could not update data in iesid table")
                                GOTO ENDOFSCRIPT
                        #}
                        ENDIF

                        #------------------------------
                        #Delete record from mod table
                        #------------------------------
                        BANCS.INPARAM.BINDVARS = CUST.CSIIET.siSrlNum + "|" + CUST.CSIIET.bankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_s = "DELETE FROM CUSTOM.IESID_MOD "
                        sv_s = sv_s + "WHERE SI_SRL_NUM = ?SVAR "
                        sv_s = sv_s + "AND BANK_ID = ?SVAR"
                        print(sv_s)

                        sv_d = urhk_dbSQLWithBind(sv_s)
                        print(sv_d)

                        IF(sv_d != 0)THEN
                        #{
                                print("Could not delete custom data from mod table")
                                GOTO ENDOFSCRIPT
                        #}
                        ENDIF

			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	
	BANCS.INPARAM.BINDVARS = CUST.CSIIET.siSrlNum + "|" + CUST.CSIIET.bankId
	print(BANCS.INPARAM.BINDVARS)
	
	sv_s = "targetEntity,targetEntityAcctId,siInterEntityFlg,entityCreFlg,delFlg|"
	sv_s = sv_s + "SELECT target_entity,target_entity_account_id,inter_entity_si_flg,entity_cre_flg,del_flg "
	sv_s = sv_s + "FROM CUSTOM.IESID_MOD "
	sv_s = sv_s + "WHERE si_srl_num = ?SVAR "
	sv_s = sv_s + "AND bank_id = ?SVAR"
	print(sv_s)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	IF(sv_d == 0)THEN
	#{
		CUST.CSIIET.targetEntity = BANCS.OUTPARAM.targetEntity
		CUST.CSIIET.targetEntityAcctId = BANCS.OUTPARAM.targetEntityAcctId
		CUST.CSIIET.siInterEntityFlg = BANCS.OUTPARAM.siInterEntityFlg
		CUST.CSIIET.entityCreFlg = BANCS.OUTPARAM.entityCreFlg
		CUST.CSIIET.delFlg = BANCS.OUTPARAM.delFlg
		print(CUST.CSIIET.targetEntity)
		print(CUST.CSIIET.targetEntityAcctId)
		print(CUST.CSIIET.siInterEntityFlg)
		print(CUST.CSIIET.entityCreFlg)
		print(CUST.CSIIET.delFlg)
				
		IF((CUST.CSIIET.entityCreFlg == "Y") AND (CUST.CSIIET.delFlg == "N"))THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CSIIET.targetEntity +"|" + CUST.CSIIET.targetEntityAcctId + "|" + CUST.CSIIET.siInterEntityFlg 
			BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.CSIIET.siSrlNum + "|" + CUST.CSIIET.bankId
			print(BANCS.INPARAM.BINDVARS)
			
			sv_s = "UPDATE CUSTOM.IESID "
			sv_s = sv_s + "SET TARGET_ENTITY = ?SVAR, "
			sv_s = sv_s + "TARGET_ENTITY_ACCOUNT_ID = ?SVAR, "
			sv_s = sv_s + "INTER_ENTITY_SI_FLG = ?SVAR "
			sv_s = sv_s + "WHERE SI_SRL_NUM = ?SVAR "
			sv_s = sv_s + "AND BANK_ID = ?SVAR"
			print(sv_s)
			
			sv_d = urhk_dbSQLWithBind(sv_s)
			print(sv_d)
			
			IF(sv_d != 0)THEN
			#{
				print("Could not update data in iesid table")
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			
			#------------------------------
			#Delete record from mod table
			#------------------------------
			BANCS.INPARAM.BINDVARS = CUST.CSIIET.siSrlNum + "|" + CUST.CSIIET.bankId
			print(BANCS.INPARAM.BINDVARS)
			
			sv_s = "DELETE FROM CUSTOM.IESID_MOD "
			sv_s = sv_s + "WHERE SI_SRL_NUM = ?SVAR "
			sv_s = sv_s + "AND BANK_ID = ?SVAR"
			print(sv_s)
			
			sv_d = urhk_dbSQLWithBind(sv_s)
			print(sv_d)
			
			IF(sv_d != 0)THEN
			#{
				print("Could not delete custom data from mod table")
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		
		IF(CUST.CSIIET.delFlg == "Y")THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CSIIET.delFlg + "|" + CUST.CSIIET.siSrlNum + "|" + CUST.CSIIET.bankId
			print(BANCS.INPARAM.BINDVARS)
			
			sv_s = "UPDATE CUSTOM.IESID "
			sv_s = sv_s + "SET DEL_FLG = ?SVAR "
			sv_s = sv_s + "WHERE SI_SRL_NUM = ?SVAR "
			sv_s = sv_s + "AND BANK_ID = ?SVAR"
			print(sv_s)
			sv_d = urhk_dbSQLWithBind(sv_s)
			print(sv_d)
			
			IF(sv_d != 0)THEN
			#{
				print("Could not update data in iesid table")
				GOTO ENDOFSCRIPT
			#}
			ENDIF
	
			#------------------------------
                        #Delete record from mod table
                        #------------------------------
                        BANCS.INPARAM.BINDVARS = CUST.CSIIET.siSrlNum + "|" + CUST.CSIIET.bankId
                        print(BANCS.INPARAM.BINDVARS)

                        sv_s = "DELETE FROM CUSTOM.IESID_MOD "
                        sv_s = sv_s + "WHERE SI_SRL_NUM = ?SVAR "
                        sv_s = sv_s + "AND BANK_ID = ?SVAR"
                        print(sv_s)

                        sv_d = urhk_dbSQLWithBind(sv_s)
                        print(sv_d)

                        IF(sv_d != 0)THEN
                        #{
                                print("Could not delete custom data from mod table")
                                GOTO ENDOFSCRIPT
                        #}
                        ENDIF
		#}
		ENDIF
		
	#}
	ELSE
	#{
		print("add verify")
		BANCS.INPARAM.BINDVARS = CUST.CSIIET.siSrlNum + "|" + CUST.CSIIET.bankId
		print(BANCS.INPARAM.BINDVARS)
		
		sv_s = "UPDATE CUSTOM.IESID "
		sv_s = sv_s + "SET ENTITY_CRE_FLG = 'Y' "
		sv_s = sv_s + "WHERE SI_SRL_NUM = ?SVAR "
		sv_s = sv_s + "AND BANK_ID = ?SVAR"
		print(sv_s)
		
		sv_d = urhk_dbSQLWithBind(sv_s)
		print(sv_d)
		
		IF(sv_d != 0)THEN
		#{
			print("Could not update custom table")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
		
	ENDOFSCRIPT:
		EXITSCRIPT
		
		trace off
END-->
