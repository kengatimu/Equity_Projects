#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: accmdp018.scr
# event: NA
# description: NA
#=================================================
#**************************************************************************************************************
# Filename              : accmdp019.scr
# Description           : The script isused for fetching the terminate reason code in case of termination of a contract .
# Input                 :
# Output                :
# Called Script         :
# Calling Script        : accmdp018.scr
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----          ------           --------          ----------------        ---------
#  1           25-04-2013       Kalvin Joseph    Original Version		             1.1
#**************************************************************************************************************

<--START
CHECKSUM="b81b72a7df800b676f7a829f01b120c6c8416905d2f00aaaa00595eaa5729a2d"
	# Script body goes here
	TRACE ON
	
	
	#============================================================================
	# Create the necessary Repositories and Classes
	#============================================================================
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
		#}
	ELSE
		#{
		DELETEREP("CUST")
		CREATEREP("CUST")
	#}
	ENDIF
	
	
	IF (CLASSEXISTS("CUST", "CONTRACT") == 0) THEN
	#{
		CREATECLASS("CUST", "CONTRACT", 5)
		#}
	ELSE
		#{
		DELETECLASS("CUST", "CONTRACT")
		CREATECLASS("CUST", "CONTRACT", 5)
	#}
	ENDIF
	
	CUST.CONTRACT.reasonCode = " "
	CUST.CONTRACT.reasonValue = " "
	CUST.CONTRACT.recordCount = "0"
	
	PRINT(BANCS.INPUT.funcCode)
	CUST.CONTRACT.funCode=BANCS.INPUT.funcCode
	
	IF(CUST.CONTRACT.funCode== "T")THEN
	#{
		BANCS.INPARAM.BINDVARS = "Y"
		print(BANCS.INPARAM.BINDVARS)
		
		sv_s= "recordCount|SELECT count(1) "
		sv_s = sv_s +"FROM CUSTOM.C_CPVALUE "
		sv_s = sv_s +"WHERE ENTITY_CRE_FLG =?SVAR "
		sv_s = sv_s +"AND PARAMETER_ID LIKE 'TERM%'"
		sv_s = sv_s +"AND MODULE_NAME =  'CATOD'"
		
		sv_c = urhk_dbSelectWithBind(sv_s)
		print(sv_c)
		IF(sv_c == 0)THEN
		#{
			CUST.CONTRACT.recordCount = BANCS.OUTPARAM.recordCount
			print(CUST.CONTRACT.recordCount)
		#}
		ENDIF
		#}
	ELSE
		#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	
	sv_u = urhk_SetOrbOut("recordCount|"+CUST.CONTRACT.recordCount)
	print(sv_u)
	
	#--------------------------------------------------------------------------
	# Create cursor and open it
	#--------------------------------------------------------------------------
	
	BANCS.INPARAM.BINDVARS = "Y"
	sv_s= "resCode,resValue|"
	sv_s = sv_s +" SELECT PARAMETER_ID, PARAMETER_VALUE "
	sv_s = sv_s +"FROM CUSTOM.C_CPVALUE "
	sv_s = sv_s +"WHERE ENTITY_CRE_FLG ='Y' "
	sv_s = sv_s +"AND PARAMETER_ID LIKE 'TERM%' "
	sv_s = sv_s +"AND MODULE_NAME = 'CATOD' "
	
	sv_r = urhk_dbCursorOpenWithBind(sv_s)
	
	IF (sv_r != 0) THEN
	#{
		BANCS.OUTPUT.fatalErrorMsg = "Unable to open cursor. Contact system admin"
		BANCS.OUTPUT.fatalErrorFlg = "Y"
		GOTO FINISH
	#}
	ENDIF
	
	CUST.CONTRACT.cursorNumber = BANCS.OUTPARAM.DB_CURSOR_NUMBER
	sv_r = urhk_dbCursorFetch(CUST.CONTRACT.cursorNumber)
	
	sv_i = 1
	
	WHILE(sv_r == 0)
	#{
		CUST.CONTRACT.reasonCode = BANCS.OUTPARAM.resCode
		CUST.CONTRACT.reasonValue 	 = BANCS.OUTPARAM.resValue
		print(CUST.CONTRACT.reasonCode)
		print(CUST.CONTRACT.reasonValue)
		sv_y = format$(sv_i, "%d")
		print(sv_y)
		sv_s = "resCode_" + sv_y + "|" +CUST.CONTRACT.reasonCode
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		
		sv_s = "resValue_" + sv_y + "|" + CUST.CONTRACT.reasonValue
		print(sv_s)
		sv_r = urhk_SetOrbOut(sv_s)
		
		
		sv_i = sv_i + 1
		print(sv_i)
		#----------------------------------------------------------------------
		# Fetch next record
		#----------------------------------------------------------------------
		
		sv_r = urhk_dbCursorFetch(CUST.CONTRACT.cursorNumber)
		
	#}
	DO
	#--------------------------------------------------------------------------
	#Close the cursor
	#--------------------------------------------------------------------------
	sv_r = urhk_dbCursorClose(CUST.CONTRACT.cursorNumber)
	
	FINISH:
		
	ENDOFSCRIPT:
		TRACE OFF
END-->
