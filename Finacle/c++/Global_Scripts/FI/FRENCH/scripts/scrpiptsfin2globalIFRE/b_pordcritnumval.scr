#=================================================
# IMPORTANT| Auto Generated Header. DO NOT CHANGE MANUALLY.
# category| Script Name Based
# script| b_controlnumberval.scr
# event| NA
# description| 
#====================================================================
<--start
CHECKSUM="0f0c2c5398bbb59b03a1a915d9ddc40fc263d145ee48ce2ad76d83ad0e75107f"
TRACE ON
IF (REPEXISTS("CUST") == 0) THEN
#{
	CREATEREP("CUST")
#}
ENDIF

IF (CLASSEXISTS("CUST","INTERFACE") == 0) THEN
#{
	CREATECLASS ("CUST","INTERFACE",5)
#}
ENDIF

IF (CLASSEXISTS("CUST","UBP") == 0) THEN
#{
	CREATECLASS ("CUST","UBP",5)
#}
ENDIF

sv_a = urhk_b2k_printRepos("BANCS")

CUST.UBP.errMsg = ""

print(BANCS.INPUT.controlnum)

IF (BANCS.INPUT.controlnum!="") THEN

	#BANCS.INPUT.applcntNo1="~ ~ ~"
	BANCS.INPUT.applcntNo1="%"
	#CUST.UBP.control=BANCS.INPUT.controlnum + BANCS.INPUT.applcntNo1
	CUST.UBP.control=BANCS.INPUT.applcntNo1 + BANCS.INPUT.controlnum + BANCS.INPUT.applcntNo1
	#CUST.UBP.control=BANCS.INPUT.controlnum
	print(CUST.UBP.control)
	sv_u = "count|select count(*) from tbaadm.pord where RECVR_BIC='TARATZTZXXX' and REMIT_INFO like ?SVAR and DELETE_PYMT='N' and bank_id=?SVAR"
	BANCS.INPARAM.BINDVARS = CUST.UBP.control + "|" + BANCS.STDIN.contextBankId
	print(BANCS.INPARAM.BINDVARS)
	print(sv_u)

	sv_r =  urhk_dbSelectWithBind(sv_u)
	print(sv_r)

	IF (sv_r ==0) THEN
	#{
		CUST.UBP.count = TRIM(BANCS.OUTPARAM.count)
		print (CUST.UBP.count)
		IF (CUST.UBP.count >=1) THEN
		#{
			sv_u = "CONTROL NUMBER ALREADY USED"
			sv_g = "MSG1|"+sv_u
			sv_i = urhk_SetOrbOut(sv_g)

		ELSE
			sv_u = "OK"
			sv_g = "MSG1|"+sv_u
			sv_i = urhk_SetOrbOut(sv_g)

			
		#}
		ENDIF

	#}
	ELSE
	#{
		CUST.UBP.count = " "
	#}
	ENDIF

ENDIF

END-->