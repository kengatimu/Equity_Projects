#-------------------------------------------------------------------------------------------------------
# Source Name           :   bwyormdp001.scr
# Date                  :   18-03-2013
# Author                :   chandrasekhar
# Title                 :   E-BANKING SWIFT TABLE MAINTAINENECE
# Description           :   validates the srlnum entered for  CBWYORM menu
# Sources called        :   NA
# Sources called from   :   cbwyorm_crit_ginc.jsp
# RS Approach Document  :
# Modification History  :
# <Serial No.>      <Date>          <Author Name>       <Description>
# 0.00a             18-03-2013     chandrasekhar        Original version
#
#-------------------------------------------------------------------------------------------------------
<--START
CHECKSUM="748c34026ab84488b4e017dbbc88125f9286fa339cdfc0af2e6eb8245b0075b2"
	TRACE ON
	
	#==========================================================*
	# CREATING NEW REPOSITORY AND CLASS
	#==========================================================*
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST", "DATA") == 0) THEN
	#{
		CREATECLASS("CUST", "DATA", 5)
	#}
	ENDIF
	
	
	print(BANCS.INPUT.serial)
	CUST.DATA.serial=BANCS.INPUT.serial
	
	sv_s = "Count|SELECT count(*) "
	sv_s = sv_s + " FROM CUSTOM.CREM "
	sv_s = sv_s + " WHERE bway_sequenceno = ?SVAR "
	sv_s = sv_s	+ " AND bank_id = ?SVAR"
	
	print(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.DATA.serial + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	IF (sv_d==0) THEN
	#{
		CUST.DATA.Count = BANCS.OUTPARAM.Count
		print(CUST.DATA.Count)
	#}
	ENDIF
	IF(CUST.DATA.Count==0) THEN
	#{
		
		sv_z = urhk_SetOrbOut("stat|Enter a valid serial number")
		
	#}
	ENDIF
	
	EXITSCRIPT
	TRACE OFF
END-->
