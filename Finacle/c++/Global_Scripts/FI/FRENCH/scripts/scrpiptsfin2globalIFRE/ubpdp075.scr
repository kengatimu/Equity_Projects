#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: ubpdp075.scr
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START
CHECKSUM="887a81dc02fb6b8cf02c3adf2aa044d0cd80706746528d159ef479e4f51b66d4"
	# Script body goes here
	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp056.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called from hoccm_crit_link.js for fetching biller Account Id
	#								details if the account id belongs to any biller.
	#
	# Called Script         :
	# Calling Function      :		function fnFetchUbpDetCrAcctNum
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           16-09-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	
	#========================================================
	# Check whether CUST Repository exists else create it
	#========================================================
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	#========================================================
	# Check whether UBP Class exists else create it
	#========================================================
	sv_r = CLASSEXISTS("CUST","UBP")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","UBP",5)
	#}
	ENDIF
	#========================================================
	# Initialize the variables
	#========================================================
	
	CUST.UBP.billerId = " "
	CUST.UBP.billAcctId = " "
	CUST.UBP.errorFlg="N"
	CUST.UBP.errorMsg=" "
	CUST.UBP.valType=" "
	CUST.UBP.ubpCounter="N"
	CUST.UBP.billerId=" "
	IF(FIELDEXISTS(BANCS.INPUT.critCrAcctNumUbp))THEN
	#{
		IF(TRIM(BANCS.INPUT.critCrAcctNumUbp) != "")THEN
		#{
			CUST.UBP.critCrAcctNumUbp = TOUPPER(TRIM(BANCS.INPUT.critCrAcctNumUbp))
			PRINT(CUST.UBP.critCrAcctNumUbp)
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.errorFlg = "Y"
		CUST.UBP.errorMsg = "Unable to get CR Account Number"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	#========================================================
	# Fetching tthe values from BRGT table and populating
	# the searcher list
	#========================================================
	BANCS.INPARAM.BINDVARS = CUST.UBP.critCrAcctNumUbp+"|Y|N|"+BANCS.STDIN.contextBankId+"|"+BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_s = "count1|"
	sv_s = sv_s + " SELECT count(1)"
	sv_s = sv_s + " FROM TBAADM.BRGT "
	sv_s = sv_s + " WHERE FREE_TEXT1=?SVAR"
	sv_s = sv_s + " AND NVL(ENTITY_CRE_FLG, 'Y') = ?SVAR"
	sv_s = sv_s + " AND NVL(DEL_FLG, 'N') = ?SVAR"
	sv_s = sv_s + " AND FREE_TEXT1 IS NOT NULL"
	sv_s = sv_s + " AND FREE_TEXT2 IS NOT NULL"
	sv_s = sv_s + " AND FREE_TEXT4 IS NOT NULL"
	sv_s = sv_s + " AND FREE_TEXT2 =?SVAR"
	sv_s = sv_s + " AND BANK_ID =?SVAR"
	PRINT(sv_s)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d != 0)THEN
	#{
		#Error Handling
		
		CUST.UBP.errorFlg = "Y"
		#CUST.UBP.errorMsg = "Enter a valid Biller Id"
		CUST.UBP.errorCode = "UBP_ERR001"
		CALL("ubpdp044.scr")
		CUST.UBP.errorMsg =CUST.UBP.errorDesc
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		CUST.UBP.count1 = BANCS.OUTPARAM.count1
		PRINT(CUST.UBP.count1)
		IF(CUST.UBP.count1==0)THEN
		#{
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		CUST.UBP.ubpCounter="Y"
	#}
	ENDIF
	#========================================================
	# Fetching tthe values from BRGT table and populating
	# the searcher list
	#========================================================
	BANCS.INPARAM.BINDVARS = CUST.UBP.critCrAcctNumUbp+"|Y|N|"+BANCS.STDIN.contextBankId+"|"+BANCS.STDIN.contextBankId
	sv_s = "billerId,billAcctId,bankId,chgEvetId,valType|"
	sv_s = sv_s + " SELECT BILLER_ID,FREE_TEXT1,FREE_TEXT2,FREE_TEXT3,FREE_TEXT4"
	sv_s = sv_s + " FROM TBAADM.BRGT "
	sv_s = sv_s + " WHERE FREE_TEXT1=?SVAR"
	sv_s = sv_s + " AND NVL(ENTITY_CRE_FLG, 'Y') = ?SVAR"
	sv_s = sv_s + " AND NVL(DEL_FLG, 'N') = ?SVAR"
	sv_s = sv_s + " AND FREE_TEXT1 IS NOT NULL"
	sv_s = sv_s + " AND FREE_TEXT2 IS NOT NULL"
	sv_s = sv_s + " AND FREE_TEXT4 IS NOT NULL"
	sv_s = sv_s + " AND FREE_TEXT2 =?SVAR"
	sv_s = sv_s + " AND BANK_ID =?SVAR AND rownum =1"
	PRINT(sv_s)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d != 0)THEN
	#{
		#Error Handling
		
		CUST.UBP.errorFlg = "Y"
		#CUST.UBP.errorMsg = "Enter a valid Biller Id"
		CUST.UBP.errorCode = "UBP_ERR001"
		CALL("ubpdp044.scr")
		CUST.UBP.errorMsg =CUST.UBP.errorDesc
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		CUST.UBP.billerId = BANCS.OUTPARAM.billerId
		PRINT(CUST.UBP.billerId)
		CUST.UBP.billAcctId = BANCS.OUTPARAM.billAcctId
		PRINT(CUST.UBP.billAcctId)
		CUST.UBP.bankId = BANCS.OUTPARAM.bankId
		PRINT(CUST.UBP.bankId)
		CUST.UBP.chgEvetId = BANCS.OUTPARAM.chgEvetId
		PRINT(CUST.UBP.chgEvetId)
		CUST.UBP.valType = BANCS.OUTPARAM.valType
		PRINT(CUST.UBP.valType)
	#}
	ENDIF
	
	
	ENDOFSCRIPT:
		sv_s = "errorFlg|" + CUST.UBP.errorFlg
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "errorMsg|" + CUST.UBP.errorMsg
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "critCrAcctNum|" + CUST.UBP.billAcctId
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "valType|" + CUST.UBP.valType
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "ubpCounter|" + CUST.UBP.ubpCounter
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "billerId|" + CUST.UBP.billerId
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
END-->
