#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: Actual_outstanding_accs_custval.scr
# Type: NA|default
# Description: Actual_outstanding_accs_custval.scr
# Author:
# Date:
# Modification Log:
#============================================================

<--START
CHECKSUM="d9bbd61aa41a0a5ee98972a0270938cf077546d2f3e3be584dec53a23283a897"
	TRACE ON
	
	PRINT(BANCS.FINRPTDEF.SOL_ID)
	PRINT(BANCS.FINRPTDEF.CREATE_DATE)
	PRINT(BANCS.FINRPTDEF.END_DATE)
	
	IF(BANCS.FINRPTDEF.SOL_ID == "") THEN
	#{
		sv_u = Urhk_FINRPT_SetErr("SOL_ID|SOL_ID must be entered")
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		IF(BANCS.FINRPTDEF.SOL_ID != "ALL") THEN
		#{
			sv_u = urhk_B2k_valSolId(BANCS.FINRPTDEF.SOL_ID)
			IF(sv_u != 0) THEN
			#{
				sv_u = Urhk_FINRPT_SetErr("SOL_ID|Invalid SOL_ID")
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			IF(BANCS.STDIN.homeSolId != BANCS.FINRPTDEF.SOL_ID) THEN
			#{
				IF(BANCS.STDIN.homeSolId != "000") THEN
				#{
					sv_u = Urhk_FINRPT_SetErr("SOL_ID|Report can be generated only for  users Home Sol ID")
					GOTO ENDOFSCRIPT
				#}
				ENDIF
			#}
			ENDIF
		#}
		ENDIF
		IF(BANCS.STDIN.homeSolId != "000") THEN
		#{
			IF(BANCS.FINRPTDEF.SOL_ID == "ALL") THEN
			#{
				sv_u = Urhk_FINRPT_SetErr("SOL_ID|Report can be generated only for users Sol ID")
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			
		#}
		ENDIF
		
	#}
	ENDIF
	
	IF(BANCS.FINRPTDEF.CREATE_DATE != "") THEN
	#{
		sv_u = urhk_B2k_valDate(BANCS.FINRPTDEF.CREATE_DATE)
		IF(sv_u !=0) THEN
		#{
			sv_u = Urhk_FINRPT_SetErr("CREATE_DATE|Invalid CREATE_DATE")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	IF(BANCS.FINRPTDEF.END_DATE != "") THEN
	#{
		sv_u = urhk_B2k_valDate(BANCS.FINRPTDEF.END_DATE)
		IF(sv_u !=0) THEN
		#{
			sv_u = Urhk_FINRPT_SetErr("END_DATE|Invalid END_DATE")
			GOTO ENDOFSCRIPT
			#}
		#}
		ENDIF
		
		
		
		ENDOFSCRIPT:
			
			TRACE OFF
	END-->
	
