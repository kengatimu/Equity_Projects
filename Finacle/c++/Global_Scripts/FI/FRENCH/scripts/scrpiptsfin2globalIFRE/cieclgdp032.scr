##########################################################################################
# Source Name           : cieclgdp029.scr
# Date                  : 04-Mar-2013
# Description           : show Sort Code/Bank Code/Branch Code
# Author                : Mohan Raghu Arudi
# Modification History  :
# <Serial No.>  <Date>        <Author Name>             <Description>
# ------------  -----------   -----------------         --------------
#      1        04-Mar-2013   Mohan Raghu Arudi         Base Version
###########################################################################################

<--START
CHECKSUM="4a294fd702bc348a13ab20b0660eb3c6e7cdd79aba4b0eff93e60a3dfe704330"
	TRACE ON
	
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CIECLG Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CIECLG")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CIECLG",5)
	#}
	ENDIF
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	
	CUST.CIECLG.instrumentBankCode = " "
	CUST.CIECLG.instrumentBranchCode = " "
	CUST.CIECLG.instrumentSortCode = " "
	CUST.CIECLG.instrumentEntity = " "
	CUST.CIECLG.shortName = " "
	CUST.CIECLG.instrumentBranchName = " "
	CUST.CIECLG.delFlg = " "
	
	CUST.CIECLG.errorFlg = "N"
	CUST.CIECLG.errorMsg = " "
	
	IF(FIELDEXISTS(BANCS.INPUT.instrumentBankCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.instrumentBankCode) != "")THEN
		#{
			CUST.CIECLG.instrumentBankCode = BANCS.INPUT.instrumentBankCode
			print(CUST.CIECLG.instrumentBankCode)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.instrumentBranchCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.instrumentBranchCode) != "")THEN
		#{
			CUST.CIECLG.instrumentBranchCode = BANCS.INPUT.instrumentBranchCode
			print(CUST.CIECLG.instrumentBranchCode)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.instrumentSortCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.instrumentSortCode) != "")THEN
		#{
			CUST.CIECLG.instrumentSortCode = BANCS.INPUT.instrumentSortCode
			print(CUST.CIECLG.instrumentSortCode)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.instrumentEntity))THEN
	#{
		IF(TRIM(BANCS.INPUT.instrumentEntity) != "")THEN
		#{
			CUST.CIECLG.instrumentEntity = BANCS.INPUT.instrumentEntity
			print(CUST.CIECLG.instrumentEntity)
		#}
		ENDIF
	#}
	ENDIF
	
	BANCS.INPARAM.BINDVARS = CUST.CIECLG.instrumentEntity + "|" + CUST.CIECLG.instrumentSortCode
	print(BANCS.INPARAM.BINDVARS)
	
	sv_s = "instrumentBankCode,instrumentBranchCode,shortName,instrumentBranchName,instrumentSortCode,delFlg|"
	#sv_s = sv_s + " SELECT BANK_CODE,BR_CODE,REPLACE(NVL(BR_SHORT_NAME,'-'),'''','`'),REPLACE(NVL(BR_NAME,'-'),'''','`'),CONCAT(CONCAT(MICR_CENTRE_CODE,MICR_BANK_CODE),MICR_BRANCH_CODE),DEL_FLG"
	sv_s = sv_s + " SELECT BANK_CODE,BR_CODE,NVL(BR_SHORT_NAME,'-'),NVL(BR_NAME,'-'),CONCAT(CONCAT(MICR_CENTRE_CODE,MICR_BANK_CODE),MICR_BRANCH_CODE),DEL_FLG"
	sv_s = sv_s + " FROM TBAADM.TBA_BRANCH_CODE_TBL "
	sv_s = sv_s + " WHERE TBA_BRANCH_CODE_TBL.BANK_ID = ?SVAR"
	sv_s = sv_s + " AND  LOCAL_CLG_FLG = 'Y' "
	sv_s = sv_s + " AND BR_CODE >= '!' "
	sv_s = sv_s + " AND CONCAT(CONCAT(MICR_CENTRE_CODE,MICR_BANK_CODE),MICR_BRANCH_CODE) = ?SVAR"
	print(sv_s)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	IF(sv_d != 0)THEN
	#{
		CUST.CIECLG.errorFlg = "Y"
		CUST.CIECLG.errorMsg = "Invalid Sort Code"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	CUST.CIECLG.instrumentBankCode = BANCS.OUTPARAM.instrumentBankCode
	print(CUST.CIECLG.instrumentBankCode)
	
	CUST.CIECLG.instrumentBranchCode = BANCS.OUTPARAM.instrumentBranchCode
	print(CUST.CIECLG.instrumentBranchCode)
	
	CUST.CIECLG.shortName = BANCS.OUTPARAM.shortName
	print(CUST.CIECLG.shortName)
	
	CUST.CIECLG.instrumentBranchName = BANCS.OUTPARAM.instrumentBranchName
	print(CUST.CIECLG.instrumentBranchName)
	
	CUST.CIECLG.instrumentSortCode = BANCS.OUTPARAM.instrumentSortCode
	print(CUST.CIECLG.instrumentSortCode)
	
	CUST.CIECLG.delFlg = BANCS.OUTPARAM.delFlg
	print(CUST.CIECLG.delFlg)
	
	ENDOFSCRIPT:
		
		sv_s = "errorFlg|" + CUST.CIECLG.errorFlg
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_s = "errorMsg|" + CUST.CIECLG.errorMsg
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_s = "instrumentSortCode|" + CUST.CIECLG.instrumentSortCode
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_s = "instrumentBankCode|" + CUST.CIECLG.instrumentBankCode
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_s = "instrumentBranchCode|" + CUST.CIECLG.instrumentBranchCode
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_s = "shortName|" + CUST.CIECLG.shortName
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_s = "instrumentBranchName|" + CUST.CIECLG.instrumentBranchName
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_s = "delFlg|" + CUST.CIECLG.delFlg
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
END-->
