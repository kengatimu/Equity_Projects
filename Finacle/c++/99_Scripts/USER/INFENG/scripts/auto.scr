	
	################################################################
	########################################################################################################
	#Name               :Userid.scr
	#Date               :01-12-2010
	#Author             :Suhasini Murthi
	#Description        :This script is used to fetch values from UPR table
	#Called script      :None
	#Calling Script     :None
	#Input              :User ID
	#Output             : userWorkClass,solId,userSolTenor,remoteAccessFlg,userApplName
	########################################################################################################
<--START
CHECKSUM="7220618180f8d2708fca097567452b7c6cc991e360f5edf75cfac8230e6389df"
	#trace on
	print ("MARION")
	
	####################################################################
	#section      :creating new repository if required
	#Description  :Checks for the existence of the repository STDWFS and
	#               creates a new one by that name if it does not exist
	#####################################################################
	
	IF (REPEXISTS("CUST") == 0 ) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	############################################################################
	#section      :creating new class if required
	#Desscription :checks for the existence of the class GLBDATA in the
	#repository
	#               create a new one if it does not exist
	##############################################################################
	
	
	IF (CLASSEXISTS("CUST","DATA") == 0) THEN
	#{
		CREATECLASS("CUST","DATA",5)
		#}
	ELSE
		#{
		DELETECLASS("CUST","DATA")
		CREATECLASS("CUST","DATA",5)
	#}
	ENDIF
	
	############################################
	#Assigns the values to the variables
	############################################
	#sv_a = urhk_b2k_PrintRepos("BANCS")
	
	sv_y=0
	print("MARION")
	sv_a = urhk_GetChannelInfo("")
	print(sv_a)
	CUST.DATA.DccId =BANCS.OUTPARAM.DccId
	print(CUST.DATA.DccId)
	
	IF (FIELDEXISTS(BANCS.INPUT.natid) == 1) THEN
	#{
		CUST.DATA.natid=trim(BANCS.INPUT.natid)
		print(CUST.DATA.natid)
		sv_b = "foracid, acct_name, schm_code, clr_bal_amt|select foracid, acct_name, schm_code, clr_bal_amt from tbaadm.gam,tbaadm.cmg where NAT_ID_CARD_NUM = ?SVAR
		AND tbaadm.gam.cust_id=TBAADM.CMG.cust_id"
		print(sv_b)
		BANCS.INPARAM.BINDVARS = CUST.DATA.natid
		print(BANCS.INPARAM.BINDVARS)
		#sv_q= urhk_dbSelectWithBind(sv_b)
		#print(sv_q)
		#sv_b = BANCS.OUTPARAM.foracid
		#print(sv_b)
		sv_e = urhk_dbCursorOpenWithBind(sv_b)
		
		#IF( sv_e == 0 ) THEN
		sv_m = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		print(sv_m)
		sv_n = urhk_dbCursorFetch(sv_m)
	#}
	ENDIF
	IF (FIELDEXISTS(BANCS.INPUT.natid) == 1) THEN
	#{
		CUST.DATA.natid=trim(BANCS.INPUT.natid)
		print(CUST.DATA.natid)
		sv_b = "foracid, acct_name, schm_code, clr_bal_amt|select foracid, acct_name, schm_code, clr_bal_amt from tbaadm.gam,tbaadm.cmg where NAT_ID_CARD_NUM = ?SVAR
		AND tbaadm.gam.cust_id=TBAADM.CMG.cust_id"
		print(sv_b)
		BANCS.INPARAM.BINDVARS = CUST.DATA.natid
		print(BANCS.INPARAM.BINDVARS)
		#sv_q= urhk_dbSelectWithBind(sv_b)
		#print(sv_q)
		#sv_b = BANCS.OUTPARAM.foracid
		#print(sv_b)
		sv_e = urhk_dbCursorOpenWithBind(sv_b)
		
		#IF( sv_e == 0 ) THEN
		sv_m = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		print(sv_m)
		sv_n = urhk_dbCursorFetch(sv_m)
	#}
	ENDIF
	
	IF (FIELDEXISTS(BANCS.INPUT.foracid) == 1) THEN
	#{
		sv_b = BANCS.OUTPARAM.foracid
		print(sv_b)
		sv_b = "foracid, acct_name, schm_code, clr_bal_amt|select foracid, acct_name, schm_code, clr_bal_amt from tbaadm.gam,tbaadm.cmg where foracid = ?SVAR
		AND tbaadm.gam.cust_id=TBAADM.CMG.cust_id"
		print(sv_b)
		#BANCS.INPARAM.BINDVARS = CUST.DATA.foracid
		#print(BANCS.INPARAM.BINDVARS)
		#sv_q= urhk_dbSelectWithBind(sv_b)
		#print(sv_q)
		#sv_b = BANCS.OUTPARAM.foracid
		#print(sv_b)
		sv_e = urhk_dbCursorOpenWithBind(sv_b)
		
		#IF( sv_e == 0 ) THEN
		sv_m = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		print(sv_m)
		sv_n = urhk_dbCursorFetch(sv_m)
		#sv_q= urhk_dbSelectWithBind(sv_b)
		#sv_n= urhk_dbSelectWithBind(sv_b)
		
		#sv_b = BANCS.OUTPARAM.foracid
		#print(sv_b)
		
		
		
	#}
	ENDIF
	
	
	
	sv_f = 0
	CUST.DATA.number = "0"
	WHILE(sv_n == 0)
	#{
		sv_b = BANCS.OUTPARAM.foracid
		sv_c = BANCS.OUTPARAM.acct_name
		sv_d = BANCS.OUTPARAM.schm_code
		sv_g = BANCS.OUTPARAM.clr_bal_amt
		IF (sv_b != "") THEN
		#{
			sv_p = "ACCTLISTStruct.<rec_" + CUST.DATA.number + ">.Acct.AcctId|" + sv_b
			print(sv_p)
			sv_u = urhk_SetOrbOut(sv_p)
			
		#}
		ENDIF
		IF (sv_c != "") THEN
		#{
			sv_p = "ACCTLISTStruct.<rec_" + CUST.DATA.number + ">.Acct.AcctName|" + sv_c
			print(sv_p)
			sv_u = urhk_SetOrbOut(sv_p)
			
		#}
		ENDIF
		IF (CUST.DATA.DccId == "CAS")THEN
		#{
			IF (sv_d != "") THEN
			#{
				sv_p = "ACCTLISTStruct.<rec_" + CUST.DATA.number + ">.Acct.SchmCode|" + sv_d
				print(sv_p)
				sv_u = urhk_SetOrbOut(sv_p)
			#}
			ENDIF
		#}
		ENDIF
		IF (CUST.DATA.DccId == "CAS")THEN
		#{
			IF (sv_d != "") THEN
			#{
				sv_p = "ACCTLISTStruct.<rec_" + CUST.DATA.number + ">.Acct.ClearBalance|" + sv_g
				print(sv_p)
				sv_u = urhk_SetOrbOut(sv_p)
			#}
			ENDIF
			
		#}
		ENDIF
		sv_n = urhk_dbCursorFetch(sv_m)
		sv_f = sv_f + 1
		CUST.DATA.number = sv_f
		print(CUST.DATA.number)
		
	#}
	DO
	sv_n = urhk_dbCursorClose(sv_m)
	
	exitscript
	
	# #CUST.DATA.clearBalance = BANCS.OUTPARAM.clearBalance
	# IF (CUST.DATA.clearBalance < 0) THEN
	#{
	#    CUST.DATA.ACCTBALCRDRIND = "D"
	#}
	#ENDIF
	#IF (CUST.DATA.clearBalance > 0) THEN
	#{
	#   CUST.DATA.ACCTBALCRDRIND = "C"
	#}
	#ENDIF
	sv_o = urhk_getAcctDetailsInRepository(sv_b)
	CUST.DATA.schm_code=BANCS.OUTPARAM.schmCode
	print(CUST.DATA.schm_code)
	CUST.DATA.schm_type=BANCS.OUTPARAM.schmType
	print(CUST.DATA.schm_type)
	CUST.DATA.acctName=BANCS.OUTPARAM.acctName
	print(CUST.DATA.acctName)
	CUST.DATA.crncy_code=BANCS.OUTPARAM.acctCrncyCode
	print(CUST.DATA.crncy_code)
	CUST.DATA.frez_code=BANCS.OUTPARAM.freezeCode
	print(CUST.DATA.frez_code)
	CUST.DATA.clr_bal_amt=BANCS.OUTPARAM.clearBalance
	print(BANCS.OUTPARAM.AcctId)
	CUST.DATA.foracid=BANCS.OUTPARAM.AcctId
	#CUST.DATA.foracid=BANCS.OUTPARAM.foracid
	#print(CUST.DATA.foracid)
	CUST.DATA.cust_id=BANCS.OUTPARAM.custId
	CUST.DATA.OWNERSHIP=BANCS.OUTPARAM.acctOwnerShip
	sv_x = "acid,ACCT_STATUS| SELECT  tbaadm.gam.acid , tbaadm.smt.acct_status FROM tbaadm.gam,tbaadm.SMT
	WHERE tbaadm.gam.foracid = ?SVAR
	AND tbaadm.gam.acct_cls_flg !='Y' AND tbaadm.gam.ACID=tbaadm.SMT.ACID "
	print(sv_x)
	BANCS.INPARAM.BINDVARS = CUST.DATA.foracid
	#+ "|" + BANCS.STDIN.contextBankId
	sv_p= urhk_dbSelectWithBind(sv_x)
	print(sv_p)
	
	CUST.DATA.acid=BANCS.OUTPARAM.acid
	CUST.DATA.ACCT_STATUS=BANCS.OUTPARAM.ACCT_STATUS
	
	
	
	EXITSCRIPT
	
	
	#trace off
END-->
