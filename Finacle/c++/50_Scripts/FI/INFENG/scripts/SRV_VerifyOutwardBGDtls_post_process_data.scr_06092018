<--start
	TRACE ON
	IF(FIELDEXISTS(BANCS.STDIN.menuId))THEN
	#{
		IF(BANCS.STDIN.menuId == "OGM")THEN
		#{
			#Additional charges are collected after disbursment
			CALL("Eprocurementnot.scr")
			
		#}
		ENDIF
	#}
	ENDIF
	
	EXITSCRIPT
END-->
