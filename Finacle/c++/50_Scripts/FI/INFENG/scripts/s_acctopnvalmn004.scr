#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: s_acctopnvalmn004.scr
# Type: NA|default
# Description: s_acctopnvalmn004.scr
# Author:
# Date:
# Modification Log:
#============================================================
#------------------------------------------------------------------------------------------------
#    Name			 : s_acctopnvalmn004.scr
#    Description     : Validating the fields
#    Date            : 15-03-2013
#    Author          : Farhan
#    Input           : NA
#    Output			 : NA
#    Called Script	 : NA
#    Calling Script	 : NA
#    Menu Option	 : HOAACLA
#    Srl. No			Date			Author			Description.
#    ------			----------			-------			---------------
#    1.0			15-03-2013			Farhan			Original Version
#-----------------------------------------------------------------------------------------------
<--START
CHECKSUM="3c3638704a6440d986219faacfcbaf84ba11dd4584f147df1b66633156a232a6"
	TRACE ON
	
	#-----------------------------------------------------------------
	#Creating repository and class
	#-----------------------------------------------------------------
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST", "ACCTOPEN") == 0) THEN
	#{
		CREATECLASS("CUST", "ACCTOPEN", 5)
	#}
	ENDIF
	
	#---------------------------------------------------------------------------
	# Checking that some filters have been enabled
	#---------------------------------------------------------------------------
	
	CUST.ACCTOPEN.errMsg     	= ""
	CUST.ACCTOPEN.schmcode12  	= ""
	CUST.ACCTOPEN.schmcode13  	= ""
	CUST.ACCTOPEN.schmcode14 	= ""
	CUST.ACCTOPEN.schmcode15 	= ""
	CUST.ACCTOPEN.borrcode   	= ""
	CUST.ACCTOPEN.purpadv    	= ""
	CUST.ACCTOPEN.schmCode   	= ""
	CUST.ACCTOPEN.schmType   	= ""
	CUST.ACCTOPEN.Period     	= ""
	CUST.ACCTOPEN.visitFlg  	= ""
	CUST.ACCTOPEN.custId	    = ""
	CUST.ACCTOPEN.nationalidnum	= ""
	CUST.ACCTOPEN.amt		= ""
	CUST.ACCTOPEN.Period 	= ""
	CUST.ACCTOPEN.borrcatcode	= ""
	BANCS.INPUT.moduleName 	= "ACCOUNT_OPENING"
	
	BANCS.INPUT.parameterId = "SCHM_COD12"
	CALL("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.ACCTOPEN.schmcode12 = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		CUST.ACCTOPEN.errMsg = "Scheme code12 setup not done in CPARAM"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	BANCS.INPUT.parameterId = "SCHM_COD13"
	CALL("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.ACCTOPEN.schmcode13 = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		CUST.ACCTOPEN.errMsg = "Scheme code 13 setup not done in CPARAM"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	BANCS.INPUT.parameterId = "SCHM_COD14"
	CALL("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.ACCTOPEN.schmcode14 = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		CUST.ACCTOPEN.errMsg = "Scheme code 14 setup not done in CPARAM"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	BANCS.INPUT.parameterId = "SCHM_COD15"
	CALL("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.ACCTOPEN.schmcode15 = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		CUST.ACCTOPEN.errMsg = "Scheme code 15 setup not done in CPARAM"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	BANCS.INPUT.parameterId = "STAT_FLG"
	CALL("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.ACCTOPEN.statFlg = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		CUST.ACCTOPEN.errMsg = "Status Flg setup not done in CPARAM"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	
	BANCS.INPUT.parameterId = "BORR_CODE"
	CALL("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.ACCTOPEN.borrcode = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		CUST.ACCTOPEN.errMsg = "Borrower category code setup not done in CPARAM"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	BANCS.INPUT.parameterId = "PURP_ADV"
	CALL("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.ACCTOPEN.purpadv = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		CUST.ACCTOPEN.errMsg = "Purpose of advance setup not done in CPARAM"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_u = urhk_SRV_GetVal("acctCommonInfo.acctGeneralInfo.acctBasic.schmCode.schmCode")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
		CUST.ACCTOPEN.schmCode = TOUPPER(BANCS.OUTPARAM.srvValue)
		PRINT(CUST.ACCTOPEN.schmCode)
	#}
	ENDIF
	
	CUST.ACCTOPEN.schmCode = TRIM(CUST.ACCTOPEN.schmCode)
	PRINT(CUST.ACCTOPEN.schmCode)

	sv_u = urhk_SRV_GetVal("acctCommonInfo.acctGeneralInfo.acctBasic.schmCode.schmType")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
		CUST.ACCTOPEN.schmType = TOUPPER(BANCS.OUTPARAM.srvValue)
		PRINT(CUST.ACCTOPEN.schmType)
	#}
	ENDIF
	
	CUST.ACCTOPEN.schmType = TRIM(CUST.ACCTOPEN.schmType)
	PRINT(CUST.ACCTOPEN.schmType)
	
	IF (CUST.ACCTOPEN.schmCode != "") THEN
	#{
		sv_s = "schmtype|SELECT schm_type "
		sv_s = sv_s + "FROM TBAADM.GSP "
		sv_s = sv_s + "WHERE  del_flg ='N' "
		sv_s = sv_s + "AND schm_code=?SVAR "
		sv_s = sv_s + "AND bank_id = ?SVAR "
		BANCS.INPARAM.BINDVARS = CUST.ACCTOPEN.schmCode + "|"  + BANCS.STDIN.contextBankId
		
		sv_d =  urhk_dbSelectWithBind(sv_s)
		IF (sv_d == 0) THEN
		#{
			sv_j = BANCS.OUTPARAM.schmtype
			IF (sv_j == "LAA") THEN
			#{
				sv_a = urhk_SRV_GetVal("acctCommonInfo.acctClassMISC.freeText1")
				sv_a = BANCS.OUTPARAM.srvValue
				PRINT(sv_a)
				
				IF (sv_a == "") THEN
				#{
					sv_u = urhk_SRV_setErr("acctCommonInfo.acctClassMISC.freeText1|Enter the number of Partners/Lives in Free Text 1 IF Partnership")
					BANCS.OUTPUT.successOrFailure="F"
					GOTO ENDOFSCRIPT
					#}
				ELSE
					#{
					sv_r = urhk_B2k_isStringNumeric(sv_a)
					IF (sv_r == 1) THEN
					#{
						sv_u = urhk_SRV_setErr("acctCommonInfo.acctClassMISC.freeText1|Only Numerics allowed in Free Text 1")
						BANCS.OUTPUT.successOrFailure="F"
						GOTO ENDOFSCRIPT
					#}
					ENDIF
				#}
				ENDIF
				
				sv_a = urhk_SRV_GetVal("acctCommonInfo.acctClassMISC.freeText2")
				sv_a = BANCS.OUTPARAM.srvValue
				PRINT(sv_a)
				IF (sv_a == "") THEN
				#{
					sv_u = urhk_SRV_setErr("acctCommonInfo.acctClassMISC.freeText2|Enter (Y-YES / N-NO) in Free Text 2 IF Ltd Liability Company by shares AND not by gurantee OR Society/College/School/Institute/Church loan")
					BANCS.OUTPUT.successOrFailure="F"
					GOTO ENDOFSCRIPT
					#}
				ELSE
					#{
					IF (( sv_a != "Y" ) AND (sv_a != "N")) THEN
					#{
						sv_u = urhk_SRV_setErr("acctCommonInfo.acctClassMISC.freeText2| Invalid Code . Valid codes Y-YES OR N-NO ")
						BANCS.OUTPUT.successOrFailure="F"
						GOTO ENDOFSCRIPT
					#}
					ENDIF
				#}
				ENDIF
				
				IF ((CUST.ACCTOPEN.schmCode == CUST.ACCTOPEN.schmcode12) OR (CUST.ACCTOPEN.schmCode == CUST.ACCTOPEN.schmcode13)) THEN
				#{
					sv_a = urhk_SRV_GetVal("acctCommonInfo.acctClassMISC.freeText3")
					sv_a = BANCS.OUTPARAM.srvValue
					PRINT(sv_a)
					IF (sv_a == "") THEN
					#{
						sv_u = urhk_SRV_setErr("acctCommonInfo.acctClassMISC.freeText3| Enter Insuarance Value of the property in Free Text 3")
						BANCS.OUTPUT.successOrFailure="F"
						GOTO ENDOFSCRIPT
						#}
					ELSE
						#{
						sv_r = urhk_B2k_isStringNumeric(sv_a)
						IF ( sv_r == 1) THEN
						#{
							sv_u = urhk_SRV_setErr("acctCommonInfo.acctClassMISC.freeText3| Only Numerics allowed in Free Text 3")
							BANCS.OUTPUT.successOrFailure="F"
							GOTO ENDOFSCRIPT
						#}
						ENDIF
					#}
					ENDIF
				#}
				ENDIF
			#}
			ENDIF
		#}
		ENDIF
		#}
	ELSE
		#{
		sv_j = ""
	#}
	ENDIF

	print(CUST.ACCTOPEN.schmType)

	IF ((CUST.ACCTOPEN.schmType == "SBA") OR (CUST.ACCTOPEN.schmType == "CAA") OR (CUST.ACCTOPEN.schmType == "TDA")) THEN
	#{
		sv_a = urhk_SRV_GetVal("acctCommonInfo.acctClassMISC.freeCode3.refCode")
		sv_a = BANCS.OUTPARAM.srvValue
		PRINT(sv_a)
		CUST.ACCTOPEN.amt = sv_a
		IF (sv_a == "") THEN
		#{
			sv_u = urhk_SRV_setErr("acctCommonInfo.acctClassMISC.freeCode3.refCode|Economic Subsector is Mandatory - Free Code 3")
			BANCS.OUTPUT.successOrFailure="F"
			GOTO ENDOFSCRIPT
			#}
		ENDIF
		sv_a = urhk_SRV_GetVal("acctCommonInfo.acctClassMISC.freeCode5.refCode")
		sv_a = BANCS.OUTPARAM.srvValue
		PRINT(sv_a)
		CUST.ACCTOPEN.amt = sv_a
		IF (sv_a == "") THEN
		#{
			sv_u = urhk_SRV_setErr("acctCommonInfo.acctClassMISC.freeCode5.refCode|The Economic Sector Code - ISIC is Mandatory - Free Code 5")
			BANCS.OUTPUT.successOrFailure="F"
			GOTO ENDOFSCRIPT
			#}
		ENDIF
		sv_a = urhk_SRV_GetVal("acctCommonInfo.acctClassMISC.freeCode7.refCode")
		sv_a = BANCS.OUTPARAM.srvValue
		PRINT(sv_a)
		CUST.ACCTOPEN.amt = sv_a
		IF (sv_a == "") THEN
		#{
			sv_u = urhk_SRV_setErr("acctCommonInfo.acctClassMISC.freeCode7.refCode|The Public Sector Code is Mandatory - Free Code 7")
			BANCS.OUTPUT.successOrFailure="F"
			GOTO ENDOFSCRIPT
			#}
		ENDIF
		sv_a = urhk_SRV_GetVal("acctCommonInfo.acctClassMISC.freeCode8.refCode")
		sv_a = BANCS.OUTPARAM.srvValue
		PRINT(sv_a)
		CUST.ACCTOPEN.amt = sv_a
		IF (sv_a == "") THEN
		#{
			sv_u = urhk_SRV_setErr("acctCommonInfo.acctClassMISC.freeCode8.refCode|The Institutional Sector Code is Mandatory - Free Code 8")
			BANCS.OUTPUT.successOrFailure="F"
			GOTO ENDOFSCRIPT
			#}
		ENDIF
	#}
	ENDIF
	
	IF (CUST.ACCTOPEN.schmCode == CUST.ACCTOPEN.schmcode14) THEN
	#{
		CUST.ACCTOPEN.visitFlg = "N"
		sv_a = urhk_SRV_GetVal("acctCommonInfo.acctClassMISC.freeText1")
		sv_a = BANCS.OUTPARAM.srvValue
		PRINT(sv_a)
		CUST.ACCTOPEN.amt = sv_a
		IF (sv_a == "") THEN
		#{
			sv_u = urhk_SRV_setErr("acctCommonInfo.acctClassMISC.freeText1|Installment Amount is mandatory - Free Text 1")
			CUST.ACCTOPEN.visitFlg = "N"
			BANCS.OUTPUT.successOrFailure="F"
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			sv_r = urhk_B2k_isStringNumeric(sv_a)
			IF (sv_r != "0") THEN
			#{
				sv_u = urhk_SRV_setErr("acctCommonInfo.acctClassMISC.freeText1|Invalid Amount - Free Text 1")
				CUST.ACCTOPEN.visitFlg = "N"
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		
		sv_u = urhk_SRV_GetVal("acctCommonInfo.acctClassMISC.freeText2")
		IF (sv_u == 0) THEN
		#{
			sv_a = TOUPPER(BANCS.OUTPARAM.srvValue)
			CUST.ACCTOPEN.visitFlg = "N"
		#}
		ENDIF
		
		CUST.ACCTOPEN.Period = TRIM(sv_a)
		
		IF (CUST.ACCTOPEN.Period == "") THEN
		#{
			sv_u = urhk_SRV_setErr("acctCommonInfo.acctClassMISC.freeText2|Installment Period(Months) is mandatory - Free Text 2")
			CUST.ACCTOPEN.visitFlg = "N"
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			sv_r = urhk_B2k_isStringNumeric(CUST.ACCTOPEN.Period)
			IF (sv_r != "0") THEN
			#{
				sv_u = urhk_SRV_setErr("acctCommonInfo.acctClassMISC.freeText2|Invalid Period - Free Text 2")
				CUST.ACCTOPEN.visitFlg = "N"
				BANCS.OUTPUT.successOrFailure="F"
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		
#########################################################james 28/06/2024#######################################

 		IF (BANCS.STDIN.moduleIdentity=="FI") THEN
 		#{
	 		CUST.ACCTOPEN.DccId=""
	 		sv_a = urhk_GetChannelInfo("")
 			print(sv_a)

 			IF (sv_a!=0) THEN
			#{
				CUST.ACCTOPEN.DccId = BANCS.OUTPARAM.DccId
				print(CUST.ACCTOPEN.DccId)
				IF((CUST.ACCTOPEN.DccId !="OMN")AND (CUST.ACCTOPEN.schmCode!="SB103"))THEN
				#{

					sv_o = LEFT$(BANCS.STDIN.BODDate,10)
		
					sv_a = (sv_o + "|" + CUST.ACCTOPEN.Period + "|" + "0")
					sv_u = urhk_B2k_Add_MonthsDays_To_date(sv_a)
					IF (sv_u == 0) THEN
					#{
						sv_l = BANCS.OUTPARAM.MODIFIED_DATE
						PRINT(sv_l)
						sv_l = LEFT$(sv_l,10)
						PRINT(sv_l)
						sv_a = urhk_SRV_SetVal("acctCommonInfo.acctClassMISC.freeText3|" + sv_l)
						PRINT(sv_a)
					#}
					ENDIF
				#}
				ENDIF
			#}
			ENDIF
		#}
		ELSE
		#{
			sv_o = LEFT$(BANCS.STDIN.BODDate,10)
		
			sv_a = (sv_o + "|" + CUST.ACCTOPEN.Period + "|" + "0")
			sv_u = urhk_B2k_Add_MonthsDays_To_date(sv_a)
			IF (sv_u == 0) THEN
			#{
				sv_l = BANCS.OUTPARAM.MODIFIED_DATE
				PRINT(sv_l)
				sv_l = LEFT$(sv_l,10)
				PRINT(sv_l)
				sv_a = urhk_SRV_SetVal("acctCommonInfo.acctClassMISC.freeText3|" + sv_l)
				PRINT(sv_a)
			#}
			ENDIF
		#}
		ENDIF
#########################################################james 28/06/2024#######################################
	#}
	ENDIF
	
	#-----------------------------------------------------------------
	# TCS safcom
	#------------------------------------------------------------------
	sv_u = urhk_SRV_GetVal("acctCommonInfo.acctGeneralInfo.acctBasic.schmCode.schmCode")
	IF (sv_u == 0) THEN
	#{
		sv_a = TOUPPER(BANCS.OUTPARAM.srvValue)
		PRINT(sv_a)
	#}
	ENDIF
	CUST.ACCTOPEN.schmCode = TRIM(sv_a)
	PRINT(CUST.ACCTOPEN.schmCode)
	
	IF (CUST.ACCTOPEN.schmCode == CUST.ACCTOPEN.schmcode15) THEN
	#{
		sv_u = urhk_SRV_GetVal("acctCommonInfo.acctGeneralInfo.acctBasic.parentCustId.custId")
		IF (sv_u == 0) THEN
		#{
			sv_a = TOUPPER(BANCS.OUTPARAM.srvValue)
			PRINT(sv_a)
		#}
		ENDIF
		
		CUST.ACCTOPEN.custId = sv_a
		sv_w = CUST.ACCTOPEN.custId
		PRINT (sv_w)
		
		sv_u = urhk_SRV_GetVal("acctCommonInfo.acctClassMISC.freeText12")
		IF (sv_u == 0) THEN
		#{
			sv_a = TOUPPER(BANCS.OUTPARAM.srvValue)
			PRINT(sv_a)
		#}
		ENDIF
		
		CUST.ACCTOPEN.freeText12 = sv_a
		PRINT(CUST.ACCTOPEN.freeText12)
		
		IF (CUST.ACCTOPEN.freeText12 == "") THEN
		#{
			sv_u = urhk_SRV_setErr("acctCommonInfo.acctClassMISC.freeText12|Please Enter Order Application no")
			BANCS.OUTPUT.successOrFailure="F"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		sv_s = "count|SELECT count(*) "
		sv_s = sv_s + "FROM CUSTOM.ONLINE_APPS "
		sv_s = sv_s + "WHERE STATUSFLAG = ?SVAR "
		sv_s = sv_s + "AND applicationid= ?SVAR "
		sv_s = sv_s + "AND bank_id = ?SVAR "
		BANCS.INPARAM.BINDVARS = CUST.ACCTOPEN.statFlg +"|"+ CUST.ACCTOPEN.freeText12 + "|"  + BANCS.STDIN.contextBankId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		CUST.ACCTOPEN.count = BANCS.OUTPARAM.count
		
		IF (CUST.ACCTOPEN.count < 1) THEN
		#{
			sv_u = urhk_SRV_setErr("acctCommonInfo.acctClassMISC.freeText12|The App does not exists.")
			BANCS.OUTPUT.successOrFailure="F"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		
		sv_s = "nationalidnum|SELECT NAT_ID_CARD_NUM "
		sv_s = sv_s + "FROM TBAADM.CMG "
		sv_s = sv_s + "WHERE cust_id = ?SVAR "
		sv_s = sv_s + "AND bank_id = ?SVAR "
		BANCS.INPARAM.BINDVARS = CUST.ACCTOPEN.custId + "|"  + BANCS.STDIN.contextBankId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF (sv_d == 0) THEN
		#{
			CUST.ACCTOPEN.nationalidnum = BANCS.OUTPARAM.nationalidnum
			PRINT(CUST.ACCTOPEN.nationalidnum)
		#}
		ENDIF
		
		sv_s = "applicationid,nationalidtsc,statusflag|SELECT applicationid,nationalid,statusflag "
		sv_s = sv_s + "FROM CUSTOM.ONLINE_APPS "
		sv_s = sv_s + "WHERE STATUSFLAG = ?SVAR "
		sv_s = sv_s + "AND applicationid= ?SVAR "
		sv_s = sv_s + "AND bank_id = ?SVAR "
		BANCS.INPARAM.BINDVARS = CUST.ACCTOPEN.statFlg +"|"+ CUST.ACCTOPEN.freeText12 + "|"  + BANCS.STDIN.contextBankId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF (sv_d == 0) THEN
		#{
			BANCS.INPARAM.nationalidtsc = BANCS.OUTPARAM.nationalidtsc
			PRINT(BANCS.INPARAM.nationalidtsc)
			
			BANCS.INPARAM.applicationid = TRIM(BANCS.OUTPARAM.applicationid)
			PRINT(BANCS.INPARAM.applicationid)
			
			BANCS.INPARAM.statusflag = BANCS.OUTPARAM.statusflag
			PRINT(BANCS.INPARAM.statusflag)
		#}
		ENDIF
		
		IF ((CUST.ACCTOPEN.freeText12 != BANCS.INPARAM.applicationid) OR (BANCS.INPARAM.nationalidtsc != CUST.ACCTOPEN.nationalidnum)) THEN
		#{
			sv_u = urhk_SRV_setErr("Wrong TSC Order Appli_no/NAT_ID")
			BANCS.OUTPUT.successOrFailure="F"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	ENDOFSCRIPT:
		IF (CUST.ACCTOPEN.errMsg != "") THEN
		#{
			sv_u = urhk_SRV_setErr(CUST.ACCTOPEN.errMsg)
		#}
		ENDIF
		TRACE OFF
		EXITSCRIPT
END-->
