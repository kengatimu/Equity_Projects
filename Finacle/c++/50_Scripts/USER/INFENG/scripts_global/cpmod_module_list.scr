##########################################################################################
#    Name                       :   cpmod_module_list.scr
#    Date                       :   31-03-2010
#    Description                :   This script provides listing of Modules
#    Input                      :   NA
#    Output                     :   NA
#    Author                     :   Pankaj gaur
#    Called From                :
#    Calling Script             :   NA
#    Reference		       	:
#    Modification History       :
# <Serial No>     <Date>      <Author>        	   <Description>
#   0001        06-01-2010  Pankaj Gaur      Initial Draft
##########################################################################################

<--START
CHECKSUM="3f7d67bc8a7deb6ec3abb1f8c0c5f60ecddad469ef6f7549553c945dcacd6acd"
	TRACE ON
	
	###########################################################################################
	# STEP : STEP 0 BEGIN
	# CREATE THE NECESSARY REPOSITORY AND CLASSES
	###########################################################################################
	
	IF(REPEXISTS("CUST") == 0 ) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF(CLASSEXISTS("CUST","DATA") == 0) THEN
	#{
		CREATECLASS("CUST","DATA",5)
	#}
	ENDIF
	
	CUST.DATA.recordCount	= ""
	BANCS.INPARAM.BINDVARS = ""
	
	################################################################################
	# STEP : STEP 0 END
	################################################################################
	
	################################################################################
	# STEP : STEP 1 BEGIN
	# Selecting the values of parameter from screen
	################################################################################
	
	CUST.DATA.module_id = ""
	CUST.DATA.module_desc = ""
	CUST.DATA.funcCode=BANCS.INPUT.funcCode
	print(CUST.DATA.funcCode)
	CUST.DATA.moduleId=BANCS.INPUT.moduleId
	print(CUST.DATA.moduleId)
	################################################################################
	#Initializing fields from front end to 'N'
	################################################################################
	
	################################################################################
	# STEP : STEP 1 END
	################################################################################
	
	################################################################################
	# STEP : STEP 2 BEGIN
	# Selecting the fields for module id listing
	################################################################################
	IF((CUST.DATA.funcCode=="I") OR (CUST.DATA.funcCode==""))THEN
	#{
		lv_s = "recordCount|"
		lv_s = lv_s + " SELECT count(*) "
		lv_s = lv_s + " FROM CUSTOM.MODULE_DETAILS "
		lv_s = lv_s + " WHERE BANK_ID = ?SVAR AND DEL_FLG='N'"
		lv_s = lv_s + " AND ENTITY_CRE_FLG='Y'"
		lv_s = lv_s + " AND MODULE_ID LIKE ?SVAR"
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + BANCS.INPUT.moduleId + "%"
		print(BANCS.INPARAM.BINDVARS)
		
		
		lv_d = urhk_dbSelectWithBind(lv_s)
		
		IF (lv_d != 0) THEN
		#{
			CUST.DATA.recordCount = "0"
			#}
		ELSE
			#{
			CUST.DATA.recordCount = BANCS.OUTPARAM.recordCount
		#}
		ENDIF
		CUST.DATA.recordCount = "recordCount|"+CUST.DATA.recordCount
		lv_u = urhk_SetOrbOut(CUST.DATA.recordCount)
		
		lv_s = "module_id,module_desc|"
		lv_s = lv_s + " SELECT MODULE_ID,MODULE_DESC "
		lv_s = lv_s + " FROM CUSTOM.MODULE_DETAILS "
		lv_s = lv_s + " WHERE BANK_ID = ?SVAR AND DEL_FLG='N'"
		lv_s = lv_s + " AND ENTITY_CRE_FLG='Y'"
		lv_s = lv_s + " AND MODULE_ID LIKE ?SVAR"
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + BANCS.INPUT.moduleId + "%"
		print(BANCS.INPARAM.BINDVARS)
		
	#}
	ENDIF
	
	IF(CUST.DATA.funcCode=="D") THEN
	#{
		lv_s = "recordCount|"
		lv_s = lv_s + " SELECT count(*) "
		lv_s = lv_s + " FROM CUSTOM.MODULE_DETAILS "
		lv_s = lv_s + " WHERE BANK_ID = ?SVAR AND DEL_FLG='N'"
		lv_s = lv_s + " AND ENTITY_CRE_FLG='Y'"
		lv_s = lv_s + " AND MODULE_ID LIKE ?SVAR"
		
		
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" +  BANCS.INPUT.moduleId + "%"
		print(BANCS.INPARAM.BINDVARS)
		
		lv_d = urhk_dbSelectWithBind(lv_s)
		
		
		IF (lv_d != 0) THEN
		#{
			CUST.DATA.recordCount = "0"
			#}
		ELSE
			#{
			CUST.DATA.recordCount = BANCS.OUTPARAM.recordCount
		#}
		ENDIF
		CUST.DATA.recordCount = "recordCount|"+CUST.DATA.recordCount
		lv_u = urhk_SetOrbOut(CUST.DATA.recordCount)
		
		lv_s = "module_id,module_desc|"
		lv_s = lv_s + " SELECT MODULE_ID,MODULE_DESC "
		lv_s = lv_s + " FROM CUSTOM.MODULE_DETAILS "
		lv_s = lv_s + " WHERE BANK_ID = ?SVAR AND DEL_FLG='N'"
		lv_s = lv_s + " AND ENTITY_CRE_FLG='Y'"
		lv_s = lv_s + " AND MODULE_ID LIKE ?SVAR"
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + BANCS.INPUT.moduleId + "%"
		print(BANCS.INPARAM.BINDVARS)
		
	#}
	ENDIF
	
	
	
	IF(CUST.DATA.funcCode=="V")THEN
	#{
		lv_s = "recordCount|"
		lv_s = lv_s + " SELECT count(*) "
		lv_s = lv_s + " FROM CUSTOM.MODULE_DETAILS "
		lv_s = lv_s + " WHERE BANK_ID = ?SVAR AND DEL_FLG='N'"
		lv_s = lv_s + " AND ENTITY_CRE_FLG='N'"
		lv_s = lv_s + " AND MODULE_ID LIKE ?SVAR"
		print(lv_s)
		
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" +  BANCS.INPUT.moduleId + "%"
		print(BANCS.INPARAM.BINDVARS)
		
		lv_d = urhk_dbSelectWithBind(lv_s)
		
		print(lv_d)
		IF (lv_d != 0) THEN
		#{
			CUST.DATA.recordCount = "0"
			#}
		ELSE
			#{
			CUST.DATA.recordCount = BANCS.OUTPARAM.recordCount
		#}
		ENDIF
		print(CUST.DATA.recordCount)
		lv_b = "recordCount1|"
		lv_b = lv_b + " SELECT count(*) "
		lv_b = lv_b + " FROM CUSTOM.MODULEDET_MOD "
		lv_b = lv_b + " WHERE BANK_ID = ?SVAR AND MODULE_ID LIKE ?SVAR"
		print(lv_b)
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" +  BANCS.INPUT.moduleId + "%"
		print(BANCS.INPARAM.BINDVARS)
		
		lv_e = urhk_dbSelectWithBind(lv_b)
		
		print(lv_e)
		IF (lv_e != 0) THEN
		#{
			CUST.DATA.recordCount1 = "0"
			#}
		ELSE
			#{
			CUST.DATA.recordCount1 = BANCS.OUTPARAM.recordCount1
		#}
		ENDIF
		print(CUST.DATA.recordCount)
		CUST.DATA.recordCount = CINT(CUST.DATA.recordCount) + CINT(CUST.DATA.recordCount1)
		print(CUST.DATA.recordCount)
		IF(CUST.DATA.recordCount != 0) THEN
		#{
			CUST.DATA.recordCount = "recordCount|"+CUST.DATA.recordCount
			lv_u = urhk_SetOrbOut(CUST.DATA.recordCount)
		#}
		ENDIF
		
		
		
		
		lv_s = "module_id,module_desc|"
		lv_s = lv_s + " SELECT MODULE_ID,MODULE_DESC "
		lv_s = lv_s + " FROM CUSTOM.MODULE_DETAILS "
		lv_s = lv_s + " WHERE BANK_ID = ?SVAR AND DEL_FLG='N'"
		lv_s = lv_s + " AND ENTITY_CRE_FLG='N'"
		lv_s = lv_s + " AND MODULE_ID LIKE ?SVAR"
		lv_s = lv_s + " UNION ALL"
		lv_s = lv_s + " SELECT MODULE_ID,MODULE_DESC "
		lv_s = lv_s + " FROM CUSTOM.MODULEDET_MOD "
		lv_s = lv_s + " WHERE BANK_ID = ?SVAR AND MODULE_ID LIKE ?SVAR"
		print(lv_s)
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + BANCS.INPUT.moduleId + "%" + "|" + BANCS.STDIN.contextBankId + "|" + BANCS.INPUT.moduleId + "%"
		print(BANCS.INPARAM.BINDVARS)
		
	#}
	ENDIF
	
	
	
	IF(CUST.DATA.funcCode=="X")THEN
	#{
		lv_s = "recordCount|"
		lv_s = lv_s + " SELECT count(*) "
		lv_s = lv_s + " FROM CUSTOM.MODULE_DETAILS "
		lv_s = lv_s + " WHERE BANK_ID = ?SVAR AND DEL_FLG='N'"
		lv_s = lv_s + " AND ENTITY_CRE_FLG='N'"
		lv_s = lv_s + " AND MODULE_ID LIKE ?SVAR"
		
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" +  BANCS.INPUT.moduleId + "%"
		print(BANCS.INPARAM.BINDVARS)
		
		lv_d = urhk_dbSelectWithBind(lv_s)
		
		
		IF (lv_d != 0) THEN
		#{
			CUST.DATA.recordCount = "0"
			#}
		ELSE
			#{
			CUST.DATA.recordCount = BANCS.OUTPARAM.recordCount
		#}
		ENDIF
		
		lv_b = "recordCount1|"
		lv_b = lv_b + " SELECT count(*) "
		lv_b = lv_b + " FROM CUSTOM.MODULEDET_MOD "
		lv_b = lv_b + " WHERE BANK_ID = ?SVAR AND MODULE_ID LIKE ?SVAR"
		
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" +  BANCS.INPUT.moduleId + "%"
		print(BANCS.INPARAM.BINDVARS)
		
		lv_e = urhk_dbSelectWithBind(lv_b)
		
		
		IF (lv_e != 0) THEN
		#{
			CUST.DATA.recordCount1 = "0"
			#}
		ELSE
			#{
			CUST.DATA.recordCount1 = BANCS.OUTPARAM.recordCount1
		#}
		ENDIF
		
		CUST.DATA.recordCount = CINT(CUST.DATA.recordCount) + CINT(CUST.DATA.recordCount1)
		CUST.DATA.recordCount = "CUST.DATA.recordCount|"+CUST.DATA.recordCount
		
		lv_u = urhk_SetOrbOut(CUST.DATA.recordCount)
		
		lv_s = "module_id,module_desc|"
		lv_s = lv_s + " SELECT MODULE_ID,MODULE_DESC "
		lv_s = lv_s + " FROM CUSTOM.MODULE_DETAILS "
		lv_s = lv_s + " WHERE BANK_ID = ?SVAR AND DEL_FLG='N'"
		lv_s = lv_s + " AND ENTITY_CRE_FLG='N' "
		lv_s = lv_s + " AND MODULE_ID LIKE ?SVAR"
		lv_s = lv_s + " UNION ALL"
		lv_s = lv_s + " SELECT MODULE_ID,MODULE_DESC "
		lv_s = lv_s + " FROM CUSTOM.MODULEDET_MOD "
		lv_s = lv_s + " WHERE BANK_ID = ?SVAR AND MODULE_ID LIKE ?SVAR"
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + BANCS.INPUT.moduleId + "%" + "|" + BANCS.STDIN.contextBankId + "|" + BANCS.INPUT.moduleId + "%"
		print(BANCS.INPARAM.BINDVARS)
		
	#}
	ENDIF
	
	
	
	IF(CUST.DATA.funcCode=="M")THEN
	#{
		lv_s = "recordCount|"
		lv_s = lv_s + " SELECT count(*) "
		lv_s = lv_s + " FROM CUSTOM.MODULE_DETAILS "
		lv_s = lv_s + " WHERE BANK_ID = ?SVAR AND DEL_FLG='N'"
		lv_s = lv_s + " AND MODULE_ID LIKE ?SVAR"
		
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" +  BANCS.INPUT.moduleId + "%"
		print(BANCS.INPARAM.BINDVARS)
		
		lv_d = urhk_dbSelectWithBind(lv_s)
		
		
		IF (lv_d != 0) THEN
		#{
			CUST.DATA.recordCount = "0"
			#}
		ELSE
			#{
			CUST.DATA.recordCount = BANCS.OUTPARAM.recordCount
		#}
		ENDIF
		
		CUST.DATA.recordCount = "CUST.DATA.recordCount|"+CUST.DATA.recordCount
		
		lv_u = urhk_SetOrbOut(CUST.DATA.recordCount)
		
		lv_s = "module_id,module_desc|"
		lv_s = lv_s + " SELECT MODULE_ID,MODULE_DESC "
		lv_s = lv_s + " FROM CUSTOM.MODULE_DETAILS "
		lv_s = lv_s + " WHERE BANK_ID = ?SVAR AND DEL_FLG='N'"
		lv_s = lv_s + " AND MODULE_ID LIKE ?SVAR"
		print(lv_s)
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + BANCS.INPUT.moduleId + "%"
		print(BANCS.INPARAM.BINDVARS)
		
	#}
	ENDIF
	
	
	
	lv_u = urhk_dbCursorOpenWithBind(lv_s)
	
	
	IF ( lv_u == 0 ) THEN
	#{
		lv_m = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		
		lv_c = urhk_dbCursorFetch(lv_m)
		lv_z = 0
		WHILE(lv_c == 0)
		#{
			#########################################################################
			#Checking for Null Values
			#########################################################################
			
			IF(BANCS.OUTPARAM.module_id == "") THEN
			#{
				BANCS.OUTPARAM.module_id = " "
				#}
			ELSE
				#{
				BANCS.OUTPARAM.module_id = TRIM(BANCS.OUTPARAM.module_id)
			#}
			ENDIF
			
			IF(BANCS.OUTPARAM.module_desc == "") THEN
			#{
				BANCS.OUTPARAM.module_desc = " "
				#}
			ELSE
				#{
				BANCS.OUTPARAM.module_desc = TRIM(BANCS.OUTPARAM.module_desc)
			#}
			ENDIF
			
			lv_z = lv_z + 1
			lv_o ="moduleId_"+FORMAT$(lv_z,"%d")+"|"+BANCS.OUTPARAM.module_id
			lv_q ="module_desc_"+FORMAT$(lv_z,"%d")+"|"+BANCS.OUTPARAM.module_desc
			
			lv_u = urhk_SetOrbOut(lv_o)
			
			lv_u = urhk_SetOrbOut(lv_q)
			
			lv_c = urhk_dbCursorFetch(lv_m)
		#}
		DO
		lv_u = urhk_dbCursorClose(lv_m)
	#}
	ENDIF
	
	################################################################################
	# STEP : STEP 2 END
	################################################################################
	
	################################################################################
	#Deleting the repository
	################################################################################
	
	################################################################################
	# Exit the script
	################################################################################
	
	EXITSCRIPT
	TRACE OFF
	################################################################################
	# End of script
	################################################################################
END-->
