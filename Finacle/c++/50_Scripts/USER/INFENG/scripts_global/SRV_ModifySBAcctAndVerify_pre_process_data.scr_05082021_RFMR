<--start
trace on
if(BANCS.STDIN.contextBankId=="54")then
	if(BANCS.STDIN.moduleIdentity!="FI")then
		callscriptifexist("ebAcmModOpChk.scr")
	endif
endif
IF (BANCS.STDIN.moduleIdentity == "FI") THEN
	sv_h =""
	sv_a = urhk_GetChannelInfo("")
	print(sv_a)
	if (sv_a!=0) then
	#{
		sv_u = urhk_SRV_SetErr("Error: Channel ID is not passed/Incorrect")
		BANCS.OUTPUT.successOrFailure="F"
		GOTO ERROREXIT
	#}
	else
	#{
		sv_h = BANCS.OUTPARAM.DccId
		print(sv_h)
		IF ((sv_h=="OMN") OR (sv_h=="NWG")) THEN
			if (BANCS.STDIN.contextBankId=="54") then
				callscriptifexist("ebNomModChk.scr")
			endif
		ENDIF
	#}
	endif
ENDIF
ERROREXIT:
trace off
end-->
	
