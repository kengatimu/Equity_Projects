#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: ubpdp039.scr
# event: NA
# description: NA
#=================================================

<--START
CHECKSUM="fbf565ab7c089841031d51757622ec9819f8f703d1fd6fcd03f410260e3cf976"
	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp039.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called from ubp_det_link.js on click of get bill
	#                               details button for YU biller.
	# Called Script         :		ubpdp040.scr
	# Calling script        :		ubpdp038.scr
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           16-04-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	sv_s = "srlnum|"
	sv_s = sv_s + " SELECT CUSTOM.YUCASH_RFI_SEQ.nextval"
	sv_s = sv_s + " FROM DUAL"
	PRINT(sv_s)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d!=0)THEN
	#{
		CUST.UBP.errorFlg="Y"
		#CUST.UBP.errorMsg="A Database error has occured. Please contact your DBA."
		
		CUST.UBP.errorCode = "UBP_ERR052"
		CALL("ubpdp044.scr")
		CUST.UBP.errorMsg =CUST.UBP.errorDesc
		PRINT(CUST.UBP.errorMsg)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	CUST.UBP.srlnum=BANCS.OUTPARAM.srlnum
	
	BANCS.INPARAM.BINDVARS = "I|1|"+CUST.UBP.billRefNum+"|"+CUST.UBP.billAmt+"|"+BANCS.STDIN.mySolId
	BANCS.INPARAM.BINDVARS =BANCS.INPARAM.BINDVARS+"|"+CUST.UBP.srlnum
	BANCS.INPARAM.BINDVARS =BANCS.INPARAM.BINDVARS+"|"+ CUST.UBP.requestApp
	sv_s = " INSERT INTO CUSTOM.YUCASH_RFI_TABLE"
	sv_s = sv_s + " (CASHINOUTFLG,STATUS,MOBILENUMBER,AMOUNT,BRANCH_CODE,ID,REQUESTAPP)"
	sv_s = sv_s + " values (?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR)"
	PRINT(sv_s)
	sv_d = urhk_dbSQLWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d!=0)THEN
	#{
		CUST.UBP.errorFlg="Y"
		#CUST.UBP.errorMsg="A Database error has occured. Please contact your DBA."
		CUST.UBP.errorCode = "UBP_ERR052"
		CALL("ubpdp044.scr")
		CUST.UBP.errorMsg =CUST.UBP.errorDesc
		PRINT(CUST.UBP.errorMsg)
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		sv_d = urhk_dbSqlWithBind("COMMIT")
		#========================================================
		# Check whether CUST Repository exists else create it
		#========================================================
		sv_r = REPEXISTS("YUURL")
		IF(sv_r == 0) THEN
		#{
			CREATEREP ("YUURL")
		#}
		ENDIF
		#========================================================
		# Check whether UBP Class exists else create it
		#========================================================
		sv_r = CLASSEXISTS("YUURL","STDIN")
		IF(sv_r == 0) THEN
		#{
			CREATECLASS ("YUURL","STDIN",5)
		#}
		ENDIF
		#========================================================
		# Check whether UBP Class exists else create it
		#========================================================
		sv_r = CLASSEXISTS("YUURL","STDOUT")
		IF(sv_r == 0) THEN
		#{
			CREATECLASS ("YUURL","STDOUT",5)
		#}
		ENDIF
		YUURL.STDIN.URL="http://192.168.1.127:8080/yucash/enquireinfo?id="+CUST.UBP.srlnum
		PRINT(YUURL.STDIN.URL)
		
		#Set the Frame name to point to an existing frame.
		#To open the URL in a new window, give a window name.
		#To always open in a new window, comment out the line.
		
		YUURL.STDIN.FRAME="mywin"
		
		YUURL.STDOUT.ErrorCode="0"
		YUURL.STDOUT.ErrorDesc="No error"
		
		sv_a = urhk_TBAF_ShowHTML("YUURL")
		PRINT(YUURL.STDOUT.ErrorCode)
		PRINT(YUURL.STDOUT.ErrorDesc)
		DELETEREP("YUURL")
		#CALL("yuThirdPartyDb001.scr")
		CALL("ubpdp040.scr")
		
	#}
	ENDIF
END-->
