##########################################################################################
# Source Name           : ubpdp049.scr
# Date                  : 04-Jul-2013
# Description           : Account Balance Details - On click of Explode Icon
# Author                : Swati Ganjoo
# Modification History  :
# <Serial No.>  <Date>        <Author Name>             <Description>
# ------------  -----------   -----------------         --------------
#	1	04-Jul-2013	Swati Ganjoo		Base Version
###########################################################################################
<--START
CHECKSUM="adbc6701346263bff3310d4fa829ef307e4959fe3bcf6d5823fc359c9d3123f8"
	TRACE ON
	
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether UBP Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","UBP")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","UBP",5)
	#}
	ENDIF
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	
	CUST.UBP.errorFlg = "N"
	CUST.UBP.errorMsg = " "
	CUST.UBP.acctId = " "
	CUST.UBP.entityId = " "
	CUST.UBP.solId = " "
	CUST.UBP.crncyCode = " "
	CUST.UBP.acctName = " "
	CUST.UBP.ledgerBal = " "
	CUST.UBP.availableBal = " "
	CUST.UBP.effAvailableBal = " "
	CUST.UBP.freezeStatus = " "
	CUST.UBP.freezeReasonCode = " "
	CUST.UBP.acctClosed = " "
	CUST.UBP.shadowBal = " "
	CUST.UBP.acctStatus = " "
	CUST.UBP.bankId = BANCS.STDIN.contextBankId
	CUST.UBP.restrictAccess = "N"
	
	IF(FIELDEXISTS(BANCS.INPUT.acctId))THEN
	#{
		IF(TRIM(BANCS.INPUT.acctId) != "")THEN
		#{
			CUST.UBP.acctId = TOUPPER(TRIM(BANCS.INPUT.acctId))
			print(CUST.UBP.acctId)
			#}
		ELSE
			#{
			CUST.UBP.errorFlg = "Y"
			CUST.UBP.errorMsg = "Account Id is blank"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.errorFlg = "Y"
		CUST.UBP.errorMsg = "Invalid Input for fnExecuteScript"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	##########################################################################
	# Validate the input a/c id
	##########################################################################
	
	CUST.UBP.acid = ""
	sv_s = "acid|"
	sv_s = sv_s + " SELECT ACID"
	sv_s = sv_s + " FROM TBAADM.GAM "
	sv_s = sv_s + " WHERE FORACID = ?SVAR"
	sv_s = sv_s + " AND DEL_FLG != 'Y'"
	sv_s = sv_s + " AND BANK_ID = ?SVAR"
	print(sv_s)
	
	BANCS.INPARAM.BINDVARS = CUST.UBP.acctId + "|" + BANCS.STDIN.contextBankId
	print(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	IF(sv_d == 0)THEN
	#{
		CUST.UBP.acid = BANCS.OUTPARAM.acid
		print(CUST.UBP.acid)
		#}
	ELSE
		#{
		CUST.UBP.errorFlg = "Y"
		CUST.UBP.errorMsg = "Invalid Account ID"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_s = CUST.UBP.acctId
	print(sv_s)
	sv_u = urhk_getAcctDetailsInRepository(sv_s)
	print(sv_u)
	
	IF(sv_u != 0)THEN
	#{
		CUST.UBP.errorFlg = "Y"
		CUST.UBP.errorMsg = "Unable to get Account Balance Details"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.AcctId != "")THEN
	#{
		CUST.UBP.acctId = BANCS.OUTPARAM.AcctId
		print(CUST.UBP.acctId)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.acctSolId != "")THEN
	#{
		CUST.UBP.solId = BANCS.OUTPARAM.acctSolId
		print(CUST.UBP.solId)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.acctCrncyCode != "")THEN
	#{
		CUST.UBP.crncyCode = BANCS.OUTPARAM.acctCrncyCode
		print(CUST.UBP.crncyCode)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.acctName != "")THEN
	#{
		CUST.UBP.acctName = BANCS.OUTPARAM.acctName
		print(CUST.UBP.acctName)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.clearBalance != "")THEN
	#{
		CUST.UBP.ledgerBal = BANCS.OUTPARAM.clearBalance
		print(CUST.UBP.ledgerBal)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.AvailableAmt != "")THEN
	#{
		CUST.UBP.availableBal = BANCS.OUTPARAM.AvailableAmt
		print(CUST.UBP.availableBal)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.EffAvailableAmt != "")THEN
	#{
		CUST.UBP.effAvailableBal = BANCS.OUTPARAM.EffAvailableAmt
		print(CUST.UBP.effAvailableBal)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.freezeCode != "")THEN
	#{
		CUST.UBP.freezeStatus = BANCS.OUTPARAM.freezeCode
		print(CUST.UBP.freezeStatus)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.freezeReasonCode != "")THEN
	#{
		CUST.UBP.freezeReasonCode = BANCS.OUTPARAM.freezeReasonCode
		print(CUST.UBP.freezeReasonCode)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.acctCloseflg != "")THEN
	#{
		CUST.UBP.acctClosed = BANCS.OUTPARAM.acctCloseflg
		print(CUST.UBP.acctClosed)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.unclearBalance != "")THEN
	#{
		CUST.UBP.shadowBal = BANCS.OUTPARAM.unclearBalance
		print(CUST.UBP.shadowBal)
	#}
	ENDIF
	
	CUST.UBP.acid = ""
	sv_s = CUST.UBP.acctId
	sv_u = urhk_B2k_ForacidToAcid(sv_s)
	print(sv_u)
	
	IF(sv_u == 0)THEN
	#{
		PRINT(BANCS.OUTPARAM.Acid)
		CUST.UBP.acid= BANCS.OUTPARAM.Acid
		PRINT(CUST.UBP.acid)
	#}
	ENDIF
	
	IF(CUST.UBP.acid != "")THEN
	#{
		BANCS.INPARAM.acid = CUST.UBP.acid
		sv_u = urhk_getAcctStatus("")
		print(sv_u)
		
		IF(sv_u == 0)THEN
		#{
			print(BANCS.OUTPARAM.acctStatus)
			CUST.UBP.acctStatus = BANCS.OUTPARAM.acctStatus
			PRINT(CUST.UBP.acctStatus)
			
			IF(CUST.UBP.acctStatus == "A")THEN
			#{
				CUST.UBP.acctStatus = "A - Active"
			#}
			ENDIF
			
			IF(CUST.UBP.acctStatus == "D")THEN
			#{
				CUST.UBP.acctStatus = "D - Dormant"
			#}
			ENDIF
			
			IF(CUST.UBP.acctStatus == "I")THEN
			#{
				CUST.UBP.acctStatus = "I - Inactive"
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	##################################################################################
	# Access Restriction for user id
	##################################################################################
	
	CUST.UBP.acctAccessCode = ""
	sv_s = "acctAccessCode|SELECT ACCT_ACCESS_CODE FROM TBAADM.AAC"
	sv_s = sv_s + " WHERE BANK_ID = ?SVAR"
	sv_s = sv_s + " AND ACID = ?SVAR"
	sv_s = sv_s + " AND ENTITY_CRE_FLG = 'Y'"
	print(sv_s)
	
	BANCS.INPARAM.BINDVARS = CUST.UBP.bankId + "|" + CUST.UBP.acid
	print(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	IF(sv_d == 0)THEN
	#{
		CUST.UBP.acctAccessCode = BANCS.OUTPARAM.acctAccessCode
		PRINT(CUST.UBP.acctAccessCode)
	#}
	ENDIF
	
	IF(CUST.UBP.acctAccessCode != "")THEN
	#{
		sv_s = "count|SELECT 1 FROM DUAL WHERE EXISTS(SELECT USER_ID FROM TBAADM.UAC WHERE"
		sv_s = sv_s + " USER_ID = ?SVAR"
		sv_s = sv_s + " AND ACCT_ACCESS_CODE = ?SVAR"
		sv_s = sv_s + " AND BANK_ID = ?SVAR"
		sv_s = sv_s + " AND ENTITY_CRE_FLG = 'Y')"
		print(sv_s)
		
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.userId + "|" + CUST.UBP.acctAccessCode + "|" + CUST.UBP.bankId
		print(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		
		IF(sv_d != 0)THEN
		#{
			CUST.UBP.restrictAccess = "Y"
		#}
		ENDIF
	#}
	ENDIF
	
	ENDOFSCRIPT:
		
		sv_u = urhk_SetOrbOut("errorFlg|"+CUST.UBP.errorFlg)
		print(sv_u)
		sv_u = urhk_SetOrbOut("errorMsg|"+CUST.UBP.errorMsg)
		print(sv_u)
		sv_u = urhk_SetOrbOut("acctId|"+CUST.UBP.acctId)
		print(sv_u)
		sv_u = urhk_SetOrbOut("solId|"+CUST.UBP.solId)
		print(sv_u)
		sv_u = urhk_SetOrbOut("crncyCode|"+CUST.UBP.crncyCode)
		print(sv_u)
		sv_u = urhk_SetOrbOut("acctName|"+CUST.UBP.acctName)
		print(sv_u)
		sv_u = urhk_SetOrbOut("ledgerBal|"+CUST.UBP.ledgerBal)
		print(sv_u)
		sv_u = urhk_SetOrbOut("availableBal|"+CUST.UBP.availableBal)
		print(sv_u)
		sv_u = urhk_SetOrbOut("effAvailableBal|"+CUST.UBP.effAvailableBal)
		print(sv_u)
		sv_u = urhk_SetOrbOut("freezeStatus|"+CUST.UBP.freezeStatus)
		print(sv_u)
		sv_u = urhk_SetOrbOut("freezeReasonCode|"+CUST.UBP.freezeReasonCode)
		print(sv_u)
		sv_u = urhk_SetOrbOut("acctClosed|"+CUST.UBP.acctClosed)
		print(sv_u)
		sv_u = urhk_SetOrbOut("shadowBal|"+CUST.UBP.shadowBal)
		print(sv_u)
		sv_u = urhk_SetOrbOut("acctStatus|"+CUST.UBP.acctStatus)
		print(sv_u)
		sv_u = urhk_SetOrbOut("bankId|"+CUST.UBP.bankId)
		print(sv_u)
		sv_u = urhk_SetOrbOut("restrictAccess|"+CUST.UBP.restrictAccess)
		print(sv_u)
		
		sv_u = urhk_b2k_printrepos("BANCS")
		print(sv_u)
END-->
