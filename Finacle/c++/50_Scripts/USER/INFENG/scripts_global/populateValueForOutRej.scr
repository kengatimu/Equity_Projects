<--START
CHECKSUM="601b98038ec7f0b6330093b4b0ede63c5ba5ecbae59dd9682c7c565439ac371a"
	
	TRACE ON
	
	#####################################################
	
	IF(REPEXISTS("CUST")==0) THEN
	#{
		
		CREATEREP("CUST")
		
	#}
	ENDIF
	
	IF(CLASSEXISTS("CUST","OUTREJ")==0) THEN
	#{
		
		CREATECLASS("CUST","OUTREJ",5)
		
	#}
	ENDIF
		
	CUST.OUTREJ.acctId = BANCS.INPUT.acctId
	PRINT(CUST.OUTREJ.acctId)
	
	CUST.OUTREJ.acctCrncy = BANCS.INPUT.acctCrncy
	PRINT(CUST.OUTREJ.acctCrncy)
	
	CUST.OUTREJ.acctSolId = BANCS.INPUT.acctSolId
	PRINT(CUST.OUTREJ.acctSolId)
	
	CUST.OUTREJ.instrNum = BANCS.INPUT.instrNum
	PRINT(CUST.OUTREJ.instrNum)

	CUST.OUTREJ.amount = BANCS.INPUT.amount
	PRINT(CUST.OUTREJ.amount)

	
	CUST.OUTREJ.outRejSolId = " "
	CUST.OUTREJ.outRejZoneCode = " "
	CUST.OUTREJ.outRejZoneDate = " "
	CUST.OUTREJ.outRejSetNum = " "
	CUST.OUTREJ.outRejInstSrlNum = " "
	
	sv_a = urhk_B2k_ForacidToAcid(CUST.OUTREJ.acctId)
	if(sv_a != 0) THEN
		GOTO ENDOFSCRIPT
	ELSE
		CUST.OUTREJ.ACID = BANCS.OUTPARAM.Acid
		PRINT(CUST.OUTREJ.ACID)	
	ENDIF
	

	sv_s = ""
	sv_s = sv_s + "outRejZoneCode, outRejZoneDate, outRejSolId, outRejSetNum, outRejInstSrlNum|"
	sv_s = sv_s + "SELECT I.CLG_ZONE_CODE , TO_CHAR(I.CLG_ZONE_DATE,'DD-MM-YYYY'), I.SOL_ID, I.SET_NUM, INSTRMNT_SRL_NUM FROM TBAADM.OCI I, TBAADM.OCP P"	
	sv_s = sv_s + " WHERE I.INSTRMNT_ID = ?SVAR AND I.BANK_ID = ?SVAR "
	sv_s = sv_s + " AND I.SET_NUM = P.SET_NUM AND ACID = ?SVAR AND P.BANK_ID = ?SVAR "
	sv_s = sv_s + " and i.INSTRMNT_AMT = ?SVAR and I.clg_zone_code = p.clg_zone_code  and I.DEL_FLG = 'N' and i.SOL_ID = P.SOL_ID"
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS = LPAD(CUST.OUTREJ.instrNum,16,' ')  + "|" + BANCS.STDIN.contextBankId
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.OUTREJ.ACID  + "|" + BANCS.STDIN.contextBankId
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.OUTREJ.amount
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_q = urhk_dbSelectWithBind(sv_s)
	
	if(sv_q != 0 ) THEN
		
		GOTO ENDOFSCRIPT
	
	ELSE

	#ENDIF
	#sv_s = ""
	#sv_s = sv_s + "outRejZoneCode, outRejZoneDate, outRejSolId, outRejSetNum, outRejInstSrlNum|"
	#sv_s = sv_s + "SELECT I.CLG_ZONE_CODE , TO_CHAR(I.CLG_ZONE_DATE,'DD-MM-YYYY'), I.SOL_ID, I.SET_NUM, INSTRMNT_SRL_NUM FROM TBAADM.OCI I, TBAADM.OCP P"	
	#sv_s = sv_s + " WHERE I.INSTRMNT_ID = ?SVAR AND I.BANK_ID = ?SVAR "
	#sv_s = sv_s + " AND I.SET_NUM = P.SET_NUM AND ACID = ?SVAR AND P.BANK_ID = ?SVAR "
	#sv_s = sv_s + " and i.CLG_ZONE_DATE = P.CLG_ZONE_DATE and I.clg_zone_code = p.clg_zone_code  and I.DEL_FLG = 'N' and i.SOL_ID = P.SOL_ID"
	#PRINT(sv_s)
	#BANCS.INPARAM.BINDVARS = LPAD(CUST.OUTREJ.instrNum,16,' ')  + "|" + BANCS.STDIN.contextBankId
	#BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.OUTREJ.ACID  + "|" + BANCS.STDIN.contextBankId
	#PRINT(BANCS.INPARAM.BINDVARS)
	#sv_q = urhk_dbSelectWithBind(sv_s)
	
	#if(sv_q != 0 ) THEN
		
	#	GOTO ENDOFSCRIPT
	
	#ELSE
		CUST.OUTREJ.outRejSolId = BANCS.OUTPARAM.outRejSolId
		PRINT(CUST.OUTREJ.outRejSolId)
		CUST.OUTREJ.outRejZoneCode = BANCS.OUTPARAM.outRejZoneCode
		PRINT(CUST.OUTREJ.outRejZoneCode)
		CUST.OUTREJ.outRejZoneDate = BANCS.OUTPARAM.outRejZoneDate
		PRINT(CUST.OUTREJ.outRejZoneDate)
		CUST.OUTREJ.outRejSetNum = BANCS.OUTPARAM.outRejSetNum
		PRINT(CUST.OUTREJ.outRejSetNum)
		CUST.OUTREJ.outRejInstSrlNum = BANCS.OUTPARAM.outRejInstSrlNum	
		PRINT(CUST.OUTREJ.outRejInstSrlNum)
		
		
		sv_u = urhk_SetOrbOut("outRejSolId|"+CUST.OUTREJ.outRejSolId)
		PRINT(sv_u)
		
		sv_u = urhk_SetOrbOut("outRejZoneCode|"+CUST.OUTREJ.outRejZoneCode)
		PRINT(sv_u)
		
		sv_u = urhk_SetOrbOut("outRejZoneDate|"+CUST.OUTREJ.outRejZoneDate)
		PRINT(sv_u)
		
		sv_u = urhk_SetOrbOut("outRejSetNum|"+CUST.OUTREJ.outRejSetNum)
		PRINT(sv_u)
		
		sv_u = urhk_SetOrbOut("outRejInstSrlNum|"+CUST.OUTREJ.outRejInstSrlNum)
		PRINT(sv_u)
		
		
	ENDIF
	ENDOFSCRIPT:
		EXITSCRIPT
	
END-->