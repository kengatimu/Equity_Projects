#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: ubpdp077.scr
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START
CHECKSUM="cfb9c4e101eb802089faa665824e53d295a904edea52f0e00b3a0bde45c67b27"
	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp077.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called for getting the notification call inputs(KRA type)
	# Called Script         :
	# Calling Script        :
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           27-09-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	CUST.UBP.servName="notifyPayment"
	IF(FIELDEXISTS(CUST.UBP.servName))THEN
	#{
		IF(TRIM(CUST.UBP.servName) != "")THEN
		#{
			CUST.UBP.retStrngValHid=CUST.UBP.servName
			PRINT(CUST.UBP.servName)
			#}
		ELSE
			#{
			CUST.UBP.servName=""
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.servName=""
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.UBP.billAcctId))THEN
	#{
		IF(TRIM(CUST.UBP.billAcctId) != "")THEN
		#{
			CUST.UBP.billAcctId=CUST.UBP.billAcctId
			PRINT(CUST.UBP.billAcctId)
			#}
		ELSE
			#{
			CUST.UBP.billAcctId=""
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.billAcctId=""
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.UBP.billAmt))THEN
	#{
		IF(TRIM(CUST.UBP.billAmt) != "")THEN
		#{
			CUST.UBP.billAmt=CUST.UBP.billAmt
			PRINT(CUST.UBP.billAmt)
			#}
		ELSE
			#{
			CUST.UBP.billAmt=""
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.billAmt=""
	#}
	ENDIF
	
	IF(FIELDEXISTS(CUST.UBP.billRefNum))THEN
	#{
		IF(TRIM(CUST.UBP.billRefNum) != "")THEN
		#{
			CUST.UBP.billRefNum=CUST.UBP.billRefNum
			PRINT(CUST.UBP.billRefNum)
			#}
		ELSE
			#{
			CUST.UBP.billRefNum=""
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.billRefNum=""
	#}
	ENDIF
	
	
	CUST.UBP.channelId="COR"
	
	
	IF(FIELDEXISTS(CUST.UBP.bankId))THEN
	#{
		IF(TRIM(CUST.UBP.bankId) != "")THEN
		#{
			CUST.UBP.channelId=CUST.UBP.bankId
			PRINT(CUST.UBP.bankId)
			#}
		ELSE
			#{
			CUST.UBP.bankId=""
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.bankId=""
	#}
	ENDIF
	
	IF(FIELDEXISTS(CUST.UBP.branchId))THEN
	#{
		IF(TRIM(CUST.UBP.branchId) != "")THEN
		#{
			CUST.UBP.branchId=CUST.UBP.branchId
			PRINT(CUST.UBP.branchId)
			#}
		ELSE
			#{
			CUST.UBP.branchId=""
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.branchId=""
	#}
	ENDIF
	
	
	IF(FIELDEXISTS(CUST.UBP.userId))THEN
	#{
		IF(TRIM(CUST.UBP.userId) != "")THEN
		#{
			CUST.UBP.userId=CUST.UBP.userId
			PRINT(CUST.UBP.userId)
			#}
		ELSE
			#{
			CUST.UBP.userId=""
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.userId=""
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.UBP.instrmntNum))THEN
	#{
		IF(TRIM(CUST.UBP.instrmntNum) != "")THEN
		#{
			CUST.UBP.chequeNum=CUST.UBP.instrmntNum
			PRINT(CUST.UBP.chequeNum)
			#}
		ELSE
			#{
			CUST.UBP.chequeNum=""
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.chequeNum=""
	#}
	ENDIF
	
	print(CUST.UBP.bankId)
	
	
	CUST.UBP.createdBy=CUST.UBP.userId
	print(CUST.UBP.createdBy)
	IF(FIELDEXISTS(CUST.UBP.payRefCrncy))THEN
	#{
		IF(TRIM(CUST.UBP.payRefCrncy) != "")THEN
		#{
			CUST.UBP.payRefCrncy=CUST.UBP.payRefCrncy
			PRINT(CUST.UBP.payRefCrncy)
			#}
		ELSE
			#{
			CUST.UBP.payRefCrncy=""
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.payRefCrncy=""
	#}
	ENDIF
	
	
	print(CUST.UBP.mySolId)
	
	IF(FIELDEXISTS(CUST.UBP.tranDate))THEN
	#{
		IF(TRIM(CUST.UBP.tranDate) != "")THEN
		#{
			CUST.UBP.tranDate=mid$(CUST.UBP.tranDate,0,10)
			PRINT(CUST.UBP.tranDate)
			#}
		ELSE
			#{
			CUST.UBP.tranDate=""
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.tranDate=""
	#}
	ENDIF
	
	IF(FIELDEXISTS(CUST.UBP.tranId))THEN
	#{
		IF(TRIM(CUST.UBP.tranId) != "")THEN
		#{
			CUST.UBP.tranId=CUST.UBP.tranId
			PRINT(CUST.UBP.tranId)
			#}
		ELSE
			#{
			CUST.UBP.tranId=""
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.tranId=""
	#}
	ENDIF
	IF(FIELDEXISTS(CUST.UBP.tranPart))THEN
	#{
		IF(TRIM(CUST.UBP.tranPart) != "")THEN
		#{
			CUST.UBP.tranPart=CUST.UBP.tranPart
			PRINT(CUST.UBP.tranPart)
			#}
		ELSE
			#{
			CUST.UBP.tranPart=""
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.tranPart=""
	#}
	ENDIF
	
	IF(FIELDEXISTS(CUST.UBP.tranRem))THEN
	#{
		IF(TRIM(CUST.UBP.tranRem) != "")THEN
		#{
			CUST.UBP.tranRem=CUST.UBP.tranRem
			PRINT(CUST.UBP.tranRem)
			#}
		ELSE
			#{
			CUST.UBP.tranRem=""
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.tranRem=""
	#}
	ENDIF
	
	
	
	
	BANCS.INPARAM.BINDVARS = CUST.UBP.tranId+"|"+CUST.UBP.tranDate+"|"+CUST.UBP.bankId
	sv_s = "count11|"
	sv_s = sv_s + " SELECT count(11)"
	sv_s = sv_s + " FROM TBAADM.DTD "
	sv_s = sv_s + " WHERE TRAN_ID= LPAD(?SVAR,9,' ')"
	sv_s = sv_s + " AND TRAN_DATE= to_date(?SVAR,'DD-MM-YYYY')"
	sv_s = sv_s + " AND BANK_ID = ?SVAR"
	
	PRINT(sv_s)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d!=0)THEN
	#{
		CUST.UBP.numOfPartTran="0"
		#}
	ELSE
		#{
		CUST.UBP.numOfPartTran=BANCS.OUTPARAM.count11
		PRINT(CUST.UBP.numOfPartTran)
		
	#}
	ENDIF
	
	
	
	CUST.UBP.valueDate=CUST.UBP.BODDate
	print(CUST.UBP.valueDate)
	
	IF(FIELDEXISTS(CUST.UBP.tranSubType))THEN
	#{
		IF(TRIM(CUST.UBP.tranSubType) != "")THEN
		#{
			CUST.UBP.tranSubType=CUST.UBP.tranSubType
			PRINT(CUST.UBP.tranSubType)
			#}
		ELSE
			#{
			CUST.UBP.tranSubType=""
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.tranSubType=""
	#}
	ENDIF
	
	CUST.UBP.verifiedBy=CUST.UBP.userId
	print(CUST.UBP.verifiedBy)
	
	
	
	
