#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: tfclimitdp001.scr
# Type: NA|default
# Description: tfclimitdp001.scr
# Author:
# Date:
# Modification Log:
#============================================================

##############################################################################
#   Description         : Searcher functionality for selecting limit id belonging to Cif Id alone
#   Input               : NULL
#   Output              : LimitId,currncy_code,Original_sanction_limit,Sanction_limit,drawing_power,liabilities,del_flg
#   Called Script       : NA
#   Calling Script      : NA
#   Modification History:
#   SNo  	Date   			Author Name    	Description
#   001		10 Jul 2013		Priya K		Original Version
#   002		12 Aug 2013		Priyansh	Modified Version TOL:364604
#################################################################################
<--START
CHECKSUM="c32ae6457b403dbc3c2025a2558f8ebdf98057af7bc5d5b984362ef9e81668cd"
	TRACE ON
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST","TFC") == 0) THEN
	#{
		CREATECLASS("CUST","TFC",5)
	#}
	ENDIF
	#--------------------------------------------------------------------------
	# Initialize variables
	#--------------------------------------------------------------------------
	
	#BANCS.INPUT.cifId = "DUMMYCIF7"
	PRINT(BANCS.INPUT.cifId)
	CUST.TFC.cifId = TOUPPER(BANCS.INPUT.cifId)
	CUST.TFC.limPrefix  = ""
	CUST.TFC.limSuffix  = ""
	CUST.TFC.curr  = ""
	CUST.TFC.orgSanctLimit  = ""
	CUST.TFC.sanctLimit  = ""
	CUST.TFC.drwngPower  = ""
	CUST.TFC.liab  = ""
	CUST.TFC.delFlg  = ""
	CUST.TFC.recordCount ="0"
	sv_s = "1"
	
	#--------------------------------------------------------------------------
	# Get record Count
	#--------------------------------------------------------------------------
	sv_a =  "count | SELECT COUNT(*) "
	sv_a = sv_a + " FROM TBAADM.LLT  WHERE DEL_FLG != 'Y' "
	sv_a = sv_a + " AND BANK_ID = ?SVAR "
	sv_a = sv_a + " AND CUST_ID = (SELECT DISTINCT(CUST_ID) FROM "
	sv_a = sv_a + " TBAADM.GAM WHERE CIF_ID = ?SVAR  AND ENTITY_CRE_FLG = 'Y' AND DEL_FLG = 'N' AND BANK_ID = ?SVAR)"
	sv_a = sv_a +" AND ENTITY_CRE_FLG = 'Y' AND DEL_FLG = 'N' "
	BANCS.INPARAM.BINDVARS =  BANCS.STDIN.contextBankId + "|" + CUST.TFC.cifId + "|" + BANCS.STDIN.contextBankId
	
	PRINT(sv_a)
	
	sv_d = urhk_dbSelectWithBind(sv_a)
	PRINT(sv_d)
	
	CUST.TFC.recordCount = BANCS.OUTPARAM.count
	PRINT(CUST.TFC.recordCount)
	
	sv_u = urhk_SetOrbOut("recordCount|" + CUST.TFC.recordCount)
	
	
	#--------------------------------------------------------------------------
	# Fetch records from DB
	#--------------------------------------------------------------------------
	
	sv_a =  "limPrefix,limSuffix,curr,orgSanctLimit,sanctLimit,drwngPower,delFlg| "
	sv_a = sv_a + " SELECT limit_prefix,limit_suffix,crncy_code,orig_sanct_lim,sanct_lim,drwng_power,del_flg "
	sv_a = sv_a + " FROM TBAADM.LLT  WHERE DEL_FLG != 'Y' "
	sv_a = sv_a + " AND BANK_ID = ?SVAR "
	sv_a = sv_a + " AND CUST_ID = ( SELECT DISTINCT(CUST_ID) FROM "
	sv_a = sv_a + " TBAADM.GAM WHERE CIF_ID = ?SVAR AND ENTITY_CRE_FLG = 'Y' AND DEL_FLG = 'N' AND BANK_ID = ?SVAR)"
	sv_a = sv_a +" AND ENTITY_CRE_FLG = 'Y' AND DEL_FLG = 'N' "
	BANCS.INPARAM.BINDVARS =  BANCS.STDIN.contextBankId + "|" + CUST.TFC.cifId + "|" + BANCS.STDIN.contextBankId
	
	PRINT(sv_a)
	
	sv_u = urhk_dbCursorOpenWithBind(sv_a)
	
	IF (sv_u == 0) THEN
	#{
		sv_b = (BANCS.OUTPARAM.DB_CURSOR_NUMBER)
		PRINT(sv_b)
		sv_r = urhk_dbCursorFetch(sv_b)
		PRINT(sv_r)
		# Else fetch details from cursor
		CUST.TFC.count = sv_s
		WHILE(sv_r == 0)
		#{
			IF(TRIM(BANCS.OUTPARAM.limPrefix) != "") THEN
			#{
				CUST.TFC.limPrefix = BANCS.OUTPARAM.limPrefix
				PRINT(CUST.TFC.limPrefix)
			#}
			ENDIF
			IF(TRIM(BANCS.OUTPARAM.limSuffix) != "") THEN
			#{
				CUST.TFC.limSuffix = BANCS.OUTPARAM.limSuffix
				PRINT(CUST.TFC.limSuffix)
			#}
			ENDIF
			IF(TRIM(BANCS.OUTPARAM.curr) != "") THEN
			#{
				CUST.TFC.curr = BANCS.OUTPARAM.curr
				PRINT(CUST.TFC.curr)
			#}
			ENDIF
			IF(TRIM(BANCS.OUTPARAM.orgSanctLimit) != "") THEN
			#{
				CUST.TFC.orgSanctLimit = BANCS.OUTPARAM.orgSanctLimit
				PRINT(CUST.TFC.orgSanctLimit)
			#}
			ENDIF
			IF(TRIM(BANCS.OUTPARAM.sanctLimit) != "") THEN
			#{
				CUST.TFC.sanctLimit = BANCS.OUTPARAM.sanctLimit
				PRINT(CUST.TFC.sanctLimit)
			#}
			ENDIF
			IF(TRIM(BANCS.OUTPARAM.drwngPower) != "") THEN
			#{
				CUST.TFC.drwngPower = BANCS.OUTPARAM.drwngPower
				PRINT(CUST.TFC.drwngPower)
			#}
			ENDIF
			#		IF(TRIM(BANCS.OUTPARAM.liab) != "") THEN
			#		#{
			#			CUST.TFC.liab = BANCS.OUTPARAM.liab
			#			print(CUST.TFC.liab)
			#			CUST.TFC.liab = CDOUBLE(CUST.TFC.sanctLimit) - CDOUBLE(CUST.TFC.liab)
			#			print(CUST.TFC.liab)
			#			sv_c = CDOUBLE(CUST.TFC.liab)
			#			CUST.TFC.liab = FORMAT$(sv_c,"%.2f")
			#			print(CUST.TFC.liab)
			#
			#		#}
			#		ENDIF
			IF(TRIM(BANCS.OUTPARAM.delFlg) != "") THEN
			#{
				CUST.TFC.delFlg = BANCS.OUTPARAM.delFlg
				PRINT(CUST.TFC.delFlg)
			#}
			ENDIF
			
			# Set the values to front end
			sv_z = "limitIdPrefix_" + CUST.TFC.count + "|" + CUST.TFC.limPrefix
			sv_m = urhk_SetOrbOut(sv_z)
			PRINT(sv_z)
			PRINT(sv_m)
			
			sv_z = "limitIdSuffix_" + CUST.TFC.count + "|" + CUST.TFC.limSuffix
			sv_m = urhk_SetOrbOut(sv_z)
			PRINT(sv_z)
			PRINT(sv_m)
			
			sv_z = "curr_" + CUST.TFC.count + "|" + CUST.TFC.curr
			sv_m = urhk_SetOrbOut(sv_z)
			PRINT(sv_z)
			PRINT(sv_m)
			
			sv_z = "orgSanctLimit_" + CUST.TFC.count + "|" + FORMAT$(CDOUBLE(CUST.TFC.orgSanctLimit),"%.2f")
			sv_m = urhk_SetOrbOut(sv_z)
			PRINT(sv_z)
			PRINT(sv_m)
			
			sv_z = "sanctLimit_" + CUST.TFC.count + "|" + FORMAT$(CDOUBLE(CUST.TFC.sanctLimit),"%.2f")
			sv_m = urhk_SetOrbOut(sv_z)
			PRINT(sv_z)
			PRINT(sv_m)
			
			sv_z = "drwngPower_" + CUST.TFC.count + "|" + FORMAT$(CDOUBLE(CUST.TFC.drwngPower),"%.2f")
			sv_m = urhk_SetOrbOut(sv_z)
			PRINT(sv_z)
			PRINT(sv_m)
			
			#		sv_z = "liab_" + CUST.TFC.count + "|" + CUST.TFC.liab
			#		sv_m = urhk_SetOrbOut(sv_z)
			#		print(sv_z)
			#		print(sv_m)
			
			sv_z = "delFlg_" + CUST.TFC.count + "|" + CUST.TFC.delFlg
			sv_m = urhk_SetOrbOut(sv_z)
			PRINT(sv_z)
			PRINT(sv_m)
			
			# Fetch next record
			
			CUST.TFC.count = CINT(CUST.TFC.count) + 1
			PRINT(CUST.TFC.count)
			sv_r = urhk_dbCursorFetch(sv_b)
		#}
		DO
		sv_x = urhk_dbCursorClose(sv_b)
	#}
	ENDIF
	
	EXITSCRIPT
END-->
