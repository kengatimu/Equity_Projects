
#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: TurnOver.scr 
# event: NA
# description: Script to Output General Details called from FI
#=================================================

<--start
trace on



####################################################################
#section      :creating new repository if required
#Description  :Checks for the existence of the repository CUST and
#               creates a new one by that name if it does not exist
#####################################################################

IF (REPEXISTS("CUST") == 0 ) THEN
#{
    CREATEREP("CUST")
    #}
ENDIF

############################################################################
#section      :creating new class if required
#Desscription :checks for the existence of the class DATA in the
#repository #  create a new one if it does not exist
##############################################################################


IF (CLASSEXISTS("CUST","DATA") == 0) THEN
#{
    CREATECLASS("CUST","DATA",5)
#}
ENDIF

############################################
#Assigns the values to the variables
############################################

sv_a = urhk_b2k_PrintRepos("BANCS")
print(sv_a)

CUST.DATA.AcctNumber =BANCS.INPUT.AcctNumber
CUST.DATA.From_date =BANCS.INPUT.From_date
CUST.DATA.To_date =BANCS.INPUT.To_date

BANCS.INPARAM.BINDVARS = CUST.DATA.AcctNumber + "|" + CUST.DATA.AcctNumber
BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId + "|" + CUST.DATA.AcctNumber
BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.DATA.From_date + "|" +  CUST.DATA.To_date
print(BANCS.INPARAM.BINDVARS)

sv_b="uniqueid,AcctNumber,AcctName,TOT_DR, TOT_CR, AVG_CR,AVG_DR, LOW_DR, LOW_CR,HIGH_DR,HIGH_CR, Month |"
sv_b= sv_b+ " select a.acid,b.foracid,b.acct_name, " 
sv_b= sv_b+ " sum(DECODE(PART_TRAN_TYPE,'D',tran_amt,0)) total_dr, "
sv_b= sv_b+ " sum(DECODE(PART_TRAN_TYPE,'C',tran_amt,0)) total_cr, "
sv_b= sv_b+ " avg(DECODE(PART_TRAN_TYPE,'C',tran_amt,0)) avg_cr, "
sv_b= sv_b+ " avg(DECODE(PART_TRAN_TYPE,'D',tran_amt,0)) avg_dr, "
sv_b= sv_b+ " min(DECODE(PART_TRAN_TYPE,'D',tran_amt,0)) low_dr, "
sv_b= sv_b+ " min(DECODE(PART_TRAN_TYPE,'C',tran_amt,0)) low_cr, "
#sv_b= sv_b+ " max(DECODE(PART_TRAN_TYPE,'D',tran_amt,0)) high_dr, "
sv_b= sv_b+ " max(DECODE(PART_TRAN_TYPE,'D',(case when "
sv_b= sv_b+ " (select TRIM(substr(tran_particular,17,6)) chequeno "
sv_b= sv_b+ " from tbaadm.htd where acid in (select acid from tbaadm.gam where foracid =?SVAR) "
sv_b= sv_b+ " and  tran_particular like 'Unpaid ChequeNo.%' and part_tran_type ='C' "
sv_b= sv_b+ " AND substr(tran_particular,17,6)=substr(a.tran_particular,10,6) ) = "
sv_b= sv_b+ " (select trim(substr(tran_particular,10,6)) chequeno from tbaadm.htd "
sv_b= sv_b+ " where acid in (select acid from tbaadm.gam where foracid =?SVAR) "
sv_b= sv_b+ " and  tran_particular like 'ChequeNo.%'  and part_tran_type ='D' and tran_date = a.tran_date  "
sv_b= sv_b+ " AND substr(tran_particular,10,6)=substr(a.tran_particular,10,6)  )  then 0 else  A.tran_amt end),0)) high_dr, "
sv_b= sv_b+ " max(DECODE(PART_TRAN_TYPE,'C',tran_amt,0)) high_cr, "
sv_b= sv_b+ " to_char(a.tran_date,'MON-YYYY') Month  from tbaadm.htd a,tbaadm.gam b "
sv_b= sv_b+ " where a.bank_id= b.bank_id and b.bank_id= ?SVAR and a.acid=b.acid "
sv_b= sv_b+ " and b.foracid= ?SVAR and a.del_flg='N' "
sv_b= sv_b+ " and a.tran_date>= ?SVAR and a.tran_date<= ?SVAR and tran_particular not like  '%Unpaid Cheque%' "
sv_b= sv_b+ " and tran_particular not like  '% Disbursement Credit%' and tran_particular not like '%REJECT:%' "
sv_b= sv_b+ " and TRAN_PARTICULAR not like 'Ac xfr from gl%' and TRAN_PARTICULAR not like 'Loan Payment Reversal For :%' "
sv_b= sv_b+ " group by a.acid,b.foracid,b.acct_name,to_char(a.tran_date,'MON-YYYY') "
print (sv_b)

sv_e = urhk_dbCursorOpenWithBind(sv_b)
IF( sv_e == 0 ) THEN
sv_m = BANCS.OUTPARAM.DB_CURSOR_NUMBER
print(sv_m)
sv_n = urhk_dbCursorFetch(sv_m)

#$$$$$$$$$$$$$$$$

sv_g = 0
CUST.DATA.number = "0"
WHILE(sv_n == 0)
#{
CUST.DATA.uniqueid = BANCS.OUTPARAM.uniqueid
print (CUST.DATA.uniqueid)
CUST.DATA.AcctNumber = BANCS.OUTPARAM.AcctNumber
print (CUST.DATA.AcctNumber)
CUST.DATA.AcctName = BANCS.OUTPARAM.AcctName
 print (CUST.DATA.AcctName)
CUST.DATA.Month = BANCS.OUTPARAM.Month
 print (CUST.DATA.Month)
 CUST.DATA.TOT_DR = BANCS.OUTPARAM.TOT_DR
 print (CUST.DATA.TOT_DR)
 CUST.DATA.HIGH_DR = BANCS.OUTPARAM.HIGH_DR
 print (CUST.DATA.HIGH_DR)
 CUST.DATA.TOT_CR = BANCS.OUTPARAM.TOT_CR
 print (CUST.DATA.TOT_CR)
 CUST.DATA.AVG_CR = BANCS.OUTPARAM.AVG_CR
 print (CUST.DATA.AVG_CR)
 CUST.DATA.AVG_DR = BANCS.OUTPARAM.AVG_DR
 print (CUST.DATA.AVG_DR)
 CUST.DATA.HIGH_CR = BANCS.OUTPARAM.HIGH_CR
 print (CUST.DATA.HIGH_CR)
 CUST.DATA.LOW_DR = BANCS.OUTPARAM.LOW_DR
 print (CUST.DATA.LOW_DR)
 CUST.DATA.LOW_CR = BANCS.OUTPARAM.LOW_CR
 print (CUST.DATA.LOW_CR)

 #################################
 BANCS.INPARAM.BINDVARS =BANCS.STDIN.contextBankId + "|" + CUST.DATA.uniqueid + "|" +  CUST.DATA.Month 
sv_a = "BouncedInh|select sum(BouncedTot) from ( SELECT TRAN_DATE,TRAN_ID,PART_TRAN_TYPE,TRAN_AMT as BouncedTot,TRAN_PARTICULAR ,SUBSTR(TRAN_PARTICULAR, "
sv_a = sv_a + " INSTR(TRAN_PARTICULAR,'.')+1,6) CHQ_NUM, (SELECT TRAN_AMT FROM TBAADM.HTD WHERE TRAN_AMT =A.TRAN_AMT AND SUBSTR(TRAN_PARTICULAR, "
sv_a = sv_a + " INSTR(TRAN_PARTICULAR,'.')+1,6)=SUBSTR(A.TRAN_PARTICULAR,INSTR(A.TRAN_PARTICULAR,'.')+1,6) AND  ACID = A.ACID AND BANk_ID =A.BANK_ID "
sv_a = sv_a + " AND PART_TRAN_TYPE ='D' AND TRAN_PARTICULAR LIKE '%ChequeNo.%' ) ORIG_CREDIT_AMT ,(SELECT TRAN_DATE FROM TBAADM.HTD WHERE "
sv_a = sv_a + " TRAN_AMT =A.TRAN_AMT AND SUBSTR(TRAN_PARTICULAR,INSTR(TRAN_PARTICULAR,'.')+1,6)=SUBSTR(A.TRAN_PARTICULAR,INSTR(A.TRAN_PARTICULAR,'.')+1,6) "
sv_a = sv_a + " AND  ACID = A.ACID AND BANk_ID =A.BANK_ID AND PART_TRAN_TYPE ='D' AND TRAN_PARTICULAR LIKE '%ChequeNo.%') ORIG_CREDIT_TRANDATE, "
sv_a = sv_a + " (SELECT PART_TRAN_TYPE FROM TBAADM.HTD WHERE TRAN_AMT =A.TRAN_AMT AND SUBSTR(TRAN_PARTICULAR,INSTR(TRAN_PARTICULAR,'.')+1,6)= "
sv_a = sv_a + " SUBSTR(A.TRAN_PARTICULAR,INSTR(A.TRAN_PARTICULAR,'.')+1,6) AND ACID = A.ACID AND BANk_ID =A.BANK_ID AND PART_TRAN_TYPE ='D' "
sv_a = sv_a + " AND TRAN_PARTICULAR LIKE '%ChequeNo.%') PART_TRAN_TYPE, (SELECT  SUBSTR(TRAN_PARTICULAR,INSTR(TRAN_PARTICULAR,'.')+1,6) "
sv_a = sv_a + " FROM TBAADM.HTD WHERE TRAN_AMT =A.TRAN_AMT AND SUBSTR(TRAN_PARTICULAR,INSTR(TRAN_PARTICULAR,'.')+1,6)=SUBSTR(A.TRAN_PARTICULAR, "
sv_a = sv_a + " INSTR(A.TRAN_PARTICULAR,'.')+1,6) AND  ACID = A.ACID AND BANk_ID =A.BANK_ID AND PART_TRAN_TYPE ='D' AND TRAN_PARTICULAR LIKE '%ChequeNo.%') CHQ_NUM "
sv_a = sv_a + " FROM TBAADM.HTD A WHERE BANK_ID = ?SVAR AND ACID =?SVAR  AND TRAN_PARTICULAR LIKE '%Unpaid%ChequeNo%' and  to_char(A.tran_date,'MON-YYYY') =?SVAR ORDER BY TRAN_DATE DESC) "
print(sv_a)
sv_f=urhk_dbSelectWithBind(sv_a)
       print(sv_f)
         IF(sv_f==0) then
		CUST.DATA.BouncedInh = cdouble(BANCS.OUTPARAM.BouncedInh)
		print (CUST.DATA.BouncedInh)
		IF (CUST.DATA.BouncedInh =="")THEN
			CUST.DATA.BouncedInh = CDOUBLE("0")
			print (CUST.DATA.BouncedInh)
		ENDIF
	 ELSE 
		CUST.DATA.BouncedInh = CDOUBLE("0")
		print (CUST.DATA.BouncedInh)
	ENDIF

BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + CUST.DATA.uniqueid + "|" +  CUST.DATA.Month 
print(BANCS.INPARAM.BINDVARS)
sv_a = "BouncedTot| select sum(TRAN_AMT) from (SELECT TRAN_DATE,TRAN_ID,PART_TRAN_TYPE,TRAN_AMT,TRAN_PARTICULAR ,SUBSTR(TRAN_PARTICULAR, "
sv_a = sv_a + " INSTR(TRAN_PARTICULAR,':')+1,6) CHQ_NUM,(SELECT TRAN_AMT FROM TBAADM.HTD WHERE TRAN_AMT =A.TRAN_AMT "
sv_a = sv_a + " AND SUBSTR(TRAN_PARTICULAR,INSTR(TRAN_PARTICULAR,':')+1,6)=SUBSTR(A.TRAN_PARTICULAR,INSTR(A.TRAN_PARTICULAR,':')+1,6) "
sv_a = sv_a + " AND  ACID = A.ACID AND BANk_ID =A.BANK_ID AND PART_TRAN_TYPE ='C' AND TRAN_PARTICULAR LIKE '%Chq:%' ) ORIG_CREDIT_AMT, "
sv_a = sv_a + " (SELECT TRAN_DATE FROM TBAADM.HTD WHERE TRAN_AMT =A.TRAN_AMT AND SUBSTR(TRAN_PARTICULAR, "
sv_a = sv_a + " INSTR(TRAN_PARTICULAR,':')+1,6)=SUBSTR(A.TRAN_PARTICULAR,INSTR(A.TRAN_PARTICULAR,':')+1,6) AND  ACID = A.ACID "
sv_a = sv_a + " AND BANk_ID =A.BANK_ID AND PART_TRAN_TYPE ='C' AND TRAN_PARTICULAR LIKE '%Chq:%') ORIG_CREDIT_TRANDATE "
sv_a = sv_a + " ,(SELECT PART_TRAN_TYPE FROM TBAADM.HTD WHERE TRAN_AMT =A.TRAN_AMT AND SUBSTR(TRAN_PARTICULAR, "
sv_a = sv_a + " INSTR(TRAN_PARTICULAR,':')+1,6)=SUBSTR(A.TRAN_PARTICULAR,INSTR(A.TRAN_PARTICULAR,':')+1,6) AND ACID = A.ACID "
sv_a = sv_a + " AND BANk_ID =A.BANK_ID AND PART_TRAN_TYPE ='C' AND TRAN_PARTICULAR LIKE '%Chq:%') PART_TRAN_TYPE "
sv_a = sv_a + " ,(SELECT  SUBSTR(TRAN_PARTICULAR,INSTR(TRAN_PARTICULAR,':')+1,6) FROM TBAADM.HTD WHERE TRAN_AMT =A.TRAN_AMT "
sv_a = sv_a + " AND SUBSTR(TRAN_PARTICULAR,INSTR(TRAN_PARTICULAR,':')+1,6)=SUBSTR(A.TRAN_PARTICULAR,INSTR(A.TRAN_PARTICULAR,':')+1,6) "
sv_a = sv_a + " AND  ACID = A.ACID AND BANk_ID =A.BANK_ID AND PART_TRAN_TYPE ='C' AND TRAN_PARTICULAR LIKE '%Chq:%') CHQ_NUM "
sv_a = sv_a + " FROM TBAADM.HTD A WHERE BANK_ID = ?SVAR AND ACID =?SVAR  AND TRAN_PARTICULAR LIKE '%REJECT:%' and  "
sv_a = sv_a + " to_char(A.tran_date,'MON-YYYY') =?SVAR   ORDER BY TRAN_DATE DESC) "
sv_f=urhk_dbSelectWithBind(sv_a)
       print(sv_f)
         IF(sv_f==0) then
		CUST.DATA.BouncedTot = cdouble(BANCS.OUTPARAM.BouncedTot)
		print (CUST.DATA.BouncedTot)
		IF (CUST.DATA.BouncedTot =="")THEN
			CUST.DATA.BouncedTot = CDOUBLE("0")
			print (CUST.DATA.BouncedTot)
		ENDIF
	 ELSE 
		CUST.DATA.BouncedTot = CDOUBLE("0")
		print (CUST.DATA.BouncedTot)
	ENDIF
BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + CUST.DATA.uniqueid + "|" +  CUST.DATA.Month 
print(BANCS.INPARAM.BINDVARS)
sv_a = "DisbuseAmt |select sum(tran_amt) from tbaadm.htd where bank_id = ?SVAR and acid in (?SVAR) "
sv_a = sv_a + "and  to_char(tran_date,'MON-YYYY') =?SVAR "
sv_a = sv_a + "and TRAN_PARTICULAR like '% Disbursement Credit' and PART_TRAN_TYPE = 'C' "
print(sv_a)
sv_f=urhk_dbSelectWithBind(sv_a)
       print(sv_f)
         IF(sv_f==0) then
		CUST.DATA.DisbuseAmt = cdouble(BANCS.OUTPARAM.DisbuseAmt)
		print (CUST.DATA.DisbuseAmt)
		IF (CUST.DATA.DisbuseAmt =="")THEN
			CUST.DATA.DisbuseAmt = CDOUBLE("0")
			print (CUST.DATA.DisbuseAmt)
		ENDIF
	 ELSE 
		CUST.DATA.DisbuseAmt = CDOUBLE("0")
		print (CUST.DATA.DisbuseAmt)
	ENDIF
BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + CUST.DATA.uniqueid + "|" +  CUST.DATA.Month 
print(BANCS.INPARAM.BINDVARS)
sv_a = "AssetClassAmt |select sum(tran_amt) from tbaadm.htd where bank_id = ?SVAR and acid in (?SVAR) "
sv_a = sv_a + "and  to_char(tran_date,'MON-YYYY') =?SVAR "
sv_a = sv_a + "and TRAN_PARTICULAR like 'Ac xfr from gl%' and PART_TRAN_TYPE = 'C' "
print(sv_a)
sv_f=urhk_dbSelectWithBind(sv_a)
       print(sv_f)
         IF(sv_f==0) then
		CUST.DATA.AssetClassAmt = cdouble(BANCS.OUTPARAM.AssetClassAmt)
		print (CUST.DATA.AssetClassAmt)
		IF (CUST.DATA.AssetClassAmt =="")THEN
			CUST.DATA.AssetClassAmt = CDOUBLE("0")
			print (CUST.DATA.AssetClassAmt)
		ENDIF
	 ELSE 
		CUST.DATA.AssetClassAmt = CDOUBLE("0")
		print (CUST.DATA.AssetClassAmt)
	ENDIF

CUST.DATA.DEBITAMT_DR = (cdouble(CUST.DATA.BouncedTot) + cdouble(CUST.DATA.BouncedInh) + cdouble(CUST.DATA.AssetClassAmt))
PRINT(CUST.DATA.DEBITAMT_DR)
CUST.DATA.DEBITAMT_CR = (cdouble(CUST.DATA.BouncedTot) + cdouble(CUST.DATA.BouncedInh) + cdouble(CUST.DATA.DisbuseAmt) + cdouble(CUST.DATA.AssetClassAmt))
PRINT(CUST.DATA.DEBITAMT_CR)

 CUST.DATA.TOT_DR = cdouble(CUST.DATA.TOT_DR) - cdouble(CUST.DATA.DEBITAMT_DR)
 print (CUST.DATA.TOT_DR)
 CUST.DATA.TOT_CR = cdouble(CUST.DATA.TOT_CR) - cdouble(CUST.DATA.DEBITAMT_CR)
 print (CUST.DATA.TOT_CR)

IF ((cdouble(CUST.DATA.BouncedTot) > cdouble(CUST.DATA.HIGH_DR)) OR (cdouble(CUST.DATA.BouncedInh) > cdouble(CUST.DATA.HIGH_DR)) OR (cdouble(CUST.DATA.AssetClassAmt) > cdouble(CUST.DATA.HIGH_DR))) THEN
	 CUST.DATA.HIGH_DR = cdouble(CUST.DATA.HIGH_DR) 
ENDIF

IF ((cdouble(CUST.DATA.BouncedTot) > cdouble(CUST.DATA.HIGH_CR)) OR (cdouble(CUST.DATA.BouncedInh) > cdouble(CUST.DATA.HIGH_CR)) OR (cdouble(CUST.DATA.DisbuseAmt) > cdouble(CUST.DATA.HIGH_CR)) OR (cdouble(CUST.DATA.AssetClassAmt) > cdouble(CUST.DATA.HIGH_CR))) THEN
	 CUST.DATA.HIGH_CR = cdouble(CUST.DATA.HIGH_CR) 
ENDIF

#####################################

 IF (CUST.DATA.AcctNumber != "") THEN
		#{
			sv_p = "ACCTLISTStruct.<rec_" + CUST.DATA.number + ">.Acct.AcctNumber|" + CUST.DATA.AcctNumber
			print(sv_p)
			sv_u = urhk_SetOrbOut(sv_p)

		ENDIF
		IF (CUST.DATA.AcctName != "") THEN
		#{
			sv_p = "ACCTLISTStruct.<rec_" + CUST.DATA.number + ">.Acct.AcctName|" + CUST.DATA.AcctName
			print(sv_p)
			sv_u = urhk_SetOrbOut(sv_p)

		ENDIF
		IF (CUST.DATA.Month != "") THEN
			#{
				sv_p = "ACCTLISTStruct.<rec_" + CUST.DATA.number + ">.Acct.Month|" + CUST.DATA.Month
				print(sv_p)
				sv_u = urhk_SetOrbOut(sv_p)
			
			
		ENDIF


         IF (CUST.DATA.TOT_DR != "") THEN
			#{
				sv_p = "ACCTLISTStruct.<rec_" + CUST.DATA.number + ">.Acct.TOT_DR|" + CUST.DATA.TOT_DR
				print(sv_p)
				sv_u = urhk_SetOrbOut(sv_p)
			
			
		ENDIF
	IF (CUST.DATA.HIGH_DR != "") THEN
			#{
				sv_p = "ACCTLISTStruct.<rec_" + CUST.DATA.number + ">.Acct.HIGH_DR|" + CUST.DATA.HIGH_DR
				print(sv_p)
				sv_u = urhk_SetOrbOut(sv_p)
			
			
		ENDIF
		IF (CUST.DATA.TOT_CR != "") THEN
			#{
				sv_p = "ACCTLISTStruct.<rec_" + CUST.DATA.number + ">.Acct.TOT_CR|" + CUST.DATA.TOT_CR
				print(sv_p)
				sv_u = urhk_SetOrbOut(sv_p)
			
			
		ENDIF
	IF (CUST.DATA.HIGH_CR != "") THEN
			#{
				sv_p = "ACCTLISTStruct.<rec_" + CUST.DATA.number + ">.Acct.HIGH_CR|" + CUST.DATA.HIGH_CR
				print(sv_p)
				sv_u = urhk_SetOrbOut(sv_p)
			
			
		ENDIF
		sv_n = urhk_dbCursorFetch(sv_m)
sv_g = sv_g + 1
CUST.DATA.number = sv_g
print(CUST.DATA.number)
						
DO
IF(sv_g ==0)THEN
  ###################################
  #check to see if account exists
  #################################

   sv_a="Acid|select ACID from tbaadm.gam where foracid =?SVAR"
    BANCS.INPARAM.BINDVARS = CUST.DATA.AcctNumber
     sv_e=urhk_dbSelectWithBind(sv_a)
       print(sv_e)
         if(sv_e!=0) then
	    sv_o=urhk_SetOrbOut("Error|Account Does not Exist")

sv_n = urhk_dbCursorClose(sv_m)

	    GOTO ENDOFSCRIPT
    #################################################
    #it exists but there is some other unknown errors
    ###############################################
	 ELSE

		CUST.DATA.success="N"
		BANCS.OUTPUT.successOrFailure="F"
		sv_o=urhk_SetOrbOut("Error|No data found")
		GOTO ENDOFSCRIPT
         ENDIF		
ELSE
  sv_n = urhk_dbCursorClose(sv_m)								
								
ENDIF



ENDOFSCRIPT:
EXITSCRIPT
trace off
end-->
