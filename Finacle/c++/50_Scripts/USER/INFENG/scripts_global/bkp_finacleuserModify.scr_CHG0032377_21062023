<--START
	TRACE ON
	
	####################################################################
	# Check whether CUST Repository exists else create it
	####################################################################
		sv_r = REPEXISTS("CUST")
		IF(sv_r == 0) THEN
		#{
			CREATEREP ("CUST")
		#}
		ENDIF
	
	####################################################################
	# Check whether INTERFACE Class exists else create it
	####################################################################
		sv_r = CLASSEXISTS("CUST","INTERFACE")
		IF(sv_r == 0) THEN
		#{
			CREATECLASS ("CUST","INTERFACE",5)
		#}
		ENDIF
		
		CUST.INTERFACE.GOTO = ""
		CUST.INTERFACE.VAL_STATUS = "S"
	####################################################################
	# Inputs from Frontend
	####################################################################
		
		IF(FIELDEXISTS(("BANCS").("INPUT").("EmployeeID")) == 1) THEN
		#{
			CUST.INTERFACE.EmployeeID = ("BANCS").("INPUT").("EmployeeID")
			print("EmployeeID is")
			print(CUST.INTERFACE.EmployeeID)
		#}
		ELSE
		#{
			CUST.INTERFACE.EmployeeID = " "
			PRINT("Please enter EMPLOYEE ID ")
			CUST.INTERFACE.VAL_STATUS = "F"	
			sv_b = "UserMod_Val.MandatoryInput_EmployeeID|Please enter EmployeeID"
			sv_u = urhk_SetOrbOut(sv_b)
		#}
		ENDIF

		IF(FIELDEXISTS(("BANCS").("INPUT").("UserId")) == 1) THEN
		#{
			CUST.INTERFACE.userId = ("BANCS").("INPUT").("UserId")
			print("userId is")
			print(CUST.INTERFACE.userId)
		#}
		ELSE
		#{
			CUST.INTERFACE.userId = " "
			PRINT("Please enter UserId ")
			CUST.INTERFACE.VAL_STATUS = "F"	
			sv_b = "UserMod_Val.MandatoryInput_UserId|Please enter UserId"
			sv_u = urhk_SetOrbOut(sv_b)
		#}
		ENDIF
		
		IF(FIELDEXISTS(("BANCS").("INPUT").("SolID")) == 1) THEN
		#{
			CUST.INTERFACE.SolID = ("BANCS").("INPUT").("SolID")
			print("sol_id is")
			print(CUST.INTERFACE.SolID)
		#}
		ELSE
		#{
			CUST.INTERFACE.SolID = " "
			#PRINT("Please enter SolID ")
			#CUST.INTERFACE.VAL_STATUS = "F"	
			#sv_b = "UserMod_Val.MandatoryInput_SolID|Please enter SolID"
			#sv_u = urhk_SetOrbOut(sv_b)
		#}
		ENDIF
		IF(FIELDEXISTS(("BANCS").("INPUT").("EmailID")) == 1) THEN
		#{
			CUST.INTERFACE.EmailID = ("BANCS").("INPUT").("EmailID")
			print("emp_email_id is")
			print(CUST.INTERFACE.EmailID)
		#}
		ELSE
		#{
			CUST.INTERFACE.EmailID = " "
			#PRINT("Please enter EmailID ")
			#CUST.INTERFACE.VAL_STATUS = "F"	
			#sv_b = "UserMod_Val.MandatoryInput_EmailID|Please enter EmailID"
			#sv_u = urhk_SetOrbOut(sv_b)
		#}
		ENDIF
	###################CORE
		IF(FIELDEXISTS(("BANCS").("INPUT").("WorkClass")) == 1) THEN
		#{
			CUST.INTERFACE.workClass = ("BANCS").("INPUT").("WorkClass")
			print("workClass is")
			print(CUST.INTERFACE.workClass)
		#}
		ELSE
		#{
			CUST.INTERFACE.workClass = " "
			#PRINT("Please enter WorkClass ")
			#CUST.INTERFACE.VAL_STATUS = "F"	
			#sv_b = "UserMod_Val.MandatoryInput_WorkClass|Please enter WorkClass"
			#sv_u = urhk_SetOrbOut(sv_b)
		#}
		ENDIF


		IF(FIELDEXISTS(("BANCS").("INPUT").("UsrRoleId")) == 1) THEN
		#{
			CUST.INTERFACE.usrRoleId = ("BANCS").("INPUT").("UsrRoleId")
			print("usrRoleId is")
			print(CUST.INTERFACE.usrRoleId)
		#}
		ELSE
		#{
			CUST.INTERFACE.usrRoleId = " "
			#PRINT("Please enter UsrRoleId ")
			#CUST.INTERFACE.VAL_STATUS = "F"	
			#sv_b = "UserMod_Val.MandatoryInput_UsrRoleId|Please enter UsrRoleId"
			#sv_u = urhk_SetOrbOut(sv_b)
		#}
		ENDIF
		
	#########SSO
		IF(FIELDEXISTS(("BANCS").("INPUT").("ReqTwoFactorAuth")) == 1) THEN
		#{
			CUST.INTERFACE.reqTwoFactorAuth = ("BANCS").("INPUT").("ReqTwoFactorAuth")
			CUST.INTERFACE.reqTwoFactorAuth = TRIM(CUST.INTERFACE.reqTwoFactorAuth)
			print("reqTwoFactorAuth is")
			print(CUST.INTERFACE.reqTwoFactorAuth)
		#}
		ELSE
		#{
			CUST.INTERFACE.reqTwoFactorAuth = " "
			#PRINT("Please enter reqTwoFactorAuth ")
			#CUST.INTERFACE.VAL_STATUS = "F"	
			#sv_b = "UserMod_Val.MandatoryInput_reqTwoFactorAuth|Please enter reqTwoFactorAuth"
			#sv_u = urhk_SetOrbOut(sv_b)
		#}
		ENDIF

		IF(CUST.INTERFACE.VAL_STATUS == "F")THEN
		#{
		
			sv_a = "UserMod_Val.Status|Missing Mandatory Input"
			sv_u = urhk_SetOrbOut(sv_a)	
			sv_b = "UserMod_Val.ErrorMessage|Please enter the above Mandatory Inputs"
			sv_u = urhk_SetOrbOut(sv_b)
			exitscript
		#}
		ENDIF
		
		sv_s = "homeBankId,usrWrkClass1,roleId1,solId1|SELECT HOME_BANK_ID,USER_WORK_CLASS,ROLE_ID,SOL_ID FROM TBAADM.UPR WHERE USER_ID=?SVAR"
		BANCS.INPARAM.BINDVARS = CUST.INTERFACE.userId 
		PRINT(sv_s)
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		IF(sv_d == 0) THEN
		#{
			CUST.INTERFACE.homeBankId  	=  BANCS.OUTPARAM.homeBankId
			CUST.INTERFACE.usrWrkClass1 =  BANCS.OUTPARAM.usrWrkClass1
			CUST.INTERFACE.roleId1  	=  BANCS.OUTPARAM.roleId1
			CUST.INTERFACE.SolID1  		=  BANCS.OUTPARAM.solId1
		
			PRINT(CUST.INTERFACE.homeBankId)
			PRINT(CUST.INTERFACE.usrWrkClass1)
			PRINT(CUST.INTERFACE.roleId1)
			PRINT(CUST.INTERFACE.SolID1)
		#}
		ELSE
		#{
			
			CUST.INTERFACE.VAL_STATUS = "F"	
			sv_b = "UserMod_Val.MandatoryInput_userId|Invalid User ID"
			sv_u = urhk_SetOrbOut(sv_b)
		#}
		ENDIF
		
		sv_s = "emp_email_id1|select EMP_EMAIL_ID from TBAADM.GET WHERE EMP_ID=?SVAR"
		BANCS.INPARAM.BINDVARS = CUST.INTERFACE.EmployeeID
		PRINT(sv_s)
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		IF(sv_d == 0) THEN
		#{
			CUST.INTERFACE.EmailID1 = BANCS.OUTPARAM.emp_email_id1
			PRINT(CUST.INTERFACE.EmailID1)

		#}
		ELSE
		#{
			
			CUST.INTERFACE.VAL_STATUS = "F"	
			sv_b = "UserMod_Val.MandatoryInput_EmployeeID|Invalid Employee ID"
			sv_u = urhk_SetOrbOut(sv_b)	
		#}
		ENDIF

		IF(CUST.INTERFACE.VAL_STATUS == "F")THEN
		#{
		
			sv_a = "UserMod_Val.Status|Invalid Input"
			sv_u = urhk_SetOrbOut(sv_a)	
			sv_b = "UserMod_Val.ErrorMessage|Please enter the valid Inputs"
			sv_u = urhk_SetOrbOut(sv_b)
			exitscript
		#}
		ENDIF
		
		CUST.INTERFACE.bank_id = CUST.INTERFACE.homeBankId
	
		sv_s = "reqTwoFactorAuth1|select REQ_TWO_FACTOR_AUTH from SSOADM.USER_PROFILE WHERE USER_ID=?SVAR"
		BANCS.INPARAM.BINDVARS = CUST.INTERFACE.userId 
		PRINT(sv_s)
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		IF(sv_d == 0) THEN
		#{
			CUST.INTERFACE.reqTwoFactorAuth1 = BANCS.OUTPARAM.reqTwoFactorAuth1

		#}
		ELSE
		#{
			
			CUST.INTERFACE.reqTwoFactorAuth1 = " "
		#}
		ENDIF			
		PRINT(CUST.INTERFACE.reqTwoFactorAuth1)
	
	####################################################################
	# Inputs - HARDCODED
	####################################################################
	
		CUST.INTERFACE.IsHeadTeller			= "Y" 
		CUST.INTERFACE.viewScheduledRptFlg 	= "O" 
		CUST.INTERFACE.maxInactTime 		= "999"
		CUST.INTERFACE.userLangCode  		= "INFENG"
		CUST.INTERFACE.userBranchAccessCode = "F"
		CUST.INTERFACE.userApplName 		= "GU"
		CUST.INTERFACE.EnableAudit  		= "N"
		CUST.INTERFACE.realUserId 			= CUST.INTERFACE.userId
		CUST.INTERFACE.defaultCalendar 		= "G"
		CUST.INTERFACE.defaultTimeZone 		= "EAT"
		#CUST.INTERFACE.defaultTimeZone 		= "GMT"
		CUST.INTERFACE.userMaxInactiveTime 	= "3"
		CUST.INTERFACE.login_time_low 		= "00:00:01"
		CUST.INTERFACE.login_time_high 		= "23:59:59"
		CUST.INTERFACE.acct_inactive_days 	= "15"
		CUST.INTERFACE.DateFormat 			= "dd/mm/yyyy"
		CUST.INTERFACE.TimeFormat 			= "hh:mm:ss"
		

		CUST.INTERFACE.agentType 			= "Representative"	
	#	CUST.INTERFACE.agentType 			= "Manager"	
		CUST.INTERFACE.defaultChannel 		= "Branch"
		CUST.INTERFACE.homeBankID 			= BANCS.STDIN.contextBankId
		#CUST.INTERFACE.personType 			= "Representative"
		CUST.INTERFACE.personType 			= "Agent"
		CUST.INTERFACE.active 				= "Y"
		CUST.INTERFACE.locale 				= "en_US"
		#CUST.INTERFACE.userType 			= "Representative"
		CUST.INTERFACE.userType 			= "Agent"
		CUST.INTERFACE.time_Zone 			= "EAT+2"
		#CUST.INTERFACE.time_Zone 			= "GMT +02:00"
		CUST.INTERFACE.designation 			= "Officer"
		#CUST.INTERFACE.primaryRoleID 		= "Guest"
		CUST.INTERFACE.primaryRoleID 		= "1"
		CUST.INTERFACE.primarySegment 		= "Preferential Banking"
		
		
		CUST.INTERFACE.EFM_EXISTS 			= "N"
		CUST.INTERFACE.CORE_EXISTS 			= "N"
		CUST.INTERFACE.SSO_EXISTS 			= "N"
		CUST.INTERFACE.CRM_EXISTS 			= "N"
		CUST.INTERFACE.RI_EXISTS 			= "N"
		CUST.INTERFACE.SVS_EXISTS 			= "N"	
		
		#-----------------------------------------------------------------
		# INPUTS - reassigned
		#-----------------------------------------------------------------
		
		
		CUST.INTERFACE.realUserId 		  = CUST.INTERFACE.userId 
		#CUST.INTERFACE.disabled_from_date = CUST.INTERFACE.disabledFromDate
		#CUST.INTERFACE.disabled_upto_date = CUST.INTERFACE.disabledUptoDate
		#CUST.INTERFACE.pw_expy_date 	  = CUST.INTERFACE.pwdExpyDate
		#CUST.INTERFACE.acct_expy_date 	  = CUST.INTERFACE.acctExpyDate
		CUST.INTERFACE.UserID 			  = CUST.INTERFACE.userId
		CUST.INTERFACE.loginId			  = CUST.INTERFACE.userId
		CUST.INTERFACE.defaultEmail 	  = CUST.INTERFACE.EmailID
		CUST.INTERFACE.branchId 		  = CUST.INTERFACE.SolID
		CUST.INTERFACE.empId 			  = CUST.INTERFACE.EmployeeID
		#CUST.INTERFACE.userName			  = CUST.INTERFACE.EmployeeName
		
		#CUST.INTERFACE.lastName 		  = CUST.INTERFACE.EmpShortName
		#CUST.INTERFACE.firstName 		  = CUST.INTERFACE.EmployeeName

		CUST.INTERFACE.wrkclassroleIDFlg = "N"
		
	####################################################################
	# VARIABLE INITAILIZATION
	####################################################################
	
		CUST.INTERFACE.EFM_USER_FLG  = ""
		CUST.INTERFACE.EFM_REMARKS  = ""
		CUST.INTERFACE.CORE_USER_FLG  = ""
		CUST.INTERFACE.CORE_REMARKS  = ""
		CUST.INTERFACE.SSO_USER_FLG  = ""
		CUST.INTERFACE.SSO_REMARKS  = ""
		CUST.INTERFACE.CRM_USER_FLG  = ""
		CUST.INTERFACE.CRM_REMARKS  = ""
		CUST.INTERFACE.SVS_USER_FLG  = ""
		CUST.INTERFACE.SVS_REMARKS  = ""
		CUST.INTERFACE.RI_USER_FLG  = ""
		CUST.INTERFACE.RI_REMARKS  = ""		
		CUST.INTERFACE.RCRE_USERID  = ""
		CUST.INTERFACE.RCRE_TIME  = ""
		CUST.INTERFACE.LCHG_USERID  = ""
		CUST.INTERFACE.LCHG_TIME  = ""
		CUST.INTERFACE.API_NAME  = "FinacleUserMod"
		
   		sv_s = "disabledFromDate,disabledUptoDate,pwdExpyDate,acctExpyDate|select to_char(disabled_from_date,'DD-MM-YYYY'),"
   		sv_s = sv_s + "to_char(disabled_upto_date,'DD-MM-YYYY'),to_char(pw_expy_date,'DD-MM-YYYY'),to_char(acct_expy_date,'DD-MM-YYYY') "
                sv_s = sv_s + "from SSOADM.user_creds_tbl where user_id= ?SVAR"
                BANCS.INPARAM.BINDVARS = CUST.INTERFACE.userId
                PRINT(sv_s)
                PRINT(BANCS.INPARAM.BINDVARS)
                sv_d = urhk_dbSelectWithBind(sv_s)
                IF(sv_d == 0) THEN
                #{

                        CUST.INTERFACE.disabled_from_date       = BANCS.OUTPARAM.disabledFromDate
                        CUST.INTERFACE.disabled_upto_date       = BANCS.OUTPARAM.disabledUptoDate
                        CUST.INTERFACE.pw_expy_date             = BANCS.OUTPARAM.pwdExpyDate
                        CUST.INTERFACE.acct_expy_date           = BANCS.OUTPARAM.acctExpyDate
                #}
                ENDIF

                sv_u = urhk_B2k_getDateDiffFromBODDate(CUST.INTERFACE.pw_expy_date)
                print(sv_u)
                CUST.INTERFACE.dateDiff = BANCS.OUTPARAM.DateDiff

                CUST.INTERFACE.BODDate = MID$(BANCS.STDIN.BODDate,0,10)
                PRINT(CUST.INTERFACE.BODDate)

                IF(CUST.INTERFACE.dateDiff <= "0") THEN
                #{
                        sv_s = "pwexpydate|SELECT ?SVAR + 59 FROM DUAL"
                        BANCS.INPARAM.BINDVARS = CUST.INTERFACE.BODDate
                        PRINT(sv_s)
                        PRINT(BANCS.INPARAM.BINDVARS)
                        sv_d = urhk_dbSelectWithBind(sv_s)
                        IF(sv_d == 0) THEN
                        #{
                                CUST.INTERFACE.pw_expy_date = BANCS.OUTPARAM.pwexpydate
                                PRINT(CUST.INTERFACE.pw_expy_date)
                        #}
                        ENDIF

                #}
                ENDIF

	####################################################################
	# INPUTS from DB
	####################################################################
	
		IF(((TRIM(CUST.INTERFACE.workClass) != "") OR (TRIM(CUST.INTERFACE.usrRoleId) != "")) AND ((CUST.INTERFACE.workClass != CUST.INTERFACE.usrWrkClass1) OR (CUST.INTERFACE.usrRoleId != CUST.INTERFACE.roleId1)))THEN
		#{
			sv_s = "ASSIGN_SSOADMIN,CRE_ASSIGN_SVSSERVER,CRE_ASSIGN_REPORT_INFRA,ASSIGN_ADMIN_WORKFLOW,ASSIGN_FCSE,ASSIGN_FINACLE_INTEGRATOR,"
			sv_s = sv_s + "CRE_ASSIGN_CORE_SERVER,ASSIGN_CRM_SERVICE,CRE_ASSIGN_CRM_SERVER,IS_GLOBAL_ADMIN,USER_GROUP_NAME,SSO_ROLE_NAME|SELECT "
			sv_s = sv_s + " ASSIGN_SSOADMIN,CRE_ASSIGN_SVSSERVER,CRE_ASSIGN_REPORT_INFRA,ASSIGN_ADMIN_WORKFLOW,ASSIGN_FCSE,ASSIGN_FINACLE_INTEGRATOR,"
			sv_s = sv_s + "CRE_ASSIGN_CORE_SERVER,ASSIGN_CRM_SERVICE,CRE_ASSIGN_CRM_SERVER,IS_GLOBAL_ADMIN,USER_GROUP_NAME,SSO_ROLE_NAME FROM CUSTOM.C_FINACLEUSER_MAPPING_TBL"
			sv_s = sv_s + " where ROLE_ID = ?SVAR  and USER_WORK_CLASS = ?SVAR"
			BANCS.INPARAM.BINDVARS = CUST.INTERFACE.usrRoleId + "|" + CUST.INTERFACE.workClass 
			PRINT(sv_s)
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSelectWithBind(sv_s)
			IF(sv_d == 0) THEN
			#{
				CUST.INTERFACE.ASSIGN_SSOADMIN  =  BANCS.OUTPARAM.ASSIGN_SSOADMIN
				CUST.INTERFACE.CRE_ASSIGN_SVSSERVER  =  BANCS.OUTPARAM.CRE_ASSIGN_SVSSERVER
				CUST.INTERFACE.CRE_ASSIGN_REPORT_INFRA  =  BANCS.OUTPARAM.CRE_ASSIGN_REPORT_INFRA
				CUST.INTERFACE.ASSIGN_ADMIN_WORKFLOW  =  BANCS.OUTPARAM.ASSIGN_ADMIN_WORKFLOW
				CUST.INTERFACE.ASSIGN_FCSE  =  BANCS.OUTPARAM.ASSIGN_FCSE
				CUST.INTERFACE.ASSIGN_FINACLE_INTEGRATOR  =  BANCS.OUTPARAM.ASSIGN_FINACLE_INTEGRATOR
				CUST.INTERFACE.CRE_ASSIGN_CORE_SERVER  = BANCS.OUTPARAM.CRE_ASSIGN_CORE_SERVER
				CUST.INTERFACE.ASSIGN_CRM_SERVICE  = BANCS.OUTPARAM.ASSIGN_CRM_SERVICE
				CUST.INTERFACE.CRE_ASSIGN_CRM_SERVER  = BANCS.OUTPARAM.CRE_ASSIGN_CRM_SERVER
				CUST.INTERFACE.isGlobalAdmin = BANCS.OUTPARAM.IS_GLOBAL_ADMIN
				CUST.INTERFACE.USER_GROUP_NAME = BANCS.OUTPARAM.USER_GROUP_NAME
				CUST.INTERFACE.RoleName = BANCS.OUTPARAM.SSO_ROLE_NAME
				PRINT(CUST.INTERFACE.RoleName)
                IF(TRIM(CUST.INTERFACE.RoleName) == "")THEN
                #{
                        sv_a = "UserMod_Val.Status|ERROR"
                        sv_u = urhk_SetOrbOut(sv_a)
                        sv_b = "UserMod_Val.ErrorMessage|SSO Role Name not maintained in mapping table for Role Id and User Work Class."
                        sv_u = urhk_SetOrbOut(sv_b)
                        EXITSCRIPT
                #}
                ENDIF

			#}
			ELSE
			#{
				sv_a = "UserMod_Val.Status|ERROR"
				sv_u = urhk_SetOrbOut(sv_a)	
				sv_b = "UserMod_Val.ErrorMessage| Invalid WorkClass or UserRoleId"
				sv_u = urhk_SetOrbOut(sv_b)
				EXITSCRIPT
			#}
			ENDIF
			CUST.INTERFACE.wrkclassroleIDFlg = "Y"
		#}
		ELSE
		#{
			GOTO FIELDMODIFY
		#}
		ENDIF
		
		
		CUST.INTERFACE.userGroupName= CUST.INTERFACE.USER_GROUP_NAME + "-" + TRIM(CUST.INTERFACE.homeBankId)


			#from table	
	
		CUST.INTERFACE.primaryGroupID = "19"
	
	####################################################################
	# VALUES TO BE FETCHED
	####################################################################
	
		#EFM and CORE INPUT --- CHK  ---->TBA_REFERENCE_CODE_TBL
		
		CUST.INTERFACE.EmpDesig = "OTH"
		
		
		sv_s = "c_authUserId|select distinct max(user_id) from tbaadm.upr where sol_id =?SVAR and del_flg !='Y' and user_work_class !='011' and role_id !='DEL'"
		BANCS.INPARAM.BINDVARS = CUST.INTERFACE.SolID 
		PRINT(sv_s)
		
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		IF(sv_d == 0) THEN
		#{
			CUST.INTERFACE.authUserId  =  BANCS.OUTPARAM.c_authUserId
		#}
		ELSE
		#{
			sv_a = "UserMod_Val.Status_authUserId|ERROR"
			sv_u = urhk_SetOrbOut(sv_a)	
			sv_b = "UserMod_Val.ErrorMessage_authUserId| failed to fetch AuthUserId from mapping table"
			sv_u = urhk_SetOrbOut(sv_b)
			EXITSCRIPT
		#}
		ENDIF
	
		
		#CUST.INTERFACE.ssouser =0
		
		#CALL("UserDel_SSO.scr")
		
		#sv_s = "ssouser|SELECT COUNT(1) FROM SSOADM.USER_PROFILE_TBL WHERE USER_ID=?SVAR AND DEL_FLG='Y'"
	
		#BANCS.INPARAM.BINDVARS = CUST.INTERFACE.userId
		#sv_d = urhk_dbSelectWithBind(sv_s)
		
		#IF(sv_d == 0) THEN
		#{
		#	CUST.INTERFACE.ssouser = BANCS.OUTPARAM.ssouser
		#	PRINT(CUST.INTERFACE.ssouser)
		#}
		#ENDIF 
		
		#IF(CUST.INTERFACE.ssouser != 0)THEN
		#{
			CALL("UserMod_SSO001.scr")
			IF((TRIM(CUST.INTERFACE.SolID) !="") AND (CUST.INTERFACE.SolID !=CUST.INTERFACE.SolID1))THEN
			#{
			
				CALL("UserMod_EFM.scr")
			#}
			ENDIF

			CALL("UserMod_CORE.scr")
			#CALL("UserMod_CRM.scr")
			CALL("UserMod_RI.scr")
			CALL("UserMod_SVS.scr")
		#}
		#ENDIF
		

		FIELDMODIFY:

		
		IF((TRIM(CUST.INTERFACE.reqTwoFactorAuth) !="") AND (CUST.INTERFACE.reqTwoFactorAuth !=CUST.INTERFACE.reqTwoFactorAuth1))THEN
		#{
			
			CALL("UserMod_SSO.scr")
		#}
		ENDIF

		IF(((TRIM(CUST.INTERFACE.EmailID) !="") AND (CUST.INTERFACE.EmailID != CUST.INTERFACE.EmailID1)) OR ((TRIM(CUST.INTERFACE.SolID) !="") AND (CUST.INTERFACE.SolID !=CUST.INTERFACE.SolID1)))THEN
		#{
			
			CALL("UserMod_EFM.scr")
		#}
		ENDIF

		IF((TRIM(CUST.INTERFACE.SolID) !="") AND (CUST.INTERFACE.SolID !=CUST.INTERFACE.SolID1))THEN
		#{
			
			CALL("UserMod_CORE.scr")
		#}
		ENDIF
		
	ENDOFSCRIPT:
	
	CUST.INTERFACE.c_user = "0"
        sv_a = "c_user|SELECT COUNT(*) FROM "
        sv_a = sv_a + " CUSTOM.C_FINACLEUSER_STATUS_TBL WHERE USER_ID = ?SVAR AND API_NAME = ?SVAR"

        BANCS.INPARAM.BINDVARS = CUST.INTERFACE.userId + "|" + CUST.INTERFACE.API_NAME

        sv_d = urhk_dbSelectWithBind(sv_a)

        IF(sv_d == 0) THEN
        #{
	        CUST.INTERFACE.c_user  = BANCS.OUTPARAM.c_user
                PRINT(CUST.INTERFACE.c_user)
        #}
        ELSE
        #{
                sv_a = "UserAdd.Status|ERROR"
                sv_u = urhk_SetOrbOut(sv_a)
                sv_b = "UserAdd.ErrorMessage|Unable to fetch data"
                sv_u = urhk_SetOrbOut(sv_b)
                exitscript


        #}
        ENDIF

	IF(CUST.INTERFACE.c_user == "0")THEN
	#{
		sv_a = "INSERT INTO CUSTOM.C_FINACLEUSER_STATUS_TBL(USER_ID,EMP_ID,BANK_ID,API_NAME,EFM_USER_FLG,EFM_REMARKS,CORE_USER_FLG,CORE_REMARKS,SSO_USER_FLG,"
		sv_a = sv_a +"SSO_REMARKS,CRM_USER_FLG,CRM_REMARKS,SVS_USER_FLG,SVS_REMARKS,RI_USER_FLG,RI_REMARKS,LCHG_USER_ID,LCHG_TIME,RCRE_USER_ID,RCRE_TIME)"
		sv_a = sv_a + "VALUES(?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,sysdate,?SVAR,sysdate)"

		CUST.INTERFACE.RCRE_USER_ID = BANCS.STDIN.userId
		CUST.INTERFACE.LCHG_USERID = BANCS.STDIN.userId
		BANCS.INPARAM.BINDVARS = CUST.INTERFACE.userId + "|" + CUST.INTERFACE.EmployeeID + "|" + CUST.INTERFACE.bank_id + "|" + CUST.INTERFACE.API_NAME + "|" +  CUST.INTERFACE.EFM_USER_FLG
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.INTERFACE.EFM_REMARKS  + "|" + CUST.INTERFACE.CORE_USER_FLG  + "|" + CUST.INTERFACE.CORE_REMARKS 
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.INTERFACE.SSO_USER_FLG + "|" + CUST.INTERFACE.SSO_REMARKS  + "|" +	CUST.INTERFACE.CRM_USER_FLG  
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.INTERFACE.CRM_REMARKS  + "|" + CUST.INTERFACE.SVS_USER_FLG  + "|" + CUST.INTERFACE.SVS_REMARKS
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +	CUST.INTERFACE.RI_USER_FLG + "|" + CUST.INTERFACE.RI_REMARKS + "|" + CUST.INTERFACE.LCHG_USERID 
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +	 CUST.INTERFACE.RCRE_USER_ID 
		PRINT(BANCS.INPARAM.BINDVARS)
		PRINT(CUST.INTERFACE.RI_REMARKS)
		sv_d = urhk_dbSqlWithBind(sv_a)
		IF(sv_d == 0) THEN
		#{
			print("inserted")
		#}
		ELSE
		#{
			sv_a = "STATUS_TABLE_UPDATE.Status|ERROR"
			sv_u = urhk_SetOrbOut(sv_a)	
			sv_b = "STATUS_TABLE_UPDATE.ErrorMessage| FAILED TO UPDATE STATUS TABLE"
			sv_u = urhk_SetOrbOut(sv_b)
			EXITSCRIPT
		#}
		ENDIF
	#}
	ELSE
	#{
		CUST.INTERFACE.LCHG_USERID = BANCS.STDIN.userId
		sv_a = "UPDATE CUSTOM.C_FINACLEUSER_STATUS_TBL SET EFM_USER_FLG = ?SVAR,EFM_REMARKS = ?SVAR,CORE_USER_FLG = ?SVAR,CORE_REMARKS = ?SVAR,SSO_USER_FLG = ?SVAR,"
		sv_a = sv_a +"SSO_REMARKS = ?SVAR,CRM_USER_FLG = ?SVAR,CRM_REMARKS = ?SVAR,SVS_USER_FLG = ?SVAR,SVS_REMARKS = ?SVAR,RI_USER_FLG = ?SVAR,RI_REMARKS = ?SVAR,"
		sv_a = sv_a + "LCHG_USER_ID = ?SVAR,LCHG_TIME = sysdate WHERE USER_ID = ?SVAR AND EMP_ID = ?SVAR AND API_NAME = ?SVAR"
		
		BANCS.INPARAM.BINDVARS =  CUST.INTERFACE.EFM_USER_FLG
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.INTERFACE.EFM_REMARKS  + "|" + CUST.INTERFACE.CORE_USER_FLG  + "|" + CUST.INTERFACE.CORE_REMARKS 
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.INTERFACE.SSO_USER_FLG + "|" + CUST.INTERFACE.SSO_REMARKS  + "|" +	CUST.INTERFACE.CRM_USER_FLG  
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.INTERFACE.CRM_REMARKS  + "|" + CUST.INTERFACE.SVS_USER_FLG  + "|" + CUST.INTERFACE.SVS_REMARKS
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +	CUST.INTERFACE.RI_USER_FLG + "|" + CUST.INTERFACE.RI_REMARKS + "|" + CUST.INTERFACE.LCHG_USERID 
		
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +	CUST.INTERFACE.userId + "|" + CUST.INTERFACE.EmployeeID + "|" + CUST.INTERFACE.API_NAME
	
		PRINT(CUST.INTERFACE.RI_REMARKS)
		print(sv_a)
		print(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSqlWithBind(sv_a)
		IF(sv_d == 0) THEN
		#{
			print("updated")
		#}
		ELSE
		#{
			sv_a = "STATUS_TABLE_UPDATE.Status|ERROR"
			sv_u = urhk_SetOrbOut(sv_a)	
			sv_b = "STATUS_TABLE_UPDATE.ErrorMessage| FAILED TO UPDATE STATUS TABLE"
			sv_u = urhk_SetOrbOut(sv_b)
			EXITSCRIPT
		#}
		ENDIF
	
	#}
	ENDIF
	
	#-------------------------------------------------------------------------
	# INPUT VALIDATION
	#-------------------------------------------------------------------------
	
	#TRACE OFF
END-->
