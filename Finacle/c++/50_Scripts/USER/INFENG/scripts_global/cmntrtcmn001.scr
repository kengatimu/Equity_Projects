#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cmntrtcmn001.scr
# Type: NA|default
# Description: script on click of Go in CMNTRTC
# Author: haneesha.kanumuru
# Date: 5/4/18 8:56 AM
# Modification Log:
#============================================================

<--START
CHECKSUM="6a3f6816b916f785005d6ad106e7ea3ae56e20762abd737d519807eaf24c189e"
	# Script body goes here
	TRACE ON
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CMNTRTC Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CMNTRTC")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CMNTRTC",5)
	#}
	ENDIF
	
	#==========================================================*
	# Intilializing the Variables
	#==========================================================*
	CUST.CMNTRTC.recCount =""
	CUST.CMNTRTC.count =""
	IF(FIELDEXISTS(BANCS.INPUT.funcCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.funcCode) != "")THEN
		#{
			CUST.CMNTRTC.funcCode = TOUPPER(TRIM(BANCS.INPUT.funcCode))
			PRINT(CUST.CMNTRTC.funcCode)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.fromCrncy)) THEN
	#{
		IF(TRIM(BANCS.INPUT.fromCrncy) != "")THEN
		#{
			CUST.CMNTRTC.fromCrncy= TOUPPER(TRIM(BANCS.INPUT.fromCrncy))
			PRINT(CUST.CMNTRTC.fromCrncy)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.toCrncy)) THEN
	#{
		IF(TRIM(BANCS.INPUT.toCrncy) != "")THEN
		#{
			CUST.CMNTRTC.toCrncy= TOUPPER(TRIM(BANCS.INPUT.toCrncy))
			PRINT(CUST.CMNTRTC.toCrncy)
		#}
		ENDIF
	#}
	ENDIF
	#==========================================================*
	#For A-ADD
	#==========================================================*
	IF(CUST.CMNTRTC.funcCode == "A") THEN
	#{
		sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.C_RATECODE"
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		#sv_s = sv_s + " AND CIF_ID=?SVAR"
		sv_s = sv_s + "  AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d != 0)THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
		
		CUST.CMNTRTC.recCount = BANCS.OUTPARAM.recCount
		PRINT(CUST.CMNTRTC.recCount)
		
		sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.C_RATECODE_MOD"
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		#sv_s = sv_s + " AND CIF_ID=?SVAR"
		sv_s = sv_s + "  AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		
		IF(sv_d != 0)THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		CUST.CMNTRTC.count=BANCS.OUTPARAM.recCount
		PRINT(CUST.CMNTRTC.count)
		IF(CINT(CUST.CMNTRTC.count) >0)THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Record already exists ,Verification pending")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		IF(CINT(CUST.CMNTRTC.recCount) > 0)THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Record already exists")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		
	#}
	ENDIF
	sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
	PRINT(sv_u)
	
	
	#==========================================================*
	#For M-MODIFY
	#==========================================================*
	IF(CUST.CMNTRTC.funcCode == "M") THEN
	#{
		sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.C_RATECODE"
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		#sv_s = sv_s + " AND CIF_ID=?SVAR"
		sv_s = sv_s + "  AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d != 0)THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		CUST.CMNTRTC.recCount = BANCS.OUTPARAM.recCount
		PRINT(CUST.CMNTRTC.recCount)
		
		
		sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.C_RATECODE_MOD"
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		#sv_s = sv_s + " AND CIF_ID=?SVAR"
		sv_s = sv_s + "  AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		
		IF(sv_d != 0)THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		CUST.CMNTRTC.count=BANCS.OUTPARAM.recCount
		PRINT(CUST.CMNTRTC.count)
		
		IF((CUST.CMNTRTC.recCount == 0) AND (CUST.CMNTRTC.count == 0))THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Record does not exists")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.C_RATECODE_MOD"
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		#sv_s = sv_s + " AND CIF_ID=?SVAR"
		sv_s = sv_s + "  AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		
		IF(sv_d == 0)THEN
		#{
			CUST.CMNTRTC.count=BANCS.OUTPARAM.recCount
		#}
		ENDIF
		
		
		sv_s = "lchg_user,flg,del_flg| SELECT LCHG_USER_ID,ENTITY_CRE_FLG,DEL_FLG "
		IF(CUST.CMNTRTC.count > 0) THEN
		#{
			sv_s = sv_s + "FROM CUSTOM.C_RATECODE_MOD"
			
			#}
		ELSE
			#{
			sv_s = sv_s + "FROM CUSTOM.C_RATECODE"
		#}
		ENDIF
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		sv_s = sv_s + " AND TO_CURRENCY =?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d==0) THEN
		#{
			
			CUST.CMNTRTC.lchg_user = BANCS.OUTPARAM.lchg_user
			CUST.CMNTRTC.flg = BANCS.OUTPARAM.flg
			CUST.CMNTRTC.delFlg= BANCS.OUTPARAM.del_flg
			PRINT(CUST.CMNTRTC.delFlg)
			PRINT(CUST.CMNTRTC.lchg_user)
		#}
		ENDIF
		
		IF((CUST.CMNTRTC.flg == "N") AND (CUST.CMNTRTC.lchg_user != BANCS.STDIN.userId))THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Last changes are not verified only same user can modify")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
		
		sv_s = "rateCash,rateTran| select RATECODE_CASH,RATECODE_TRAN "
		IF(CUST.CMNTRTC.count > 0) THEN
		#{
			sv_s = sv_s + "FROM CUSTOM.C_RATECODE_MOD"
			
			#}
		ELSE
			#{
			sv_s = sv_s + "FROM CUSTOM.C_RATECODE"
		#}
		ENDIF
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		sv_s = sv_s + " AND TO_CURRENCY =?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		CUST.CMNTRTC.rateCash =BANCS.OUTPARAM.rateCash
		PRINT(CUST.CMNTRTC.rateCash)
		CUST.CMNTRTC.rateTran = BANCS.OUTPARAM.rateTran
		PRINT(CUST.CMNTRTC.rateTran)
		sv_u = urhk_SetOrbOut("rateCodeCash|"+ CUST.CMNTRTC.rateCash)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("rateCodeTransfer|"+ CUST.CMNTRTC.rateTran)
		PRINT(sv_u)
	#}
	ENDIF
	
	#==========================================================*
	#For I-INQUIRE
	#==========================================================*
	IF(CUST.CMNTRTC.funcCode == "I") THEN
	#{
		sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.C_RATECODE"
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		#sv_s = sv_s + " AND CIF_ID=?SVAR"
		sv_s = sv_s + "  AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d != 0)THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		CUST.CMNTRTC.recCount = BANCS.OUTPARAM.recCount
		PRINT(CUST.CMNTRTC.recCount)
		sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.C_RATECODE_MOD"
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		#sv_s = sv_s + " AND CIF_ID=?SVAR"
		sv_s = sv_s + "  AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		
		IF(sv_d != 0)THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		CUST.CMNTRTC.count=BANCS.OUTPARAM.recCount
		PRINT(CUST.CMNTRTC.count)
		
		IF((CUST.CMNTRTC.recCount == 0) AND (CUST.CMNTRTC.count == 0))THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Record does not exists")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.C_RATECODE_MOD"
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		#sv_s = sv_s + " AND CIF_ID=?SVAR"
		sv_s = sv_s + "  AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		
		IF(sv_d == 0)THEN
		#{
			CUST.CMNTRTC.count=BANCS.OUTPARAM.recCount
		#}
		ENDIF
		
		
		sv_s = "lchg_user,flg,del_flg| SELECT LCHG_USER_ID,ENTITY_CRE_FLG,DEL_FLG "
		IF(CUST.CMNTRTC.count > 0) THEN
		#{
			sv_s = sv_s + "FROM CUSTOM.C_RATECODE_MOD"
			
			#}
		ELSE
			#{
			sv_s = sv_s + "FROM CUSTOM.C_RATECODE"
		#}
		ENDIF
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		sv_s = sv_s + " AND TO_CURRENCY =?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d==0) THEN
		#{
			
			CUST.CMNTRTC.lchg_user = BANCS.OUTPARAM.lchg_user
			CUST.CMNTRTC.flg = BANCS.OUTPARAM.flg
			CUST.CMNTRTC.delFlg= BANCS.OUTPARAM.del_flg
			PRINT(CUST.CMNTRTC.delFlg)
			PRINT(CUST.CMNTRTC.lchg_user)
		#}
		ENDIF
		
		IF((CUST.CMNTRTC.flg == "N") AND (CUST.CMNTRTC.lchg_user != BANCS.STDIN.userId))THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Last changes are not verified only same user can Inquire")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
		sv_s = "rateCash,rateTran| select RATECODE_CASH,RATECODE_TRAN "
		IF(CUST.CMNTRTC.count > 0) THEN
		#{
			sv_s = sv_s + "FROM CUSTOM.C_RATECODE_MOD"
			
			#}
		ELSE
			#{
			sv_s = sv_s + "FROM CUSTOM.C_RATECODE"
		#}
		ENDIF
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		sv_s = sv_s + " AND TO_CURRENCY =?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		CUST.CMNTRTC.rateCash =BANCS.OUTPARAM.rateCash
		PRINT(CUST.CMNTRTC.rateCash)
		CUST.CMNTRTC.rateTran = BANCS.OUTPARAM.rateTran
		PRINT(CUST.CMNTRTC.rateTran)
		sv_u = urhk_SetOrbOut("rateCodeCash|"+ CUST.CMNTRTC.rateCash)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("rateCodeTransfer|"+ CUST.CMNTRTC.rateTran)
		PRINT(sv_u)
	#}
	ENDIF
	#==========================================================*
	#For V-VERIFY
	#==========================================================*
	IF(CUST.CMNTRTC.funcCode == "V") THEN
	#{
		sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.C_RATECODE"
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		#sv_s = sv_s + " AND CIF_ID=?SVAR"
		sv_s = sv_s + "  AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d != 0)THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		CUST.CMNTRTC.recCount = BANCS.OUTPARAM.recCount
		PRINT(CUST.CMNTRTC.recCount)
		sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.C_RATECODE_MOD"
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		#sv_s = sv_s + " AND CIF_ID=?SVAR"
		sv_s = sv_s + "  AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		
		IF(sv_d != 0)THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		CUST.CMNTRTC.count=BANCS.OUTPARAM.recCount
		PRINT(CUST.CMNTRTC.count)
		
		IF((CUST.CMNTRTC.recCount == 0) AND (CUST.CMNTRTC.count == 0))THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Record does not exists")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.C_RATECODE_MOD"
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		#sv_s = sv_s + " AND CIF_ID=?SVAR"
		sv_s = sv_s + "  AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		
		IF(sv_d == 0)THEN
		#{
			CUST.CMNTRTC.count=BANCS.OUTPARAM.recCount
		#}
		ENDIF
		
		
		sv_s = "lchg_user,flg,del_flg| SELECT LCHG_USER_ID,ENTITY_CRE_FLG,DEL_FLG "
		IF(CUST.CMNTRTC.count > 0) THEN
		#{
			sv_s = sv_s + "FROM CUSTOM.C_RATECODE_MOD"
			
			#}
		ELSE
			#{
			sv_s = sv_s + "FROM CUSTOM.C_RATECODE"
		#}
		ENDIF
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		sv_s = sv_s + " AND TO_CURRENCY =?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d==0) THEN
		#{
			
			CUST.CMNTRTC.lchg_user = BANCS.OUTPARAM.lchg_user
			CUST.CMNTRTC.flg = BANCS.OUTPARAM.flg
			CUST.CMNTRTC.delFlg= BANCS.OUTPARAM.del_flg
			PRINT(CUST.CMNTRTC.delFlg)
			PRINT(CUST.CMNTRTC.lchg_user)
		#}
		ENDIF
		IF(CUST.CMNTRTC.flg == "Y") THEN
		#{
			#			sv_u = urhk_SetOrbOut("Error|Y")
			#			sv_a = urhk_SetOrbOut("Error_01|Error :^Nothing to verify^acctno")
			sv_a =urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_a)
			sv_u = urhk_SetOrbErr("There is nothing to verify")
			PRINT(sv_u)
			
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		
		IF(CUST.CMNTRTC.lchg_user == BANCS.STDIN.userId) THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Same user cannot verify")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
		sv_s = "rateCash,rateTran| select RATECODE_CASH,RATECODE_TRAN "
		IF(CUST.CMNTRTC.count > 0) THEN
		#{
			sv_s = sv_s + "FROM CUSTOM.C_RATECODE_MOD"
			
			#}
		ELSE
			#{
			sv_s = sv_s + "FROM CUSTOM.C_RATECODE"
		#}
		ENDIF
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		sv_s = sv_s + " AND TO_CURRENCY =?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		CUST.CMNTRTC.rateCash =BANCS.OUTPARAM.rateCash
		PRINT(CUST.CMNTRTC.rateCash)
		CUST.CMNTRTC.rateTran = BANCS.OUTPARAM.rateTran
		PRINT(CUST.CMNTRTC.rateTran)
		sv_u = urhk_SetOrbOut("rateCodeCash|"+ CUST.CMNTRTC.rateCash)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("rateCodeTransfer|"+ CUST.CMNTRTC.rateTran)
		PRINT(sv_u)
		
	#}
	ENDIF
	
	#==========================================================*
	#For X-CANCEL
	#==========================================================*
	IF(CUST.CMNTRTC.funcCode == "X") THEN
	#{
		sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.C_RATECODE"
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		#sv_s = sv_s + " AND CIF_ID=?SVAR"
		sv_s = sv_s + "  AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d != 0)THEN
		#{
			
		#}
		ENDIF
		
		CUST.CMNTRTC.recCount = BANCS.OUTPARAM.recCount
		PRINT(CUST.CMNTRTC.recCount)
		sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.C_RATECODE_MOD"
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		#sv_s = sv_s + " AND CIF_ID=?SVAR"
		sv_s = sv_s + "  AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		
		IF(sv_d != 0)THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		CUST.CMNTRTC.count=BANCS.OUTPARAM.recCount
		PRINT(CUST.CMNTRTC.count)
		
		IF((CUST.CMNTRTC.recCount == 0) AND (CUST.CMNTRTC.count == 0))THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Record does not exists")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.C_RATECODE_MOD"
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		#sv_s = sv_s + " AND CIF_ID=?SVAR"
		sv_s = sv_s + "  AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		
		IF(sv_d == 0)THEN
		#{
			CUST.CMNTRTC.count=BANCS.OUTPARAM.recCount
		#}
		ENDIF
		
		
		sv_s = "lchg_user,flg,del_flg| SELECT LCHG_USER_ID,ENTITY_CRE_FLG,DEL_FLG "
		IF(CUST.CMNTRTC.count > 0) THEN
		#{
			sv_s = sv_s + "FROM CUSTOM.C_RATECODE_MOD"
			
			#}
		ELSE
			#{
			sv_s = sv_s + "FROM CUSTOM.C_RATECODE"
		#}
		ENDIF
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		sv_s = sv_s + " AND TO_CURRENCY =?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d==0) THEN
		#{
			
			CUST.CMNTRTC.lchg_user = BANCS.OUTPARAM.lchg_user
			CUST.CMNTRTC.flg = BANCS.OUTPARAM.flg
			CUST.CMNTRTC.delFlg= BANCS.OUTPARAM.del_flg
			PRINT(CUST.CMNTRTC.delFlg)
			PRINT(CUST.CMNTRTC.lchg_user)
		#}
		ENDIF
		IF((CUST.CMNTRTC.flg !="N")) THEN
		#{
			sv_m = urhk_SetOrbErr("Noting to Cancel")
			sv_z = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		IF((CUST.CMNTRTC.lchg_user != BANCS.STDIN.userId)) THEN
		#{
			sv_m = urhk_SetOrbErr("Last changes are not verified only same user can Cancel")
			sv_z = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		sv_s = "rateCash,rateTran| select RATECODE_CASH,RATECODE_TRAN "
		IF(CUST.CMNTRTC.count > 0) THEN
		#{
			sv_s = sv_s + "FROM CUSTOM.C_RATECODE_MOD"
			
			#}
		ELSE
			#{
			sv_s = sv_s + "FROM CUSTOM.C_RATECODE"
		#}
		ENDIF
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		sv_s = sv_s + " AND TO_CURRENCY =?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		CUST.CMNTRTC.rateCash =BANCS.OUTPARAM.rateCash
		PRINT(CUST.CMNTRTC.rateCash)
		CUST.CMNTRTC.rateTran = BANCS.OUTPARAM.rateTran
		PRINT(CUST.CMNTRTC.rateTran)
		sv_u = urhk_SetOrbOut("rateCodeCash|"+ CUST.CMNTRTC.rateCash)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("rateCodeTransfer|"+ CUST.CMNTRTC.rateTran)
		PRINT(sv_u)
		
	#}
	ENDIF
	#==========================================================*
	#ForD-DELETE
	#==========================================================*
	IF(CUST.CMNTRTC.funcCode == "D") THEN
	#{
		sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.C_RATECODE"
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		#sv_s = sv_s + " AND CIF_ID=?SVAR"
		sv_s = sv_s + "  AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d != 0)THEN
		#{
			
		#}
		ENDIF
		
		CUST.CMNTRTC.recCount = BANCS.OUTPARAM.recCount
		PRINT(CUST.CMNTRTC.recCount)
		sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.C_RATECODE_MOD"
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		#sv_s = sv_s + " AND CIF_ID=?SVAR"
		sv_s = sv_s + "  AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		
		IF(sv_d != 0)THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		CUST.CMNTRTC.count=BANCS.OUTPARAM.recCount
		PRINT(CUST.CMNTRTC.count)
		
		IF((CUST.CMNTRTC.recCount == 0) AND (CUST.CMNTRTC.count == 0))THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Record does not exists")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.C_RATECODE_MOD"
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		#sv_s = sv_s + " AND CIF_ID=?SVAR"
		sv_s = sv_s + "  AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		
		IF(sv_d == 0)THEN
		#{
			CUST.CMNTRTC.count=BANCS.OUTPARAM.recCount
		#}
		ENDIF
		sv_s = "lchg_user,flg,del_flg| SELECT LCHG_USER_ID,ENTITY_CRE_FLG,DEL_FLG "
		IF(CUST.CMNTRTC.count > 0) THEN
		#{
			sv_s = sv_s + "FROM CUSTOM.C_RATECODE_MOD"
			
			#}
		ELSE
			#{
			sv_s = sv_s + "FROM CUSTOM.C_RATECODE"
		#}
		ENDIF
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		sv_s = sv_s + " AND TO_CURRENCY =?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d==0) THEN
		#{
			
			CUST.CMNTRTC.lchg_user = BANCS.OUTPARAM.lchg_user
			CUST.CMNTRTC.flg = BANCS.OUTPARAM.flg
			CUST.CMNTRTC.delFlg= BANCS.OUTPARAM.del_flg
			PRINT(CUST.CMNTRTC.delFlg)
			PRINT(CUST.CMNTRTC.lchg_user)
		#}
		ENDIF
		
		IF((CUST.CMNTRTC.flg == "N"))THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Last changes are not verified")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
		sv_s = "rateCash,rateTran| select RATECODE_CASH,RATECODE_TRAN from CUSTOM.C_RATECODE"
		sv_s = sv_s + " WHERE FROM_CURRENCY = ?SVAR"
		sv_s = sv_s + " AND TO_CURRENCY =?SVAR AND BANK_ID = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CMNTRTC.fromCrncy + "|" + CUST.CMNTRTC.toCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		CUST.CMNTRTC.rateCash =BANCS.OUTPARAM.rateCash
		PRINT(CUST.CMNTRTC.rateCash)
		CUST.CMNTRTC.rateTran = BANCS.OUTPARAM.rateTran
		PRINT(CUST.CMNTRTC.rateTran)
		sv_u = urhk_SetOrbOut("rateCodeCash|"+ CUST.CMNTRTC.rateCash)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("rateCodeTransfer|"+ CUST.CMNTRTC.rateTran)
		PRINT(sv_u)
	#}
	ENDIF
	sv_z = urhk_SetOrbOut("SuccessOrFailure|Y")
	PRINT(sv_z)
	ENDOFSCRIPT:
		TRACE OFF
END-->
