##################################################################################################################################
#Script Name    : wthmn027.scr
#Start Date     : 20-June-2013
#Description    : This script is called on click of go button on crit page for menus OISA,OICA,OITUA AND OCMIDA
#				  And this script will fetch value for custom field "Withholding Tax Applicable" on scheme tab. 		 
#Input para     : None
#Output para    : None
#Author         : Nitin_Kumar16
#Called Script  : NA
#Calling Script : SRV_FetchDfltCAAcctDtls_post_process_data.scr,SRV_FetchDfltSBAcctDtls_post_process_data.scr
#				  SRV_FetchDfltTUAcctDtls_post_process_data.scr
#Reference      : None
#Modification History:
#Srl No.  		Date         		Author          		Description
#-------  		-----------  		---------   			--------------
#   1.0   		20-June-2013 		Nitin_Kumar16      		Original version
##################################################################################################################################
<--start

TRACE ON
	#========================================================
	# Check whether CUST Repository exists else create it
	#========================================================
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	#========================================================
	# Check whether CMITUGSP Class exists else create it
	#========================================================
	sv_r = CLASSEXISTS("CUST","CMGSP")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CMGSP",5)
	#}
	ENDIF
	CUST.CMGSP.bankId = " "
	CUST.CMGSP.errorFlg="N"
	CUST.CMGSP.errorMsg=" "
	
	IF(FIELDEXISTS(BANCS.STDIN.contextBankId))THEN
	#{
		CUST.CMGSP.bankId=BANCS.STDIN.contextBankId
		PRINT(CUST.CMGSP.bankId)
	#}
	ELSE
	#{
		CUST.CMGSP.errorFlg = "Y"
		CUST.CMGSP.errorMsg = "Error in fetching bank id"
	
	#}
	ENDIF
	
	
	ENDOFSCRIPT:
			sv_s = "errorFlg|" + CUST.CMGSP.errorFlg
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			
			sv_s = "errorMsg|" + CUST.CMGSP.errorMsg
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			
			sv_s = "bankId|" + CUST.CMGSP.bankId
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
end-->
