#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cietxndp199end.scr
# Type: NA|default
# Description: a
# Author: Balaji_P05
# Date: 3/27/20 3:08 PM
# Modification Log:
#============================================================

<--START
	TRACE ON
	sv_a = urhk_B2K_PrintRepos("BANCS")
	PRINT(sv_a)
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CIETXN Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CIETXN")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CIETXN",5)
	#}
	ENDIF


CUST.CIETXN.signUrl="NA"

sv_s = "endPointUrl|"
sv_s = sv_s + " SELECT SIGN_URL"
sv_s = sv_s + " FROM custom.Inter_entity "
sv_s = sv_s + " WHERE ENTITY_ID = ?SVAR"
sv_s = sv_s + " AND DEL_FLG != 'Y'"
PRINT(sv_s)
BANCS.INPARAM.BINDVARS = BANCS.INPUT.signEntityId
PRINT(BANCS.INPARAM.BINDVARS)
sv_d = urhk_dbSelectWithBind(sv_s)
PRINT(sv_d)

IF(sv_d == 0)THEN
#{
	CUST.CIETXN.signUrl = BANCS.OUTPARAM.endPointUrl
	PRINT(CUST.CIETXN.signUrl)
#}
ENDIF
sv_b=urhk_setOrbOut("signUrl|"+CUST.CIETXN.signUrl)
PRINT(sv_b)
END-->
