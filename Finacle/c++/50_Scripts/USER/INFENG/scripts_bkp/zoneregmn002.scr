#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: zoneregmn002.scr
# Type: NA|default
# Description: zoneregmn002.scr
# Author:
# Date:
# Modification Log:
#============================================================

#-------------------------------------------------------------------------------------
#    Name				: zoneregmn002.scr
#    Description     	: This script is execute through batch job RGZN2 to regularize
#						: prompt zone
#    Date            	: 01-01-2013
#    Author          	: Divya
#    Input           	: NA
#    Output				: NA
#    Called Script	 	: NA
#    Calling Script	 	: getcparamvalue.scr
#    Menu Option		: HBJSTM, HBJE
#    Srl. No		Date			Author			Description.
#    -------		----			------			----------------
#    1.0			01-01-2013		Divya	 		Original Version
#    2.0			23-02-2013      Divya			Added Rate Code
#-------------------------------------------------------------------------------------
<--START
CHECKSUM="1c26c58ba4698e7675a03909bf0b7781e386d85846e3748d9499ab8b41134f78"
	TRACE ON
	
	#------------------------------------------------------------------------------
	# Creating repository and class
	# Initializing the variables
	# Getting all the values from CPARAM
	#------------------------------------------------------------------------------
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST","PRREG") == 0) THEN
	#{
		CREATECLASS("CUST","PRREG",5)
	#}
	ENDIF
	
	CUST.PRREG.errMsg    = ""
	CUST.PRREG.crAccount = ""
	CUST.PRREG.drAccount = ""
	CUST.PRREG.zoneCode  = ""
	CUST.PRREG.zoneSolid = ""
	CUST.PRREG.desc      = ""
	CUST.PRREG.amount    = "0"
	CUST.PRREG.zoneDate  = ""
	
	BANCS.INPUT.moduleName  = "ZONEREG"
	BANCS.INPUT.parameterId = "PRREG_STAN"
	
	CALL ("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.PRREG.crAccount = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		CUST.PRREG.errMsg = "Credit Account setup not done CPARAM"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	PRINT(CUST.PRREG.crAccount)
	BANCS.INPUT.moduleName  = "ZONEREG"
	BANCS.INPUT.parameterId = "PRREG_PARK"
	
	CALL ("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.PRREG.drAccount = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		CUST.PRREG.errMsg = "Debit Account setup not done CPARAM"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	PRINT(CUST.PRREG.drAccount)
	BANCS.INPUT.moduleName  = "ZONEREG"
	BANCS.INPUT.parameterId = "PRREG_CODE"
	
	CALL ("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.PRREG.zoneCode = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		CUST.PRREG.errMsg = "Zone Setup not done CPARAM"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	PRINT(CUST.PRREG.zoneCode)
	BANCS.INPUT.moduleName  = "ZONEREG"
	BANCS.INPUT.parameterId = "PRREG_SOLID"
	
	CALL ("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.PRREG.zoneSolid = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		CUST.PRREG.errMsg = "Zone Sol Id not done CPARAM"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	PRINT(CUST.PRREG.zoneSolid)
	BANCS.INPUT.moduleName  = "ZONEREG"
	BANCS.INPUT.parameterId = "PRREG_DESC"
	
	CALL ("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.PRREG.desc = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		CUST.PRREG.desc = ""
	#}
	ENDIF
	
	PRINT(CUST.PRREG.desc)
	BANCS.INPUT.moduleName  = "GENERAL"
	BANCS.INPUT.parameterId = "NORRATECODE"
	
	CALL ("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.PRREG.refCode = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		CUST.PRREG.errMsg = "NORRATECODE Code not done CPARAM"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	#------------------------------------------------------------------------------
	# Getting zoneDate, amount
	# Creating narration for tran remarks
	#------------------------------------------------------------------------------
	sv_s = "zoneDate,amount|SELECT clg_zone_date,SUM(instrmnt_amt)"
	sv_s = sv_s + " FROM TBAADM.OCI"
	sv_s = sv_s + " WHERE clg_zone_code = ?SVAR AND"
	sv_s = sv_s + " value_date =  TO_DATE(?SVAR ,'DD-MM-YYYY HH24:MI:SS') AND"
	sv_s = sv_s + " sol_id = ?SVAR AND"
	sv_s = sv_s + " status_flg = 'G' AND"
	sv_s = sv_s + " bank_id = ?SVAR"
	sv_s = sv_s + " GROUP BY sol_id, clg_zone_date, clg_zone_code"
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.PRREG.zoneCode + "|" + BANCS.STDIN.BODDate + "|" + CUST.PRREG.zoneSolid + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_d =  urhk_dbselectWithBind(sv_s)
	PRINT(sv_d)
	IF (sv_d == 0) THEN
	#{
		CUST.PRREG.amount = BANCS.OUTPARAM.amount
		PRINT(CUST.PRREG.amount)
		CUST.PRREG.zoneDate = BANCS.OUTPARAM.zoneDate
		PRINT(CUST.PRREG.zoneDate)
		#}
	ELSE
		#{
		CUST.PRREG.amount = "0"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	CUST.PRREG.narr = CUST.PRREG.zoneDate + " " + CUST.PRREG.desc
	PRINT(CUST.PRREG.narr)
	
	#---------------------------------------------------------------
	# Passing transaction inputs to the service
	# Adding and Posting the transaction
	#---------------------------------------------------------------
	sv_u = urhk_SetUrhkInp("tranHdr.tranIdentifier.tranDate|"+BANCS.STDIN.BODDate)
	sv_u = urhk_SetUrhkInp("tranHdr.tranTypeSubType.tranType|T")
	sv_u = urhk_SetUrhkInp("tranHdr.tranTypeSubType.tranSubType|BI")
	sv_u = urhk_SetUrhkInp("tranHdr.tranRmks|"+CUST.PRREG.narr)
	sv_u = urhk_SetUrhkInp("tranDtl.ignoreXcpnFlg|I")
	sv_u = urhk_SetUrhkInp("tranDtl.pstFlg|Y")
	
	sv_w = "tranDtl.partTranDetailLL.<rec_0>."
	sv_u = urhk_SetUrhkInp(sv_w + "key.serial_num|1")
	sv_u = urhk_SetUrhkInp(sv_w + "pTranIdentifier.tranDate|"+BANCS.STDIN.BODDate)
	sv_u = urhk_SetUrhkInp(sv_w + "partTranType.code|C")
	sv_u = urhk_SetUrhkInp(sv_w + "acctId.foracid|"+CUST.PRREG.crAccount)
	sv_u = urhk_SetUrhkInp(sv_w + "tranParticulars|"+CUST.PRREG.narr)
	sv_u = urhk_SetUrhkInp(sv_w + "tranRmks|"+CUST.PRREG.narr)
	sv_u = urhk_SetUrhkInp(sv_w + "refNum| ")
	sv_u = urhk_SetUrhkInp(sv_w + "valueDate|"+BANCS.STDIN.BODDate)
	sv_u = urhk_SetUrhkInp(sv_w + "glDate|"+BANCS.STDIN.BODDate)
	sv_u = urhk_SetUrhkInp(sv_w + "tranAmt|"+CUST.PRREG.amount+"|"+ BANCS.STDIN.homeCrncyCode)
	sv_u = urhk_SetUrhkInp(sv_w + "refCrncyCode.crncyCode|" + BANCS.STDIN.homeCrncyCode)
	sv_u = urhk_SetUrhkInp(sv_w + "refAmt|"+CUST.PRREG.amount+"|"+BANCS.STDIN.homeCrncyCode)
	sv_u = urhk_SetUrhkInp(sv_w + "entryUserId.userId|" + BANCS.STDIN.userId)
	sv_u = urhk_SetUrhkInp(sv_w + "vfdUserId.userId|" + BANCS.STDIN.userId)
	sv_u = urhk_SetUrhkInp(sv_w + "rateCode.refCode|" + CUST.PRREG.refCode)
	
	sv_w = "tranDtl.partTranDetailLL.<rec_1>."
	sv_u = urhk_SetUrhkInp(sv_w + "key.serial_num|2")
	sv_u = urhk_SetUrhkInp(sv_w + "pTranIdentifier.tranDate|"+BANCS.STDIN.BODDate)
	sv_u = urhk_SetUrhkInp(sv_w + "partTranType.code|D")
	sv_u = urhk_SetUrhkInp(sv_w + "acctId.foracid|"+CUST.PRREG.drAccount)
	sv_u = urhk_SetUrhkInp(sv_w + "tranParticulars|"+CUST.PRREG.narr)
	sv_u = urhk_SetUrhkInp(sv_w + "tranRmks|"+CUST.PRREG.narr)
	sv_u = urhk_SetUrhkInp(sv_w + "refNum| ")
	sv_u = urhk_SetUrhkInp(sv_w + "valueDate|"+BANCS.STDIN.BODDate)
	sv_u = urhk_SetUrhkInp(sv_w + "glDate|"+BANCS.STDIN.BODDate)
	sv_u = urhk_SetUrhkInp(sv_w + "tranAmt|"+CUST.PRREG.amount+"|"+BANCS.STDIN.homeCrncyCode)
	sv_u = urhk_SetUrhkInp(sv_w + "tranParticulars|"+CUST.PRREG.narr)
	sv_u = urhk_SetUrhkInp(sv_w + "refCrncyCode.crncyCode|"+BANCS.STDIN.homeCrncyCode)
	sv_u = urhk_SetUrhkInp(sv_w + "refAmt|"+CUST.PRREG.amount+"|"+BANCS.STDIN.homeCrncyCode)
	sv_u = urhk_SetUrhkInp(sv_w + "entryUserId.userId|" + BANCS.STDIN.userId)
	sv_u = urhk_SetUrhkInp(sv_w + "vfdUserId.userId|" + BANCS.STDIN.userId)
	sv_u = urhk_SetUrhkInp(sv_w + "rateCode.refCode|" + CUST.PRREG.refCode)
	
	sv_u = urhk_ExecSrv("SRV_AddXferTran|retain_all_output = Y")
	PRINT(sv_u)
	IF (sv_u != 0) THEN
	#{
		sv_u = urhk_CopyOutToIn("")
		sv_u = urhk_ExecSrv("SRV_AddXferTran|retain_all_output = Y")
		PRINT(sv_u)
	#}
	ENDIF
	
	IF (sv_u == 0) THEN
	#{
		sv_u = urhk_CopyOutToIn("")
		sv_u = urhk_ExecSrv("SRV_FetchTranDtlsForPost|retain_all_output = Y")
		PRINT(sv_u)
		IF (sv_u == 0) THEN
		#{
			sv_u = urhk_CopyOutToIn("")
			sv_u = urhk_SetUrhkOut("tranDtl.tranIdentifier.tranId|tranId")
			sv_u = urhk_SetUrhkOut("tranDtl.tranIdentifier.tranDate|tranDate")
			sv_u = urhk_ExecSrv("SRV_PostTran|retain_all_output = Y")
			PRINT(sv_u)
			IF (sv_u != 0 ) THEN
			#{
				sv_u = urhk_CopyOutToIn("")
				sv_u = urhk_SetUrhkOut("tranDtl.tranIdentifier.tranId|tranId")
				sv_u = urhk_SetUrhkOut("tranDtl.tranIdentifier.tranDate|tranDate")
				sv_u = urhk_ExecSrv("SRV_PostTran|retain_all_output = Y")
				PRINT(sv_u)
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	IF (sv_u != 0) THEN
	#{
		IF (FIELDEXISTS(BANCS.OUTPARAM.Error_num) == 1) THEN
		#{
			PRINT(BANCS.OUTPARAM.Error_num)
			sv_v = BANCS.OUTPARAM.Error_num
			#}
		ELSE
			#{
			sv_v = 0
		#}
		ENDIF
		IF (sv_v > 0) THEN
		#{
			sv_i = 1
			WHILE (sv_i <= sv_v)
			#{
				sv_f = FORMAT$(sv_i,"%d")
				sv_e = ("BANCS").("OUTPARAM").("ErrorMesg_"+sv_f)
				PRINT(sv_e)
				sv_e = ("BANCS").("OUTPARAM").("ErrorCode_"+sv_f)
				PRINT(sv_e)
				sv_e = ("BANCS").("OUTPARAM").("Tag_"+sv_f)
				PRINT(sv_e)
				sv_i = sv_i + 1
			#}
			DO
		#}
		ENDIF
	#}
	ENDIF
	
	IF (FIELDEXISTS(BANCS.OUTPARAM.tranId) == 1) THEN
	#{
		PRINT(BANCS.OUTPARAM.tranId)
	#}
	ENDIF
	
	ENDOFSCRIPT:
		IF (CUST.PRREG.errMsg != "") THEN
		#{
			sv_u = SYSTEM("echo '"+CUST.PRREG.errMsg+"' >>RGZN2.log")
		#}
		ENDIF
		
		TRACE OFF
		EXITSCRIPT
END-->
