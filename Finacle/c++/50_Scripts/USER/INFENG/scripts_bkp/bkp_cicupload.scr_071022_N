########################################################################################
# Source Name         			: cicupload.scr
# Author              			: Katumo
# Date                			: 06-OCT-2022
# Title               			: Bulk Uplaod transfer through uploads
# Description         			: This Script selects records from uploaded CSV file and read the record

#   <Serial No.>     	<Date>         <Author Name>			<Description>
#	1		06-OCT-2022	Katumo			Original version
#########################################################################################
<--start
trace on
CUST.CATOD.BANK_ID  =BANCS.STDIN.contextBankId
CUST.CATOD.DEL_FLG = "N"

	#------------------------------------------------------------------------------------------
	# IF Function Value is "U"
	#------------------------------------------------------------------------------------------
	
	IF (CUST.CATOD.Function=="U")  THEN
	#{

		sv_a=" Insert into CUSTOM.C_CIC "
		sv_a=sv_a + " (ACCTNUM, REFNUM, NAME, DEL_FLG, BANK_ID) "
		sv_a=sv_a + " Values "
		sv_a=sv_a + " (?SVAR,?SVAR,?SVAR,?SVAR,?SVAR) "
		print(sv_a)
		BANCS.INPARAM.BINDVARS=CUST.CATOD.ACCTNUM +"|"+ CUST.CATOD.REFNUM +"|"+ CUST.CATOD.name
		BANCS.INPARAM.BINDVARS=BANCS.INPARAM.BINDVARS +"|"+ CUST.CATOD.DEL_FLG +"|"+ CUST.CATOD.BANK_ID 
		print(BANCS.INPARAM.BINDVARS)
		sv_b = urhk_dbsqlWithBind(sv_a)

		IF (sv_b != 0)  THEN
		#{
			sv_v = "N"
			sv_c = sv_c + 1
			CUST.CATOD.errColl = CUST.CATOD.errColl + "~" + "ref num " + CUST.CATOD.REFNUM + " already exists"
			GOTO ENDOFSCRIPT
			#}
		ELSE
			sv_c = urhk_dbSQLWithBind("commit")
			print(sv_c)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	ENDOFSCRIPT:
		EXITSCRIPT
trace off

end-->