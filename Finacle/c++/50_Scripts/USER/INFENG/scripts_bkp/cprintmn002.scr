#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cprintmn002.scr
# Type: NA|default
# Description: script on click of Submit
# Author: haneesha.kanumuru
# Date: 8/22/18 4:50 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="4b6b2d5be603691dc18b489da3b5b40f34a01d9e09af0377e289ec4d63215d04"
	# Script body goes here
	TRACE ON
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CPRINT Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CPRINT")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CPRINT",5)
	#}
	ENDIF
	#==========================================================*
	# Intilializing the Variables
	#==========================================================*
	CUST.CPRINT.recCount =""
	CUST.CPRINT.count =""
	CUST.CPRINT.paramVal=""
	sv_u = urhk_B2k_PrintRepos("BANCS")
	IF(FIELDEXISTS(BANCS.INPUT.funcCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.funcCode) != "")THEN
		#{
			CUST.CPRINT.funcCode = TOUPPER(TRIM(BANCS.INPUT.funcCode))
			PRINT(CUST.CPRINT.funcCode)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.roleId)) THEN
	#{
		IF(TRIM(BANCS.INPUT.roleId) != "")THEN
		#{
			CUST.CPRINT.roleId= TOUPPER(TRIM(BANCS.INPUT.roleId))
			PRINT(CUST.CPRINT.roleId)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.prIntrst)) THEN
	#{
		IF(TRIM(BANCS.INPUT.prIntrst) != "")THEN
		#{
			CUST.CPRINT.prIntrst= TOUPPER(TRIM(BANCS.INPUT.prIntrst))
			PRINT(CUST.CPRINT.prIntrst)
		#}
		ENDIF
	#}
	ENDIF
	
	
	#==========================================================*
	#For A-ADD
	#==========================================================*
	IF(CUST.CPRINT.funcCode == "A") THEN
	#{
		sv_s = "insert into custom.c_prefint_mod(ROLE_ID,PREF_INTEREST,DEL_FLG,ENTITY_CRE_FLG,RCRE_USER_ID,RCRE_TIME,LCHG_USER_ID,LCHG_TIME,BANK_ID)"
		sv_s = sv_s + " values(?SVAR,?SVAR,'N','N',?SVAR,sysdate,?SVAR,sysdate,?SVAR)"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS=CUST.CPRINT.roleId + "|" + CUST.CPRINT.prIntrst +  "|" + BANCS.STDIN.userId + "|"  + BANCS.STDIN.userId + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSqlWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d != 0)THEN
		#{
			#ERROR
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbOut("Error|Database Insertion failed")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		CUST.CPRINT.resultMsg = "Record has been Added successfully"
		sv_u = urhk_SetOrbOut("RESULT_MSG|"+CUST.CPRINT.resultMsg)
		PRINT(sv_u)
		#}
	#}
	ENDIF
	#==========================================================*
	#For M-MODIFY
	#==========================================================*
	IF(CUST.CPRINT.funcCode == "M") THEN
	#{
		sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
		sv_s = sv_s + " SELECT 1 FROM custom.c_prefint_mod"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR)"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d == 0) THEN
		#{
			sv_s = "update custom.c_prefint_mod set "
			sv_s = sv_s + " PREF_INTEREST=?SVAR,"
			
			sv_s = sv_s + " LCHG_USER_ID=?SVAR,"
			sv_s = sv_s + " LCHG_TIME = sysdate,"
			sv_s = sv_s + " ENTITY_CRE_FLG = 'N'"
			sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
			sv_s = sv_s + "  AND BANK_ID = ?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.CPRINT.prIntrst  + "|" + BANCS.STDIN.userId + "|" +CUST.CPRINT.roleId + "|" +  BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0)THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error_1|Error^Database Updation Failed^settlccy")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			#}
		ELSE
			#{
			
			sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
			sv_s = sv_s + " SELECT 1 FROM custom.c_prefint"
			sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
			sv_s = sv_s + "  AND BANK_ID = ?SVAR)"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_d)
			IF(sv_d == 0) THEN
			#{
				CUST.CPRINT.recCount = BANCS.OUTPARAM.recCount
				PRINT(BANCS.OUTPARAM.recCount)
				
			#}
			ENDIF
			
			IF(CUST.CPRINT.recCount == 1) THEN
			#{
				sv_s = "update custom.c_prefint set "
				sv_s = sv_s + " PREF_INTEREST=?SVAR,"
				
				sv_s = sv_s + " LCHG_USER_ID=?SVAR,"
				sv_s = sv_s + " LCHG_TIME = sysdate,"
				sv_s = sv_s + " ENTITY_CRE_FLG = 'N'"
				sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
				sv_s = sv_s + "  AND BANK_ID = ?SVAR"
				PRINT(sv_s)
				BANCS.INPARAM.BINDVARS = CUST.CPRINT.prIntrst  + "|" + BANCS.STDIN.userId + "|" +CUST.CPRINT.roleId + "|" +  BANCS.STDIN.contextBankId
				PRINT(BANCS.INPARAM.BINDVARS)
				sv_d = urhk_dbSqlWithBind(sv_s)
				PRINT(sv_d)
				
				IF(sv_d != 0)THEN
				#{
					#ERROR
					sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
					PRINT(sv_u)
					sv_u = urhk_SetOrbOut("Error_1|Error^Database Updation Failed^settlccy")
					PRINT(sv_u)
					GOTO ENDOFSCRIPT
				#}
				ENDIF
				#}
			ELSE
				#{
				
				
				sv_s = "insert into custom.c_prefint_mod(ROLE_ID,PREF_INTEREST,DEL_FLG,ENTITY_CRE_FLG,RCRE_USER_ID,RCRE_TIME,LCHG_USER_ID,LCHG_TIME,BANK_ID)"
				sv_s = sv_s + " values(?SVAR,?SVAR,'N','N',?SVAR,sysdate,?SVAR,sysdate,?SVAR)"
				PRINT(sv_s)
				BANCS.INPARAM.BINDVARS=CUST.CPRINT.roleId + "|" + CUST.CPRINT.prIntrst +  "|" + BANCS.STDIN.userId + "|"  + BANCS.STDIN.userId + "|" + BANCS.STDIN.contextBankId
				PRINT(BANCS.INPARAM.BINDVARS)
				sv_d = urhk_dbSqlWithBind(sv_s)
				PRINT(sv_d)
				IF(sv_d != 0)THEN
				#{
					#ERROR
					sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
					PRINT(sv_u)
					sv_u = urhk_SetOrbOut("Error|Database Insertion failed")
					PRINT(sv_u)
					GOTO ENDOFSCRIPT
				#}
				ENDIF
			#}
			ENDIF
		#}
		ENDIF
		CUST.CPRINT.resultMsg = "Record has been Modified successfully"
		sv_u = urhk_SetOrbOut("RESULT_MSG|"+CUST.CPRINT.resultMsg)
		PRINT(sv_u)
	#}
	ENDIF
	#==========================================================*
	#For V- VERIFY
	#==========================================================*
	IF(CUST.CPRINT.funcCode == "V") THEN
	#{
		sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
		sv_s = sv_s + " SELECT 1 FROM custom.c_prefint_mod"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR)"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d == 0) THEN
		#{
			CUST.CPRINT.recCount = BANCS.OUTPARAM.recCount
			PRINT(BANCS.OUTPARAM.recCount)
			
		#}
		ENDIF
		
		IF(CUST.CPRINT.recCount == 1) THEN
		#{
			sv_s = "update  custom.c_prefint_mod set "
			
			sv_s = sv_s + " LCHG_USER_ID=?SVAR,"
			sv_s = sv_s + " LCHG_TIME = sysdate,"
			sv_s = sv_s + " ENTITY_CRE_FLG = 'Y'"
			sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
			sv_s = sv_s + "  AND BANK_ID = ?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS =  BANCS.STDIN.userId +"|" + CUST.CPRINT.roleId + "|"  + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database update failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			
			#			sv_s = "delete from custom.c_prefint  where ROLE_ID = ?SVAR AND  BANK_ID=?SVAR"
			#			PRINT(sv_s)
			#			BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId + "|"  + BANCS.STDIN.contextBankId
			#			PRINT(BANCS.INPARAM.BINDVARS)
			#			sv_d = urhk_dbSqlWithBind(sv_s)
			#			PRINT(sv_d)
			#			IF(sv_d != 0) THEN
			#			#{
			#				#ERROR
			#				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			#				PRINT(sv_u)
			#				sv_u = urhk_SetOrbOut("Error|Database insert failed")
			#				PRINT(sv_u)
			#				GOTO ENDOFSCRIPT
			#			#}
			#			ENDIF
			
			sv_s = "insert into custom.c_prefint select * from custom.c_prefint_mod where ROLE_ID = ?SVAR AND  BANK_ID=?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId + "|"  + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database insert failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			
			sv_s = "delete from custom.c_prefint_mod where  ROLE_ID = ?SVAR AND  BANK_ID=?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId + "|"  + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database delete failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			#}
		ELSE
			#{
			sv_s = "update  custom.c_prefint set "
			sv_s = sv_s + " ENTITY_CRE_FLG = 'Y'"
			sv_s = sv_s + " where ROLE_ID = ?SVAR AND BANK_ID = ?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS =   CUST.CPRINT.roleId + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database update failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		CUST.CPRINT.resultMsg = "Record has been Verified successfully"
		sv_u = urhk_SetOrbOut("RESULT_MSG|"+CUST.CPRINT.resultMsg)
		PRINT(sv_u)
	#}
	ENDIF
	#==========================================================*
	#For X-CANCEL
	#==========================================================*
	IF(CUST.CPRINT.funcCode == "X") THEN
	#{
		sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
		sv_s = sv_s + " SELECT 1 FROM custom.c_prefint_mod"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR)"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d == 0) THEN
		#{
			sv_s = "delete from custom.c_prefint_mod where ROLE_ID = ?SVAR  AND BANK_ID=?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId +  "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database delete failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			#}
		ELSE
			#{
			sv_s = "delete from custom.c_prefint where ROLE_ID = ?SVAR AND  BANK_ID=?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database delete failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		CUST.CPRINT.resultMsg = "Record has been cancelled successfully"
		sv_u = urhk_SetOrbOut("RESULT_MSG|"+CUST.CPRINT.resultMsg)
		PRINT(sv_u)
	#}
	ENDIF
	#==========================================================*
	#For D- DELETE
	#==========================================================*
	IF(CUST.CPRINT.funcCode == "D") THEN
	#{
		sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
		sv_s = sv_s + " SELECT 1 FROM custom.c_prefint_mod"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
		sv_s = sv_s + "  AND BANK_ID = ?SVAR)"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId  + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d == 0) THEN
		#{
			sv_s = "delete from custom.c_prefint_mod where ROLE_ID = ?SVAR  AND BANK_ID=?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database delete failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			#}
		ELSE
			#{
			sv_s = "delete from custom.c_prefint where ROLE_ID = ?SVAR  AND BANK_ID=?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId +  "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database delete failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		CUST.CPRINT.resultMsg = "Record has been deleted successfully"
		sv_u = urhk_SetOrbOut("RESULT_MSG|"+CUST.CPRINT.resultMsg)
		PRINT(sv_u)
		
	#}
	ENDIF
	sv_z = urhk_SetOrbOut("SuccessOrFailure|Y")
	PRINT(sv_z)
	TRACE OFF
	ENDOFSCRIPT:
END-->
