#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: Biller_Acct_check.scr 
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START

TRACE ON

	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       Biller_Acct_check.scr 
	# Author                :       Anand Pansare 
	# Description           :       This script gets on click  find account linked to bill_ID or bill_Account .
	# Called Script         :	
	# Calling Script        :
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           16-04-2013         Anand Pansare     Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON

	lv_u = urhk_b2k_printrepos("BANCS")

	#========================================================
	# Check whether CUST Repository exists else create it
	#========================================================
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	#========================================================
	# Check whether BILL Class exists else create it
	#========================================================
	sv_r = CLASSEXISTS("CUST","BILL")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","BILL",5)
	#}
	ENDIF
	#========================================================
	# Initialization
	#========================================================
	CUST.BILL.errorFlg = "N"
	CUST.BILL.srvSuccess="Y"
	CUST.BILL.reverseFlg="N"
	CUST.BILL.prntRevFlg="N"
	CUST.BILL.bankId=BANCS.STDIN.contextBankId
	CUST.BILL.BODDate=BANCS.STDIN.BODDate
	CUST.BILL.billerValReq="N"
	#========================================================
	# Validating the inputs-biller Account Id
	#========================================================
	
		PRINT(BANCS.INPUT.acctId)	

	
		IF(TRIM(BANCS.INPUT.acctId) != "")THEN
		#{
			
			CUST.BILL.acctId = BANCS.INPUT.acctId
			PRINT(BANCS.INPUT.acctId)
			
			#Change starts
				
			sv_s = "Record_count|"
			sv_s = sv_s + " SELECT COUNT(*)"
			sv_s = sv_s + " FROM TBAADM.BRGT "
			sv_s = sv_s + " WHERE FREE_TEXT1='"+ CUST.BILL.acctId + "'"
			
			PRINT(sv_s)
			sv_d = urhk_dbSelectWithBind(sv_s)
			IF(sv_d==0)THEN
			#{
				CUST.BILL.Record_count = BANCS.OUTPARAM.Record_count
				PRINT(CUST.BILL.Record_count)
				
	
				IF(CUST.BILL.Record_count=="0" )THEN
				#{
					sv_e=urhk_SetOrbOut("biller_chk_flg|N")
				#}
				 ELSE
        	                #{
                        		sv_e=urhk_SetOrbOut("biller_chk_flg|Y")
				#}
                        	ENDIF
			#}
			ELSE
			#{
				EXITSCRIPT
			#}
			ENDIF
			
		#}
		ENDIF
TRACE OFF

EXITSCRIPT	
END-->
