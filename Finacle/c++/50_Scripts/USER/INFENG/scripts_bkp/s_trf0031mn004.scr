# for cancel 
#---------------------------------------------------------------------------------------------
# Source Name           :   tfcl032mn004.scr
# Date                  :   02-05-2013
# Author                :
# Title                 :   Customization for requirement TFCL-032
# Description           :   This script deletes the data in the table when cancel operation is done during modify or add
# Modification History  :
# <Serial No.>      <Date>          <Author Name>       <Description>
#      1             02-05-2013      Samrat Saha      Original version
#----------------------------------------------------------------------------------------------
<--START
CHECKSUM="9529c35ac386b32fd21812bb41d2b7034509c14afd2dd2bbec71e11b617b3336"
	#TRACE ON
	
	IF(REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF(CLASSEXISTS("CUST","TRF0031") == 0) THEN
	#{
		CREATECLASS("CUST","TRF0031",5)
	#}
	ENDIF
	
	CUST.TRF0031.remtId=""
	#---------------------------------------------------------------------------------------------
	#User hook used to get menu id from screen
	#---------------------------------------------------------------------------------------------
	#sv_a=urhk_b2k_printrepos("BANCS")
	
	CUST.TRF0031.menuId = BANCS.STDIN.menuId
	print(CUST.TRF0031.menuId)
	
	sv_a=urhk_SRV_GetVal("remCritDtls.remitId")
	IF(sv_a == 0) THEN
	#{
		CUST.TRF0031.remtId = trim(BANCS.OUTPARAM.srvValue)
		print(CUST.TRF0031.remtId)
		#}
	ELSE
		#{
		CUST.TRF0031.errorMsg = "Unable to get remittance id from front end"
		sv_v = urhk_SRV_SetErr(CUST.TRF0031.errorMsg)
	#}
	ENDIF
	
	#---------------------------------------------------------------------------------------------
	# Finding count of the no records of the Remittance Id and Deleting Those records
	#---------------------------------------------------------------------------------------------
	
	sv_s = ""
	sv_s = sv_s + "cnt|"
	sv_s = sv_s + " SELECT COUNT(*)"
	sv_s = sv_s + " FROM CUSTOM.C_PTYDTL"
	sv_s = sv_s + " WHERE C_PTYDTL.remt_id =?SVAR"
	sv_s = sv_s + " AND C_PTYDTL.entity_cre_flg ='N'"
	sv_s = sv_s + " AND C_PTYDTL.del_flg = 'N'"
	sv_s = sv_s + " AND C_PTYDTL.bank_id = ?SVAR"
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS	=""
	BANCS.INPARAM.BINDVARS 	= BANCS.INPARAM.BINDVARS+ CUST.TRF0031.remtId + "|"
	BANCS.INPARAM.BINDVARS 	= BANCS.INPARAM.BINDVARS+ BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d ==0) THEN
	#{
		IF(CINT(BANCS.OUTPARAM.cnt) >0) THEN
		#{
			sv_s =""
			sv_s = sv_s + " DELETE CUSTOM.C_PTYDTL"
			sv_s = sv_s + " WHERE C_PTYDTL.remt_id =?SVAR"
			sv_s = sv_s + " AND C_PTYDTL.entity_cre_flg ='N'"
			sv_s = sv_s + " AND C_PTYDTL.del_flg = 'N'"
			sv_s = sv_s + " AND C_PTYDTL.bank_id = ?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS	=""
			BANCS.INPARAM.BINDVARS 	= BANCS.INPARAM.BINDVARS+ CUST.TRF0031.remtId + "|"
			BANCS.INPARAM.BINDVARS 	= BANCS.INPARAM.BINDVARS+ BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			
			sv_d = urhk_dbSQLWithBind(sv_s)
			PRINT(sv_d)
			IF (sv_d == 0 ) THEN
			#{
				sv_d = urhk_dbSQLWithBind("COMMIT")
				#}
			ELSE
				#{
				CUST.TRF0031.errMsg = "The records are not commited"
				sv_v = urhk_SRV_SetErr(CUST.TRF0031.errorMsg)
				
			#}
			ENDIF
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.TRF0031.errMsg = "Unable to find records for the remittance id"
		sv_v = urhk_SRV_SetErr(CUST.TRF0031.errorMsg)
		
	#}
	ENDIF
	
	
	ENDOFSCRIPT:
		
		
END-->
