#-------------------------------------------------------------------------------------
#	Name			: catodscoredp007.scr
#	Description		: The script is used For validating user for using upload function
#	Date			: 29-04-2013
#	Author			: Kalvin
#	Input			: NA
#	Output			: NA
#	Called Script	:
#	Calling Script	:
#	Menu Option		: CATODAPP
#	Srl. No		Date			Author				Description.
#	-------		----			------				----------------
#	1.0			28-08-2013		Kalvin		Original Version
#-------------------------------------------------------------------------------------

<--START
CHECKSUM="3298828980daa299fb8347e5e9f30b0f4b3f64ae1c691fdfeefff4a8bf1ee54f"
	
	TRACE ON
	#=======================================================================================*
	# Create repository and class
	#=======================================================================================*
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST","CATOD") == 0) THEN
	#{
		CREATECLASS("CUST","CATOD",5)
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST","CPARAM") == 0) THEN
	#{
		CREATECLASS("CUST","CPARAM",5)
	#}
	ENDIF
	#sv_u = urhk_B2k_PrintRepos("BANCS")
	
	BANCS.INPARAM.BINDVARS=BANCS.STDIN.EmployeeId +"|"+BANCS.STDIN.contextBankId+"|"+"Y|N"
	sv_s = " solId|SELECT SOL_ID  "
	sv_s = sv_s + "  FROM TBAADM.UPR  "
	sv_s = sv_s + "  WHERE USER_ID=?SVAR "
	sv_s = sv_s + "  AND HOME_BANK_ID = ?SVAR "
	sv_s = sv_s + "  AND ENTITY_CRE_FLG= ?SVAR "
	sv_s = sv_s + "  AND DEL_FLG=?SVAR"
	PRINT(sv_s)
	sv_c = urhk_dbSelectWithBind(sv_s)
	print(sv_c)
	IF(sv_c == 0)THEN
	#{
		CUST.CATOD.solId =BANCS.OUTPARAM.solId
		#}
	ELSE
		#{
		CUST.CATOD.errorMsg="UNABLE TO FETCH SOL FROM TABLE"
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		print(sv_u)
		sv_u = urhk_SetOrbOut("Error_0|ERROR^" + CUST.CATOD.errorMsg + "^acctNum")
		print(sv_u)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	IF (CUST.CATOD.solId!="000") THEN
	#{
		sv_u = urhk_SetOrbOut("STATUS|F")
		
		#}
	ELSE
		#{
		sv_u = urhk_SetOrbOut("STATUS|S")
		
	#}
	ENDIF
	sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
	
	ENDOFSCRIPT:
		
		#TRACE OFF
		EXITSCRIPT
END-->
