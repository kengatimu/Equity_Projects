#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: ubpdp055.scr
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START
CHECKSUM="6eaf8c30c9aa2c5772551efbe099eace02c674dec6fe4509bc4f93e7d55bc6a8"
	# Script body goes here
	TRACE ON
	TRACE ON
	CUST.UBP.cparamErrorFlg="N"
	PRINT(CUST.UBP.paramId)
	BANCS.INPARAM.BINDVARS = CUST.UBP.paramId + "|" + "Y" + "|" + "N"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + "ALL"+"|"+"UBP"+"|"+BANCS.STDIN.contextBankId
	sv_s = "paramValue|SELECT PARAMETER_VALUE FROM CUSTOM.C_CPARAM_VALUE_TABLE"
	sv_s = sv_s + " WHERE PARAMETER_ID = ?SVAR AND ENTITY_CRE_FLG = ?SVAR"
	sv_s = sv_s + " AND DEL_FLG = ?SVAR AND LEVELCODE_1 = ?SVAR AND MODULE_NAME=?SVAR"
	sv_s = sv_s + " AND BANK_ID=?SVAR"
	PRINT(sv_s)
	sv_d = urhk_dbselectWithBind(sv_s)
	IF(sv_d == 0) THEN
	#{
		CUST.UBP.paramVal = BANCS.OUTPARAM.paramValue
		print(CUST.UBP.paramVal)
		#}
	ELSE
		#{
		CUST.UBP.paramVal = "Could not fetch cparam value for Param Id: " + CUST.UBP.paramId
		print(CUST.UBP.paramVal)
		CUST.UBP.cparamErrorFlg="Y"
	#}
	ENDIF
	PRINT(CUST.UBP.paramVal)
	#trace off
END-->
