#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: ubpdp048.scr
# event: NA
# description: NA
#=================================================

<--START
CHECKSUM="2e9c50734dea9279d32c0d313bdf5e4d53608907217cc7d229628760aac1d26e"
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp048.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called for HBLRG menu from hblrgdtl_custom.js for
	#                               getting bank id
	# Called Script         :
	# Calling script        :		ubpdp052.scr
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           24-07-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether UBP Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","UBP")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","UBP",5)
	#}
	ENDIF
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	
	CUST.UBP.targetEntity = " "
	CUST.UBP.targetEntityDesc = " "
	CUST.UBP.targetEntityHomeCcy = " "
	CUST.UBP.recordCount = "0"
	
	BANCS.INPARAM.BINDVARS = ""
	sv_s = "recordCount|"
	sv_s = sv_s + " SELECT COUNT(*)"
	sv_s = sv_s + " FROM TBAADM.GCT "
	sv_s = sv_s + " WHERE DEL_FLG != 'Y'"
	print(sv_s)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	IF(sv_d == 0)THEN
	#{
		CUST.UBP.recordCount = BANCS.OUTPARAM.recordCount
		print(CUST.UBP.recordCount)
	#}
	ENDIF
	
	sv_u = urhk_SetOrbOut("recordCount|"+CUST.UBP.recordCount)
	print(sv_u)
	
	BANCS.INPARAM.BINDVARS = ""
	sv_s = "targetEntity,targetEntityDesc,targetEntityHomeCcy|"
	sv_s = sv_s + " SELECT BANK_ID,GROUP_DESC,HOME_CRNCY_CODE"
	sv_s = sv_s + " FROM TBAADM.GCT "
	sv_s = sv_s + " WHERE DEL_FLG != 'Y'"
	sv_s = sv_s + " ORDER BY BANK_ID"
	print(sv_s)
	
	sv_c = urhk_dbCursorOpenWithBind(sv_s)
	print(sv_c)
	
	IF(sv_c != 0)THEN
	#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_n = BANCS.OUTPARAM.DB_CURSOR_NUMBER
	print(sv_n)
	
	sv_f = urhk_dbCursorFetch(sv_n)
	print(sv_f)
	
	sv_i = 1
	
	WHILE(sv_f == 0)
	#{
		CUST.UBP.targetEntity = BANCS.OUTPARAM.targetEntity
		print(CUST.UBP.targetEntity)
		
		CUST.UBP.targetEntityDesc = TOUPPER(BANCS.OUTPARAM.targetEntityDesc)
		print(CUST.UBP.targetEntityDesc)
		
		CUST.UBP.targetEntityHomeCcy = BANCS.OUTPARAM.targetEntityHomeCcy
		print(CUST.UBP.targetEntityHomeCcy)
		
		sv_s = "targetEntity_" + Format$(sv_i,"%d") + "|" + CUST.UBP.targetEntity
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_s = "targetEntityDesc_" + Format$(sv_i,"%d") + "|" + CUST.UBP.targetEntityDesc
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_s = "targetEntityHomeCcy_" + Format$(sv_i,"%d") + "|" + CUST.UBP.targetEntityHomeCcy
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_i = sv_i + 1
		
		sv_f = urhk_dbCursorFetch(sv_n)
		print(sv_f)
		
	#}
	DO
	
	sv_c = urhk_dbCursorClose(sv_n)
	print(sv_c)
	
	
	ENDOFSCRIPT:
		
END-->
