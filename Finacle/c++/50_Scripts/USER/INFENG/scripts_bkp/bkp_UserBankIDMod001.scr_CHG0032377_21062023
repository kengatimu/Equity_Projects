<--START
TRACE ON
	####################################################################
	# Check whether CUST Repository exists else create it
	####################################################################
		sv_r = REPEXISTS("CUST")
		IF(sv_r == 0) THEN
		#{
			CREATEREP ("CUST")
		#}
		ENDIF
	
	####################################################################
	# Check whether INTERFACE Class exists else create it
	####################################################################
		sv_r = CLASSEXISTS("CUST","INTERFACE")
		IF(sv_r == 0) THEN
		#{
			CREATECLASS ("CUST","INTERFACE",5)
		#}
		ENDIF
		
		sv_s = "multbankproID|select multi_bank_user_profile_id from tbaadm.mpb where del_flg ='N' and user_work_class =?SVAR"
		sv_s = sv_s + "and home_bank_id=?SVAR and target_bank_id=?SVAR and role_id=?SVAR"
		BANCS.INPARAM.BINDVARS = CUST.INTERFACE.usrWrkClass1 + "|" + CUST.INTERFACE.homeBankId
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS +"|" + CUST.INTERFACE.BankID + "|" + CUST.INTERFACE.roleId1
		PRINT(sv_s)
		
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		IF(sv_d == 0) THEN
		#{
			CUST.INTERFACE.multbankproID  =  BANCS.OUTPARAM.multbankproID
			PRINT(CUST.INTERFACE.multbankproID)
		#}
		ELSE
		#{
			sv_a = "UserMod.Status|ERROR"
            		sv_u = urhk_SetOrbOut(sv_a)
            		sv_b = "UserMod.ErrorMessage|No Valid Multi bank profile Exists for role ID " +CUST.INTERFACE.roleId1+ " And target bank ID " +CUST.INTERFACE.BankID
			CUST.INTERFACE.CORE_USER_FLG = "FAILURE"
			CUST.INTERFACE.CORE_REMARKS = "No Valid Multi bank profile Exists for role ID " +CUST.INTERFACE.roleId1+ " And target bank ID " +CUST.INTERFACE.BankID
            		sv_u = urhk_SetOrbOut(sv_b)
            		exitscript
		#}
		ENDIF
		
		sv_s = "UPDATE TBAADM.UPR SET MULTI_BANK_USER_PROFILE_ID = ?SVAR WHERE USER_ID=?SVAR"
		BANCS.INPARAM.BINDVARS = CUST.INTERFACE.multbankproID +"|"+ CUST.INTERFACE.userId
		
		PRINT(sv_s)
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbSqlWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d == "0")THEN
		#{
			CALL("UserBankIDMod002.scr")
		#}
		ELSE
		#{
			sv_a = "UserMod.Status|ERROR"
            		sv_u = urhk_SetOrbOut(sv_a)
            		sv_b = "UserMod.ErrorMessage|Multi bank profile ID update failed"
			CUST.INTERFACE.CORE_USER_FLG = "FAILURE"
			CUST.INTERFACE.CORE_REMARKS = "Multi bank profile ID update failed"
            		sv_u = urhk_SetOrbOut(sv_b)
            		exitscript
		#}
		ENDIF
END-->
