#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: s_acctopnvalmn007.scr
# Type: NA|default
# Description: s_acctopnvalmn007.scr
# Author:
# Date:
# Modification Log:
#============================================================

#-------------------------------------------------------------------------------------------------------
# Source Name           :   s_acctopnvalmn007.scr
# Date                  :   03-04-2013
# Author                :   Shishira
# Title                 :   AcountOpening
# Sources called        :   SRV_OpenSBAcct_pre_process_data.scr
# Sources calling       :   NA
# RS Approach Document  :
# Modification History  :
# <Serial No.>      <Date>          <Author Name>       <Description>
# 0.00a             01-04-2013     		Manoj        Original version
#
#-----------------------------------------------------------------------------------------------------

<--START
CHECKSUM="ec0b05fcfeec3ae1acf9c493deb7d3be890a5d9521cff6df37edfbef72ca0c00"
	TRACE ON
	#==========================================================*
	# CREATING NEW REPOSITORY AND CLASS
	#==========================================================*
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	IF (CLASSEXISTS("CUST", "ACCOPEN") == 0) THEN
	#{
		CREATECLASS("CUST", "ACCOPEN", 5)
	#}
	ENDIF
	
	CUST.ACCOPEN.pSchmCode1	= ""
	CUST.ACCOPEN.pSchmCode2	= ""
	CUST.ACCOPEN.freeText1	= ""
	CUST.ACCOPEN.freeText2	= ""
	CUST.ACCOPEN.schmCode	= ""
	CUST.ACCOPEN.foracid	= ""
	CUST.ACCOPEN.errMsg		= ""
	
	BANCS.INPUT.moduleName = "ACCOUNT_OPENING"
	BANCS.INPUT.parameterId = "SCHM_SB103"
	CALL("getcparamvalue.scr")
	
	IF (BANCS.OUTPUT.parameterVal != "")THEN
	#{
		CUST.ACCOPEN.pSchmCode1 =BANCS.OUTPUT.parameterVal
		PRINT(CUST.ACCOPEN.pSchmCode1)
		#}
	ELSE
		#{
		CUST.ACCOPEN.errMsg = "SCHME TYPE setup is not done in CPARAM"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	BANCS.INPUT.parameterId = "SCHM_SB109"
	CALL("getcparamvalue.scr")
	
	IF (BANCS.OUTPUT.parameterVal != "")THEN
	#{
		CUST.ACCOPEN.pSchmCode2 =BANCS.OUTPUT.parameterVal
		PRINT(CUST.ACCOPEN.pSchmCode2)
		#}
	ELSE
		#{
		CUST.ACCOPEN.errMsg = "Schme type is not set in CPARAM TABLE"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_u=urhk_SRV_GetVal("acctCommonInfo.acctClassMISC.freeText1")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
		CUST.ACCOPEN.freeText1 = TOUPPER(BANCS.OUTPARAM.srvValue)
		PRINT(CUST.ACCOPEN.freeText1)
	#}
	ENDIF
	
	sv_u=urhk_SRV_GetVal("acctCommonInfo.acctClassMISC.freeText2")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
		CUST.ACCOPEN.freeText2 = TOUPPER(BANCS.OUTPARAM.srvValue)
		PRINT(CUST.ACCOPEN.freeText2)
	#}
	ENDIF
	
	sv_u=urhk_SRV_GetVal("acctCommonInfo.acctGeneralInfo.acctBasic.schmCode.schmCode")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
		CUST.ACCOPEN.schmCode = TOUPPER(BANCS.OUTPARAM.srvValue)
		PRINT(CUST.ACCOPEN.schmCode)
	#}
	ENDIF
	
	sv_u=urhk_SRV_GetVal("acctCommonInfo.acctGeneralInfo.acctBasic.foracid")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
		CUST.ACCOPEN.foracid = TOUPPER(BANCS.OUTPARAM.srvValue)
		PRINT(CUST.ACCOPEN.foracid)
	#}
	ENDIF
	
	IF (CUST.ACCOPEN.schmCode == CUST.ACCOPEN.pSchmCode1) THEN
	#{
		IF (CUST.ACCOPEN.freeText1 == "") THEN
		#{
			sv_u =urhk_SRV_SetErr("acctCommonInfo.acctClassMISC.freeText1|MIS Code must be visited,U have to enter free_text1 (Installment Amount),free_text2(period in months)")
			BANCS.OUTPUT.successOrFailure="F"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	sv_u = urhk_SRV_GetVal("acctCommonInfo.acctDSA.acctDSAEntered")
	IF (sv_u == 0) THEN
	#{
		sv_b = TOUPPER(BANCS.OUTPARAM.srvValue)
		PRINT(sv_b)
	#}
	ENDIF
	
	sv_u = urhk_SRV_GetVal("acctCommonInfo.acctDSA.disbDealerId.dsaId")
	IF (sv_u == 0) THEN
	#{
		CUST.ACCOPEN.disbId = TOUPPER(BANCS.OUTPARAM.srvValue)
		PRINT(CUST.ACCOPEN.disbId)
	#}
	ENDIF
	
	sv_u = urhk_SRV_GetVal("acctCommonInfo.acctDSA.sourceDealerId.dsaId")
	IF (sv_u == 0) THEN
	#{
		CUST.ACCOPEN.sourceId = TOUPPER(BANCS.OUTPARAM.srvValue)
		PRINT(CUST.ACCOPEN.sourceId)
	#}
	ENDIF
	
	IF (CUST.ACCOPEN.schmCode == CUST.ACCOPEN.pSchmCode2) THEN
	#{
		IF ((CUST.ACCOPEN.disbId == "") AND (CUST.ACCOPEN.sourceId == "") ) THEN
		#{
			sv_u = urhk_SRV_setErr("acctCommonInfo.acctDSA.sourceDealerId.dsaId|Visit Other Details Tab to Enter Sourcing Delear ID & Disbursing Dealer Id")
			#sv_u = urhk_SRV_setErr("Visit Other Details Tab to Enter Sourcing Delear ID & Disbursing Dealer Id")
			PRINT(sv_u)
		#}
		ENDIF
	#}
	ENDIF
	
	ENDOFSCRIPT:
		IF(CUST.ACCOPEN.errMsg != "") THEN
		#{
			sv_u = urhk_SRV_setErr(CUST.ACCOPEN.errMsg)
		#}
		ENDIF
		
		EXITSCRIPT
		TRACE OFF
END-->
