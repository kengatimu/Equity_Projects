#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: Actual_scored_accs_custval.scr
# Type: NA|default
# Description: Actual_scored_accs_custval.scr
# Author:
# Date:
# Modification Log:
#============================================================

<--START
CHECKSUM="c3e1be7ae83b0f6fa849206a2f9b3187c17787dbdcc0e0edae73b867e4ae29f2"
	TRACE ON
	
	IF(FIELDEXISTS(BANCS.FINRPTDEF.P_SOL_ID))THEN
	#{
		PRINT(BANCS.FINRPTDEF.P_SOL_ID)
		
		#}
	ELSE
		#{
		BANCS.FINRPTDEF.P_SOL_ID = ""
		#}
		PRINT(BANCS.FINRPTDEF.P_FROMDATE)
		IF(FIELDEXISTS(BANCS.FINRPTDEF.P_TODATE))THEN
		#{
			PRINT(BANCS.FINRPTDEF.P_TODATE)
			
			#}
		ELSE
			#{
			BANCS.FINRPTDEF.P_TODATE= ""
			#}
			IF(BANCS.FINRPTDEF.P_SOL_ID == "") THEN
			#{
				sv_u = Urhk_FINRPT_SetErr("P_SOL_ID|SOL_ID must be entered")
				GOTO ENDOFSCRIPT
				#}
			ELSE
				#{
				IF(BANCS.FINRPTDEF.P_SOL_ID != "ALL") THEN
				#{
					sv_u = urhk_B2k_valSolId(BANCS.FINRPTDEF.P_SOL_ID)
					IF(sv_u != 0) THEN
					#{
						sv_u = Urhk_FINRPT_SetErr("P_SOL_ID|Invalid SOL ID")
						GOTO ENDOFSCRIPT
					#}
					ENDIF
					IF(BANCS.STDIN.homeSolId != BANCS.FINRPTDEF.P_SOL_ID) THEN
					#{
						IF(BANCS.STDIN.homeSolId != "000") THEN
						#{
							sv_u = Urhk_FINRPT_SetErr("P_SOL_ID|Report can be generated only for  users Home Sol ID")
							GOTO ENDOFSCRIPT
						#}
						ENDIF
					#}
					ENDIF
				#}
				ENDIF
				IF(BANCS.STDIN.homeSolId != "000") THEN
				#{
					IF(BANCS.FINRPTDEF.P_SOL_ID == "ALL") THEN
					#{
						sv_u = Urhk_FINRPT_SetErr("P_SOL_ID|Report can be generated only for users Sol ID")
						GOTO ENDOFSCRIPT
					#}
					ENDIF
					
				#}
				ENDIF
				
			#}
			ENDIF
			
			
			IF(BANCS.FINRPTDEF.P_FROMDATE != "") THEN
			#{
				sv_u = urhk_B2k_valDate(BANCS.FINRPTDEF.P_FROMDATE)
				IF(sv_u !=0) THEN
				#{
					sv_u = Urhk_FINRPT_SetErr("P_FROMDATE|Invalid P_FROMDATE")
					GOTO ENDOFSCRIPT
				#}
				ENDIF
			#}
			ENDIF
			
			IF(BANCS.FINRPTDEF.P_TODATE != "") THEN
			#{
				sv_u = urhk_B2k_valDate(BANCS.FINRPTDEF.P_TODATE)
				IF(sv_u !=0) THEN
				#{
					sv_u = Urhk_FINRPT_SetErr("P_TODATE|Invalid P_TODATE")
					GOTO ENDOFSCRIPT
					#}
				#}
				ENDIF
				
				
				
				ENDOFSCRIPT:
					
					TRACE OFF
			END-->
			
