<--START
trace on

	sv_r=urhk_B2k_PrintRepos("BANCS")
	print (sv_r)   

##################################################################
# Script for slabwise insuarance fee collection
##################################################################

#--------------------------------------------------------------------------
#  Creation of CUST repository and INSU class
#--------------------------------------------------------------------------

	sv_r = REPEXISTS("INSU")
	IF (sv_r == 0) THEN
	#{
		CREATEREP("INSU")
	#}
	endif

	sv_r = CLASSEXISTS("INSU", "LOAN")
	IF (sv_r == 0) THEN
	#{
		CREATECLASS("INSU", "LOAN", 5)
	#}
	ENDIF

	sv_u=urhk_B2k_PrintRepos("BANCS")
	print(sv_u)

INSU.LOAN.errMsg =""
	

	IF (FIELDEXISTS(BANCS.INPUT.senderToRecvrInfo6)) THEN
		print(BANCS.INPUT.senderToRecvrInfo6)
		sv_h = BANCS.INPUT.senderToRecvrInfo6

		BANCS.OUTPUT.collAmount =CDOUBLE(sv_h)
		print(BANCS.OUTPUT.collAmount)

		sv_e =  BANCS.INPUT.senderToRecvrInfo6

		BANCS.OUTPUT.AmountWithNoDiscount = CDOUBLE(sv_e)
		print(BANCS.OUTPUT.AmountWithNoDiscount)

		BANCS.OUTPUT.collCrncy = BANCS.STDIN.homeCrncyCode
	ENDIF



ENDOFSCRIPT :
IF ( INSU.LOAN.errMsg != "" ) THEN
#{
	sv_u = system("echo '"+INSU.LOAN.errMsg+"' >>insurance.log")
#}
ENDIF

print(BANCS.OUTPUT.collAmount)
TRACE OFF
EXITSCRIPT
END-->
