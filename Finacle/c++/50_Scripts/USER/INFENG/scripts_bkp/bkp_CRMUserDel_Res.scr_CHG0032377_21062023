<--START

	TRACE ON
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether INTERFACE Class exists else create it
	###########################################################################################
		
	sv_r = CLASSEXISTS("CUST","INTERFACE")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","INTERFACE",5)
	#}
	ENDIF
	
	
	CUST.INTERFACE.status = ""
	CUST.INTERFACE.crm_respCode = ""
	CUST.INTERFACE.crm_mesg = ""
	CUST.INTERFACE.crm_errorCode = ""
	CUST.INTERFACE.crm_errorDesc = ""
	CUST.INTERFACE.crm_errType = ""
	CUST.INTERFACE.crm_errorDesc = ""
	CUST.INTERFACE.crm_errorCode = ""
	CUST.INTERFACE.crm_service = ""
	CUST.INTERFACE.crm_loginId = ""
	CUST.INTERFACE.crm_desc = ""
	CUST.INTERFACE.crm_entity = ""
	
	sv_x = CUST.INTERFACE.respXMLMsg
	
	#--------------------------------------------------------------------------------------------------------
	# Fetching the status of the request
	#--------------------------------------------------------------------------------------------------------
	sv_y = "<HostTransaction>"
	sv_z = "</HostTransaction>"
	sv_b = GETPOSITION(sv_x, sv_y) + STRLEN(sv_y) - 1
	sv_e = GETPOSITION(sv_x, sv_z) - 1
	IF (GETPOSITION(sv_x, sv_y) != 0) THEN
	#{
		CUST.INTERFACE.ubusStuatus = MID$(sv_x, sv_b, (sv_e - sv_b))
		sv_x = CUST.INTERFACE.ubusStuatus
		PRINT(sv_x)
		sv_y = "<Status>"
		sv_z = "</Status>"
		sv_b = GETPOSITION(sv_x, sv_y) + STRLEN(sv_y) - 1
		sv_e = GETPOSITION(sv_x, sv_z) - 1
		IF (GETPOSITION(sv_x, sv_y) != 0) THEN
		#{
			CUST.INTERFACE.status = MID$(MID$(sv_x, sv_b, (sv_e - sv_b)),0,10)
			PRINT(CUST.INTERFACE.status)
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.INTERFACE.status = ""
	#}
	ENDIF
	PRINT(CUST.INTERFACE.status)
	
	#--------------------------------------------------------------------------------------------------------
	# If Success the fetch other values from the response xml
	#--------------------------------------------------------------------------------------------------------
	IF(CUST.INTERFACE.status == "SUCCESS") THEN
	#{
		CUST.INTERFACE.crm_errorMsgFlg = "N"
		
		
		sv_x = CUST.INTERFACE.respXMLMsg
		sv_y = "<UserDelResponse>"
		sv_z = "</UserDelResponse>"
		sv_b = GETPOSITION(sv_x, sv_y) + STRLEN(sv_y) - 1
		sv_e = GETPOSITION(sv_x, sv_z) - 1
		IF (GETPOSITION(sv_x, sv_y) != 0) THEN
		#{
			
			sv_y = "<UserDelRs>"
			sv_z = "</UserDelRs>"
			sv_b = GETPOSITION(sv_x, sv_y) + STRLEN(sv_y) - 1
			sv_e = GETPOSITION(sv_x, sv_z) - 1
			IF (GETPOSITION(sv_x, sv_y) != 0) THEN
			#{
			
				sv_x = CUST.INTERFACE.respXMLMsg
				sv_y = "<Service>"
				sv_z = "</Service>"
				sv_b = GETPOSITION(sv_x, sv_y) + STRLEN(sv_y) - 1
				sv_e = GETPOSITION(sv_x, sv_z) - 1
				IF (GETPOSITION(sv_x, sv_y) != 0) THEN
				#{
					CUST.INTERFACE.crm_service = MID$(MID$(sv_x, sv_b, (sv_e - sv_b)),0,10)
					print(CUST.INTERFACE.crm_service)
				#}
				ENDIF
				
				sv_x = CUST.INTERFACE.respXMLMsg
				sv_y = "<loginId>"
				sv_z = "</loginId>"
				sv_b = GETPOSITION(sv_x, sv_y) + STRLEN(sv_y) - 1
				sv_e = GETPOSITION(sv_x, sv_z) - 1
				IF (GETPOSITION(sv_x, sv_y) != 0) THEN
				#{
					CUST.INTERFACE.crm_loginId = MID$(MID$(sv_x, sv_b, (sv_e - sv_b)),0,100)
					print(CUST.INTERFACE.crm_loginId)
				#}
				ENDIF
				sv_x = CUST.INTERFACE.respXMLMsg
				sv_y = "<Desc>"
				sv_z = "</Desc>"
				sv_b = GETPOSITION(sv_x, sv_y) + STRLEN(sv_y) - 1
				sv_e = GETPOSITION(sv_x, sv_z) - 1
				IF (GETPOSITION(sv_x, sv_y) != 0) THEN
				#{
					CUST.INTERFACE.crm_desc = MID$(MID$(sv_x, sv_b, (sv_e - sv_b)),0,100)
					print(CUST.INTERFACE.crm_desc)
				#}
				ENDIF
				sv_x = CUST.INTERFACE.respXMLMsg
				sv_y = "<Entity>"
				sv_z = "</Entity>"
				sv_b = GETPOSITION(sv_x, sv_y) + STRLEN(sv_y) - 1
				sv_e = GETPOSITION(sv_x, sv_z) - 1
				IF (GETPOSITION(sv_x, sv_y) != 0) THEN
				#{
					CUST.INTERFACE.crm_entity = MID$(MID$(sv_x, sv_b, (sv_e - sv_b)),0,100)
					print(CUST.INTERFACE.crm_entity)
				#}
				ENDIF
				
			
			#}
			ENDIF
		#}
		ENDIF
		
		
	#}
	ELSE
	#{
		CUST.INTERFACE.crm_errorMsgFlg = "Y"
		sv_x = CUST.INTERFACE.respXMLMsg
		sv_y = "<Error>"
		sv_z = "</Error>"
		sv_b = GETPOSITION(sv_x, sv_y) + STRLEN(sv_y) - 1
		sv_e = GETPOSITION(sv_x, sv_z) - 1
		IF (GETPOSITION(sv_x, sv_y) != 0) THEN
		#{
			sv_x = CUST.INTERFACE.respXMLMsg
			sv_y = "<FIBusinessException>"
			sv_z = "</FIBusinessException>"
			sv_b = GETPOSITION(sv_x, sv_y) + STRLEN(sv_y) - 1
			sv_e = GETPOSITION(sv_x, sv_z) - 1
			IF (GETPOSITION(sv_x, sv_y) != 0) THEN
			#{
				sv_x = CUST.INTERFACE.respXMLMsg
				sv_y = "<ErrorDetail>"
				sv_z = "</ErrorDetail>"
				sv_b = GETPOSITION(sv_x, sv_y) + STRLEN(sv_y) - 1
				sv_e = GETPOSITION(sv_x, sv_z) - 1
				IF (GETPOSITION(sv_x, sv_y) != 0) THEN
				#{
					sv_x = CUST.INTERFACE.respXMLMsg
					sv_y = "<ErrorCode>"
					sv_z = "</ErrorCode>"
					sv_b = GETPOSITION(sv_x, sv_y) + STRLEN(sv_y) - 1
					sv_e = GETPOSITION(sv_x, sv_z) - 1
					IF (GETPOSITION(sv_x, sv_y) != 0) THEN
					#{
						CUST.INTERFACE.crm_errorCode = MID$(MID$(sv_x, sv_b, (sv_e - sv_b)),0,20)
						print(CUST.INTERFACE.crm_errorCode)
					#}
					ENDIF
					
					sv_x = CUST.INTERFACE.respXMLMsg
					sv_y = "<ErrorDesc>"
					sv_z = "</ErrorDesc>"
					sv_b = GETPOSITION(sv_x, sv_y) + STRLEN(sv_y) - 1
					sv_e = GETPOSITION(sv_x, sv_z) - 1
					IF (GETPOSITION(sv_x, sv_y) != 0) THEN
					#{
						CUST.INTERFACE.crm_errorDesc = MID$(MID$(sv_x, sv_b, (sv_e - sv_b)),0,100)
						print(CUST.INTERFACE.crm_errorDesc)
					#}
					ENDIF
					sv_x = CUST.INTERFACE.respXMLMsg
					sv_y = "<ErrorType>"
					sv_z = "</ErrorType>"
					sv_b = GETPOSITION(sv_x, sv_y) + STRLEN(sv_y) - 1
					sv_e = GETPOSITION(sv_x, sv_z) - 1
					IF (GETPOSITION(sv_x, sv_y) != 0) THEN
					#{
						CUST.INTERFACE.crm_errType = MID$(MID$(sv_x, sv_b, (sv_e - sv_b)),0,10)
						print(CUST.INTERFACE.crm_errType)
					#}
					ENDIF
				#}
				ENDIF
				
			#}
			ENDIF
		#}
		ENDIF
		
		IF((CUST.INTERFACE.crm_errorCode == "") AND (CUST.INTERFACE.crm_errorDesc == "")) THEN
		#{
			CUST.INTERFACE.crm_errorCode = "SYSERR"
			CUST.INTERFACE.crm_errorDesc = "Could not get response from server"
		#}
		ENDIF
	#}
	ENDIF
	
	PRINT(CUST.INTERFACE.crm_errorMsgFlg)
	
	PRINT(CUST.INTERFACE.crm_errorCode)
	PRINT(CUST.INTERFACE.crm_errorDesc)

	
	
	
	#TRACE OFF
END-->
