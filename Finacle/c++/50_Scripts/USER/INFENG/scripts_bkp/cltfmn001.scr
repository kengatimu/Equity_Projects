#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cltfmn001.scr
# Type: NA|default
# Description: script used on clicking of GO
# Author: haneesha.kanumuru
# Date: 2/6/18 5:38 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="4097b25feeb961025feef8d832b84e8cc01fbce06b86d1f4ec1f011c3c72b822"
	
	# Script body goes here
	TRACE ON
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CLTFM Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CLTFM")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CLTFM",5)
	#}
	ENDIF
	#==========================================================*
	# Intilializing the Variables
	#==========================================================*
	CUST.CLTFM.cifId =""
	CUST.CLTFM.acctId =""
	CUST.CLTFM.schmCode =""
	CUST.CLTFM.flg =""
	CUST.CLTFM.lchg_user =""
	CUST.CLTFM.percent =""
	CUST.CLTFM.delFlg =""
	IF(FIELDEXISTS(BANCS.INPUT.funcCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.funcCode) != "")THEN
		#{
			CUST.CLTFM.funcCode = TOUPPER(TRIM(BANCS.INPUT.funcCode))
			PRINT(CUST.CLTFM.funcCode)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.schmCode)) THEN
	#{
		IF(TRIM(BANCS.INPUT.schmCode) != "")THEN
		#{
			CUST.CLTFM.schmCode= TOUPPER(TRIM(BANCS.INPUT.schmCode))
			PRINT(CUST.CLTFM.schmCode)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.type)) THEN
	#{
		IF(TRIM(BANCS.INPUT.type) != "")THEN
		#{
			CUST.CLTFM.type= TOUPPER(TRIM(BANCS.INPUT.type))
			PRINT(CUST.CLTFM.type)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.cifId)) THEN
	#{
		IF(TRIM(BANCS.INPUT.cifId) != "")THEN
		#{
			CUST.CLTFM.cifId= TOUPPER(TRIM(BANCS.INPUT.cifId))
			PRINT(CUST.CLTFM.cifId)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.acctId)) THEN
	#{
		IF(TRIM(BANCS.INPUT.acctId) != "")THEN
		#{
			CUST.CLTFM.acctId= TOUPPER(TRIM(BANCS.INPUT.acctId))
			PRINT(CUST.CLTFM.acctId)
		#}
		ENDIF
	#}
	ENDIF
	
	#==========================================================*
	#For A-ADD
	#==========================================================*
	
	IF(CUST.CLTFM.funcCode == "A") THEN
	#{
		IF(CUST.CLTFM.cifId != "")THEN
		#{
			BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode != "") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
		#}
		ENDIF
		IF(CUST.CLTFM.acctId !="") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
			
			
		#}
		ENDIF
		BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" +BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		IF(CUST.CLTFM.cifId != "") THEN
		#{
			sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.CLTFM"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + "  AND CIF_ID = ?SVAR AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		
		IF(CUST.CLTFM.schmCode!="") THEN
		#{
			sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.CLTFM"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + " AND SCHEME_CODE = ?SVAR  AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		IF(CUST.CLTFM.acctId!="") THEN
		#{
			
			sv_s = "count|"
			sv_s = sv_s + " SELECT COUNT(*)"
			sv_s = sv_s + " FROM TBAADM.GAM "
			sv_s = sv_s + " WHERE FORACID = ?SVAR"
			sv_s = sv_s + " AND DEL_FLG != 'Y'"
			sv_s = sv_s + " AND ENTITY_CRE_FLG != 'N'"
			sv_s = sv_s + " AND acct_cls_flg !='Y'"
			sv_s = sv_s + " AND SCHM_TYPE = 'LAA'"
			sv_s = sv_s + " AND BANK_ID = ?SVAR"
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_b = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_b)
			
			CUST.CLTFM.count = BANCS.OUTPARAM.count
			PRINT(CUST.CLTFM.count)
			
			IF(CUST.CLTFM.count == 0)THEN
			#{
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error_1|Error^Only Loan Accounts needs to be Selected^moduleName")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
				
			#}
			ENDIF
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" +BANCS.INPARAM.BINDVARS
			
			sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.CLTFM"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + " AND ACCOUNT_NUMBER = ?SVAR  AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		
		PRINT(sv_s)
		
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		
		IF(sv_d != 0)THEN
		#{
			
		#}
		ENDIF
		
		CUST.CLTFM.recCount = BANCS.OUTPARAM.recCount
		PRINT(CUST.CLTFM.recCount)
		
		IF(CINT(CUST.CLTFM.recCount) > 0)THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Record already exists")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
	#}
	ENDIF
	sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
	PRINT(sv_u)
	
	
	#==========================================================*
	#For M-MODIFY
	#==========================================================*
	
	IF(CUST.CLTFM.funcCode == "M") THEN
	#{
		IF(CUST.CLTFM.cifId != "")THEN
		#{
			BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode != "") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
		#}
		ENDIF
		IF(CUST.CLTFM.acctId !="") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
			
			
		#}
		ENDIF
		BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" +BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		IF(CUST.CLTFM.cifId != "") THEN
		#{
			sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.CLTFM"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + "  AND CIF_ID = ?SVAR AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		
		IF(CUST.CLTFM.schmCode!="") THEN
		#{
			sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.CLTFM"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + " AND SCHEME_CODE = ?SVAR  AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		IF(CUST.CLTFM.acctId!="") THEN
		#{
			sv_s = "count|"
			sv_s = sv_s + " SELECT COUNT(*)"
			sv_s = sv_s + " FROM TBAADM.GAM "
			sv_s = sv_s + " WHERE FORACID = ?SVAR"
			sv_s = sv_s + " AND DEL_FLG != 'Y'"
			sv_s = sv_s + " AND ENTITY_CRE_FLG != 'N'"
			sv_s = sv_s + " AND acct_cls_flg !='Y'"
			sv_s = sv_s + " AND SCHM_TYPE = 'LAA'"
			sv_s = sv_s + " AND BANK_ID = ?SVAR"
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_b = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_b)
			
			CUST.CLTFM.count = BANCS.OUTPARAM.count
			PRINT(CUST.CLTFM.count)
			
			IF(CUST.CLTFM.count == 0)THEN
			#{
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error_1|Error^Only Loan Accounts needs to be Selected^moduleName")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
				
			#}
			ENDIF
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" +BANCS.INPARAM.BINDVARS
			
			sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.CLTFM"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + " AND ACCOUNT_NUMBER = ?SVAR  AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		
		PRINT(sv_s)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		
		IF(sv_d == 0)THEN
		#{
			CUST.CLTFM.recCount=BANCS.OUTPARAM.recCount
		#}
		ENDIF
		
		
		
		IF(CINT(CUST.CLTFM.recCount) == 0)THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Record does not exists")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		
		IF(CUST.CLTFM.cifId != "")THEN
		#{
			BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode != "") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
		#}
		ENDIF
		IF(CUST.CLTFM.acctId !="") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
			
			
		#}
		ENDIF
		BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" +BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		IF(CUST.CLTFM.cifId != "") THEN
		#{
			sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.CLTFM_MOD"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + "  AND CIF_ID = ?SVAR AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode!="") THEN
		#{
			sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.CLTFM_MOD"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + " AND SCHEME_CODE = ?SVAR  AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		IF(CUST.CLTFM.acctId!="") THEN
		#{
			sv_s = "count|"
			sv_s = sv_s + " SELECT COUNT(*)"
			sv_s = sv_s + " FROM TBAADM.GAM "
			sv_s = sv_s + " WHERE FORACID = ?SVAR"
			sv_s = sv_s + " AND DEL_FLG != 'Y'"
			sv_s = sv_s + " AND ENTITY_CRE_FLG != 'N'"
			sv_s = sv_s + " AND acct_cls_flg !='Y'"
			sv_s = sv_s + " AND SCHM_TYPE = 'LAA'"
			sv_s = sv_s + " AND BANK_ID = ?SVAR"
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_b = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_b)
			
			CUST.CLTFM.count = BANCS.OUTPARAM.count
			PRINT(CUST.CLTFM.count)
			
			IF(CUST.CLTFM.count == 0)THEN
			#{
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error_1|Error^Only Loan Accounts needs to be Selected^moduleName")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
				
			#}
			ENDIF
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" +BANCS.INPARAM.BINDVARS
			
			sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.CLTFM_MOD"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + " AND ACCOUNT_NUMBER = ?SVAR  AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d == 0)THEN
		#{
			CUST.CLTFM.recCount=BANCS.OUTPARAM.recCount
		#}
		ENDIF
		IF(CUST.CLTFM.cifId != "")THEN
		#{
			BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode != "") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
		#}
		ENDIF
		IF(CUST.CLTFM.acctId !="") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
			
			
		#}
		ENDIF
		IF(CUST.CLTFM.cifId != "") THEN
		#{
			sv_s = "lchg_user,flg,del_flg| SELECT LCHG_USER_ID,ENTITY_CRE_FLAG,DEL_FLG "
			IF(CUST.CLTFM.recCount > 0) THEN
			#{
				sv_s = sv_s + "FROM CUSTOM.CLTFM_MOD"
				
				#}
			ELSE
				#{
				sv_s = sv_s + "FROM CUSTOM.CLTFM"
			#}
			ENDIF
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			sv_s = sv_s + " AND CIF_ID =?SVAR AND BANK_ID = ?SVAR"
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode!="") THEN
		#{
			sv_s = "lchg_user,flg,del_flg| SELECT LCHG_USER_ID,ENTITY_CRE_FLAG,DEL_FLG "
			IF(CUST.CLTFM.recCount > 0) THEN
			#{
				sv_s = sv_s + "FROM CUSTOM.CLTFM_MOD"
				
				#}
			ELSE
				#{
				sv_s = sv_s + "FROM CUSTOM.CLTFM"
			#}
			ENDIF
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			sv_s = sv_s + " AND SCHEME_CODE =?SVAR AND BANK_ID = ?SVAR"
		#}
		ENDIF
		IF(CUST.CLTFM.acctId!="") THEN
		#{
			sv_s = "lchg_user,flg,del_flg| SELECT LCHG_USER_ID,ENTITY_CRE_FLAG,DEL_FLG "
			IF(CUST.CLTFM.recCount > 0) THEN
			#{
				sv_s = sv_s + "FROM CUSTOM.CLTFM_MOD"
				
				#}
			ELSE
				#{
				sv_s = sv_s + "FROM CUSTOM.CLTFM"
			#}
			ENDIF
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			sv_s = sv_s + " AND ACCOUNT_NUMBER =?SVAR AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" + BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
		
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d==0) THEN
		#{
			
			CUST.CLTFM.lchg_user = BANCS.OUTPARAM.lchg_user
			CUST.CLTFM.flg = BANCS.OUTPARAM.flg
			CUST.CLTFM.delFlg= BANCS.OUTPARAM.del_flg
			PRINT(CUST.CLTFM.delFlg)
			PRINT(CUST.CLTFM.lchg_user)
		#}
		ENDIF
		
		IF((CUST.CLTFM.flg == "N") AND (CUST.CLTFM.lchg_user != BANCS.STDIN.userId))THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Last changes are not verified only same user can modify")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
		IF(CUST.CLTFM.cifId != "")THEN
		#{
			BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode != "") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
		#}
		ENDIF
		IF(CUST.CLTFM.acctId !="") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
			
			
		#}
		ENDIF
		IF(CUST.CLTFM.cifId != "") THEN
		#{
			sv_a = "schm,cif,acct,tax,percent| select SCHEME_CODE,CIF_ID,ACCOUNT_NUMBER,TAX_APPLICABLE,TAX_PERCENTAGE "
			IF(CUST.CLTFM.recCount > 0) THEN
			#{
				sv_a = sv_a + "FROM CUSTOM.CLTFM_MOD"
				
				#}
			ELSE
				#{
				sv_a = sv_a + "FROM CUSTOM.CLTFM"
			#}
			ENDIF
			sv_a = sv_a + " where TYPE_LEVEL=?SVAR"
			sv_a = sv_a + " AND  CIF_ID =?SVAR AND BANK_ID=?SVAR"
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode!="") THEN
		#{
			sv_a = "schm,cif,acct,tax,percent| select SCHEME_CODE,CIF_ID,ACCOUNT_NUMBER,TAX_APPLICABLE,TAX_PERCENTAGE "
			IF(CUST.CLTFM.recCount > 0) THEN
			#{
				sv_a = sv_a + "FROM CUSTOM.CLTFM_MOD"
				
				#}
			ELSE
				#{
				sv_a = sv_a + "FROM CUSTOM.CLTFM"
			#}
			ENDIF
			sv_a = sv_a + " where TYPE_LEVEL=?SVAR"
			sv_a = sv_a + " AND  SCHEME_CODE =?SVAR AND BANK_ID=?SVAR"
		#}
		ENDIF
		
		IF(CUST.CLTFM.acctId!="") THEN
		#{
			sv_a = "schm,cif,acct,tax,percent| select SCHEME_CODE,CIF_ID,ACCOUNT_NUMBER,TAX_APPLICABLE,TAX_PERCENTAGE "
			IF(CUST.CLTFM.recCount > 0) THEN
			#{
				sv_a = sv_a + "FROM CUSTOM.CLTFM_MOD"
				
				#}
			ELSE
				#{
				sv_a = sv_a + "FROM CUSTOM.CLTFM"
			#}
			ENDIF
			sv_a = sv_a + " where TYPE_LEVEL=?SVAR"
			sv_a = sv_a + " AND  ACCOUNT_NUMBER =?SVAR AND BANK_ID=?SVAR"
		#}
		ENDIF
		PRINT(sv_a)
		
		BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" + BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
		
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d =urhk_dbSelectWithBind(sv_a)
		PRINT(sv_d)
		
		#		PRINT(BANCS.OUTPARAM.schm)
		#		PRINT(BANCS.OUTPARAM.cif)
		#		PRINT(BANCS.OUTPARAM.tax)
		
		CUST.CLTFM.schmCode=BANCS.OUTPARAM.schm
		PRINT(CUST.CLTFM.schmCode)
		CUST.CLTFM.cifId=BANCS.OUTPARAM.cif
		PRINT(CUST.CLTFM.cifId)
		CUST.CLTFM.acctId = BANCS.OUTPARAM.acct
		PRINT(CUST.CLTFM.acctId)
		CUST.CLTFM.tax=BANCS.OUTPARAM.tax
		PRINT(CUST.CLTFM.tax)
		CUST.CLTFM.percent = BANCS.OUTPARAM.percent
		PRINT(CUST.CLTFM.percent)
		
		sv_u = urhk_SetOrbOut("schmCode|"+ CUST.CLTFM.schmCode)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("cifId|"+ CUST.CLTFM.cifId)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("acctId|"+ CUST.CLTFM.acctId)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("tax|"+ CUST.CLTFM.tax)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("percentage|"+ CUST.CLTFM.percent)
		PRINT(sv_u)
	#}
	ENDIF
	
	
	IF(CUST.CLTFM.funcCode == "I") THEN
	#{
		IF(CUST.CLTFM.cifId != "")THEN
		#{
			BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode != "") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
		#}
		ENDIF
		IF(CUST.CLTFM.acctId !="") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
			
			
		#}
		ENDIF
		BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" +BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		IF(CUST.CLTFM.cifId != "") THEN
		#{
			sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.CLTFM"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + "  AND CIF_ID = ?SVAR AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		
		IF(CUST.CLTFM.schmCode!="") THEN
		#{
			sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.CLTFM"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + " AND SCHEME_CODE = ?SVAR  AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		IF(CUST.CLTFM.acctId!="") THEN
		#{
			sv_s = "count|"
			sv_s = sv_s + " SELECT COUNT(*)"
			sv_s = sv_s + " FROM TBAADM.GAM "
			sv_s = sv_s + " WHERE FORACID = ?SVAR"
			sv_s = sv_s + " AND DEL_FLG != 'Y'"
			sv_s = sv_s + " AND ENTITY_CRE_FLG != 'N'"
			sv_s = sv_s + " AND acct_cls_flg !='Y'"
			sv_s = sv_s + " AND SCHM_TYPE = 'LAA'"
			sv_s = sv_s + " AND BANK_ID = ?SVAR"
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_b = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_b)
			
			CUST.CLTFM.count = BANCS.OUTPARAM.count
			PRINT(CUST.CLTFM.count)
			
			IF(CUST.CLTFM.count == 0)THEN
			#{
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error_1|Error^Only Loan Accounts needs to be Selected^moduleName")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
				
			#}
			ENDIF
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" +BANCS.INPARAM.BINDVARS
			
			sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.CLTFM"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + " AND ACCOUNT_NUMBER = ?SVAR  AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		
		PRINT(sv_s)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		
		IF(sv_d != 0)THEN
		#{
			
		#}
		ENDIF
		
		CUST.CLTFM.recCount = BANCS.OUTPARAM.recCount
		PRINT(CUST.CLTFM.recCount)
		
		IF(CINT(CUST.CLTFM.recCount) == 0)THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Record does not exists")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		IF(CUST.CLTFM.cifId != "")THEN
		#{
			BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode != "") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
		#}
		ENDIF
		IF(CUST.CLTFM.acctId !="") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
			
			
		#}
		ENDIF
		BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" +BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		IF(CUST.CLTFM.cifId != "") THEN
		#{
			sv_s = "lchg_user,flg,del_flg| SELECT LCHG_USER_ID,ENTITY_CRE_FLAG,DEL_FLG FROM CUSTOM.CLTFM"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			sv_s = sv_s + " AND  CIF_ID =?SVAR AND BANK_ID=?SVAR"
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode!="") THEN
		#{
			sv_s = "lchg_user,flg,del_flg| SELECT LCHG_USER_ID,ENTITY_CRE_FLAG,DEL_FLG FROM CUSTOM.CLTFM"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			sv_s = sv_s + " AND  SCHEME_CODE =?SVAR AND BANK_ID=?SVAR"
		#}
		ENDIF
		IF(CUST.CLTFM.acctId!="") THEN
		#{
			sv_s = "lchg_user,flg,del_flg| SELECT LCHG_USER_ID,ENTITY_CRE_FLAG,DEL_FLG FROM CUSTOM.CLTFM"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			sv_s = sv_s + " AND  ACCOUNT_NUMBER =?SVAR AND BANK_ID=?SVAR"
		#}
		ENDIF
		PRINT(sv_s)
		
		
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d==0) THEN
		#{
			
			CUST.CLTFM.lchg_user = BANCS.OUTPARAM.lchg_user
			CUST.CLTFM.flg = BANCS.OUTPARAM.flg
			CUST.CLTFM.delFlg= BANCS.OUTPARAM.del_flg
			PRINT(CUST.CLTFM.delFlg)
			PRINT(CUST.CLTFM.lchg_user)
		#}
		ENDIF
		IF((CUST.CLTFM.flg == "N") AND (CUST.CLTFM.lchg_user != BANCS.STDIN.userId) AND (CUST.CLTFM.delFlg !='Y'))THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Last changes are not verified only same user can Inquire")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
		
		IF(CUST.CLTFM.cifId != "")THEN
		#{
			BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode != "") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
		#}
		ENDIF
		IF(CUST.CLTFM.acctId !="") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
			
			
		#}
		ENDIF
		IF(CUST.CLTFM.cifId != "") THEN
		#{
			sv_a = "schm,cif,acct,tax,percent| select SCHEME_CODE,CIF_ID,ACCOUNT_NUMBER,TAX_APPLICABLE,TAX_PERCENTAGE FROM CUSTOM.CLTFM "
			
			sv_a = sv_a + " where TYPE_LEVEL=?SVAR"
			sv_a = sv_a + " AND  CIF_ID =?SVAR AND BANK_ID=?SVAR"
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode!="") THEN
		#{
			sv_a = "schm,cif,acct,tax,percent| select SCHEME_CODE,CIF_ID,ACCOUNT_NUMBER,TAX_APPLICABLE,TAX_PERCENTAGE from custom.cltfm "
			
			sv_a = sv_a + " where TYPE_LEVEL=?SVAR"
			sv_a = sv_a + " AND  SCHEME_CODE =?SVAR AND BANK_ID=?SVAR"
		#}
		ENDIF
		
		IF(CUST.CLTFM.acctId!="") THEN
		#{
			sv_a = "schm,cif,acct,tax,percent| select SCHEME_CODE,CIF_ID,ACCOUNT_NUMBER,TAX_APPLICABLE,TAX_PERCENTAGE from custom.cltfm "
			
			sv_a = sv_a + " where TYPE_LEVEL=?SVAR"
			sv_a = sv_a + " AND  ACCOUNT_NUMBER =?SVAR AND BANK_ID=?SVAR"
		#}
		ENDIF
		PRINT(sv_a)
		
		BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" + BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
		
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d =urhk_dbSelectWithBind(sv_a)
		PRINT(sv_d)
		
		PRINT(BANCS.OUTPARAM.schm)
		PRINT(BANCS.OUTPARAM.cif)
		PRINT(BANCS.OUTPARAM.acct)
		PRINT(BANCS.OUTPARAM.tax)
		PRINT(BANCS.OUTPARAM.percent)
		CUST.CLTFM.schmCode=BANCS.OUTPARAM.schm
		PRINT(CUST.CLTFM.schmCode)
		CUST.CLTFM.cifId=BANCS.OUTPARAM.cif
		PRINT(CUST.CLTFM.cifId)
		CUST.CLTFM.acctId = BANCS.OUTPARAM.acct
		PRINT(CUST.CLTFM.acctId)
		CUST.CLTFM.tax=BANCS.OUTPARAM.tax
		PRINT(CUST.CLTFM.tax)
		CUST.CLTFM.percent=BANCS.OUTPARAM.percent
		PRINT(CUST.CLTFM.percent)
		sv_u = urhk_SetOrbOut("schmCode|"+ CUST.CLTFM.schmCode)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("cifId|"+ CUST.CLTFM.cifId)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("acctId|"+ CUST.CLTFM.acctId)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("tax|"+ CUST.CLTFM.tax)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("percentage|"+ CUST.CLTFM.percent)
		PRINT(sv_u)
		
	#}
	ENDIF
	
	
	IF(CUST.CLTFM.funcCode == "V") THEN
	#{
		IF(CUST.CLTFM.cifId != "")THEN
		#{
			BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode != "") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
		#}
		ENDIF
		IF(CUST.CLTFM.acctId !="") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
			
			
		#}
		ENDIF
		BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" +BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		IF(CUST.CLTFM.cifId != "") THEN
		#{
			sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.CLTFM"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + "  AND CIF_ID = ?SVAR AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		
		IF(CUST.CLTFM.schmCode!="") THEN
		#{
			sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.CLTFM"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + " AND SCHEME_CODE = ?SVAR  AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		IF(CUST.CLTFM.acctId!="") THEN
		#{
			sv_s = "count|"
			sv_s = sv_s + " SELECT COUNT(*)"
			sv_s = sv_s + " FROM TBAADM.GAM "
			sv_s = sv_s + " WHERE FORACID = ?SVAR"
			sv_s = sv_s + " AND DEL_FLG != 'Y'"
			sv_s = sv_s + " AND ENTITY_CRE_FLG != 'N'"
			sv_s = sv_s + " AND acct_cls_flg !='Y'"
			sv_s = sv_s + " AND SCHM_TYPE = 'LAA'"
			sv_s = sv_s + " AND BANK_ID = ?SVAR"
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_b = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_b)
			
			CUST.CLTFM.count = BANCS.OUTPARAM.count
			PRINT(CUST.CLTFM.count)
			
			IF(CUST.CLTFM.count == 0)THEN
			#{
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error_1|Error^Only Loan Accounts needs to be Selected^moduleName")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
				
			#}
			ENDIF
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" +BANCS.INPARAM.BINDVARS
			
			sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.CLTFM"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + " AND ACCOUNT_NUMBER = ?SVAR  AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		
		PRINT(sv_s)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		
		IF(sv_d == 0)THEN
		#{
			CUST.CLTFM.recCount=BANCS.OUTPARAM.recCount
		#}
		ENDIF
		
		
		
		IF(CINT(CUST.CLTFM.recCount) == 0)THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Record does not exists")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		
		IF(CUST.CLTFM.cifId != "")THEN
		#{
			BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode != "") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
		#}
		ENDIF
		IF(CUST.CLTFM.acctId !="") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
			
			
		#}
		ENDIF
		BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" +BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		IF(CUST.CLTFM.cifId != "") THEN
		#{
			sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.CLTFM_MOD"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + "  AND CIF_ID = ?SVAR AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode!="") THEN
		#{
			sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.CLTFM_MOD"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + " AND SCHEME_CODE = ?SVAR  AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		IF(CUST.CLTFM.acctId!="") THEN
		#{
			sv_s = "count|"
			sv_s = sv_s + " SELECT COUNT(*)"
			sv_s = sv_s + " FROM TBAADM.GAM "
			sv_s = sv_s + " WHERE FORACID = ?SVAR"
			sv_s = sv_s + " AND DEL_FLG != 'Y'"
			sv_s = sv_s + " AND ENTITY_CRE_FLG != 'N'"
			sv_s = sv_s + " AND acct_cls_flg !='Y'"
			sv_s = sv_s + " AND SCHM_TYPE = 'LAA'"
			sv_s = sv_s + " AND BANK_ID = ?SVAR"
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_b = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_b)
			
			CUST.CLTFM.count = BANCS.OUTPARAM.count
			PRINT(CUST.CLTFM.count)
			
			IF(CUST.CLTFM.count == 0)THEN
			#{
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error_1|Error^Only Loan Accounts needs to be Selected^moduleName")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
				
			#}
			ENDIF
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" +BANCS.INPARAM.BINDVARS
			
			sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.CLTFM_MOD"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + " AND ACCOUNT_NUMBER = ?SVAR  AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d == 0)THEN
		#{
			CUST.CLTFM.recCount=BANCS.OUTPARAM.recCount
		#}
		ENDIF
		
		IF(CUST.CLTFM.cifId != "")THEN
		#{
			BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode != "") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
		#}
		ENDIF
		IF(CUST.CLTFM.acctId !="") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
			
			
		#}
		ENDIF
		IF(CUST.CLTFM.cifId != "") THEN
		#{
			sv_s = "lchg_user,flg,del_flg| SELECT LCHG_USER_ID,ENTITY_CRE_FLAG,DEL_FLG "
			IF(CUST.CLTFM.recCount > 0) THEN
			#{
				sv_s = sv_s + "FROM CUSTOM.CLTFM_MOD"
				
				#}
			ELSE
				#{
				sv_s = sv_s + "FROM CUSTOM.CLTFM"
			#}
			ENDIF
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			sv_s = sv_s + " AND CIF_ID =?SVAR AND BANK_ID = ?SVAR"
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode!="") THEN
		#{
			sv_s = "lchg_user,flg,del_flg| SELECT LCHG_USER_ID,ENTITY_CRE_FLAG,DEL_FLG "
			IF(CUST.CLTFM.recCount > 0) THEN
			#{
				sv_s = sv_s + "FROM CUSTOM.CLTFM_MOD"
				
				#}
			ELSE
				#{
				sv_s = sv_s + "FROM CUSTOM.CLTFM"
			#}
			ENDIF
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			sv_s = sv_s + " AND SCHEME_CODE =?SVAR AND BANK_ID = ?SVAR"
		#}
		ENDIF
		IF(CUST.CLTFM.acctId!="") THEN
		#{
			sv_s = "lchg_user,flg,del_flg| SELECT LCHG_USER_ID,ENTITY_CRE_FLAG,DEL_FLG "
			IF(CUST.CLTFM.recCount > 0) THEN
			#{
				sv_s = sv_s + "FROM CUSTOM.CLTFM_MOD"
				
				#}
			ELSE
				#{
				sv_s = sv_s + "FROM CUSTOM.CLTFM"
			#}
			ENDIF
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			sv_s = sv_s + " AND ACCOUNT_NUMBER =?SVAR AND BANK_ID = ?SVAR"
		#}
		ENDIF
		PRINT(sv_s)
		
		BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" + BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
		
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d==0) THEN
		#{
			
			CUST.CLTFM.lchg_user = BANCS.OUTPARAM.lchg_user
			CUST.CLTFM.flg = BANCS.OUTPARAM.flg
			CUST.CLTFM.delFlg= BANCS.OUTPARAM.del_flg
			PRINT(CUST.CLTFM.delFlg)
			PRINT(CUST.CLTFM.lchg_user)
		#}
		ENDIF
		IF(CUST.CLTFM.flg == "Y") THEN
		#{
			#			sv_u = urhk_SetOrbOut("Error|Y")
			#			sv_a = urhk_SetOrbOut("Error_01|Error :^Nothing to verify^acctno")
			sv_a =urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_a)
			sv_u = urhk_SetOrbErr("There is nothing to verify")
			PRINT(sv_u)
			
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		
		IF(CUST.CLTFM.lchg_user == BANCS.STDIN.userId) THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Same user cannot verify")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
		
		IF(CUST.CLTFM.cifId != "")THEN
		#{
			BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode != "") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
		#}
		ENDIF
		IF(CUST.CLTFM.acctId !="") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
			
			
		#}
		ENDIF
		IF(CUST.CLTFM.cifId != "") THEN
		#{
			sv_a = "schm,cif,acct,tax,percent| select SCHEME_CODE,CIF_ID,ACCOUNT_NUMBER,TAX_APPLICABLE,TAX_PERCENTAGE "
			IF(CUST.CLTFM.recCount > 0) THEN
			#{
				sv_a = sv_a + "FROM CUSTOM.CLTFM_MOD"
				
				#}
			ELSE
				#{
				sv_a = sv_a + "FROM CUSTOM.CLTFM"
			#}
			ENDIF
			sv_a = sv_a + " where TYPE_LEVEL=?SVAR"
			sv_a = sv_a + " AND  CIF_ID =?SVAR AND BANK_ID=?SVAR"
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode!="") THEN
		#{
			sv_a = "schm,cif,acct,tax,percent| select SCHEME_CODE,CIF_ID,ACCOUNT_NUMBER,TAX_APPLICABLE,TAX_PERCENTAGE "
			IF(CUST.CLTFM.recCount > 0) THEN
			#{
				sv_a = sv_a + "FROM CUSTOM.CLTFM_MOD"
				
				#}
			ELSE
				#{
				sv_a = sv_a + "FROM CUSTOM.CLTFM"
			#}
			ENDIF
			sv_a = sv_a + " where TYPE_LEVEL=?SVAR"
			sv_a = sv_a + " AND  SCHEME_CODE =?SVAR AND BANK_ID=?SVAR"
		#}
		ENDIF
		
		IF(CUST.CLTFM.acctId!="") THEN
		#{
			sv_a = "schm,cif,acct,tax,percent| select SCHEME_CODE,CIF_ID,ACCOUNT_NUMBER,TAX_APPLICABLE,TAX_PERCENTAGE "
			IF(CUST.CLTFM.recCount > 0) THEN
			#{
				sv_a = sv_a + "FROM CUSTOM.CLTFM_MOD"
				
				#}
			ELSE
				#{
				sv_a = sv_a + "FROM CUSTOM.CLTFM"
			#}
			ENDIF
			sv_a = sv_a + " where TYPE_LEVEL=?SVAR"
			sv_a = sv_a + " AND  ACCOUNT_NUMBER =?SVAR AND BANK_ID=?SVAR"
		#}
		ENDIF
		PRINT(sv_a)
		
		BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" + BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
		
		
		
		
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d =urhk_dbSelectWithBind(sv_a)
		PRINT(sv_d)
		
		PRINT(BANCS.OUTPARAM.schm)
		PRINT(BANCS.OUTPARAM.cif)
		PRINT(BANCS.OUTPARAM.acct)
		PRINT(BANCS.OUTPARAM.tax)
		PRINT(BANCS.OUTPARAM.percent)
		CUST.CLTFM.schmCode=BANCS.OUTPARAM.schm
		PRINT(CUST.CLTFM.schmCode)
		CUST.CLTFM.cifId=BANCS.OUTPARAM.cif
		PRINT(CUST.CLTFM.cifId)
		CUST.CLTFM.acctId = BANCS.OUTPARAM.acct
		PRINT(CUST.CLTFM.acctId)
		CUST.CLTFM.tax=BANCS.OUTPARAM.tax
		PRINT(CUST.CLTFM.tax)
		CUST.CLTFM.percent= BANCS.OUTPARAM.percent
		PRINT(CUST.CLTFM.percent)
		sv_u = urhk_SetOrbOut("schmCode|"+ CUST.CLTFM.schmCode)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("cifId|"+ CUST.CLTFM.cifId)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("acctId|"+ CUST.CLTFM.acctId)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("tax|"+ CUST.CLTFM.tax)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("percentage|"+ CUST.CLTFM.percent)
		PRINT(sv_u)
		
		
		
	#}
	ENDIF
	
	
	
	IF(CUST.CLTFM.funcCode == "X") THEN
	#{
		IF(CUST.CLTFM.cifId != "")THEN
		#{
			BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode != "") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
		#}
		ENDIF
		IF(CUST.CLTFM.acctId !="") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
			
			
		#}
		ENDIF
		BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" +BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		IF(CUST.CLTFM.cifId != "") THEN
		#{
			sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.CLTFM"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + "  AND CIF_ID = ?SVAR AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		
		IF(CUST.CLTFM.schmCode!="") THEN
		#{
			sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.CLTFM"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + " AND SCHEME_CODE = ?SVAR  AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		IF(CUST.CLTFM.acctId!="") THEN
		#{
			sv_s = "count|"
			sv_s = sv_s + " SELECT COUNT(*)"
			sv_s = sv_s + " FROM TBAADM.GAM "
			sv_s = sv_s + " WHERE FORACID = ?SVAR"
			sv_s = sv_s + " AND DEL_FLG != 'Y'"
			sv_s = sv_s + " AND ENTITY_CRE_FLG != 'N'"
			sv_s = sv_s + " AND acct_cls_flg !='Y'"
			sv_s = sv_s + " AND SCHM_TYPE = 'LAA'"
			sv_s = sv_s + " AND BANK_ID = ?SVAR"
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_b = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_b)
			
			CUST.CLTFM.count = BANCS.OUTPARAM.count
			PRINT(CUST.CLTFM.count)
			
			IF(CUST.CLTFM.count == 0)THEN
			#{
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error_1|Error^Only Loan Accounts needs to be Selected^moduleName")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
				
			#}
			ENDIF
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" +BANCS.INPARAM.BINDVARS
			
			sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.CLTFM"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + " AND ACCOUNT_NUMBER = ?SVAR  AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		PRINT(sv_s)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		
		IF(sv_d == 0)THEN
		#{
			CUST.CLTFM.recCount=BANCS.OUTPARAM.recCount
		#}
		ENDIF
		
		
		
		IF(CINT(CUST.CLTFM.recCount) == 0)THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbErr("Record does not exists")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		
		IF(CUST.CLTFM.cifId != "")THEN
		#{
			BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode != "") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
		#}
		ENDIF
		IF(CUST.CLTFM.acctId !="") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
			
			
		#}
		ENDIF
		BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" +BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		IF(CUST.CLTFM.cifId != "") THEN
		#{
			sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.CLTFM_MOD"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + "  AND CIF_ID = ?SVAR AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode!="") THEN
		#{
			sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.CLTFM_MOD"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + " AND SCHEME_CODE = ?SVAR  AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		IF(CUST.CLTFM.acctId!="") THEN
		#{
			sv_s = "count|"
			sv_s = sv_s + " SELECT COUNT(*)"
			sv_s = sv_s + " FROM TBAADM.GAM "
			sv_s = sv_s + " WHERE FORACID = ?SVAR"
			sv_s = sv_s + " AND DEL_FLG != 'Y'"
			sv_s = sv_s + " AND ENTITY_CRE_FLG != 'N'"
			sv_s = sv_s + " AND acct_cls_flg !='Y'"
			sv_s = sv_s + " AND SCHM_TYPE = 'LAA'"
			sv_s = sv_s + " AND BANK_ID = ?SVAR"
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_b = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_b)
			
			CUST.CLTFM.count = BANCS.OUTPARAM.count
			PRINT(CUST.CLTFM.count)
			
			IF(CUST.CLTFM.count == 0)THEN
			#{
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error_1|Error^Only Loan Accounts needs to be Selected^moduleName")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
				
			#}
			ENDIF
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" +BANCS.INPARAM.BINDVARS
			
			sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.CLTFM_MOD"
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			#sv_s = sv_s + " AND CIF_ID=?SVAR"
			sv_s = sv_s + " AND ACCOUNT_NUMBER = ?SVAR  AND BANK_ID = ?SVAR"
			
		#}
		ENDIF
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d == 0)THEN
		#{
			CUST.CLTFM.recCount=BANCS.OUTPARAM.recCount
		#}
		ENDIF
		
		IF(CUST.CLTFM.cifId != "")THEN
		#{
			BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode != "") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
		#}
		ENDIF
		IF(CUST.CLTFM.acctId !="") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
			
			
		#}
		ENDIF
		IF(CUST.CLTFM.cifId != "") THEN
		#{
			sv_s = "lchg_user,flg,del_flg| SELECT LCHG_USER_ID,ENTITY_CRE_FLAG,DEL_FLG "
			IF(CUST.CLTFM.recCount > 0) THEN
			#{
				sv_s = sv_s + "FROM CUSTOM.CLTFM_MOD"
				
				#}
			ELSE
				#{
				sv_s = sv_s + "FROM CUSTOM.CLTFM"
			#}
			ENDIF
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			sv_s = sv_s + " AND CIF_ID =?SVAR AND BANK_ID = ?SVAR"
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode!="") THEN
		#{
			sv_s = "lchg_user,flg,del_flg| SELECT LCHG_USER_ID,ENTITY_CRE_FLAG,DEL_FLG "
			IF(CUST.CLTFM.recCount > 0) THEN
			#{
				sv_s = sv_s + "FROM CUSTOM.CLTFM_MOD"
				
				#}
			ELSE
				#{
				sv_s = sv_s + "FROM CUSTOM.CLTFM"
			#}
			ENDIF
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			sv_s = sv_s + " AND SCHEME_CODE =?SVAR AND BANK_ID = ?SVAR"
		#}
		ENDIF
		IF(CUST.CLTFM.acctId!="") THEN
		#{
			sv_s = "lchg_user,flg,del_flg| SELECT LCHG_USER_ID,ENTITY_CRE_FLAG,DEL_FLG "
			IF(CUST.CLTFM.recCount > 0) THEN
			#{
				sv_s = sv_s + "FROM CUSTOM.CLTFM_MOD"
				
				#}
			ELSE
				#{
				sv_s = sv_s + "FROM CUSTOM.CLTFM"
			#}
			ENDIF
			sv_s = sv_s + " WHERE TYPE_LEVEL = ?SVAR"
			sv_s = sv_s + " AND ACCOUNT_NUMBER =?SVAR AND BANK_ID = ?SVAR"
		#}
		ENDIF
		PRINT(sv_s)
		
		BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" + BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
		
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d==0) THEN
		#{
			
			CUST.CLTFM.lchg_user = BANCS.OUTPARAM.lchg_user
			CUST.CLTFM.flg = BANCS.OUTPARAM.flg
			CUST.CLTFM.delFlg= BANCS.OUTPARAM.del_flg
			PRINT(CUST.CLTFM.delFlg)
			PRINT(CUST.CLTFM.lchg_user)
		#}
		ENDIF
		
		IF((CUST.CLTFM.flg !="N") AND (CUST.CLTFM.recCount == 0)) THEN
		#{
			sv_m = urhk_SetOrbErr("Noting to Cancel")
			sv_z = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		IF((CUST.CLTFM.lchg_user != BANCS.STDIN.userId)) THEN
		#{
			sv_m = urhk_SetOrbErr("Last changes are not verified only same user can Cancel")
			sv_z = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		IF(CUST.CLTFM.cifId != "")THEN
		#{
			BANCS.INPARAM.BINDVARS =  CUST.CLTFM.cifId
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode != "") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.schmCode
		#}
		ENDIF
		IF(CUST.CLTFM.acctId !="") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId
			
			
		#}
		ENDIF
		IF(CUST.CLTFM.cifId != "") THEN
		#{
			sv_a = "schm,cif,acct,tax,percent| select SCHEME_CODE,CIF_ID,ACCOUNT_NUMBER,TAX_APPLICABLE,TAX_PERCENTAGE "
			IF(CUST.CLTFM.recCount > 0) THEN
			#{
				sv_a = sv_a + "FROM CUSTOM.CLTFM_MOD"
				
				#}
			ELSE
				#{
				sv_a = sv_a + "FROM CUSTOM.CLTFM"
			#}
			ENDIF
			sv_a = sv_a + " where TYPE_LEVEL=?SVAR"
			sv_a = sv_a + " AND  CIF_ID =?SVAR AND BANK_ID=?SVAR"
		#}
		ENDIF
		IF(CUST.CLTFM.schmCode!="") THEN
		#{
			sv_a = "schm,cif,acct,tax,percent| select SCHEME_CODE,CIF_ID,ACCOUNT_NUMBER,TAX_APPLICABLE,TAX_PERCENTAGE "
			IF(CUST.CLTFM.recCount > 0) THEN
			#{
				sv_a = sv_a + "FROM CUSTOM.CLTFM_MOD"
				
				#}
			ELSE
				#{
				sv_a = sv_a + "FROM CUSTOM.CLTFM"
			#}
			ENDIF
			sv_a = sv_a + " where TYPE_LEVEL=?SVAR"
			sv_a = sv_a + " AND  SCHEME_CODE =?SVAR AND BANK_ID=?SVAR"
		#}
		ENDIF
		
		IF(CUST.CLTFM.acctId!="") THEN
		#{
			sv_a = "schm,cif,acct,tax,percent| select SCHEME_CODE,CIF_ID,ACCOUNT_NUMBER,TAX_APPLICABLE,TAX_PERCENTAGE "
			IF(CUST.CLTFM.recCount > 0) THEN
			#{
				sv_a = sv_a + "FROM CUSTOM.CLTFM_MOD"
				
				#}
			ELSE
				#{
				sv_a = sv_a + "FROM CUSTOM.CLTFM"
			#}
			ENDIF
			sv_a = sv_a + " where TYPE_LEVEL=?SVAR"
			sv_a = sv_a + " AND  ACCOUNT_NUMBER =?SVAR AND BANK_ID=?SVAR"
		#}
		ENDIF
		PRINT(sv_a)
		
		BANCS.INPARAM.BINDVARS = CUST.CLTFM.type + "|" + BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
		
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d =urhk_dbSelectWithBind(sv_a)
		PRINT(sv_d)
		
		PRINT(BANCS.OUTPARAM.schm)
		PRINT(BANCS.OUTPARAM.cif)
		PRINT(BANCS.OUTPARAM.acct)
		PRINT(BANCS.OUTPARAM.tax)
		PRINT(BANCS.OUTPARAM.percent)
		
		CUST.CLTFM.schmCode=BANCS.OUTPARAM.schm
		PRINT(CUST.CLTFM.schmCode)
		CUST.CLTFM.cifId=BANCS.OUTPARAM.cif
		PRINT(CUST.CLTFM.cifId)
		CUST.CLTFM.acctId = BANCS.OUTPARAM.acct
		PRINT(CUST.CLTFM.acctId)
		CUST.CLTFM.tax=BANCS.OUTPARAM.tax
		PRINT(CUST.CLTFM.tax)
		CUST.CLTFM.percent =BANCS.OUTPARAM.percent
		PRINT(CUST.CLTFM.percent)
		
		sv_u = urhk_SetOrbOut("schmCode|"+ CUST.CLTFM.schmCode)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("cifId|"+ CUST.CLTFM.cifId)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("acctId|"+ CUST.CLTFM.acctId)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("tax|"+ CUST.CLTFM.tax)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("percentage|"+ CUST.CLTFM.percent)
		PRINT(sv_u)
	#}
	ENDIF
	
	sv_z = urhk_SetOrbOut("SuccessOrFailure|Y")
	PRINT(sv_z)
	ENDOFSCRIPT:
		TRACE OFF
END-->
