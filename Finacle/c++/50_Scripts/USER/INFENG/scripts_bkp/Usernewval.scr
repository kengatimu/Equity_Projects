	
<--START
CHECKSUM="c9787b87ea084508ccf525d0b277bc53d5661f1c2db3234e4049b5389a233ea8"
	TRACE ON
	
	####################################################################
	#section      :creating new repository if required
	#Description  :Checks for the existence of the repository STDWFS and
	#               creates a new one by that name if it does not exist
	#####################################################################
	
	IF (REPEXISTS("CUST") == 0 ) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	############################################################################
	#section      :creating new class if required
	#Desscription :checks for the existence of the class GLBDATA in the
	#repository
	#               create a new one if it does not exist
	##############################################################################
	
	
	IF (CLASSEXISTS("CUST","DATA") == 0) THEN
	#{
		CREATECLASS("CUST","DATA",5)
	#}
	ENDIF
	
	############################################
	#Assigns the values to the variables
	############################################
	
	sv_a = urhk_b2k_PrintRepos("BANCS")
	print(sv_a)
	sv_y=0
	
	IF( (FIELDEXISTS(("BANCS").("INPUT").("Employee_id"))== 0) ) THEN
	#{
		CUST.DATA.errMsg="Kindly Supply Employee_id"
		sv_e=urhk_SetOrbOut("Error|"+CUST.DATA.errMsg)
		GOTO End
		#}
	ELSE
		#{
		CUST.DATA.Employee_id=BANCS.INPUT.Employee_id
		print(CUST.DATA.Employee_id)
	#}
	ENDIF
	CUST.DATA.sol_id =""
	print (CUST.DATA.sol_id)
	CUST.DATA.emp_id =""
	print (CUST.DATA.emp_id)
	CUST.DATA.emp_name =""
	print (CUST.DATA.emp_name)
	CUST.DATA.emp_intls =""
	print (CUST.DATA.emp_intls)
	CUST.DATA.emp_email_id =""
	print (CUST.DATA.emp_email_id)
	CUST.DATA.is_head_teller =""
	print (CUST.DATA.is_head_teller)
	CUST.DATA.user_id =""
	print (CUST.DATA.user_id)
	CUST.DATA.del_flg =""
	print (CUST.DATA.del_flg)
	CUST.DATA.role_id =""
	print (CUST.DATA.role_id)
	CUST.DATA.user_work_class =""
	print (CUST.DATA.user_work_class)

	CUST.DATA.mngruser_id =""
	print (CUST.DATA.mngruser_id)
	CUST.DATA.mngremp_id =""
	print (CUST.DATA.mngremp_id)
	
	############################################
	# Fetching the value from table
	############################################
	
		sv_q = "sol_id,emp_id,emp_name,emp_intls,emp_email_id,is_head_teller,user_id,del_flg,role_id,user_work_class,mngruser_id,mngremp_id| "
		sv_q = sv_q + " select a.SOL_ID, a.EMP_ID, a.EMP_NAME, a.EMP_INTLS, a.EMP_EMAIL_ID, a.IS_HEAD_TELLER, a.USER_ID, a.DEL_FLG, a.ROLE_ID, a.USER_WORK_CLASS,b.LOGINID,b.USER_EMP_ID from "
		sv_q = sv_q + " (SELECT EMP.SOL_ID, EMP.EMP_ID, EMP.EMP_NAME, EMP.EMP_INTLS, EMP.EMP_EMAIL_ID, EMP.IS_HEAD_TELLER, Role.USER_ID, EMP.DEL_FLG, ROLE.ROLE_ID, ROLE.USER_WORK_CLASS "
		sv_q = sv_q + " FROM tbaadm.get EMP, tbaadm.upr ROLE WHERE EMP.EMP_ID = ROLE.USER_EMP_ID and role.del_flg='N' and emp.del_flg='N' AND EMP.EMP_ID =?SVAR) a, "
		sv_q = sv_q + " (select LOGINID,USER_EMP_ID,bccode from CRMUSER.BIZCENTER c, crmuser.users u,tbaadm.upr p where c.ISACTIVE ='Y' and u.CUSTRELATION ='Y' and " 
		sv_q = sv_q + "	u.PERSONID = c.MANAGER and p.user_id =u.LOGINID)b "
		sv_q = sv_q + " where a.sol_id = b.bccode "
		print(sv_q)
		BANCS.INPARAM.BINDVARS = CUST.DATA.Employee_id 
		
		sv_p= urhk_dbSelectWithBind(sv_q)
		print(sv_p)
		
		IF (sv_p==0) THEN
		#{
			###  sol_id,emp_id,emp_name,emp_intls,emp_email_id,is_head_teller,user_id,del_flg,role_id,user_work_class
			CUST.DATA.sol_id = BANCS.OUTPARAM.sol_id
			print (CUST.DATA.sol_id)
			CUST.DATA.emp_id = BANCS.OUTPARAM.emp_id
			print (CUST.DATA.emp_id)
			CUST.DATA.emp_name = BANCS.OUTPARAM.emp_name
			print (CUST.DATA.emp_name)
			CUST.DATA.emp_intls = BANCS.OUTPARAM.emp_intls
			print (CUST.DATA.emp_intls)
			CUST.DATA.emp_email_id = BANCS.OUTPARAM.emp_email_id
			print (CUST.DATA.emp_email_id)
			CUST.DATA.is_head_teller = BANCS.OUTPARAM.is_head_teller
			print (CUST.DATA.is_head_teller)
			IF (CUST.DATA.is_head_teller =="") THEN
				CUST.DATA.is_head_teller = "N"
				print(CUST.DATA.is_head_teller)
			ENDIF
			CUST.DATA.user_id = BANCS.OUTPARAM.user_id
			print (CUST.DATA.user_id)
			CUST.DATA.del_flg = BANCS.OUTPARAM.del_flg
			print (CUST.DATA.del_flg)
			CUST.DATA.role_id = BANCS.OUTPARAM.role_id
			print (CUST.DATA.role_id)
			IF (CUST.DATA.role_id =="DEL") THEN
				CUST.DATA.errMsg="User_id Does Not Exist(Role-DEL)"
				sv_e=urhk_SetOrbOut("StattusCode|2")
				sv_e=urhk_SetOrbOut("Error|"+CUST.DATA.errMsg)
				GOTO End
			ENDIF
			CUST.DATA.user_work_class = BANCS.OUTPARAM.user_work_class
			print (CUST.DATA.user_work_class)
			IF (CUST.DATA.user_work_class =="011") THEN
				CUST.DATA.errMsg="User_id Does Not Exist(workclass-011)"
				sv_e=urhk_SetOrbOut("StattusCode|2")
				sv_e=urhk_SetOrbOut("Error|"+CUST.DATA.errMsg)
				GOTO End
			ENDIF
			CUST.DATA.mngruser_id = BANCS.OUTPARAM.mngruser_id
			print (CUST.DATA.mngruser_id)
			IF (CUST.DATA.mngruser_id =="") THEN
				CUST.DATA.errMsg="No relationship Manager in the branch"
				sv_e=urhk_SetOrbOut("StattusCode|2")
				sv_e=urhk_SetOrbOut("Error|"+CUST.DATA.errMsg)
				GOTO End
			ENDIF
			CUST.DATA.mngremp_id = BANCS.OUTPARAM.mngremp_id
			print (CUST.DATA.mngremp_id)
			IF (CUST.DATA.mngremp_id =="") THEN
				CUST.DATA.errMsg="No relationship Manager in the branch"
				sv_e=urhk_SetOrbOut("StattusCode|2")
				sv_e=urhk_SetOrbOut("Error|"+CUST.DATA.errMsg)
				GOTO End
			ENDIF
			sv_z = urhk_SetOrbOut("SOL_ID|"+CUST.DATA.sol_id)
			print(sv_z)
			sv_z = urhk_SetOrbOut("EMP_ID|"+CUST.DATA.emp_id)
			print(sv_z)
			sv_z = urhk_SetOrbOut("EMP_NAME|"+CUST.DATA.emp_name)
			print(sv_z)
			sv_z = urhk_SetOrbOut("EMP_INTLS|"+CUST.DATA.emp_intls)
			print(sv_z)
			sv_z = urhk_SetOrbOut("EMP_EMAIL_ID|"+CUST.DATA.emp_email_id)
			print(sv_z)
			sv_z = urhk_SetOrbOut("IS_HEAD_TELLER|"+CUST.DATA.is_head_teller)
			print(sv_z)
			sv_z = urhk_SetOrbOut("USER_ID|"+CUST.DATA.user_id)
			print(sv_z)
			sv_z = urhk_SetOrbOut("ROLE_ID|"+CUST.DATA.role_id)
			print(sv_z)
			sv_z = urhk_SetOrbOut("USER_WORK_CLASS|"+CUST.DATA.user_work_class)
			print(sv_z)
			sv_z = urhk_SetOrbOut("MNGRUSER_ID|"+CUST.DATA.mngruser_id)
			print(sv_z)
			sv_z = urhk_SetOrbOut("MNGREMP_ID|"+CUST.DATA.mngremp_id)
			print(sv_z)
			sv_e=urhk_SetOrbOut("StattusCode|1")
			#}
		ELSE
			#{
			CUST.DATA.errMsg="User_id Does Not Exist"
			sv_e=urhk_SetOrbOut("StattusCode|2")
			sv_e=urhk_SetOrbOut("Error|"+CUST.DATA.errMsg)
		#}
		ENDIF
	
	End:
		EXITSCRIPT
		TRACE OFF
END-->
