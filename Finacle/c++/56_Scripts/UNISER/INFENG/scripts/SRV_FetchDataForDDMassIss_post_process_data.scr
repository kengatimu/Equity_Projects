#------------------------------------------------------------------------------------------------    
#   Name		: SRV_FetchDataForDDMassIss_post_process_data.scr
#   Description		: dd issue
#   Date		: 23-02-2015
#   Author		: jane
#   Input		: NA
#   Output		: NA
#   Called Script	:
#   Calling Script	: s_ast0057mn003.scr
#   Menu Option		:
#	Srl. No         Date            Author          	Description.
#	-------         ------          ------          	------------
#	1.0             23-02-2015      jane			Original Version
#-----------------------------------------------------------------------------------------------
<--START
CHECKSUM="faae4c2a8701e824c50225085e4e2f35e1fc9e3785819ab47b16b07285af919f"
	TRACE ON
	#------------------------------------------------------------------------------
	#	Creating repository and class
	#	Initializing the variables
	#	Getting all the values from CPARAM
	#------------------------------------------------------------------------------
	IF (REPEXISTS("CUSTS") == 0) THEN
	#{
		CREATEREP("CUSTS")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUSTS","DDMISS") == 0) THEN
	#{
		CREATECLASS("CUSTS","DDMISS",5)
	#}
	ENDIF
	
	IF (REPEXISTS("CUSTS") == 0) THEN
	#{
		CREATEREP("CUSTS")
	#}
	ENDIF
	
	CUSTS.DDMISS.errMsg       = ""
	
	#-------------------------------------------------------------------------------------------
	# Getting foracid from from frontend
	#-------------------------------------------------------------------------------------------
	sv_u = urhk_SRV_GetVal("ddMassIssHdr.purAcct.foracid")
	print(sv_u)
	IF (sv_u == 0) THEN
	#{
		CUSTS.DDMISS.foracid = BANCS.OUTPARAM.srvValue
		print(CUSTS.DDMISS.foracid)
		
		IF (CUSTS.DDMISS.foracid=="") THEN
		#{
			
			EXITSCRIPT
			
		#}
		ENDIF
		#}
	ELSE
		#{
		CUSTS.DDMISS.errMsg = "Error: urhk_SRV_GetVal failed for foracid"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_t = "status|"
	sv_t = sv_t + "select acct_status from tbaadm.smt where bank_id= ?SVAR AND  "
	sv_t = sv_t + " acid in (select acid from tbaadm.gam where foracid= ?SVAR AND bank_id= ?SVAR) "
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + CUSTS.DDMISS.foracid + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	print(sv_t)
	sv_e = urhk_dbSelectWithBind(sv_t)
	print(sv_e)
	IF(sv_e == 0) THEN
	#{
		CUSTS.DDMISS.status = BANCS.OUTPARAM.status
		print(CUSTS.DDMISS.status)
		
	#}
	ENDIF
	
	
	IF (CUSTS.DDMISS.status=="I")  THEN
	#{
		sv_u = urhk_SRV_SetErr("DEBIT TO INACTIVE ACCOUNT NOT ALLOWED")
		print(sv_u)
	#}
	ENDIF
	
	
	ENDOFSCRIPT:
		IF (CUSTS.DDMISS.errMsg != "") THEN
		#{
			print(CUSTS.DDMISS.errMsg)
		#}
		ENDIF
		
		
		EXITSCRIPT
END-->
