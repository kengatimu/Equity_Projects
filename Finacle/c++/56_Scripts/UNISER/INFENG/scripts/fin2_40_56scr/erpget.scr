<--START
CHECKSUM="50172bf92808c7830e02c22df80c80753ae12c5231acd0b2b88db80b03e980b2"
	TRACE ON
	
	
	sv_q="custId|"
	sv_q = sv_q + " select  G.EMP_ID from EB_EMP_STATUS_SUMMARY@erp_link e ,tbaadm.v_upr u,get g where lpad(EMPLOYEE_NUMBER, 5, 0) = user_emp_id AND u.del_flg!='Y' AND g.del_flg!='Y' AND G.EMP_ID=lpad(EMPLOYEE_NUMBER, 5, 0) AND U.USER_WORK_CLASS!='011' AND EFFECTIVE_END_DATE < sysdate -1 "
	
	print(sv_q)
	
	sv_e = urhk_dbCursorOpenWithBind(sv_q)
	
	
	IF(sv_e==0) THEN
	#{
		sv_m = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		sv_q = urhk_dbCursorFetch(sv_m)
		print(sv_q)
		IF(sv_q == 1) THEN
		#{
			sv_x = urhk_dbCursorClose(sv_m)
			print(sv_x)
			exitscript
		#}
		ENDIF
		print(sv_q)
		WHILE(sv_q==0)
		#{
			#######################################################################
			#               ASSIGNING VALUES FOR WORKFLOW                         #
			#######################################################################
			#print(BANCS.OUTPARAM.custId)
			sv_a=BANCS.OUTPARAM.custId
			
			
			
			sv_d = "UPDATE get SET del_FLG = 'Y' where emp_id = ?SVAR "
			print(sv_d)
			BANCS.INPARAM.BINDVARS = sv_a
			print(BANCS.INPARAM.BINDVARS)
			
			sv_e = urhk_dbSQLWithBind(sv_d)
			
			sv_f = urhk_dbSQLWithBind("commit")
			print(sv_f)
			
			IF (sv_f ==0) THEN
			#{
				sv_q=" date1|"
				sv_q = sv_q + " select  to_char(EFFECTIVE_END_DATE) from EB_EMP_STATUS_SUMMARY@erp_link where lpad(EMPLOYEE_NUMBER, 5,0)=?SVAR"
				print(sv_q)
				BANCS.INPARAM.BINDVARS = sv_a
				print(BANCS.INPARAM.BINDVARS)
				
				sv_t=urhk_dbSelectWithBind(sv_q)
				
				print(sv_t)
				sv_l=BANCS.OUTPARAM.date1
				print(sv_l)
				
				#IF (sv_l ==0) THEN
				
				sv_j = "Insert into EB_EMP_STATUS_LOG@erp_link (PF_NO, EXIT_DATE, TRANSACTION_DATE, SYSTEM)Values (?SVAR,?SVAR, sysdate, 'FINACLE')"
				print(sv_j)
				
				BANCS.INPARAM.BINDVARS = sv_a + "|" +  sv_l
				print(BANCS.INPARAM.BINDVARS)
				
				sv_w = urhk_dbsqlWithBind(sv_j)
				
				sv_w = urhk_dbsqlWithBind("COMMIT")
				print(sv_w)
				#ENDIF
			#}
			ENDIF
			
			sv_q = urhk_dbCursorFetch(sv_m)
			print(sv_q)
			#print("Account Created ")
		#}
		DO
		
		TRACE OFF
		exitscript
		
	END-->
	
	
