<--START
	TRACE ON
                if(BANCS.STDIN.moduleIdentity=="FI")then
		#{
			sv_a = urhk_GetChannelInfo("")
			print(sv_a)
			BANCS.INPUT.DccId =BANCS.OUTPARAM.DccId
			print(BANCS.INPUT.DccId)
			if((BANCS.INPUT.DccId=="CRS")and (BANCS.STDIN.contextBankId=="56"))then
				callscriptifexist("ebodla.scr")
				exitscript
			endif
		#}
		endif

	
	TRACE OFF
	EXITSCRIPT
END-->
