<--start
trace on 
        if(repexists("CUST") == 0)then
                createrep("CUST")
       	endif

        if(classexists("CUST","DATA") == 0)then
                createclass("CUST","DATA",5)
       	endif

	sv_a=urhk_B2k_PrintRepos("BANCS")
	print (sv_a)
	
	print(BANCS.INPUT.uAccount)

	sv_a= "acid1, foracid1,dpd_counter1 |select acid, foracid,dpd_counter from  CUSTOM.ALERT_ODCA1 where foracid =?SVAR"

	BANCS.INPARAM.BINDVARS =  BANCS.INPUT.uAccount
	print(BANCS.INPARAM.BINDVARS)

	sv_d = urhk_dbSelectWithBind(sv_a)
	PRINT(sv_d)
	
	if(sv_d==0)then
	#{
		CUST.DATA.acid1 = BANCS.OUTPARAM.acid1
		print(CUST.DATA.acid1)
		
		CUST.DATA.foracid1= BANCS.OUTPARAM.foracid1
		print(CUST.DATA.foracid1)

		CUST.DATA.dpd_counter1= BANCS.OUTPARAM.dpd_counter1
		print(CUST.DATA.dpd_counter1)
	#}
	else
	#{
		GOTO EXITSCRIPT1
	#}
	endif
	
	#sv_a = urhk_SRV_GetVal("acctODAInfo.daysPastDue")
	#sv_c = BANCS.OUTPARAM.srvValue
	#BANCS.INPUT.remarks = sv_c
	#print(sv_c)

	sv_b=urhk_SRV_SetVal("acctODAInfo.daysPastDue|"+CUST.DATA.dpd_counter1)
	print(sv_b)
	
EXITSCRIPT1:
trace off
end-->
