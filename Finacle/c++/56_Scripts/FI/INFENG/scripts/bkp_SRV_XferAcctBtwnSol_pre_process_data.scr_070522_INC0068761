<--start
	
	 IF(REPEXISTS("CUST") == 0) THEN
	 #{
	  CREATEREP ("CUST")
	 #}
	 ENDIF

	 
	 IF(CLASSEXISTS("CUST","DATA") == 0) THEN
	 #{
	  CREATECLASS ("CUST","DATA",5)
	 #}
	 ENDIF

		 sv_u = urhk_b2k_printrepos("BANCS")
		 print(sv_u)
		

		 sv_u = urhk_SRV_GetVal("xferAcct.foracid")
		 CUST.DATA.foracid = BANCS.OUTPARAM.srvValue
		 PRINT(CUST.DATA.foracid)

		 sv_u = urhk_SRV_GetVal("xferAcct.solId")
		 CUST.DATA.AccSolId = BANCS.OUTPARAM.srvValue
		 PRINT(CUST.DATA.AccSolId)

		 sv_u = urhk_SRV_GetVal("fromSol.solId")
		 CUST.DATA.SourceSolId = BANCS.OUTPARAM.srvValue
		 PRINT(CUST.DATA.SourceSolId)
		#---------------------------------------katumo M
		if (BANCS.STDIN.contextBankId=="56") then
			if ((BANCS.STDIN.homeSolId != CUST.DATA.AccSolId) and (CUST.DATA.AccSolId !="1000")) then
			   sv_s = urhk_SRV_SetErr("initiaiting sol differs with account sol id")
			   sv_s = urhk_SRV_SetErr("initiating sol is " + BANCS.STDIN.homeSolId + ", acct in sol " + CUST.DATA.AccSolId)
			   GOTO end
			endif
		endif
		#---------------------------------------Katumo M

end-->