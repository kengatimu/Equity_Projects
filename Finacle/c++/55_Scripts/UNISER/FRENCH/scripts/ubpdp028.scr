#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: fdsa_res_val.scr
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START
CHECKSUM="fc4656cf512b8375dd4ce424882951802f16f485906fd06f33c91115f05b23c9"
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp028.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called from ubp_det_link.js on click of get bill
	#                               details button for FDSA biller response validation.This script gets
	#                               called from respone outbound scr(through CPARAM)
	# Called Script         :
	# Calling function      :		function fnGetBillDtl()
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           16-04-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	IF(FIELDEXISTS(BANCS.INPUT.billerId))THEN
	#{
		IF(TRIM(BANCS.INPUT.billerId) != "")THEN
		#{
			CUST.UBP.billerId=BANCS.INPUT.billerId
			PRINT(CUST.UBP.billerId)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.billRefNum))THEN
	#{
		IF(TRIM(BANCS.INPUT.billRefNum) != "")THEN
		#{
			CUST.UBP.billRefNum=BANCS.INPUT.billRefNum
			PRINT(CUST.UBP.billRefNum)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.billAmt))THEN
	#{
		IF(TRIM(BANCS.INPUT.billAmt) != "")THEN
		#{
			CUST.UBP.billAmt=BANCS.INPUT.billAmt
			PRINT(CUST.UBP.billAmt)
		#}
		ENDIF
	#}
	ENDIF
	
	
	IF(FIELDEXISTS(CUST.INTERFACE.clientname))THEN
	#{
		IF(TRIM(CUST.INTERFACE.clientname) != "")THEN
		#{
			CUST.UBP.clientname=CUST.INTERFACE.clientname
			print(CUST.UBP.clientname)
			CUST.UBP.remName=CUST.UBP.clientname
			print(CUST.UBP.remName)
			
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(CUST.INTERFACE.dateadded))THEN
	#{
		IF(TRIM(CUST.INTERFACE.dateadded) != "")THEN
		#{
			CUST.UBP.dateadded=CUST.INTERFACE.dateadded
			print(CUST.UBP.dateadded)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(CUST.INTERFACE.dueamount))THEN
	#{
		IF(TRIM(CUST.INTERFACE.dueamount) != "")THEN
		#{
			CUST.UBP.dueamount=CUST.INTERFACE.dueamount
			print(CUST.UBP.dueamount)
			#VALIDATION
			IF(CDOUBLE(CUST.UBP.billAmt) != CDOUBLE(CUST.INTERFACE.dueamount))THEN
			#{
				#CUST.UBP.errorFlg="Y"
				#CUST.UBP.errorMsg="Amt recieved in response differs from Amt Entered"
				CUST.UBP.errorCode = "UBP_ERR044"
				CALL("ubpdp044.scr")
				CUST.UBP.errorMsg =CUST.UBP.errorDesc
				CUST.INTERFACE.errorMsgFlg = "Y"
				CUST.INTERFACE.errorMsgDesc=CUST.UBP.errorMsg
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(CUST.INTERFACE.orderref))THEN
	#{
		IF(TRIM(CUST.INTERFACE.orderref) != "")THEN
		#{
			CUST.UBP.orderref=CUST.INTERFACE.orderref
			print(CUST.UBP.orderref)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(CUST.INTERFACE.response))THEN
	#{
		IF(TRIM(CUST.INTERFACE.response) != "")THEN
		#{
			CUST.UBP.response=CUST.INTERFACE.response
			print(CUST.UBP.response)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(CUST.INTERFACE.status))THEN
	#{
		IF(TRIM(CUST.INTERFACE.status) != "")THEN
		#{
			CUST.UBP.status=CUST.INTERFACE.status
			print(CUST.UBP.status)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(CUST.INTERFACE.totalpersons))THEN
	#{
		IF(TRIM(CUST.INTERFACE.totalpersons) != "")THEN
		#{
			CUST.UBP.totalpersons=CUST.INTERFACE.totalpersons
			print(CUST.UBP.totalpersons)
			#CUST.UBP.remTranPart=" No. Of Member: "+CUST.INTERFACE.totalpersons
			CUST.UBP.remtranRem="Number Of Member: "+CUST.INTERFACE.totalpersons
			print(CUST.UBP.remTranPart)
		#}
		ENDIF
	#}
	ENDIF
	
	
	ENDOFSCRIPT:
		
END-->
