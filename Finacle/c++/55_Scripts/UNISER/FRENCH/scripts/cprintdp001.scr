#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cprintdp001.scr
# Type: NA|default
# Description: validation script for roleId
# Author: haneesha.kanumuru
# Date: 8/23/18 10:25 AM
# Modification Log:
#============================================================

<--START
CHECKSUM="998c69e7b7e0261d7f472ea892a1bc98d7d9dd91d29e5afe10291274cfb5d90c"
	# Script body goes here
	TRACE ON
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CPRINT Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CPRINT")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CPRINT",5)
	#}
	ENDIF
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	CUST.CPRINT.roleId ="0"
	CUST.CPRINT.errorFlg ="N"
	CUST.CPRINT.errorMsg = " "
	CUST.CPRINT.count = " "
	CUST.CPRINT.roleDesc = " "
	IF(FIELDEXISTS(BANCS.INPUT.roleId)) THEN
	#{
		IF(TRIM(BANCS.INPUT.roleId) != "")THEN
		#{
			CUST.CPRINT.roleId= TOUPPER(TRIM(BANCS.INPUT.roleId))
			PRINT(CUST.CPRINT.roleId)
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.CPRINT.errorFlg = "Y"
		CUST.CPRINT.errorMsg = "Invalid Input for fnExecuteScript"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_s = "role|"
	sv_s = sv_s + " SELECT COUNT(1)"
	sv_s = sv_s + " FROM tbaadm.rpt "
	sv_s = sv_s + " WHERE ROLE_ID = ?SVAR"
	sv_s = sv_s + " AND DEL_FLG != 'Y'"
	#sv_s = sv_s + " AND entity_cre_flg !='N'"
	#sv_s = sv_s + " AND schm_type='LAA'"
	sv_s = sv_s + " AND bank_id=?SVAR"
	
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.CPRINT.roleId + "|" +BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	CUST.CPRINT.count = BANCS.OUTPARAM.role
	PRINT(CUST.CPRINT.count)
	
	IF( CUST.CPRINT.count =="0" )THEN
	#{
		
		CUST.CPRINT.errorFlg = "Y"
		CUST.CPRINT.errorMsg = "Invalid Role Id"
	#}
	ENDIF
	IF(CUST.CPRINT.count > "0") THEN
	#{
		sv_s="roleDesc|SELECT ROLE_DESC FROM TBAADM.RPT WHERE BANK_ID=?SVAR AND ROLE_ID=?SVAR AND ENTITY_CRE_FLG='Y' AND DEL_FLG='N'"
		BANCS.INPARAM.BINDVARS=BANCS.STDIN.contextBankId + "|"+ CUST.CPRINT.roleId
		PRINT(sv_s)
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d != 0)THEN
		#{
			#sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			#sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			CUST.CPRINT.errorFlg = "Y"
			CUST.CPRINT.errorMsg = "Error in Fetching Data"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		CUST.CPRINT.roleDesc =BANCS.OUTPARAM.roleDesc
	#}
	ENDIF
	
	
	
	
	ENDOFSCRIPT:
		sv_u = urhk_SetOrbOut("errorFlg|"+CUST.CPRINT.errorFlg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("errorMsg|"+CUST.CPRINT.errorMsg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("roleId|"+CUST.CPRINT.roleId)
		
		PRINT(sv_u)
		sv_u=urhk_SetOrbOut("roleDesc|"+CUST.CPRINT.roleDesc)
		PRINT(sv_u)
END-->
