#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: hddmpdp001.scr
# Type: NA|default
# Description: hddmpdp001.scr
# Author:
# Date:
# Modification Log:
#============================================================

#-------------------------------------------------------------------------------------------
# Filename              :       hddmpdp001.scr
# Author                :       Venki
# Description           :       This script used to update ref num in DTD for HDDMP transaction
# Input                 :       NA
# Output                :       NA
# Called Script         :       None
# Calling Script        :       None
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----        ----------         --------------    ----------------        ---------
#  1           10-08-2013         Venkatesh          Original Version          1.0
#--------------------------------------------------------------------------------------------
<--START
CHECKSUM="f5cb8f0af684442e8de2b10e2f29b2390c86f8ae7af2affe9988c6b7f057e453"
	TRACE ON
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST","DDMP") == 0) THEN
	#{
		CREATECLASS("CUST","DDMP",5)
	#}
	ENDIF
	
	
	sv_u = urhk_SRV_NumOfRecs("tranDetail.partTranDetailLL")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
		CUST.DDMP.noOfRecords = CINT(BANCS.OUTPARAM.srvValue)
		PRINT(CUST.DDMP.noOfRecords)
		#}
	ELSE
		#{
		PRINT("Could not fetch record count")
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	CUST.DDMP.tranId = ""
	sv_l = 0
	PRINT(sv_l)
	WHILE (sv_l < CINT(CUST.DDMP.noOfRecords))
	#{
		sv_r = "<rec_" + FORMAT$(sv_l,"%d") + ">"
		PRINT(sv_r)
		
		sv_u = urhk_SRV_GetVal("tranDetail.partTranDetailLL." + sv_r + ".pTranIdentifier.tranDate")
		PRINT(sv_u)
		CUST.DDMP.tranDate = BANCS.OUTPARAM.srvValue
		PRINT(CUST.DDMP.tranDate)
		
		sv_u = urhk_SRV_GetVal("tranDetail.partTranDetailLL." + sv_r + ".pTranIdentifier.tranId")
		PRINT(sv_u)
		CUST.DDMP.tranId=TRIM(BANCS.OUTPARAM.srvValue)
		PRINT(CUST.DDMP.tranId)
		
		sv_u = urhk_SRV_GetVal("tranDetail.partTranDetailLL." + sv_r + ".pTranIdentifier.partTranSrlNum")
		PRINT(sv_u)
		CUST.DDMP.partTranSrl = TRIM(BANCS.OUTPARAM.srvValue)
		PRINT(CUST.DDMP.partTranSrl)
		
		sv_u = urhk_SRV_GetVal("tranDetail.partTranDetailLL." + sv_r + ".pTranIdentifier.partTranSrlNum")
		PRINT(sv_u)
		CUST.DDMP.partTranSrl = TRIM(BANCS.OUTPARAM.srvValue)
		PRINT(CUST.DDMP.partTranSrl)
		
		sv_u = urhk_SRV_GetVal("tranDetail.partTranDetailLL." + sv_r + ".instrmnt.instrmntNum")
		PRINT(sv_u)
		CUST.DDMP.ddNo = TRIM(BANCS.OUTPARAM.srvValue)
		PRINT(CUST.DDMP.ddNo)
		
		IF(CUST.DDMP.ddNo != "") THEN
		#{
			BANCS.INPARAM.BINDVARS = CUST.DDMP.ddNo + "|" + CUST.DDMP.tranId
			BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.DDMP.tranDate
			BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.DDMP.partTranSrl
			BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_s = "UPDATE TBAADM.DTD SET REF_NUM = ?SVAR WHERE TRIM(TRAN_ID) = ?SVAR"
			sv_s = sv_s + " AND TRAN_DATE = TO_DATE(?SVAR,'DD-MM-YYYY HH24:MI:SS')"
			sv_s = sv_s + " AND TRIM(PART_TRAN_SRL_NUM) = ?SVAR"
			sv_s = sv_s + " AND BANK_ID = ?SVAR"
			sv_d = urhk_dbSQLWithBind(sv_s)
			PRINT(sv_d)
		#}
		ENDIF
		sv_l = sv_l + 1
		
	#}
	DO
	
	#trace off
END-->
