#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: Event Based
# Script: cexchrtmn002.scr
# Type: TPLIM|default
# Description: TPLIM - TPLIM
# Author: Sridevi_Vutukuru
# Date: 8/29/18 4:49 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="32062140f25f5d7bb4af459773b0942d041caa7337a5a60ab6ca2e6dd23b45e1"
	TRACE ON
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CEXCH Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CEXCH")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CEXCH",5)
	#}
	ENDIF
	
	#==========================================================*
	# Intilializing the Variables
	#==========================================================*
	
	CUST.CEXCH.funcCode = ""
	CUST.CEXCH.roleId = ""
	CUST.CEXCH.frmccyCode = ""
	CUST.CEXCH.toccyCode = ""
	CUST.CEXCH.tranMarginAmt = ""
	CUST.CEXCH.cashMarginAmt = ""
	CUST.CEXCH.rate = ""
	CUST.CEXCH.resultMsg = ""
	CUST.CEXCH.recCount = ""
	
	
	sv_u = urhk_B2k_PrintRepos("BANCS")
	IF(FIELDEXISTS(BANCS.INPUT.funcCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.funcCode) != "")THEN
		#{
			CUST.CEXCH.funcCode = TOUPPER(TRIM(BANCS.INPUT.funcCode))
			PRINT(CUST.CEXCH.funcCode)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.roleId)) THEN
	#{
		IF(TRIM(BANCS.INPUT.roleId) != "")THEN
		#{
			CUST.CEXCH.roleId= TOUPPER(TRIM(BANCS.INPUT.roleId))
			PRINT(CUST.CEXCH.roleId)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.frmccyCode)) THEN
	#{
		IF(TRIM(BANCS.INPUT.frmccyCode) != "")THEN
		#{
			CUST.CEXCH.frmccyCode= TOUPPER(TRIM(BANCS.INPUT.frmccyCode))
			PRINT(CUST.CEXCH.frmccyCode)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.toccyCode)) THEN
	#{
		IF(TRIM(BANCS.INPUT.toccyCode) != "")THEN
		#{
			CUST.CEXCH.toccyCode= TOUPPER(TRIM(BANCS.INPUT.toccyCode))
			PRINT(CUST.CEXCH.toccyCode)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.txmrgnAmt)) THEN
	#{
		IF(TRIM(BANCS.INPUT.txmrgnAmt) != "")THEN
		#{
			CUST.CEXCH.tranMarginAmt = TRIM(BANCS.INPUT.txmrgnAmt)
			PRINT(CUST.CEXCH.tranMarginAmt)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.cashmrgnAmt)) THEN
	#{
		IF(TRIM(BANCS.INPUT.cashmrgnAmt) != "")THEN
		#{
			CUST.CEXCH.cashMarginAmt= TRIM(BANCS.INPUT.cashmrgnAmt)
			PRINT(CUST.CEXCH.cashMarginAmt)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.rate)) THEN
	#{
		IF(TRIM(BANCS.INPUT.rate) != "")THEN
		#{
			CUST.CEXCH.rate= TRIM(BANCS.INPUT.rate)
			PRINT(CUST.CEXCH.rate)
		#}
		ENDIF
	#}
	ENDIF
	
	#==========================================================*
	#For A-ADD
	#==========================================================*
	IF(CUST.CEXCH.funcCode == "A") THEN
	#{
		sv_s = "insert into CUSTOM.CUSTOM_CEXCHRT(ROLE_ID,FROM_CURRENCY,TO_CURRENCY,ENTITY_CRE_FLG,DEL_FLG"
		sv_s = sv_s + ",LCHG_USER_ID,LCHG_TIME,RCRE_USER_ID,RCRE_TIME,BANK_ID"
		sv_s = sv_s + ",TRANSFER_MARGIN_AMT,CASH_MARGIN_AMT,RATE)"
		sv_s = sv_s + " values(?SVAR,?SVAR,?SVAR,'N','N',?SVAR,sysdate,?SVAR,sysdate,?SVAR,?SVAR,?SVAR,?SVAR)"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CEXCH.roleId + "|" + CUST.CEXCH.frmccyCode + "|" + CUST.CEXCH.toccyCode
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS +  "|" + BANCS.STDIN.userId +  "|" + BANCS.STDIN.userId + "|" + BANCS.STDIN.contextBankId
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.CEXCH.tranMarginAmt + "|" +  CUST.CEXCH.cashMarginAmt + "|" + CUST.CEXCH.rate
		
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSqlWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d != 0)THEN
		#{
			#ERROR
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			sv_u = urhk_SetOrbOut("Error|Database Insertion failed")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		CUST.CEXCH.resultMsg = "Record has been Added successfully"
		sv_u = urhk_SetOrbOut("RESULT_MSG|"+CUST.CEXCH.resultMsg)
		PRINT(sv_u)
	#}
	ENDIF
	
	#==========================================================*
	#For M-MODIFY
	#==========================================================*
	IF(CUST.CEXCH.funcCode == "M") THEN
	#{
	
		sv_s = "del_flg|select count(*) from CUSTOM.CUSTOM_CEXCHRT where del_flg = 'N' and "
		sv_s = sv_s + " ROLE_ID = ?SVAR AND FROM_CURRENCY = ?SVAR AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR "
		
		
		BANCS.INPARAM.BINDVARS = CUST.CEXCH.roleId + "|" + CUST.CEXCH.frmccyCode + "|" + CUST.CEXCH.toccyCode
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d == 0) THEN
		#{
			sv_a = BANCS.OUTPARAM.del_flg
			print(sv_a)
			if(sv_a == 0) then
			#{
					
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Record does not exists")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS(SELECT 1 FROM CUSTOM.CUSTOM_CEXCHRT_MOD WHERE "
		sv_s = sv_s + " ROLE_ID = ?SVAR AND FROM_CURRENCY = ?SVAR AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR "
		sv_s = sv_s + " )"
		
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CEXCH.roleId + "|" + CUST.CEXCH.frmccyCode + "|" + CUST.CEXCH.toccyCode
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d == 0) THEN
		#{
			sv_s = "update CUSTOM.CUSTOM_CEXCHRT_MOD set"
			sv_s = sv_s + " TRANSFER_MARGIN_AMT = ?SVAR,"
			sv_s = sv_s + " CASH_MARGIN_AMT = ?SVAR,"
			sv_s = sv_s + " RATE = ?SVAR,"
			sv_s = sv_s + " LCHG_USER_ID=?SVAR,"
			sv_s = sv_s + " LCHG_TIME = sysdate,"
			sv_s = sv_s + " ENTITY_CRE_FLG = 'N'"
			sv_s = sv_s + " WHERE ROLE_ID = ?SVAR AND FROM_CURRENCY = ?SVAR AND TO_CURRENCY = ?SVAR"
			sv_s = sv_s + "  AND BANK_ID = ?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.CEXCH.tranMarginAmt + "|" +  CUST.CEXCH.cashMarginAmt + "|" + CUST.CEXCH.rate + "|" + BANCS.STDIN.userId
			BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.CEXCH.roleId + "|" + CUST.CEXCH.frmccyCode + "|" + CUST.CEXCH.toccyCode
			BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +  BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0)THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error_1|Error^Database Updation Failed^settlccy")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ELSE
		#{
			sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS(SELECT 1 FROM CUSTOM.CUSTOM_CEXCHRT WHERE "
			sv_s = sv_s + " ROLE_ID = ?SVAR AND FROM_CURRENCY = ?SVAR AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR "
			sv_s = sv_s + " and ENTITY_CRE_FLG='Y' and DEL_FLG = 'N')"
			
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.CEXCH.roleId + "|" + CUST.CEXCH.frmccyCode + "|" + CUST.CEXCH.toccyCode
			BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +  BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_d)
			IF(sv_d == 0) THEN
			#{
				###################
				sv_s = "insert into CUSTOM.CUSTOM_CEXCHRT_MOD(ROLE_ID,FROM_CURRENCY,TO_CURRENCY,ENTITY_CRE_FLG,DEL_FLG"
				sv_s = sv_s + ",LCHG_USER_ID,LCHG_TIME,RCRE_USER_ID,RCRE_TIME,BANK_ID"
				sv_s = sv_s + ",TRANSFER_MARGIN_AMT,CASH_MARGIN_AMT,RATE)"
				sv_s = sv_s + " values(?SVAR,?SVAR,?SVAR,'N','N',?SVAR,sysdate,?SVAR,sysdate,?SVAR,?SVAR,?SVAR,?SVAR)"
				PRINT(sv_s)
				BANCS.INPARAM.BINDVARS = CUST.CEXCH.roleId + "|" + CUST.CEXCH.frmccyCode + "|" + CUST.CEXCH.toccyCode
				BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS +  "|" + BANCS.STDIN.userId +  "|" + BANCS.STDIN.userId + "|" + BANCS.STDIN.contextBankId
				BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.CEXCH.tranMarginAmt + "|" +  CUST.CEXCH.cashMarginAmt + "|" + CUST.CEXCH.rate
				
				PRINT(BANCS.INPARAM.BINDVARS)
				sv_d = urhk_dbSqlWithBind(sv_s)
				PRINT(sv_d)
				IF(sv_d != 0)THEN
				#{
					#ERROR
					sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
					PRINT(sv_u)
					sv_u = urhk_SetOrbOut("Error|Database Insertion failed")
					PRINT(sv_u)
					GOTO ENDOFSCRIPT
				#}
				ENDIF
				################
			#}
			ELSE
			#{
				sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS(SELECT 1 FROM CUSTOM.CUSTOM_CEXCHRT WHERE "
				sv_s = sv_s + " ROLE_ID = ?SVAR AND FROM_CURRENCY = ?SVAR AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR "
				sv_s = sv_s + " and ENTITY_CRE_FLG='N' and DEL_FLG = 'N')"
			
				PRINT(sv_s)
				BANCS.INPARAM.BINDVARS = CUST.CEXCH.roleId + "|" + CUST.CEXCH.frmccyCode + "|" + CUST.CEXCH.toccyCode
				BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +  BANCS.STDIN.contextBankId
				PRINT(BANCS.INPARAM.BINDVARS)
				sv_d = urhk_dbSelectWithBind(sv_s)
				PRINT(sv_d)
				IF(sv_d == 0) THEN
				#{
					sv_s = "update CUSTOM.CUSTOM_CEXCHRT set"
					sv_s = sv_s + " TRANSFER_MARGIN_AMT = ?SVAR,"
					sv_s = sv_s + " CASH_MARGIN_AMT = ?SVAR,"
					sv_s = sv_s + " RATE = ?SVAR,"
					sv_s = sv_s + " LCHG_USER_ID=?SVAR,"
					sv_s = sv_s + " LCHG_TIME = sysdate,"
					sv_s = sv_s + " ENTITY_CRE_FLG = 'N'"
					sv_s = sv_s + " WHERE ROLE_ID = ?SVAR AND FROM_CURRENCY = ?SVAR AND TO_CURRENCY = ?SVAR"
					sv_s = sv_s + "  AND BANK_ID = ?SVAR"
					PRINT(sv_s)
					BANCS.INPARAM.BINDVARS = CUST.CEXCH.tranMarginAmt + "|" +  CUST.CEXCH.cashMarginAmt + "|" + CUST.CEXCH.rate + "|" + BANCS.STDIN.userId
					BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.CEXCH.roleId + "|" + CUST.CEXCH.frmccyCode + "|" + CUST.CEXCH.toccyCode
					BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +  BANCS.STDIN.contextBankId
					PRINT(BANCS.INPARAM.BINDVARS)
					sv_d = urhk_dbSqlWithBind(sv_s)
					PRINT(sv_d)
					
					IF(sv_d != 0)THEN
					#{
						#ERROR
						sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
						PRINT(sv_u)
						sv_u = urhk_SetOrbOut("Error_1|Error^Database Updation Failed^settlccy")
						PRINT(sv_u)
						GOTO ENDOFSCRIPT
					#}
					ENDIF
				#}
				ENDIF
			#}
			ENDIF
		#}
		ENDIF
		CUST.CEXCH.resultMsg = "Record has been Modified successfully"
		sv_u = urhk_SetOrbOut("RESULT_MSG|"+CUST.CEXCH.resultMsg)
		PRINT(sv_u)
	#}
	ENDIF
	#==========================================================*
	#For V- VERIFY
	#==========================================================*
	IF(CUST.CEXCH.funcCode == "V") THEN
	#{
		sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS(SELECT 1 FROM CUSTOM.CUSTOM_CEXCHRT_MOD WHERE "
		sv_s = sv_s + " ROLE_ID = ?SVAR AND FROM_CURRENCY = ?SVAR AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR)"
		
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CEXCH.roleId + "|" + CUST.CEXCH.frmccyCode + "|" + CUST.CEXCH.toccyCode
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d == 0) THEN
		#{
			CUST.CEXCH.recCount = BANCS.OUTPARAM.recCount
			PRINT(BANCS.OUTPARAM.recCount)
			
		#}
		ENDIF
		
		IF(CUST.CEXCH.recCount == 1) THEN
		#{
			sv_s = "update  CUSTOM.CUSTOM_CEXCHRT_MOD SET"
			sv_s = sv_s + " LCHG_USER_ID=?SVAR,"
			sv_s = sv_s + " LCHG_TIME = sysdate,"
			sv_s = sv_s + " ENTITY_CRE_FLG = 'Y'"
			sv_s = sv_s + " WHERE ROLE_ID = ?SVAR AND FROM_CURRENCY = ?SVAR "
			sv_s = sv_s + " AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS =  BANCS.STDIN.userId +"|" + CUST.CEXCH.roleId + "|" + CUST.CEXCH.frmccyCode + "|" + CUST.CEXCH.toccyCode
			BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +  BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database update failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			
			sv_s = "delete from CUSTOM.CUSTOM_CEXCHRT where ROLE_ID = ?SVAR AND FROM_CURRENCY = ?SVAR "
			sv_s = sv_s + " AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.CEXCH.roleId + "|" + CUST.CEXCH.frmccyCode + "|" + CUST.CEXCH.toccyCode
			BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +  BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database deletion failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			
			sv_s = "insert into custom.CUSTOM_CEXCHRT select * from custom.CUSTOM_CEXCHRT_MOD "
			sv_s = sv_s + " where ROLE_ID = ?SVAR AND FROM_CURRENCY = ?SVAR "
			sv_s = sv_s + " AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.CEXCH.roleId + "|" + CUST.CEXCH.frmccyCode + "|" + CUST.CEXCH.toccyCode
			BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +  BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database insert failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			
			sv_s = "delete from custom.CUSTOM_CEXCHRT_MOD "
			sv_s = sv_s + " where ROLE_ID = ?SVAR AND FROM_CURRENCY = ?SVAR "
			sv_s = sv_s + " AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.CEXCH.roleId + "|" + CUST.CEXCH.frmccyCode + "|" + CUST.CEXCH.toccyCode
			BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +  BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database delete failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			#}
		ELSE
			#{
			sv_s = "update  custom.CUSTOM_CEXCHRT set "
			sv_s = sv_s + " LCHG_USER_ID=?SVAR,"
			sv_s = sv_s + " LCHG_TIME = sysdate,"
			sv_s = sv_s + " ENTITY_CRE_FLG = 'Y'"
			sv_s = sv_s + " where ROLE_ID = ?SVAR AND FROM_CURRENCY = ?SVAR "
			sv_s = sv_s + " AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = BANCS.STDIN.userId + "|" + CUST.CEXCH.roleId + "|" + CUST.CEXCH.frmccyCode + "|" + CUST.CEXCH.toccyCode
			BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +  BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database update failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		CUST.CEXCH.resultMsg = "Record has been Verified successfully"
		sv_u = urhk_SetOrbOut("RESULT_MSG|"+CUST.CEXCH.resultMsg)
		PRINT(sv_u)
	#}
	ENDIF
	#==========================================================*
	#For X-CANCEL
	#==========================================================*
	IF(CUST.CEXCH.funcCode == "X") THEN
	#{
		sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
		sv_s = sv_s + " SELECT 1 FROM custom.CUSTOM_CEXCHRT_MOD"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR AND FROM_CURRENCY = ?SVAR "
		sv_s = sv_s + " AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR)"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS =  CUST.CEXCH.roleId + "|" + CUST.CEXCH.frmccyCode + "|" + CUST.CEXCH.toccyCode
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d == 0) THEN
		#{
			sv_s = "delete from custom.CUSTOM_CEXCHRT_MOD where ROLE_ID = ?SVAR AND FROM_CURRENCY = ?SVAR "
			sv_s = sv_s + " AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS =  CUST.CEXCH.roleId + "|" + CUST.CEXCH.frmccyCode + "|" + CUST.CEXCH.toccyCode
			BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +  BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database delete failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			#}
		ELSE
			#{
			sv_s = "delete from custom.CUSTOM_CEXCHRT where ROLE_ID = ?SVAR AND FROM_CURRENCY = ?SVAR "
			sv_s = sv_s + " AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS =  CUST.CEXCH.roleId + "|" + CUST.CEXCH.frmccyCode + "|" + CUST.CEXCH.toccyCode
			BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +  BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database delete failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		CUST.CEXCH.resultMsg = "Previous operation on Record has been cancelled successfully"
		sv_u = urhk_SetOrbOut("RESULT_MSG|"+CUST.CEXCH.resultMsg)
		PRINT(sv_u)
	#}
	ENDIF
	#==========================================================*
	#For D - DELETE
	#==========================================================*
	IF(CUST.CEXCH.funcCode == "D") THEN
	#{
		sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
		sv_s = sv_s + " SELECT 1 FROM custom.CUSTOM_CEXCHRT_MOD"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR AND FROM_CURRENCY = ?SVAR "
		sv_s = sv_s + " AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR)"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS =  CUST.CEXCH.roleId + "|" + CUST.CEXCH.frmccyCode + "|" + CUST.CEXCH.toccyCode
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d == 0) THEN
		#{
			sv_s = "delete from custom.CUSTOM_CEXCHRT_MOD where ROLE_ID = ?SVAR AND FROM_CURRENCY = ?SVAR "
			sv_s = sv_s + " AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS =  CUST.CEXCH.roleId + "|" + CUST.CEXCH.frmccyCode + "|" + CUST.CEXCH.toccyCode
			BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +  BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database delete failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		
		sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
		sv_s = sv_s + " SELECT 1 FROM custom.CUSTOM_CEXCHRT"
		sv_s = sv_s + " WHERE ROLE_ID = ?SVAR AND FROM_CURRENCY = ?SVAR "
		sv_s = sv_s + " AND TO_CURRENCY = ?SVAR AND BANK_ID = ?SVAR and DEL_FLG = 'N')"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS =  CUST.CEXCH.roleId + "|" + CUST.CEXCH.frmccyCode + "|" + CUST.CEXCH.toccyCode
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +  BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d == 0) THEN
		#{		
			sv_z = BANCS.OUTPARAM.recCount
			
			sv_s = "update CUSTOM.CUSTOM_CEXCHRT set"
			sv_s = sv_s + " DEL_FLG = 'Y'"
			sv_s = sv_s + " WHERE ROLE_ID = ?SVAR AND FROM_CURRENCY = ?SVAR AND TO_CURRENCY = ?SVAR"
			sv_s = sv_s + "  AND BANK_ID = ?SVAR and DEL_FLG = 'N'"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.CEXCH.roleId + "|" + CUST.CEXCH.frmccyCode + "|" + CUST.CEXCH.toccyCode
			BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" +  BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d != 0) THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("Error|Database update failed")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ELSE
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbOut("Error|Record does not Exist")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		CUST.CEXCH.resultMsg = "Record has been deleted successfully"
		sv_u = urhk_SetOrbOut("RESULT_MSG|"+CUST.CEXCH.resultMsg)
		PRINT(sv_u)
			
	#}
	ENDIF
	sv_z = urhk_SetOrbOut("SuccessOrFailure|Y")
	PRINT(sv_z)
	
	ENDOFSCRIPT:
		
		TRACE OFF
		
END-->
