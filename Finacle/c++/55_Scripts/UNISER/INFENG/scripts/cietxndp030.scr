##########################################################################################
# Source Name           : cietxndp030.scr
# Date                  : 04-Mar-2013
# Description           : Show Tran Id List
# Author                : Mohan Raghu Arudi
# Modification History  :
# <Serial No.>  <Date>        <Author Name>             <Description>
# ------------  -----------   -----------------         --------------
#      1        04-Mar-2013   Mohan Raghu Arudi         Base Version
###########################################################################################

<--START
CHECKSUM="7bf11edd56534fb9625e59913701bf32711f6323d3a693fd900e7ffd047ea022"
	TRACE ON
	
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CIETXN Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CIETXN")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CIETXN",5)
	#}
	ENDIF
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	
	CUST.CIETXN.tranId = ""
	CUST.CIETXN.tranDate = ""
	CUST.CIETXN.recordCount = "0"
	
	IF(FIELDEXISTS(BANCS.INPUT.tranId))THEN
	#{
		IF(TRIM(BANCS.INPUT.tranId) != "")THEN
		#{
			CUST.CIETXN.tranId = TOUPPER(TRIM(BANCS.INPUT.tranId))
			print(CUST.CIETXN.tranId)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.tranDate))THEN
	#{
		IF(TRIM(BANCS.INPUT.tranDate) != "")THEN
		#{
			CUST.CIETXN.tranDate = TOUPPER(TRIM(BANCS.INPUT.tranDate))
			print(CUST.CIETXN.tranDate)
		#}
		ENDIF
	#}
	ENDIF
	
	BANCS.INPARAM.BINDVARS = ""
	sv_s = "recordCount|"
	sv_s = sv_s + " SELECT COUNT(*)"
	sv_s = sv_s + " FROM CUSTOM.IETD "
	sv_s = sv_s + " WHERE REVERSE_FLG != 'Y'"
	sv_s = sv_s + " AND INIT_ENTITY_ID = '"+BANCS.STDIN.contextBankId+"'"
	
	IF(CUST.CIETXN.tranId != "")THEN
	#{
		sv_s = sv_s + " AND TRIM(ENTITY1_TRAN_ID) LIKE '"+CUST.CIETXN.tranId+"%'"
	#}
	ENDIF
	
	IF(CUST.CIETXN.tranDate != "")THEN
	#{
		sv_s = sv_s + " AND ENTITY1_TRAN_DATE = to_date('"+CUST.CIETXN.tranDate+"','DD-MM-YYYY')"
	#}
	ENDIF
	
	print(sv_s)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	IF(sv_d == 0)THEN
	#{
		CUST.CIETXN.recordCount = BANCS.OUTPARAM.recordCount
		print(CUST.CIETXN.recordCount)
	#}
	ENDIF
	
	sv_u = urhk_SetOrbOut("recordCount|"+CUST.CIETXN.recordCount)
	print(sv_u)
	
	BANCS.INPARAM.BINDVARS = ""
	sv_s = "tranId,tranDate,initEntity,targetEntity,targetEntityTranId,targetEntityTranDate,chargeTranId,chargeTranDate|"
	sv_s = sv_s + " SELECT ENTITY1_TRAN_ID,to_char(ENTITY1_TRAN_DATE,'DD-MM-YYYY'),INIT_ENTITY_ID,TARGET_ENTITY_ID,ENTITY2_TRAN_ID,"
	sv_s = sv_s + " to_char(ENTITY2_TRAN_DATE,'DD-MM-YYYY'),CHARGE_TRAN_ID,to_char(CHARGE_TRAN_DATE,'DD-MM-YYYY')"
	sv_s = sv_s + " FROM CUSTOM.IETD"
	sv_s = sv_s + " WHERE REVERSE_FLG != 'Y'"
	sv_s = sv_s + " AND INIT_ENTITY_ID = '"+BANCS.STDIN.contextBankId+"'"
	
	IF(CUST.CIETXN.tranId != "")THEN
	#{
		sv_s = sv_s + " AND TRIM(ENTITY1_TRAN_ID) LIKE '"+CUST.CIETXN.tranId+"%'"
	#}
	ENDIF
	
	IF(CUST.CIETXN.tranDate != "")THEN
	#{
		sv_s = sv_s + " AND ENTITY1_TRAN_DATE = to_date('"+CUST.CIETXN.tranDate+"','DD-MM-YYYY')"
	#}
	ENDIF
	sv_s = sv_s + " ORDER BY 2 desc ,1 desc"
	
	print(sv_s)
	
	sv_c = urhk_dbCursorOpenWithBind(sv_s)
	print(sv_c)
	
	IF(sv_c != 0)THEN
	#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_n = BANCS.OUTPARAM.DB_CURSOR_NUMBER
	print(sv_n)
	
	sv_f = urhk_dbCursorFetch(sv_n)
	print(sv_f)
	
	sv_i = 1
	
	WHILE(sv_f == 0)
	#{
		CUST.CIETXN.tranId = BANCS.OUTPARAM.tranId
		print(CUST.CIETXN.tranId)
		
		CUST.CIETXN.tranDate = BANCS.OUTPARAM.tranDate
		print(CUST.CIETXN.tranDate)
		
		CUST.CIETXN.initEntity = BANCS.OUTPARAM.initEntity
		print(CUST.CIETXN.initEntity)
		
		CUST.CIETXN.targetEntity = BANCS.OUTPARAM.targetEntity
		print(CUST.CIETXN.targetEntity)
		
		CUST.CIETXN.targetEntityTranId = BANCS.OUTPARAM.targetEntityTranId
		print(CUST.CIETXN.targetEntityTranId)
		
		CUST.CIETXN.targetEntityTranDate = BANCS.OUTPARAM.targetEntityTranDate
		print(CUST.CIETXN.targetEntityTranDate)
		
		CUST.CIETXN.chargeTranId = BANCS.OUTPARAM.chargeTranId
		print(CUST.CIETXN.chargeTranId)
		
		CUST.CIETXN.chargeTranDate = BANCS.OUTPARAM.chargeTranDate
		print(CUST.CIETXN.chargeTranDate)
		
		IF(CUST.CIETXN.tranId == "")THEN
		#{
			CUST.CIETXN.tranId = "-"
		#}
		ENDIF
		
		IF(CUST.CIETXN.tranDate == "")THEN
		#{
			CUST.CIETXN.tranDate = "-"
		#}
		ENDIF
		
		sv_s = "tranId_" + Format$(sv_i,"%d") + "|" + CUST.CIETXN.tranId
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_s = "tranDate_" + Format$(sv_i,"%d") + "|" + CUST.CIETXN.tranDate
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_s = "initEntity_" + Format$(sv_i,"%d") + "|" + CUST.CIETXN.initEntity
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_s = "targetEntity_" + Format$(sv_i,"%d") + "|" + CUST.CIETXN.targetEntity
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_s = "targetEntityTransaction_" + Format$(sv_i,"%d") + "|" + CUST.CIETXN.targetEntityTranId + " / " + CUST.CIETXN.targetEntityTranDate
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_s = "chargeTransaction_" + Format$(sv_i,"%d") + "|" + CUST.CIETXN.chargeTranId + " / " + CUST.CIETXN.chargeTranDate
		print(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		print(sv_u)
		
		sv_i = sv_i + 1
		
		sv_f = urhk_dbCursorFetch(sv_n)
		print(sv_f)
		
	#}
	DO
	
	sv_c = urhk_dbCursorClose(sv_n)
	print(sv_c)
	
	
	ENDOFSCRIPT:
		
END-->
