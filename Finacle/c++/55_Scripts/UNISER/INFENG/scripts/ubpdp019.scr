#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: ubpdp019.scr
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START
CHECKSUM="86a19b8391b0eb928e6446df6be4cd3fa2254fa32033da4435d84a42169408f3"
	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp019.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called from det page link file for fetching rate
	# Called Script         :
	# Calling Script        :	function fnGetRate()
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           16-04-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	# Script body goes here
	#========================================================
	# Check whether CUST Repository exists else create it
	#========================================================
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	#========================================================
	# Check whether UBP Class exists else create it
	#========================================================
	sv_r = CLASSEXISTS("CUST","UBP")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","UBP",5)
	#}
	ENDIF
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	
	CUST.UBP.refCrncy = " "
	CUST.UBP.acctCrncy = " "
	CUST.UBP.rateCode = " "
	CUST.UBP.foracid = " "
	CUST.UBP.billAmt=" "
	CUST.UBP.errorFlg = "N"
	CUST.UBP.errorMsg = " "
	CUST.UBP.rate = " "
	
	#sv_u = urhk_b2k_printrepos("BANCS")
	#print(sv_u)
	
	IF(FIELDEXISTS(BANCS.INPUT.refCrncy))THEN
	#{
		IF(TRIM(BANCS.INPUT.refCrncy) != "")THEN
		#{
			CUST.UBP.refCrncy = TOUPPER(TRIM(BANCS.INPUT.refCrncy))
			PRINT(CUST.UBP.refCrncy)
			
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.acctCrncy))THEN
	#{
		IF(TRIM(BANCS.INPUT.acctCrncy) != "")THEN
		#{
			CUST.UBP.acctCrncy= TOUPPER(TRIM(BANCS.INPUT.acctCrncy))
			PRINT(CUST.UBP.acctCrncy)
			sv_u=urhk_B2k_valCrncy(CUST.UBP.acctCrncy)
			IF(sv_u!=0)THEN
			#{
				CUST.UBP.errorFlg = "Y"
				#CUST.UBP.errorMsg = "Enter a valid currency code"
				CUST.UBP.errorCode = "UBP_ERR034"
				CALL("ubpdp044.scr")
				CUST.UBP.errorMsg =CUST.UBP.errorDesc
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.rateCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.rateCode) != "")THEN
		#{
			CUST.UBP.rateCode= TOUPPER(TRIM(BANCS.INPUT.rateCode))
			PRINT(CUST.UBP.rateCode)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.foracid))THEN
	#{
		IF(TRIM(BANCS.INPUT.foracid) != "")THEN
		#{
			CUST.UBP.foracid= TOUPPER(TRIM(BANCS.INPUT.foracid))
			PRINT(CUST.UBP.foracid)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.billAmt))THEN
	#{
		IF(TRIM(BANCS.INPUT.billAmt) != "")THEN
		#{
			CUST.UBP.billAmt= TOUPPER(TRIM(BANCS.INPUT.billAmt))
			PRINT(CUST.UBP.billAmt)
		#}
		ENDIF
	#}
	ENDIF
	
	#	sv_s = "cifId|"
	#	sv_s = sv_s + " SELECT CIF_ID"
	#	sv_s = sv_s + " FROM TBAADM.GAM "
	#	sv_s = sv_s + " WHERE FORACID = ?SVAR"
	#	sv_s = sv_s + " AND ENTITY_CRE_FLG = 'Y'"
	#	sv_s = sv_s + " AND DEL_FLG != 'Y'"
	#	print(sv_s)
	#
	#	BANCS.INPARAM.BINDVARS = CUST.UBP.foracid
	#	print(BANCS.INPARAM.BINDVARS)
	#
	#	sv_d = urhk_dbSelectWithBind(sv_s)
	#	print(sv_d)
	#
	#	IF(sv_d == 0)THEN
	#{
	#		CUST.UBP.cifId = BANCS.OUTPARAM.cifId
	#		print(CUST.UBP.cifId)
	
	#BANCS.INPARAM.InputAmount = "1"
	BANCS.INPARAM.InputAmount =CUST.UBP.billAmt
	BANCS.INPARAM.FromCurrency = CUST.UBP.acctCrncy
	BANCS.INPARAM.ToCurrency = CUST.UBP.refCrncy
	BANCS.INPARAM.RateCode = CUST.UBP.rateCode
	BANCS.INPARAM.Rate = ""
	#BANCS.INPARAM.CustId = CUST.UBP.cifId
	
	sv_u = urhk_B2k_ConvertAmount("")
	PRINT(sv_u)
	
	IF(sv_u == 0)THEN
	#{
		#CUST.UBP.rate  = CDOUBLE(BANCS.OUTPARAM.OutputAmount)
		CUST.UBP.rate=BANCS.OUTPARAM.Rate
		PRINT(CUST.UBP.rate)
		sv_u=urhk_B2k_validateAmtWithCrncy(CUST.UBP.rate+"|"+CUST.UBP.refCrncy)
		PRINT(sv_u)
		IF(sv_u == 0) THEN
		#{
			CUST.UBP.rate = TRIM(BANCS.OUTPARAM.OutputAmount)
			PRINT(CUST.UBP.rate)
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.errorFlg = "Y"
		#CUST.UBP.errorMsg = "Rate not maintained for this CCY OR Invalid Rate Code passed. Unable to fetch the Rate."
		#CUST.UBP.errorMsg ="Invalid Rate Code passed"
		
		CUST.UBP.errorCode = "UBP_ERR035"
		CALL("ubpdp044.scr")
		CUST.UBP.errorMsg =CUST.UBP.errorDesc
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	#}
	#	ELSE
	#{
	#		CUST.UBP.errorFlg = "Y"
	#		CUST.UBP.errorMsg = "Invalid Account ID"
	#		GOTO ENDOFSCRIPT
	#}
	#	ENDIF
	
	
	ENDOFSCRIPT:
		
		sv_u = urhk_SetOrbOut("errorFlg|"+CUST.UBP.errorFlg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("errorMsg|"+CUST.UBP.errorMsg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("rate|"+CUST.UBP.rate)
		PRINT(sv_u)
		
		
END-->
