#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: ubpdp070.scr
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START
CHECKSUM="f2eb3b557c3a461c7b34ed4fc5d6d61bee20a77522b5a7af687d0f28922e5911"
	# Script body goes here
	# Script body goes here
	TRACE ON
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp070.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets checks if the set num is for bill pay or not
	# Called Script         :
	# Calling Script        :		hoccmdp011.scr
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1          06-09-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	#========================================================
	# Check whether CUST Repository exists else create it
	#========================================================
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	#========================================================
	# Check whether UBP Class exists else create it
	#========================================================
	sv_r = CLASSEXISTS("CUST","UBP")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","UBP",5)
	#}
	ENDIF
	#========================================================
	# Initialize the variables
	#========================================================
	CUST.UBP.errorFlg="N"
	CUST.UBP.errorMsg=" "
	CUST.UBP.zoneCode = " "
	CUST.UBP.setNum = " "
	CUST.UBP.zoneDate=" "
	CUST.UBP.billPayYes="N"
	CUST.UBP.billerId=" "
	CUST.UBP.typOfServ=" "
	CUST.UBP.valType=" "
	CUST.UBP.critCrAcctNum=" "
	CUST.UBP.critCrAcctBnkId=" "
	
	IF(FIELDEXISTS(BANCS.INPUT.zoneCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.zoneCode) != "")THEN
		#{
			CUST.UBP.zoneCode = TOUPPER(TRIM(BANCS.INPUT.zoneCode))
			PRINT(CUST.UBP.zoneCode)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.setNum))THEN
	#{
		IF(TRIM(BANCS.INPUT.setNum) != "")THEN
		#{
			CUST.UBP.setNum = TOUPPER(TRIM(BANCS.INPUT.setNum))
			PRINT(CUST.UBP.setNum)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.zoneDate))THEN
	#{
		IF(TRIM(BANCS.INPUT.zoneDate) != "")THEN
		#{
			CUST.UBP.zoneDate = TOUPPER(TRIM(BANCS.INPUT.zoneDate))
			PRINT(CUST.UBP.zoneDate)
		#}
		ENDIF
	#}
	ENDIF
	BANCS.INPARAM.BINDVARS = CUST.UBP.zoneCode + "|" + CUST.UBP.zoneDate
	
	print(BANCS.INPARAM.BINDVARS)
	sv_s = "count1|"
	sv_s = sv_s + " SELECT COUNT(1)"
	sv_s = sv_s + " FROM CUSTOM.C_UBP "
	sv_s = sv_s + " WHERE CLG_ZONE_CODE = ?SVAR "
	sv_s = sv_s + " AND CLG_ZONE_DATE = TO_DATE(?SVAR,'DD-MM-YYYY') "
	sv_s = sv_s + " AND trim(SET_NUM) = '" + CUST.UBP.setNum + "' "
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	IF(sv_d != 0)THEN
	#{
		CUST.UBP.errorFlg = "Y"
		CUST.UBP.errorMsg ="Error in fetching data from CUSTOM.C_UBP table"
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		CUST.UBP.count1 = BANCS.OUTPARAM.count1
		print(CUST.UBP.count1)
	#}
	ENDIF
	IF(CUST.UBP.count1>0)THEN
	#{
		BANCS.INPARAM.BINDVARS = CUST.UBP.zoneCode + "|" + CUST.UBP.zoneDate
		print(BANCS.INPARAM.BINDVARS)
		sv_s = "billerId,typOfServ,valType,critCrAcctNum,critCrAcctBnkId|"
		sv_s = sv_s + " SELECT BILLER_ID,TYP_OF_SERV,VAL_TYPE,BILLER_ACCT_ID,BILLER_BNK_ID"
		sv_s = sv_s + " FROM CUSTOM.C_UBP "
		sv_s = sv_s + " WHERE CLG_ZONE_CODE = ?SVAR "
		sv_s = sv_s + " AND CLG_ZONE_DATE = TO_DATE(?SVAR,'DD-MM-YYYY') "
		sv_s = sv_s + " AND trim(SET_NUM) = '" + CUST.UBP.setNum + "' "
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		print(sv_d)
		IF(sv_d != 0)THEN
		#{
			
			CUST.UBP.errorFlg = "Y"
			CUST.UBP.errorMsg ="Error in fetching data from CUSTOM.C_UBP table"
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			CUST.UBP.billPayYes="Y"
			CUST.UBP.billerId = BANCS.OUTPARAM.billerId
			print(CUST.UBP.billerId)
			CUST.UBP.typOfServ = BANCS.OUTPARAM.typOfServ
			print(CUST.UBP.typOfServ)
			CUST.UBP.valType = BANCS.OUTPARAM.valType
			print(CUST.UBP.valType)
			CUST.UBP.critCrAcctNum = BANCS.OUTPARAM.critCrAcctNum
			print(CUST.UBP.critCrAcctNum)
			CUST.UBP.critCrAcctBnkId = BANCS.OUTPARAM.critCrAcctBnkId
			print(CUST.UBP.critCrAcctBnkId)
		#}
		ENDIF
	#}
	ENDIF
	ENDOFSCRIPT:
		sv_s = "errorFlg|" + CUST.UBP.errorFlg
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "errorMsg|" + CUST.UBP.errorMsg
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		IF(CUST.UBP.billPayYes=="")THEN
		#{
			CUST.UBP.billPayYes=" "
		#}
		ENDIF
		sv_s = "billPayYes|" + CUST.UBP.billPayYes
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		
		IF(CUST.UBP.billerId=="")THEN
		#{
			CUST.UBP.billerId=" "
		#}
		ENDIF
		sv_s = "billerId|" + CUST.UBP.billerId
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		IF(CUST.UBP.typOfServ=="")THEN
		#{
			CUST.UBP.typOfServ=" "
		#}
		ENDIF
		sv_s = "typOfServ|" + CUST.UBP.typOfServ
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		
		IF(CUST.UBP.valType=="")THEN
		#{
			CUST.UBP.valType=" "
		#}
		ENDIF
		sv_s = "valType|" + CUST.UBP.valType
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		IF(CUST.UBP.critCrAcctNum=="")THEN
		#{
			CUST.UBP.critCrAcctNum=" "
		#}
		ENDIF
		sv_s = "critCrAcctNum|" + CUST.UBP.critCrAcctNum
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		IF(CUST.UBP.critCrAcctBnkId=="")THEN
		#{
			CUST.UBP.critCrAcctBnkId=" "
		#}
		ENDIF
		sv_s = "critCrAcctBnkId|" + CUST.UBP.critCrAcctBnkId
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
END-->
