#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: InstrmntDtls_FetchInputDetails.scr
# event: NA
# description: NA
#=================================================
#----------------------------------------------------------------------------------------------------------
# Filename              :       InstrmntDtls_FetchInputDetails.scr
# Author                :       Nitin Prabhakar
# Description           :       This script fetches the values passed in request XML.
# Input                 :       Request XML
# Output                :       4 input fields
# Called Script         :       InstrmntDtls_FetchInputDetails.scr.scr
# Calling Script        :       None
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----           -----           --------          ----------------        ---------
#  1           26-07-2013      Nitin Prabhakar       Original Version          1.0
#-----------------------------------------------------------------------------------------------------------
<--start
TRACE ON
#-------------------------------------------------------------------------------
# Getting input data from xml
#-------------------------------------------------------------------------------
# AcctNum
sv_v = "InstrmntDtls_REQ.AcctNum"
IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
#{
	CUST.INTERFACE.AcctNum =("BANCS").("INPUT").(sv_v)
#}
ELSE
#{
	CUST.INTERFACE.AcctNum = ""
#}
ENDIF
print(CUST.INTERFACE.AcctNum)

# ChequeNum
sv_v = "InstrmntDtls_REQ.ChequeNum"
IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
#{
	CUST.INTERFACE.ChequeNum = ("BANCS").("INPUT").(sv_v)
#}
ELSE
#{
	CUST.INTERFACE.ChequeNum = ""
#}
ENDIF
print(CUST.INTERFACE.ChequeNum)

# SolId
sv_v = "InstrmntDtls_REQ.SolId"
IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
#{
	CUST.INTERFACE.SolId = ("BANCS").("INPUT").(sv_v)
#}
ELSE
#{
	CUST.INTERFACE.SolId = ""
#}
ENDIF
print(CUST.INTERFACE.SolId)

# InstrmntType
sv_v = "InstrmntDtls_REQ.InstrmntType"
IF(FIELDEXISTS(("BANCS").("INPUT").(sv_v))) THEN
#{
	CUST.INTERFACE.InstrmntType = ("BANCS").("INPUT").(sv_v)
#}
ELSE
#{
	CUST.INTERFACE.InstrmntType = ""
#}
ENDIF
print(CUST.INTERFACE.InstrmntType)


EXITSCRIPT
end-->
