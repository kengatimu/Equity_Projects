#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: wthmn035.scr
# Type: NA|default
# Description: wthmn035.scr
# Author:
# Date:
# Modification Log:
#============================================================

###################################################################################################################################################
#Script Name    : wthmn035.scr
#Start Date     : 20-June-2013
#Description    : This script is called on click of submit button on details page for menus MISAO,MICAO,MITUAO and MCMIDAO
#				  MILA and MITUA  for Modify mode
#				  And this script will capture the value for custom field "Withholding Tax Applicable" on scheme tab  into custom table.
#Input para     : None
#Output para    : None
#Author         : Nitin_Kumar16
#Called Script  : NA
#Calling Script : SRV_ModifyCAAcct_post_process_data.scr,SRV_ModifySBAcct_post_process_data.scr
#				  SRV_ModifyTUAcct_post_process_data.scr
#Reference      : None
#Modification History:
#Srl No.  		Date         		Author          		Description
#-------  		-----------  		---------   			--------------
#   1.0   		20-June-2013 		Nitin_Kumar16      		Original version
###################################################################################################################################################
<--START
CHECKSUM="44ecf5ec246d148db30e7945f35e97289e311ecd83491b7335d57e9fde769fb7"
	#Trace on
	#--------------------------------------------------------------------------
	# Create Repos and Classes
	#--------------------------------------------------------------------------
	sv_r = REPEXISTS("CUST")
	IF (sv_r == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	sv_r = CLASSEXISTS("CUST", "WITHTAX")
	IF (sv_r == 0) THEN
	#{
		CREATECLASS("CUST", "WITHTAX", 5)
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.STDIN.menuId) == "0") THEN
	#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	PRINT(ONS.CUSTOM.custDataIn)
	IF((BANCS.STDIN.menuId != "MISAO") AND (BANCS.STDIN.menuId != "MICAO") AND (BANCS.STDIN.menuId != "MITUAO") AND (BANCS.STDIN.menuId != "MCMIDAO"))THEN
	#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	CUST.WITHTAX.withdTaxAppFlg 		= ""
	CUST.WITHTAX.schmCode 				= ""
	CUST.WITHTAX.schmType				= ""
	CUST.WITHTAX.foracid				= ""
	CUST.WITHTAX.delFlg 				= "N"
	CUST.WITHTAX.entityFlg 			 	= "N"
	
	#sv_a = urhk_B2k_printrepos("ONS")
	#print(sv_a)
	
	PRINT(ONS.CUSTOM.custDataIn)
	
	#IF((BANCS.STDIN.menuId == "MILA") OR (BANCS.STDIN.menuId == "MITUA")) THEN
	#{
	#	GOTO ENDOFSCRIPT
	#}
	#ENDIF
	IF((BANCS.STDIN.menuId == "MITUAO") OR (BANCS.STDIN.menuId == "MCMIDAO")) THEN
	#{
		sv_u = urhk_SRV_GetVal("acctCommonInfo.acctGeneralInfo.acctBasic.foracid")
		CUST.WITHTAX.foracid = BANCS.OUTPARAM.srvValue
		PRINT(CUST.WITHTAX.foracid)
	#}
	ENDIF
	IF((BANCS.STDIN.menuId == "MICAO") OR (BANCS.STDIN.menuId == "MISAO")) THEN
	#{
		sv_u = urhk_SRV_GetVal("acctCommonInfo.acctGeneralInfo.acctBasic.foracid")
		CUST.WITHTAX.foracid = BANCS.OUTPARAM.srvValue
		PRINT(CUST.WITHTAX.foracid)
	#}
	ENDIF
	PRINT(CUST.WITHTAX.foracid)
	PRINT(BANCS.STDIN.menuId)
	
	PRINT(ONS.CUSTOM.custDataIn)
	PRINT("helllo")
	sv_u = urhk_ParseONSCustData(ONS.CUSTOM.custDataIn)
	PRINT(sv_u)
	IF((BANCS.STDIN.menuId == "MITUAO") OR (BANCS.STDIN.menuId == "MCMIDAO")) THEN
	#{
		IF (FIELDEXISTS(BANCS.INPUT.oituaschdet_FIELD_1) != 0) THEN
		#{
			CUST.WITHTAX.withdTaxAppFlg = BANCS.INPUT.oituaschdet_FIELD_1
			PRINT(CUST.WITHTAX.withdTaxAppFlg)
			#}
		ELSE
		#{
			#}
			CUST.WITHTAX.withdTaxAppFlg = ""
		#}
		ENDIF
	#}
	ENDIF
	IF((BANCS.STDIN.menuId == "MICAO") OR (BANCS.STDIN.menuId == "MISAO")) THEN
	#{
		IF (FIELDEXISTS(BANCS.INPUT.oilasch_FIELD_1) != 0) THEN
		#{
			CUST.WITHTAX.withdTaxAppFlg = BANCS.INPUT.oilasch_FIELD_1
			PRINT(CUST.WITHTAX.withdTaxAppFlg)
			#}
		ELSE
		#{
			#}
			CUST.WITHTAX.withdTaxAppFlg = ""
		#}
		ENDIF
	#}
	ENDIF
	PRINT(CUST.WITHTAX.withdTaxAppFlg)
	
	#BANCS.INPARAM.BINDVARS =  CUST.WITHTAX.withdTaxAppFlg +"|"+ BANCS.STDIN.userId +"|"+BANCS.STDIN.sysDate + "|" + CUST.WITHTAX.foracid
	BANCS.INPARAM.BINDVARS =  CUST.WITHTAX.withdTaxAppFlg +"|"+ BANCS.STDIN.userId + "|" + CUST.WITHTAX.foracid + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_s = "UPDATE CUSTOM.C_WTHAO SET WTH_TAX_APP_FLG = ?SVAR,LCHG_USER_ID = ?SVAR,LCHG_TIME = SYSDATE WHERE FORACID = ?SVAR AND BANK_ID = ?SVAR "
	PRINT(sv_s)
	sv_d = urhk_dbSqlWithBind(sv_s)
	PRINT(sv_d)
	IF (sv_d != 0) THEN
	#{
		sv_p = urhk_SRV_SetErr("Unable to Verify the record")
		BANCS.OUTPUT.SuccessOrFailure = "F"
		sv_p = urhk_SetOrbOut("SuccessOrFailure|N")
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	
	ENDOFSCRIPT:
		#############################################################################
		# Deleting the custom repository and class
		############################################################################
		#Trace off
		
END-->
