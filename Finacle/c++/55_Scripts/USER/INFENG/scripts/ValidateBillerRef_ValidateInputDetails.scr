#----------------------------------------------------------------------------------------------------------
# Filename              :       ValidateBillerRef_ValidateInputDetails.scr
# Author                :       Venkatesh T R
# Description           :       This script validates the values passed in request XMK
# Input                 :       Account ID
# Output                :       Success or Failure
# Called Script         :       ValidateBillerRef_main.scr
# Calling Script        :       None
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----           -----           --------          ----------------        ---------
#  1           09-03-2013         Venkatesh          Original Version          1.0
#-----------------------------------------------------------------------------------------------------------
<--start
trace on
	
	#----------------------------------------------
	# Validate mandatory inputs
	#----------------------------------------------
	IF((CUST.INTERFACE.refNum == "") OR (CUST.INTERFACE.refNum == "") OR (CUST.INTERFACE.refNum == "")) THEN
	#{
		CUST.INTERFACE.successOrFailure = "F"
		CUST.INTERFACE.errorCode = "ERR042"
		GOTO ERROREXIT
	#}
	ENDIF
	
	#----------------------------------------------
	# Validate Account ID
	#----------------------------------------------
	IF(CUST.INTERFACE.accountID != "") THEN
	#{
#		CUST.INTERFACE.successOrFailure = "F"
#		CUST.INTERFACE.errorCode = "ERR023"
#		GOTO ERROREXIT
#	#}
#	ELSE
#	#{
		sv_u = urhk_valAcctNumber(CUST.INTERFACE.accountID)
		IF((sv_u != 0) OR (BANCS.OUTPARAM.Acid == "")) THEN
		#{
			CUST.INTERFACE.successOrFailure = "F"
			CUST.INTERFACE.errorCode = "ERR003"
			GOTO ERROREXIT
		#}
		ENDIF
	#}
	ENDIF
	
	#----------------------------------------------
	# Validate Account ID
	#----------------------------------------------
	IF(CUST.INTERFACE.bankID != "") THEN
	#{
#		CUST.INTERFACE.successOrFailure = "F"
#		CUST.INTERFACE.errorCode = "ERR032"
#		GOTO ERROREXIT
#	#}
#	ELSE
#	#{	
		#-------------------------------------------------------------------------------------------------
		# Validating if the given BankId is valid one or not
		#--------------------------------------------------------------------------------------------------
		BANCS.INPARAM.BINDVARS = CUST.INTERFACE.bankID + "|" + "N"
		sv_s= "count|"
		sv_s = sv_s + "SELECT  COUNT(1) FROM  TBAADM.GCT "
		sv_s = sv_s + "WHERE BANK_ID = ?SVAR "
		sv_s = sv_s + "AND DEL_FLG = ?SVAR "
		print(sv_s)
		sv_d = urhk_dbSelectWithBind(sv_s)
		IF (sv_d==0) THEN
		#{
			CUST.INTERFACE.count=BANCS.OUTPARAM.count
			IF (CUST.INTERFACE.count== "0") THEN
			#{
				CUST.INTERFACE.successOrFailure = "F"
				CUST.INTERFACE.errorCode = "ERR033"
				GOTO ERROREXIT
			#}
			ENDIF
		#}
		ENDIF
		
	#}
	ENDIF
	
	
#trace off
end-->
