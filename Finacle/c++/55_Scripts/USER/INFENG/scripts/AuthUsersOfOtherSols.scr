#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: AuthUsersOfOtherSols.scr
# event: NA
# description: AuthUsersOfOtherSols
#=================================================
# Script Name : AuthUsersOfOtherSols.scr
<--start
trace on 

IF(FIELDEXISTS(BANCS.STDIN.menuId) !=1)THEN
#{
	BANCS.STDIN.menuId = ""
#}
ENDIF

IF(FIELDEXISTS(BANCS.STDIN.menuOption) !=1)THEN
#{
	BANCS.STDIN.menuOption = ""
#}
ENDIF

IF(FIELDEXISTS(BANCS.STDIN.moduleIdentity))THEN
#{
	IF((BANCS.STDIN.moduleIdentity == "FI") OR (BANCS.STDIN.moduleIdentity == "BJMS")  OR (BANCS.STDIN.moduleIdentity == "CDCI") OR (BANCS.STDIN.menuId=="HOCCM") OR (BANCS.STDIN.menuOption=="HOCCM")) THEN
	#{
		BANCS.OUTPUT.allowAuthOfOtherSolFlg = "Y"
		print(BANCS.OUTPUT.allowAuthOfOtherSolFlg)
		GOTO FINISH
	#}
	ENDIF
#}
ENDIF

#ENDED
FINISH:
	exitscript
end-->

