#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: accmdp016.scr
# event: NA
# description:
#=================================================
#**************************************************************************************************************
# Filename              : accmdp016.scr
# Description           : The script isused for calculating the charges for the contract.
# Input                 :
# Output                :
# Called Script         : accmdp019.scr
# Calling Script        : None
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----          ------           --------          ----------------        ---------
#   1          25-03-2013       Kalvin Joseph     Original Version          1.0
#**************************************************************************************************************


<--START
CHECKSUM="ad6c43d080aae42c0d76ae2c4161c1cc81e19285d29fd07e53ef725847dd594f"
	TRACE ON
	# Script body goes here
	#*******************************************************************************************
	# Check whether CUST Repository exists else create it
	#******************************************************************************************
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	#******************************************************************************************
	# Check whether Class exists else create it
	#******************************************************************************************
	
	sv_r = CLASSEXISTS("CUST","CONTRACT")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CONTRACT",5)
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST", "INTERFACE") == 0) THEN
	#{
		CREATECLASS("CUST", "INTERFACE", 5)
	#}
	ENDIF
	
	
	#sv_u = urhk_B2k_PrintRepos("BANCS")
	print(BANCS.INPUT.contractAmt)
	CUST.CONTRACT.acctNum=BANCS.INPUT.acctNo
	CUST.CONTRACT.contractAmt=BANCS.INPUT.contractAmt
	
	BANCS.INPARAM.BINDVARS = CUST.CONTRACT.acctNum
	print(BANCS.INPARAM.BINDVARS)
	#sv_s = "crncyCode,schmCode,schmType|"
	#sv_s = sv_s + " SELECT ACCT_CRNCY_CODE,SCHM_CODE,SCHM_TYPE"
	sv_s = "crncyCode,schmType|"
	sv_s = sv_s + " SELECT ACCT_CRNCY_CODE,SCHM_TYPE"
	sv_s = sv_s + " FROM TBAADM.GAM "
	sv_s = sv_s + " WHERE FORACID = ?SVAR "
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	IF(sv_d != 0)THEN
	#{
		CUST.CONTRACT.schmCode = ""
		CUST.CONTRACT.schmType = ""
		CUST.CONTRACT.crncyCode = ""
		#}
	ELSE
		#{
		CUST.CONTRACT.crncyCode = BANCS.OUTPARAM.crncyCode
		print(CUST.CONTRACT.crncyCode)
		#	CUST.CONTRACT.schmCode = BANCS.OUTPARAM.schmCode
		#	print(CUST.CONTRACT.schmCode)
		CUST.CONTRACT.schmType = BANCS.OUTPARAM.schmType
		print(CUST.CONTRACT.schmType)
	#}
	ENDIF
	CALL("accmdp019.scr")
	print(BANCS.OUTPUT.AmountWithNoDiscount)
	
	CUST.CONTRACT.charges=BANCS.OUTPUT.AmountWithNoDiscount
	sv_u = urhk_SetOrbOut("charges|"+ CUST.CONTRACT.charges)
	print(sv_u)
	TRACE OFF
END-->
