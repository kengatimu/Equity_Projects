#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script:
# event: NA
# description: g
#=================================================

############################################################################################################

# Source Name                       :
# Date                              : 30-07-2013
# Description                       : This script is called from the custom share purchase page to display a list of unverified transactions
##Depending on the country and branch
#03/02/2014  During inquiry also show all transaactios for the last three days
# Input                             : None
# Output                            : None
# Called Script                     : None
# Calling Script                    :
# Author                            :
# Modification History              :

############################################################################################################
<--START
CHECKSUM="589e977195d25bf2a015e05a38ca9c5cbb5b8b40472eea890b1e9c558eb9bf74"
	TRACE ON
	
	#==========================================================*
	# CREATING NEW REPOSITORY AND CLASS
	#==========================================================*
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	IF (CLASSEXISTS("CUST", "CUDDS") == 0) THEN
	#{
		CREATECLASS("CUST", "CUDDS", 5)
	#}
	ENDIF
	sv_a = 1
	
	sv_x=urhk_B2k_PrintRepos("BANCS")
	print(BANCS.INPUT.Originator)
	
	CUST.CUDDS.deleted="0"
	
	
	
	sv_s = "recCount"
	sv_s = sv_s + " | "
	sv_s = sv_s + " SELECT COUNT(*) "
	sv_s = sv_s + " FROM CUSTOM.SHARE_PURCHASE_MASTER "
	sv_s = sv_s + " where IS_VERIFIED=0"
	sv_s = sv_s + " AND tran_sol_id = ?SVAR "
	
	
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.mySolId
	print(sv_s)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	CUST.CUDDS.recCount = BANCS.OUTPARAM.recCount
	print(CUST.CUDDS.recCount)
	
	sv_q = urhk_SetOrbOut("recCount|"+CUST.CUDDS.recCount)
	
	sv_s = "purchasekey,tranid,tranamount,createdby,solid"
	sv_s = sv_s + " | "
	sv_s = sv_s + " select ID_KEY,TRAN_ID,TRAN_AMOUNT,CREATED_BY_ID,TRAN_SOL_ID "
	sv_s = sv_s + " FROM CUSTOM.SHARE_PURCHASE_MASTER"
	sv_s = sv_s + " WHERE IS_VERIFIED= 0"
	sv_s = sv_s + " AND tran_sol_id = ?SVAR "
	
	
	
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.mySolId
	
	print(sv_s)
	sv_c = urhk_dbCursorOpenWithBind(sv_s)
	
	IF( sv_c != 0) THEN
	#{
		sv_u = urhk_SRV_SetErr("There were no records that were fetched")
		BANCS.OUTPUT.SuccessOrFailure = "F"
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		sv_l = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		print(sv_l)
		sv_c = urhk_dbCursorFetch(sv_l)
		print(sv_c)
		
		WHILE( sv_c == 0)
		#{
			CUST.CUDDS.count = sv_a
			print(CUST.CUDDS.count)
			
			CUST.CUDDS.purchasekey= BANCS.OUTPARAM.purchasekey
			print(CUST.CUDDS.purchasekey)
			
			CUST.CUDDS.tranid= BANCS.OUTPARAM.tranid
			print(CUST.CUDDS.tranid)
			
			
			CUST.CUDDS.tranamount= BANCS.OUTPARAM.tranamount
			print(CUST.CUDDS.tranamount)
			
			
			CUST.CUDDS.createdby= BANCS.OUTPARAM.createdby
			print(CUST.CUDDS.createdby)
			
			CUST.CUDDS.solid= BANCS.OUTPARAM.solid
			print(CUST.CUDDS.solid)
			
			sv_z = "tranid_" + CUST.CUDDS.count
			sv_z = sv_z + "|" + CUST.CUDDS.purchasekey
			PRINT(sv_z)
			sv_u = urhk_SetOrbOut(sv_z)
			
			sv_z = "txtfldname28_" + CUST.CUDDS.count
			sv_z = sv_z + "|" + CUST.CUDDS.tranid
			sv_u = urhk_SetOrbOut(sv_z)
			
			sv_z = "txtfldname29_" + CUST.CUDDS.count
			sv_z = sv_z + "|" + CUST.CUDDS.tranamount
			sv_u = urhk_SetOrbOut(sv_z)
			
			sv_z = "txtfldname30_" + CUST.CUDDS.count
			sv_z = sv_z + "|" + CUST.CUDDS.createdby
			sv_u = urhk_SetOrbOut(sv_z)
			
			sv_z = "txtfldname31_" + CUST.CUDDS.count
			sv_z = sv_z + "|" + CUST.CUDDS.solid
			sv_u = urhk_SetOrbOut(sv_z)
			
			sv_a = sv_a + 1
			sv_c = urhk_dbCursorFetch(sv_l)
			print(sv_c)
		#}
		DO
		sv_c = urhk_dbCursorClose(sv_l)
		print(sv_c)
	#}
	ENDIF
	
	ENDOFSCRIPT:
		exitscript
		
END-->
