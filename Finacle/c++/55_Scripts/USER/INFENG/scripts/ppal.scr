#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: ppal.scr
# event: NA
# description: Validations in paypal
#=================================================

<--START
CHECKSUM="5f81f1e95fbb6074310547bf00907d327cb0e6b5d67fc8b7d8e0b243e64c0ca8"
	
	TRACE ON
	
	
	##############################################################################
	
	IF(REPEXISTS("CUST")==0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	
	IF (CLASSEXISTS("CUST","DATA") == 0) THEN
	#{
		CREATECLASS("CUST","DATA",5)
	#}
	ENDIF
	
	############################################
	#Assigns the values to the variables
	############################################
	
	
	
	print("Geting data from client application...")
	
	#'0450296638320'
	#288807
	
	
	CUST.DATA.nationalid=BANCS.INPUT.nationalid
	CUST.DATA.foracid=BANCS.INPUT.foracid
	
	#CUST.DATA.nationalid="288807"
	#CUST.DATA.foracid="0450296638320"
	
	BANCS.INPARAM.BINDVARS=CUST.DATA.foracid+"|"+CUST.DATA.nationalid
	
	
	sv_q="REC_COUNT|select COUNT(*) AS REC_COUNT from tbaadm.aas where acid=(select acid from tbaadm.gam where foracid=?SVAR) AND nma_key_id = (select ORGKEY from crmuser.accounts a where UNIQUEID=?SVAR)"
	
	sv_q=urhk_dbSelectWithBind(sv_q)
	
	PRINT(BANCS.OUTPARAM.REC_COUNT)
	
	IF(CINT(BANCS.OUTPARAM.REC_COUNT)==1) THEN
	#{
		
		sv_q="CUST_ID,CUST_NAME|select cust_id, acct_poa_as_name as CUST_NAME from tbaadm.aas where acid=(select acid from tbaadm.gam where foracid=?SVAR) AND nma_key_id = (select ORGKEY from crmuser.accounts a where UNIQUEID=?SVAR)"
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_q=urhk_dbSelectWithBind(sv_q)
		PRINT(sv_q)
		PRINT(BANCS.OUTPARAM.CUST_ID)
		PRINT(BANCS.OUTPARAM.CUST_NAME)
		sv_c=BANCS.OUTPARAM.CUST_ID
		sv_n=BANCS.OUTPARAM.CUST_NAME
		sv_x=urhk_SetOrbOut("Response|"+"Success")
		sv_x=urhk_SetOrbOut("CustomerId|"+sv_c)
		sv_x=urhk_SetOrbOut("CustomerName|"+sv_n)
		
		
		#}
	ELSE
		#{
		
		sv_u = urhk_SetOrbErr("NOT FOUND")
		
	#}
	ENDIF
	TRACE OFF
	
END-->
