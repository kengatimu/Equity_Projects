#-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
# Source Name           : b_roleidvalidate.scr
# Date                  : 20-june-2016
# Author                : jane
# Description           : This script is used to validate the role id for httum
#
# Sources called        : NA
# Sources called from   : NA
# Modification History  :
# <Serial No.>      <Date>          <Author Name>       <Description>
# 0.00a             20-june-2016     jane        Original version
#-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
<--START
CHECKSUM="31007980ea2c76105b2f0da22bd4078cb068fde4edcb0ce643d85b49f7e5c474"
	TRACE ON
	sv_a = urhk_B2k_PrintRepos("BANCS")
	print(sv_a)
	
	IF (REPEXISTS("CUSTS") == 0) THEN
	#{
		CREATEREP("CUSTS")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUSTS", "EOD") == 0) THEN
	#{
		CREATECLASS("CUSTS", "EOD", 5)
	#}
	ENDIF
	
	CUSTS.EOD.roleid = BANCS.STDIN.roleId
	print(CUSTS.EOD.roleid)

	IF(BANCS.STDIN.menuId=="HTTUM") THEN
	#{
		IF ((BANCS.STDIN.roleId=="ABC2") OR (BANCS.STDIN.roleId=="HOFO2")OR (BANCS.STDIN.roleId=="TFO2") OR (BANCS.STDIN.roleId=="CRM2") OR (BANCS.STDIN.roleId=="HCL2") OR (BANCS.STDIN.roleId=="CAD2")) THEN
		#{
			sv_u = urhk_SetOrbOut("paramV1|FAIL")
		#}
		ELSE
		#{
			sv_u = urhk_SetOrbOut("paramV1|PASS")
		#}
		ENDIF
	#}
	ENDIF

	IF((BANCS.STDIN.menuId=="HCAACTD") OR (BANCS.STDIN.menuId=="HOAACTD")) THEN
	#{
	
		IF (BANCS.STDIN.userWorkClass=="240") THEN
		#{
			IF (BANCS.STDIN.roleId!="TFOO") THEN
			#{
				sv_u = urhk_SetOrbOut("paramV1|FAIL")
			#}
			ELSE
			#{
				sv_u = urhk_SetOrbOut("paramV1|PASS")
			#}	
			ENDIF
			
		#}
		ENDIF
	#}
	ENDIF

	IF((BANCS.STDIN.menuId=="HCAACVTD") OR (BANCS.STDIN.menuId=="HOAACVTD")) THEN
	#{
	
		IF (BANCS.STDIN.userWorkClass=="240") THEN
		#{
			IF (BANCS.STDIN.roleId!="TFO2") THEN
			#{
				sv_u = urhk_SetOrbOut("paramV1|FAIL")
			#}
			ELSE
			#{
				sv_u = urhk_SetOrbOut("paramV1|PASS")
			#}	
			ENDIF
			
		#}
		ENDIF
	#}
	ENDIF
	
		
	TRACE OFF
	EXITSCRIPT
	END-->
	
	
