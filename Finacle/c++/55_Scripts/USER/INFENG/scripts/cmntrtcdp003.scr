#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cmntrtcdp003.scr
# Type: NA|default
# Description: validation script for ratecode in CMNTRTC
# Author: haneesha.kanumuru
# Date: 5/7/18 6:09 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="56aa6dbf213964462c609fe4d4f2cea9598d294677e16fb717ca7bb123dade15"
	# Script body goes here
	TRACE ON
	#==========================================================*
	# create repository
	#==========================================================*
	sv_a = REPEXISTS("CUST")
	IF(sv_a == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	sv_r = CLASSEXISTS("CUST","CMNTRTC")
	IF (sv_r == 0) THEN
	#{
		CREATECLASS("CUST", "CMNTRTC", 5)
	#}
	ENDIF
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	
	
	CUST.CMNTRTC.errorFlg = "N"
	CUST.CMNTRTC.errorMsg = " "
	CUST.CMNTRTC.rateCodeCash = " "
	IF(FIELDEXISTS(BANCS.INPUT.rateCodeCash)) THEN
	#{
		IF(TRIM(BANCS.INPUT.rateCodeCash) != "")THEN
		#{
			CUST.CMNTRTC.rateCodeCash= TOUPPER(TRIM(BANCS.INPUT.rateCodeCash))
			PRINT(CUST.CMNTRTC.rateCodeCash)
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.CMNTRTC.errorFlg = "Y"
		CUST.CMNTRTC.errorMsg = "Invalid Input for fnExecuteScript"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	
	
	
	sv_s = "count|"
	sv_s = sv_s + " SELECT COUNT(*)"
	sv_s = sv_s + " FROM TBAADM.RCT "
	sv_s = sv_s + " WHERE REF_CODE = ?SVAR"
	#sv_s = sv_s + " AND DEL_FLG != 'Y'"
	#sv_s = sv_s + " AND ENTITY_CRE_FLG != 'N'"
	#sv_s = sv_s + " WHERE ACCT_NUM = ?SVAR"
	sv_s = sv_s + " AND BANK_ID = ?SVAR"
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS =   CUST.CMNTRTC.rateCodeCash + "|"+ BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	CUST.CMNTRTC.count = BANCS.OUTPARAM.count
	PRINT(CUST.CMNTRTC.count)
	
	
	IF( CUST.CMNTRTC.count ==0 )THEN
	#{
		#		CUST.CMNTRTC.acctNum = BANCS.OUTPARAM.acctNum
		#		PRINT(CUST.CMNTRTC.acctNum)
		#		#}
		#	ELSE
		#		#{
		CUST.CMNTRTC.errorFlg = "Y"
		CUST.CMNTRTC.errorMsg = "Invalid Rate Code"
	#}
	ENDIF
	ENDOFSCRIPT:
		sv_u = urhk_SetOrbOut("errorFlg|"+CUST.CMNTRTC.errorFlg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("errorMsg|"+CUST.CMNTRTC.errorMsg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("rateCodeCash|"+CUST.CMNTRTC.rateCodeCash)
		
		PRINT(sv_u)
		TRACE OFF
END-->
