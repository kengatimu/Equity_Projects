#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: b_hapohapoverf.scr
# Type: NA|default
# Description: b_hapohapoverf.scr
# Author:
# Date:
# Modification Log:
#============================================================

#------------------------------------------------------------------------------------------------
#   Name            : b_hapohapoverf.scr
#   Description     : This script is called from job
#   Date            : 19-03-2014
#   Author          : jane
#   Input           : NA
#   Output          : NA
#   Called Script   :
#   Calling Script  : NA
#   Menu Option     :
#   Srl. No         Date            Author          	Description.
#   -------         ------          ------          	------------
#   1.0             19-03-2014      jane     		Original Version
#-----------------------------------------------------------------------------------------------
<--START
CHECKSUM="a407c4622b04e689f3f2c482c6324fecc0aa9cc955148df4e63c4ef2671268af"
	TRACE ON
	
	sv_b = BANCS.STDIN.mySolId
	PRINT(sv_b)
	
	sv_s="foracid,acid,schmcode|"
	sv_s = sv_s + "select foracid,acid,schm_code from tbaadm.gam "
	sv_s = sv_s + "where schm_code in ('SB777','SB778')  "
	sv_s = sv_s + "AND entity_cre_flg='N' "
	sv_s = sv_s + "AND clr_bal_amt>'0' "
	sv_s = sv_s + "AND bank_id = ?SVAR"
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_e = urhk_dbCursorOpenWithBind(sv_s)
	
	IF(sv_e==0) THEN
	#{
		sv_m = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		sv_q = urhk_dbCursorFetch(sv_m)
		PRINT(sv_q)
		
		IF(sv_q == 1) THEN
		#{
			sv_x = urhk_dbCursorClose(sv_m)
			PRINT(sv_x)
			EXITSCRIPT
		#}
		ENDIF
		PRINT(sv_q)
		
		WHILE(sv_q==0)
		#{
			sv_r = REPEXISTS("CUST")
			IF (sv_r == 0) THEN
			#{
				CREATEREP("CUST")
			#}
			ENDIF
			
			sv_r = CLASSEXISTS("CUST", "GLBDATA")
			IF (sv_r == 0) THEN
			#{
				CREATECLASS("CUST", "GLBDATA", 5)
			#}
			ENDIF
			
			
			CUST.GLBDATA.foracid=BANCS.OUTPARAM.foracid
			PRINT(CUST.GLBDATA.foracid)
			
			CUST.GLBDATA.acid=BANCS.OUTPARAM.acid
			PRINT(CUST.GLBDATA.acid)
			
			CUST.GLBDATA.schmcode=BANCS.OUTPARAM.schmcode
			PRINT(CUST.GLBDATA.schmcode)
			
			
			
			sv_a=urhk_SetUrhkInp("acctCommonInfo.acctGeneralInfo.acctBasic.foracid|" + CUST.GLBDATA.foracid)
			PRINT(sv_a)
			
			sv_u = urhk_ExecSrv("SRV_FetchSBAcctOpenDtlForVerify|retain_all_output = Y")
			PRINT(sv_u)
			
			IF (sv_u == 0 ) THEN
			#{
				sv_u = urhk_CopyOutToIn("")
				PRINT(sv_u)
				
				sv_u = urhk_SetUrhkInp("acctCommonInfo.acctGeneralInfo.acctBasic.foracid|" + CUST.GLBDATA.foracid)
				PRINT(sv_u)
				sv_u = urhk_SetUrhkInp("acctCommonInfo.acctGeneralInfo.acctBasic.acid|" + CUST.GLBDATA.acid)
				PRINT(sv_u)
				sv_u = urhk_SetUrhkInp("acctCommonInfo.acctGeneralInfo.acctBasic.schmCode.schmCode|" + CUST.GLBDATA.schmcode)
				PRINT(sv_u)
				
				sv_u = urhk_ExecSrv("SRV_VerifySBAcct|retain_all_output = Y")
				PRINT(sv_u)
				
				
			#}
			ENDIF
			
			IF (sv_u != 0) THEN
			#{
				IF(FIELDEXISTS(BANCS.OUTPARAM.Error_num) == 1) THEN
				#{
					PRINT(BANCS.OUTPARAM.Error_num)
					sv_v = BANCS.OUTPARAM.Error_num
					#}
				ELSE
					#{
					sv_v = 0
				#}
				ENDIF
				
				IF (sv_v > 0) THEN
				#{
					sv_i = 1
					WHILE (sv_i <= sv_v)
					#{
						sv_f = FORMAT$(sv_i,"%d")
						sv_e = ("BANCS").("OUTPARAM").("ErrorMesg_"+sv_f)
						PRINT(sv_e)
						sv_e = ("BANCS").("OUTPARAM").("ErrorCode_"+sv_f)
						PRINT(sv_e)
						sv_e = ("BANCS").("OUTPARAM").("Tag_"+sv_f)
						PRINT(sv_e)
						sv_i = sv_i + 1
					#}
					DO
				#}
				ENDIF
			#}
			ENDIF
			
			
			sv_q = urhk_dbCursorFetch(sv_m)
			PRINT(sv_q)
			
		#}
		DO
		PRINT("ALL ACCOUNTS ARE VERIFIED")
	#}
	ENDIF
	
	
	TRACE OFF
	EXITSCRIPT
END-->
