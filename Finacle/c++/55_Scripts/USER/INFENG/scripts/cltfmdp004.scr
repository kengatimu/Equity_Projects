#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cltfmdp004.scr
# Type: NA|default
# Description: validation script for account number
# Author: haneesha.kanumuru
# Date: 2/23/18 2:26 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="d38bfef9a371936c75212f8be8df11e1e1c96ac27bf0355e6d09679371f0dea3"
	# Script body goes here
	TRACE ON
	#==========================================================*
	# create repository
	#==========================================================*
	sv_a = REPEXISTS("CUST")
	IF(sv_a == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	sv_r = CLASSEXISTS("CUST","CLTFM")
	IF (sv_r == 0) THEN
	#{
		CREATECLASS("CUST", "CLTFM", 5)
	#}
	ENDIF
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	
	CUST.CLTFM.errorFlg = "N"
	CUST.CLTFM.errorMsg = " "
	CUST.CLTFM.acctId =" "
	#	CUST.CLTFM.count =" "
	IF(FIELDEXISTS(BANCS.INPUT.acctId)) THEN
	#{
		IF(TRIM(BANCS.INPUT.acctId) != "")THEN
		#{
			CUST.CLTFM.acctId= TOUPPER(TRIM(BANCS.INPUT.acctId))
			PRINT(CUST.CLTFM.acctId)
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.CLTFM.errorFlg = "Y"
		CUST.CLTFM.errorMsg = "Invalid Input for fnExecuteScript"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	sv_s = "acct|"
	sv_s = sv_s + " SELECT foracid"
	sv_s = sv_s + " FROM TBAADM.GAM "
	sv_s = sv_s + " WHERE FORACID = ?SVAR"
	sv_s = sv_s + " AND DEL_FLG != 'Y'"
	sv_s = sv_s + " AND ENTITY_CRE_FLG != 'N'"
	sv_s = sv_s + " AND acct_cls_flg !='Y'"
	sv_s = sv_s + " AND SCHM_TYPE = 'LAA'"
	sv_s = sv_s + " AND BANK_ID = ?SVAR"
	
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.CLTFM.acctId + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	#	CUST.CLTFM.count = BANCS.OUTPARAM.count
	#	PRINT(CUST.CLTFM.count)
	IF( sv_d ==0 )THEN
	#{
		CUST.CLTFM.acctId =BANCS.OUTPARAM.acct
		PRINT(CUST.CLTFM.acctId)
		#}
	ELSE
		#{
		CUST.CLTFM.errorFlg = "Y"
		CUST.CLTFM.errorMsg = "Invalid Account Number"
		
		
		#}
		
	#}
	ENDIF
	CUST.CLTFM.acctId = CUST.CLTFM.acctId
	
	ENDOFSCRIPT:
		sv_u = urhk_SetOrbOut("errorFlg|"+CUST.CLTFM.errorFlg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("errorMsg|"+CUST.CLTFM.errorMsg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("acctId|"+CUST.CLTFM.acctId)
		
		PRINT(sv_u)
END-->
