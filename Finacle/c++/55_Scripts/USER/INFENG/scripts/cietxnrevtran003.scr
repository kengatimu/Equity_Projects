#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cietxnrevtran003.scr
# Type: NA|default
# Description: a
# Author: Balaji_P05
# Date: 4/9/20 10:34 PM
# Modification Log:
#============================================================

<--START
	TRACE ON
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CIETXN Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CIETXN")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CIETXN",5)
	#}
	ENDIF
	IF(MID$(BANCS.STDIN.userId,0,6) == "FIVUSR")THEN
	#{
		PRINT(BANCS.INPUT.inputData)
		CUST.CIETXN.refNum=BANCS.INPUT.inputData
		CUST.CIETXN.revFlg="N"
	#}
	ENDIF

	PRINT(CUST.CIETXN.revFlg)
			sv_s = "recCount|SELECT COUNT(*) FROM CUSTOM.IETD WHERE ie_txn_ref_no=?SVAR"
		BANCS.INPARAM.BINDVARS = CUST.CIETXN.refNum
		PRINT(sv_s)
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d  = urhk_dbSelectWithBind(sv_s)
		IF(sv_d == "0")THEN
		#{
			CUST.CIETXN.recCount = BANCS.OUTPARAM.recCount
			PRINT(CUST.CIETXN.recCount)
		#}
		ENDIF	
		IF(CUST.CIETXN.recCount == "0")THEN
		#{
			CUST.CIETXN.revFlg="Y"
		#}
		ELSE
		#{
			sv_s = "src_tran_id|SELECT ENTITY1_TRAN_ID FROM CUSTOM.IETD WHERE ie_txn_ref_no=?SVAR"
			BANCS.INPARAM.BINDVARS = CUST.CIETXN.refNum
	                PRINT(sv_s)
        	        PRINT(BANCS.INPARAM.BINDVARS)
                	sv_d  = urhk_dbSelectWithBind(sv_s)
                	IF(sv_d == "0")THEN
                	#{
                        	CUST.CIETXN.src_tran_id = BANCS.OUTPARAM.src_tran_id
				PRINT(CUST.CIETXN.src_tran_id)
				IF(TRIM(CUST.CIETXN.src_tran_id) == "")THEN
				#{
					CUST.CIETXN.revFlg="Y"
				#}
				ENDIF
			#}
			ENDIF
	#}
	ENDIF
	ENDOFSCRIPT:
		IF(MID$(BANCS.STDIN.userId,0,6) == "FIVUSR")THEN
		#{
			
			sv_b=urhk_setOrbOut("outputData|"+CUST.CIETXN.revFlg)
			PRINT(sv_b)
		#}
		ENDIF
		EXITSCRIPT
END-->
