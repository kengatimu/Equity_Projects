#----------------------------------------------------------------------------------------------------------------------------- 
#    Name			 : crvCifmn001.scr
#    Description		: to populate cif id type radio button and  solID for TOL - 356357
#    Date			: 11-07-2013
#    Author			  : Vijaya V
#    Called Script		 : NA
#    Calling Script	 : custsrchicrit_custom.jsp
#    Menu Option		 : HCRV
#    Srl. No			Date			Author			Description.   
#    ------				----------			-------			---------------			
#    1.0			11-07-2013			Vijaya V		Original Version
#----------------------------------------------------------------------------------------------------------------------------
<--START
#TRACE ON

	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF

	IF (CLASSEXISTS("CUST", "CRV") == 0) THEN
	#{
		CREATECLASS("CUST", "CRV", 5)
	#}
	ENDIF
	
	CUST.CRV.type	=	" "
	CUST.CRV.corpId	=	" "
	CUST.CRV.solId	=	BANCS.STDIN.mySolId
	
	#########################################################
	#Inputs from jsp
	#########################################################
	print(BANCS.INPUT.cif)	

	CUST.CRV.cif = trim(BANCS.INPUT.cif)
	
	#sv_q = urhk_B2k_PrintRepos("BANCS")
	#print(sv_q)

	print(BANCS.STDIN.contextBankId)

	sv_s = "corpId|SELECT CORP_ID "
	sv_s = sv_s + " FROM TBAADM.CMG "
	sv_s = sv_s + " WHERE del_flg ='N' "
	sv_s = sv_s + " AND ENTITY_CRE_FLG= 'Y' "
	sv_s = sv_s + " AND CIF_ID =  ?SVAR "
	sv_s = sv_s + " AND bank_id = ?SVAR "
	BANCS.INPARAM.BINDVARS = CUST.CRV.cif  + "|"  + BANCS.STDIN.contextBankId	
	PRINT(sv_s)

	sv_d = urhk_dbSelectWithBind(sv_s)
	IF (sv_d == 0) THEN
	#{
		CUST.CRV.corpId = BANCS.OUTPARAM.corpId
		print(CUST.CRV.corpId)

		IF(CUST.CRV.corpId =="") THEN
		#{
			CUST.CRV.type = "R"
		#}
		ELSE
		#{
			CUST.CRV.type = "C"
		#}
		ENDIF

		sv_u = urhk_SetOrbOut("paramV1|"+CUST.CRV.type)
		sv_u = urhk_SetOrbOut("paramV2|"+CUST.CRV.solId)
	#}
	ELSE
	#{
		sv_u = urhk_SetOrbOut("err|"+CUST.CRV.corpId)     		
		GOTO ENDSCRIPT
	#}
	ENDIF

				
ENDOFSCRIPT:
#TRACE OFF
EXITSCRIPT
END-->
