#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: tlrdenadp003.scr
# Type: NA|default
# Description: Custom Menu : HTLRDENA
# Author:
# Date:
# Modification Log:
#============================================================

###########################################################################################
# Source Name           : tlrdenadp003.scr
# Date                  :
# Description           : To get the list of teller id
# Input                 : none
# Output                : none
# Called Script         : none
# Calling Script        : none
############################################################################################


<--START
CHECKSUM="9833767b01e935ccfa507b7e581ba1b20cf820da434ee2c710750ca00f1fef21"
	TRACE ON
	#-------------------------------------------------------------------------------
	#	Creating Repository and Class
	#-------------------------------------------------------------------------------
	sv_r = REPEXISTS("CUST")
	IF (sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	sv_r = CLASSEXISTS("CUST","DENOM")
	IF (sv_r ==0) THEN
	#{
		CREATECLASS("CUST","DENOM",5)
	#}
	ENDIF
	#sv_u=urhk_b2k_PrintRepos("BANCS")
	#print (sv_u)
	CUST.DENOM.funcCode =BANCS.INPUT.funcCode
	PRINT(CUST.DENOM.funcCode)
	
	sv_q = "teller_id,crncy,sol_id|SELECT DISTINCT teller_id,crncy_code,sol_id  FROM"
	IF((CUST.DENOM.funcCode == "I") OR (CUST.DENOM.funcCode == "M"))THEN
	#{
		sv_q = sv_q + " custom.C_DENM"
		#}
	ELSE
		#{
		sv_q = sv_q + " custom.C_DENM_MOD "
	#}
	ENDIF
	sv_q = sv_q  + " where sol_id = '"+BANCS.STDIN.mySolId+"'"
	PRINT(sv_q)
	sv_b = urhk_dbCursorOpenWithBind(sv_q)
	PRINT(sv_b)
	IF (sv_b == 0) THEN
	#{
		CUST.DENOM.cursorNumber1 = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		#}
	ELSE
		#{
		GOTO FINISH
	#}
	ENDIF
	sv_o = 0
	PRINT(CUST.DENOM.cursorNumber1)
	sv_f = urhk_dbCursorFetch(CUST.DENOM.cursorNumber1)
	WHILE(sv_f == 0)
	#{
		sv_o = sv_o + 1
		PRINT(sv_o)
		sv_f = urhk_dbCursorFetch(CUST.DENOM.cursorNumber1)
	#}
	DO
	sv_f = urhk_dbCursorClose(CUST.DENOM.cursorNumber1)
	PRINT(sv_f)
	PRINT(sv_o)
	IF(sv_o>1) THEN
	#{
		CUST.DENOM.COUNT = sv_o - 1
		#}
	ELSE
		#{
		CUST.DENOM.COUNT = sv_o
	#}
	ENDIF
	
	CUST.DENOM.COUNT = FORMAT$(sv_o,"%d")
	PRINT("recCount|"+CUST.DENOM.COUNT)
	
	#--- 1st output from a listing script should be the count of the total records that will be listed out --> recCount
	sv_u    =   urhk_SetOrbOut("recCount|"+CUST.DENOM.COUNT)
	
	sv_q = "tellerId,crncy,solId|SELECT DISTINCT teller_id,crncy_code,sol_id FROM "
	IF((CUST.DENOM.funcCode == "I") OR (CUST.DENOM.funcCode == "M")) THEN
	#{
		sv_q = sv_q + " custom.C_DENM"
		#}
	ELSE
		#{
		sv_q = sv_q + "custom.C_DENM_MOD "
	#}
	ENDIF
	sv_q = sv_q  + " where sol_id = '"+BANCS.STDIN.mySolId+"'"
	sv_q =sv_q + " ORDER BY teller_id"
	PRINT(sv_q)
	sv_b = urhk_dbCursorOpenWithBind(sv_q)
	PRINT(sv_b)
	IF (sv_b == 0) THEN
	#{
		CUST.DENOM.cursorNumber = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		#}
	ELSE
		#{
		GOTO FINISH
	#}
	ENDIF
	
	PRINT(CUST.DENOM.cursorNumber)
	sv_f = urhk_dbCursorFetch(CUST.DENOM.cursorNumber)
	sv_z = 1
	WHILE(sv_f == 0)
	#{
		CUST.DENOM.Limit_Suffix = BANCS.OUTPARAM.tellerId
		CUST.DENOM.Limit_Prefix = BANCS.OUTPARAM.crncy
		CUST.DENOM.solId	= BANCS.OUTPARAM.solId
		sv_y = format$(sv_z,"%d")
		
		#--- Followed by record count, the outputs should be as name value pairs with name as
		#--- <outputNames>_<rec_no> defined as 2nd paramenter in fnExecuteScriptForList()
		
		CUST.DENOM.tellerId = "tellerId_"+sv_y+"|"+CUST.DENOM.Limit_Suffix
		PRINT(CUST.DENOM.tellerId)
		sv_u = urhk_SetOrbOut(CUST.DENOM.tellerId)
		PRINT(sv_u)
		CUST.DENOM.crncy = "crncy_"+sv_y+"|"+CUST.DENOM.Limit_Prefix
		PRINT(CUST.DENOM.crncy)
		sv_u = urhk_SetOrbOut(CUST.DENOM.crncy)
		PRINT(sv_u)
		
		CUST.DENOM.solId = "solId_"+sv_y+"|"+CUST.DENOM.solId
		PRINT(CUST.DENOM.solId)
		sv_u = urhk_SetOrbOut(CUST.DENOM.solId)
		PRINT(sv_u)
		
		sv_z = sv_z + 1
		PRINT(sv_z)
		sv_f = urhk_dbCursorFetch(CUST.DENOM.cursorNumber)
	#}
	DO
	###########################################################
	# Cursor Closure
	###########################################################
	sv_f = urhk_dbCursorClose(CUST.DENOM.cursorNumber)
	PRINT(sv_f)
	
	FINISH:
		EXITSCRIPT
		TRACE OFF
END-->
