################################################################################################################################################################################
#    Name			 : s_miscfreetext002
#    Description     : Adding the fields to the custom table
#    Date            : 08-01-2024
#    Author          : Osuka S
#    Input           : misFreeText16, misFreeText17, misFreeText18, misFreeText19, misFreeText20, misFreeText21, misFreeText22, misFreeText23, misFreeText24, misFreeText25
#    Output			 : CUST.ACCTOPENLA.retMsg, CUST.ACCTOPENLA.sucFail, CUST.ACCTOPENLA.fieldId
#    Called Script	 : NA
#    Calling Script	 : NA
#    Menu Option	 : HOAACLA
#    Srl. No		Date			    Author			Description.   
#    ------			------------        --------		---------------			
#    1.0			08-01-2024			Osuka S			Original Version
################################################################################################################################################################################
<--START
TRACE ON
#-----------------------------------------------------------------
#Creating repository and class
#-----------------------------------------------------------------

IF (REPEXISTS("CUST") == 0) THEN
#{
	CREATEREP("CUST")
#}
ENDIF

IF (CLASSEXISTS("CUST", "ACCTOPENLA") == 0) THEN
#{
	CREATECLASS("CUST", "ACCTOPENLA", 5)
#}
ENDIF

###################################################################################################################
#---------------------------------------- CONFIRM IF AN ACCOUNT EXISTS IN GAM ------------------------------------#
###################################################################################################################

sv_a = urhk_B2k_PrintRepos("BANCS")
print(sv_a)

IF(FIELDEXISTS(BANCS.INPUT.mopId))THEN
#{	
	IF(TRIM(BANCS.INPUT.mopId ) != "")THEN
	#{
		CUST.ACCTOPENLA.mopId=BANCS.INPUT.mopId
		PRINT(CUST.ACCTOPENLA.mopId)
	#}
	ENDIF
#}
ELSE 
#{
	IF(FIELDEXISTS(BANCS.STDIN.menuId))THEN
	#{
		IF(TRIM(BANCS.STDIN.menuId ) != "")THEN
		#{
			CUST.ACCTOPENLA.mopId=BANCS.STDIN.menuId
			PRINT(CUST.ACCTOPENLA.mopId)
		#}
		ENDIF
	#}
	ENDIF
#}
ENDIF

IF(FIELDEXISTS(BANCS.INPUT.misFreeText16))THEN
#{	
	IF(TRIM(BANCS.INPUT.misFreeText16) != "")THEN
	#{
		CUST.ACCTOPENLA.misFreeText16=BANCS.INPUT.misFreeText16
		PRINT(CUST.ACCTOPENLA.misFreeText16)
	#}
	ENDIF
#}
ENDIF

IF(FIELDEXISTS(BANCS.INPUT.misFreeText17))THEN
#{	
	IF(TRIM(BANCS.INPUT.misFreeText17) != "")THEN
	#{
		CUST.ACCTOPENLA.misFreeText17=BANCS.INPUT.misFreeText17
		PRINT(CUST.ACCTOPENLA.misFreeText17)
	#}
	ENDIF
#}
ENDIF

IF(FIELDEXISTS(BANCS.INPUT.misFreeText18))THEN
#{	
	IF(TRIM(BANCS.INPUT.misFreeText18) != "")THEN
	#{
		CUST.ACCTOPENLA.misFreeText18=BANCS.INPUT.misFreeText18
		PRINT(CUST.ACCTOPENLA.misFreeText18)
	#}
	ENDIF
#}
ENDIF

IF(FIELDEXISTS(BANCS.INPUT.misFreeText19))THEN
#{	
	IF(TRIM(BANCS.INPUT.misFreeText19) != "")THEN
	#{
		CUST.ACCTOPENLA.misFreeText19=BANCS.INPUT.misFreeText19
		PRINT(CUST.ACCTOPENLA.misFreeText19)
	#}
	ENDIF
#}
ENDIF

IF(FIELDEXISTS(BANCS.INPUT.misFreeText20))THEN
#{	
	IF(TRIM(BANCS.INPUT.misFreeText20) != "")THEN
	#{
		CUST.ACCTOPENLA.misFreeText20=BANCS.INPUT.misFreeText20
		PRINT(CUST.ACCTOPENLA.misFreeText20)
	#}
	ENDIF
#}
ENDIF

IF(FIELDEXISTS(BANCS.INPUT.misFreeText21))THEN
#{	
	IF(TRIM(BANCS.INPUT.misFreeText21) != "")THEN
	#{
		CUST.ACCTOPENLA.misFreeText21=BANCS.INPUT.misFreeText21
		PRINT(CUST.ACCTOPENLA.misFreeText21)
	#}
	ENDIF
#}
ENDIF

IF(FIELDEXISTS(BANCS.INPUT.misFreeText22))THEN
#{	
	IF(TRIM(BANCS.INPUT.misFreeText22) != "")THEN
	#{
		CUST.ACCTOPENLA.misFreeText22=BANCS.INPUT.misFreeText22
		PRINT(CUST.ACCTOPENLA.misFreeText22)
	#}
	ENDIF
#}
ENDIF

IF(FIELDEXISTS(BANCS.INPUT.misFreeText23))THEN
#{	
	IF(TRIM(BANCS.INPUT.misFreeText23) != "")THEN
	#{
		CUST.ACCTOPENLA.misFreeText23=BANCS.INPUT.misFreeText23
		PRINT(CUST.ACCTOPENLA.misFreeText23)
	#}
	ENDIF
#}
ENDIF

IF(FIELDEXISTS(BANCS.INPUT.misFreeText24))THEN
#{	
	IF(TRIM(BANCS.INPUT.misFreeText24) != "")THEN
	#{
		CUST.ACCTOPENLA.misFreeText24=BANCS.INPUT.misFreeText24
		PRINT(CUST.ACCTOPENLA.misFreeText24)
	#}
	ENDIF
#}
ENDIF

IF(FIELDEXISTS(BANCS.INPUT.misFreeText25))THEN
#{	
	IF(TRIM(BANCS.INPUT.misFreeText25) != "")THEN
	#{
		CUST.ACCTOPENLA.misFreeText25=BANCS.INPUT.misFreeText25
		PRINT(CUST.ACCTOPENLA.misFreeText25)
	#}
	ENDIF
#}
ENDIF


IF(FIELDEXISTS(CUST.LAOPN.foracid))THEN
#{	
	IF(TRIM(CUST.LAOPN.foracid) != "")THEN
	#{
		CUST.ACCTOPENLA.foracid=TRIM(CUST.LAOPN.foracid)
		PRINT(CUST.ACCTOPENLA.foracid)
	#}
	ENDIF
#}
ENDIF

BANCS.INPARAM.BINDVARS = CUST.ACCTOPENLA.foracid
PRINT(BANCS.INPARAM.BINDVARS)
sv_s = "acid|"
sv_s = sv_s + " SELECT ACID"
sv_s = sv_s + " FROM TBAADM.GAC"
sv_s = sv_s + " WHERE ACID IN"
sv_s = sv_s + " ( SELECT ACID"
sv_s = sv_s + " FROM TBAADM.GAM"
sv_s = sv_s + " WHERE FORACID =?SVAR)"
PRINT(sv_s)

sv_d =  urhk_dbSelectWithBind(sv_s)
PRINT(sv_d)
IF (sv_d == 0 ) THEN
#{
	CUST.ACCTOPENLA.acid = BANCS.OUTPARAM.acid
	PRINT(CUST.ACCTOPENLA.acid)

	###################################################################################################################
	#---------------------------------------- CONFIRM IF AN ACCOUNT EXISTS IN CUSTOM TABLE ---------------------------#
	###################################################################################################################

	###################################################################################################################
	#---------------------------------------- INSERTING A NEW ACCOUNT ------------------------------------------------#
	###################################################################################################################
	
	GOTO ENDOFSCRIPT
#}
ELSE
#{
	PRINT("Failed to get the ACID")
	GOTO ENDOFSCRIPT
#}
ENDIF



###################################################################################################################
#-----------------------------------------------------------------------------------------------------------------#
###################################################################################################################



ENDOFSCRIPT:
IF(CUST.ACCTOPENLA.sucFail != "Y") THEN
#{
	print(CUST.ACCTOPENLA.retMsg)
	print(CUST.ACCTOPENLA.sucFail)
	print(CUST.ACCTOPENLA.fieldId)

	sv_u = urhk_SetOrbOut("retMsg|" + CUST.ACCTOPENLA.retMsg)
	sv_u = urhk_SetOrbOut("sucFail|" + CUST.ACCTOPENLA.sucFail)
	sv_u = urhk_SetOrbOut("fieldId|" + CUST.ACCTOPENLA.fieldId)
#}
ELSE
#{
	sv_u = urhk_SetOrbOut("retMsg| ")
	sv_u = urhk_SetOrbOut("sucFail|" + CUST.ACCTOPENLA.sucFail)
	sv_u = urhk_SetOrbOut("fieldId| ")
#}
ENDIF

TRACE OFF
EXITSCRIPT
END-->