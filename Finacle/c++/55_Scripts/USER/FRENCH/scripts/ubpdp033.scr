#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: ubpdp033.scr
# event: NA
# description: NA
#=================================================

<--START
CHECKSUM="b98f2e865f70de917d379a5e0773b2ff6fdf03775ac053ace0ea479ca467a28d"
	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp033.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called from ubpdp032.scr
	#                                for SAFARICOM biller.
	# Called Script         :		ubpdp033.scr
	# Calling function      :		function fnGetBillDtl()
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           16-04-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	
	sv_s = "srlnum|"
	sv_s = sv_s + " SELECT CUSTOM.YUCASH_RFI_SEQ.nextval"
	sv_s = sv_s + " FROM DUAL"
	PRINT(sv_s)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d!=0)THEN
	#{
		CUST.UBP.errorFlg="Y"
		#CUST.UBP.errorMsg="Database error has occured.Please contact DBA."
		CUST.UBP.errorCode = "UBP_ERR050"
		CALL("ubpdp044.scr")
		CUST.UBP.errorMsg =CUST.UBP.errorDesc
		PRINT(CUST.UBP.errorMsg)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	CUST.UBP.srlnum=BANCS.OUTPARAM.srlnum
	print(CUST.UBP.srlnum)
	
	
	
	BANCS.INPARAM.BINDVARS = "I|0|0|"+CUST.UBP.billRefNum+"|"+CUST.UBP.billAmt+"|"+BANCS.STDIN.mySolId
	BANCS.INPARAM.BINDVARS =BANCS.INPARAM.BINDVARS+"|"+CUST.UBP.srlnum+"|"+CUST.UBP.payment
	BANCS.INPARAM.BINDVARS =BANCS.INPARAM.BINDVARS+"|"+ CUST.UBP.requestApp
	sv_s = " INSERT INTO CUSTOM.YUCASH_RFI_TABLE"
	sv_s = sv_s + " (CASHINOUTFLG,STATUS,STATUS_CODE,MOBILENUMBER,AMOUNT,BRANCH_CODE,ID,PAYMENT_METHOD,REQUESTAPP)"
	sv_s = sv_s + " values (?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR)"
	PRINT(sv_s)
	sv_d = urhk_dbSQLWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d!=0)THEN
	#{
		CUST.UBP.errorFlg="Y"
		#CUST.UBP.errorMsg="Database error has occured.Please contact DBA."
		CUST.UBP.errorCode = "UBP_ERR050"
		CALL("ubpdp044.scr")
		CUST.UBP.errorMsg =CUST.UBP.errorDesc
		PRINT(CUST.UBP.errorMsg)
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		sv_d = urhk_dbSqlWithBind("COMMIT")
		
		#========================================================
		# Check whether CUST Repository exists else create it
		#========================================================
		sv_r = REPEXISTS("YUURL")
		IF(sv_r == 0) THEN
		#{
			CREATEREP ("YUURL")
		#}
		ENDIF
		#========================================================
		# Check whether UBP Class exists else create it
		#========================================================
		sv_r = CLASSEXISTS("YUURL","STDIN")
		IF(sv_r == 0) THEN
		#{
			CREATECLASS ("YUURL","STDIN",5)
		#}
		ENDIF
		#========================================================
		# Check whether UBP Class exists else create it
		#========================================================
		sv_r = CLASSEXISTS("YUURL","STDOUT")
		IF(sv_r == 0) THEN
		#{
			CREATECLASS ("YUURL","STDOUT",5)
		#}
		ENDIF
		YUURL.STDIN.URL="http://10.11.2.6:8080/safaricom-dealer-portal/enquireinfo?id="+CUST.UBP.srlnum
		PRINT(YUURL.STDIN.URL)
		
		#Set the Frame name to point to an existing frame.
		#To open the URL in a new window, give a window name.
		#To always open in a new window, comment out the line.
		
		YUURL.STDIN.FRAME="mywin"
		
		YUURL.STDOUT.ErrorCode="0"
		YUURL.STDOUT.ErrorDesc="No error"
		
		sv_a = urhk_TBAF_ShowHTML("YUURL")
		PRINT(YUURL.STDOUT.ErrorCode)
		PRINT(YUURL.STDOUT.ErrorDesc)
		DELETEREP("YUURL")
		#CALL("safThirdPartyDb001.scr")
		IF ((CUST.UBP.billerId !="AIRTEL") AND (CUST.UBP.billerId !="AIRTELKE") AND (CUST.UBP.billerId !="SAFARICOM")) THEN
		#{
			CALL("ubpdp034.scr")
		#}
		ELSE
		#{
			IF (CUST.UBP.billerId =="SAFARICOM") THEN
			#{
				CUST.UBP.particulars = "SAF.DEALER /" + CUST.UBP.billRefNum + "/" + CUST.UBP.name
				PRINT(CUST.UBP.particulars)
				
			#}
			ELSE
			#{
				CUST.UBP.particulars = CUST.UBP.billRefNum + "/" + CUST.UBP.particulars
				PRINT(CUST.UBP.particulars)
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
END-->
