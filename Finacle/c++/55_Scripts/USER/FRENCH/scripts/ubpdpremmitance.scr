#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script:   ubpdpremmitance.scr
# event: NA
# description: NA
#=================================================

<--START
CHECKSUM="d72c933fd712ef54bfad271bb13d4841b02bafa8bdcc4175dfb0b4082e61f56c"
	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp032.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called from ubp_det_link.js on click of get bill
	#                               details button for REMMITANCE  biller.
	# Called Script         :		ubpdp033.scr
	# Calling function      :		function fnGetBillDtl()
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           16-04-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	#========================================================
	# Check whether CUST Repository exists else create it
	#========================================================
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	#========================================================
	# Check whether UBP Class exists else create it
	#========================================================
	sv_r = CLASSEXISTS("CUST","UBP")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","UBP",5)
	#}
	ENDIF
	
	#========================================================
	# Initialize the variables
	#========================================================
	CUST.UBP.errorFlg="N"
	CUST.UBP.errorMsg=" "
	CUST.UBP.requestApp="002"
	CUST.UBP.payment="000001"
	CUST.UBP.particulars=" "
	CUST.UBP.name=" "
	
	IF(FIELDEXISTS(BANCS.INPUT.billerId))THEN
	#{
		IF(TRIM(BANCS.INPUT.billerId) != "")THEN
		#{
			CUST.UBP.billerId=BANCS.INPUT.billerId
			PRINT(CUST.UBP.billerId)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.billRefNum))THEN
	#{
		IF(TRIM(BANCS.INPUT.billRefNum) != "")THEN
		#{
			CUST.UBP.billRefNum=TOUPPER(TRIM(BANCS.INPUT.billRefNum))
			PRINT(CUST.UBP.billRefNum)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.billAmt))THEN
	#{
		IF(TRIM(BANCS.INPUT.billAmt) != "")THEN
		#{
			CUST.UBP.billAmt=BANCS.INPUT.billAmt
			PRINT(CUST.UBP.billAmt)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.billAcctId))THEN
	#{
		IF(TRIM(BANCS.INPUT.billAcctId) != "")THEN
		#{
			CUST.UBP.billAcctId=BANCS.INPUT.billAcctId
			PRINT(CUST.UBP.billAcctId)
		#}
		ENDIF
	#}
	ENDIF
	
	
	
	##########################################
	#Robert added  for Safaricom
	##########################################
	
	
	
	
	CALL("RemmitanceValidation.scr")
	
	
	IF (CUST.UBP.errorFlg=="Y") THEN
	#{
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		#CALL("safThirdPartyDb.scr")
		# GOTO ENDOFSCRIPT
		CALL("ubpdp033.scr")
		#}
		
		#########################
		#  End of Addition
		#########################
		
	#}
	ENDIF
	
	
	
	
	
	
	
	ENDOFSCRIPT:
		
		sv_s = "errorFlg|"+CUST.UBP.errorFlg
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		IF(CUST.UBP.errorMsg=="")THEN
		#{
			CUST.UBP.errorMsg=" "
		#}
		ENDIF
		sv_s = "errorMsg|"+CUST.UBP.errorMsg
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		IF(CUST.UBP.particulars=="")THEN
		#{
			CUST.UBP.particulars=" "
		#}
		ENDIF
		sv_s = "particulars|"+CUST.UBP.particulars
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		IF(CUST.UBP.name=="")THEN
		#{
			CUST.UBP.name=" "
		#}
		ENDIF
		sv_s = "name|"+CUST.UBP.name
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		#TRACE OFF
		EXITSCRIPT
END-->
