##########################################################################################
# Source Name           : cietxndp018.scr
# Date                  : 04-Mar-2013
# Description           : Get Entity Desc
# Author                : Mohan Raghu Arudi
# Modification History  :
# <Serial No.>  <Date>        <Author Name>             <Description>
# ------------  -----------   -----------------         --------------
#      1        04-Mar-2013   Mohan Raghu Arudi         Base Version
###########################################################################################
<--START
CHECKSUM="64f3ac320606d20b569061c0614582430dffe76cb6ddd4c5bc2f5a2589963800"
	TRACE ON
	
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CIECLG Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CIECLG")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CIECLG",5)
	#}
	ENDIF
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	
	CUST.CIECLG.targetEntity = " "
	CUST.CIECLG.errorFlg = "N"
	CUST.CIECLG.errorMsg = " "
	CUST.CIECLG.targetEntityDesc = " "
	
	sv_u = urhk_b2k_printrepos("BANCS")
	print(sv_u)
	
	IF(FIELDEXISTS(BANCS.INPUT.targetEntity))THEN
	#{
		IF(TRIM(BANCS.INPUT.targetEntity) != "")THEN
		#{
			CUST.CIECLG.targetEntity = TOUPPER(TRIM(BANCS.INPUT.targetEntity))
			print(CUST.CIECLG.targetEntity)
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.CIECLG.errorFlg = "Y"
		CUST.CIECLG.errorMsg = "Invalid Input for fnExecuteScript"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_s = "targetEntity,targetEntityDesc|"
	sv_s = sv_s + " SELECT BANK_ID,GROUP_DESC"
	sv_s = sv_s + " FROM TBAADM.GCT "
	sv_s = sv_s + " WHERE BANK_ID = ?SVAR"
	sv_s = sv_s + " AND DEL_FLG != 'Y'"
	print(sv_s)
	
	BANCS.INPARAM.BINDVARS = CUST.CIECLG.targetEntity
	print(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	IF(sv_d == 0)THEN
	#{
		CUST.CIECLG.targetEntity = BANCS.OUTPARAM.targetEntity
		print(CUST.CIECLG.targetEntity)
		CUST.CIECLG.targetEntityDesc = BANCS.OUTPARAM.targetEntityDesc
		print(CUST.CIECLG.targetEntityDesc)
		
		#}
	ELSE
		#{
		CUST.CIECLG.errorFlg = "Y"
		CUST.CIECLG.errorMsg = "Invalid Entity ID"
	#}
	ENDIF
	
	CUST.CIECLG.targetEntityDesc = TOUPPER(CUST.CIECLG.targetEntityDesc)
	
	ENDOFSCRIPT:
		
		sv_u = urhk_SetOrbOut("errorFlg|"+CUST.CIECLG.errorFlg)
		print(sv_u)
		sv_u = urhk_SetOrbOut("errorMsg|"+CUST.CIECLG.errorMsg)
		print(sv_u)
		sv_u = urhk_SetOrbOut("targetEntity|"+CUST.CIECLG.targetEntity)
		print(sv_u)
		sv_u = urhk_SetOrbOut("targetEntityDesc|"+CUST.CIECLG.targetEntityDesc)
		print(sv_u)
		
		
END-->
