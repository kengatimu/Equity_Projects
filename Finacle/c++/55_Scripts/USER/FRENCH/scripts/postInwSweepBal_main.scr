#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: postInwSweepBal_main.scr
# Type: NA|default
# Description: postInwSweepBal_main.scr
# Author:
# Date:
# Modification Log:
#============================================================

#----------------------------------------------------------------------------------------------------------
# Filename              :       postInwSweepBal_main.scr
# Description           :       This is the main script that sweeps the balance from dummy bacid to corresponding account
# Input                 :       None
# Output                :       None
# Called Script         :       None
# Calling Script        :
#
# Modification History
# Sl. #           Date             Author             Modification           Version
# -----           -----           --------          ----------------        ---------
#  1           15-11-2013         Venkatesh         Original Version         1.0
#-----------------------------------------------------------------------------------------------------------
<--START
CHECKSUM="728bcd17e2010522d274d80bdb5789ce56ef6de691991072a62807fc0df37fc7"
	TRACE ON
	
	#----------------------------------------------------------------------
	# Create the necessary repositories and Classes
	#----------------------------------------------------------------------
	IF(REPEXISTS("CUST") == 0 ) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF(CLASSEXISTS("CUST","INTERFACE") == 0) THEN
	#{
		CREATECLASS("CUST","INTERFACE",5)
	#}
	ENDIF
	
	CUST.INTERFACE.successOrFailure = "S"
	CUST.INTERFACE.recCount = "0"
	sv_p = urhk_B2k_PID("")
	CUST.INTERFACE.failLog = "SweepFail_" + BANCS.STDIN.contextBankId + "_" + TRIM(BANCS.OUTPARAM.PID) + ".log"
	CUST.INTERFACE.succLog = "SweepSucc_" + BANCS.STDIN.contextBankId + "_" + TRIM(BANCS.OUTPARAM.PID) + ".log"
	
	#----------------------------------------------------------------------
	# Fetch the Credit accounts for sweep
	#----------------------------------------------------------------------
	BANCS.INPUT.moduleName  = "VERSWEEP"
	BANCS.INPUT.parameterId = "CRACCTLST"
	CALL ("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "") THEN
	#{
		CUST.INTERFACE.crAcctIdList = BANCS.OUTPUT.parameterVal
		PRINT(CUST.INTERFACE.crAcctIdList)
		#}
	ELSE
		#{
		CUST.INTERFACE.errMsg = "No sweeping required for entity id " + BANCS.STDIN.contextBankId
		CUST.INTERFACE.successOrFailure = "S"
		sv_w = SYSTEM("echo " + CUST.INTERFACE.errMsg + ">>" + CUST.INTERFACE.failLog)
		GOTO FETCHNEXTSWEEPREC
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	WHILE(GETPOSITION(CUST.INTERFACE.crAcctIdList,"|") != 0)
	#{
		CUST.INTERFACE.crAcctId = MID$(CUST.INTERFACE.crAcctIdList,0,GETPOSITION(CUST.INTERFACE.crAcctIdList,"|") -1)
		PRINT(CUST.INTERFACE.crAcctId)
		#----------------------------------------------------------------------
		# Fetch the accounts for sweeps
		#----------------------------------------------------------------------
		CALL("postInwSweepBal_FetchAcctForSweep.scr")
		
		#----------------------------------------------------------------------
		# Do Sweeping
		#----------------------------------------------------------------------
		CALL("postInwSweepBal_DoSweepTran.scr")
		
		FETCHNEXTSWEEPREC:
			CUST.INTERFACE.crAcctIdList = MID$(CUST.INTERFACE.crAcctIdList,GETPOSITION(CUST.INTERFACE.crAcctIdList,"|"), STRLEN(CUST.INTERFACE.crAcctIdList))
			PRINT(CUST.INTERFACE.crAcctIdList)
		#}
		DO
		
	ENDOFSCRIPT:
		
		TRACE OFF
END-->
