##########################################################################################
# Source Name           : cparam_module_list.scr
# Date                  : 04-Mar-2013
# Description           :
# Author                : Mohan Raghu Arudi
# Modification History  :
# <Serial No.>  <Date>        <Author Name>             <Description>
# ------------  -----------   -----------------         --------------
#      1        04-Mar-2013   Mohan Raghu Arudi         Base Version
###########################################################################################
# description: TO Delete the parameter value
#=================================================

<--START
CHECKSUM="14bdcca3e6fae8b10b94b1dcad185325164e02bfd545bf77fe664d30450f27d2"
	# Script body goes here
	TRACE ON
	
	print(CUST.CPARAM.funcCode)
	print(CUST.CPARAM.moduleName)
	print(CUST.CPARAM.parameterLevel)
	print(CUST.CPARAM.parameterId)
	print(CUST.CPARAM.schemeCode)
	print(CUST.CPARAM.currencyCode)
	print(CUST.CPARAM.genericCode)
	print(CUST.CPARAM.genericId)
	print(CUST.CPARAM.solId)
	
	print(CUST.CPARAM.parameterId)
	print(CUST.CPARAM.parameterType)
	print(CUST.CPARAM.parameterDesc)
	print(CUST.CPARAM.listType)
	print(CUST.CPARAM.queryName)
	print(CUST.CPARAM.queryId)
	print(CUST.CPARAM.queryDetails)
	print(CUST.CPARAM.parameterValue)
	
	#----------------------------
	#checking for entry in MOD table
	#--------------------------------
	sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
	sv_s = sv_s + " SELECT 1 FROM CUSTOM.C_CPVALUE_MOD"
	sv_s = sv_s + " WHERE"
	sv_s = sv_s + " MODULE_NAME = ?SVAR"
	sv_s = sv_s + " AND PARAMETER_ID = ?SVAR"
	sv_s = sv_s + " AND BANK_ID = ?SVAR"
	sv_s = sv_s + " AND ENTITY_CRE_FLG = 'Y')"
	
	BANCS.INPARAM.BINDVARS = CUST.CPARAM.moduleName + "|" + CUST.CPARAM.parameterId + "|" + BANCS.STDIN.contextBankId
	print(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	IF(sv_d == 0)THEN
	#{
		#ERROR
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		print(sv_u)
		sv_u = urhk_SetOrbOut("Error_1|Error^Unverified Record Exists. Unable to Delete^moduleName")
		print(sv_u)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	#----------------------------------
	
	sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
	sv_s = sv_s + " SELECT 1 FROM CUSTOM.C_CPVALUE"
	sv_s = sv_s + " WHERE"
	sv_s = sv_s + " MODULE_NAME = ?SVAR"
	sv_s = sv_s + " AND PARAMETER_ID = ?SVAR"
	sv_s = sv_s + " AND BANK_ID = ?SVAR"
	sv_s = sv_s + " AND DEL_FLG = 'N'"
	sv_s = sv_s + " AND ENTITY_CRE_FLG = 'Y')"
	
	print(sv_s)
	
	BANCS.INPARAM.BINDVARS = CUST.CPARAM.moduleName + "|" + CUST.CPARAM.parameterId + "|" + BANCS.STDIN.contextBankId
	print(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	IF(sv_d == 0)THEN
	#{
		sv_s = "INSERT INTO CUSTOM.C_CPVALUE_MOD VALUES("
		sv_s = sv_s + "?SVAR,"
		sv_s = sv_s + "?SVAR,"
		sv_s = sv_s + "' ',"
		sv_s = sv_s + "?SVAR,"
		sv_s = sv_s + "'Y',"
		sv_s = sv_s + "'Y',"
		sv_s = sv_s + "?SVAR,"
		sv_s = sv_s + "sysdate,"
		sv_s = sv_s + "?SVAR,"
		sv_s = sv_s + "sysdate,"
		sv_s = sv_s + "?SVAR,"
		sv_s = sv_s + "?SVAR)"
		
		print(sv_s)
		
		BANCS.INPARAM.BINDVARS = CUST.CPARAM.parameterId + "|" + CUST.CPARAM.solId + "|" + CUST.CPARAM.parameterValue + "|" + BANCS.STDIN.userId + "|" + BANCS.STDIN.userId + "|" + BANCS.STDIN.contextBankId + "|" + CUST.CPARAM.moduleName
		print(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbSqlWithBind(sv_s)
		print(sv_d)
		
		IF(sv_d != 0)THEN
		#{
			#ERROR
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			print(sv_u)
			sv_u = urhk_SetOrbOut("Error_1|Error^Database Insertion failed^moduleName")
			print(sv_u)
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			#SUCCESS
			CUST.CPARAM.resultMsg = "Parameter Value of " + CUST.CPARAM.parameterId + " Deleted  Successfully"
			sv_u = urhk_SetOrbOut("RESULT_MSG|"+CUST.CPARAM.resultMsg)
			print(sv_u)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
			print(sv_u)
		#}
		ENDIF
		
		#}
	ELSE
		#{
		#ERROR
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		print(sv_u)
		sv_u = urhk_SetOrbOut("Error_1|Error^Nothing to Delete^moduleName")
		print(sv_u)
		GOTO ENDOFSCRIPT
		
	#}
	ENDIF
	
	
	ENDOFSCRIPT:
END-->
