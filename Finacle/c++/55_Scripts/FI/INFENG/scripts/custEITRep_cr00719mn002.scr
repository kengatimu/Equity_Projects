#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: custEITRep_cr00719mn002.scr
# Type: NA|default
# Description: custEITRep_cr00719mn002.scr
# Author:
# Date:
# Modification Log:
#============================================================

#############################################################################################
# Source Name          : custEITRep_cr00719mn002.scr
# Date                 : 21-10-2012
# Description          : Insert data into c_eit
# Input Values         : NA
# Output Values        : NA
# Called Script        : NA
# Calling Script       : custEITRep_cr00719mn001.com
# Author               : Parikshith V
# Bank                 : EQUITY BANK
# Modification History :
# <Version No.>    <Date>     <Author Name>         <Description>
#   1            21-10-2012   Parikshith V	Original Version
#############################################################################################
<--START
CHECKSUM="61314defa83fcfc8a9d225e40a378f02ac047dbbf14052addf1eb492b59db75b"
	TRACE ON
	#LIBNAME ABGMASTER
	LIBNAME CUSTOMSO
	#-------------------------------------------------------------------------------
	# Creating Repository and Class
	#-------------------------------------------------------------------------------
	sv_r = REPEXISTS("CUST")
	IF (sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	sv_r = CLASSEXISTS("CUST","DATA")
	IF (sv_r ==0) THEN
	#{
		CREATECLASS("CUST","DATA",5)
	#}
	ENDIF
	
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
	sv_s = " BODDate|select to_char(dc_cls_date,'dd-mm-yyyy') from tbaadm.gct  "
	sv_s = sv_s + " WHERE bank_id = ?SVAR "
	sv_u = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_u)
	
	sv_b = BANCS.OUTPARAM.BODDate
	
	CUST.DATA.solId = BANCS.STDIN.mySolId
	PRINT(CUST.DATA.solId)
	
	BANCS.INPARAM.BINDVARS = sv_b +"|"+ CUST.DATA.solId+"|"+ BANCS.STDIN.contextBankId
	sv_s="SELECT NULL FROM custom.c_eit "
	sv_s=sv_s+" WHERE tran_date=to_date(?SVAR,'dd-mm-yyyy')"
	sv_s=sv_s+" AND sol_id=?SVAR"
	sv_s=sv_s+" AND bank_id=?SVAR FOR UPDATE NOWAIT"
	#sv_d=urhk_dbSelectWithBind(sv_s)
	#print(sv_d)
	
	BANCS.INPARAM.BINDVARS = sv_b +"|"+ CUST.DATA.solId+"|"+ BANCS.STDIN.contextBankId
	sv_s="DELETE FROM custom.c_eit"
	sv_s=sv_s+" WHERE tran_date=to_date(?SVAR,'dd-mm-yyyy')"
	sv_s=sv_s+" AND sol_id=?SVAR"
	sv_s=sv_s+" AND bank_id=?SVAR"
	sv_d=urhk_dbSqlWithBind(sv_s)
	PRINT(sv_d)
	
	sv_u = urhk_dbSqlWithBind("COMMIT")
	PRINT(sv_u)
	
	CUST.DATA.comFile = "custEITRep_cr00719mn001.com"
	sv_u = urhk_getFileLocation("COM|" + CUST.DATA.comFile)
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
		CUST.DATA.com = BANCS.OUTPARAM.fileLocation + CUST.DATA.comFile
		PRINT(CUST.DATA.com)
		
		#----------------------------------------------------
		#----------------Command for executing com file------
		#----------------------------------------------------
		sv_a = CUST.DATA.com + " " + BANCS.STDIN.contextBankId + "!" + sv_b + "!" +CUST.DATA.solId
		
		PRINT(sv_a)
		sv_d = SYSTEM(sv_a)
		PRINT(sv_d)
		IF (sv_d == "0") THEN
		#{
			PRINT("Generate Report Successfully")
			BANCS.INPARAM.BINDVARS=""
			sv_u = urhk_dbSqlWithBind("COMMIT")
			PRINT(sv_u)
			#}
		ELSE
			#{
			BANCS.INPARAM.BINDVARS=""
			sv_u = urhk_dbSqlWithBind("ROLLBACK")
			PRINT("RESULT_MSG|Generate Report Failed")
		#}
		ENDIF
		#}
	ELSE
		#{
		PRINT("RESULT_MSG|Generated Report Failed")
	#}
	ENDIF
	
	#=============================================
	# Delete the created Repositories and Classes
	#=============================================
	IF (REPEXISTS("CUST") == 1) THEN
	#{
		DELETEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST", "DATA") == 1) THEN
	#{
		DELETECLASS("CUST", "DATA")
	#}
	ENDIF
	
	TRACE OFF
END-->
