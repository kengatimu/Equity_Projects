<--START
	TRACE ON
	
	####################################################################
	# Check whether CUST Repository exists else create it
	####################################################################
		sv_r = REPEXISTS("CUST")
		IF(sv_r == 0) THEN
		#{
			CREATEREP ("CUST")
		#}
		ENDIF
	
	####################################################################
	# Check whether INTERFACE Class exists else create it
	####################################################################
		sv_r = CLASSEXISTS("CUST","INTERFACE")
		IF(sv_r == 0) THEN
		#{
			CREATECLASS ("CUST","INTERFACE",5)
		#}
		ENDIF
		
	####################################################################
	# CORE ADD
	####################################################################
	
	CALL("FCUserMod_Req.scr")
	CALL("finacleUserAdd_urhk.scr")
	call("FCUserMod_Res.scr")
	IF(CUST.INTERFACE.core_errorMsgFlg == "N")THEN
	#{
		sv_z = "COREMod_RES.Status|SUCCESS"
		sv_u = urhk_SetOrbOut(sv_z)
		PRINT(sv_u)
		
		
		CUST.INTERFACE.CORE_USER_FLG  = "SUCCESS"
		CUST.INTERFACE.CORE_REMARKS  = "User Modified successfully"
	#}
	ELSE
	#{
		sv_z = "COREMod_RES.Status|FAILURE"
		sv_u = urhk_SetOrbOut(sv_z)
		PRINT(sv_u)
		
		sv_z = "COREMod_RES.core_errorDesc |" + CUST.INTERFACE.core_errorDesc 
		sv_u = urhk_SetOrbOut(sv_z)
		PRINT(sv_u)
		
		IF(TRIM(CUST.INTERFACE.core_ErrorSource) != "")THEN
		#{
			sv_z = "COREMod_RES.core_ErrorSource |" + CUST.INTERFACE.core_ErrorSource 
			sv_u = urhk_SetOrbOut(sv_z)
			PRINT(sv_u)
		#}
		ENDIF
		
		CUST.INTERFACE.CORE_USER_FLG  = "FAILURE"
		CUST.INTERFACE.CORE_REMARKS  = CUST.INTERFACE.core_errorDesc 
		#GOTO ENDOFSCRIPT
		CUST.INTERFACE.GOTO = "Y"
	#}
	ENDIF

	
	
	#TRACE OFF
end-->