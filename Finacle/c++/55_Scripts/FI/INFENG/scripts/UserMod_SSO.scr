<--START
	TRACE ON
	
	####################################################################
	# Check whether CUST Repository exists else create it
	####################################################################
		sv_r = REPEXISTS("CUST")
		IF(sv_r == 0) THEN
		#{
			CREATEREP ("CUST")
		#}
		ENDIF
	
	####################################################################
	# Check whether INTERFACE Class exists else create it
	####################################################################
		sv_r = CLASSEXISTS("CUST","INTERFACE")
		IF(sv_r == 0) THEN
		#{
			CREATECLASS ("CUST","INTERFACE",5)
		#}
		ENDIF
		
	####################################################################
	# SSO ADD
	####################################################################	

	CALL("finacleuserMod_TwoFactAuth.scr")
	CALL("finacleUserAdd_urhk.scr")
	CALL("SSOUserMod_Res.scr")
	
	IF(CUST.INTERFACE.sso_errorMsgFlg == "N")THEN
	#{
		
		PRINT(CUST.INTERFACE.sso_respCode )
		sv_z = "SSOMod_RES.Status|SUCCESS"
		sv_u = urhk_SetOrbOut(sv_z)
		PRINT(sv_u)
	
		CUST.INTERFACE.SSO_USER_FLG  = "SUCCESS"
		CUST.INTERFACE.SSO_REMARKS  = CUST.INTERFACE.sso_mesg
	#}
	ELSE
	#{
		sv_a = "SSOMod_RES.Status|ERROR"
		IF((CUST.INTERFACE.sso_errorCode=="") AND (CUST.INTERFACE.sso_errorDesc==""))THEN
		#{
			CUST.INTERFACE.sso_errorCode = "SYSERR"
			CUST.INTERFACE.sso_errorDesc = "Could not get response from server"
		#}
		ENDIF	
		#sv_z = "SSOMod_RES.sso_errorCode|" + CUST.INTERFACE.sso_errorCode
		#sv_u = urhk_SetOrbOut(sv_z)
		#PRINT(sv_u)
		
		sv_z = "SSOMod_RES.sso_errorDesc|" + CUST.INTERFACE.sso_errorDesc 
		sv_u = urhk_SetOrbOut(sv_z)
		PRINT(sv_u)
		CUST.INTERFACE.SSO_USER_FLG  = "FAILURE"
		CUST.INTERFACE.SSO_REMARKS  = CUST.INTERFACE.sso_errorDesc
		#GOTO ENDOFSCRIPT

		CUST.INTERFACE.GOTO = "Y"
	#}
	ENDIF
	


	#TRACE OFF
end-->
