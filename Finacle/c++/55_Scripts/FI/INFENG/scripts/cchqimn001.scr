#--------------------------------------------------------------------------------    
#	Name			: cchqimn001.scr
#	Description		: It validates all the inputs for cheque book maintenance menu
#	Date			: 16-03-2013
#	Author			: GOKULKRISHNA
#	Input			: NA
#	Output			: NA
#	Called Script	:
#	Calling Scrip	: NA
#	Menu Option		: CCHQI
#	Srl. No			Date			Author			Description.
#	-------			---------		-------			---------------
#	1.0				16-03-2013		GOKULKRISHNA			Original Version
#----------------------------------------------------------------------------------

<--START
CHECKSUM="9acd77e4180ed3742fb8db713b3156e0aaac8d54e0b46ea5bce7b2abaac4de28"
	TRACE ON
	#---------------------------------------------------------------------------------
	# CREATING NEW REPOSITORY AND CLASS
	#---------------------------------------------------------------------------------
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST", "DATA") == 0) THEN
	#{
		CREATECLASS("CUST", "DATA", 5)
	#}
	ENDIF
	
	sv_a = urhk_B2k_PrintRepos("BANCS")
	print(sv_a)
	
	PRINT(BANCS.INPUT.solID)
	PRINT(BANCS.INPUT.foracid)
	PRINT(BANCS.INPUT.status)
	
	CUST.DATA.solID = TRIM(BANCS.INPUT.solID)
	PRINT(CUST.DATA.solID)
	CUST.DATA.foracid = TRIM(BANCS.INPUT.foracid)
	PRINT(CUST.DATA.foracid)
	CUST.DATA.status  = TRIM(BANCS.INPUT.status)
	PRINT(CUST.DATA.status)
	CUST.DATA.date  = TRIM(BANCS.INPUT.reqdate_ui)
	PRINT(CUST.DATA.date)
	CUST.DATA.errMsg = ""
	
	
	IF(CUST.DATA.date == "") THEN
	#{
		
		IF((CUST.DATA.foracid != "") AND (CUST.DATA.solID != "ALL") AND (CUST.DATA.solID != ""))THEN
		#{
			print("VALID")
			#}
		ELSE
			#{
			sv_e = "Enter valid Date"
			sv_m = "date"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		#}
	ELSE
		#{
		sv_r = urhk_B2k_valDate(CUST.DATA.date)
		IF (sv_r != 0) THEN
		#{
			sv_e = "Invalid Date  "
			sv_m = "date"
			sv_e = "Error_01|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		
	#}
	ENDIF
	
	IF(CUST.DATA.solID == "ALL") THEN
	#{
		CUST.DATA.solID = "%"
	#}
	ENDIF
	
	#---------------------------------------------------------------------------------
	# Validation of SOL ID
	#---------------------------------------------------------------------------------
	#IF(CUST.DATA.solID != "") THEN
	##{
	#	sv_s = "cnt|Select count(1) from tbaadm.sol where sol_id = ?SVAR "
	#	sv_s = sv_s + " and del_flg = 'N' "
	#	sv_s = sv_s + " and bank_id = ?SVAR "
	#	BANCS.INPARAM.BINDVARS =  CUST.DATA.solID  + "|" + BANCS.STDIN.contextBankId
	#	PRINT(BANCS.INPARAM.BINDVARS)
	#	sv_d = urhk_dbSelectWithBind(sv_s)
	#	IF (sv_d != 0 ) THEN
	#	#{
	#		sv_e = "A Database error has occured. Please contact your DBA."
	#		sv_m = "count"
	#		sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
	#		sv_u = urhk_SetOrbOut(sv_e)
	#		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
	#		GOTO ENDOFSCRIPT
	#    #}
	#    ENDIF
	#
	#    IF(CINT(BANCS.OUTPARAM.cnt) <= 0) THEN
	#	#{
	#		sv_e = "Enter valid sol ID"
	#		sv_m = "solID"
	#		sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
	#		sv_u = urhk_SetOrbOut(sv_e)
	#		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
	#		GOTO ENDOFSCRIPT
	#    #}
	#	ENDIF
	##}
	#ENDIF
	
	IF (CUST.DATA.foracid != "") THEN
	#{
		sv_s = "ValidateAccout|Select count(*) from tbaadm.gam where foracid = ?SVAR "
		IF((BANCS.STDIN.contextBankId == "50"))THEN
		#{
			sv_s = sv_s + " AND schm_type in ('CAA','ODA') "
			#}
		ELSE
			#{
			sv_s = sv_s + " AND schm_type in ('CAA','ODA','SBA') "
		#}
		ENDIF
		sv_s = sv_s + " AND entity_cre_flg = 'Y' AND del_flg !='Y' AND acct_cls_flg !='Y' "
		sv_s = sv_s + " AND bank_id = ?SVAR "
		BANCS.INPARAM.BINDVARS =  CUST.DATA.foracid  + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		IF (sv_d != 0 ) THEN
		#{
			sv_e = "A Database error has occured. Please contact your DBA."
			sv_m = "count"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		IF(CINT(BANCS.OUTPARAM.ValidateAccout) <= 0) THEN
		#{
			IF((BANCS.STDIN.contextBankId == "50"))THEN
			#{
				sv_e = "Incorrect Account No. / Accounts of schm other than CAA,ODA are not allowed"
				#}
			ELSE
				#{
				sv_e = "Incorrect Account No. / Accounts of schm other than SBA,CAA,ODA are not allowed"
			#}
			ENDIF
			
			sv_m = "foracid"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	IF ( CUST.DATA.status != "" ) THEN
	#{
		IF ( (CUST.DATA.status != "E") AND (CUST.DATA.status != "A") AND (CUST.DATA.status != "D") AND (CUST.DATA.status != "S")) THEN
		#{
			sv_e = "Invalid Status [Request-E, Authorised-A, Deleted-D, Processed-S]"
			sv_m = "status"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	IF(CUST.DATA.status == "")THEN
		CUST.DATA.status = "%"
	ENDIF
	IF(CUST.DATA.foracid == "")THEN
		CUST.DATA.foracid = "%"
	ENDIF
	sv_u = urhk_getFileLocation("COM|cchqidp001.com")
	print(sv_u)
	IF (sv_u == 0) THEN
	#{
		#----------------------------------------------------------------------------------
		#---    Setting Env variables
		#----------------------------------------------------------------------------------
		sv_a = "solid|" + CUST.DATA.solID
		sv_u = urhk_putEnvForCallShellScript(sv_a)
		PRINT(sv_u)
		IF (sv_u != 0) THEN
		#{
			CUST.DATA.errMsg = "Error in setting env variable"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		IF(CUST.DATA.date == "")THEN
			CUST.DATA.date = "NA"
		ENDIF
		sv_a = "date|" + CUST.DATA.date
		sv_u = urhk_putEnvForCallShellScript(sv_a)
		PRINT(sv_u)
		IF (sv_u != 0) THEN
		#{
			CUST.DATA.errMsg = "Error in setting env variable"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		sv_a = "foracid|" + CUST.DATA.foracid
		sv_u = urhk_putEnvForCallShellScript(sv_a)
		PRINT(sv_u)
		IF (sv_u != 0) THEN
		#{
			CUST.DATA.errMsg = "Error in setting env variable"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		sv_a = "status|" + CUST.DATA.status
		sv_u = urhk_putEnvForCallShellScript(sv_a)
		PRINT(sv_u)
		IF (sv_u != 0) THEN
		#{
			CUST.DATA.errMsg = "Error in setting env variable"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		sv_a = "bankId|" + BANCS.STDIN.contextBankId
		sv_u = urhk_putEnvForCallShellScript(sv_a)
		PRINT(sv_u)
		IF (sv_u != 0) THEN
		#{
			CUST.DATA.errMsg = "Error in setting env"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		#----------------------------------------------------------------------------------
		#---    Executing com file in background
		#----------------------------------------------------------------------------------
		BANCS.INPARAM.scriptName = "cchqidp001.com"
		BANCS.INPARAM.arguments = ""
		sv_u = urhk_CallShellScript("b")
		print(sv_u)
		IF (sv_u != 0) THEN
		#{
			CUST.DATA.errMsg = "Report generation failed please retry"
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.DATA.errMsg = "Unable to load com file"
	#}
	ENDIF
	
	ENDOFSCRIPT:
		IF (CUST.DATA.errMsg != "" ) THEN
		#{
			sv_z = urhk_SetOrbOut("Error_01|ERROR^"+CUST.DATA.errMsg+"^testMode")
			sv_z = urhk_SetOrbOut("SuccessOrFailure|N")
			#}
		ELSE
			#{
			sv_z = urhk_SetOrbOut("SuccessOrFailure|Y")
			sv_g = "RESULT_MSG|Report generated successfully. Check Report in HPR"
			sv_z = urhk_SetOrbOut(sv_g)
		#}
		ENDIF
		
		EXITSCRIPT
END-->
