<--start
trace on


        IF (REPEXISTS("CUST") == 0) THEN
        #{
                CREATEREP("CUST")
        #}
        ENDIF

        IF (CLASSEXISTS("CUST","DD") == 0) THEN
        #{
                CREATECLASS("CUST","DD",5)
        #}
        ENDIF

	sv_a=urhk_B2k_PrintRepos("BANCS")
	print (sv_a)
	
					#<CancelUserID>DM22645</CancelUserID>
				#<Function>C</Function>	
				#<TranID>5467</TranID>
				#<tranType>T</tranType>
				#<DDissueDate>15-01-2024 00:00:00</DDissueDate>
				#<ValueDate>15-01-2024 00:00:00</ValueDate>
				
		IF( (FIELDEXISTS(("BANCS").("INPUT").("CancelUserID"))== 0) ) THEN
	#{
   		CUST.DD.errMsg="Cancel UserID is not available"
		sv_z = urhk_SetOrbOut("SuccessOrFailure|Failed")
		sv_t = "Msg| " +CUST.DD.errMsg
		sv_u = urhk_SetOrbOut(sv_t)
   		GOTO ENDSCRIPT
	#}
	ELSE
	
		CUST.DD.CancelUserID = BANCS.INPUT.CancelUserID
		PRINT(CUST.DD.CancelUserID)
	
	ENDIF
	
	IF( (FIELDEXISTS(("BANCS").("INPUT").("Function"))== 0) ) THEN
	#{
   		CUST.DD.errMsg="Function is not available"
		sv_z = urhk_SetOrbOut("SuccessOrFailure|Failed")
		sv_t = "Msg| " +CUST.DD.errMsg
		sv_u = urhk_SetOrbOut(sv_t)
   		GOTO ENDSCRIPT
	#}
	ELSE
	
		CUST.DD.Function = BANCS.INPUT.Function
		PRINT(CUST.DD.Function)
	ENDIF
	
	
	IF( (FIELDEXISTS(("BANCS").("INPUT").("TranID"))== 0) ) THEN
	#{
   		CUST.DD.errMsg="TranID Must be provided"
		sv_z = urhk_SetOrbOut("SuccessOrFailure|Failed")
		sv_t = "Msg| " +CUST.DD.errMsg
		sv_u = urhk_SetOrbOut(sv_t)
   		GOTO ENDSCRIPT
	#}
	
	ELSE
	CUST.DD.TranID = BANCS.INPUT.TranID
	PRINT(CUST.DD.TranID)
	ENDIF
	
	
	IF( (FIELDEXISTS(("BANCS").("INPUT").("DDissueDate"))== 0) ) THEN
	#{
   		CUST.DD.errMsg="DDissueDate is not available"
		sv_z = urhk_SetOrbOut("SuccessOrFailure|Failed")
		sv_t = "Msg| " +CUST.DD.errMsg
		sv_u = urhk_SetOrbOut(sv_t)
   		GOTO ENDSCRIPT
	#}
	ELSE
	CUST.DD.DDissueDate = BANCS.INPUT.DDissueDate
	PRINT(CUST.DD.DDissueDate)
	
	ENDIF
	
	
	IF( (FIELDEXISTS(("BANCS").("INPUT").("ValueDate"))== 0) ) THEN
	#{
   		CUST.DD.errMsg="ValueDate is not available"
		sv_z = urhk_SetOrbOut("SuccessOrFailure|Failed")
		sv_t = "Msg| " +CUST.DD.errMsg
		sv_u = urhk_SetOrbOut(sv_t)
   		GOTO ENDSCRIPT
	#}
	ELSE
	CUST.DD.ValueDate = BANCS.INPUT.ValueDate
	PRINT(CUST.DD.ValueDate)
	
	ENDIF



	##########Validating the DD TranID
	
			sv_s = "count|select count(*) from tbaadm.ddc  "
			sv_s = sv_s + "where BANK_ID=?SVAR and trim(tran_id) =?SVAR AND dd_date = TO_DATE( ?SVAR ,'DD-MM-YYYY HH24:MI:SS') "
			sv_s = sv_s + " AND tran_date = TO_DATE( ?SVAR ,'DD-MM-YYYY HH24:MI:SS') AND STAT !='C'"
	
			BANCS.INPARAM.BINDVARS =  BANCS.STDIN.contextBankId + "|" + CUST.DD.TranID + "|" + CUST.DD.DDissueDate + "|" + CUST.DD.ValueDate
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_r =  urhk_dbSelectWithBind(sv_s)
			print(sv_r)
			IF (sv_r ==0) THEN
			#{
				CUST.DD.COUNT= BANCS.OUTPARAM.count
				print (CUST.DD.COUNT)
				#---------------------------------------------------------------------------------------------------------------
				#Fetch demand draft account
				#---------------------------------------------------------------------------------------------------------------
				IF(CUST.DD.COUNT < 1)THEN
				#{
				
				CUST.DD.MSG="Foreign Draft Not Found"
				sv_z = urhk_SetOrbOut("SuccessOrFailure|Failed")
				sv_t = "Msg| " +CUST.DD.MSG
				sv_u = urhk_SetOrbOut(sv_t)
				
				ELSE
				
				
						
						
						
				###Fetching Info for transaction
				
			sv_s = "DD_AMT, DD_CRNCY_CODE, DD_RATE, COMM_AMT_CALC, PUR_ACID|select DD_AMT, DD_CRNCY_CODE, DD_RATE, COMM_AMT_CALC, PUR_ACID from tbaadm.ddc  "
			sv_s = sv_s + "where BANK_ID=?SVAR and trim(tran_id) =?SVAR AND dd_date = TO_DATE( ?SVAR ,'DD-MM-YYYY HH24:MI:SS') "
			sv_s = sv_s + " AND tran_date = TO_DATE( ?SVAR ,'DD-MM-YYYY HH24:MI:SS') AND STAT !='C'"
	
			BANCS.INPARAM.BINDVARS =  BANCS.STDIN.contextBankId + "|" + CUST.DD.TranID + "|" + CUST.DD.DDissueDate + "|" + CUST.DD.ValueDate
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_r =  urhk_dbSelectWithBind(sv_s)
			print(sv_r)
			IF (sv_r ==0) THEN
			#{
				CUST.DD.DD_AMT= BANCS.OUTPARAM.DD_AMT
				print (CUST.DD.DD_AMT)
				
				CUST.DD.DD_CRNCY_CODE= BANCS.OUTPARAM.DD_CRNCY_CODE
				print (CUST.DD.DD_CRNCY_CODE)
				
				CUST.DD.rate= BANCS.OUTPARAM.DD_RATE
				print (CUST.DD.rate)
				
				CUST.DD.COMM_AMT_CALC= BANCS.OUTPARAM.COMM_AMT_CALC
				print (CUST.DD.COMM_AMT_CALC)
				
				CUST.DD.PUR_ACID= BANCS.OUTPARAM.PUR_ACID
				print (CUST.DD.PUR_ACID)
				
				IF(CUST.DD.PUR_ACID !="") THEN
				
				sv_a= urhk_B2k_AcidToForacid(CUST.DD.PUR_ACID)
                if(sv_a==0)then
                #{
                        CUST.DD.PURFORACID = BANCS.OUTPARAM.Foracid
						PRINT(CUST.DD.PURFORACID)
                #}
                else
                #{
                        CUST.DD.PURFORACID = ""
                #}
                endif
				ENDIF 
				
	##########################################MTT######################
	CUST.DD.Date = BANCS.STDIN.BODDate
	CUST.DD.TranParticulars = "DD CANCEL"
	CUST.DD.drAcctNum = "0720197502423"
	#---------------------------------
		# Populate MTT details
		#---------------------------------

			sv_r = urhk_MTTS_PopulateInputDetails("")
			Print(sv_r)

			IF (sv_r != 0) THEN
			#{
				sv_r = urhk_MTTS_ReportErrorCondition("")
				CUST.DATA.errMsg = MTT.Error.Description
				PRINT(CUST.DATA.errMsg)
				EXITSCRIPT
			#}
			ENDIF

		#---------------------------------
		#  Closing of MTT if it is opened
		#---------------------------------

		sv_g = urhk_MTTS_Close("")
		print(sv_g)

		IF (sv_g != 0) THEN
		#{
			sv_r = urhk_MTTS_ReportErrorCondition("")
			CUST.DATA.errMsg = MTT.Error.Description
			PRINT(CUST.DATA.errMsg)
			EXITSCRIPT
		#}
		ENDIF

		#-----------------------------
		#--- Initialize MTT details
		#-----------------------------
					
		BANCS.INPARAM.PostTranFlg               = "Y"
		BANCS.INPARAM.CommitFlg                 = "Y"
		BANCS.INPARAM.InsertTranWithValidnFlg   = "Y"
		BANCS.INPARAM.ValidationFlag            = "N"
		BANCS.INPARAM.RestrictModifyInd         = "FIRST_GROUP"
		BANCS.INPARAM.ErrorCheckForLuw          = "Y"
		BANCS.INPARAM.PartTranLUWFlg            = "Y"
		BANCS.INPARAM.IgnoreExcp        = "I"
		BANCS.INPARAM.CheckExceptions           = "Y"

		sv_o = urhk_MTTS_Initialize("")
		PRINT(sv_o)

		IF(sv_o != 0)THEN
		#{
			sv_o = urhk_MTTS_ReportErrorCondition("")
			CUST.DATA.errMsg = MTT.Error.Description
			PRINT(CUST.DATA.errMsg)
			EXITSCRIPT
		#}
		ENDIF

		#-------------------------------
		# Define the transaction  #HEADER DETAILS
		#-------------------------------

		MTT.TranDetails.TranDate        = "0"
		MTT.TranDetails.TranType        = "T"
		MTT.TranDetails.TranSubType     = "BI"
		MTT.TranDetails.ValidationFlag  = "N"
		MTT.TranDetails.IgnoreExcp      = "I"
		MTT.TranDetails.CommitFlg       = "Y"
		MTT.TranDetails.PostFlag        = "Y"

		sv_r = urhk_MTTS_DefineTransaction("AUTOPOST")
		print(sv_r)

		IF (sv_r != 0) THEN
		#{
			sv_r = urhk_MTTS_ReportErrorCondition("")
			CUST.DATA.errMsg = MTT.Error.Description
			PRINT(CUST.DATA.errMsg)
			EXITSCRIPT
		#}
		ENDIF

		#----------------------------------------------
		#Main Transaction-------------------------------
		#----------------------------------------------

		#--------------------------------------------------------------------------
		#Credit to  account
		#--------------------------------------------------------------------------
		MTT.PartTranDetails.Account = CUST.DD.PURFORACID
		MTT.PartTranDetails.RefAmount = cdouble(CUST.DD.DD_AMT)
		MTT.PartTranDetails.Amount              = cdouble(CUST.DD.DD_AMT)
		MTT.PartTranDetails.TranCurrency        = CUST.DD.DD_CRNCY_CODE
		MTT.PartTranDetails.RefCurrency = CUST.DD.DD_CRNCY_CODE
		MTT.PartTranDetails.ValueDate      = CUST.DD.Date
		MTT.PartTranDetails.Particulars    = CUST.DD.TranParticulars
		MTT.PartTranDetails.Particulars2    = CUST.DD.TranParticulars
		MTT.PartTranDetails.Remarks        = CUST.DD.TranParticulars
		MTT.PartTranDetails.TranIdentifier = "AUTOPOST"
		MTT.PartTranDetails.RateCode            = "TTS"
		MTT.PartTranDetails.Consolidate    = "N"
		MTT.PartTranDetails.Refnum         = CUST.DD.PURFORACID
		MTT.TranDetails.PostFlag           = "Y"
		MTT.TranDetails.IgnoreExcp         = "Y"

		sv_i = urhk_MTTS_DefinePartTran("CR")
		PRINT(sv_i)

		IF (sv_i != 0) THEN
		#{
			sv_r = urhk_MTTS_ReportErrorCondition("")
			CUST.DATA.errMsg = MTT.Error.Description
			PRINT(CUST.DATA.errMsg)
		#}
		ENDIF
			
		#Debit to Account 1
		#-----------------------------------------------------------------------------
		MTT.PartTranDetails.Account        = CUST.DD.drAcctNum 
		MTT.PartTranDetails.RefAmount = -(cdouble(CUST.DD.DD_AMT))
		MTT.PartTranDetails.Amount              = -(cdouble(CUST.DD.DD_AMT))
		MTT.PartTranDetails.TranCurrency        = CUST.DD.DD_CRNCY_CODE
		MTT.PartTranDetails.RefCurrency = CUST.DD.DD_CRNCY_CODE
		MTT.PartTranDetails.ValueDate      = "0"
		MTT.PartTranDetails.Particulars    = CUST.DD.TranParticulars
		MTT.PartTranDetails.Particulars2    = CUST.DD.TranParticulars
		MTT.PartTranDetails.RateCode            = "TTS"
		MTT.PartTranDetails.Remarks        = CUST.DD.TranParticulars
		MTT.PartTranDetails.TranIdentifier = "AUTOPOST"
		MTT.PartTranDetails.Consolidate    = "N"
		MTT.PartTranDetails.Refnum         = CUST.DD.PURFORACID
		MTT.TranDetails.PostFlag           = "Y"
		MTT.TranDetails.IgnoreExcp         = "Y"

		sv_i = urhk_MTTS_DefinePartTran("DR")
		PRINT(sv_i)

		IF (sv_i != 0) THEN
		#{
			sv_r = urhk_MTTS_ReportErrorCondition("")
			CUST.DATA.errMsg = MTT.Error.Description
			PRINT(CUST.DATA.errMsg)
		#}
		ENDIF


		#--------------------------------------------------------------
		# MTT ending definition to create transaction
		#--------------------------------------------------------------

		sv_r = urhk_MTTS_EndDefinition("")
		PRINT(sv_r)

		IF(FIELDEXISTS(MTT.Error.Description)) THEN
		#{
			print(MTT.Error.Description)
		#}
		ENDIF

		CUST.DD.tranId = ""
		IF(FIELDEXISTS(BANCS.OUTPARAM.MTTTranId)) THEN
		#{
			CUST.DD.tranId = TRIM(BANCS.OUTPARAM.MTTTranId)
			PRINT(CUST.DD.tranId)

			
		#}
		ENDIF

		#--------------------------------------------------------------
		# MTT Closing an initialized MTT
		#--------------------------------------------------------------

		sv_r = urhk_MTTS_Close("")
		print(sv_r)
	
				
				
	##########################################MTT######################
	
	########UPDATING DDC
	
	IF(CUST.DD.tranId !="") THEN
	
				
						sv_s = "UPDATE tbaadm.ddc SET STAT='C' "
						sv_s = sv_s + "where BANK_ID=?SVAR and trim(tran_id) =?SVAR AND dd_date = TO_DATE( ?SVAR ,'DD-MM-YYYY HH24:MI:SS') "
						sv_s = sv_s + " AND tran_date = TO_DATE( ?SVAR ,'DD-MM-YYYY HH24:MI:SS') AND STAT !='C'"
				
						BANCS.INPARAM.BINDVARS =  BANCS.STDIN.contextBankId + "|" + CUST.DD.TranID + "|" + CUST.DD.DDissueDate + "|" + CUST.DD.ValueDate
						PRINT(BANCS.INPARAM.BINDVARS)
						sv_r =  urhk_dbSQLWithBind(sv_s)
						print(sv_r)
						IF (sv_r ==0) THEN
						#{
						
							CUST.DD.MSG="Foreign Draft Cancelled Successfully"
							sv_z = urhk_SetOrbOut("SuccessOrFailure|Success")
							sv_t = "Msg| " +CUST.DD.MSG
							sv_u = urhk_SetOrbOut(sv_t)
				
				
						ENDIF
				
				

				ENDIF
				
				
	ENDIF
				
		#{
			ENDIF
			#sv_u = urhk_SetUrhkInp("ddBasicDtl.ddIssDate|15-01-2024 00:00:00")
			#sv_u = urhk_SetUrhkInp("ddBasicDtl.pTranIdentifier.tranId|     5467")
			#sv_u = urhk_SetUrhkInp("ddBasicDtl.ddNum|           79676")
			#sv_u = urhk_SetUrhkInp("ddBasicDtl.pTranIdentifier.partTranSrlNum|  1")
			#sv_u = urhk_SetUrhkInp("ddBasicDtl.tranType.code|T")
			#sv_u = urhk_SetUrhkInp("ddBasicDtl.issBankBr.bankCode|68")
			#sv_u = urhk_SetUrhkInp("ddCancelMiscDtl.nonSysDD|N")
			#sv_u = urhk_SetUrhkInp("ddCancelMiscDtl.createChrgs|N")
			#sv_u = urhk_SetUrhkInp("ddBasicDtl.ddValueDate|15-01-2024 00:00:00")
			#sv_u = urhk_SetUrhkInp("ddBasicDtl.instrmntDtl.instrmntDate|15-01-2024 00:00:00")
			#sv_u = urhk_SetUrhkInp("ddBasicDtl.purCrncy.crncyCode.crncyCode|KES")
			


			
		#}
		


		#sv_u = urhk_ExecSrv("SRV_FetchDDForCancel|retain_all_output = Y|same_user_verify = Y")
		#PRINT(sv_u)
		#IF(sv_u==0)THEN
		#{
           # PRINT("WORKING FOR FETCH")
			
			#sv_u = urhk_CopyOutToIn("")

			#sv_u = urhk_ExecSrv("SRV_CancelDD|retain_all_output = Y|same_user_verify = Y")
			#PRINT(sv_u)
			#IF(sv_u==0)THEN
			#{
				#sv_k ="DD Cancel Success"
				#sv_t = "Msg | " +sv_k
				#sv_u = urhk_SetOrbOut(sv_t)
				
			#ELSE
			#GOTO ERRORCHECK
			#ENDIF
			
		#ELSE
        #GOTO ERRORCHECK
        #ENDIF


GOTO ENDSCRIPT

	ERRORCHECK:		

	#----------------------------------------------------------------------------------------------------
	# SRV Error check
	#----------------------------------------------------------------------------------------------------
	IF (sv_u != 0) THEN
	#{
		IF(FIELDEXISTS(BANCS.OUTPARAM.Error_num) == 1) THEN
		#{
			print(BANCS.OUTPARAM.Error_num)
			sv_v = BANCS.OUTPARAM.Error_num
		#}									
		ELSE
		#{
			sv_v = 0
		#}
		ENDIF
		IF (sv_v > 0) then
		#{
			sv_u = system("echo 'Error '")
			sv_i = 1
			WHILE (sv_i <= sv_v)
			#{
				sv_f = FORMAT$(sv_i,"%d")
				sv_e = ("BANCS").("OUTPARAM").("ErrorMesg_"+sv_f)
				sv_e = sv_e + " " + ("BANCS").("OUTPARAM").("Tag_"+sv_f)
				sv_e = sv_e + " Error Code:" + ("BANCS").("OUTPARAM").("ErrorCode_"+sv_f)
				print(sv_e)
				sv_i = sv_i + 1
				sv_u = system("echo '"+sv_e+"' >>ddadd.log")
				print(sv_u)
				
				#Error response
				sv_t = "Msg | " +sv_e
				sv_u = urhk_SetOrbOut(sv_t)
				GOTO ENDSCRIPT
			#}
			DO
			#}
		ENDIF
	#}
	ENDIF


ENDSCRIPT:

trace off

end-->

