<--START
CHECKSUM="782c198ad4bbd7241cb4c12e30d8cc179c55a304927815a3cf21110d4e1d5ee8"
	TRACE ON
	
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether DENREP Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","DENREP")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","DENREP",5)
	#}
	ENDIF
	
	sv_r = urhk_B2k_PrintRepos("BANCS")
	print(sv_r)
	
	
	########################
	#Initializing variables
	########################
	BANCS.OUTPUT.lv_token_id = ""
        BANCS.OUTPUT.lv_units  = ""
        BANCS.OUTPUT.lv_Fuel  = ""
        BANCS.OUTPUT.lv_FxAdj  = ""
        BANCS.OUTPUT.lv_Inflation  = ""
	
	IF(BANCS.STDIN.onlineOrBatch == "O") THEN
	#{
		print(BANCS.INPUT.tran_id)
		sv_a = "FREETEXT1,FREETEXT2,FREETEXT4,FREETEXT5,FREETEXT6 | select FREETEXT1,FREETEXT2,FREETEXT4,FREETEXT5,FREETEXT6 from custom.c_ubp_details"
		sv_a = sv_a + "  where tran_id = ?SVAR AND tran_date = to_date(?SVAR,'DD-MM-YYYY HH24:MI:SS') "
		print(sv_a)
		BANCS.INPARAM.BINDVARS = BANCS.INPUT.tran_id + "|" + BANCS.STDIN.BODDate
		print(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_a)
		print(sv_d)
		
		IF(sv_d == 0)THEN
		#{
			BANCS.OUTPUT.lv_token_id  = BANCS.OUTPARAM.FREETEXT1
			BANCS.OUTPUT.lv_units  = BANCS.OUTPARAM.FREETEXT2
			BANCS.OUTPUT.lv_Fuel  = BANCS.OUTPARAM.FREETEXT4
			BANCS.OUTPUT.lv_FxAdj  = BANCS.OUTPARAM.FREETEXT5
			BANCS.OUTPUT.lv_Inflation  = BANCS.OUTPARAM.FREETEXT6
			
		#}
		ENDIF
		
	#}
	ENDIF
	
	TRACE OFF
END-->
