#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: wthmn036.scr
# Type: NA|default
# Description: wthmn036.scr
# Author:
# Date:
# Modification Log:
#============================================================

###################################################################################################################################################
#Script Name    : wthmn036.scr
#Start Date     : 20-June-2013
#Description    : This script is called on click of submit button on details page for menus VISAO,VICAO,VITUAO,VCMIDAO
#				  MILA and MITUA  for Verify mode
#				  And this script will capture the value for custom field "Withholding Tax Applicable" on scheme tab  into custom table.
#Input para     : None
#Output para    : None
#Author         : Nitin_Kumar16
#Called Script  : NA
#Calling Script : SRV_VerifyCAAcct_post_process_data.scr,SRV_VerifySBAcct_post_process_data.scr
#				  SRV_VerifyTUAcct_post_process_data.scr
#Reference      : None
#Modification History:
#Srl No.  		Date         		Author          		Description
#-------  		-----------  		---------   			--------------
#   1.0   		20-June-2013 		Nitin_Kumar16      		Original version
###################################################################################################################################################
<--START
CHECKSUM="adc15f6e7378492a5e4b1bc95682465f3e22c24ce700e32284008f076cb86223"
	TRACE ON
	#--------------------------------------------------------------------------
	# Create Repos and Classes
	#--------------------------------------------------------------------------
	sv_r = REPEXISTS("CUST")
	IF (sv_r == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	sv_r = CLASSEXISTS("CUST", "WITHTAX")
	IF (sv_r == 0) THEN
	#{
		CREATECLASS("CUST", "WITHTAX", 5)
	#}
	ENDIF
	
	CUST.WITHTAX.withdTaxAppFlg 		= ""
	CUST.WITHTAX.schmCode 				= ""
	CUST.WITHTAX.schmType				= ""
	CUST.WITHTAX.foracid				= ""
	CUST.WITHTAX.delFlg 				= "N"
	CUST.WITHTAX.entityFlg 			 	= "Y"
	
	IF(FIELDEXISTS(BANCS.STDIN.menuId) == "0") THEN
	#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	IF((BANCS.STDIN.menuId != "VISAO") AND (BANCS.STDIN.menuId != "VICAO") AND (BANCS.STDIN.menuId != "VITUAO") AND (BANCS.STDIN.menuId != "VCMIDAO"))THEN
	#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	#IF((BANCS.STDIN.menuId == "MILA") OR (BANCS.STDIN.menuId == "MITUA")) THEN
	#{
	#	GOTO ENDOFSCRIPT
	#}
	#ENDIF
	
	IF((BANCS.STDIN.menuId == "VITUAO") OR (BANCS.STDIN.menuId == "VCMIDAO")) THEN
	#{
		sv_u = urhk_SRV_GetVal("acctCommonInfo.acctGeneralInfo.acctBasic.foracid")
		CUST.WITHTAX.foracid = BANCS.OUTPARAM.srvValue
		PRINT(CUST.WITHTAX.foracid)
	#}
	ENDIF
	IF((BANCS.STDIN.menuId == "VICAO") OR (BANCS.STDIN.menuId == "VISAO")) THEN
	#{
		sv_u = urhk_SRV_GetVal("acctCommonInfo.acctGeneralInfo.acctBasic.foracid")
		CUST.WITHTAX.foracid = BANCS.OUTPARAM.srvValue
		PRINT(CUST.WITHTAX.foracid)
	#}
	ENDIF
	PRINT(CUST.WITHTAX.foracid)
	
	BANCS.INPARAM.BINDVARS =  CUST.WITHTAX.entityFlg +"|"+ BANCS.STDIN.userId +"|"+ CUST.WITHTAX.foracid+ "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_s = "UPDATE CUSTOM.C_WTHAO SET ENTITY_CRE_FLG = ?SVAR,LCHG_USER_ID = ?SVAR,LCHG_TIME = SYSDATE WHERE FORACID = ?SVAR AND BANK_ID = ?SVAR"
	PRINT(sv_s)
	sv_d = urhk_dbSqlWithBind(sv_s)
	PRINT(sv_d)
	IF (sv_d != 0) THEN
	#{
		sv_p = urhk_SRV_SetErr("Unable to Verify the record")
		BANCS.OUTPUT.SuccessOrFailure = "F"
		sv_p = urhk_SetOrbOut("SuccessOrFailure|N")
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	
	ENDOFSCRIPT:
		#Trace off
		
END-->
