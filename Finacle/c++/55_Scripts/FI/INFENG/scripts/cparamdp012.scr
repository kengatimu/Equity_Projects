##########################################################################################
# Source Name           : cparam_module_list.scr
# Date                  : 04-Mar-2013
# Description           :
# Author                : Mohan Raghu Arudi
# Modification History  :
# <Serial No.>  <Date>        <Author Name>             <Description>
# ------------  -----------   -----------------         --------------
#      1        04-Mar-2013   Mohan Raghu Arudi         Base Version
###########################################################################################
#Modify New Parameter Submit
<--START
CHECKSUM="dcb3373aeb15df293de7bb630ce805812bd2421958816fab4ae1ec337659f8f8"
	TRACE ON
	
	print(CUST.CPARAM.funcCode)
	print(CUST.CPARAM.moduleName)
	print(CUST.CPARAM.parameterLevel)
	print(CUST.CPARAM.parameterId)
	print(CUST.CPARAM.schemeCode)
	print(CUST.CPARAM.currencyCode)
	print(CUST.CPARAM.genericCode)
	print(CUST.CPARAM.genericId)
	print(CUST.CPARAM.solId)
	
	print(CUST.CPARAM.parameterId)
	print(CUST.CPARAM.parameterType)
	print(CUST.CPARAM.parameterDesc)
	print(CUST.CPARAM.listType)
	print(CUST.CPARAM.queryName)
	print(CUST.CPARAM.queryId)
	print(CUST.CPARAM.queryDetails)
	
	sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
	sv_s = sv_s + " SELECT 1 FROM CUSTOM.C_CPMASTER"
	sv_s = sv_s + " WHERE"
	sv_s = sv_s + " MODULE_NAME = ?SVAR"
	sv_s = sv_s + " AND PARAMETER_LEVEL = ?SVAR"
	sv_s = sv_s + " AND PARAMETER_ID = ?SVAR"
	sv_s = sv_s + " AND PARAMETER_TYPE = ?SVAR"
	sv_s = sv_s + " AND LIST_TYPE = ?SVAR"
	sv_s = sv_s + " AND BANK_ID = ?SVAR"
	sv_s = sv_s + " AND DEL_FLG = 'N'"
	sv_s = sv_s + " AND ENTITY_CRE_FLG = 'N')"
	
	print(sv_s)
	
	BANCS.INPARAM.BINDVARS = CUST.CPARAM.moduleName + "|" + CUST.CPARAM.parameterLevel + "|" + CUST.CPARAM.parameterId + "|" + CUST.CPARAM.parameterType + "|" + CUST.CPARAM.listType + "|" + BANCS.STDIN.contextBankId
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	IF(sv_d == 0)THEN
	#{
		sv_s = "UPDATE CUSTOM.C_CPMASTER SET"
		sv_s = sv_s + " PARAMETER_DESC = ?SVAR,"
		sv_s = sv_s + " LCHG_USER_ID = ?SVAR,"
		sv_s = sv_s + " LCHG_TIME = sysdate"
		sv_s = sv_s + " WHERE"
		sv_s = sv_s + " MODULE_NAME = ?SVAR"
		sv_s = sv_s + " AND PARAMETER_LEVEL = ?SVAR"
		sv_s = sv_s + " AND PARAMETER_ID = ?SVAR"
		sv_s = sv_s + " AND PARAMETER_TYPE = ?SVAR"
		sv_s = sv_s + " AND LIST_TYPE = ?SVAR"
		sv_s = sv_s + " AND BANK_ID = ?SVAR"
		sv_s = sv_s + " AND DEL_FLG = 'N'"
		
		print(sv_s)
		
		BANCS.INPARAM.BINDVARS = CUST.CPARAM.parameterDesc + "|" + BANCS.STDIN.userId + "|" + CUST.CPARAM.moduleName + "|" + CUST.CPARAM.parameterLevel + "|" + CUST.CPARAM.parameterId + "|" + CUST.CPARAM.parameterType + "|" + CUST.CPARAM.listType + "|" + BANCS.STDIN.contextBankId
		
		sv_d = urhk_dbSqlWithBind(sv_s)
		print(sv_d)
		
		IF(sv_d != 0)THEN
		#{
			#ERROR
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			print(sv_u)
			sv_u = urhk_SetOrbOut("Error_1|Error^Database Updation Failed^moduleName")
			print(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		#}
	ELSE
		#{
		sv_s = "recCount|SELECT 1 FROM DUAL WHERE EXISTS("
		sv_s = sv_s + " SELECT 1 FROM CUSTOM.C_CPMASTER_MOD"
		sv_s = sv_s + " WHERE"
		sv_s = sv_s + " MODULE_NAME = ?SVAR"
		sv_s = sv_s + " AND PARAMETER_LEVEL = ?SVAR"
		sv_s = sv_s + " AND PARAMETER_ID = ?SVAR"
		sv_s = sv_s + " AND PARAMETER_TYPE = ?SVAR"
		sv_s = sv_s + " AND LIST_TYPE = ?SVAR"
		sv_s = sv_s + " AND BANK_ID = ?SVAR"
		sv_s = sv_s + " AND DEL_FLG = 'N'"
		sv_s = sv_s + " AND ENTITY_CRE_FLG = 'Y')"
		
		print(sv_s)
		
		BANCS.INPARAM.BINDVARS = CUST.CPARAM.moduleName + "|" + CUST.CPARAM.parameterLevel + "|" + CUST.CPARAM.parameterId + "|" + CUST.CPARAM.parameterType + "|" + CUST.CPARAM.listType + "|" + BANCS.STDIN.contextBankId
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		
		IF(sv_d == 0)THEN
		#{
			sv_s = "UPDATE CUSTOM.C_CPMASTER_MOD SET"
			sv_s = sv_s + " PARAMETER_DESC = ?SVAR,"
			sv_s = sv_s + " LCHG_USER_ID = ?SVAR,"
			sv_s = sv_s + " LCHG_TIME = sysdate"
			sv_s = sv_s + " WHERE"
			sv_s = sv_s + " MODULE_NAME = ?SVAR"
			sv_s = sv_s + " AND PARAMETER_LEVEL = ?SVAR"
			sv_s = sv_s + " AND PARAMETER_ID = ?SVAR"
			sv_s = sv_s + " AND PARAMETER_TYPE = ?SVAR"
			sv_s = sv_s + " AND LIST_TYPE = ?SVAR"
			sv_s = sv_s + " AND BANK_ID = ?SVAR"
			sv_s = sv_s + " AND DEL_FLG = 'N'"
			
			print(sv_s)
			
			BANCS.INPARAM.BINDVARS = CUST.CPARAM.parameterDesc + "|" + BANCS.STDIN.userId + "|" + CUST.CPARAM.moduleName + "|" + CUST.CPARAM.parameterLevel + "|" + CUST.CPARAM.parameterId + "|" + CUST.CPARAM.parameterType + "|" + CUST.CPARAM.listType + "|" + BANCS.STDIN.contextBankId
			
			sv_d = urhk_dbSqlWithBind(sv_s)
			print(sv_d)
			
			IF(sv_d != 0)THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				print(sv_u)
				sv_u = urhk_SetOrbOut("Error_1|Error^Database Updation Failed^moduleName")
				print(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			
			#}
		ELSE
			#{
			sv_s = "INSERT INTO CUSTOM.C_CPMASTER_MOD VALUES("
			sv_s = sv_s + "?SVAR,"
			sv_s = sv_s + "?SVAR,"
			sv_s = sv_s + "?SVAR,"
			sv_s = sv_s + "?SVAR,"
			sv_s = sv_s + "?SVAR,"
			sv_s = sv_s + "?SVAR,"
			sv_s = sv_s + "?SVAR,"
			sv_s = sv_s + "'Y',"
			sv_s = sv_s + "'N',"
			sv_s = sv_s + "?SVAR,"
			sv_s = sv_s + "sysdate,"
			sv_s = sv_s + "?SVAR,"
			sv_s = sv_s + "sysdate,"
			sv_s = sv_s + "?SVAR)"
			
			print(sv_s)
			
			BANCS.INPARAM.BINDVARS = CUST.CPARAM.moduleName + "|" + CUST.CPARAM.parameterLevel + "|" + CUST.CPARAM.parameterId + "|" + CUST.CPARAM.parameterDesc + "|" + CUST.CPARAM.parameterType + "|" + CUST.CPARAM.queryId + "|" + CUST.CPARAM.listType + "|" + BANCS.STDIN.userId + "|" + BANCS.STDIN.userId + "|" + BANCS.STDIN.contextBankId
			
			sv_d = urhk_dbSqlWithBind(sv_s)
			print(sv_d)
			
			IF(sv_d != 0)THEN
			#{
				#ERROR
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				print(sv_u)
				sv_u = urhk_SetOrbOut("Error_1|Error^Database Insertion failed^moduleName")
				print(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			
		#}
		ENDIF
		
	#}
	ENDIF
	
	CUST.CPARAM.resultMsg = "Parameter " + CUST.CPARAM.parameterId + " Modified Successfully"
	sv_u = urhk_SetOrbOut("RESULT_MSG|"+CUST.CPARAM.resultMsg)
	print(sv_u)
	sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
	print(sv_u)
	
	ENDOFSCRIPT:
END-->
