#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: denomdp048.scr
# event: NA
# description: q
#=================================================

###############################################################################
#    Name			:   denomdp048.scr
#    Date			:   18-02-2008
#    Description		:  script for teller listing
#
#    Author			:  Mandar
#    Called From		:   HDENRO
#    Calling Script		:
#    Modification History	:
################################################################################

<--START
CHECKSUM="444b1cb27f9b8e970ea48b450f9294fb75ba788abcb27dcc92bcc522be64a8fd"
	TRACE ON
	
	#-------------------------------------------------------------------------------
	#	Creating Repository and Class
	#-------------------------------------------------------------------------------
	sv_r = REPEXISTS("CUST")
	IF (sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	sv_r = CLASSEXISTS("CUST","DENOM")
	IF (sv_r ==0) THEN
	#{
		CREATECLASS("CUST","DENOM",5)
	#}
	ENDIF
	
	sv_u=urhk_b2k_PrintRepos("BANCS")
	print (sv_u)
	
	CUST.DENOM.funcCode =BANCS.INPUT.funcCode
	print(CUST.DENOM.funcCode)
	
	sv_q = "cnt|SELECT distinct teller_id  from C_DENM"
	
	
	print(sv_q)
	sv_b = urhk_dbCursorOpenWithBind(sv_q)
	print(sv_b)
	IF (sv_b == 0) THEN
	#{
		CUST.DENOM.cursorNumber = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		#}
	ELSE
		#{
		GOTO FINISH
	#}
	ENDIF
	
	print(CUST.DENOM.cursorNumber)
	sv_f = urhk_dbCursorFetch(CUST.DENOM.cursorNumber)
	sv_z = 1
	sv_a=0
	WHILE(sv_f == 0)
	#{
		
		sv_a = sv_a + 1
		print(sv_a)
		sv_f = urhk_dbCursorFetch(CUST.DENOM.cursorNumber)
	#}
	DO
	
	###########################################################
	# Cursor Closure
	###########################################################
	
	sv_f = urhk_dbCursorClose(CUST.DENOM.cursorNumber)
	print(sv_f)
	
	print(sv_a)
	IF(sv_a ==0)THEN
	#{
		CUST.DENOM.COUNT  = FORMAT$(sv_a,"%d")
		print(CUST.DENOM.COUNT)
		#}
	ELSE
		#{
		CUST.DENOM.COUNT  = sv_a
		print(CUST.DENOM.COUNT)
	#}
	ENDIF
	
	
	
	
	
	
	#-----------------------------------------------------------------------
	# Getting the number of count for setting the number of rows in frontend
	#-----------------------------------------------------------------------
	
	
	#--- 1st output from a listing script should be the count of the total records that will be listed out --> recCount
	sv_u    =   urhk_SetOrbOut("recCount|"+CUST.DENOM.COUNT)
	
	
	sv_q = "Tier_Code|SELECT distinct teller_id from C_DENM"
	
	
	print(sv_q)
	sv_b = urhk_dbCursorOpenWithBind(sv_q)
	print(sv_b)
	IF (sv_b == 0) THEN
	#{
		CUST.DENOM.cursorNumber = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		#}
	ELSE
		#{
		GOTO FINISH
	#}
	ENDIF
	
	print(CUST.DENOM.cursorNumber)
	sv_f = urhk_dbCursorFetch(CUST.DENOM.cursorNumber)
	sv_z = 1
	
	WHILE(sv_f == 0)
	#{
		CUST.DENOM.Tier_Code = BANCS.OUTPARAM.Tier_Code
		print(CUST.DENOM.Tier_Code)
		CUST.DENOM.Tier_Code =trim(CUST.DENOM.Tier_Code)
		sv_q = "setId|select distinct sol_id from C_DENM where teller_id = '"+CUST.DENOM.Tier_Code+"'"
		sv_q = sv_q + " AND rownum<2"
		print(sv_q)
		
		sv_s = urhk_dbSelectWithBind(sv_q)
		CUST.DENOM.baseint = BANCS.OUTPARAM.setId
		
		#CUST.DENOM.DENOM_Prefix = BANCS.OUTPARAM.DENOM_Prefix
		
		sv_y = format$(sv_z,"%d")
		
		#--- Followed by record count, the outputs should be as name value pairs with name as
		#--- <outputNames>_<rec_no> defined as 2nd paramenter in fnExecuteScriptForList()
		
		CUST.DENOM.Tier_Code = "Tier_Code_"+sv_y+"|"+CUST.DENOM.Tier_Code
		print(CUST.DENOM.Tier_Code)
		sv_u = urhk_SetOrbOut(CUST.DENOM.Tier_Code)
		print(sv_u)
		
		CUST.DENOM.baseint = "baseint_"+sv_y+"|"+CUST.DENOM.baseint
		print(CUST.DENOM.baseint)
		sv_u = urhk_SetOrbOut(CUST.DENOM.baseint)
		print(sv_u)
		#CUST.DENOM.Limit_Prefix = "Limit_Prefix_"+sv_y+"|"+CUST.DENOM.Limit_Prefix
		#sv_u = urhk_SetOrbOut(CUST.DENOM.Limit_Prefix)
		print(sv_u)
		
		sv_z = sv_z + 1
		print(sv_z)
		sv_f = urhk_dbCursorFetch(CUST.DENOM.cursorNumber)
	#}
	DO
	
	###########################################################
	# Cursor Closure
	###########################################################
	
	sv_f = urhk_dbCursorClose(CUST.DENOM.cursorNumber)
	print(sv_f)
	
	
	
	FINISH:
		exitscript
		TRACE OFF
END-->
