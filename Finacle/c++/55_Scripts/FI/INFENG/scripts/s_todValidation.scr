#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: s_tod_Validation.scr
# event: NA
# description: NA
#   Srl. No         Date                Author                  Description.
#   -------         ------              ------                  ------------
#   1.0             16-10-2013          Vijaya V                 For TO 379353
#=================================================

<--START
CHECKSUM="50b523e27f79dd5beae2433322755534dd5666404df2c2de7c6ce75c60d93e23"
	TRACE ON
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST","TODV") == 0) THEN
	#{
		CREATECLASS("CUST","TODV",5)
	#}
	ENDIF
	
	#-------------------------------------------------------------------------------------------
	# Fetch Foracid
	#-------------------------------------------------------------------------------------------
	sv_u = urhk_SRV_GetVal("criteria.acct.foracid")
	PRINT(sv_u)
	
	IF (sv_u == 0) THEN
	#{
		CUST.TODV.foracid = BANCS.OUTPARAM.srvValue
		PRINT(CUST.TODV.foracid)
		#}
	ELSE
		#{
		CUST.TODV.errMsg = "Error: urhk_SRV_GetVal failed for acid"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	#-------------------------------------------------------------------------------------------
	# Acctount status
	#-------------------------------------------------------------------------------------------
	
	sv_s = " acctStatus|select ACCT_STATUS from SMT where acid in  "
	sv_s = sv_s + "  ( select acid from gam where foracid =?SVAR "
	sv_s = sv_s + "  AND bank_id = ?SVAR AND del_flg ='N' AND entity_cre_flg ='Y')  "
	sv_s = sv_s + "  AND bank_id = ?SVAR "
	
	BANCS.INPARAM.BINDVARS = CUST.TODV.foracid + "|" + BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.contextBankId
	
	sv_u = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_u)
	
	IF (sv_u == 0) THEN
	#{
		CUST.TODV.acctStatus = BANCS.OUTPARAM.acctStatus
		PRINT(CUST.TODV.acctStatus)
		#}
	ELSE
		#{
		sv_s = " acctStatus|select ACCT_STATUS from CAM where acid in  "
		sv_s = sv_s + "  ( select acid from gam where foracid =?SVAR "
		sv_s = sv_s + "  AND bank_id = ?SVAR AND del_flg ='N' AND entity_cre_flg ='Y')  "
		sv_s = sv_s + "  AND bank_id = ?SVAR "
		
		BANCS.INPARAM.BINDVARS = CUST.TODV.foracid + "|" + BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.contextBankId
		
		sv_u = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_u)
		
		IF (sv_u == 0) THEN
		#{
			CUST.TODV.acctStatus = BANCS.OUTPARAM.acctStatus
			PRINT(CUST.TODV.acctStatus)
		#}
		ENDIF
		
		CUST.TODV.acctStatus = ""
		print(CUST.TODV.acctStatus)
	#}
	ENDIF
	
	
	IF (CUST.TODV.acctStatus == "D") THEN
	#{
		sv_e = "TOD cannot be granted ON a Dormant Account."
		sv_u = urhk_SRV_SetErr(sv_e)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	ENDOFSCRIPT:
		TRACE OFF
		
END-->
