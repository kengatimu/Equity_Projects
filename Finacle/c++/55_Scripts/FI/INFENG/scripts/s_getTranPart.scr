#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: s_getTranPart.scr
# Type: NA|default
# Description: s_getTranPart.scr
# Author:
# Date:
# Modification Log:
#============================================================

#-------------------------------------------------------------------------------------------------------------
#    Name            : s_getTranPart.scr
#    Description     : This script is used to populate tran particulars for menu HPORDM TO Id:369948.
#    Date            : 29-08-2013
#    Author          : Priyansh
#    Input           : NA
#    Output              : NA
#    Called Script   : NA
#    Calling Script  : SRV_PostAnEnteredTransaction_post_process_data.scr
#    Menu Option     : HPORDM
#    Srl. No            Date                    Author                  Description.
#    ------                     ----------              -------                 ---------------
#    1.0                        29-08-2013              Priyansh                 Original Version
#    2.0                        26-08-2014              jane                 change narration
#-------------------------------------------------------------------------------------------------------------
<--START
CHECKSUM="1a518d3e3d374c3f2b1f08c8bca349a3c80c3446d379fa70d555a5fb42c4e240"
	TRACE ON
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST","PORD") == 0) THEN
	#{
		CREATECLASS("CUST","PORD",5)
	#}
	ENDIF
	
	sv_q=urhk_B2k_PrintRepos("BANCS")
	PRINT(sv_q)
	
	sv_u = urhk_SRV_NumOfRecs("tranDetail.partTranDetailLL")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
		CUST.PORD.noOfRecords = CINT(BANCS.OUTPARAM.srvValue)
		PRINT(CUST.PORD.noOfRecords)
		#}
	ELSE
		#{
		CUST.PORD.errMsg = "Error in getting record count"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_l = 0
	PRINT(sv_l)
	WHILE (sv_l < CINT(CUST.PORD.noOfRecords))
	#{
		sv_r = "<rec_" + Format$(sv_l,"%d") + ">"
		PRINT(sv_r)
		
		sv_u=urhk_SRV_GetVal("tranDetail.partTranDetailLL." + sv_r + ".tranParticulars")
		PRINT(sv_u)
		CUST.PORD.tranPart=BANCS.OUTPARAM.srvValue
		PRINT(CUST.PORD.tranPart)
		
		sv_u=urhk_SRV_GetVal("tranDetail.partTranDetailLL." + sv_r + ".pTranIdentifier.tranDate")
		PRINT(sv_u)
		CUST.PORD.tranDate=BANCS.OUTPARAM.srvValue
		PRINT(CUST.PORD.tranDate)
		
		sv_u=urhk_SRV_GetVal("tranDetail.partTranDetailLL." + sv_r + ".pTranIdentifier.tranId")
		PRINT(sv_u)
		CUST.PORD.tranId=BANCS.OUTPARAM.srvValue
		PRINT(CUST.PORD.tranId)
		
		sv_u=urhk_SRV_GetVal("tranDetail.remarks")
		PRINT(sv_u)
		CUST.PORD.tranRmks=BANCS.OUTPARAM.srvValue
		PRINT(CUST.PORD.tranRmks)
		
		sv_u=urhk_SRV_GetVal("tranDetail.partTranDetailLL." + sv_r + ".pTranIdentifier.partTranSrlNum")
		PRINT(sv_u)
		CUST.PORD.partTranSrl=BANCS.OUTPARAM.srvValue
		PRINT(CUST.PORD.partTranSrl)
		
		sv_u = "freetext,name,no|select FREE_TEXT1,BENEF_PARTY_NAME,FREE_TEXT from tbaadm.pord where bank_id=?SVAR AND PYMT_REF_NUM=?SVAR"
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + (RIGHT$(CUST.PORD.tranPart,12))
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_r =  urhk_dbSelectWithBind(sv_u)
		PRINT(sv_r)
		IF (sv_r ==0) THEN
		#{
			CUST.PORD.freetext = TRIM(BANCS.OUTPARAM.freetext)
			PRINT (CUST.PORD.freetext)
			CUST.PORD.name = TRIM(BANCS.OUTPARAM.name)
			PRINT (CUST.PORD.name)
			CUST.PORD.no = TRIM(BANCS.OUTPARAM.no)
			PRINT (CUST.PORD.no)
			#}
		ELSE
			#{
			CUST.PORD.freetext = " "
			CUST.PORD.name = " "
			CUST.PORD.no = " "
		#}
		ENDIF
		
		IF(MID$(CUST.PORD.tranRmks,0,4) == "SWIF")THEN
		#{
			IF(MID$(CUST.PORD.tranPart,0,7) == "Charges")THEN
			#{
				#CUST.PORD.tranParticular = "remmittance charges RTGS/SWIFT no: " + RIGHT$(CUST.PORD.tranRmks,12)
				CUST.PORD.tranParticular = "remmittance charges RTGS/SWIFT no: " + CUST.PORD.no
				PRINT(CUST.PORD.tranParticular)
				#}
			ELSE
				#{
				IF(CUST.PORD.freetext == "")THEN
				#{
					
					#CUST.PORD.tranParticular = (MID$(CUST.PORD.tranPart,0,7)) + " SWIFT OUT NO: " + (RIGHT$(CUST.PORD.tranPart,12))
					CUST.PORD.tranParticular ="SWFT:" + CUST.PORD.no + " "+ CUST.PORD.name
					PRINT(CUST.PORD.tranParticular)
					#}
				ELSE
					#{
					#CUST.PORD.tranParticular = (MID$(CUST.PORD.tranPart,0,7)) + " SWIFT OUT NO: " + (RIGHT$(CUST.PORD.tranPart,12) + ":" + CUST.PORD.freetext + CUST.PORD.name)
					#CUST.PORD.tranParticular ="SWIFT NO:" + (RIGHT$(CUST.PORD.tranPart,12) + ":" + CUST.PORD.freetext + CUST.PORD.name)
					CUST.PORD.tranParticular ="FRGN/RMT:" + (RIGHT$(CUST.PORD.tranPart,12) + ":" + CUST.PORD.freetext + CUST.PORD.name)
					PRINT(CUST.PORD.tranParticular)
					
				#}
				ENDIF
			#}
			ENDIF
		#}
		ENDIF
		IF(MID$(CUST.PORD.tranRmks,0,4) == "RTGS")THEN
		#{
			IF(MID$(CUST.PORD.tranPart,0,7) == "Charges")THEN
			#{
				#CUST.PORD.tranParticular = "remmittance charges RTGS/SWIFT no: " + RIGHT$(CUST.PORD.tranRmks,12)
				CUST.PORD.tranParticular = "remmittance charges RTGS/SWIFT no: " + CUST.PORD.no
				PRINT(CUST.PORD.tranParticular)
				#}
			ELSE
				#{
				IF(CUST.PORD.freetext== "")THEN
				#{
					#CUST.PORD.tranParticular = (MID$(CUST.PORD.tranPart,0,7)) + " RTGS OUT NO: " + (RIGHT$(CUST.PORD.tranPart,12))
					CUST.PORD.tranParticular ="RTGS:" + CUST.PORD.no + " " + CUST.PORD.name
					PRINT(CUST.PORD.tranParticular)
					#}
				ELSE
					#{
					#CUST.PORD.tranParticular = (MID$(CUST.PORD.tranPart,0,7)) + " RTGS OUT NO: " + (RIGHT$(CUST.PORD.tranPart,12)  + ":" + CUST.PORD.freetext + CUST.PORD.name)
					#CUST.PORD.tranParticular ="RTGS NO:" + (RIGHT$(CUST.PORD.tranPart,12)  + ":" + CUST.PORD.freetext + CUST.PORD.name)
					CUST.PORD.tranParticular ="RTGS/RMT:" + (RIGHT$(CUST.PORD.tranPart,12)  + ":" + CUST.PORD.freetext + CUST.PORD.name)
					PRINT(CUST.PORD.tranParticular)
					
				#}
				ENDIF
			#}
			ENDIF
		#}
		ENDIF
		sv_s = " Update tbaadm.dtd set tran_particular=?SVAR ,tran_rmks =?SVAR "
		sv_s = sv_s + " where tran_id = ?SVAR "
		sv_s = sv_s + " AND tran_date = TO_DATE(?SVAR,'DD-MM-YYYY HH24:MI:SS') "
		sv_s = sv_s + " AND del_flg != 'Y' "
		sv_s = sv_s + " AND bank_id = ?SVAR "
		sv_s = sv_s + " AND PART_TRAN_SRL_NUM = ?SVAR "
		PRINT(sv_s)
		
		
		#BANCS.INPARAM.BINDVARS = CUST.PORD.tranParticular + "|" + mid$(CUST.PORD.tranParticular,0,30) + "|" + CUST.PORD.tranId  + "|"  + CUST.PORD.tranDate + "|" + BANCS.STDIN.contextBankId + "|" + CUST.PORD.partTranSrl
		BANCS.INPARAM.BINDVARS = mid$(CUST.PORD.tranParticular,0,50) + "|" + mid$(CUST.PORD.tranParticular,0,30) + "|" + CUST.PORD.tranId  + "|"  + CUST.PORD.tranDate + "|" + BANCS.STDIN.contextBankId + "|" + CUST.PORD.partTranSrl
		
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbSQLWithBind(sv_s)
		PRINT(sv_d)
		#        IF(sv_d==0)THEN
		#        #{
		#                sv_d = urhk_dbSqlWithBind("COMMIT")
		#        #}
		#        ENDIF
		sv_l = sv_l + 1
	#}
	DO
	
	EXITSCRIPT
END-->
