#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: ccprmdp001.scr
# Type: NA|default
# Description: script for fetching parameter id's
# Author: haneesha.kanumuru
# Date: 7/31/18 2:09 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="06258e36312d4e6d2d1d38f23db3723ce0f14875050e361b5346e6649475be16"
	# Script body goes here
	TRACE ON
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CCPRM Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CCPRM")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CCPRM",5)
	#}
	ENDIF
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	CUST.CCPRM.schmCode =" "
	CUST.CCPRM.errorFlg ="N"
	CUST.CCPRM.errorMsg = " "
	CUST.CCPRM.count = " "
	IF(FIELDEXISTS(BANCS.INPUT.schmCode)) THEN
	#{
		IF(TRIM(BANCS.INPUT.schmCode) != "")THEN
		#{
			CUST.CCPRM.schmCode= TOUPPER(TRIM(BANCS.INPUT.schmCode))
			PRINT(CUST.CCPRM.schmCode)
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.CCPRM.errorFlg = "Y"
		CUST.CCPRM.errorMsg = "Invalid Input for fnExecuteScript"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_s = "schm|"
	sv_s = sv_s + " SELECT SCHM_CODE"
	sv_s = sv_s + " FROM tbaadm.gsp "
	sv_s = sv_s + " WHERE SCHM_CODE = ?SVAR"
	sv_s = sv_s + " AND DEL_FLG != 'Y'"
	#sv_s = sv_s + " AND entity_cre_flg !='N'"
	#sv_s = sv_s + " AND schm_type='LAA'"
	sv_s = sv_s + " AND bank_id=?SVAR"
	
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.CCPRM.schmCode + "|" +BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	#	CUST.CCPRM.count = BANCS.OUTPARAM.count
	#	PRINT(CUST.CCPRM.count)
	
	IF( sv_d == 0 )THEN
	#{
		CUST.CCPRM.schmCode = BANCS.OUTPARAM.schm
		PRINT(CUST.CCPRM.schmCode)
		#		CUST.CCPRM.errorFlg = "Y"
		#		CUST.CCPRM.errorMsg = "Invalid Scheme Code"
		#}
	ELSE
		#{
		CUST.CCPRM.errorFlg = "Y"
		CUST.CCPRM.errorMsg = "Invalid Scheme Code"
	#}
	ENDIF
	CUST.CCPRM.schmCode = CUST.CCPRM.schmCode
	ENDOFSCRIPT:
		sv_u = urhk_SetOrbOut("errorFlg|"+CUST.CCPRM.errorFlg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("errorMsg|"+CUST.CCPRM.errorMsg)
		PRINT(sv_u)
		sv_u = urhk_SetOrbOut("schmCode|"+CUST.CCPRM.schmCode)
		
		PRINT(sv_u)
END-->

