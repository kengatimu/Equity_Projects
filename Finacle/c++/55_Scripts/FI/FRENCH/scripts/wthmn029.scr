#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: wthmn029.scr
# Type: NA|default
# Description: wthmn029.scr
# Author:
# Date:
# Modification Log:
#============================================================

##################################################################################################################################
#Script Name    : wthmn029.scr
#Start Date     : 20-June-2013
#Description    : This script is called on click of go button on crit page for menus OISA,OICA,OITUA AND OCMIDA
#				  And this script will fetch value for custom field "Withholding Tax Applicable" on scheme tab.
#Input para     : None
#Output para    : None
#Author         : Nitin_Kumar16
#Called Script  : NA
#Calling Script : SRV_FetchDfltCAAcctDtls_post_process_data.scr,SRV_FetchDfltSBAcctDtls_post_process_data.scr
#				  SRV_FetchDfltTUAcctDtls_post_process_data.scr
#Reference      : None
#Modification History:
#Srl No.  		Date         		Author          		Description
#-------  		-----------  		---------   			--------------
#   1.0   		20-June-2013 		Nitin_Kumar16      		Original version
##################################################################################################################################
<--START
CHECKSUM="b83d602f715ab3d28f6b1945fcc8fba57e02154369b0a6783b9247aa0259e6e2"
	#trace on
	#sv_u = urhk_B2k_PrintRepos("BANCS")
	#print(sv_u)
	#--------------------------------------------------------------------------
	# Create Repos and Classes
	#--------------------------------------------------------------------------
	sv_r = REPEXISTS("CUST")
	IF (sv_r == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	sv_r = CLASSEXISTS("CUST", "WITHTAX")
	IF (sv_r == 0) THEN
	#{
		CREATECLASS("CUST", "WITHTAX", 5)
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.STDIN.menuId) == "0") THEN
	#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	IF((BANCS.STDIN.menuId != "OISA") AND (BANCS.STDIN.menuId != "OICA") AND (BANCS.STDIN.menuId != "OITUA") AND (BANCS.STDIN.menuId != "OCMIDA"))THEN
	#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	CUST.WITHTAX.withdTaxAppFlg 		= ""
	CUST.WITHTAX.schmCode 				= ""
	CUST.WITHTAX.schmType				= ""
	CUST.WITHTAX.delFlg 				= "N"
	CUST.WITHTAX.entityFlg 			 	= "Y"
	
	IF(BANCS.STDIN.menuId == "OISA") THEN
	#{
		
		sv_u = urhk_SRV_GetVal("dfltSBAcctDtl.acctCommonInfo.acctGeneralInfo.acctBasic.schmCode.schmCode")
		PRINT(sv_u)
		IF(sv_u == 0)THEN
		#{
			CUST.WITHTAX.schmCode = BANCS.OUTPARAM.srvValue
		#}
		ENDIF
		
		sv_u = urhk_SRV_GetVal("dfltSBAcctDtl.acctCommonInfo.acctGeneralInfo.acctBasic.schmCode.schmType")
		PRINT(sv_u)
		IF(sv_u == 0)THEN
		#{
			CUST.WITHTAX.schmType = BANCS.OUTPARAM.srvValue
		#}
		ENDIF
	#}
	ENDIF
	
	IF(BANCS.STDIN.menuId == "OICA") THEN
	#{
		sv_u = urhk_SRV_GetVal("acctCriteria.schmCode.schmCode")
		PRINT(sv_u)
		IF(sv_u == 0)THEN
		#{
			CUST.WITHTAX.schmCode = BANCS.OUTPARAM.srvValue
		#}
		ENDIF
		
		sv_u = urhk_SRV_GetVal("acctCriteria.schmCode.schmType")
		PRINT(sv_u)
		IF(sv_u == 0)THEN
		#{
			CUST.WITHTAX.schmType = BANCS.OUTPARAM.srvValue
		#}
		ENDIF
	#}
	ENDIF
	
	IF((BANCS.STDIN.menuId == "OITUA") OR (BANCS.STDIN.menuId == "OCMIDA")) THEN
	#{
		sv_u = urhk_SRV_GetVal("acctCrit.schmCode.schmCode")
		PRINT(sv_u)
		IF(sv_u == 0)THEN
		#{
			CUST.WITHTAX.schmCode = BANCS.OUTPARAM.srvValue
		#}
		ENDIF
		
		sv_u = urhk_SRV_GetVal("acctCrit.schmCode.schmType")
		PRINT(sv_u)
		IF(sv_u == 0)THEN
		#{
			CUST.WITHTAX.schmType = BANCS.OUTPARAM.srvValue
		#}
		ENDIF
	#}
	ENDIF
	
	
	
	PRINT(CUST.WITHTAX.schmCode)
	PRINT(CUST.WITHTAX.schmType)
	
	BANCS.INPARAM.BINDVARS = ""
	BANCS.INPARAM.BINDVARS = CUST.WITHTAX.schmType + "|" + CUST.WITHTAX.schmCode + "|" +BANCS.STDIN.contextBankId+ "|" + CUST.WITHTAX.delFlg+ "|" + CUST.WITHTAX.entityFlg
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_s = "withdTaxAppFlg|SELECT WTH_TAX_APP_FLG"
	sv_s = sv_s + " FROM CUSTOM.C_WTH_GSP_DET"
	sv_s = sv_s + " WHERE  SCHM_TYPE = ?SVAR"
	sv_s = sv_s + " AND  SCHM_CODE = ?SVAR"
	sv_s = sv_s + " AND  BANK_ID = ?SVAR"
	sv_s = sv_s + " AND NVL(DEL_FLG,'N') = ?SVAR"
	sv_s = sv_s + " AND NVL(ENTITY_CRE_FLG,'N')= ?SVAR "
	
	PRINT(sv_s)
	sv_v = urhk_dbselectWithBind(sv_s)
	PRINT(sv_v)
	IF(sv_v == 0) THEN
	#{
		CUST.WITHTAX.withdTaxAppFlg = BANCS.OUTPARAM.withdTaxAppFlg
		PRINT(CUST.WITHTAX.withdTaxAppFlg)
		#}
	ELSE
		#{
		CUST.WITHTAX.withdTaxAppFlg = ""
	#}
	ENDIF
	
	IF(CUST.WITHTAX.withdTaxAppFlg != "") THEN
	#{
		IF((BANCS.STDIN.menuId == "OITUA") OR (BANCS.STDIN.menuId == "OCMIDA")) THEN
		#{
			ONS.CUSTOM.custDataOut = "~oituaschdet|" + CUST.WITHTAX.withdTaxAppFlg
			PRINT(ONS.CUSTOM.custDataOut)
		#}
		ENDIF
		IF((BANCS.STDIN.menuId == "OICA") OR (BANCS.STDIN.menuId == "OISA")) THEN
		#{
			ONS.CUSTOM.custDataOut = "~oilasch|" + CUST.WITHTAX.withdTaxAppFlg
			PRINT(ONS.CUSTOM.custDataOut)
		#}
		ENDIF
	#}
	ENDIF
	
	ENDOFSCRIPT:
		#Trace off
		
END-->
