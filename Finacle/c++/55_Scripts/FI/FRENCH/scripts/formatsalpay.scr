#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: formatsalpay.scr
# Type: NA|default
# Description: formatsalpay.scr
# Author:
# Date:
# Modification Log:
#============================================================

#------------------------------------------------------------------------------------------------
#    Name			 : formatsalpay.scr
#    Description     :
#    Date            : 08-03-2013
#    Author          : Rajavel
#    Input           : NA
#    Output			 : NA
#    Called Script	 : NA
#    Calling Script	 : NA
#    Menu Option	 : HSALPAY
#    Srl. No			Date			Author			Description.
#    ------			----------			-------			---------------
#    1.0			08-03-2013			Rajavel			Original Version
#-----------------------------------------------------------------------------------------------
<--START
CHECKSUM="d196e71ce8e93943c502719e429ac0f2fd4c0eb3a6e1613bfb82366fe96ad6d8"
	#TRACE ON
	
	#------------------------------------------------------------------------------
	#       Creating repository and class
	#       Initializing the variables
	#------------------------------------------------------------------------------
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST", "FORMT") == 0) THEN
	#{
		CREATECLASS("CUST", "FORMT", 5)
	#}
	ENDIF
	
	CUST.FORMT.errMsg = ""
	BANCS.INPUT.moduleName = "FORMTACT"
	BANCS.INPUT.parameterId = "SALSOLID"
	CALL ("getcparamvalue.scr")
	IF(BANCS.OUTPUT.parameterVal != "")THEN
	#{
		CUST.FORMT.salSolID = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		CUST.FORMT.errMsg = "sol id setup not done in CPARAM"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	PRINT(BANCS.INPUT.operAcctNo)
	sv_s ="forAcct|SELECT foracid FROM TBAADM.GAM "
	sv_s = sv_s + "WHERE foracid = ?SVAR "
	sv_s = sv_s + "AND GAM.bank_id = ?SVAR "
	sv_s = sv_s + "AND sol_id != ?SVAR "
	BANCS.INPARAM.BINDVARS = BANCS.INPUT.operAcctNo + "|" + BANCS.STDIN.contextBankId + "|" + CUST.FORMT.salSolID
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF (sv_d == 0) THEN
	#{
		sv_z = "Accnt| " + BANCS.INPUT.operAcctNo
		sv_u = urhk_SetOrbOut(sv_z)
		#}
	ELSE
		#{
		sv_s = "fin_acct_num|SELECT foracid FROM CUSTOM.OLD_ACCT_MAP "
		sv_s = sv_s + "WHERE old_num = ?SVAR "
		sv_s = sv_s + "AND bank_id = ?SVAR "
		sv_s = sv_s + "AND sol_id = ?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = BANCS.INPUT.operAcctNo + "|" + BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.mySolId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF (sv_d == 0) THEN
		#{
			sv_z = "Accnt| " + BANCS.OUTPARAM.fin_acct_num
			sv_u = urhk_SetOrbOut(sv_z)
			#}
		ELSE
			#{
			sv_w = mid$(BANCS.INPUT.operAcctNo,0,3)
			sv_s = "fin_acct_num|SELECT foracid FROM CUSTOM.OLD_ACCT_MAP "
			sv_s = sv_s + "WHERE old_num = '" + mid$(BANCS.INPUT.operAcctNo,3,(STRLEN(BANCS.INPUT.operAcctNo)-3)) + "' "
			sv_s = sv_s + "AND bank_id = ?SVAR "
			sv_s = sv_s + "AND sol_id = ?SVAR "
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.mySolId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_d)
			IF (sv_d == 0) THEN
			#{
				sv_z = "Accnt| " + BANCS.OUTPARAM.fin_acct_num
				sv_u = urhk_SetOrbOut(sv_z)
				#}
			ELSE
				#{
				sv_z = "Accnt| " + BANCS.INPUT.operAcctNo
				sv_u = urhk_SetOrbOut(sv_z)
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	ENDOFSCRIPT:
		IF(CUST.FORMT.errMsg != "") THEN
		#{
			sv_u = urhk_setOrbOut("Error| "+CUST.FORMT.errMsg)
			PRINT(sv_u)
		#}
		ENDIF
		
		#TRACE OFF
		EXITSCRIPT
END-->
