#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: Biller_ID_searcher.scr 
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
##============================================================

<--START

TRACE ON

	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       tm_bill_check.scr
	# Author                :       Anand Pansare 
	# Description           :       This script s used for biller ID seracher and its detaiils  .
	# Called Script         :	
	# Calling Script        :
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1            01-12-2015        Anand Pansare     Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON

	lv_u = urhk_b2k_printrepos("BANCS")

	#========================================================
	# Check whether CUST Repository exists else create it
	#========================================================
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	#========================================================
	# Check whether BILL Class exists else create it
	#========================================================
	sv_r = CLASSEXISTS("CUST","BILL")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","BILL",5)
	#}
	ENDIF
	#========================================================
	# Initialization
	#========================================================
	CUST.BILL.errorFlg = "N"
	CUST.BILL.srvSuccess="Y"
	CUST.BILL.reverseFlg="N"
	CUST.BILL.prntRevFlg="N"
	CUST.BILL.bankId=BANCS.STDIN.contextBankId
	CUST.BILL.BODDate=BANCS.STDIN.BODDate
	CUST.BILL.billerValReq="N"
	#========================================================
	# Finding All biller Deatail
	#========================================================
		
			sv_s = "Biller_ID,acctId,BILLER_NAME,BANK_CODE,VALIDATION_TYPE|"
			sv_s = " SELECT BILLER_ID,"
			sv_s = " FREE_TEXT1,"
			sv_s = " BILLER_NAME,"
			sv_s = " FREE_TEXT2,"
			sv_s = " FREE_TEXT4"
			sv_s = " FROM TBAADM.BRGT"
		
			PRINT(sv_s)
			
			sv_c = urhk_dbCursorOpenWithBind(sv_s)
                        print(sv_c)

                        IF(sv_c != 0)THEN
                        #{
                                GOTO ENDOFSCRIPT
                        #}
                        ENDIF

                        sv_n = BANCS.OUTPARAM.DB_CURSOR_NUMBER
                        print(sv_n)

                        sv_m = urhk_dbCursorFetch(sv_n)
                        print(sv_m)

                        sv_g = 1

                        WHILE(sv_m == 0)
                        #{
				CUST.BILL.denomCount = BANCS.OUTPARAM.denomCount
                                print(CUST.BILL.denomCount)
                                ("CUST").("BILL").("denomCount"+Format$(sv_g,"%d"))=CUST.BILL.denomCount

                                IF(sv_g==1)THEN
                                #{
                                        sv_u =  ("CUST").("BILL").("denomCount"+Format$(sv_g,"%d"))
                                        print(sv_u)
                                        #}
                                ELSE
                                        #{
                                        sv_w = ("CUST").("BILL").("denomCount"+Format$(sv_g,"%d"))
                                        print(sv_w)
                                #}
                                ENDIF

                                sv_m = urhk_dbCursorFetch(sv_n)
                                print(sv_m)
                                sv_g = sv_g + 1
                                print(sv_g)
                        #}
                        DO

                        #Denom count for PB/GB record
                        sv_v = sv_u + "-" + sv_w
                        print(sv_v)
                        print(CUST.BILL.PBGBchk)

                        #Closing cursor
                        sv_d = urhk_dbCursorClose(sv_n)
                        print(sv_d)

			
			




	
			sv_d = urhk_dbSelectWithBind(sv_s)
		
			IF(sv_d==0)THEN
			#{
				#{
					sv_e=urhk_SetOrbOut("biller_chk_flg|Y")
					sv_f=urhk_SetOrbOut("Biller_ID|"+CUST.BILL.BILLER_ID)
				#}
				 ELSE
        	                #{
                        		sv_e=urhk_SetOrbOut("biller_chk_flg|N")
                                        sv_f=urhk_SetOrbOut("Biller_ID|"+CUST.BILL.BILLER_ID)
				#}
                        	ENDIF
			#}
			ELSE
			#{
				CUST.BILL.COUNT=""
			#}
			ENDIF
			
		#}
		ENDIF

	ENDOFSCRIPT:


TRACE OFF
	
END-->
