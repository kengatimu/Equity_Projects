############################################################################################################
# Source Name                       : cichbmaudp001.scr
# Date                              : 10-03-2013
# Description                       : This script will get call when CDDUM(P) is invoked
# Input                             : None
# Output                            : None
# Called Script                     : None
# Calling Script                    :
# Author                            : Shilpa
# Modification History              :
# < Version No.>    <Date>      <Author Name>       <Description>
#   1               10-03-2013  Shilpa              Original Version
#
# Using tablespace 4
############################################################################################################
<--START
CHECKSUM="5d73d9d58c8d116f9e66c3bbf933574e2e6ee6845a93ee0b09439032c6622b2b"
	TRACE ON
	
	#==========================================================*
	# CREATING NEW REPOSITORY AND CLASS
	#==========================================================*
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	IF (CLASSEXISTS("CUST", "CDDUM") == 0) THEN
	#{
		CREATECLASS("CUST", "CDDUM", 5)
	#}
	ENDIF
	sv_a = 1
	
	sv_s = "recCount"
	sv_s = sv_s + " | "
	sv_s = sv_s + " SELECT COUNT(*) "
	sv_s = sv_s + " FROM CUSTOM.C_CBT_MOD "
	sv_s = sv_s + " WHERE del_flg != 'Y' AND bank_id = ?SVAR "
	BANCS.INPARAM.BINDVARS =  BANCS.STDIN.contextBankId
	print(sv_s)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	CUST.CDDUM.recCount = BANCS.OUTPARAM.recCount
	print(CUST.CDDUM.recCount)
	
	sv_q = urhk_SetOrbOut("recCount|"+CUST.CDDUM.recCount)
	
	sv_s = "foracid,instType"
	sv_s = sv_s + " | "
	sv_s = sv_s + " SELECT rpad(FORACID,20,' '),lpad(INST_TYPE,6,' ') "
	sv_s = sv_s + " FROM CUSTOM.C_CBT_MOD "
	sv_s = sv_s + " WHERE del_flg != 'Y' "
	sv_s = sv_s + " AND bank_id =?SVAR "
	BANCS.INPARAM.BINDVARS =  BANCS.STDIN.contextBankId
	print(sv_s)
	
	sv_c = urhk_dbCursorOpenWithBind(sv_s)
	
	IF( sv_c != 0) THEN
	#{
		sv_u = urhk_SRV_SetErr("No recods fetched for the entered Account Number")
		BANCS.OUTPUT.SuccessOrFailure = "F"
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		sv_l = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		print(sv_l)
		sv_c = urhk_dbCursorFetch(sv_l)
		print(sv_c)
		
		WHILE( sv_c == 0)
		#{
			CUST.CDDUM.count = sv_a
			print(CUST.CDDUM.count)
			
			CUST.CDDUM.foracid = BANCS.OUTPARAM.foracid
			print(CUST.CDDUM.foracid)
			
			CUST.CDDUM.instType1 = BANCS.OUTPARAM.instType
			print(CUST.CDDUM.instType1)
			
			sv_z = "foracid_" + CUST.CDDUM.count
			sv_z = sv_z + "|" + CUST.CDDUM.foracid
			sv_u = urhk_SetOrbOut(sv_z)
			
			sv_z = "instType1_" + CUST.CDDUM.count
			sv_z = sv_z + "|" + CUST.CDDUM.instType1
			sv_u = urhk_SetOrbOut(sv_z)
			
			sv_a = sv_a + 1
			sv_c = urhk_dbCursorFetch(sv_l)
			print(sv_c)
		#}
		DO
		sv_c = urhk_dbCursorClose(sv_l)
		print(sv_c)
	#}
	ENDIF
	
	ENDOFSCRIPT:
		exitscript
		
END-->
