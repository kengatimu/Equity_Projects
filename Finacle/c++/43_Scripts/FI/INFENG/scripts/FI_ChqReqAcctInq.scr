#-----------------------------------------------------------------------------------------------

<--start
trace on

        IF (REPEXISTS("CUST") == 0) THEN
        #{
                CREATEREP("CUST")
        #}
        ENDIF

        IF (CLASSEXISTS("CUST","CHQ") == 0) THEN
        #{
                CREATECLASS("CUST","CHQ",5)
        #}
        ENDIF

	sv_a=urhk_B2k_PrintRepos("BANCS")
	print (sv_a)

	IF( (FIELDEXISTS(("BANCS").("INPUT").("acctNo"))== 0) ) THEN
	#{
   		CUST.CHQ.errMsg="acctNo is not available"
		sv_z = urhk_SetOrbOut("SuccessOrFailure|Failed")
		sv_t = "Msg| " +CUST.CHQ.errMsg
		sv_u = urhk_SetOrbOut(sv_t)
   		GOTO ENDSCRIPT
	#}
	ENDIF




	################################ Variable Inititalization#######################################################
	CUST.CHQ.acctNo= BANCS.INPUT.acctNo
	print(CUST.CHQ.acctNo)
        CUST.CHQ.count = "0"

	sv_a ="reqid, solname, solid, no_of_cheqs, reqdate,status,leaves,beginNum,endNum|select req_id, (select sol_desc from tbaadm.sol "
	sv_a =sv_a + " where bank_id=?SVAR and sol_id=branch_id) as branch_name, DEST_BRANCH,NUM_OF_CHB,REQ_DATE,REQ_STATUS,NUM_OF_CHQ_LVS,BEGIN_CHQ_NUM, END_CHQ_NUM"
	sv_a =sv_a + " from CUSTOM.CUSTOM_CBR where bank_id=?SVAR and foracid=?SVAR "
	print(sv_a)
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.contextBankId+ "|" + CUST.CHQ.acctNo
	print(BANCS.INPARAM.BINDVARS)
        sv_d = urhk_dbCursorOpenWithBind(sv_a)
        print(sv_d)
	IF(sv_d!=0) THEN
       	#{
       		sv_u = urhk_dbCursorClose(sv_a)
       		print(sv_u)
			
		sv_z = urhk_SetOrbOut("SuccessOrFailure|Failed")
        	sv_u = urhk_SetOrbOut("Msg| No record found")
               	print(sv_u)
        #}
        ELSE
        #{
        	CUST.CHQ.DB_CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
               	print(CUST.CHQ.DB_CURSOR_NUMBER)

               	sv_c = urhk_dbCursorFetch(CUST.CHQ.DB_CURSOR_NUMBER)
               	print(sv_c)

		IF(sv_c != "0")THEN
                #{
                        sv_e = urhk_SetOrbOut("SuccessOrFailure|Success")
                        sv_u = urhk_SetOrbOut("Msg| No record found")
                        print(sv_u)

                        sv_c = urhk_dbCursorClose(CUST.CHQ.DB_CURSOR_NUMBER)
                        print(sv_c)

                        EXITSCRIPT

                #}
                ENDIF

               	while(sv_c ==0)
               	#{
               		CUST.CHQ.reqid= BANCS.OUTPARAM.reqid
                        print(CUST.CHQ.reqid)
               		CUST.CHQ.solname= BANCS.OUTPARAM.solname
                        print(CUST.CHQ.solname)
               		CUST.CHQ.solid= BANCS.OUTPARAM.solid
                        print(CUST.CHQ.solid)
               		CUST.CHQ.no_of_cheqs= BANCS.OUTPARAM.no_of_cheqs
                        print(CUST.CHQ.no_of_cheqs)
               		CUST.CHQ.reqdate= BANCS.OUTPARAM.reqdate
                        print(CUST.CHQ.reqdate)
               		CUST.CHQ.status= BANCS.OUTPARAM.status
                        print(CUST.CHQ.status)
               		CUST.CHQ.leaves= BANCS.OUTPARAM.leaves
                        print(CUST.CHQ.leaves)
               		CUST.CHQ.beginNum= BANCS.OUTPARAM.beginNum
                        print(CUST.CHQ.beginNum)
               		CUST.CHQ.endNum= BANCS.OUTPARAM.endNum
                        print(CUST.CHQ.endNum)


			sv_z = urhk_SetOrbOut("SuccessOrFailure|Success")
                        sv_c = urhk_SetOrbOut("AcctChqReqDetails.<rec_"+CUST.CHQ.count+">.ReqId"+"| "+CUST.CHQ.reqid)
                        sv_c = urhk_SetOrbOut("AcctChqReqDetails.<rec_"+CUST.CHQ.count+">.branchName"+"| "+CUST.CHQ.solname)
                        sv_c = urhk_SetOrbOut("AcctChqReqDetails.<rec_"+CUST.CHQ.count+">.BranchCode"+"| "+CUST.CHQ.solid)
                        sv_c = urhk_SetOrbOut("AcctChqReqDetails.<rec_"+CUST.CHQ.count+">.NoOfChqbks"+"| "+CUST.CHQ.no_of_cheqs)
                        sv_c = urhk_SetOrbOut("AcctChqReqDetails.<rec_"+CUST.CHQ.count+">.DateCreated"+"| "+CUST.CHQ.reqdate)
                        sv_c = urhk_SetOrbOut("AcctChqReqDetails.<rec_"+CUST.CHQ.count+">.Status"+"| "+CUST.CHQ.status)
                        sv_c = urhk_SetOrbOut("AcctChqReqDetails.<rec_"+CUST.CHQ.count+">.leaves"+"| "+CUST.CHQ.leaves)
                        sv_c = urhk_SetOrbOut("AcctChqReqDetails.<rec_"+CUST.CHQ.count+">.BeginNum"+"| "+CUST.CHQ.beginNum)
                        sv_c = urhk_SetOrbOut("AcctChqReqDetails.<rec_"+CUST.CHQ.count+">.EndNum"+"| "+CUST.CHQ.endNum)


                        CUST.CHQ.count = CINT(CUST.CHQ.count)+ 1
                        print(CUST.CHQ.count)

			sv_c = urhk_dbCursorFetch(CUST.CHQ.DB_CURSOR_NUMBER)
			print(sv_c)

                  #}
                  DO
                         sv_c = urhk_dbCursorClose(CUST.CHQ.DB_CURSOR_NUMBER)
                         print(sv_c)

	#}
	ENDIF
ENDSCRIPT:

trace off

end-->

