#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: vatTableODUpdate.scr
# Type: NA|default
# Description: vatTableODUpdate.scr
# Author: AnuragGhosh_D
# Date: 9/14/18 5:55 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="f3dfcd1648ad456833b2884830b76f0b1c201748fde7875745e482e4b20b3297"
	# Script body goes here
	TRACE ON
	
	#=====Inserting into Tran table============######
	
	sv_s = "INSERT INTO custom.C_OD_VAT_TRAN_TBL("
	sv_s = sv_s +"FORACID,"
	sv_s = sv_s +"ACID,"
	sv_s = sv_s +"SOL_ID,"
	sv_s = sv_s +"ACCT_CCY_CODE,"
	sv_s = sv_s +"DR_INT_AMT_FCY,"
	sv_s = sv_s +"VAT_AMT_FCY,"
	sv_s = sv_s +"DR_INT_AMT_LCY,"
	sv_s = sv_s +"VAT_AMT_LCY,"
	sv_s = sv_s +"VAT_PCNT,"
	sv_s = sv_s +"TRAN_DATE,"
	sv_s = sv_s +"TRAN_AMT,"
	sv_s = sv_s +"RATE_CODE,"
	sv_s = sv_s +"RATE,"
	sv_s = sv_s +"DR_CR_IND,"
	sv_s = sv_s +"RCRE_TIME,"
	sv_s = sv_s +"LCHG_TIME,"
	sv_s = sv_s +"RCRE_USER_ID,"
	sv_s = sv_s +"LCHG_USER_ID,"
	sv_s = sv_s +"BANK_ID, "
	sv_s = sv_s +"VAT_TRAN_ID,"
	sv_s = sv_s +"SAT_TRAN_ID)"
	
	sv_s = sv_s + " VALUES ( "
	sv_s = sv_s + " ?SVAR, "
	sv_s = sv_s + " ?SVAR, "
	sv_s = sv_s + " ?SVAR, "
	sv_s = sv_s + " ?SVAR, "
	sv_s = sv_s + " ?SVAR, "
	sv_s = sv_s + " ?SVAR, "
	sv_s = sv_s + " ?SVAR, "
	sv_s = sv_s + " ?SVAR, "
	sv_s = sv_s + " ?SVAR, "
	sv_s = sv_s + " sysdate, "
	sv_s = sv_s + " ?SVAR, "
	sv_s = sv_s + " ?SVAR, "
	sv_s = sv_s + " ?SVAR, "
	sv_s = sv_s + " ?SVAR, "
	sv_s = sv_s + " sysdate, "
	sv_s = sv_s + " sysdate, "
	sv_s = sv_s + " ?SVAR, "
	sv_s = sv_s + " ?SVAR, "
	sv_s = sv_s + " ?SVAR, "
	sv_s = sv_s + " ?SVAR, "
	sv_s = sv_s + " ?SVAR)"
	
	
	BANCS.INPARAM.BINDVARS = ""
	
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.foracid + "|"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.acid + "|"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.mySolId + "|"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.acctCrncyCode + "|"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.totalIndSatFcy + "|"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.totalVatSatFcy + "|"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.totalIndSatLcy + "|"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.totalVatSatLcy + "|"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.vatPcnt + "|"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.tranAmt + "|"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.rateCode + "|"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.rate + "|"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.crDrInd + "|"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.userId + "|"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.userId + "|"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.contextBankId + "|"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.tranId + "|"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.SattranId
	
	PRINT(BANCS.INPARAM.BINDVARS)
	PRINT(sv_s)
	sv_d = urhk_dbSQLWithBind(sv_s)
	PRINT(sv_d)
	
	
	#=====================Inserting or Updating master table============================#
	
	sv_s = "recCount | select count(*) from custom.C_OD_VAT_MASTER_TBL where foracid = ?SVAR AND bank_id =?SVAR "
	
	BANCS.INPARAM.BINDVARS = ""
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.foracid + "|"
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.contextBankId
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	CUST.VAT.recCount = BANCS.OUTPARAM.recCount
	PRINT(CUST.VAT.recCount)
	
	IF(CUST.VAT.recCount == 0) THEN
	#{
		sv_s = "INSERT INTO custom.C_OD_VAT_MASTER_TBL("
		sv_s = sv_s +"FORACID,"
		sv_s = sv_s +"ACID,"
		sv_s = sv_s +"SOL_ID,"
		sv_s = sv_s +"ACCT_CCY_CODE,"
		sv_s = sv_s +"TOT_DR_INT_FCY,"
		sv_s = sv_s +"TOT_VAT_AMT_FCY,"
		sv_s = sv_s +"TOT_DR_INT_LCY,"
		sv_s = sv_s +"TOT_VAT_AMT_LCY,"
		sv_s = sv_s +"RCRE_TIME,"
		sv_s = sv_s +"LCHG_TIME,"
		sv_s = sv_s +"RCRE_USER_ID,"
		sv_s = sv_s +"LCHG_USER_ID,"
		sv_s = sv_s +"BANK_ID)"
		
		sv_s = sv_s + " VALUES ( "
		sv_s = sv_s + " ?SVAR, "
		sv_s = sv_s + " ?SVAR, "
		sv_s = sv_s + " ?SVAR, "
		sv_s = sv_s + " ?SVAR, "
		sv_s = sv_s + " ?SVAR, "
		sv_s = sv_s + " ?SVAR, "
		sv_s = sv_s + " ?SVAR, "
		sv_s = sv_s + " ?SVAR, "
		sv_s = sv_s + " sysdate, "
		sv_s = sv_s + " sysdate, "
		sv_s = sv_s + " ?SVAR, "
		sv_s = sv_s + " ?SVAR, "
		sv_s = sv_s + " ?SVAR)"
		
		BANCS.INPARAM.BINDVARS = ""
		
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.foracid + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.acid + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.mySolId + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.acctCrncyCode + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.totalIndSatFcy + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.totalVatSatFcy + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.totalIndSatLcy + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.totalVatSatLcy + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.userId + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.userId + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.contextBankId
		
		PRINT(BANCS.INPARAM.BINDVARS)
		PRINT(sv_s)
		sv_d = urhk_dbSQLWithBind(sv_s)
		
		PRINT(sv_d)
	#}
	ENDIF
	
	IF(CUST.VAT.recCount > 0) THEN
	#{
		
		
		sv_s = "UPDATE custom.C_OD_VAT_MASTER_TBL SET "
		
		sv_s = sv_s +"TOT_DR_INT_FCY= (TOT_DR_INT_FCY+ ?SVAR),"
		sv_s = sv_s +"TOT_VAT_AMT_FCY= (TOT_VAT_AMT_FCY + ?SVAR),"
		sv_s = sv_s +"TOT_DR_INT_LCY= (TOT_DR_INT_LCY + ?SVAR),"
		sv_s = sv_s +"TOT_VAT_AMT_LCY= (TOT_VAT_AMT_LCY + ?SVAR),"
		sv_s = sv_s +"LCHG_TIME= sysdate ,"
		sv_s = sv_s +"LCHG_USER_ID= ?SVAR "
		
		sv_s = sv_s + " WHERE FORACID = ?SVAR "
		sv_s = sv_s + " AND BANK_ID = ?SVAR "
		
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = ""
		
		
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.totalIndSatFcy + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.totalVatSatFcy + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.totalIndSatLcy + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.totalVatSatLcy  + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.userId + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.VAT.foracid + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.contextBankId
		
		PRINT(BANCS.INPARAM.BINDVARS )
		PRINT(sv_s)
		sv_d = urhk_dbSQLWithBind(sv_s)
		PRINT(sv_d)
	#}
	ENDIF
	EXITSCRIPT
	
	
END-->
