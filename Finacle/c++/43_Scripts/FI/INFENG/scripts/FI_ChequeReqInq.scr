#-----------------------------------------------------------------------------------------------

<--start
trace on

        IF (REPEXISTS("CUST") == 0) THEN
        #{
                CREATEREP("CUST")
        #}
        ENDIF

        IF (CLASSEXISTS("CUST","CHQ") == 0) THEN
        #{
                CREATECLASS("CUST","CHQ",5)
        #}
        ENDIF

	sv_a=urhk_B2k_PrintRepos("BANCS")
	print (sv_a)

	IF( (FIELDEXISTS(("BANCS").("INPUT").("reqid"))== 0) ) THEN
	#{
   		CUST.CHQ.errMsg="reqid is not available"
		sv_z = urhk_SetOrbOut("SuccessOrFailure|Failed")
		sv_t = "Msg| " +CUST.CHQ.errMsg
		sv_u = urhk_SetOrbOut(sv_t)
   		GOTO ENDSCRIPT
	#}
	ENDIF


#test


	################################ Variable Inititalization#######################################################
	CUST.CHQ.reqid= BANCS.INPUT.reqid
	print(CUST.CHQ.reqid)

	sv_s = "sol|select DEST_BRANCH from  CUSTOM.CUSTOM_CBR   "
	sv_s = sv_s + "where bank_id=?SVAR and req_id=?SVAR "
	
	BANCS.INPARAM.BINDVARS =  BANCS.STDIN.contextBankId + "|" + CUST.CHQ.reqid
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_r =  urhk_dbSelectWithBind(sv_s)
	print(sv_r)
	IF (sv_r ==0) THEN
	#{
		CUST.CHQ.sol= BANCS.OUTPARAM.sol
		print (CUST.CHQ.sol)



		sv_s ="reqid, solname, solid, no_of_cheqs, reqdate,status,leaves,beginNum, endNum|select req_id, (select sol_desc from tbaadm.sol "
		sv_s =sv_s + " where bank_id=?SVAR and sol_id=?SVAR) as branch_name, DEST_BRANCH,NUM_OF_CHB,REQ_DATE,REQ_STATUS,NUM_OF_CHQ_LVS,BEGIN_CHQ_NUM, END_CHQ_NUM"
		sv_s =sv_s + " from CUSTOM.CUSTOM_CBR where bank_id=?SVAR and req_id=?SVAR "

		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + CUST.CHQ.sol+ "|" + BANCS.STDIN.contextBankId+ "|" + CUST.CHQ.reqid
		print(sv_s)
		sv_d = urhk_dbSelectWithBind(sv_s)
		print(sv_d)
		IF(sv_d == 0)then
		#{
			CUST.CHQ.reqid= BANCS.OUTPARAM.reqid
			print (CUST.CHQ.reqid)
			CUST.CHQ.solname= BANCS.OUTPARAM.solname
			print (CUST.CHQ.solname)
			CUST.CHQ.solid= BANCS.OUTPARAM.solid
			print (CUST.CHQ.solid)
			CUST.CHQ.no_of_cheqs= BANCS.OUTPARAM.no_of_cheqs
			print (CUST.CHQ.no_of_cheqs)
			CUST.CHQ.reqdate= BANCS.OUTPARAM.reqdate
			print (CUST.CHQ.reqdate)
			CUST.CHQ.status= BANCS.OUTPARAM.status
			print (CUST.CHQ.status)
			CUST.CHQ.leaves= BANCS.OUTPARAM.leaves
			print (CUST.CHQ.leaves)
			CUST.CHQ.beginNum= BANCS.OUTPARAM.beginNum
			print (CUST.CHQ.beginNum)
			CUST.CHQ.endNum= BANCS.OUTPARAM.endNum
			print (CUST.CHQ.endNum)



			sv_u = urhk_SetOrbOut("SuccessOrFailure|Success")
			sv_u = urhk_SetOrbOut("ReqId|"+ CUST.CHQ.reqid)
			sv_u = urhk_SetOrbOut("branchName|"+ CUST.CHQ.solname)
			sv_u = urhk_SetOrbOut("BranchCode|"+ CUST.CHQ.solid)
			sv_u = urhk_SetOrbOut("NoOfChqbks|"+ CUST.CHQ.no_of_cheqs)
			sv_u = urhk_SetOrbOut("DateCreated|"+ CUST.CHQ.reqdate)
			sv_u = urhk_SetOrbOut("Status|"+CUST.CHQ.status)
			sv_u = urhk_SetOrbOut("leaves|"+CUST.CHQ.leaves)
			sv_u = urhk_SetOrbOut("BeginNum|"+CUST.CHQ.beginNum)
			sv_u = urhk_SetOrbOut("EndNum|"+CUST.CHQ.endNum)
			sv_u = urhk_SetOrbOut("Msg| ")

		#}
		ENDIF

	#}
	ELSE
	#{
   		CUST.CHQ.errMsg="Invalid Request Id"
		sv_t = "Msg| " +CUST.CHQ.errMsg
		sv_u = urhk_SetOrbOut("SuccessOrFailure|Failed")
		sv_u = urhk_SetOrbOut(sv_t)
   		GOTO ENDSCRIPT		
	#}
	ENDIF


ENDSCRIPT:

trace off

end-->

