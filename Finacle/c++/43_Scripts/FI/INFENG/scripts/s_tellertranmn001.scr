#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: s_tellertranmn001.scr
# Type: NA|default
# Description: s_tellertranmn001.scr
# Author:
# Date:
# Modification Log:
#============================================================

#------------------------------------------------------------------------------------------------
#   Name            : s_tellertranmn001.scr
#   Description     : Raise custom exception for teller transaction
#   Date            : 01-10-2013
#   Author          : Bharath Reddy B
#   Input           : NA
#   Output          : NA
#   Called Script   : NA
#   Calling Script  : NA
#   Menu Option     :
#   Srl. No         Date            Author          	Description.
#   -------         ------          ------          	------------
#   1.0             01-10-2013      Bharath Reddy B		Original Version
#-----------------------------------------------------------------------------------------------
<--START
CHECKSUM="da99cdc057faee76ebf0975c5b9a6c743b54c1a908e614af21f664756f893cc7"
	#TRACE ON
	IF (CLASSEXISTS("CUST","TELLER") == 0) THEN
	#{
		CREATECLASS("CUST","TELLER",5)
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST","CPARAM") == 0) THEN
	#{
		CREATECLASS("CUST","CPARAM",5)
	#}
	ENDIF
	
	#-----------------------------------------------------------------------------------------------
	# Initializing Out Variables
	#-----------------------------------------------------------------------------------------------
	CUST.TELLER.errMsg = ""
	
	#-----------------------------------------------------------------------------------------------
	# Finding no of part trans in transaction
	#-----------------------------------------------------------------------------------------------
	sv_u = urhk_SRV_GetVal("tranHdr.tranTypeSubType.tranType")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
		CUST.TELLER.tranType = BANCS.OUTPARAM.srvValue
		PRINT(CUST.TELLER.tranType)
	#}
	ENDIF
	
	sv_u = urhk_SRV_GetVal("tranHdr.tranTypeSubType.tranSubType")
	PRINT(sv_u)
	IF (sv_u == 0) THEN
	#{
		CUST.TELLER.tranSubType = BANCS.OUTPARAM.srvValue
		PRINT(CUST.TELLER.tranSubType)
	#}
	ENDIF
	
	IF ( (CUST.TELLER.tranType == "C" ) AND ((CUST.TELLER.tranSubType == "CP") OR (CUST.TELLER.tranSubType == "CR") OR (CUST.TELLER.tranSubType == "CT")) ) THEN
	#{
		#---------------------------------------------------------------------
		#---Finding whether user is teller or not
		#---------------------------------------------------------------------
		sv_s=""
		sv_s = sv_s + " cnt|"
		sv_s = sv_s + " SELECT count(1)"
		sv_s = sv_s + " FROM TBAADM.UPR, TBAADM.GEC"
		sv_s = sv_s + " WHERE UPR.user_id = ?SVAR"
		sv_s = sv_s + " AND GEC.emp_id = UPR.user_emp_id"
		sv_s = sv_s + " AND UPR.home_bank_id = GEC.bank_id"
		sv_s = sv_s + " AND UPR.sol_id = GEC.sol_id"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.userId
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbselectWithBind(sv_s)
		PRINT(sv_d)
		IF ( CINT(BANCS.OUTPARAM.cnt) > 0 ) THEN
		#{
			PRINT(BANCS.OUTPARAM.cnt)
			IF ( BANCS.INPUT.srvcallmode == "E" ) THEN
			#{
				sv_u = urhk_B2k_PutUserException("VAL")
				PRINT(sv_u)
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	EXITSCRIPT
END-->
