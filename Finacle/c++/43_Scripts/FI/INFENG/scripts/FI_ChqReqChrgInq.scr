#-----------------------------------------------------------------------------------------------

<--start
trace on

        IF (REPEXISTS("CUST") == 0) THEN
        #{
                CREATEREP("CUST")
        #}
        ENDIF

        IF (CLASSEXISTS("CUST","CHQ") == 0) THEN
        #{
                CREATECLASS("CUST","CHQ",5)
        #}
        ENDIF

	sv_a=urhk_B2k_PrintRepos("BANCS")
	print (sv_a)

	IF( (FIELDEXISTS(("BANCS").("INPUT").("funcCode"))== 0) ) THEN
	#{
   		CUST.CHQ.errMsg="funcCode is not available"
		sv_z = urhk_SetOrbOut("SuccessOrFailure|Failed")
		sv_t = "Msg| " +CUST.CHQ.errMsg
		sv_u = urhk_SetOrbOut(sv_t)
   		GOTO ENDSCRIPT
	#}
	ENDIF


	IF( (FIELDEXISTS(("BANCS").("INPUT").("acctNo"))== 0) ) THEN
	#{
   		CUST.CHQ.errMsg="acctNo is not available"
		sv_z = urhk_SetOrbOut("SuccessOrFailure|Failed")
		sv_t = "Msg| " +CUST.CHQ.errMsg
		sv_u = urhk_SetOrbOut(sv_t)
   		GOTO ENDSCRIPT
	#}
	ENDIF

	IF( (FIELDEXISTS(("BANCS").("INPUT").("noOfChqs"))== 0) ) THEN
	#{
   		CUST.CHQ.errMsg="noOfChqs is not available"
		sv_z = urhk_SetOrbOut("SuccessOrFailure|Failed")
		sv_t = "Msg| " +CUST.CHQ.errMsg
		sv_u = urhk_SetOrbOut(sv_t)
   		GOTO ENDSCRIPT
	#}
	ENDIF

	IF( (FIELDEXISTS(("BANCS").("INPUT").("leaves"))== 0) ) THEN
	#{
   		CUST.CHQ.errMsg="leaves is not available"
		sv_z = urhk_SetOrbOut("SuccessOrFailure|Failed")
		sv_t = "Msg| " +CUST.CHQ.errMsg
		sv_u = urhk_SetOrbOut(sv_t)
   		GOTO ENDSCRIPT
	#}
	ENDIF



	################################ Variable Inititalization#######################################################
	print(BANCS.INPUT.funcCode)
	BANCS.INPUT.requestId=" "
	print(BANCS.INPUT.requestId)
	print(BANCS.INPUT.acctNo)
	print(BANCS.INPUT.noOfChqs)
	print(BANCS.INPUT.leaves)

	BANCS.INPUT.reqStat= "E"

	call("cchqmvat.scr")
	sv_z = urhk_SetOrbOut("SuccessOrFailure|Success")



ENDSCRIPT:

trace off

end-->

