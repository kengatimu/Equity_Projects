############################################################################################################									
# Source Name						: rmdfltpopmn004.scr
# Date								: 28-02-2013
# Description						: This script is used to get the CIF_ID.
# Input								: None
# Output							: To get populate the the CIF_ID.
# Called Script						: None
# Calling Script					: None
# Author     						: Shilpa
# Bank								:
# Modification History				:
#		< Version No.> 			<Date>		<Author Name> 		<Description>
#		 0.1					28-02-2013 	Shilpa 			Original Version
############################################################################################################

<--START
CHECKSUM="776986d8e23d36d393415273c2d291a492e14168b0fcf8283e78fc2ba36347ab"
	TRACE ON
	
	################################################
	# Custom repository creation
	################################################
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST","ORM") == 0) THEN
	#{
		CREATECLASS("CUST","ORM",5)
	#}
	ENDIF
	
	print(BANCS.INPUT.oprAcct)
	CUST.ORM.oprAcct= BANCS.INPUT.oprAcct
	PRINT(CUST.ORM.oprAcct)
	
	IF (CUST.ORM.oprAcct!="") THEN
	#{
		sv_q = "cifId|SELECT CIF_ID"
		sv_q = sv_q + " FROM TBAADM.GAM "
		sv_q = sv_q + " WHERE del_flg='N' "
		sv_q = sv_q + " AND acct_cls_flg='N' "
		sv_q = sv_q + " AND bank_id= ?SVAR "
		sv_q = sv_q + " AND entity_cre_flg='Y' "
		sv_q = sv_q + " AND foracid = ?SVAR "
		
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + CUST.ORM.oprAcct
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_u = urhk_dbSelectWithBind(sv_q)
		PRINT(sv_u)
		IF (sv_u == 0 ) THEN
		#{
			PRINT(BANCS.OUTPARAM.cifId)
			CUST.ORM.cifId= BANCS.OUTPARAM.cifId
			PRINT(CUST.ORM.cifId)
			#}
		ELSE
			#{
			sv_g = urhk_SetOrbOut("cifId| ")
			GOTO FINISH
		#}
		ENDIF
		
		IF (CUST.ORM.cifId == "") THEN
		#{
			CUST.ORM.cifId = " "
		#}
		ENDIF
		sv_g = urhk_SetOrbOut("cifId|"+CUST.ORM.cifId)
	#}
	ENDIF
	FINISH:
		TRACE OFF
		EXITSCRIPT
END-->
