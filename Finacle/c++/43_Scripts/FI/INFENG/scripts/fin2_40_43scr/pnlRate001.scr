#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: pnlRate001.scr
# Type: NA|default
# Description: OD Penalty Script
# Author: Anshul_Singh04
# Date: 12/4/18 10:40 AM
# Modification Log:
#============================================================

<--START
CHECKSUM="271f6668907e99bdf99fc7a7e385a999f22e0a4e252dcce3688304938e878e68"
	# Script body goes here
	TRACE ON
	
	#	sv_u = urhk_B2K_PrintRepos("BANCS")
	#	PRINT(sv_u)
	
	sv_a = REPEXISTS("EQT")
	IF (sv_a == 0) THEN
	#{
		CREATEREP("EQT")
	#}
	ENDIF
	sv_a = CLASSEXISTS("EQT", "ODP")
	IF (sv_a == 0 ) THEN
	#{
		CREATECLASS("EQT", "ODP", 5)
	#}
	ENDIF
	EQT.ODP.successOrFailure=""
	EQT.ODP.queryTbl= " "
	EQT.ODP.delFlg = "N"
	EQT.ODP.funcCode=TRIM(BANCS.INPUT.funcCode)
	PRINT(EQT.ODP.funcCode)
	PRINT(BANCS.INPUT.levelType)
	BANCS.INPUT.levelType = TOUPPER(TRIM(BANCS.INPUT.levelType))
	EQT.ODP.levelType =  BANCS.INPUT.levelType
	
	EQT.ODP.levelId = TOUPPER(TRIM(BANCS.INPUT.levelId))
	print(EQT.ODP.levelId)
	
	sv_a=0
	sv_b=0
	sv_c=" "
	sv_d=0
	sv_f=0
	
	sv_m = "recCountMod|SELECT count(1)"
	sv_m= sv_m +" FROM CUSTOM.C_PPT_MOD WHERE LEVEL_TYPE = ?SVAR and LEVEL_ID = ?SVAR and bank_id=?SVAR"
	
	BANCS.INPARAM.BINDVARS =  EQT.ODP.levelType + "|" + EQT.ODP.levelId + "|" + BANCS.STDIN.contextBankId
	
	PRINT(sv_m)
	sv_r = urhk_dbSelectWithBind(sv_m)
	
	IF (sv_r != 0) THEN
	#{
		sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
		sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		sv_d=BANCS.OUTPARAM.recCountMod
		PRINT(sv_d)
		IF(sv_d!=0)THEN
		#{
			sv_f=1
		#}
		ENDIF
	#}
	ENDIF
	
	IF(sv_f==1)THEN
	#{
		IF((EQT.ODP.funcCode=="D") OR (EQT.ODP.funcCode=="U"))THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Record Pending for Verification for the Selected Level Id.^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	IF((sv_d==0) AND (EQT.ODP.funcCode=="V"))THEN
	#{
		sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
		sv_t= urhk_SetOrbOut("Error_1|ERR^No Record Pending for Verification for the Selected Level Id.^Go")
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	IF((sv_d==0) AND (EQT.ODP.funcCode=="X"))THEN
	#{
		sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
		sv_t= urhk_SetOrbOut("Error_1|ERR^No Record Modified for the Selected Level Id!^Go")
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	IF( (EQT.ODP.funcCode=="M") OR (EQT.ODP.funcCode=="D") OR (EQT.ODP.funcCode=="U") )THEN
	#{
		sv_m = "delFlg|SELECT nvl(max(DEL_FLG),0)"
		sv_m= sv_m +" FROM CUSTOM.C_PPT WHERE LEVEL_TYPE = ?SVAR and LEVEL_ID = ?SVAR and bank_id=?SVAR "
		
		BANCS.INPARAM.BINDVARS = EQT.ODP.levelType + "|" + EQT.ODP.levelId + "|" + BANCS.STDIN.contextBankId
		
		PRINT(sv_m)
		sv_r = urhk_dbselectWithBind(sv_m)
		
		IF (sv_r != 0) THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			EQT.ODP.delFlg= BANCS.OUTPARAM.delFlg
			PRINT(EQT.ODP.delFlg)
		#}
		ENDIF
		
		IF((EQT.ODP.delFlg=="Y") AND (EQT.ODP.funcCode !="U"))THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Record is Deleted! Only Inquiry is Allowed.^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		IF((EQT.ODP.delFlg=="N") AND (EQT.ODP.funcCode =="U"))THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Record is Not Deleted!^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		IF(EQT.ODP.delFlg=="0")THEN
		#{
			IF(sv_f==0)THEN
			#{
				sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
				sv_t= urhk_SetOrbOut("Error_1|ERR^No Record Exists for the Selected Level Id.^Go")
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	sv_m = "recCount|SELECT count(1)"
	sv_m= sv_m +" FROM CUSTOM.C_PPT WHERE LEVEL_TYPE = ?SVAR and LEVEL_ID = ?SVAR and bank_id=?SVAR "
	
	BANCS.INPARAM.BINDVARS = EQT.ODP.levelType + "|" + EQT.ODP.levelId + "|" + BANCS.STDIN.contextBankId
	
	PRINT(sv_m)
	sv_r = urhk_dbselectWithBind(sv_m)
	
	IF (sv_r != 0) THEN
	#{
		sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
		sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		sv_a= BANCS.OUTPARAM.recCount
		PRINT(sv_a)
	#}
	ENDIF
	
	
	
	IF( (EQT.ODP.funcCode=="M") OR (EQT.ODP.funcCode=="X") OR (EQT.ODP.funcCode=="V") OR (EQT.ODP.funcCode=="D") OR (EQT.ODP.funcCode=="U"))THEN
	#{
		IF((sv_a==0) AND (sv_d== 0))THEN
		#{
			PRINT(sv_d)
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^No Record Exists for the Selected Level Id.^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		IF((sv_a==0) AND (sv_d!= 0) AND (EQT.ODP.funcCode=="D"))THEN
		#{
			PRINT(sv_d)
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Cannot Delete the Record. Verification is Pending !!!^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		IF((sv_a!=0) AND (sv_d== 0))THEN
		#{
			GOTO FETCHDATA
		#}
		ENDIF
		
		sv_k = "lchgUserID|SELECT distinct LCHG_USER_ID"
		sv_k= sv_k +" FROM CUSTOM.C_PPT_MOD WHERE LEVEL_TYPE = ?SVAR and LEVEL_ID = ?SVAR and bank_id=?SVAR "
		
		BANCS.INPARAM.BINDVARS = EQT.ODP.levelType + "|" + EQT.ODP.levelId + "|" + BANCS.STDIN.contextBankId
		
		PRINT(sv_k)
		sv_r = urhk_dbselectWithBind(sv_k)
		
		IF (sv_r != 0) THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data!^Go")
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			EQT.ODP.lchgUserID= BANCS.OUTPARAM.lchgUserID
			PRINT(EQT.ODP.lchgUserID)
			
			IF((sv_d!=0) AND (EQT.ODP.funcCode=="V") AND (EQT.ODP.lchgUserID==BANCS.STDIN.userId))THEN
			#{
				sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
				sv_t= urhk_SetOrbOut("Error_1|ERR^Enterer cannot verify the Record !!!^Go")
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			
			IF((sv_d!=0)AND (EQT.ODP.funcCode!="V")) THEN
			#{
				IF(EQT.ODP.lchgUserID!=BANCS.STDIN.userId)THEN
				#{
					sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
					sv_t= urhk_SetOrbOut("Error_1|ERR^Only Enterer can Modify/Cancel the Record.^Go")
					GOTO ENDOFSCRIPT
					#}
				ELSE
					#{
					EQT.ODP.queryTbl="_MOD"
					GOTO FETCHDATA
				#}
				ENDIF
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	IF((sv_a==0) AND (EQT.ODP.funcCode=="M") AND (sv_d!=0))THEN
	#{
		EQT.ODP.queryTbl="_MOD"
	#}
	ENDIF
	
	IF( (sv_a==0)) THEN
	#{
		IF((EQT.ODP.funcCode=="I")) THEN
		#{
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^No Record Exists for the Selected Level Id!^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	IF(EQT.ODP.funcCode=="A")THEN
	#{
		IF(sv_a!= 0)THEN
		#{
			PRINT(sv_a)
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Record Already Exists for the Selected Level Id!^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		IF(sv_d!= 0)THEN
		#{
			PRINT(sv_d)
			sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_t= urhk_SetOrbOut("Error_1|ERR^Record Already Present. Verification Pending !!!^Go")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	FETCHDATA:
		CALL("pnlRate003.scr")
		IF (EQT.ODP.successOrFailure!= "F") THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
			#}
		ELSE
			#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			sv_u = urhk_SetOrbOut("Error_1|ERR^"+EQT.ODP.errorMsg+"!!!^Go")
		#}
		ENDIF
		
	ENDOFSCRIPT:
		DELETECLASS("EQT", "ODP")
		DELETEREP("EQT")
		
		EXITSCRIPT
		TRACE OFF
END-->

