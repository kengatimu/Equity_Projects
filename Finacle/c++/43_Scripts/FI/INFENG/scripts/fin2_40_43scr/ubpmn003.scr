#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# product: Finacle Core
# category: User Defined
# script: ubpmn003.scr
# type: NA|default
# description: These types of scripts do not belong to any particular category and are standalone scripts with no script specific repositories or fields.
#============================================================

<--START
CHECKSUM="580a2273da2016b7b15cc0126fdd989eff3921325918e1f52ba150aaa54bfce4"
	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpmn003.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called on click og GO button for REVERSAL mode.
	# Called Script         :		ubpdp007.scr
	# Calling Script        :		ubpdp015.scr
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           16-04-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	#========================================================
	# Check whether CUST Repository exists else create it
	#========================================================
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	#========================================================
	# Check whether UBP Class exists else create it
	#========================================================
	sv_r = CLASSEXISTS("CUST","UBP")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","UBP",5)
	#}
	ENDIF
	CUST.UBP.errorFlg="N"
	CUST.UBP.payAcctId=" "
	CUST.UBP.billAcctId=" "
	CUST.UBP.chgEvetId=" "
	CUST.UBP.typOfServ=" "
	CUST.UBP.valType=" "

	PRINT(BANCS.INPUT.funcCode)

	IF(FIELDEXISTS(BANCS.INPUT.payRefNum))THEN
	#{
		IF(TRIM(BANCS.INPUT.payRefNum) != "")THEN
		#{
			CUST.UBP.payRefNum=BANCS.INPUT.payRefNum
			PRINT(CUST.UBP.payRefNum)
			#}
		ELSE
			#{
			CUST.UBP.errorFlg = "Y"
			CUST.UBP.errorMsg = "Enter a Bill Payment Ref. Num"
			CUST.UBP.errorFocus="random"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.errorFlg = "Y"
		CUST.UBP.errorMsg = "Enter a Bill Payment Ref. Num"
		CUST.UBP.errorFocus="random"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	IF ((BANCS.STDIN.contextBankId == "54") OR (BANCS.STDIN.contextBankId == "43")) THEN
	#{
		
		#{
		IF(FIELDEXISTS(BANCS.INPUT.AcctNum))THEN
		#{
			IF(TRIM(BANCS.INPUT.AcctNum) != "")THEN
			#{
				CUST.UBP.AcctNum=BANCS.INPUT.AcctNum
				PRINT(CUST.UBP.AcctNum)
				#}
			ELSE
				#{
				CUST.UBP.errorFlg = "Y"
				CUST.UBP.errorMsg = "Enter credit Account No."
				CUST.UBP.errorFocus="random"
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		
		CUST.UBP.Superagent = MID$(CUST.UBP.AcctNum,3,13)
		print (CUST.UBP.Superagent)
		IF (CUST.UBP.Superagent=="3000103216" ) THEN
		#{
			IF (BANCS.STDIN.userWorkClass != "310") THEN
			#{
				CUST.UBP.errorFlg = "Y"
				CUST.UBP.errorMsg = "Only worklass 310 is allowed for reversal ON this account"
				CUST.UBP.errorFocus="random"
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		
		IF (CUST.UBP.AcctNum == "0810297617335") THEN
		#{
			sv_b = BANCS.STDIN.homeSolId
			print(BANCS.STDIN.homeSolId)
			IF ( BANCS.STDIN.homeSolId != "081" ) THEN
			#{
				CUST.UBP.errorFlg = "Y"
				CUST.UBP.errorMsg = "Reversal ON this account is allowed only in sol 081, Contact Equity Center Branch"
				CUST.UBP.errorFocus="random"
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		
		IF (CUST.UBP.AcctNum == "0750294239156") THEN
		#{
			sv_b = BANCS.STDIN.homeSolId
			print(BANCS.STDIN.homeSolId)
			IF ( BANCS.STDIN.homeSolId != "075" ) THEN
			#{
				CUST.UBP.errorFlg = "Y"
				CUST.UBP.errorMsg = "Reversal ON this account is allowed only in sol 075, Contact Bondo Branch"
				CUST.UBP.errorFocus="random"
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	
	#Added
	#Added
	IF(FIELDEXISTS(BANCS.INPUT.billerId))THEN
	#{
		IF(TRIM(BANCS.INPUT.billerId) != "")THEN
		#{
			CUST.UBP.billerId=TOUPPER(TRIM(BANCS.INPUT.billerId))
			PRINT(CUST.UBP.billerId)
			BANCS.INPARAM.BINDVARS = CUST.UBP.payRefNum+"|"+CUST.UBP.billerId
			sv_s = "count3|"
			sv_s = sv_s + " SELECT COUNT(3)"
			IF(BANCS.INPUT.funcCode == "R")THEN
			#{
				sv_s = sv_s + " FROM CUSTOM.C_UBP_DETAILS"
			#}
			ELSE
			#{
				sv_s = sv_s + " FROM CUSTOM.C_UBP_DETAILS_MOD"
			#}
			ENDIF	
			sv_s = sv_s + " WHERE BILL_PAY_REF_NUM=?SVAR"
			sv_s = sv_s + " AND BILLER_ID=?SVAR"
			PRINT(sv_s)
			sv_d = urhk_dbSelectWithBind(sv_s)
			print(sv_d)
			IF(sv_d==0)THEN
			#{
				CUST.UBP.count3=BANCS.OUTPARAM.count3
				IF(CUST.UBP.count3==0)THEN
				#{
					CUST.UBP.errorFlg = "Y"
					#CUST.UBP.errorMsg = "BILLER ID entered doesn't match Bill Pay Ref Num"
					CUST.UBP.errorCode = "UBP_ERR053"
					CALL("ubpdp044.scr")
					CUST.UBP.errorMsg =CUST.UBP.errorDesc
					CUST.UBP.errorFocus="billerId"
					GOTO ENDOFSCRIPT
					
				#}
				ENDIF
			#}
			ENDIF
			#}
		ELSE
			#{
			CUST.UBP.billerId=""
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.UBP.billerId=""
	#}
	ENDIF
	BANCS.INPARAM.BINDVARS = CUST.UBP.payRefNum+"|N"
	sv_s = "payAcctId,chgEvetId,billAcctId,billerId,typOfServ,valType|"
	sv_s = sv_s + " SELECT PAYER_ACCT_ID,CHARGE_EVNT_ID,BILLER_ACCT_ID,BILLER_ID,TYP_OF_SERV,VAL_TYPE"
	#sv_s = sv_s + " FROM CUSTOM.C_UBP_DETAILS "
	IF(BANCS.INPUT.funcCode == "R")THEN
	#{
		sv_s = sv_s + " FROM CUSTOM.C_UBP_DETAILS"
	#}
	ELSE
	#{
		sv_s = sv_s + " FROM CUSTOM.C_UBP_DETAILS_MOD"
	#}
	ENDIF	
	sv_s = sv_s + " WHERE BILL_PAY_REF_NUM=?SVAR"
	sv_s = sv_s + " AND REVERSE_FLG = ?SVAR"
	PRINT(sv_s)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d!=0)THEN
	#{
		CUST.UBP.errorFlg = "Y"
		#CUST.UBP.errorMsg = "Error in fetching details from UBP table"
		CUST.UBP.errorCode = "UBP_ERR032"
		CALL("ubpdp044.scr")
		CUST.UBP.errorMsg =CUST.UBP.errorDesc
		CUST.UBP.errorFocus="random"
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		CUST.UBP.payAcctId=BANCS.OUTPARAM.payAcctId
		PRINT(CUST.UBP.payAcctId)
		CUST.UBP.chgEvetId=BANCS.OUTPARAM.chgEvetId
		PRINT(CUST.UBP.chgEvetId)
		CUST.UBP.billAcctId=BANCS.OUTPARAM.billAcctId
		PRINT(CUST.UBP.billAcctId)
		CUST.UBP.billerId=BANCS.OUTPARAM.billerId
		PRINT(CUST.UBP.billerId)
		CUST.UBP.typOfServ=BANCS.OUTPARAM.typOfServ
		PRINT(CUST.UBP.typOfServ)
		CUST.UBP.valType=BANCS.OUTPARAM.valType
		PRINT(CUST.UBP.valType)
		
	#}
	ENDIF
	IF(CUST.UBP.chgEvetId=="")THEN
	#{
		
		CUST.UBP.chgEvetId=" "
	#}
	ENDIF
	IF(CUST.UBP.payAcctId=="")THEN
	#{
		
		CUST.UBP.payAcctId=" "
	#}
	ENDIF
	IF(CUST.UBP.billAcctId=="")THEN
	#{
		
		CUST.UBP.billAcctId=" "
	#}
	ENDIF
	IF(CUST.UBP.billerId=="")THEN
	#{
		
		CUST.UBP.billerId=" "
	#}
	ENDIF
	IF(CUST.UBP.typOfServ=="")THEN
	#{
		CUST.UBP.typOfServ=" "
	#}
	ENDIF
	IF(CUST.UBP.valType=="")THEN
	#{
		CUST.UBP.valType=" "
	#}
	ENDIF
	ENDOFSCRIPT:
		IF(CUST.UBP.errorFlg=="Y")THEN
		#{
			sv_u = urhk_SetOrbOut("Error_1|Error^"+CUST.UBP.errorMsg+"^"+CUST.UBP.errorFocus)
			PRINT(sv_u)
			sv_b=urhk_SetOrbOut("SuccessOrFailure|N")
			#}
		ELSE
			#{
			sv_b=urhk_SetOrbOut("payAcctId|"+CUST.UBP.payAcctId)
			sv_b=urhk_SetOrbOut("billAcctId|"+CUST.UBP.billAcctId)
			sv_b=urhk_SetOrbOut("chgEvetId|"+CUST.UBP.chgEvetId)
			sv_b=urhk_SetOrbOut("billerId|"+CUST.UBP.billerId)
			sv_b=urhk_SetOrbOut("payRefNum|"+CUST.UBP.payRefNum)
			sv_b=urhk_SetOrbOut("typOfServ|"+CUST.UBP.typOfServ)
			sv_b=urhk_SetOrbOut("valType|"+CUST.UBP.valType)
			sv_b=urhk_SetOrbOut("billerId|"+CUST.UBP.billerId)
			
			sv_b=urhk_SetOrbOut("SuccessOrFailure|Y")
		#}
		ENDIF
		EXITSCRIPT
END-->
