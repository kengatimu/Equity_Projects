#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: SRV_CancelDisbData_post_process_data.scr
# Type: NA|default
# Description: This script is used for calling custom script to perform custom table delete operation during Cancel operation
# Author:
# Date:
# Modification Log:
#============================================================

<--START
CHECKSUM="7264c873a257b16f3e875f4fafb897ade5dcf56877af84bc19c3fd7c340a064b"
	TRACE ON
	IF(FIELDEXISTS(BANCS.STDIN.menuId))THEN
	#{
		IF(BANCS.STDIN.menuId == "HLADISB")THEN
		#{
			sv_r = urhk_B2k_PrintRepos("BANCS")
			PRINT(sv_r)
			sv_a = ONS.CUSTOM.custDataIn
			PRINT(sv_a)
			sv_u = urhk_ParseONSCustData(sv_a)
			PRINT(sv_u)
			sv_r = urhk_B2k_PrintRepos("BANCS")
			PRINT(sv_r)
			
			PRINT(BANCS.INPUT.ladisb_main_FIELD_5)
			IF(BANCS.INPUT.ladisb_main_FIELD_5 == "Y") THEN
			#{
				CALL("DelRecForCancel.scr")
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	EXITSCRIPT
END-->
