#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: comsnCommCode.scr
# Type: NA|default
# Description: comsnCommCode.scr
# Author: shivakrishna.d
# Date: 6/18/18 7:20 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="c4fe2f49c1bc1c855591cd1a893452d960791482d2f921431bd8bfa837338490"
	# Script body goes here
	TRACE ON
	
	#sv_u = urhk_B2K_PrintRepos("BANCS")
	#PRINT(sv_u)
	
	###########################################################################################
	# Check whether EQT Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("EQT")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("EQT")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether COMM Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("EQT","COMM")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("EQT","COMM",5)
	#}
	ENDIF
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	EQT.COMM.commCode = ""
	EQT.COMM.commDesc = " "
	EQT.COMM.crncyCode = ""
	EQT.COMM.recordCount ="0"
	EQT.COMM.qryTbl =" "
	
	IF((BANCS.INPUT.funcCode=="V") OR (BANCS.INPUT.funcCode=="X"))THEN
	#{
		EQT.COMM.qryTbl ="_MOD"
	#}
	ENDIF
	
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
	
	sv_s = "recordCount|SELECT count(1) FROM CUSTOM.C_COMM"+EQT.COMM.qryTbl
	sv_s = sv_s + " WHERE bank_id = ?SVAR"
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	IF(sv_d == 0)THEN
	#{
		EQT.COMM.recordCount = BANCS.OUTPARAM.recordCount
		PRINT(EQT.COMM.recordCount)
	#}
	ENDIF
	
	sv_u = urhk_SetOrbOut("recordCount|"+EQT.COMM.recordCount)
	PRINT(sv_u)
	
	sv_s = "commCode,commDesc,crncyCode,delFlg|"
	
	IF(EQT.COMM.qryTbl =="_MOD") THEN
	#{
		sv_s = sv_s + " SELECT COMM_CODE, COMM_DESC, CRNCY_CODE, 'N' FROM CUSTOM.C_COMM_MOD"
		#}
	ELSE
		#{
		sv_s = sv_s + " SELECT COMM_CODE, COMM_DESC, CRNCY_CODE,DEL_FLG FROM CUSTOM.C_COMM"
	#}
	ENDIF
	
	sv_s = sv_s + " WHERE bank_id = ?SVAR ORDER BY COMM_CODE"
	PRINT(sv_s)
	
	sv_c = urhk_dbCursorOpenWithBind(sv_s)
	PRINT(sv_c)
	
	IF(sv_c != 0)THEN
	#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_n = BANCS.OUTPARAM.DB_CURSOR_NUMBER
	PRINT(sv_n)
	
	sv_f = urhk_dbCursorFetch(sv_n)
	PRINT(sv_f)
	
	sv_i = 1
	
	WHILE(sv_f == 0)
	#{
		EQT.COMM.commCode = BANCS.OUTPARAM.commCode
		EQT.COMM.commDesc = BANCS.OUTPARAM.commDesc
		EQT.COMM.crncyCode = BANCS.OUTPARAM.crncyCode
		EQT.COMM.delFlg = BANCS.OUTPARAM.delFlg
		
		PRINT(EQT.COMM.commCode)
		PRINT(EQT.COMM.commDesc)
		PRINT(EQT.COMM.crncyCode)
		PRINT(EQT.COMM.delFlg)
		
		#####################################################################
		# Code To Replace Special Character ' before setting value in Searcher
		#####################################################################
		
		lv_a = EQT.COMM.commDesc
		
		lv_l = STRLEN(lv_a)
		lv_n = 0
		lv_s = ""
		WHILE(lv_n < lv_l)
		#{
			PRINT(lv_a)
			PRINT(lv_n)
			lv_c = ""
			lv_c = MID$(lv_a,lv_n,1)
			PRINT(lv_c)
			
			IF(lv_c == "'")THEN
			#{
				PRINT("Found One")
				lv_s = lv_s + ""
				PRINT(lv_s)
				GOTO NEXT_CHAR1
			#}
			ENDIF
			lv_s = lv_s + lv_c
			PRINT(lv_s)
			
			NEXT_CHAR1:
				
				lv_n = lv_n + 1
			#}
			DO
			
			PRINT(lv_a)
			PRINT(lv_s)
			
			EQT.COMM.commDesc=lv_s
			
			#####################################################################
			
			sv_s = "commCode_" + Format$(sv_i,"%d") + "|" + EQT.COMM.commCode
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			
			sv_s = "commDesc_" + Format$(sv_i,"%d") + "|" + EQT.COMM.commDesc
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			
			sv_s = "crncyCode_" + Format$(sv_i,"%d") + "|" + EQT.COMM.crncyCode
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			
			sv_s = "delFlg_" + Format$(sv_i,"%d") + "|" + EQT.COMM.delFlg
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			
			sv_i = sv_i + 1
			PRINT(sv_i)
			
			PRINT(sv_n)
			sv_f = urhk_dbCursorFetch(sv_n)
			PRINT(sv_f)
			
		#}
		DO
		
		sv_c = urhk_dbCursorClose(sv_n)
		PRINT(sv_c)
		
		
	ENDOFSCRIPT:
		DELETECLASS("EQT", "COMM")
		DELETEREP("EQT")
		
		EXITSCRIPT
		TRACE OFF
END-->
