#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: fetchcomdtl.scr
# Type: NA|default
# Description: This script is for fetching commission details
# Author: shivakrishna.d
# Date: 6/27/18 5:35 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="81d6872388bca59c95c29f1d2d5e82603a9f8b0510e9ad7908aafdba13bbe30a"
	
	# Script body goes here
	TRACE ON
	sv_r=urhk_B2k_PrintRepos("BANCS")
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether COMMS Class exists else create it
	###########################################################################################
	sv_c = CLASSEXISTS("CUST", "COMMS")
	IF(sv_c == 0) THEN
	#{
		CREATECLASS("CUST", "COMMS", 5)
	#}
	ENDIF
	
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	CUST.COMMS.errorFlg = "N"
	CUST.COMMS.errorMsg = " "
	CUST.COMMS.lcOrBgType = ""
	CUST.COMMS.lcOrBgNumber = ""
	CUST.COMMS.funcCode = ""
	CUST.COMMS.commfreq = " "
	CUST.COMMS.commdate = " "
	CUST.COMMS.commcode = " "
	CUST.COMMS.commflg = " "
	
	
	
	IF(FIELDEXISTS(BANCS.INPUT.funccode))THEN
	#{
		IF(BANCS.INPUT.funccode != "")THEN
		#{
			CUST.COMMS.funcCode = BANCS.INPUT.funccode
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.lcOrBgNumber))THEN
	#{
		IF (BANCS.INPUT.lcOrBgNumber != "")THEN
		#{
			CUST.COMMS.lcOrBgNumber = BANCS.INPUT.lcOrBgNumber
			PRINT(CUST.COMMS.lcOrBgNumber)
		#}
		ENDIF
	#}
	ENDIF
	
	
	IF((CUST.COMMS.funcCode != "C") OR (CUST.COMMS.funcCode!="S")) THEN
	#{
		sv_s = "commfreq,commdate,commcode,commflg| SELECT COMM_FREQ,to_char(NEXT_COMM_DATE,'DD-MM-YYYY'),COMM_CODE,COMM_FLG FROM CUSTOM.C_COMM_MAINT WHERE ENTITY_CRE_FLG='Y' AND LC_OR_BG_NUMBER=?SVAR AND BANK_ID= ?SVAR"
		BANCS.INPARAM.BINDVARS = CUST.COMMS.lcOrBgNumber + "|" + BANCS.STDIN.contextBankId
		PRINT(sv_s)
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_q = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_q)
		IF(sv_q == 0) THEN
		#{
			CUST.COMMS.commfreq = BANCS.OUTPARAM.commfreq
			PRINT(CUST.COMMS.commfreq)
			CUST.COMMS.commdate = BANCS.OUTPARAM.commdate
			PRINT(CUST.COMMS.commdate)
			CUST.COMMS.commcode = BANCS.OUTPARAM.commcode
			PRINT(CUST.COMMS.commcode)
			CUST.COMMS.commflg = BANCS.OUTPARAM.commflg
			PRINT(CUST.COMMS.commflg)
			
			#}
		ELSE
			#{
			
			CUST.COMMS.errorFlg = "Y"
			CUST.COMMS.errorMsg = " "
		#}
		ENDIF
		
	#}
	ENDIF
	
	IF((CUST.COMMS.funcCode == "M") OR (CUST.COMMS.funcCode =="V") OR (CUST.COMMS.funcCode == "X")) THEN
	#{
		sv_s = "commfreq,commdate,commcode,commflg| SELECT COMM_FREQ,to_char(NEXT_COMM_DATE,'DD-MM-YYYY'),COMM_CODE,COMM_FLG FROM CUSTOM.C_COMM_MAINT_MOD WHERE LC_OR_BG_NUMBER=?SVAR AND BANK_ID= ?SVAR"
		BANCS.INPARAM.BINDVARS = CUST.COMMS.lcOrBgNumber + "|" + BANCS.STDIN.contextBankId
		PRINT(sv_s)
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_q = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_q)
		IF(sv_q == 0) THEN
		#{
			CUST.COMMS.commfreq = BANCS.OUTPARAM.commfreq
			PRINT(CUST.COMMS.commfreq)
			CUST.COMMS.commdate = BANCS.OUTPARAM.commdate
			PRINT(CUST.COMMS.commdate)
			CUST.COMMS.commcode = BANCS.OUTPARAM.commcode
			PRINT(CUST.COMMS.commcode)
			CUST.COMMS.commflg = BANCS.OUTPARAM.commflg
			PRINT(CUST.COMMS.commflg)
			#}
		ELSE
			#{
			sv_s = "commfreq,commdate,commcode,commflg| SELECT COMM_FREQ,to_char(NEXT_COMM_DATE,'DD-MM-YYYY'),COMM_CODE,COMM_FLG FROM CUSTOM.C_COMM_MAINT WHERE LC_OR_BG_NUMBER=?SVAR AND BANK_ID= ?SVAR"
			BANCS.INPARAM.BINDVARS = CUST.COMMS.lcOrBgNumber + "|" + BANCS.STDIN.contextBankId
			PRINT(sv_s)
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_q = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_q)
			IF(sv_q == 0) THEN
			#{
				CUST.COMMS.commfreq = BANCS.OUTPARAM.commfreq
				PRINT(CUST.COMMS.commfreq)
				CUST.COMMS.commdate = BANCS.OUTPARAM.commdate
				PRINT(CUST.COMMS.commdate)
				CUST.COMMS.commcode = BANCS.OUTPARAM.commcode
				PRINT(CUST.COMMS.commcode)
				CUST.COMMS.commflg = BANCS.OUTPARAM.commflg
				PRINT(CUST.COMMS.commflg)
				#}
			ELSE
				#{
				CUST.COMMS.errorFlg = "Y"
				CUST.COMMS.errorMsg = " "
			#}
			ENDIF
		#}
		ENDIF
		
	#}
	ENDIF
	
	IF(CUST.COMMS.commcode == "")THEN
	#{
		CUST.COMMS.commcode = " "
	#}
	ENDIF
	IF(CUST.COMMS.commfreq == "")THEN
	#{
		CUST.COMMS.commfreq = " "
	#}
	ENDIF
	IF(CUST.COMMS.commdate == "")THEN
	#{
		CUST.COMMS.commdate = " "
	#}
	ENDIF
	IF(CUST.COMMS.commflg == "")THEN
	#{
		CUST.COMMS.commflg = " "
	#}
	ENDIF
	sv_u = urhk_SetOrbOut("errMsg|"+ CUST.COMMS.errorMsg)
	sv_u = urhk_SetOrbOut("errFlg|"+ CUST.COMMS.errorFlg)
	sv_u = urhk_SetOrbOut("commcode|"+ CUST.COMMS.commcode)
	sv_u = urhk_SetOrbOut("commfreq|"+ CUST.COMMS.commfreq)
	sv_u = urhk_SetOrbOut("commdate|"+ CUST.COMMS.commdate)
	sv_u = urhk_SetOrbOut("commflg|"+ CUST.COMMS.commflg)
	
	
	
	TRACE OFF
END-->
