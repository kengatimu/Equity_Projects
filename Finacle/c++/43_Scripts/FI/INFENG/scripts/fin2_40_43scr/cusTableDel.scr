#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cusTableDel.scr
# Type: NA|default
# Description: This script is used for deleting records when we click on 'Cance' button
# Author:
# Date:
# Modification Log:
#============================================================

<--START
CHECKSUM="e6ac37654488177c7484c23102f3ab281871b761ab6c3bdd82c3459d509a2453"
	TRACE ON
	
	sv_a = REPEXISTS("CUST")
	IF (sv_a == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	sv_a = CLASSEXISTS("CUST", "LADISB")
	IF (sv_a == 0 ) THEN
	#{
		CREATECLASS("CUST", "LADISB", 5)
	#}
	ENDIF
	
	CUST.LADISB.errFlg = " "
	CUST.LADISB.errMsg = " "
	
	sv_u = urhk_b2k_printrepos("BANCS")
	CUST.LADISB.LoanAcctNum = BANCS.INPUT.acctNum
	CUST.LADISB.funcCode = BANCS.INPUT.funcCode
	
	sv_s = "recCnt|select count(1) from CUSTOM.C_LADISB_PAYOFF_OLDLOAN where LOAN_FORACID = ?SVAR AND bank_id = ?SVAR AND payoff_flg='N' AND entity_cre_flg = 'N'"
	BANCS.INPARAM.BINDVARS = CUST.LADISB.LoanAcctNum + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	IF(sv_d == 0)THEN
	#{
		PRINT(BANCS.OUTPARAM.recCnt)
		CUST.LADISB.recCnt = BANCS.OUTPARAM.recCnt
		PRINT(CUST.LADISB.recCnt)
		#}
	ELSE
		#{
		CUST.LADISB.recCnt = 0
		CUST.LADISB.errFlg = "E"
		CUST.LADISB.errMSg ="Error in DB operation!"
		EXITSCRIPT
	#}
	ENDIF
	
	IF ((CUST.LADISB.recCnt > 0) AND (CUST.LADISB.funcCode == "D")) THEN
	#{
		sv_p = ""
		sv_p = "DELETE CUSTOM.C_LADISB_PAYOFF_OLDLOAN where LOAN_FORACID = ?SVAR AND bank_id = ?SVAR AND payoff_flg='N' AND entity_cre_flg='N'"
		BANCS.INPARAM.BINDVARS = CUST.LADISB.LoanAcctNum + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_q = urhk_dbSQLWithBind(sv_p)
		PRINT(sv_q)
	#}
	ENDIF
	
	IF ((CUST.LADISB.recCnt > 0) AND ((CUST.LADISB.funcCode == "X") OR (CUST.LADISB.funcCode == "V"))) THEN
	#{
		sv_p = ""
		sv_p = "UPDATE CUSTOM.C_LADISB_PAYOFF_OLDLOAN set funccode='D' where LOAN_FORACID = ?SVAR AND bank_id = ?SVAR AND payoff_flg='N' AND entity_cre_flg='N'"
		BANCS.INPARAM.BINDVARS = CUST.LADISB.LoanAcctNum + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_q = urhk_dbSQLWithBind(sv_p)
		PRINT(sv_q)
	#}
	ENDIF
	
	IF(CUST.LADISB.recCnt > 0) THEN
	#{
		IF(sv_q == 0)THEN
		#{
			sv_d = urhk_dbSqlWithBind("COMMIT")
			PRINT(sv_d)
			#}
		ELSE
			#{
			CUST.LADISB.errFlg = "E"
			CUST.LADISB.errMSg = "Error in updating the record"
		#}
		ENDIF
	#}
	ENDIF
	
	sv_u = urhk_SetOrbOut("errorFlg|"+CUST.LADISB.errFlg)
	sv_u = urhk_SetOrbOut("errorMsg|"+CUST.LADISB.errMsg)
	
	TRACE OFF
END-->
