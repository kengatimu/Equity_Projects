#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: commcode_val.scr
# Type: NA|default
# Description: commcode_val.scr
# Author: nagamani.boddu
# Date: 6/26/18 2:06 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="55d985a0d35ceafad16b2f300e264a85e0e0f5c38d76f11fcaa1aaf0333fb056"
	
	# Script body goes here
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	###########################################################################################
	# Check whether DATA Calss exists else create it
	###########################################################################################
	sv_r = CLASSEXISTS("CUST","DATA")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","DATA",5)
	#}
	ENDIF
	
	###########################################################################################
	#Initialize the variables
	###########################################################################################
	CUST.DATA.reccount = "0"
	CUST.DATA.errorFlg = "N"
	CUST.DATA.errorMsg = " "
	CUST.DATA.commcode = " "
	IF(FIELDEXISTS(BANCS.INPUT.commcode))THEN
	#{
		IF(TRIM(BANCS.INPUT.commcode) != "")THEN
		#{
			CUST.DATA.commcode = TOUPPER(BANCS.INPUT.commcode)
			PRINT(CUST.DATA.commcode)
		#}
		ENDIF
	#}
	ENDIF
	sv_s="reccount|SELECT COUNT(1) FROM CUSTOM.C_COMM WHERE BANK_ID=?SVAR AND COMM_CODE=?SVAR"
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + CUST.DATA.commcode
	PRINT(sv_s)
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	
	IF(sv_d == 0) THEN
	#{
		CUST.DATA.reccount = BANCS.OUTPARAM.reccount
		IF(CUST.DATA.reccount == "0")THEN
		#{
			CUST.DATA.errorFlg = "Y"
			CUST.DATA.errorMsg = CUST.DATA.errorMsg + "  " + "Invalid Commission Code"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	ENDOFSCRIPT:
		sv_u = urhk_SetOrbOut("errFlg|"+CUST.DATA.errorFlg)
		sv_u = urhk_SetOrbOut("errMsg|"+CUST.DATA.errorMsg)
		sv_u = urhk_SetOrbOut("commcode|"+CUST.DATA.commcode)
		TRACE OFF
		
END-->
