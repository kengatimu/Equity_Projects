#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: acctopnvalmn009.scr
# Type: NA|default
# Description: acctopnvalmn009.scr
# Author:
# Date:
# Modification Log:
#============================================================

#------------------------------------------------------------------------------------------------
#    Name			 : acctopnvalmn009.scr
#    Description     : Validating the fields
#    Date            : 17-05-2013
#    Author          : Manoj
#    Called Script	 : NA
#    Calling Script	 : mis_codes_custom.jsp
#    Menu Option	 : HOAACVSB
#    Srl. No			Date			Author			Description.
#    ------			----------			-------			---------------
#    1.0			17-05-2013			Manoj			Original Version
#-----------------------------------------------------------------------------------------------
<--START
CHECKSUM="d743bf099ed3e811de43b9eb831f4c795eb0500a76190058d3bd677cd4ae2542"
	TRACE ON
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST", "ACCTOPEN") == 0) THEN
	#{
		CREATECLASS("CUST", "ACCTOPEN", 5)
	#}
	ENDIF
	
	##############################################
	# CPARAM VALUES
	##############################################
	CUST.ACCTOPEN.errMsg 	=	""
	CUST.ACCTOPEN.pSchmCode1	=	""
	CUST.ACCTOPEN.schmCode	=	""
	
	BANCS.INPUT.moduleName = "ACCOUNT_OPENING"
	BANCS.INPUT.parameterId = "SCHM_SB109"
	CALL("getcparamvalue.scr")
	
	IF (BANCS.OUTPUT.parameterVal != "")THEN
	#{
		CUST.ACCTOPEN.pSchmCode1 =BANCS.OUTPUT.parameterVal
		PRINT(CUST.ACCTOPEN.pSchmCode1)
		#}
	ELSE
		#{
		CUST.ACCTOPEN.errMsg = "SCHME TYPE setup is not done in CPARAM"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	#########################################################
	#Inputs from jsp
	#########################################################
	PRINT(BANCS.INPUT.funcCode)
	sv_a = BANCS.INPUT.funcCode
	
	PRINT(BANCS.INPUT.acctNum)
	sv_b = BANCS.INPUT.acctNum
	
	IF( sv_a == "V" ) THEN
	#{
		sv_s = "schmCode|SELECT SCHM_CODE "
		sv_s = sv_s + " FROM TBAADM.GAM "
		sv_s = sv_s + " WHERE del_flg ='N' "
		sv_s = sv_s + " AND FORACID = ?SVAR "
		sv_s = sv_s + " AND bank_id = ?SVAR "
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS =  sv_b + "|" + BANCS.STDIN.contextBankId
		sv_d = urhk_dbSelectWithBind(sv_s)
		IF (sv_d == 0) THEN
		#{
			CUST.ACCTOPEN.schmCode = BANCS.OUTPARAM.schmCode
			PRINT(CUST.ACCTOPEN.schmCode)
		#}
		ENDIF
		
		#Skiping below block because DB Link for MKESHO_APPLICATIONS is not available
		GOTO SKIP1
		IF ( (sv_b != "")  AND (CUST.ACCTOPEN.schmCode == CUST.ACCTOPEN.pSchmCode1)) THEN
		#{
			sv_s =" Update custom.MKESHO_APPLICATIONS set FINACLE_VERIFIED_FLG='Y'"
			sv_s = sv_s + " WHERE BANK_AC_NUMBER = ?SVAR "
			sv_s = sv_s + " AND bank_id=?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = sv_b + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_d = urhk_dbSqlWithBind(sv_s)
			PRINT(sv_d)
			IF (sv_d == 0) THEN
			#{
				sv_w = urhk_dbSqlWithBind("COMMIT")
				IF (sv_w != 0 ) THEN
				#{
					PRINT("true")
					sv_u = urhk_SetOrbOut("paramV|Z")
					PRINT(sv_u)
					GOTO ENDOFSCRIPT
					#sv_u =urhk_SRV_SetErr("A Database error has occured. Please contact your DBA.")
					#}
				ELSE
					#{
					PRINT("true")
					sv_u = urhk_SetOrbOut("MSG|"+CUST.ACCTOPEN.schmCode + " Account Verified AND updated flag in Express")
					PRINT(sv_u)
					GOTO ENDOFSCRIPT
					#sv_u =urhk_SRV_SetErr("SB109 Account Verified and updated flag in Express .... ")
				#}
				ENDIF
				#}
			ELSE
				#{
				sv_u = urhk_SetOrbOut("Error|Database Error Record Not Found")
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		SKIP1:
		#}
		ENDIF
		
	ENDOFSCRIPT:
		IF (CUST.ACCTOPEN.errMsg != "") THEN
		#{
			PRINT("false2")
			sv_u = urhk_SetOrbOut("Error|"+CUST.ACCTOPEN.errMsg)
		#}
		ENDIF
		
		TRACE OFF
		EXITSCRIPT
END-->
