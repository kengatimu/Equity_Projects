s<--START
CHECKSUM="697f763168de19be50f4e5a850bb6be346ecebb897d420f7a1b1588b4977553c"
	TRACE ON
	sv_k=""
	sv_y=""
	sv_w=""
	sv_c=""
	
	sv_q=" foracid,tran_amt,tran_particular|"
	sv_q = sv_q + " select foracid,tran_amt,tran_particular from custom.int_credit  where bank_id='43'  "
	print(sv_q)
	
	sv_e = urhk_dbCursorOpenWithBind(sv_q)
	
	IF(sv_e==0) THEN
	#{
		sv_m = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		sv_q = urhk_dbCursorFetch(sv_m)
		print(sv_q)
		IF(sv_q == 1) THEN
		#{
			sv_x = urhk_dbCursorClose(sv_m)
			print(sv_x)
			exitscript
		#}
		ENDIF
		print(sv_q)
		WHILE(sv_q==0)
		#{
			sv_f=BANCS.OUTPARAM.foracid
			print(sv_f)
			sv_b=BANCS.OUTPARAM.tran_amt
			print(sv_b)
			sv_c=BANCS.OUTPARAM.tran_particular
			print(sv_c)
			sv_g = "PI41024"
			print(sv_g)

			######

			                sv_u = urhk_SetUrhkInp("usrGenDtl.usrId|" + sv_f)
                                        PRINT(sv_u)

                                        sv_u = urhk_ExecSrv("SRV_FetchUsrProfileForModify|retain_all_output = Y|same_user_verify = Y")
                                        PRINT(sv_u)

                                        IF (sv_u == 0) THEN
                                        #{

                                                sv_u = urhk_CopyOutToIn("")
                                                PRINT(sv_u)

                                                sv_u = urhk_SetUrhkInp("usrGenDtl.usrRole.id|" + sv_c)
                                                PRINT(sv_u)
                                                sv_k = urhk_SetUrhkInp("usrGenDtl.authUsr.userId|" + sv_g)
                                                PRINT(sv_k)

                                                sv_u = urhk_ExecSrv("SRV_ModifyUsrProfileAndVerify|retain_all_output = Y|same_user_verify = Y")
                                                PRINT(sv_u)

                                                IF (sv_u == 0) THEN
                                                #{
                                                        print("SUCCESS")
							BANCS.INPARAM.BINDVARS = sv_f 
							print(BANCS.INPARAM.BINDVARS)
							sv_j = "update  custom.int_credit set bank_id='88'  where bank_id='43' and foracid=?SVAR  "
							sv_w = urhk_dbSQLWithBind(sv_j)
							sv_w = urhk_dbSQLWithBind("COMMIT")
							print(sv_w)
							sv_q = urhk_dbCursorFetch(sv_m)
							print(sv_q)
                                                #}
                                                ENDIF
                                        #}
                                        ENDIF

			#####
		#}
		DO
		TRACE OFF
		EXITSCRIPT
		
	END-->
	
	
