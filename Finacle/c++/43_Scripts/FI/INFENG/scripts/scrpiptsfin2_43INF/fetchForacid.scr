#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: fetchForacid.scr
# Type: NA|default
# Description: script to fetch the foracid
# Author: AnuragGhosh_D
# Date: 6/22/18 2:44 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="bf620318135ec7c30a1d8fd3ee6d6b3c84b338a7f0a40dcb030c9f30bbf7df6f"
	# Script body goes here
	TRACE ON
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether FTCACMGR Class exists else create it
	###########################################################################################
	sv_c = CLASSEXISTS("CUST", "FTCACMGR")
	IF(sv_c == 0) THEN
	#{
		CREATECLASS("CUST", "FTCACMGR", 5)
	#}
	ENDIF
	
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	CUST.FTCACMGR.errorFlg = "N"
	CUST.FTCACMGR.errorMsg = " "
	CUST.FTCACMGR.foracid = " "
	CUST.FTCACMGR.acctNum = " "
	
	
	IF(FIELDEXISTS(BANCS.INPUT.acctNum))THEN
	#{
		IF (BANCS.INPUT.acctNum != "")THEN
		#{
			CUST.FTCACMGR.acctNum = BANCS.INPUT.acctNum
			PRINT(CUST.FTCACMGR.acctNum)
		#}
		ENDIF
	#}
	ENDIF
	
	sv_s = "foracid| SELECT foracid FROM TBAADM.GAM WHERE ACID=?SVAR AND BANK_ID=?SVAR"
	BANCS.INPARAM.BINDVARS = CUST.FTCACMGR.acctNum + "|" + BANCS.STDIN.contextBankId
	PRINT(sv_s)
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_q = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_q)
	IF(sv_q == 0) THEN
	#{
		CUST.FTCACMGR.foracid = BANCS.OUTPARAM.foracid
		#PRINT(CUST.FTCACMGR.acid)
		IF(CUST.FTCACMGR.foracid == "")THEN
		#{
			CUST.FTCACMGR.foracid = " "
		#}
		ENDIF
		
		#}
	ELSE
		#{
		
		CUST.FTCACMGR.errorFlg = "Y"
		CUST.FTCACMGR.errorMsg = " "
	#}
	ENDIF
	
	
	sv_u = urhk_SetOrbOut("errMsg|"+ CUST.FTCACMGR.errorMsg)
	sv_u = urhk_SetOrbOut("errFlg|"+ CUST.FTCACMGR.errorFlg)
	sv_u = urhk_SetOrbOut("acid|"+ CUST.FTCACMGR.foracid)
	
	
	TRACE OFF
END-->
