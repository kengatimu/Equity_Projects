<--START
CHECKSUM="697f763168de19be50f4e5a850bb6be346ecebb897d420f7a1b1588b4977553c"
	TRACE ON
	sv_k=""
	sv_y=""
	sv_w=""
	sv_c=""
	
	sv_q=" phoneemailid,orgkey,tobe|"
	sv_q = sv_q + " select PHONEEMAILID,ORGKEY,TOBE from custom.drcphone_all where BANK_ID='43' and pull_flg='7'  "
	print(sv_q)
	
	sv_e = urhk_dbCursorOpenWithBind(sv_q)
	
	IF(sv_e==0) THEN
	#{
		sv_m = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		sv_q = urhk_dbCursorFetch(sv_m)
		print(sv_q)
		IF(sv_q == 1) THEN
		#{
			sv_x = urhk_dbCursorClose(sv_m)
			print(sv_x)
			exitscript
		#}
		ENDIF
		print(sv_q)
		WHILE(sv_q==0)
		#{
			sv_a=BANCS.OUTPARAM.phoneemailid
			print(sv_a)
			sv_b=BANCS.OUTPARAM.orgkey
			print(sv_b)
			sv_c=BANCS.OUTPARAM.tobe
			print(sv_c)
			sv_k = 'P'
			print(sv_k)

			######
				BANCS.INPARAM.BINDVARS = sv_c + "|" + BANCS.STDIN.contextBankId  + "|" + sv_b + "|" + sv_a
				print(BANCS.INPARAM.BINDVARS)
				sv_j = "update CRMUSER.PHONEEMAIL set PHONENOLOCALCODE=?SVAR  where PHONEOREMAIL ='PHONE' and PREFERREDFLAG ='Y' and BanK_id=?SVAR AND ORGKEY=?SVAR and PHONEEMAILID=?SVAR  "
				sv_w = urhk_dbSQLWithBind(sv_j)
				sv_w = urhk_dbSQLWithBind("COMMIT")
				print(sv_w)
			#####
			if (sv_w==0) then
				BANCS.INPARAM.BINDVARS = sv_k + "|" + BANCS.STDIN.contextBankId  + "|" + sv_b + "|" + sv_a
				print(BANCS.INPARAM.BINDVARS)
				sv_j = "update  custom.drcphone_all set pull_flg =?SVAR where BanK_id=?SVAR AND ORGKEY=?SVAR and PHONEEMAILID=?SVAR  "
				sv_w = urhk_dbSQLWithBind(sv_j)
				sv_w = urhk_dbSQLWithBind("COMMIT")
				print(sv_w)
				sv_q = urhk_dbCursorFetch(sv_m)
				print(sv_q)
			endif
		#}
		DO
		TRACE OFF
		EXITSCRIPT
		
	END-->
	
	
