#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: chqbookmn003.scr
# Type: NA|default
# Description: chqbookmn003.scr
# Author:
# Date:
# Modification Log:
#============================================================

#---------------------------------------------------------------------------------
#       Name                    : chqbookdp001.scr
#       Description             : It validates all the inputs for cheque book maintenance menu
#       Date                    : 16-03-2013
#       Author                  : GOKULKRISHNA
#       Input                   : NA
#       Output                  : NA
#       Called Script   :
#       Calling Scrip   : NA
#       Menu Option             : CCHQM
#       Srl. No          Date              Author               Description.
#       -------          ---------         -------              ---------------
#       1.0              16-03-2013       GOKULKRISHNA          Original Version
#       2.0              22-05-2013       Shishira	        Added Foracid
#	3.0		25-03-2014	jane	 		Application charge on verification
#----------------------------------------------------------------------------------

<--START
CHECKSUM="7c32011543d46781d9cfc8c57008bfdbc7af486c1a56ac72b2db3fdab598f542"
	
	TRACE ON
	#---------------------------------------------------------------------------------
	# CREATING NEW REPOSITORY AND CLASS
	#---------------------------------------------------------------------------------
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST", "CHQB") == 0) THEN
	#{
		CREATECLASS("CUST", "CHQB", 5)
	#}
	ENDIF
	
	#-----------------------------------------------------
	#Input from criteria
	#-----------------------------------------------------
	CUST.CHQB.requestId = BANCS.INPUT.requestId
	PRINT(CUST.CHQB.requestId)
	CUST.CHQB.func	= BANCS.INPUT.funcCode
	PRINT(CUST.CHQB.func)
	CUST.CHQB.acctNo  = BANCS.INPUT.acctNo
	PRINT(CUST.CHQB.acctNo)
	CUST.CHQB.destbrnch = " "
	CUST.CHQB.count = "0"

	#------------------------------------------------------
	#Cparam
	#------------------------------------------------------
	BANCS.INPUT.moduleName  = "CHQB"
	BANCS.INPUT.parameterId = "SCHMCODE9"
	CALL ("getcparamvalue.scr")
	IF(BANCS.OUTPUT.parameterVal != "")THEN
	#{
		CUST.CHQB.schmCode9 = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		sv_e = "Schm code setup not done in CPARAM"
		sv_m = "count"
		sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
		sv_u = urhk_SetOrbOut(sv_e)
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		GOTO ENDOFSCRIPT
		
	#}
	ENDIF
	
	BANCS.INPUT.parameterId = "SCHMCODE10"
	CALL ("getcparamvalue.scr")
	IF(BANCS.OUTPUT.parameterVal != "")THEN
	#{
		CUST.CHQB.schmCode10 = BANCS.OUTPUT.parameterVal
		#}
	ELSE
		#{
		sv_e = "Schm code setup not done in CPARAM"
		sv_m = "count"
		sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
		sv_u = urhk_SetOrbOut(sv_e)
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	
	#---------------------------------------------------------------------------
	# Request Id Validation
	#---------------------------------------------------------------------------
	#IF ((TRIM(CUST.CHQB.func)!="U") AND (TRIM(CUST.CHQB.func)!="R") AND (TRIM(CUST.CHQB.func)!="S")) THEN
	IF ((TRIM(CUST.CHQB.func)!="U") AND (TRIM(CUST.CHQB.func)!="R") AND (TRIM(CUST.CHQB.func)!="S") AND (TRIM(CUST.CHQB.func)!="SB")) THEN
	#{
		IF (TRIM(CUST.CHQB.requestId) == "") THEN
		#{
			sv_e = "Please Enter Request ID OR press Seracher ON Request ID field to see the list"
			sv_m = "requestId"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	IF ((CUST.CHQB.func == "I") OR (CUST.CHQB.func == "D") OR (CUST.CHQB.func == "V")) THEN
	#{
		IF (TRIM(CUST.CHQB.requestId) != "") THEN
		#{
			sv_s = "count|"
			sv_s = sv_s + " SELECT count(*)"
			sv_s = sv_s + " FROM CUSTOM.CUSTOM_CBR"
			sv_s = sv_s + " WHERE req_id = ?SVAR"
			sv_s = sv_s + " AND bank_id = ?SVAR"
			#sv_s = sv_s + " AND foracid = ?SVAR"
			sv_s = sv_s + " AND del_flg != 'Y'"
			
			BANCS.INPARAM.BINDVARS = CUST.CHQB.requestId +"|"+ BANCS.STDIN.contextBankId
			sv_d = urhk_dbSelectWithBind(sv_s)
			IF (sv_d == 0) THEN
			#{
				CUST.CHQB.count = BANCS.OUTPARAM.count
				PRINT(CUST.CHQB.count)
			#}
			ENDIF
			IF (CUST.CHQB.count <= 0) THEN
			#{
				sv_e = "Please Enter a Valid Request ID OR press Seracher ON Request ID field to see the list"
				sv_m = "requestId"
				sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
				sv_u = urhk_SetOrbOut(sv_e)
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	IF (CUST.CHQB.func == "U") THEN
	#{
		IF (CINT(CUST.CHQB.acctNo) == "") THEN
		#{
			sv_e = "Please enter account no"
			sv_m = "acctNo"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			sv_x = "ValidateAccout|Select count(*) from tbaadm.gam where foracid = ?SVAR "
			sv_x = sv_x + " AND schm_type in ('CAA','ODA','SBA') AND entity_cre_flg = 'Y' AND del_flg !='Y' AND acct_cls_flg !='Y'"
			sv_x = sv_x + " AND bank_id = ?SVAR "
			PRINT(sv_x)
			BANCS.INPARAM.BINDVARS =  CUST.CHQB.acctNo   + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_x = urhk_dbSelectWithBind(sv_x)
			PRINT(sv_x)
			IF (sv_x != 0 ) THEN
			#{
				sv_e = "A Database error has occured. Please contact your DBA."
				sv_m = "count"
				sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
				sv_u = urhk_SetOrbOut(sv_e)
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				GOTO ENDOFSCRIPT
				#}
			ELSE
				#{
				CUST.CHQB.valAcct = BANCS.OUTPARAM.ValidateAccout
				PRINT(BANCS.OUTPARAM.ValidateAccout)
			#}
			ENDIF
			IF (CUST.CHQB.valAcct <= 0) THEN
			#{
				sv_e = "Incorrect Account No. / Accounts of schm other than (SBA,CAA,ODA) are not allowed"
				sv_m = "acctNo"
				sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
				sv_u = urhk_SetOrbOut(sv_e)
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			
			sv_a = urhk_getAcctDetailsInRepository(CUST.CHQB.acctNo)
			PRINT(sv_a)
			IF (sv_a == 0) THEN
			#{
				PRINT(BANCS.OUTPARAM.freezeCode)
				IF (BANCS.OUTPARAM.freezeCode != " ") THEN
				#{
					sv_e = "Frozen Account ,Cheque Book request cannot be processed"
					sv_m = "acctNo"
					sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
					sv_u = urhk_SetOrbOut(sv_e)
					sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
					GOTO ENDOFSCRIPT
				#}
				ENDIF
				PRINT(BANCS.STDIN.mySolId)
				PRINT(BANCS.OUTPARAM.acctSolId)
				PRINT(BANCS.OUTPARAM.schmCode)
				CUST.CHQB.schmCode = BANCS.OUTPARAM.schmCode
				PRINT(CUST.CHQB.schmCode)
				CUST.CHQB.EffAvailableAmt = BANCS.OUTPARAM.EffAvailableAmt
				PRINT(CUST.CHQB.EffAvailableAmt)
				
				#IF (BANCS.STDIN.mySolId != BANCS.OUTPARAM.acctSolId) THEN
				#{
					#sv_e = "Account belongs to different SOL"
					#sv_m = "acctNo"
					#sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
					#sv_u = urhk_SetOrbOut(sv_e)
					#sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
					#GOTO ENDOFSCRIPT
				#}
				#ENDIF

                		sv_s = "count|SELECT COUNT(1) FROM CUSTOM.CUSTOM_CBR WHERE BANK_ID=?SVAR AND DEST_BRANCH = ?SVAR AND FORACID=?SVAR"
                		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId +"|"+ BANCS.STDIN.mySolId +"|"+ CUST.CHQB.acctNo
                		PRINT(sv_s)
                		PRINT(BANCS.INPARAM.BINDVARS)


                		sv_d = urhk_dbSelectWithBind(sv_s)
                		PRINT(sv_d)

                		IF(sv_d == 0 ) THEN
                		#{
                        		CUST.CHQB.count = BANCS.OUTPARAM.count
                        		PRINT(CUST.CHQB.count)
                		#}
                		ENDIF

                		IF(CUST.CHQB.count == "0")THEN
                		#{
                        		sv_e = "No request exists for entered Account to issue at this  Branch"
                                        sv_m = "acctNo"
                                        sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
                                        sv_u = urhk_SetOrbOut(sv_e)
                                        sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
                                        GOTO ENDOFSCRIPT
                                #}
                                ENDIF


				IF ((CUST.CHQB.schmCode == CUST.CHQB.schmCode9) OR (CUST.CHQB.schmCode == CUST.CHQB.schmCode10)) THEN
				#{
					sv_e = "Chq Book issue is not Allowed for this scheme"
					sv_m = "acctNo"
					sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
					sv_u = urhk_SetOrbOut(sv_e)
					sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
					GOTO ENDOFSCRIPT
				#}
				ENDIF
			#}
			ENDIF
			
			IF(CUST.CHQB.func == "V" ) THEN
			#{
				sv_x = "MinBal|Select min_bal_for_chq_book from tbaadm.ssp where schm_code = ?SVAR "
				sv_x = sv_x + " AND crncy_code = ?SVAR AND del_flg !='Y' AND bank_id = ?SVAR "
				BANCS.INPARAM.BINDVARS = CUST.CHQB.schmCode + "|" + BANCS.STDIN.homeCrncyCode + "|" + BANCS.STDIN.contextBankId
				sv_x = urhk_dbselectWithBind(sv_x)
				
				IF (sv_x != 0 ) THEN
				#{
					sv_e = "A Database error has occured. Please contact your DBA."
					sv_m = "count"
					sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
					sv_u = urhk_SetOrbOut(sv_e)
					sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
					GOTO ENDOFSCRIPT
				#}
				ENDIF
				
				PRINT(BANCS.OUTPARAM.MinBal)
				
				IF (CDOUBLE(CUST.CHQB.EffAvailableAmt) < CDOUBLE(BANCS.OUTPARAM.MinBal)) THEN
				#{
					sv_e = "Account Balance is not sufficient to issue the Chq Book"
					sv_m = "count"
					sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
					sv_u = urhk_SetOrbOut(sv_e)
					sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
					GOTO ENDOFSCRIPT
				#}
				ENDIF
			#}
			ENDIF
			
			sv_x = "ACCT_STATUS|Select ACCT_STATUS from tbaadm.SMT where acid = (select acid from tbaadm.gam where foracid= ?SVAR AND bank_id = ?SVAR) AND bank_id= ?SVAR"
			PRINT(sv_x)
			BANCS.INPARAM.BINDVARS =  CUST.CHQB.acctNo + "|" + BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_x = urhk_dbSelectWithBind(sv_x)
			PRINT(sv_x)
			
			IF (sv_x != 0 ) THEN
			#{
				sv_e = "A Database error has occured. Please contact your DBA."
				sv_m = "count"
				sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
				sv_u = urhk_SetOrbOut(sv_e)
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				GOTO ENDOFSCRIPT
				#}
			ELSE
				#{
				IF ((BANCS.OUTPARAM.ACCT_STATUS == "I") OR (BANCS.OUTPARAM.ACCT_STATUS == "D")) THEN
				#{
					sv_e = "Account is not active"
					sv_m = "acctNo"
					sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
					sv_u = urhk_SetOrbOut(sv_e)
					sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
					GOTO ENDOFSCRIPT
				#}
				ENDIF
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	IF((BANCS.STDIN.contextBankId == "11") AND (CUST.CHQB.func == "U" )) THEN
	#{
		IF (BANCS.STDIN.userWorkClass < "150") THEN
		#{
			sv_e = "Only Workclass 150/200 can upload"
			sv_m = "acctNo"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
	#}
	ENDIF
	
	IF (CUST.CHQB.func == "V" ) THEN
	#{
		sv_s = "rcreId"
		sv_s = sv_s + " | "
		sv_s = sv_s + " SELECT rcre_user_id"
		sv_s = sv_s + " FROM CUSTOM.CUSTOM_CBR "
		sv_s = sv_s + " WHERE req_id = ?SVAR "
		sv_s = sv_s + " AND bank_id = ?SVAR "
		sv_s = sv_s + " AND del_flg != 'Y'"
		PRINT(sv_s)
		
		BANCS.INPARAM.BINDVARS = CUST.CHQB.requestId + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d != 0 ) THEN
		#{
			sv_e = "A Database error has occured. Please contact your DBA."
			sv_m = "count"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			CUST.CHQB.rcreId = BANCS.OUTPARAM.rcreId
			PRINT(CUST.CHQB.rcreId)
			
		#}
		ENDIF
	if (TRIM(BANCS.STDIN.userId) != "FIVUSR43")then
		
		IF (TRIM(BANCS.STDIN.userId) == TRIM(CUST.CHQB.rcreId)) THEN
		#{
			sv_e = "Cannot authorize your own request."
			sv_m = "funcCode"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
ENDIF

		sv_s = " rcreclass "
		sv_s = sv_s + " | "
		sv_s = sv_s + " SELECT user_work_class "
		sv_s = sv_s + " FROM TBAADM.UPR "
		sv_s = sv_s + " WHERE user_id = ?SVAR "
		PRINT(sv_s)
		
		BANCS.INPARAM.BINDVARS = CUST.CHQB.rcreId
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbselectWIthBind(sv_s)
		IF (sv_d != 0 ) THEN
		#{
			sv_e = "A Database error has occured. Please contact your DBA."
			sv_m = "count"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			CUST.CHQB.rcreclass = BANCS.OUTPARAM.rcreclass
			PRINT(CUST.CHQB.rcreclass)
		#}
		ENDIF
		CUST.CHQB.authClass = BANCS.STDIN.userWorkClass
		PRINT(CUST.CHQB.authClass)
		IF(CUST.CHQB.authClass < CUST.CHQB.rcreclass) THEN
		#{
			sv_e = "Authorizer work class should be greater than OR equal to record created user work class"
			sv_m = "requestId"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		sv_s = "reqStatus|"
		sv_s = sv_s + " Select req_status "
		sv_s = sv_s + " FROM CUSTOM.CUSTOM_CBR "
		sv_s = sv_s + " WHERE req_id = ?SVAR "
		sv_s = sv_s + " AND bank_id = ?SVAR "
		sv_s = sv_s + " AND del_flg != 'Y'"
		PRINT(sv_s)
		
		BANCS.INPARAM.BINDVARS = CUST.CHQB.requestId +"|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d != 0 ) THEN
		#{
			sv_e = "A Database error has occured. Please contact your DBA."
			sv_m = "count"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			CUST.CHQB.reqStatus = BANCS.OUTPARAM.reqStatus
			PRINT(CUST.CHQB.reqStatus)
		#}
		ENDIF
		
		IF((CUST.CHQB.reqStatus == "D") OR (CUST.CHQB.reqStatus == "A")) THEN
		#{
			sv_e = "Request has been authorized/Deleted, So can not be Verify."
			sv_m = "requestId"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		IF(CUST.CHQB.reqStatus == "S") THEN
		#{
			sv_e = "Invalid Request ID"
			sv_m = "requestId"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
	#}
	ENDIF
	
	IF (CUST.CHQB.func == "D" ) THEN
	#{
		sv_s = "reqStatus|"
		sv_s = sv_s + " Select req_status "
		sv_s = sv_s + " FROM CUSTOM.CUSTOM_CBR "
		sv_s = sv_s + " WHERE req_id = ?SVAR "
		sv_s = sv_s + " AND bank_id = ?SVAR "
		sv_s = sv_s + " AND del_flg != 'Y'"
		PRINT(sv_s)
		
		BANCS.INPARAM.BINDVARS = CUST.CHQB.requestId +"|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d != 0 ) THEN
		#{
			sv_e = "A Database error has occured. Please contact your DBA."
			sv_m = "count"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			CUST.CHQB.reqStatus = BANCS.OUTPARAM.reqStatus
			PRINT(CUST.CHQB.reqStatus)
		#}
		ENDIF
		
		IF((CUST.CHQB.reqStatus == "D") OR (CUST.CHQB.reqStatus == "A")) THEN
		#{
			sv_e = "Request has been authorized/Deleted, So can not be deleted."
			sv_m = "requestId"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
		
		IF(CUST.CHQB.reqStatus == "S") THEN
		#{
			sv_e = "Invalid Request ID"
			sv_m = "requestId"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		
	#}
	ENDIF
	
	#################################################
	# Added Below steps for new functions Validation
	##################################################
	IF (CUST.CHQB.func == "U") THEN
	#{
		sv_s = "recCount|"
		sv_s = sv_s + " Select count(1) "
		sv_s = sv_s + " FROM CUSTOM.CUSTOM_CBR "
		sv_s = sv_s + " WHERE foracid = ?SVAR "
		sv_s = sv_s + " AND bank_id = ?SVAR "
		sv_s = sv_s + " AND del_flg != 'Y'"
		sv_s = sv_s + " AND req_status = 'K'"
		PRINT(sv_s)
		
		BANCS.INPARAM.BINDVARS = CUST.CHQB.acctNo +"|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d != 0 ) THEN
		#{
			sv_e = "A Database error has occured. Please contact your DBA."
			sv_m = "count"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			CUST.CHQB.recCount = BANCS.OUTPARAM.recCount
			PRINT(CUST.CHQB.recCount)
		#}
		ENDIF
		IF(CUST.CHQB.recCount == "0") THEN
		#{
			sv_e = "Only requests acknowledged at destination branch are allowed for Issue."
			sv_m = "requestId"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
		
	#}
	ENDIF
	
	#	IF (CUST.CHQB.func == "S") THEN
	#	#{
	#		sv_s = "recCount|"
	#		sv_s = sv_s + " Select count(1) "
	#		sv_s = sv_s + " FROM CUSTOM.CUSTOM_CBR "
	#		sv_s = sv_s + " WHERE req_id = ?SVAR "
	#		sv_s = sv_s + " AND bank_id = ?SVAR "
	#		sv_s = sv_s + " AND del_flg != 'Y'"
	#		sv_s = sv_s + " AND req_status = 'A' "
	#		PRINT(sv_s)
	#
	#		BANCS.INPARAM.BINDVARS = CUST.CHQB.requestId +"|" + BANCS.STDIN.contextBankId
	#		PRINT(BANCS.INPARAM.BINDVARS)
	#
	#		sv_d = urhk_dbSelectWithBind(sv_s)
	#		PRINT(sv_d)
	#
	#		IF(sv_d != 0 ) THEN
	#		#{
	#			sv_e = "A Database error has occured. Please contact your DBA."
	#			sv_m = "count"
	#			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
	#			sv_u = urhk_SetOrbOut(sv_e)
	#			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
	#			GOTO ENDOFSCRIPT
	#			#}
	#		ELSE
	#			#{
	#			CUST.CHQB.recCount = BANCS.OUTPARAM.recCount
	#			PRINT(CUST.CHQB.recCount)
	#		#}
	#		ENDIF
	#		IF(CUST.CHQB.recCount == "0") THEN
	#		#{
	#			sv_e = "Only Authorised Requests can be Downloaded AND Delivered to Vendor for printing."
	#			sv_m = "requestId"
	#			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
	#			sv_u = urhk_SetOrbOut(sv_e)
	#			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
	#			GOTO ENDOFSCRIPT
	#
	#		#}
	#		ENDIF
	#
	#	#}
	#	ENDIF
	
	#	IF (CUST.CHQB.func == "R") THEN
	#	#{
	#		sv_s = "recCount|"
	#		sv_s = sv_s + " Select count(1) "
	#		sv_s = sv_s + " FROM CUSTOM.CUSTOM_CBR "
	#		sv_s = sv_s + " WHERE req_id = ?SVAR "
	#		sv_s = sv_s + " AND bank_id = ?SVAR "
	#		sv_s = sv_s + " AND del_flg != 'Y'"
	#		sv_s = sv_s + " AND req_status = 'S' "
	#		PRINT(sv_s)
	#
	#		BANCS.INPARAM.BINDVARS = CUST.CHQB.requestId +"|" + BANCS.STDIN.contextBankId
	#		PRINT(BANCS.INPARAM.BINDVARS)
	#
	#		sv_d = urhk_dbSelectWithBind(sv_s)
	#		PRINT(sv_d)
	#
	#		IF(sv_d != 0 ) THEN
	#		#{
	#			sv_e = "A Database error has occured. Please contact your DBA."
	#			sv_m = "count"
	#			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
	#			sv_u = urhk_SetOrbOut(sv_e)
	#			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
	#			GOTO ENDOFSCRIPT
	#			#}
	#		ELSE
	#			#{
	#			CUST.CHQB.recCount = BANCS.OUTPARAM.recCount
	#			PRINT(CUST.CHQB.recCount)
	#		#}
	#		ENDIF
	#		IF(CUST.CHQB.recCount == "0") THEN
	#		#{
	#			sv_e = "Only requests which Downloaded AND sent to vendor for Printing can be uploaded AND updated to Recieved From Vendor."
	#			sv_m = "requestId"
	#			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
	#			sv_u = urhk_SetOrbOut(sv_e)
	#			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
	#			GOTO ENDOFSCRIPT
	#
	#		#}
	#		ENDIF
	#
	#	#}
	#	ENDIF
	
	IF (CUST.CHQB.func == "B") THEN
	#{
		
		sv_s = "recCount|"
		sv_s = sv_s + " Select count(1) "
		sv_s = sv_s + " FROM CUSTOM.CUSTOM_CBR "
		sv_s = sv_s + " WHERE req_id = ?SVAR "
		sv_s = sv_s + " AND bank_id = ?SVAR "
		sv_s = sv_s + " AND del_flg != 'Y'"
		sv_s = sv_s + " AND req_status = 'B' "
		PRINT(sv_s)
		
		BANCS.INPARAM.BINDVARS = CUST.CHQB.requestId +"|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d != 0 ) THEN
		#{
			sv_e = "A Database error has occured. Please contact your DBA."
			sv_m = "count"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			CUST.CHQB.recCount = BANCS.OUTPARAM.recCount
			PRINT(CUST.CHQB.recCount)
		#}
		ENDIF
		IF(CUST.CHQB.recCount > "0") THEN
		#{
			sv_e = "Request Already Sent To Destination Branch."
			sv_m = "requestId"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
		
		sv_s = "recCount|"
		sv_s = sv_s + " Select count(1) "
		sv_s = sv_s + " FROM CUSTOM.CUSTOM_CBR "
		sv_s = sv_s + " WHERE req_id = ?SVAR "
		sv_s = sv_s + " AND bank_id = ?SVAR "
		sv_s = sv_s + " AND del_flg != 'Y'"
		sv_s = sv_s + " AND req_status = 'K' "
		PRINT(sv_s)
		
		BANCS.INPARAM.BINDVARS = CUST.CHQB.requestId +"|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d != 0 ) THEN
		#{
			sv_e = "A Database error has occured. Please contact your DBA."
			sv_m = "count"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			CUST.CHQB.recCount = BANCS.OUTPARAM.recCount
			PRINT(CUST.CHQB.recCount)
		#}
		ENDIF
		IF(CUST.CHQB.recCount > "0") THEN
		#{
			sv_e = "Request Already Aknowledged at Destination Branch."
			sv_m = "requestId"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
		
		sv_s = "recCount|"
		sv_s = sv_s + " Select count(1) "
		sv_s = sv_s + " FROM CUSTOM.CUSTOM_CBR "
		sv_s = sv_s + " WHERE req_id = ?SVAR "
		sv_s = sv_s + " AND bank_id = ?SVAR "
		sv_s = sv_s + " AND del_flg != 'Y'"
		sv_s = sv_s + " AND req_status = 'R' "
		PRINT(sv_s)
		
		BANCS.INPARAM.BINDVARS = CUST.CHQB.requestId +"|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d != 0 ) THEN
		#{
			sv_e = "A Database error has occured. Please contact your DBA."
			sv_m = "count"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			CUST.CHQB.recCount = BANCS.OUTPARAM.recCount
			PRINT(CUST.CHQB.recCount)
		#}
		ENDIF
		IF(CUST.CHQB.recCount == "0") THEN
		#{
			sv_e = "Only Requests Recieved From Vendor Can be Sent To Destination Branch."
			sv_m = "requestId"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
		
		
		
	#}
	ENDIF
	
	
	IF (CUST.CHQB.func == "K") THEN
	#{
		
		sv_s = "recCount|"
		sv_s = sv_s + " Select count(1) "
		sv_s = sv_s + " FROM CUSTOM.CUSTOM_CBR "
		sv_s = sv_s + " WHERE req_id = ?SVAR "
		sv_s = sv_s + " AND bank_id = ?SVAR "
		sv_s = sv_s + " AND del_flg != 'Y'"
		sv_s = sv_s + " AND req_status = 'K' "
		PRINT(sv_s)
		
		BANCS.INPARAM.BINDVARS = CUST.CHQB.requestId +"|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d != 0 ) THEN
		#{
			sv_e = "A Database error has occured. Please contact your DBA."
			sv_m = "count"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			CUST.CHQB.recCount = BANCS.OUTPARAM.recCount
			PRINT(CUST.CHQB.recCount)
		#}
		ENDIF
		IF(CUST.CHQB.recCount > "0") THEN
		#{
			sv_e = "Record is Already Acknowledged."
			sv_m = "requestId"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
		
		sv_s = "recCount|"
		sv_s = sv_s + " Select count(1) "
		sv_s = sv_s + " FROM CUSTOM.CUSTOM_CBR "
		sv_s = sv_s + " WHERE req_id = ?SVAR "
		sv_s = sv_s + " AND bank_id = ?SVAR "
		sv_s = sv_s + " AND del_flg != 'Y'"
		sv_s = sv_s + " AND req_status = 'B' "
		PRINT(sv_s)
		
		BANCS.INPARAM.BINDVARS = CUST.CHQB.requestId +"|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d != 0 ) THEN
		#{
			sv_e = "A Database error has occured. Please contact your DBA."
			sv_m = "count"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			CUST.CHQB.recCount = BANCS.OUTPARAM.recCount
			PRINT(CUST.CHQB.recCount)
		#}
		ENDIF
		IF(CUST.CHQB.recCount == "0") THEN
		#{
			sv_e = "Acknowledgement allowed only ON requests with status Sent To Destination Branch."
			sv_m = "requestId"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
		
		sv_s = "destbrnch|"
		sv_s = sv_s + " Select dest_branch "
		sv_s = sv_s + " FROM CUSTOM.CUSTOM_CBR "
		sv_s = sv_s + " WHERE req_id = ?SVAR "
		sv_s = sv_s + " AND bank_id = ?SVAR "
		sv_s = sv_s + " AND del_flg != 'Y'"
		sv_s = sv_s + " AND req_status = 'B' "
		PRINT(sv_s)
		
		BANCS.INPARAM.BINDVARS = CUST.CHQB.requestId +"|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d != 0 ) THEN
		#{
			sv_e = "A Database error has occured. Please contact your DBA."
			sv_m = "count"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			CUST.CHQB.destbrnch = BANCS.OUTPARAM.destbrnch
			PRINT(CUST.CHQB.destbrnch)
		#}
		ENDIF
		IF(CUST.CHQB.destbrnch != BANCS.STDIN.mySolId) THEN
		#{
			sv_e = "Record Can be Acknowledged Only at Destination Branch."+CUST.CHQB.destbrnch
			sv_m = "requestId"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
			
		#}
		ENDIF
		
		
		
	#}
	ENDIF
	#IF ((CUST.CHQB.func != "U") AND (CUST.CHQB.func != "S") AND (CUST.CHQB.func != "R")) THEN
	IF ((CUST.CHQB.func != "U") AND (CUST.CHQB.func != "S") AND (CUST.CHQB.func != "R") AND (TRIM(CUST.CHQB.func)!="SB")) THEN
	#{
		#---------------------------------------------------------------------------
		# Fetching Record
		#---------------------------------------------------------------------------
	#	sv_s = "chqBook,chqLeaves,reqStatus,foracid,beginNum,destbrnch"
		#sv_s = "chqBook,chqLeaves,reqStatus,foracid,beginNum,instType "
		sv_s = "chqBook,chqLeaves,reqStatus,foracid,beginNum,destbrnch,chrgCrncy,chrgAmt,vatCrncy,vatAmt"
		sv_s = sv_s + " | "
		sv_s = sv_s + " SELECT num_of_chb,num_of_chq_lvs,nvl(req_status,' '),foracid,begin_chq_num,DEST_BRANCH,CHRG_CRNCY,CHRG_AMT,VAT_CRNCY,VAT_AMT"
		#sv_s = sv_s + " SELECT num_of_chb,num_of_chq_lvs,nvl(req_status,' '),foracid,begin_chq_num,nvl(Inst_Type, ' ') "
		sv_s = sv_s + " FROM CUSTOM.CUSTOM_CBR "
		sv_s = sv_s + " WHERE req_id = ?SVAR "
		sv_s = sv_s + " AND bank_id = ?SVAR "
		sv_s = sv_s + " AND del_flg != 'Y'"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.CHQB.requestId +"|"+ BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d != 0 ) THEN
		#{
			sv_e = "A Database error has occured. Please contact your DBA."
			sv_m = "count"
			sv_e = "Error_2|ERROR:^"+ sv_e +".^"+ sv_m +""
			sv_u = urhk_SetOrbOut(sv_e)
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			GOTO ENDOFSCRIPT
			#}
		ELSE
			#{
			CUST.CHQB.chqBook = BANCS.OUTPARAM.chqBook
			PRINT(CUST.CHQB.chqBook)
			IF(CUST.CHQB.chqBook == "")THEN
			#{
				CUST.CHQB.chqBook = " "
			#}
			ENDIF
			PRINT(CUST.CHQB.chqBook)
			
			CUST.CHQB.chqLeaves = BANCS.OUTPARAM.chqLeaves
			PRINT(CUST.CHQB.chqLeaves)
			IF(CUST.CHQB.chqLeaves == "")THEN
			#{
				CUST.CHQB.chqLeaves = " "
			#}
			ENDIF
			
			CUST.CHQB.reqStatus = BANCS.OUTPARAM.reqStatus
			PRINT(CUST.CHQB.reqStatus)
			IF(CUST.CHQB.reqStatus == "")THEN	
			#{
				CUST.CHQB.reqStatus = " "	
			#}
			ENDIF
			PRINT(CUST.CHQB.reqStatus)

			CUST.CHQB.foracid = BANCS.OUTPARAM.foracid
			PRINT(CUST.CHQB.foracid)
			IF(CUST.CHQB.foracid == "")THEN
			#{
				CUST.CHQB.foracid = " "
			#}
			ENDIF
			PRINT(CUST.CHQB.foracid)
			
			CUST.CHQB.beginNum = BANCS.OUTPARAM.beginNum
			PRINT(CUST.CHQB.beginNum)
			IF(CUST.CHQB.beginNum == "")THEN
			#{
				CUST.CHQB.beginNum = " "
			#}
			ENDIF
			PRINT(CUST.CHQB.beginNum)
			
			CUST.CHQB.destbrnch = BANCS.OUTPARAM.destbrnch
			PRINT(CUST.CHQB.destbrnch)
			IF(CUST.CHQB.destbrnch == "")THEN
			#{
				
				CUST.CHQB.destbrnch = " "
			#}
			ENDIF
			PRINT(CUST.CHQB.destbrnch)
			
			CUST.CHQB.chrgCrncy = BANCS.OUTPARAM.chrgCrncy
			PRINT(CUST.CHQB.chrgCrncy)
			IF(CUST.CHQB.chrgCrncy == "")THEN
			#{
				CUST.CHQB.chrgCrncy = " "
			#}
			ENDIF
			PRINT(CUST.CHQB.chrgCrncy)

			CUST.CHQB.chrgAmt = BANCS.OUTPARAM.chrgAmt
			PRINT(CUST.CHQB.chrgAmt)
			IF(CUST.CHQB.chrgAmt == "")THEN
			#{
				
				CUST.CHQB.chrgAmt = " "
			#}
			ENDIF
			PRINT(CUST.CHQB.chrgAmt)
			
			CUST.CHQB.vatCrncy = BANCS.OUTPARAM.vatCrncy
			PRINT(CUST.CHQB.vatCrncy)
			IF(CUST.CHQB.vatCrncy == "")THEN
			#{
				CUST.CHQB.vatCrncy = " "
			#}
			ENDIF
			PRINT(CUST.CHQB.vatCrncy)

			CUST.CHQB.vatAmt = BANCS.OUTPARAM.vatAmt
			PRINT(CUST.CHQB.vatAmt)
			IF(CUST.CHQB.vatAmt == "")THEN
			#{
				
				CUST.CHQB.vatAmt = " "
			#}
			ENDIF
			PRINT(CUST.CHQB.vatAmt)

			sv_u = urhk_SetOrbOut("noOfChqs|"+CUST.CHQB.chqBook)
			PRINT(sv_u)
			
			sv_u = urhk_SetOrbOut("leaves|"+CUST.CHQB.chqLeaves)
			PRINT(sv_u)
			
			#sv_u = urhk_SetOrbOut("reqStat|"+CUST.CHQB.reqStatus)
			#PRINT(sv_u)

			#------------------------james.m 18032025--------------

			sv_u = urhk_SetOrbOut("reqStat|A")
			PRINT(sv_u)

			#------------------------james.m 18032025--------------


			
			sv_u = urhk_SetOrbOut("acctNo|"+CUST.CHQB.foracid)
			PRINT(sv_u)
			
			sv_u = urhk_SetOrbOut("startChqNo|"+CUST.CHQB.beginNum)
			PRINT(sv_u)
			
			sv_u = urhk_SetOrbOut("destbranch|"+CUST.CHQB.destbrnch)
			PRINT(sv_u)
			
		#}
		ENDIF
		IF (TRIM(CUST.CHQB.beginNum) != "" ) THEN
		#{
			CUST.CHQB.chqEndNo = CINT(CUST.CHQB.beginNum) + CINT(CUST.CHQB.chqBook) * CINT(CUST.CHQB.chqLeaves) - 1
			PRINT(CUST.CHQB.chqEndNo)
			sv_u = urhk_SetOrbOut("endChqNo|"+CUST.CHQB.chqEndNo)
			PRINT(sv_u)
		#}
		ENDIF
			sv_u = urhk_SetOrbOut("chrgAmt|"+CUST.CHQB.chrgAmt)
			PRINT(sv_u)

			sv_u = urhk_SetOrbOut("vatAmt|"+CUST.CHQB.vatAmt)
			PRINT(sv_u)

			sv_u = urhk_SetOrbOut("chrgCrncy|"+CUST.CHQB.chrgCrncy)
			PRINT(sv_u)

			sv_u = urhk_SetOrbOut("vatCrncy|"+CUST.CHQB.vatCrncy)
			PRINT(sv_u)
	#}
	ENDIF
	sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
	
	ENDOFSCRIPT:
		TRACE OFF
		EXITSCRIPT
END-->

