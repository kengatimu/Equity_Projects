#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: addAcctMgr.scr
# Type: NA|default
# Description: Add Account Manager Name
# Author: nagamani.boddu
# Date: 6/11/18 11:01 AM
# Modification Log:
#============================================================

<--START
CHECKSUM="4474951468eccd6c3d1bb3dae82267dab8ac08dab13cbe58279ff813c57f3c58"
	
	
	# Script body goes here
	TRACE ON
	
	
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether ADDMGR Class exists else create it
	###########################################################################################
	sv_c = CLASSEXISTS("CUST", "ADDMGR")
	IF(sv_c == 0) THEN
	#{
		CREATECLASS("CUST", "ADDMGR", 5)
	#}
	ENDIF
	sv_u = urhk_b2k_printrepos("BANCS")
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	CUST.ADDMGR.errorFlg = "N"
	CUST.ADDMGR.errorMsg = ""
	CUST.ADDMGR.lcOrBgType = ""
	CUST.ADDMGR.lcOrBgNumber = ""
	CUST.ADDMGR.entityCreFlg = ""
	CUST.ADDMGR.funcCode = ""
	CUST.ADDMGR.acctManager = ""
	CUST.ADDMGR.recCount = "0"
	CUST.ADDMGR.recCount1 = "0"
	
	CUST.ADDMGR.funcCode = sv_z
	CUST.ADDMGR.lcOrBgNumber = sv_t
	CUST.ADDMGR.acctManager = sv_m
	
	#	IF(FIELDEXISTS(BANCS.INPUT.funcCode))THEN
	#	#{
	#		IF(BANCS.INPUT.funcCode != "") THEN
	#		#{
	#			CUST.ADDMGR.funcCode = TOUPPER(TRIM(BANCS.INPUT.funcCode))
	#		#}
	#		ENDIF
	#	#}
	#	ENDIF
	#
	#	IF(FIELDEXISTS(BANCS.INPUT.lcOrBgNumber))THEN
	#	#{
	#		IF (BANCS.INPUT.lcOrBgNumber != "")THEN
	#		#{
	#			CUST.ADDMGR.lcOrBgNumber = BANCS.INPUT.lcOrBgNumber
	#			PRINT(CUST.ADDMGR.lcOrBgNumber)
	#		#}
	#		ENDIF
	#	#}
	#	ENDIF
	#	IF(FIELDEXISTS(BANCS.INPUT.acctManager))THEN
	#	#{
	#		IF(BANCS.INPUT.acctManager != "") THEN
	#		#{
	#			CUST.ADDMGR.acctManager = TOUPPER(TRIM(BANCS.INPUT.acctManager))
	#		#}
	#		ENDIF
	#	#}
	#	ENDIF
	
	PRINT(CUST.ADDMGR.funcCode)
	PRINT(CUST.ADDMGR.lcOrBgNumber)
	PRINT(CUST.ADDMGR.acctManager)
	
	
	CUST.ADDMGR.menuId=BANCS.STDIN.menuId
	IF((CUST.ADDMGR.menuId == "ODCM") OR (CUST.ADDMGR.menuId == "IDCM"))THEN
	#{
		CUST.ADDMGR.lcOrBgType = "LC"
	#}
	ENDIF
	
	IF((CUST.ADDMGR.menuId == "IGM") OR (CUST.ADDMGR.menuId == "OGM"))THEN
	#{
		CUST.ADDMGR.lcOrBgType = "BG"
	#}
	ENDIF
	
	###########################################################################################
	# Inserting Acct Manager Name (funcCode - S and C)
	###########################################################################################
	IF(CUST.ADDMGR.menuId == "ODCM")THEN
	#{
		IF((CUST.ADDMGR.funcCode == "S") OR (CUST.ADDMGR.funcCode == "C"))THEN
		#{
			sv_s = "Insert into CUSTOM.ACCT_MANAGER_NAME(ACCOUNT_MANAGER_NAME, LC_OR_BG_NUMBER, LC_OR_BG_TYPE, BANK_ID,RCRE_USER_ID, RCRE_TIME, LCHG_USER_ID, LCHG_TIME, ENTITY_CRE_FLG, DEL_FLG) values ("
			sv_s = sv_s + "?SVAR, ?SVAR, ?SVAR, ?SVAR, ?SVAR, sysdate, ?SVAR, sysdate,'N', 'N')"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.ADDMGR.acctManager + "|" + CUST.ADDMGR.lcOrBgNumber + "|" + CUST.ADDMGR.lcOrBgType + "|" + BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.userId + "|" + BANCS.STDIN.userId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_q = urhk_dbSQLWithBind(sv_s)
			PRINT(sv_q)
			IF(sv_q != 0)THEN
			#{
				sv_e = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_e)
				sv_e =urhk_SetOrbErr("Error_1|Error^Database Insertion Failed^moduleName")
				PRINT(sv_e)
				GOTO ENDOFSCRIPT
				
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	IF(CUST.ADDMGR.menuId == "IDCM")THEN
	#{
		IF((CUST.ADDMGR.funcCode == "E") OR (CUST.ADDMGR.funcCode == "C"))THEN
		#{
			sv_s = "Insert into CUSTOM.ACCT_MANAGER_NAME(ACCOUNT_MANAGER_NAME, LC_OR_BG_NUMBER, LC_OR_BG_TYPE, BANK_ID,RCRE_USER_ID, RCRE_TIME, LCHG_USER_ID, LCHG_TIME, ENTITY_CRE_FLG, DEL_FLG) values ("
			sv_s = sv_s + "?SVAR, ?SVAR, ?SVAR, ?SVAR, ?SVAR, sysdate, ?SVAR, sysdate,'N', 'N')"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.ADDMGR.acctManager + "|" + CUST.ADDMGR.lcOrBgNumber + "|" + CUST.ADDMGR.lcOrBgType + "|" + BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.userId + "|" + BANCS.STDIN.userId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_q = urhk_dbSQLWithBind(sv_s)
			PRINT(sv_q)
			IF(sv_q != 0)THEN
			#{
				sv_e = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_e)
				sv_e =urhk_SetOrbErr("Error_1|Error^Database Insertion Failed^moduleName")
				PRINT(sv_e)
				GOTO ENDOFSCRIPT
				
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	IF((CUST.ADDMGR.menuId == "OGM") OR(CUST.ADDMGR.menuId == "IGM"))THEN
	#{
		IF((CUST.ADDMGR.funcCode == "A") OR (CUST.ADDMGR.funcCode == "C"))THEN
		#{
			sv_s = "Insert into CUSTOM.ACCT_MANAGER_NAME(ACCOUNT_MANAGER_NAME, LC_OR_BG_NUMBER, LC_OR_BG_TYPE, BANK_ID,RCRE_USER_ID, RCRE_TIME, LCHG_USER_ID, LCHG_TIME, ENTITY_CRE_FLG, DEL_FLG) values ("
			sv_s = sv_s + "?SVAR, ?SVAR, ?SVAR, ?SVAR, ?SVAR, sysdate, ?SVAR, sysdate,'N', 'N')"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.ADDMGR.acctManager + "|" + CUST.ADDMGR.lcOrBgNumber + "|" + CUST.ADDMGR.lcOrBgType + "|" + BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.userId + "|" + BANCS.STDIN.userId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_q = urhk_dbSQLWithBind(sv_s)
			PRINT(sv_q)
			IF(sv_q != 0)THEN
			#{
				sv_e = urhk_SetOrbOut("SuccessOrFailure|N")
				PRINT(sv_e)
				sv_e =urhk_SetOrbErr("Error_1|Error^Database Insertion Failed^moduleName")
				PRINT(sv_e)
				GOTO ENDOFSCRIPT
				
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	###########################################################################################
	# Update Acct Manager Name (FuncCode -M, A, U, R)
	###########################################################################################
	IF(((CUST.ADDMGR.funcCode == "M") OR (CUST.ADDMGR.funcCode == "A")) AND ((CUST.ADDMGR.menuId == "ODCM") OR (CUST.ADDMGR.menuId == "IDCM"))) THEN
	#{
		
		sv_s = "recCount| select COUNT(1) from CUSTOM.ACCT_MANAGER_NAME where  "
		sv_s = sv_s + "LC_OR_BG_NUMBER = ?SVAR AND ENTITY_CRE_FLG='Y'"
		sv_s = sv_s + " AND BANK_ID = ?SVAR "
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.ADDMGR.lcOrBgNumber + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_q = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_q)
		
		CUST.ADDMGR.recCount = BANCS.OUTPARAM.recCount
		IF(CUST.ADDMGR.recCount == "0")THEN
		#{
			
			sv_s = "recCount1| select COUNT(1) from CUSTOM.ACCT_MANAGER_NAME where  "
			sv_s = sv_s + "LC_OR_BG_NUMBER = ?SVAR AND ENTITY_CRE_FLG='N'"
			sv_s = sv_s + " AND BANK_ID = ?SVAR "
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.ADDMGR.lcOrBgNumber + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_q = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_q)
			CUST.ADDMGR.recCount1 = BANCS.OUTPARAM.recCount1
			IF(CUST.ADDMGR.recCount1 == "0")THEN
			#{
				sv_s = "Insert into CUSTOM.ACCT_MANAGER_NAME(ACCOUNT_MANAGER_NAME, LC_OR_BG_NUMBER, LC_OR_BG_TYPE, BANK_ID,RCRE_USER_ID, RCRE_TIME, LCHG_USER_ID, LCHG_TIME, ENTITY_CRE_FLG, DEL_FLG) values ("
				sv_s = sv_s + "?SVAR, ?SVAR, ?SVAR, ?SVAR, ?SVAR, sysdate, ?SVAR, sysdate,'N', 'N')"
				#PRINT(sv_s)
				BANCS.INPARAM.BINDVARS = CUST.ADDMGR.acctManager + "|" + CUST.ADDMGR.lcOrBgNumber + "|" + CUST.ADDMGR.lcOrBgType + "|" + BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.userId + "|" + BANCS.STDIN.userId
				
				#}
			ELSE
				#{
				sv_s = "UPDATE CUSTOM.ACCT_MANAGER_NAME set "
				sv_s = sv_s + "ACCOUNT_MANAGER_NAME = ?SVAR, "
				sv_s = sv_s + "ENTITY_CRE_FLG ='N', "
				sv_s = sv_s + "LCHG_USER_ID = ?SVAR, "
				sv_s = sv_s + "LCHG_TIME = sysdate where "
				sv_s = sv_s + "LC_OR_BG_NUMBER = ?SVAR AND "
				sv_s = sv_s + "BANK_ID = ?SVAR "
				#PRINT(sv_s)
				BANCS.INPARAM.BINDVARS = CUST.ADDMGR.acctManager + "|" + BANCS.STDIN.userId + "|" + CUST.ADDMGR.lcOrBgNumber+ "|" + BANCS.STDIN.contextBankId
				
			#}
			ENDIF
			#}
		ELSE
			#{
			
			sv_s = "recCount| select COUNT(1) from CUSTOM.ACCT_MANAGER_NAME_MOD where  "
			sv_s = sv_s + "LC_OR_BG_NUMBER = ?SVAR "
			sv_s = sv_s + " AND BANK_ID = ?SVAR "
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.ADDMGR.lcOrBgNumber + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_q = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_q)
			
			CUST.ADDMGR.recCount1 = BANCS.OUTPARAM.recCount
			IF(CUST.ADDMGR.recCount1 == "0")THEN
			#{
				sv_s = "Insert into CUSTOM.ACCT_MANAGER_NAME_MOD(ACCOUNT_MANAGER_NAME, LC_OR_BG_NUMBER, LC_OR_BG_TYPE, BANK_ID,RCRE_USER_ID, RCRE_TIME, LCHG_USER_ID, LCHG_TIME, ENTITY_CRE_FLG, DEL_FLG) values ("
				sv_s = sv_s + "?SVAR, ?SVAR, ?SVAR, ?SVAR, ?SVAR, sysdate, ?SVAR, sysdate,'N', 'N')"
				#PRINT(sv_s)
				BANCS.INPARAM.BINDVARS = CUST.ADDMGR.acctManager + "|" + CUST.ADDMGR.lcOrBgNumber + "|" + CUST.ADDMGR.lcOrBgType + "|" + BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.userId + "|" + BANCS.STDIN.userId
				
				#}
			ELSE
				#{
				
				sv_s = "UPDATE CUSTOM.ACCT_MANAGER_NAME_MOD set "
				sv_s = sv_s + "ACCOUNT_MANAGER_NAME = ?SVAR, "
				sv_s = sv_s + "ENTITY_CRE_FLG ='N', "
				sv_s = sv_s + "LCHG_USER_ID = ?SVAR, "
				sv_s = sv_s + "LCHG_TIME = sysdate where "
				sv_s = sv_s + "LC_OR_BG_NUMBER = ?SVAR AND "
				sv_s = sv_s + "BANK_ID = ?SVAR "
				#PRINT(sv_s)
				BANCS.INPARAM.BINDVARS = CUST.ADDMGR.acctManager + "|" + BANCS.STDIN.userId + "|" + CUST.ADDMGR.lcOrBgNumber+ "|" + BANCS.STDIN.contextBankId
			#}
			ENDIF
			
		#}
		ENDIF
		PRINT(sv_s)
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_q = urhk_dbSQLWithBind(sv_s)
		PRINT(sv_q)
		IF(sv_q != 0) THEN
		#{
			sv_e=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_e=urhk_SetOrbOut("Data updation failed")
			PRINT(sv_e)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
	#}
	ENDIF
	
	IF(((CUST.ADDMGR.funcCode == "M") OR (CUST.ADDMGR.funcCode == "U")) AND ((CUST.ADDMGR.menuId == "IGM") OR (CUST.ADDMGR.menuId == "OGM"))) THEN
	#{
		
		sv_s = "recCount| select COUNT(1) from CUSTOM.ACCT_MANAGER_NAME where  "
		sv_s = sv_s + "LC_OR_BG_NUMBER = ?SVAR AND ENTITY_CRE_FLG='Y'"
		sv_s = sv_s + " AND BANK_ID = ?SVAR "
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.ADDMGR.lcOrBgNumber + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_q = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_q)
		
		CUST.ADDMGR.recCount = BANCS.OUTPARAM.recCount
		IF(CUST.ADDMGR.recCount == "0")THEN
		#{
			
			sv_s = "recCount1| select COUNT(1) from CUSTOM.ACCT_MANAGER_NAME where  "
			sv_s = sv_s + "LC_OR_BG_NUMBER = ?SVAR AND ENTITY_CRE_FLG='N'"
			sv_s = sv_s + " AND BANK_ID = ?SVAR "
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.ADDMGR.lcOrBgNumber + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_q = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_q)
			
			CUST.ADDMGR.recCount1 = BANCS.OUTPARAM.recCount1
			IF(CUST.ADDMGR.recCount1 == "0")THEN
			#{
				
				sv_s = "Insert into CUSTOM.ACCT_MANAGER_NAME(ACCOUNT_MANAGER_NAME, LC_OR_BG_NUMBER, LC_OR_BG_TYPE, BANK_ID,RCRE_USER_ID, RCRE_TIME, LCHG_USER_ID, LCHG_TIME, ENTITY_CRE_FLG, DEL_FLG) values ("
				sv_s = sv_s + "?SVAR, ?SVAR, ?SVAR, ?SVAR, ?SVAR, sysdate, ?SVAR, sysdate,'N', 'N')"
				#PRINT(sv_s)
				BANCS.INPARAM.BINDVARS = CUST.ADDMGR.acctManager + "|" + CUST.ADDMGR.lcOrBgNumber + "|" + CUST.ADDMGR.lcOrBgType + "|" + BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.userId + "|" + BANCS.STDIN.userId
				#}
			ELSE
				#{
				sv_s = "UPDATE CUSTOM.ACCT_MANAGER_NAME set "
				sv_s = sv_s + "ACCOUNT_MANAGER_NAME = ?SVAR, "
				sv_s = sv_s + "ENTITY_CRE_FLG ='N', "
				sv_s = sv_s + "LCHG_USER_ID = ?SVAR, "
				sv_s = sv_s + "LCHG_TIME = sysdate where "
				sv_s = sv_s + "LC_OR_BG_NUMBER = ?SVAR AND "
				sv_s = sv_s + "BANK_ID = ?SVAR "
				#PRINT(sv_s)
				BANCS.INPARAM.BINDVARS = CUST.ADDMGR.acctManager + "|" + BANCS.STDIN.userId + "|" + CUST.ADDMGR.lcOrBgNumber+ "|" + BANCS.STDIN.contextBankId
				
			#}
			ENDIF
			#}
		ELSE
			#{
			
			sv_s = "recCount| select COUNT(1) from CUSTOM.ACCT_MANAGER_NAME_MOD where  "
			sv_s = sv_s + "LC_OR_BG_NUMBER = ?SVAR "
			sv_s = sv_s + " AND BANK_ID = ?SVAR "
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.ADDMGR.lcOrBgNumber + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_q = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_q)
			
			CUST.ADDMGR.recCount1 = BANCS.OUTPARAM.recCount
			IF(CUST.ADDMGR.recCount1 == "0")THEN
			#{
				sv_s = "Insert into CUSTOM.ACCT_MANAGER_NAME_MOD(ACCOUNT_MANAGER_NAME, LC_OR_BG_NUMBER, LC_OR_BG_TYPE, BANK_ID,RCRE_USER_ID, RCRE_TIME, LCHG_USER_ID, LCHG_TIME, ENTITY_CRE_FLG, DEL_FLG) values ("
				sv_s = sv_s + "?SVAR, ?SVAR, ?SVAR, ?SVAR, ?SVAR, sysdate, ?SVAR, sysdate,'N', 'N')"
				#PRINT(sv_s)
				BANCS.INPARAM.BINDVARS = CUST.ADDMGR.acctManager + "|" + CUST.ADDMGR.lcOrBgNumber + "|" + CUST.ADDMGR.lcOrBgType + "|" + BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.userId + "|" + BANCS.STDIN.userId
				
				#}
			ELSE
				#{
				
				sv_s = "UPDATE CUSTOM.ACCT_MANAGER_NAME_MOD set "
				sv_s = sv_s + "ACCOUNT_MANAGER_NAME = ?SVAR, "
				sv_s = sv_s + "ENTITY_CRE_FLG ='N', "
				sv_s = sv_s + "LCHG_USER_ID = ?SVAR, "
				sv_s = sv_s + "LCHG_TIME = sysdate where "
				sv_s = sv_s + "LC_OR_BG_NUMBER = ?SVAR AND "
				sv_s = sv_s + "BANK_ID = ?SVAR "
				#PRINT(sv_s)
				BANCS.INPARAM.BINDVARS = CUST.ADDMGR.acctManager + "|" + BANCS.STDIN.userId + "|" + CUST.ADDMGR.lcOrBgNumber+ "|" + BANCS.STDIN.contextBankId
			#}
			ENDIF
			
		#}
		ENDIF
		PRINT(sv_s)
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_q = urhk_dbSQLWithBind(sv_s)
		PRINT(sv_q)
		IF(sv_q != 0) THEN
		#{
			sv_e=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_e=urhk_SetOrbOut("Data updation failed")
			PRINT(sv_e)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
	#}
	ENDIF
	
	###########################################################################################
	# Update ENTITY_CRE_FLG to Y (FuncCode -V)
	###########################################################################################
	IF((CUST.ADDMGR.funcCode == "V")) THEN
	#{
		
		sv_s = "recCount| select COUNT(1) from CUSTOM.ACCT_MANAGER_NAME_MOD where  "
		sv_s = sv_s + "LC_OR_BG_NUMBER = ?SVAR "
		sv_s = sv_s + " AND BANK_ID = ?SVAR "
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.ADDMGR.lcOrBgNumber + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_q = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_q)
		
		CUST.ADDMGR.recCount1 = BANCS.OUTPARAM.recCount
		IF(CUST.ADDMGR.recCount1 == "0")THEN
		#{
			sv_s = "UPDATE CUSTOM.ACCT_MANAGER_NAME set "
			sv_s = sv_s + "ENTITY_CRE_FLG ='Y', "
			sv_s = sv_s + "LCHG_USER_ID = ?SVAR, "
			sv_s = sv_s + "LCHG_TIME = sysdate where "
			sv_s = sv_s + "LC_OR_BG_NUMBER = ?SVAR AND "
			sv_s = sv_s + "BANK_ID = ?SVAR "
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = BANCS.STDIN.userId + "|" + CUST.ADDMGR.lcOrBgNumber+ "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_q = urhk_dbSQLWithBind(sv_s)
			PRINT(sv_q)
			IF(sv_q != 0) THEN
			#{
				sv_e=urhk_SetOrbOut("SuccessOrFailure|N")
				sv_e=urhk_SetOrbOut("Data updation failed")
				PRINT(sv_e)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			#}
		ELSE
			#{
			
			sv_s = "UPDATE CUSTOM.ACCT_MANAGER_NAME set "
			sv_s = sv_s + "ACCOUNT_MANAGER_NAME = ?SVAR, "
			sv_s = sv_s + "ENTITY_CRE_FLG ='Y', "
			sv_s = sv_s + "LCHG_USER_ID = ?SVAR, "
			sv_s = sv_s + "LCHG_TIME = sysdate where "
			sv_s = sv_s + "LC_OR_BG_NUMBER = ?SVAR AND "
			sv_s = sv_s + "BANK_ID = ?SVAR "
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.ADDMGR.acctManager + "|" + BANCS.STDIN.userId + "|" + CUST.ADDMGR.lcOrBgNumber+ "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			
			sv_q = urhk_dbSQLWithBind(sv_s)
			PRINT(sv_q)
			IF(sv_q != 0) THEN
			#{
				sv_e=urhk_SetOrbOut("SuccessOrFailure|N")
				sv_e=urhk_SetOrbOut("Data updation failed")
				PRINT(sv_e)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			
			sv_s = "DELETE FROM CUSTOM.ACCT_MANAGER_NAME_MOD WHERE LC_OR_BG_NUMBER = ?SVAR AND BANK_ID = ?SVAR "
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.ADDMGR.lcOrBgNumber+ "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			
			sv_q = urhk_dbSQLWithBind(sv_s)
			PRINT(sv_q)
			IF(sv_q != 0) THEN
			#{
				sv_e=urhk_SetOrbOut("SuccessOrFailure|N")
				sv_e=urhk_SetOrbOut("Data updation failed")
				PRINT(sv_e)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			
		#}
		ENDIF
		
		#
		
		IF(sv_q == 0) THEN
		#{
			sv_r = urhk_SetOrbOut("SuccessOrFailure|Y")
		#}
		ENDIF
		
	#}
	ENDIF
	###########################################################################################
	# CANCEL
	###########################################################################################
	IF((CUST.ADDMGR.funcCode == "X")) THEN
	#{
		
		sv_s = "recCount| select COUNT(1) from CUSTOM.ACCT_MANAGER_NAME_MOD where ENTITY_CRE_FLG='N' AND "
		sv_s = sv_s + "LC_OR_BG_NUMBER = ?SVAR "
		sv_s = sv_s + " AND BANK_ID = ?SVAR "
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.ADDMGR.lcOrBgNumber + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_q = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_q)
		
		CUST.ADDMGR.recCount1 = BANCS.OUTPARAM.recCount
		IF(CUST.ADDMGR.recCount1 != "0")THEN
		#{
			sv_s = "DELETE FROM CUSTOM.ACCT_MANAGER_NAME_MOD WHERE ENTITY_CRE_FLG='N' AND LC_OR_BG_NUMBER = ?SVAR AND BANK_ID = ?SVAR "
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.ADDMGR.lcOrBgNumber+ "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			
			sv_q = urhk_dbSQLWithBind(sv_s)
			PRINT(sv_q)
			IF(sv_q != 0) THEN
			#{
				sv_e=urhk_SetOrbOut("SuccessOrFailure|N")
				sv_e=urhk_SetOrbOut("Data Deletion failed")
				PRINT(sv_e)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			#}
		ELSE
			#{
			
			sv_s = "recCount| select COUNT(1) from CUSTOM.ACCT_MANAGER_NAME where ENTITY_CRE_FLG='N' AND "
			sv_s = sv_s + "LC_OR_BG_NUMBER = ?SVAR "
			sv_s = sv_s + " AND BANK_ID = ?SVAR "
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.ADDMGR.lcOrBgNumber + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_q = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_q)
			
			CUST.ADDMGR.recCount1 = BANCS.OUTPARAM.recCount
			IF(CUST.ADDMGR.recCount1 != "0")THEN
			#{
				sv_s = "DELETE FROM CUSTOM.ACCT_MANAGER_NAME WHERE ENTITY_CRE_FLG='N' AND LC_OR_BG_NUMBER = ?SVAR AND BANK_ID = ?SVAR "
				PRINT(sv_s)
				BANCS.INPARAM.BINDVARS = CUST.ADDMGR.lcOrBgNumber+ "|" + BANCS.STDIN.contextBankId
				PRINT(BANCS.INPARAM.BINDVARS)
				
				sv_q = urhk_dbSQLWithBind(sv_s)
				PRINT(sv_q)
				IF(sv_q != 0) THEN
				#{
					sv_e=urhk_SetOrbOut("SuccessOrFailure|N")
					sv_e=urhk_SetOrbOut("Data Deletion failed")
					PRINT(sv_e)
					GOTO ENDOFSCRIPT
				#}
				ENDIF
				
			#}
			ENDIF
			
			#
			
			IF(sv_q == 0) THEN
			#{
				sv_r = urhk_SetOrbOut("SuccessOrFailure|Y")
			#}
			ENDIF
			
		#}
		ENDIF
	#}
	ENDIF
	
	ENDOFSCRIPT:
END-->
