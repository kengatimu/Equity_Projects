#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: ubpdp021.scr
# event: NA
# description: NA
#=================================================

<--START
CHECKSUM="8c3dbfe1c47190ff4c3f306b83ff0aa7247b4964357cce5a7390402a7dbdde35"
	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpdp021.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called from ubp_crit_link.js for Details for bill pay ref num
	#                               for reversal mode.
	# Called Script         :
	# Calling Function      :		function fnbillPayRefNumSrchr()
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           16-04-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	#========================================================
	# Check whether CUST Repository exists else create it
	#========================================================
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	#========================================================
	# Check whether UBP Class exists else create it
	#========================================================
	sv_r = CLASSEXISTS("CUST","UBP")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","UBP",5)
	#}
	ENDIF
	#========================================================
	# Initialize the variables
	#========================================================
	
	CUST.UBP.funcCode = BANCS.INPUT.funcCode
	print(CUST.UBP.funcCode)	
	CUST.UBP.billPayRefNum=" "
	CUST.UBP.payBankId=" "
	CUST.UBP.tranId=" "
	CUST.UBP.tranDate=" "
	CUST.UBP.chargeEventId=" "
	CUST.UBP.billerBankId=" "
	CUST.UBP.interEntityTranId=" "
	CUST.UBP.interEntityTranDate=" "
	CUST.UBP.recordCount = "0"
	#========================================================
	# Fetching the Record count
	#========================================================
	#BANCS.INPARAM.BINDVARS = "N"
	BANCS.INPARAM.BINDVARS = "N" +"|"+ BANCS.STDIN.contextBankId +"|"+ BANCS.STDIN.contextBankId
	sv_s = "recordNum|"
	sv_s = sv_s + " SELECT COUNT(*)"
	IF(CUST.UBP.funcCode == "R")THEN
	#{
		sv_s = sv_s + " FROM CUSTOM.C_UBP_DETAILS "
	#}
	ELSE
	#{
		sv_s = sv_s + " FROM CUSTOM.C_UBP_DETAILS_MOD "
	#}
	ENDIF

	#sv_s = sv_s + " WHERE REVERSE_FLG = ?SVAR"
	sv_s = sv_s + " WHERE REVERSE_FLG = ?SVAR AND (BILLER_BNK_ID=?SVAR OR PAYER_BNK_ID=?SVAR)"
	PRINT(sv_s)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d == 0)THEN
	#{
		CUST.UBP.recordNum = BANCS.OUTPARAM.recordNum
		PRINT(CUST.UBP.recordNum)
	#}
	ENDIF
	sv_u = urhk_SetOrbOut("recCount|"+CUST.UBP.recordNum)
	PRINT(sv_u)
	
	IF(CUST.UBP.recordNum=="0")THEN
	#{
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		#========================================================
		# Fetching tthe values from BRGT table and populating
		# the searcher list
		#========================================================
		#BANCS.INPARAM.BINDVARS = "N"
		BANCS.INPARAM.BINDVARS = "N" +"|"+ BANCS.STDIN.contextBankId +"|"+ BANCS.STDIN.contextBankId
		sv_s = "billPayRefNum,payBankId,tranId,tranDate,chargeTranId,chargeTranDate,"
		sv_s = sv_s + " billerBankId,interEntityTranId,interEntityTranDate|"
		sv_s = sv_s + " SELECT BILL_PAY_REF_NUM,PAYER_BNK_ID,"
		sv_s = sv_s + " TRAN_ID,to_char(TRAN_DATE,'dd-mm-yyyy'),CHARGE_TRAN_ID,to_char(CHARGE_TRAN_DATE,'dd-mm-yyyy'),"
		sv_s = sv_s + " BILLER_BNK_ID,INTER_ENTITY_TRANID,to_char(INTER_ENTITY_TRAN_DATE,'dd-mm-yyyy')"
		IF(CUST.UBP.funcCode == "R")THEN
		#{
			sv_s = sv_s + " FROM CUSTOM.C_UBP_DETAILS "
		#}
		ELSE
		#{
			sv_s = sv_s + " FROM CUSTOM.C_UBP_DETAILS_MOD "
		#}
		ENDIF
		#sv_s = sv_s + " WHERE REVERSE_FLG = ?SVAR"
		sv_s = sv_s + " WHERE REVERSE_FLG = ?SVAR AND (BILLER_BNK_ID=?SVAR OR PAYER_BNK_ID=?SVAR)"
		PRINT(sv_s)
		sv_c = urhk_dbCursorOpenWithBind(sv_s)
		PRINT(sv_c)
		IF(sv_c != 0)THEN
		#{
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		sv_n = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		PRINT(sv_n)
		sv_f = urhk_dbCursorFetch(sv_n)
		PRINT(sv_f)
		sv_i = 1
		WHILE(sv_f == 0)
		#{
			CUST.UBP.billPayRefNum = BANCS.OUTPARAM.billPayRefNum
			PRINT(CUST.UBP.billPayRefNum)
			CUST.UBP.payBankId = BANCS.OUTPARAM.payBankId
			PRINT(CUST.UBP.payBankId)
			CUST.UBP.tranId = BANCS.OUTPARAM.tranId
			PRINT(CUST.UBP.tranId)
			CUST.UBP.tranDate = BANCS.OUTPARAM.tranDate
			PRINT(CUST.UBP.tranDate)
			
			CUST.UBP.chargeTranId = BANCS.OUTPARAM.chargeTranId
			PRINT(CUST.UBP.chargeTranId)
			CUST.UBP.chargeTranDate = BANCS.OUTPARAM.chargeTranDate
			PRINT(CUST.UBP.chargeTranDate)
			CUST.UBP.billerBankId = BANCS.OUTPARAM.billerBankId
			PRINT(CUST.UBP.billerBankId)
			CUST.UBP.interEntityTranId = BANCS.OUTPARAM.interEntityTranId
			PRINT(CUST.UBP.interEntityTranId)
			CUST.UBP.interEntityTranDate = BANCS.OUTPARAM.interEntityTranDate
			PRINT(CUST.UBP.interEntityTranDate)
			
			
			sv_s = "payRefNum_" + Format$(sv_i,"%d") + "|" + CUST.UBP.billPayRefNum
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			
			sv_s = "initEntityId_" + Format$(sv_i,"%d") + "|" + BANCS.STDIN.contextBankId
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			
			sv_s = "initEntityTransaction_" + Format$(sv_i,"%d") + "|" + CUST.UBP.tranId+ " / " +CUST.UBP.tranDate
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			IF(CUST.UBP.chargeTranId=="")THEN
			#{
				CUST.UBP.chargeTranId=" "
				CUST.UBP.chargeTranDate=" "
				sv_s = "chargeTransaction_" + Format$(sv_i,"%d") + "|" + CUST.UBP.chargeTranId
				PRINT(sv_s)
				sv_u = urhk_SetOrbOut(sv_s)
				PRINT(sv_u)
				#}
			ELSE
				#{
				sv_s = "chargeTransaction_" + Format$(sv_i,"%d") + "|" + CUST.UBP.chargeTranId+ " / " +CUST.UBP.chargeTranDate
				PRINT(sv_s)
				sv_u = urhk_SetOrbOut(sv_s)
				PRINT(sv_u)
			#}
			ENDIF
			
			IF(CUST.UBP.interEntityTranId=="")THEN
			#{
				CUST.UBP.interEntityTranId=" "
				CUST.UBP.interEntityTranDate=" "
				CUST.UBP.billerBankId=" "
				sv_s = "interEntityId_" + Format$(sv_i,"%d") + "|" + CUST.UBP.billerBankId
				PRINT(sv_s)
				sv_u = urhk_SetOrbOut(sv_s)
				PRINT(sv_u)
				
				sv_s = "interEntityTransaction_" + Format$(sv_i,"%d") + "|" + CUST.UBP.interEntityTranId
				PRINT(sv_s)
				sv_u = urhk_SetOrbOut(sv_s)
				PRINT(sv_u)
				
				#}
			ELSE
				#{
				sv_s = "interEntityId_" + Format$(sv_i,"%d") + "|" + CUST.UBP.billerBankId
				PRINT(sv_s)
				sv_u = urhk_SetOrbOut(sv_s)
				PRINT(sv_u)
				
				sv_s = "interEntityTransaction_" + Format$(sv_i,"%d") + "|" + CUST.UBP.interEntityTranId+ " / " +CUST.UBP.interEntityTranDate
				PRINT(sv_s)
				sv_u = urhk_SetOrbOut(sv_s)
				PRINT(sv_u)
			#}
			ENDIF
			
			
			sv_i = sv_i + 1
			sv_f = urhk_dbCursorFetch(sv_n)
			PRINT(sv_f)
			
		#}
		DO
		sv_c = urhk_dbCursorClose(sv_n)
		PRINT(sv_c)
	#}
	ENDIF
	
	ENDOFSCRIPT:
		
END-->
