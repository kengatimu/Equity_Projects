#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: Event Based
# Script: fetchRateCode.scr
# Input	: acctCrncy - From AccountCrncy & refCrncy - To AccountCrncy
# Output : rateCode, rate, treaRate(CUST_VAR_CRNCY_UNITS)
# Description: Fetchs rate code and rate for given currency
# Author: Sridevi_Vutukuru
# Date: 10/30/18 3:42 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="22f585863d705024da38d25e22e77211004fc7c9db74d055faeb6ee390acdd8b"
	TRACE ON
	
	PRINT(CUST.RATE.acctCrncy)
	PRINT(CUST.RATE.refCrncy)
	PRINT(CUST.RATE.TranType)
	CUST.RATE.treaRate	= ""
	CUST.RATE.rate		= ""
	CUST.RATE.rateCode  = ""
	CUST.RATE.errorFlg  = "N"
	CUST.RATE.errorMsg  = " "
	IF(CUST.RATE.TranType == "T")THEN
	#{
		sv_s = "rateCode| select ratecode_tran from custom.c_ratecode where from_currency = ?SVAR AND to_currency=?SVAR AND bank_id =?SVAR"
		#}
	ELSE
		#{
		sv_s = "rateCode| select ratecode_cash from custom.c_ratecode where from_currency = ?SVAR AND to_currency=?SVAR AND bank_id =?SVAR"
	#}
	ENDIF
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.RATE.acctCrncy + "|" + CUST.RATE.refCrncy + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d ==0) THEN
	#{
		CUST.RATE.rateCode = BANCS.OUTPARAM.rateCode
		PRINT(CUST.RATE.rateCode)
		
		sv_s ="rate,treaRate| select var_crncy_units,CUST_VAR_CRNCY_UNITS from tbaadm.rtl where ratecode=?SVAR AND fxd_crncy_code=?SVAR AND var_crncy_code=?SVAR AND bank_id =?SVAR"
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS = CUST.RATE.rateCode + "|" + CUST.RATE.acctCrncy + "|" + CUST.RATE.refCrncy + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		IF(sv_d ==0) THEN
		#{
			CUST.RATE.rate = BANCS.OUTPARAM.rate
			PRINT(CUST.RATE.rate)
			
			CUST.RATE.treaRate = BANCS.OUTPARAM.treaRate
			PRINT(CUST.RATE.treaRate)
			#}
		ELSE
			#{
			sv_s ="rate,treaRate| select var_crncy_units,CUST_VAR_CRNCY_UNITS from tbaadm.rtl where ratecode=?SVAR AND fxd_crncy_code=?SVAR AND var_crncy_code=?SVAR AND bank_id =?SVAR"
			PRINT(sv_s)
			BANCS.INPARAM.BINDVARS = CUST.RATE.rateCode + "|" + CUST.RATE.refCrncy + "|" + CUST.RATE.acctCrncy + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			
			sv_d = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_d)
			IF(sv_d ==0) THEN
			#{
				CUST.RATE.rate = BANCS.OUTPARAM.rate
				PRINT(CUST.RATE.rate)
				
				CUST.RATE.treaRate = BANCS.OUTPARAM.treaRate
				PRINT(CUST.RATE.treaRate)
			#}
			ENDIF
		#}
		ENDIF
		#}
	ELSE
		#{
		PRINT("RateCode does not exist from "+CUST.RATE.acctCrncy+" to "+CUST.RATE.refCrncy)
		CUST.RATE.errorFlg  = "Y"
		CUST.RATE.errorMsg  = "RateCode does not exist from "+CUST.RATE.acctCrncy+" to "+CUST.RATE.refCrncy
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	
	
	ENDOFSCRIPT:
		
END-->

