#-----------------------------------------------------------------------------------------------

<--start
trace on

        IF (REPEXISTS("CUST") == 0) THEN
        #{
                CREATEREP("CUST")
        #}
        ENDIF

        IF (CLASSEXISTS("CUST","CHQ") == 0) THEN
        #{
                CREATECLASS("CUST","CHQ",5)
        #}
        ENDIF

	sv_a=urhk_B2k_PrintRepos("BANCS")
	print (sv_a)

	IF( (FIELDEXISTS(("BANCS").("INPUT").("acctNo"))== 0) ) THEN
	#{
   		CUST.CHQ.errMsg="acctNo is not available"
		sv_z = urhk_SetOrbOut("SuccessOrFailure|Failed")
		sv_t = "Msg| " +CUST.CHQ.errMsg
		sv_u = urhk_SetOrbOut(sv_t)
   		GOTO ENDSCRIPT
	#}
	ENDIF




	################################ Variable Inititalization#######################################################
	CUST.CHQ.acctNo= BANCS.INPUT.acctNo
	print(CUST.CHQ.acctNo)
        CUST.CHQ.count = "0"


	lv_g = urhk_getAcctDetailsInRepository(CUST.CHQ.acctNo)
	print(lv_g)
	IF(lv_g == 0) then
	#{
	 	CUST.CHQ.acid = BANCS.OUTPARAM.acid
		print(CUST.CHQ.acid) 

        	sv_a = "beginchqno,endchqnum,NumOfLvs,issuedate,chqlvsStatus,laststatus, delflg|SELECT CBT.BEGIN_CHQ_NUM,CBT.END_CHQ_NUM,CBT.CHQ_NUM_OF_LVS,"
        	sv_a = sv_a + " TO_CHAR(CBT.CHQ_ISSU_DATE,'DD-MM-YYYY HH24:MI:SS'),CBT.CHQ_LVS_STAT,SUBSTR(CHQ_LVS_STAT, -1,1),CBT.DEL_FLG FROM tbaadm.CBT "
       	 	sv_a = sv_a + " WHERE (CBT.ENTITY_CRE_FLG = 'Y' AND CBT.ACID =?SVAR AND CBT.BANK_ID =?SVAR) "
        	sv_a = sv_a + " ORDER BY CBT.CHQ_ISSU_DATE DESC,CBT.END_CHQ_NUM,CBT.BEGIN_CHQ_NUM"
        	print(sv_a)

        	BANCS.INPARAM.BINDVARS = CUST.CHQ.acid+ "|" +BANCS.STDIN.contextBankId
        	print(BANCS.INPARAM.BINDVARS)

        	sv_d = urhk_dbCursorOpenWithBind(sv_a)
        	print(sv_d)

       		IF(sv_d!=0) THEN
        	#{
 	       		sv_u = urhk_dbCursorClose(sv_a)
               		print(sv_u)
			
			sv_z = urhk_SetOrbOut("SuccessOrFailure|Failed")
               		sv_u = urhk_SetOrbOut("Msg| No record found")
               		print(sv_u)
        	#}
        	ELSE
        	#{
               		CUST.CHQ.DB_CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
               		print(CUST.CHQ.DB_CURSOR_NUMBER)

               		sv_c = urhk_dbCursorFetch(CUST.CHQ.DB_CURSOR_NUMBER)
               		print(sv_c)
               		while(sv_c ==0)
               		#{
               			CUST.CHQ.beginchqno= BANCS.OUTPARAM.beginchqno
                        	print(CUST.CHQ.beginchqno)
               			CUST.CHQ.endchqnum= BANCS.OUTPARAM.endchqnum
                        	print(CUST.CHQ.endchqnum)
               			CUST.CHQ.NumOfLvs= BANCS.OUTPARAM.NumOfLvs
                        	print(CUST.CHQ.NumOfLvs)
               			CUST.CHQ.issuedate= BANCS.OUTPARAM.issuedate
                        	print(CUST.CHQ.issuedate)
				IF(CUST.CHQ.NumOfLvs=="100")THEN
				#{
               				CUST.CHQ.chqlvsStatus= BANCS.OUTPARAM.chqlvsStatus
                        		print(CUST.CHQ.chqlvsStatus)
               				CUST.CHQ.laststatus= BANCS.OUTPARAM.laststatus
                        		print(CUST.CHQ.laststatus)

               				CUST.CHQ.chqlvsStatus=CUST.CHQ.chqlvsStatus + CUST.CHQ.laststatus
                        		print(CUST.CHQ.chqlvsStatus)

				#}
				ELSE
				#{
               				CUST.CHQ.chqlvsStatus= BANCS.OUTPARAM.chqlvsStatus
                        		print(CUST.CHQ.chqlvsStatus)
				#}
				ENDIF
               			CUST.CHQ.delflg= BANCS.OUTPARAM.delflg
                        	print(CUST.CHQ.delflg)



                                sv_c = urhk_SetOrbOut("ChqDetails.<rec_"+CUST.CHQ.count+">.beginchqno"+"| "+CUST.CHQ.beginchqno)
                                print(sv_c)
                                sv_c = urhk_SetOrbOut("ChqDetails.<rec_"+CUST.CHQ.count+">.endchqnum"+"| "+CUST.CHQ.endchqnum)
                                print(sv_c)
                                sv_c = urhk_SetOrbOut("ChqDetails.<rec_"+CUST.CHQ.count+">.NumOfLvs"+"| "+CUST.CHQ.NumOfLvs)
                                print(sv_c)
                                sv_c = urhk_SetOrbOut("ChqDetails.<rec_"+CUST.CHQ.count+">.issuedate"+"| "+CUST.CHQ.issuedate)
                                print(sv_c)
                                sv_c = urhk_SetOrbOut("ChqDetails.<rec_"+CUST.CHQ.count+">.chqlvsStatus"+"| "+CUST.CHQ.chqlvsStatus)
                                print(sv_c)

                                CUST.CHQ.count = CINT(CUST.CHQ.count)+ 1
                                print(CUST.CHQ.count)

				sv_c = urhk_dbCursorFetch(CUST.CHQ.DB_CURSOR_NUMBER)
				print(sv_c)



                         #}
                         DO
                         sv_c = urhk_dbCursorClose(CUST.CHQ.DB_CURSOR_NUMBER)
                         print(sv_c)


		#}
		ENDIF

	#}
	ELSE
	#{

   		CUST.CHQ.errMsg="Invalid account number"
		sv_z = urhk_SetOrbOut("SuccessOrFailure|Failed")
		sv_t = "Msg| " +CUST.CHQ.errMsg
		sv_u = urhk_SetOrbOut(sv_t)

	#}
	ENDIF
ENDSCRIPT:

trace off

end-->

