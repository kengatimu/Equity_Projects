#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: fetchAcctMgr.scr
# Type: NA|default
# Description: fetching Acct Manager Name
# Author: nagamani.boddu
# Date: 6/11/18 4:53 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="3cfd32c98091229216968a676ae7b4546c18180fc7414b293f9c13f24732e136"
	# Script body goes here
	TRACE ON
	sv_r=urhk_B2k_PrintRepos("BANCS")
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether FTCACMGR Class exists else create it
	###########################################################################################
	sv_c = CLASSEXISTS("CUST", "FTCACMGR")
	IF(sv_c == 0) THEN
	#{
		CREATECLASS("CUST", "FTCACMGR", 5)
	#}
	ENDIF
	
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	CUST.FTCACMGR.errorFlg = "N"
	CUST.FTCACMGR.errorMsg = " "
	CUST.FTCACMGR.lcOrBgType = ""
	CUST.FTCACMGR.lcOrBgNumber = ""
	CUST.FTCACMGR.funcCode = ""
	CUST.FTCACMGR.acctManager = " "
	
	
	IF(FIELDEXISTS(BANCS.INPUT.funccode))THEN
	#{
		IF(BANCS.INPUT.funccode != "")THEN
		#{
			CUST.FTCACMGR.funcCode = BANCS.INPUT.funccode
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.lcOrBgNumber))THEN
	#{
		IF (BANCS.INPUT.lcOrBgNumber != "")THEN
		#{
			CUST.FTCACMGR.lcOrBgNumber = BANCS.INPUT.lcOrBgNumber
			PRINT(CUST.FTCACMGR.lcOrBgNumber)
		#}
		ENDIF
	#}
	ENDIF
	
	
	IF((CUST.FTCACMGR.funcCode != "C") OR (CUST.FTCACMGR.funcCode!="S")) THEN
	#{
		sv_s = "acctMgr| SELECT ACCOUNT_MANAGER_NAME FROM CUSTOM.ACCT_MANAGER_NAME WHERE LC_OR_BG_NUMBER=?SVAR AND BANK_ID= ?SVAR"
		BANCS.INPARAM.BINDVARS = CUST.FTCACMGR.lcOrBgNumber + "|" + BANCS.STDIN.contextBankId
		PRINT(sv_s)
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_q = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_q)
		IF(sv_q == 0) THEN
		#{
			CUST.FTCACMGR.acctManager = BANCS.OUTPARAM.acctMgr
			PRINT(CUST.FTCACMGR.acctManager)
			
			#}
		ELSE
			#{
			
			CUST.FTCACMGR.errorFlg = "Y"
			CUST.FTCACMGR.errorMsg = " "
		#}
		ENDIF
		
	#}
	ENDIF
	
	IF((CUST.FTCACMGR.funcCode == "M") OR (CUST.FTCACMGR.funcCode =="V") OR (CUST.FTCACMGR.funcCode == "X")) THEN
	#{
		sv_s = "acctMgr| SELECT ACCOUNT_MANAGER_NAME FROM CUSTOM.ACCT_MANAGER_NAME_MOD WHERE LC_OR_BG_NUMBER=?SVAR AND BANK_ID= ?SVAR"
		BANCS.INPARAM.BINDVARS = CUST.FTCACMGR.lcOrBgNumber + "|" + BANCS.STDIN.contextBankId
		PRINT(sv_s)
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_q = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_q)
		IF(sv_q == 0) THEN
		#{
			CUST.FTCACMGR.acctManager = BANCS.OUTPARAM.acctMgr
			PRINT(CUST.FTCACMGR.acctManager)
			
			#}
		ELSE
			#{
			sv_s = "acctMgr| SELECT ACCOUNT_MANAGER_NAME FROM CUSTOM.ACCT_MANAGER_NAME WHERE LC_OR_BG_NUMBER=?SVAR AND BANK_ID= ?SVAR"
			BANCS.INPARAM.BINDVARS = CUST.FTCACMGR.lcOrBgNumber + "|" + BANCS.STDIN.contextBankId
			PRINT(sv_s)
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_q = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_q)
			IF(sv_q == 0) THEN
			#{
				CUST.FTCACMGR.acctManager = BANCS.OUTPARAM.acctMgr
				PRINT(CUST.FTCACMGR.acctManager)
				#}
			ELSE
				#{
				CUST.FTCACMGR.errorFlg = "Y"
				CUST.FTCACMGR.errorMsg = " "
			#}
			ENDIF
		#}
		ENDIF
		
	#}
	ENDIF
	
	sv_u = urhk_SetOrbOut("errMsg|"+ CUST.FTCACMGR.errorMsg)
	sv_u = urhk_SetOrbOut("errFlg|"+ CUST.FTCACMGR.errorFlg)
	sv_u = urhk_SetOrbOut("acctMgr|"+ CUST.FTCACMGR.acctManager)
	
	
	TRACE OFF
END-->
