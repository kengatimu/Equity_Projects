 ################################################################################################
# Source Name				: pyoffIntPeriodFetch.scr
# Date						: 
# Description				: 
# Input						: None
# Output					: 
# Called Script				: None
# Calling Script			: None
# Author					: Sridevi v
# Modification History		:
#	< Version No.>		<Date>			<Author Name>		<Description>
################################################################################################
<--START

	TRACE ON
	
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether WHT Class exists else create it
	###########################################################################################
	sv_c = CLASSEXISTS("CUST", "PYOFF")
	IF(sv_c == 0) THEN
	#{
		CREATECLASS("CUST", "PYOFF", 5)
	#}
	ENDIF
	
	CUST.PYOFF.acctNum = ""
	CUST.PYOFF.userDefinedDays = ""
	CUST.PYOFF.userDefinedMonths = ""
	CUST.PYOFF.NUM_OF_DAYS = " "
	CUST.PYOFF.NUM_OF_MONTHS = " "
	CUST.PYOFF.errorMsg = " "
	CUST.PYOFF.errorFlg = "N"
	
	IF(FIELDEXISTS(BANCS.INPUT.acctNum))THEN
	#{
		IF (BANCS.INPUT.acctNum != "")THEN
		#{
			CUST.PYOFF.acctNum = BANCS.INPUT.acctNum
			PRINT(CUST.PYOFF.acctNum)
		#}
		ENDIF
	#}
	ENDIF
	########################################################################
	#DATA FETCH
	########################################################################
		
		sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.C_PAYOFF_INT_PERIOD WHERE ACCT_NUM = ?SVAR "
		BANCS.INPARAM.BINDVARS = CUST.PYOFF.acctNum
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_b = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_b)
		
		IF(sv_b == 0)THEN
		#{
			CUST.PYOFF.recCount = BANCS.OUTPARAM.recCount
			PRINT(CUST.PYOFF.recCount)
			#######################################################
			# Data fetch
			#######################################################
			
				sv_s = "NUM_OF_DAYS,NUM_OF_MONTHS|SELECT NUM_OF_DAYS,NUM_OF_MONTHS FROM CUSTOM.C_PAYOFF_INT_PERIOD WHERE "
				sv_s = sv_s + " SRNO = (SELECT MAX(SRNO) FROM CUSTOM.C_PAYOFF_INT_PERIOD WHERE ACCT_NUM = ?SVAR) AND ACCT_NUM = ?SVAR"
				BANCS.INPARAM.BINDVARS = CUST.PYOFF.acctNum + "|" + CUST.PYOFF.acctNum 
				PRINT(BANCS.INPARAM.BINDVARS)
				sv_b = urhk_dbSelectWithBind(sv_s)
				PRINT(sv_b)
				IF(sv_b == 0)THEN
				#{
					IF(BANCS.OUTPARAM.NUM_OF_DAYS != "")THEN
					#{
						CUST.PYOFF.NUM_OF_DAYS = BANCS.OUTPARAM.NUM_OF_DAYS
						PRINT(CUST.PYOFF.NUM_OF_DAYS)
					#}
					ENDIF
					IF(BANCS.OUTPARAM.NUM_OF_MONTHS != "")THEN
					#{
						CUST.PYOFF.NUM_OF_MONTHS = BANCS.OUTPARAM.NUM_OF_MONTHS
						PRINT(CUST.PYOFF.NUM_OF_MONTHS)
					#}
					ENDIF
					
					GOTO ENDOFSCRIPT
				#}
				ELSE
				#{
					CUST.PYOFF.errorFlg = "Y"
					CUST.PYOFF.errorMsg = "Failed to fetch the record"
					GOTO ENDOFSCRIPT
				#}
				ENDIF		
			
			
		#}
		ELSE
		#{
			CUST.PYOFF.errorFlg = "Y"
			CUST.PYOFF.errorMsg = "Failed to fetch the record"
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
	
	
	ENDOFSCRIPT:
	sv_u = urhk_SetOrbOut("errFlg|"+CUST.PYOFF.errorFlg)
	sv_u = urhk_SetOrbOut("errMsg|"+CUST.PYOFF.errorMsg)
	sv_u = urhk_SetOrbOut("userDefinedDays|"+CUST.PYOFF.NUM_OF_DAYS)
	PRINT(sv_u)
	sv_u = urhk_SetOrbOut("userDefinedMonths|"+CUST.PYOFF.NUM_OF_MONTHS)
	PRINT(sv_u)
		
		TRACE OFF
END-->