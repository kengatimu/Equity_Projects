<--START
CHECKSUM="697f763168de19be50f4e5a850bb6be346ecebb897d420f7a1b1588b4977553c"
	TRACE ON
	sv_k=""
	sv_y=""
	sv_w=""
	sv_c=""
	sv_g=""
	
	sv_q=" tran_particular,foracid,classto,classfrom|"
	sv_q = sv_q + " select TRAN_PARTICULAR,FORACID,CLASSTO,CLASSFROM from CUSTOM.INT_CREDIT where BANK_ID='43' and tran_amt='5'  "
	print(sv_q)
	
	sv_e = urhk_dbCursorOpenWithBind(sv_q)
	
	IF(sv_e==0) THEN
	#{
		sv_m = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		sv_q = urhk_dbCursorFetch(sv_m)
		print(sv_q)
		IF(sv_q == 1) THEN
		#{
			sv_x = urhk_dbCursorClose(sv_m)
			print(sv_x)
			exitscript
		#}
		ENDIF
		print(sv_q)
		WHILE(sv_q==0)
		#{
			sv_a=BANCS.OUTPARAM.tran_particular
			print(sv_a)
			sv_b=BANCS.OUTPARAM.foracid
			print(sv_b)
			sv_c=BANCS.OUTPARAM.classto
			print(sv_c)
			sv_d=BANCS.OUTPARAM.classfrom
			print(sv_d)
			sv_k = "99"
			print(sv_k)

###### address

##update crmuser.address set CITY='086' ,CITY_CODE='086',ADDRESS_LINE3='086,CD,0000'  where bank_id='43' and orgkey='43000927200' and ADDRESSID='74931705';
	BANCS.INPARAM.BINDVARS = sv_c + "|" + sv_c  + "|" + sv_d + "|" + BANCS.STDIN.contextBankId + "|" + sv_b + "|" + sv_a
	print(BANCS.INPARAM.BINDVARS)
	sv_j = "update crmuser.address set CITY=?SVAR , CITY_CODE=?SVAR ,ADDRESS_LINE3=?SVAR   where bank_id=?SVAR  and orgkey=?SVAR and ADDRESSID=?SVAR "
	sv_w = urhk_dbSQLWithBind(sv_j)
	sv_w = urhk_dbSQLWithBind("COMMIT")
	print(sv_w)
	if (sv_w==0) then
		##update  crmuser.accounts set CITY='086'   where bank_id='43' and orgkey='43000927200' 
		BANCS.INPARAM.BINDVARS = sv_c + "|" + BANCS.STDIN.contextBankId + "|" + sv_b
		print(BANCS.INPARAM.BINDVARS)
		sv_j = "update  crmuser.accounts set CITY=?SVAR where bank_id=?SVAR and orgkey=?SVAR  "
		sv_w = urhk_dbSQLWithBind(sv_j)
		sv_g = urhk_dbSQLWithBind("COMMIT")
		print(sv_g)
		if (sv_g==0) then
			##update  crmuser.accounts_mod set CITY='086'   where bank_id='43' and orgkey='43000927200' 
			BANCS.INPARAM.BINDVARS = sv_c + "|" + BANCS.STDIN.contextBankId + "|" + sv_b
			print(BANCS.INPARAM.BINDVARS)
			sv_j = "update  crmuser.accounts_mod set CITY=?SVAR where bank_id=?SVAR and orgkey=?SVAR  "
			sv_w = urhk_dbSQLWithBind(sv_j)
			sv_w = urhk_dbSQLWithBind("COMMIT")
			print(sv_w)
		endif
	endif
#####
if (sv_g==0) then
	BANCS.INPARAM.BINDVARS = sv_k + "|" + BANCS.STDIN.contextBankId  + "|" + sv_b 
	print(BANCS.INPARAM.BINDVARS)
	sv_j = "update  CUSTOM.INT_CREDIT set  tran_amt=?SVAR where BANK_ID=?SVAR and foracid=?SVAR "
	sv_w = urhk_dbSQLWithBind(sv_j)
	sv_w = urhk_dbSQLWithBind("COMMIT")
	print(sv_w)
	sv_q = urhk_dbCursorFetch(sv_m)
	print(sv_q)
endif
		#}
		DO
		TRACE OFF
		EXITSCRIPT
		
	END-->
	
	
