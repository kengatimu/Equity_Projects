#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: pnlRateSubmit.scr
# Type: NA|default
# Description: OD Penalty Script
# Author: Anshul_Singh04
# Date: 12/4/18 11:00 AM
# Modification Log:
#============================================================

<--START
CHECKSUM="db79a059f7411be7e532700261c61151cdbd8c3550b506e65d8495e5c64d3687"
	# Script body goes here
	TRACE ON
	sv_a = REPEXISTS("EQT")
	IF (sv_a == 0) THEN
	#{
		CREATEREP("EQT")
	#}
	ENDIF
	sv_a = CLASSEXISTS("EQT", "ODP")
	IF (sv_a == 0 ) THEN
	#{
		CREATECLASS("EQT", "ODP", 5)
	#}
	ENDIF
	
	IF(BANCS.INPUT.funcCode=="A")THEN
	#{
		CALL("pnlRate002.scr")
		sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
		PRINT(EQT.ODP.msg)
		sv_l = urhk_SetOrbOut("RESULT_MSG|" + EQT.ODP.msg)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	IF(BANCS.INPUT.funcCode=="M")THEN
	#{
		CALL("pnlRate002.scr")
		sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
		PRINT(EQT.ODP.msg)
		sv_l = urhk_SetOrbOut("RESULT_MSG|" + EQT.ODP.msg)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	IF(BANCS.INPUT.funcCode=="X")THEN
	#{
		CALL("pnlRate004.scr")
		sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
		PRINT(EQT.ODP.msg)
		sv_l = urhk_SetOrbOut("RESULT_MSG|" + EQT.ODP.msg)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	IF(BANCS.INPUT.funcCode=="V")THEN
	#{
		CALL("pnlRate005.scr")
		sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
		PRINT(EQT.ODP.msg)
		sv_l = urhk_SetOrbOut("RESULT_MSG|" + EQT.ODP.msg)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	IF((BANCS.INPUT.funcCode=="D") OR (BANCS.INPUT.funcCode=="U"))THEN
	#{
		PRINT(BANCS.INPUT.levelType)
		PRINT(BANCS.INPUT.levelId)
		
		IF(BANCS.INPUT.funcCode=="D")THEN
		#{
			sv_p = 'Y'
			#}
		ELSE
			#{
			sv_p = 'N'
		#}
		ENDIF
		
		sv_r= "UPDATE CUSTOM.C_PPT SET DEL_FLG = ?SVAR , LCHG_TIME = sysdate, LCHG_USER_ID = ?SVAR WHERE LEVEL_TYPE = ?SVAR and LEVEL_ID = ?SVAR and bank_id=?SVAR "
		BANCS.INPARAM.BINDVARS =  sv_p + "|" + BANCS.STDIN.userId + "|" + BANCS.INPUT.levelType + "|" + BANCS.INPUT.levelId + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSqlWithBind(sv_r)
		PRINT(sv_d)
		
		IF(sv_d==0) THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
			IF(BANCS.INPUT.funcCode=="D")THEN
			#{
				EQT.ODP.msg = "Record Deleted successfully for Level Id: "+  BANCS.INPUT.levelId
				#}
			ELSE
				#{
				EQT.ODP.msg = "Record Undeleted successfully for Level Id: "+  BANCS.INPUT.levelId
			#}
			ENDIF
			#}
		ELSE
			#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			EQT.ODP.msg = " Error in Updating Record for Level Id: "+  BANCS.INPUT.levelId
			sv_u = urhk_SetOrbOut("^Error_28|Error^"+EQT.ODP.msg+"^dummyField")
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		sv_l = urhk_SetOrbOut("RESULT_MSG|" + EQT.ODP.msg)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	ENDOFSCRIPT:
		DELETECLASS("EQT", "ODP")
		DELETEREP("EQT")
		
		EXITSCRIPT
		TRACE OFF
END-->

