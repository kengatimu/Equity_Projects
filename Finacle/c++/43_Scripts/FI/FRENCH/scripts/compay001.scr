#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: compay001.scr
# Type: NA|default
# Description: Commission Payment Script
# Author: Anshul_Singh04
# Date: 6/4/18 5:00 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="8a9a27e266d1093d33feda6087a956c98471855663cd51248a74c047088661a7"
	# Script body goes here
	TRACE ON
	
	sv_a = REPEXISTS("EQT")
	IF (sv_a == 0) THEN
	#{
		CREATEREP("EQT")
	#}
	ENDIF
	sv_a = CLASSEXISTS("EQT", "COMM")
	IF (sv_a == 0 ) THEN
	#{
		CREATECLASS("EQT", "COMM", 5)
	#}
	ENDIF
	
	PRINT(BANCS.INPUT.funcCode)
	
	IF(BANCS.INPUT.funcCode=="A")THEN
	#{
		sv_u = urhk_SetOrbOut("SuccessOrFailure|Y")
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	EQT.COMM.commDesc = " "
	print(BANCS.INPUT.tranId)
	print(BANCS.INPUT.tranDate)
	
	sv_m = "recCount|SELECT count(1)"
	sv_m= sv_m +" FROM CUSTOM.C_CPTD WHERE trim(TRAN_ID)=?SVAR and TRAN_DATE=to_date(?SVAR,'DD-MM-YYYY') and bank_id=?SVAR "
	
	BANCS.INPARAM.BINDVARS = BANCS.INPUT.tranId + "|" + BANCS.INPUT.tranDate + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	PRINT(sv_m)
	sv_r = urhk_dbselectWithBind(sv_m)
	
	IF (sv_r != 0) THEN
	#{
		sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
		sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		print(BANCS.OUTPARAM.recCount)
	#}
	ENDIF
	
	IF(BANCS.OUTPARAM.recCount == "0")THEN
	#{
		sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
		sv_e ="There is no Transaction for the combination [ " +BANCS.INPUT.tranId+ " ] and [ "+ BANCS.INPUT.tranDate +" ]."
		print(sv_e)
		sv_u= urhk_SetOrbOut("Error_1|ERR^ "+sv_e+"^Go")
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	IF(BANCS.INPUT.funcCode == "R")THEN
	#{
		sv_s = "atdCount|SELECT count(1) FROM tbaadm.ATD WHERE trim(TRAN_ID) = ?SVAR AND TRAN_DATE = TO_DATE( ?SVAR ,'DD-MM-YYYY HH24:MI:SS') "
		sv_s = sv_s + " AND BANK_ID = ?SVAR AND ATD.REVERSAL_FLG IN ('O','R')"
		print(sv_s)
		
		BANCS.INPARAM.BINDVARS = BANCS.INPUT.tranId + "|" + BANCS.INPUT.tranDate + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		
		PRINT(sv_s)
		sv_r = urhk_dbselectWithBind(sv_s)
		
		IF (sv_r == 0) THEN
		#{
			print(BANCS.OUTPARAM.atdCount)
			
			if(BANCS.OUTPARAM.atdCount !="0")THEN
			#{
				sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
				sv_u= urhk_SetOrbOut("Error_1|ERR^This transaction is already Reversed!^Go")
				GOTO ENDOFSCRIPT
			#}
			endif
		#}
		ENDIF
		
	#}
	endif
	
	sv_s = " pmntType,commCode,commCrncy,commAmt,vatAmt,drAcctNum,commAcct,taxAcctNum,pmntPrps,ratecode,rate,rcreUser, tranStatus, rvsTranId, rvsTranDate |"
	sv_s = sv_s + "SELECT PAYMENT_TYPE, COMM_CODE, COMM_CRNCY_CODE, COMM_AMT, VAT_AMT, DR_ACCT_ID, COMM_ACCT_ID, VAT_ACCT_ID, "
	sv_s = sv_s + "  PAYMENT_PURPOSE, RATE_CODE, RATE, RCRE_USER_ID, TRAN_STATUS, REVERSAL_TRAN_ID, TO_CHAR(REVERSAL_TRAN_DATE,'DD-MM-YYYY') "
	sv_s = sv_s + " FROM CUSTOM.C_CPTD WHERE trim(TRAN_ID)=?SVAR AND TRAN_DATE=to_date(?SVAR,'DD-MM-YYYY') AND bank_id=?SVAR  "
	
	BANCS.INPARAM.BINDVARS = BANCS.INPUT.tranId + "|" + BANCS.INPUT.tranDate + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	PRINT(sv_s)
	sv_r = urhk_dbselectWithBind(sv_s)
	
	IF (sv_r != 0) THEN
	#{
		sv_s=urhk_SetOrbOut("SuccessOrFailure|N")
		sv_t= urhk_SetOrbOut("Error_1|ERR^Error in Fetching Data^Go")
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		print(BANCS.OUTPARAM.pmntType)
		print(BANCS.OUTPARAM.commCode)
		print(BANCS.OUTPARAM.commCrncy)
		print(BANCS.OUTPARAM.commAmt)
		print(BANCS.OUTPARAM.vatAmt)
		print(BANCS.OUTPARAM.drAcctNum)
		print(BANCS.OUTPARAM.commAcct)
		print(BANCS.OUTPARAM.taxAcctNum)
		print(BANCS.OUTPARAM.pmntPrps)
		print(BANCS.OUTPARAM.ratecode)
		print(BANCS.OUTPARAM.rate)
		print(BANCS.OUTPARAM.rcreUser)
		print(BANCS.OUTPARAM.tranStatus)
		print(BANCS.OUTPARAM.rvsTranId)
		print(BANCS.OUTPARAM.rvsTranDate)
		
		EQT.COMM.commCode = BANCS.OUTPARAM.commCode
		EQT.COMM.commCrncy = BANCS.OUTPARAM.commCrncy
		EQT.COMM.commAmt = BANCS.OUTPARAM.commAmt
		EQT.COMM.vatAmt = BANCS.OUTPARAM.vatAmt
		EQT.COMM.drAcctNum = BANCS.OUTPARAM.drAcctNum
		EQT.COMM.crAcctNum = BANCS.OUTPARAM.commAcct
		EQT.COMM.taxAcctNum = BANCS.OUTPARAM.taxAcctNum
		EQT.COMM.pmntPrps = BANCS.OUTPARAM.pmntPrps
		EQT.COMM.ratecode = BANCS.OUTPARAM.ratecode
		EQT.COMM.rate = BANCS.OUTPARAM.rate
		EQT.COMM.pmntType = BANCS.OUTPARAM.pmntType
		EQT.COMM.tranStatus = BANCS.OUTPARAM.tranStatus
		EQT.COMM.rvsTranId = BANCS.OUTPARAM.rvsTranId
		EQT.COMM.rvsTranDate = BANCS.OUTPARAM.rvsTranDate
		
		IF (EQT.COMM.pmntType!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("pmntType|"+EQT.COMM.pmntType)
			PRINT(sv_u)
		#}
		ENDIF
		
		IF (EQT.COMM.pmntType!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("dummyPmntType|"+EQT.COMM.pmntType)
			PRINT(sv_u)
		#}
		ENDIF
		
		IF (EQT.COMM.commCode!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("commCode|"+EQT.COMM.commCode)
			PRINT(sv_u)
		#}
		ENDIF
		
		IF (EQT.COMM.commAmt!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("commAmt|"+EQT.COMM.commAmt)
			PRINT(sv_u)
		#}
		ENDIF
		
		IF (EQT.COMM.commCrncy!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("commCrncy|"+EQT.COMM.commCrncy)
			PRINT(sv_u)
			
			sv_u = urhk_SetOrbOut("vatCrncy|"+EQT.COMM.commCrncy)
			PRINT(sv_u)
		#}
		ENDIF
		
		IF (EQT.COMM.vatAmt!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("vatAmt|"+EQT.COMM.vatAmt)
			PRINT(sv_u)
		#}
		ENDIF
		
		IF (EQT.COMM.drAcctNum!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("drAcctNum|"+EQT.COMM.drAcctNum)
			PRINT(sv_u)
		#}
		ENDIF
		
		IF (EQT.COMM.crAcctNum!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("crAcctNum|"+EQT.COMM.crAcctNum)
			PRINT(sv_u)
		#}
		ENDIF
		
		IF (EQT.COMM.taxAcctNum!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("taxAcctNum|"+EQT.COMM.taxAcctNum)
			PRINT(sv_u)
		#}
		ENDIF
		
		IF (EQT.COMM.pmntPrps!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("pmntPrps|"+EQT.COMM.pmntPrps)
			PRINT(sv_u)
		#}
		ENDIF
		
		IF (EQT.COMM.ratecode!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("ratecode|"+EQT.COMM.ratecode)
			PRINT(sv_u)
		#}
		ENDIF
		
		IF (EQT.COMM.rate!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("rate|"+EQT.COMM.rate)
			PRINT(sv_u)
		#}
		ENDIF
		
		IF(EQT.COMM.rvsTranId=="") THEN
		#{
			EQT.COMM.rvsTranId = " "
			EQT.COMM.rvsTranDate = " "
		#}
		endif
		
		sv_u = urhk_SetOrbOut("rvsTranId|"+EQT.COMM.rvsTranId)
		PRINT(sv_u)
		
		sv_u = urhk_SetOrbOut("rvsTranDate|"+EQT.COMM.rvsTranDate)
		PRINT(sv_u)
	#}
	ENDIF
	
	sv_s = "commDesc|SELECT COMM_DESC FROM CUSTOM.C_CPM WHERE COMM_CODE=?SVAR AND BANK_ID=?SVAR"
	BANCS.INPARAM.BINDVARS = EQT.COMM.commCode  + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	IF(sv_d != 0)THEN
	#{
		EQT.COMM.errorFlg ="Y"
		EQT.COMM.errorMsg ="Error in Database Operation!!"
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		EQT.COMM.commDesc =  BANCS.OUTPARAM.commDesc
		PRINT(EQT.COMM.commDesc)
	#}
	ENDIF
	
	IF (EQT.COMM.commDesc!= "") THEN
	#{
		sv_u = urhk_SetOrbOut("commDesc|"+EQT.COMM.commDesc)
		PRINT(sv_u)
	#}
	ENDIF
	
	BANCS.INPUT.acctNum = EQT.COMM.drAcctNum
	CALL("compay009.scr")
	
	IF(EQT.COMM.errorFlg !="Y") THEN
	#{
		IF (EQT.COMM.acctEntityId!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("acctEntityId|"+EQT.COMM.acctEntityId)
			PRINT(sv_u)
		#}
		ENDIF
		
		IF (EQT.COMM.acctSolId!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("acctSolId|"+EQT.COMM.acctSolId)
			PRINT(sv_u)
		#}
		ENDIF
		
		IF (EQT.COMM.acctCrncy!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("acctCrncy|"+EQT.COMM.acctCrncy)
			PRINT(sv_u)
		#}
		ENDIF
		
		IF (EQT.COMM.acctName!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("acctName|"+EQT.COMM.acctName)
			PRINT(sv_u)
		#}
		ENDIF
	#}
	ENDIF
	
	BANCS.INPUT.acctNum = EQT.COMM.crAcctNum
	CALL("compay009.scr")
	
	IF(EQT.COMM.errorFlg !="Y") THEN
	#{
		IF (EQT.COMM.acctEntityId!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("crAcctEntityId|"+EQT.COMM.acctEntityId)
			PRINT(sv_u)
		#}
		ENDIF
		
		IF (EQT.COMM.acctSolId!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("crAcctSolId|"+EQT.COMM.acctSolId)
			PRINT(sv_u)
		#}
		ENDIF
		
		IF (EQT.COMM.acctCrncy!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("crAcctCrncy|"+EQT.COMM.acctCrncy)
			PRINT(sv_u)
		#}
		ENDIF
		
		IF (EQT.COMM.acctName!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("crAcctName|"+EQT.COMM.acctName)
			PRINT(sv_u)
		#}
		ENDIF
	#}
	ENDIF
	
	BANCS.INPUT.acctNum = EQT.COMM.taxAcctNum
	CALL("compay009.scr")
	
	IF(EQT.COMM.errorFlg !="Y") THEN
	#{
		IF (EQT.COMM.acctEntityId!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("taxAcctEntityId|"+EQT.COMM.acctEntityId)
			PRINT(sv_u)
		#}
		ENDIF
		
		IF (EQT.COMM.acctSolId!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("taxAcctSolId|"+EQT.COMM.acctSolId)
			PRINT(sv_u)
		#}
		ENDIF
		
		IF (EQT.COMM.acctCrncy!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("taxAcctCrncy|"+EQT.COMM.acctCrncy)
			PRINT(sv_u)
		#}
		ENDIF
		
		IF (EQT.COMM.acctName!= "") THEN
		#{
			sv_u = urhk_SetOrbOut("taxAcctName|"+EQT.COMM.acctName)
			PRINT(sv_u)
		#}
		ENDIF
	#}
	ENDIF
	
	sv_s=urhk_SetOrbOut("SuccessOrFailure|Y")
	
	ENDOFSCRIPT:
		DELETECLASS("EQT", "COMM")
		DELETEREP("EQT")
		
		EXITSCRIPT
		TRACE OFF
END-->
