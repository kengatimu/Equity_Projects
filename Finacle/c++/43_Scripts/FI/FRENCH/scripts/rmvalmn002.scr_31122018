############################################################################################################									
# Source Name						: 	rmvalmn002.scr
# Date								: 24-02-2013
# Description						: This script is used to populate the values
# Input								: None
# Output							: To get populate the values
# Called Script						: SRV_LdgNRlzOutRemittance_pre_process_data.scr
# Calling Script					: None
# Author     						: Shilpa
# Bank								:
# Modification History				:
#		< Version No.> 			<Date>		<Author Name> 		<Description>
#		 0.1					24-02-2013 	Shilpa 			Original Version
#		 0.2				11-09-2013	 Suhasini	          Porting 2 changes done
############################################################################################################

<--START
CHECKSUM="76f23e211b56e298a0ea7e2003d934241b4bab15a7fa74df8f3ec69f0ebc75d7"
	TRACE ON
	
	############################################################################
	# calling c_param values
	###########################################################################
	
	BANCS.INPUT.moduleName = "TRADE_FINANCE"
	BANCS.INPUT.parameterId = "PARTRNNUM"
	CALL ("getcparamvalue.scr")
	IF (BANCS.OUTPUT.parameterVal != "")THEN
	#{
		CUST.ORMVAL.parttrannum = BANCS.OUTPUT.parameterVal
		print(CUST.ORMVAL.parttrannum)
		#}
	ELSE
		#{
		CUST.ORMVAL.errMsg = "PART TRAN SRL NUMIS NOT SET"
		GOTO ENDSCRIPT
	#}
	ENDIF
	
	
	############################################################################
	# Getting values from front end
	############################################################################
	
	sv_a=urhk_SRV_GetVal("remTranDtls.tranDetails.tranLL.<rec_3>.drCrFlg")
	print(sv_a)
	IF (sv_a == 0 ) THEN
	#{
		CUST.ORMVAL.crdrflg= BANCS.OUTPARAM.srvValue
		print(CUST.ORMVAL.crdrflg)
	#}
	ENDIF
	
	sv_a=urhk_SRV_GetVal("remTranDtls.tranDetails.tranLL.<rec_3>.acctType")
	print(sv_a)
	IF (sv_a == 0 ) THEN
	#{
		CUST.ORMVAL.accttype= BANCS.OUTPARAM.srvValue
		print(CUST.ORMVAL.accttype)
	#}
	ENDIF
	
	sv_a=urhk_SRV_GetVal("remCurrEventDtls.paySysId.refCode")
	print(sv_a)
	IF (sv_a == 0 ) THEN
	#{
		CUST.ORMVAL.paysisId= BANCS.OUTPARAM.srvValue
		print(CUST.ORMVAL.paysisId)
	#}
	ENDIF
	
	sv_a=urhk_SRV_GetVal("remTranDtls.tranDetails.tranLL.<rec_3>.tranPartcls")
	print(sv_a)
	IF (sv_a == 0 ) THEN
	#{
		CUST.ORMVAL.particular= BANCS.OUTPARAM.srvValue
		print(CUST.ORMVAL.particular)
	#}
	ENDIF
	
	sv_a=urhk_SRV_GetVal("remTranDtls.tranDetails.tranLL.<rec_3>.key.serial_num")
	print(sv_a)
	IF (sv_a == 0 ) THEN
	#{
		CUST.ORMVAL.srlnum= BANCS.OUTPARAM.srvValue
		print(CUST.ORMVAL.srlnum)
	#}
	ENDIF
	
	
	IF (CUST.ORMVAL.srlnum !="") THEN
	#{
		IF ((CINT(CUST.ORMVAL.srlnum) == CINT(CUST.ORMVAL.parttrannum) ) AND (CUST.ORMVAL.crdrflg == "D") AND (CUST.ORMVAL.accttype == CUST.ORMVAL.accttype) AND (CUST.ORMVAL.paysisId == CUST.ORMVAL.paysisId))  THEN
		#{
			CUST.ORMVAL.particular = "RTGS COMMISSION"
			sv_a=urhk_SRV_SetVal("remTranDtls.tranDetails.tranLL.<rec_3>.tranPartcls|RTGS COMMISSION")
			PRINT(sv_a)
			sv_a=urhk_SRV_SetVal("remTranDtls.tranDetails.tranLL.<rec_3>.remarks|RTGS COMMISSION")
			print(sv_a)
			#}
		ELSE
			#{
			GOTO ENDSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	ENDSCRIPT :
		TRACE OFF
		EXITSCRIPT
END-->
