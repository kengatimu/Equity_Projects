#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: checkPayOffAcct.scr
# Type: NA|default
# Description: This script is used for getting the count from custom table, based on which 'Payoff Old Loan A/c' field will be populated.
# Author:
# Date:
# Modification Log:
#============================================================

<--START
CHECKSUM="c88e65b298e82d00d5bdbbc07f8cca8812045eece8a3b679c4ad6f888248b20d"
	TRACE ON
	sv_a = REPEXISTS("CUST")
	IF (sv_a == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	sv_a = CLASSEXISTS("CUST", "LADISB")
	IF (sv_a == 0 ) THEN
	#{
		CREATECLASS("CUST", "LADISB",5)
	#}
	ENDIF
	
	sv_u = urhk_b2k_printrepos("BANCS")
	
	CUST.LADISB.errorFlg = "P"
	CUST.LADISB.errorMsg = "No Error"
	
	sv_s = "recCnt|select count(1) from CUSTOM.C_LADISB_PAYOFF_OLDLOAN where LOAN_FORACID = ?SVAR AND bank_id = ?SVAR AND payoff_flg='N' AND entity_cre_flg = 'N'"
	BANCS.INPARAM.BINDVARS = BANCS.INPUT.LoanaccNum + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	IF(sv_d == 0)THEN
	#{
		PRINT(BANCS.OUTPARAM.recCnt)
		CUST.LADISB.recCnt = BANCS.OUTPARAM.recCnt
		PRINT(CUST.LADISB.recCnt)
		#}
	ELSE
		#{
		CUST.LADISB.recCnt = 0
		CUST.LADISB.errorFlg = "E"
		CUST.LADISB.errorMsg ="Error in DB operation!"
	#}
	ENDIF
	
	sv_u = urhk_SetOrbOut("recordPresent|"+CUST.LADISB.recCnt)
	sv_u = urhk_SetOrbOut("errorFlg|"+CUST.LADISB.errorFlg)
	sv_u = urhk_SetOrbOut("errorMsg|"+CUST.LADISB.errorMsg)
	
	#-------------------------------------------------------------------------------
	# Deleting custom repositories and classes created
	#-------------------------------------------------------------------------------
	IF(CLASSEXISTS("CUST","LADISB") == 1) THEN
	#{
		DELETECLASS("CUST","LADISB")
	#}
	ENDIF
	
	IF(REPEXISTS("CUST") == 1) THEN
	#{
		DELETEREP("CUST")
	#}
	ENDIF
	TRACE OFF
END-->
