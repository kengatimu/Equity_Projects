 #============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cpenalmn001.scr
# Type: NA|default
# Description: GETDATA SCRIPT OF CPENAL MENU
# Author: Sridevi
# Date: 7/16/19 16:27 PM
# Modification Log:
#============================================================

<--START

	TRACE ON
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CPNL Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CPNL")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CPNL",5)
	#}
	ENDIF

IF(FIELDEXISTS(BANCS.INPUT.funcCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.funcCode) != "")THEN
		#{
			CUST.CPNL.funcCode = TOUPPER(TRIM(BANCS.INPUT.funcCode))
			PRINT(CUST.CPNL.funcCode)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.acctId))THEN
	#{
		IF(TRIM(BANCS.INPUT.acctId) != "")THEN
		#{
			CUST.CPNL.acctId = TOUPPER(TRIM(BANCS.INPUT.acctId))
			PRINT(CUST.CPNL.acctId)
		#}
		ENDIF
	#}
	ENDIF
	
	
	IF(FIELDEXISTS(BANCS.INPUT.addlWavingOffAmt))THEN
	#{
		IF(TRIM(BANCS.INPUT.addlWavingOffAmt) != "")THEN
		#{
			CUST.CPNL.addlWavingOffAmt = TOUPPER(TRIM(BANCS.INPUT.addlWavingOffAmt))
			PRINT(CUST.CPNL.addlWavingOffAmt)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.waivingOffReason))THEN
	#{
		IF(TRIM(BANCS.INPUT.waivingOffReason) != "")THEN
		#{
			CUST.CPNL.waivingOffReason = TOUPPER(TRIM(BANCS.INPUT.waivingOffReason))
			PRINT(CUST.CPNL.waivingOffReason)
		#}
		ENDIF
	#}
	ENDIF
	
	
	sv_s = "ACID,SOL_ID,ACCT_CCY_CODE,TOTAL_WAIVED_OFF_AMT,PENAL_ACCRUED_AMOUNT_DR,PENAL_APPLIED_AMOUNT_DR"
	sv_s = sv_s + "|SELECT ACID,SOL_ID,ACCT_CCY_CODE,TOTAL_WAIVED_OFF_AMT,PENAL_ACCRUED_AMOUNT_DR,PENAL_APPLIED_AMOUNT_DR FROM CUSTOM.C_PNAT WHERE FORACID = ?SVAR AND BANK_ID =?SVAR"
			
	BANCS.INPARAM.BINDVARS = CUST.CPNL.acctId + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_b = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_b)
	IF(sv_b == 0)THEN
	#{
		CUST.CPNL.SOL_ID = BANCS.OUTPARAM.SOL_ID
		PRINT(CUST.CPNL.SOL_ID)
		CUST.CPNL.ACCT_CCY_CODE = BANCS.OUTPARAM.ACCT_CCY_CODE
		PRINT(CUST.CPNL.ACCT_CCY_CODE)
		CUST.CPNL.ACID = BANCS.OUTPARAM.ACID
		PRINT(CUST.CPNL.ACID)
		CUST.CPNL.TOTAL_WAIVED_OFF_AMT = BANCS.OUTPARAM.TOTAL_WAIVED_OFF_AMT
		PRINT(CUST.CPNL.TOTAL_WAIVED_OFF_AMT)
		CUST.CPNL.PENAL_ACCRUED_AMOUNT_DR = BANCS.OUTPARAM.PENAL_ACCRUED_AMOUNT_DR
		PRINT(CUST.CPNL.PENAL_ACCRUED_AMOUNT_DR)
		CUST.CPNL.PENAL_APPLIED_AMOUNT_DR = BANCS.OUTPARAM.PENAL_APPLIED_AMOUNT_DR
		PRINT(CUST.CPNL.PENAL_APPLIED_AMOUNT_DR)
	#}
	ELSE
	#{
		CUST.CPNL.errFlg = "Y"
		CUST.CPNL.errMsg = "Failed to fetch the data"
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		sv_u = urhk_SetOrbOut("Error_1|Error^Failed to fetch the data^acctId")
		PRINT(sv_u)
		GOTO ENDOFSCRIPT1
	#}
	ENDIF
	
	IF(CUST.CPNL.funcCode == "W")THEN
	#{
		IF(CINT(CUST.CPNL.addlWavingOffAmt) <= 0)THEN
		#{
			CUST.CPNL.errFlg = "Y"
			CUST.CPNL.errMsg = "Additional Waiving Off amount should be greater than 0"
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			sv_u = urhk_SetOrbOut("Error_1|Error^Additional Waiving Off amount should be greater than 0^acctId")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT1
		#}
		ENDIF
	#}
	ENDIF
	###########################################################################
	#validation
	###########################################################################
	
	#(((PENAL_ACCRUED_AMOUNT_DR -  PENAL_APPLIED_AMOUNT_DR) - Total_Waived_Off_Amount ) - Additional Waiving off amount)) >=0
		
		print(CUST.CPNL.PENAL_ACCRUED_AMOUNT_DR)
		print(CUST.CPNL.PENAL_APPLIED_AMOUNT_DR)
		print(CUST.CPNL.TOTAL_WAIVED_OFF_AMT)
		print(CUST.CPNL.addlWavingOffAmt)
		sv_a = CDOUBLE(CUST.CPNL.PENAL_ACCRUED_AMOUNT_DR) - CDOUBLE(CUST.CPNL.PENAL_APPLIED_AMOUNT_DR)
		print(sv_a)
		sv_b = sv_a - CDOUBLE(CUST.CPNL.TOTAL_WAIVED_OFF_AMT)
		print(sv_b)
		sv_c = sv_b - CDOUBLE(CUST.CPNL.addlWavingOffAmt)
		print(sv_c)
		
		CUST.CPNL.diff = sv_c
		sv_z = "0"
		
		
		IF(CDOUBLE(CUST.CPNL.diff) < CDOUBLE(sv_z))THEN
		#{
			CUST.CPNL.errFlg = "Y"
			CUST.CPNL.errMsg = "The sum of existing waived off amount and additional amount to be waived does not match with the unsatisfied penal interest"
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			sv_u = urhk_SetOrbOut("Error_1|Error^The sum of existing waived off amount and additional amount to be waived does not match with the unsatisfied penal interest^acctId")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT1
		#}
		ENDIF
		
	ENDOFSCRIPT1:
	
END-->