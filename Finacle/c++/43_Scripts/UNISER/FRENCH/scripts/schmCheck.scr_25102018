#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: schmCheck.scr
# Type: NA|default
# Description: schmCheck.scr
# Author: AnuragGhosh_D
# Date: 9/28/18 5:51 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="5758aef7f1856603ee28fa99c2966292b4876ae6f844c0a9911c00af3aae9722"
	# Script body goes here
	############Fecthinh scheme codes from CPARAM ###########################
	sv_s = "LOANS"
	sv_t = "LOANS_VAT_SCHM"
	sv_p =  BANCS.STDIN.contextBankId
	
	sv_k = "outVal|select PARAMETER_VALUE from custom.C_CPVALUE "
	#sv_k = sv_k + " where MODULE_NAME= ?SVAR "
	sv_k = sv_k + " WHERE PARAMETER_ID = ?SVAR "
	sv_k = sv_k + " AND BANK_ID = ?SVAR "
	sv_k = sv_k + " AND DEL_FLG = 'N'"
	
	PRINT(sv_k)
	#BANCS.INPARAM.BINDVARS =  sv_s + "|" + sv_t + "|" + sv_p
	BANCS.INPARAM.BINDVARS =  sv_t + "|" + sv_p
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_k = urhk_dbSelectWithBind(sv_k)
	PRINT(sv_k)
	
	
	######################################################################################################
	######## Getting Variable values from c_ table ##############
	
	sv_a= ""
	sv_b = BANCS.OUTPARAM.outVal
	sv_c = GETPOSITION(sv_b,",")
	IF(sv_c!=0) THEN
	#{
		
		sv_l =STRLEN(sv_b)
		PRINT(sv_l)
		PRINT(sv_c)
		WHILE(sv_c !=0)
		#{
			sv_a =sv_a+"'"+MID$(sv_b,0,sv_c - 1)+"'"+","
			PRINT(sv_a)
			sv_b=MID$(sv_b,sv_c,sv_l)
			PRINT(sv_b)
			sv_c = GETPOSITION(sv_b,",")
			sv_l =STRLEN(sv_b)
			PRINT(sv_l)
			PRINT(sv_c)
			IF (sv_c==0) THEN
			#{
				sv_a = sv_a +"'"+sv_b+"'"
				PRINT(sv_a)
			#}
			ENDIF
		#}
		DO
		sv_y = sv_a
		#}
	ELSE
		#{
		
		sv_b = "'"+sv_b+"'"
		sv_y = sv_b
		PRINT(sv_b)
	#}
	ENDIF
	
	PRINT(sv_y)
	
	sv_k =""
	
	sv_s = "LOANS"
	sv_t = "LOANS_VAT_SCHM"
	sv_p =  BANCS.STDIN.contextBankId
	
	sv_k = "wc|select COUNT(PARAMETER_VALUE)wc from custom.c_cpvalue"
	#sv_k = sv_k + " where MODULE_NAME= ?SVAR "
	sv_k = sv_k + " WHERE PARAMETER_ID = ?SVAR "
	sv_k = sv_k + " AND BANK_ID = ?SVAR "
	sv_k = sv_k + " AND DEL_FLG = 'N'"
	sv_k = sv_k + " AND '" + CUST.VAT.schmCode + "' IN ("+sv_y+")  "
	sv_k = sv_k + " AND DEL_FLG = 'N'"
	
	PRINT(sv_k)
	#BANCS.INPARAM.BINDVARS =  sv_s + "|" + sv_t + "|" + sv_p
	BANCS.INPARAM.BINDVARS =   sv_t + "|" + sv_p
	PRINT(BANCS.INPARAM.BINDVARS)
	
	####57571E
	sv_k = urhk_dbSelectWithBind(sv_k)
	PRINT(sv_k)
	IF ( sv_k!=0) THEN
	#{
		BANCS.OUTPUT.errorMsg="Error occurred. Please try again"
		sv_v = urhk_SRV_SetErr(BANCS.OUTPUT.errorMsg)
		EXITSCRIPT
		#}
	ELSE
		#{
		sv_z = BANCS.OUTPARAM.wc
		PRINT(sv_z)
		IF(sv_z > 0)THEN
		#{
			
			CUST.VAT.schmCheck = "Y"
			#}
		ELSE
			#{
			CUST.VAT.schmCheck = "N"
		#}
		ENDIF
		
	#}
	ENDIF
	
	
	########## Checking if VAT is waived off for the account ################
	
	IF(FIELDEXISTS(CUST.VAT.loanAcctVatApl))THEN
	#{
		
		
		sv_s = "count|select count(1) from  CUSTOM.C_VAT_ON_CHRG_TBL where foracid=?SVAR AND vat_appl_FLG='N' "
		sv_s = sv_s+ "AND bank_id=?SVAR "
		sv_s = sv_s+ "AND del_flg='N' "
		PRINT(sv_s)
		BANCS.INPARAM.BINDVARS= CUST.VAT.loanAcctVatApl + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d == 0) THEN
		#{
			CUST.VAT.vatNCount = BANCS.OUTPARAM.count
			PRINT(CUST.VAT.vatNCount)
			
			IF(CUST.VAT.vatNCount > 0) THEN
			#{
				CUST.VAT.schmCheck = "N"
			#}
			ENDIF
		#}
		ENDIF
		
		
	#}
	ENDIF
	
	########## End of Checking if VAT is waived off for the account ################
	
	
	######fetching VAT PERCENT from CPARAM##############################
	
	sv_s = "OT"
	sv_t = "COMM_VAT_PERCENT"
	sv_p =  BANCS.STDIN.contextBankId
	
	sv_k = "vatPcnt|select NVL(PARAMETER_VALUE,0)PARAMETER_VALUE from custom.C_CPVALUE "
	#sv_k = sv_k + " where MODULE_NAME= ?SVAR "
	sv_k = sv_k + " WHERE PARAMETER_ID = ?SVAR "
	sv_k = sv_k + " AND BANK_ID = ?SVAR "
	sv_k = sv_k + " AND DEL_FLG = 'N'"
	
	PRINT(sv_k)
	#BANCS.INPARAM.BINDVARS =  sv_s + "|" + sv_t + "|" + sv_p
	BANCS.INPARAM.BINDVARS =   sv_t + "|" + sv_p
	
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_k = urhk_dbSelectWithBind(sv_k)
	PRINT(sv_k)
	
	IF(sv_k == 0)THEN
	#{
		CUST.VAT.vatPcnt = BANCS.OUTPARAM.vatPcnt
		PRINT(CUST.VAT.vatPcnt)
		
		#}
	ELSE
		#{
		CUST.VAT.vatPcnt = 0
		PRINT(CUST.VAT.vatPcnt)
	#}
	ENDIF
	
	#######Fetching VAT BACID FROM CPARAM##################################
	
	
	
	
	sv_s = "OT"
	sv_t = "COMMISSION_VAT_BACID"
	sv_p =  BANCS.STDIN.contextBankId
	
	sv_k = "vatBacid|select PARAMETER_VALUE from custom.C_CPVALUE "
	#sv_k = sv_k + " where MODULE_NAME= ?SVAR "
	sv_k = sv_k + " WHERE PARAMETER_ID = ?SVAR "
	sv_k = sv_k + " AND BANK_ID = ?SVAR "
	sv_k = sv_k + " AND DEL_FLG = 'N'"
	
	PRINT(sv_k)
	#BANCS.INPARAM.BINDVARS =  sv_s + "|" + sv_t + "|" + sv_p
	BANCS.INPARAM.BINDVARS =   sv_t + "|" + sv_p
	
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_k = urhk_dbSelectWithBind(sv_k)
	PRINT(sv_k)
	
	IF(sv_k == 0)THEN
	#{
		CUST.VAT.vatBacid = BANCS.OUTPARAM.vatBacid
		#}
	ELSE
		#{
		CUST.VAT.vatBacid = 0
	#}
	ENDIF
	
	
	#########################################################################
	
	
	
	ENDOFSCRIPT:
		
		EXITSCRIPT
		
END-->
