#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: Event Based
# Script: SRV_AddLienOnAcct_validate_data.scr
# Type: GCHRG|default
# Description: GCHRG - Calculation of General Charges
# Author: Sridevi_Vutukuru
# Date: 11/8/18 2:46 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="bf38d3bde8562f7bb7e05ff3a4980ace2f807d65f3977a1ab28cd36047345a7f"
	TRACE ON
	
	#-----------Get the input from Front End--------------
	
	IF (FIELDEXISTS(BANCS.STDIN.menuId) != 0) THEN
	#{
		IF(BANCS.STDIN.menuId != "HALM") THEN
		#{
			GOTO FINISH
		#}
		ENDIF
		#}
	ELSE
		#{
		GOTO FINISH
	#}
	ENDIF
	
	IF (FIELDEXISTS(BANCS.STDIN.INIT_MENU_ID) != 0) THEN
	#{
		IF(BANCS.STDIN.INIT_MENU_ID == "HBJE") THEN
		#{
			GOTO FINISH
		#}
		ENDIF
	#}
	ENDIF
	
	sv_x = urhk_SRV_NumOfRecs("lienMaintLL")
	sv_j = BANCS.OUTPARAM.srvValue
	PRINT(sv_j)
	
	IF (sv_j > 0) THEN
	#{
		sv_h = 0
		WHILE (sv_j > sv_h)
		#{
			sv_i = format$(sv_h,"%d")
			PRINT(sv_i)
			
			sv_a =  urhk_SRV_GetVal("lienMaintLL.<rec_" + sv_i + ">.lienReasonCode.refCode")
			PRINT(BANCS.OUTPARAM.srvValue)
			
			IF (BANCS.OUTPARAM.srvValue == "DCHRG") THEN
			#{
				
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				sv_u = urhk_SRV_SetErr("Lien for DCHRG cannot be added")
				
				GOTO FINISH
			#}
			ENDIF
			sv_h = sv_h + 1
			PRINT(sv_h)
		#}
		DO
	#}
	ENDIF
	
	FINISH:
		
		EXITSCRIPT
		
		TRACE OFF
		
END-->

