<--START
CHECKSUM="790bb79cb967328f7e20e8e5dd41c77cabedca98993709878d59cccd9037c449"
	TRACE ON
	
	####################################################################
	#section      :creating new repository if required
	#Description  :Checks for the existence of the repository STDWFS and
	#               creates a new one by that name if it does not exist
	# CR no: CTASK0014683
	#####################################################################
	
	IF (REPEXISTS("CUST") == 0 ) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	############################################################################
	#section      :creating new class if required
	#Desscription :checks for the existence of the class GLBDATA in the
	#repository
	#               create a new one if it does not exist
	##############################################################################
	
	
	IF (CLASSEXISTS("CUST","DATA") == 0) THEN
	#{
		CREATECLASS("CUST","DATA",5)
	#}
	ENDIF
	
	############################################
	#Assigns the values to the variables
	############################################
	
	sv_a = urhk_b2k_PrintRepos("BANCS")
	print(sv_a)
	sv_y=0
	
	IF( (FIELDEXISTS(("BANCS").("INPUT").("Bankid"))== 0) ) THEN
	#{
		CUST.DATA.errMsg="Kindly Supply Bank id"
		sv_e=urhk_SetOrbOut("Error|"+CUST.DATA.errMsg)
		GOTO End
		#}
	ELSE
		#{
		CUST.DATA.Bankid=BANCS.INPUT.Bankid
		print(CUST.DATA.Bankid)
	#}
	ENDIF
	
	IF( (FIELDEXISTS(("BANCS").("INPUT").("BranchCode"))== 0) ) THEN
	#{
		CUST.DATA.errMsg="Kindly Supply BranchCode"
		sv_e=urhk_SetOrbOut("Error|"+CUST.DATA.errMsg)
		GOTO End
		#}
	ELSE
		#{
		CUST.DATA.BranchCode=BANCS.INPUT.BranchCode
		print(CUST.DATA.BranchCode)
	#}
	ENDIF
	
	
	############################################
	# Fetching the value from table
	############################################
	
	sv_q = "user_id,emp_name| "
	sv_q = sv_q + " select user_id,emp_name  from CRMUSER.AGENTS a ,  CRMUSER.BIZCENTER b ,CRMUSER.USERS  c ,tbaadm.upr d  , tbaadm.get e where d.user_id =c.loginid and a.personid =c.personid and  b.BCID =a.BCID  "
	sv_q = sv_q + " and   d.home_bank_id =?SVAR  and d.del_flg  ='N' and e.emp_id =d.user_emp_id and d.sol_id=?SVAR  and d.user_work_class in ('150','200') and BCCODE=d.sol_id and rownum<2 "
	print(sv_q)
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + CUST.DATA.BranchCode
	
	sv_p= urhk_dbSelectWithBind(sv_q)
	print(sv_p)
	
	IF (sv_p==0) THEN
	#{

		CUST.DATA.varValue1 = BANCS.OUTPARAM.user_id
		print (CUST.DATA.varValue1)
		CUST.DATA.varValue2 = BANCS.OUTPARAM.emp_name
		print (CUST.DATA.varValue2)
		sv_z = urhk_SetOrbOut("USER_ID|"+CUST.DATA.varValue1)
		print(sv_z)
		sv_z = urhk_SetOrbOut("USER_NAME|"+CUST.DATA.varValue2)
		print(sv_z)
		
		sv_e=urhk_SetOrbOut("StattusCode|1")
		#}
	ELSE
		#{
		CUST.DATA.errMsg="No data for the sspecified items"
		sv_e=urhk_SetOrbOut("StattusCode|2")
		sv_e=urhk_SetOrbOut("Error|"+CUST.DATA.errMsg)
	#}
	ENDIF
	
	End:
		EXITSCRIPT
		TRACE OFF
END-->
