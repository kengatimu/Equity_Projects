#------------------------------------------------------------------------------------------------
#   Name            : b_opnsbacct.scr
#   Description     : This script is called from SRV_OpenSBAcct_pre_process_data.scr
#   Date            : 28-05-2013
#   Author          : jane
#   Input           : NA
#   Output          : NA
#   Called Script   : SRV_OpenSBAcct_pre_process_data.scr
#   Calling Script  : NA
#   Menu Option     : HOAACSB
#   Srl. No         Date            Author          	Description.
#   -------         ------          ------          	------------
#   1.0             28-05-2013      jane     		Original Version
#-----------------------------------------------------------------------------------------------
<--start
trace on

IF (CLASSEXISTS("CUST","DATA") == 0) THEN
#{
    CREATECLASS("CUST","DATA",5)
#}
ENDIF

sv_a=urhk_B2k_PrintRepos("BANCS")

sv_a = urhk_SRV_GetVal("acctCommonInfo.acctGeneralInfo.acctBasic.schmCode.schmCode")
sv_b=BANCS.OUTPARAM.srvValue
print(sv_b)

sv_a = urhk_SRV_GetVal("acctCommonInfo.acctGeneralInfo.acctBasic.custId.cifId")
CUST.DATA.cifid=BANCS.OUTPARAM.srvValue
print(CUST.DATA.cifid)

sv_a = urhk_SRV_GetVal("acctCommonInfo.acctGeneralInfo.acctBasic.acctCrncyCode.crncyCode")
CUST.DATA.crncyCode=BANCS.OUTPARAM.srvValue
print(CUST.DATA.crncyCode)

sv_a = urhk_SRV_GetVal("acctCommonInfo.acctGeneralInfo.acctBasic.solId.solId")
CUST.DATA.solId=BANCS.OUTPARAM.srvValue
print(CUST.DATA.solId)

sv_s = "count1|"
sv_s = sv_s + " SELECT COUNT(1)"
sv_s = sv_s + " FROM TBAADM.GAM"
sv_s = sv_s + " WHERE SCHM_CODE =?SVAR"
sv_s = sv_s + " AND BANK_ID =?SVAR"
sv_s = sv_s + " AND CIF_ID =?SVAR AND acct_cls_flg='N' AND ACCT_CRNCY_CODE=?SVAR "
PRINT(sv_s)
BANCS.INPARAM.BINDVARS=sv_b + "|" + BANCS.STDIN.contextBankId + "|" + CUST.DATA.cifid + "|" + CUST.DATA.crncyCode
print(BANCS.INPARAM.BINDVARS)
sv_d =  urhk_dbSelectWithBind(sv_s)
print(sv_d)
IF (sv_d == 0 ) THEN
 #{
	CUST.DATA.count1 = BANCS.OUTPARAM.count1
	PRINT(CUST.DATA.count1)
	IF(CUST.DATA.count1!="0")THEN
	#{
		IF (BANCS.STDIN.contextBankId=="99") THEN
			IF(sv_b != "FS113") then
				IF(CUST.DATA.count1!="0")THEN
					sv_u = urhk_SRV_SetErr("Duplicate Account/Another account exists with scheme code " + sv_b)
					BANCS.OUTPUT.successOrFailure="F"
					GOTO ERROREXIT
				ENDIF
			ENDIF
		ELSE
			IF (BANCS.STDIN.contextBankId=="54") THEN
				IF (sv_b == "VA300") THEN
					IF (FIELDEXISTS(CUST.DATA.count1) == 1) THEN
						IF (cint(CUST.DATA.count1)> 5) THEN
							sv_u = urhk_SRV_SetErr("Duplicate Account/Another account exists with scheme code " + sv_b)
							BANCS.OUTPUT.successOrFailure="F"
							GOTO ERROREXIT
						ENDIF
					ENDIF
				ELSE
					sv_u = urhk_SRV_SetErr("Duplicate Account/Another account exists with scheme code " + sv_b)
					BANCS.OUTPUT.successOrFailure="F"
					GOTO ERROREXIT
				ENDIF
			ELSE
				IF (sv_b == "VA300") THEN
					IF (FIELDEXISTS(CUST.DATA.count1) == 1) THEN
						IF (cint(CUST.DATA.count1)> 1) THEN
							sv_u = urhk_SRV_SetErr("Duplicate Account/Another account exists with scheme code " + sv_b)
							BANCS.OUTPUT.successOrFailure="F"
							GOTO ERROREXIT
						ENDIF
					ENDIF
				ELSE
					IF ((BANCS.STDIN.contextBankId=="43") AND  (sv_b == "VA301")) THEN
						print("ok VA301")
					ELSE
						
						sv_u = urhk_SRV_SetErr("Duplicate Account/Another account exists with scheme code " + sv_b)
						BANCS.OUTPUT.successOrFailure="F"
						GOTO ERROREXIT
					ENDIF
				ENDIF

			ENDIF
		ENDIF
		 IF (BANCS.STDIN.contextBankId=="54") THEN
                             IF(sv_b == "SB777") then
                                     IF(CUST.DATA.count1!="0")THEN
                                             sv_u = urhk_SRV_SetErr("Duplicate Account/Another account exists with scheme code " + sv_b)
                                             BANCS.OUTPUT.successOrFailure="F"
                                             GOTO ERROREXIT
                                     ENDIF
                             ENDIF
                 ENDIF

	#}
	ENDIF
 #}
 ELSE
 #{
	GOTO ERROREXIT
 #}
 ENDIF

 sv_s = "count1|"
sv_s = sv_s + " SELECT COUNT(1)"
sv_s = sv_s + " FROM TBAADM.GAM"
sv_s = sv_s + " WHERE BANK_ID =?SVAR"
sv_s = sv_s + " AND CIF_ID =?SVAR AND acct_cls_flg='N'  "
PRINT(sv_s)
BANCS.INPARAM.BINDVARS=BANCS.STDIN.contextBankId + "|" + CUST.DATA.cifid
print(BANCS.INPARAM.BINDVARS)
sv_d =  urhk_dbSelectWithBind(sv_s)
print(sv_d)
IF (sv_d == 0 ) THEN
 #{
        CUST.DATA.count1 = BANCS.OUTPARAM.count1
        PRINT(CUST.DATA.count1)
        IF(CUST.DATA.count1!="0")THEN
        #{
                IF (BANCS.STDIN.contextBankId=="54") THEN
                        IF(sv_b == "SB777") then
                                IF(CUST.DATA.count1!="0")THEN
                                        sv_u = urhk_SRV_SetErr("Duplicate Account/Another account exists with scheme code " + sv_b)
                                        BANCS.OUTPUT.successOrFailure="F"
                                        GOTO ERROREXIT
                                ENDIF
                        ENDIF
                ENDIF
        #}
        ENDIF
 #}
 ELSE
 #{
        GOTO ERROREXIT
 #}
 ENDIF

CUST.DATA.DccId=""
sv_a = urhk_GetChannelInfo("")
print(sv_a)
if (sv_a!=0) then
#{
	sv_u = urhk_SRV_SetErr("Error: Channel ID is not passed/Incorrect")
	BANCS.OUTPUT.successOrFailure="F"
	GOTO ERROREXIT
#}
else
#{
	CUST.DATA.DccId = BANCS.OUTPARAM.DccId
	print(CUST.DATA.DccId)
#}
endif

IF (BANCS.STDIN.contextBankId=="54") THEN 
	IF ((sv_b != "SB777") AND (sv_b != "VA300")) THEN
		IF (((sv_b== "SB100") OR (sv_b== "SB123") OR (sv_b== "SB140") OR (sv_b== "SB103") OR (sv_b== "SB108") OR (sv_b== "CA201")) AND (CUST.DATA.DccId =="MDE")) THEN
			print("okay")
		ELSE
			IF (((sv_b== "SB117") OR (sv_b== "SB118") OR (sv_b== "SB109")) AND (CUST.DATA.DccId =="PHS")) THEN
				print("okay")
			ELSE
				IF ((sv_b== "SB142") AND (CUST.DATA.DccId =="EBK")) THEN
					print("okay")
				ELSE
					IF ((CUST.DATA.DccId!="OMN") AND (CUST.DATA.DccId!="NWG")) THEN
						sv_u = urhk_SRV_SetErr("Account Opening for " + sv_b + " not Allowed With FI " )
						BANCS.OUTPUT.successOrFailure = "F"
						exitscript
					ENDIF
				ENDIF
			ENDIF
		ENDIF
	ENDIF
ELSE
	IF (BANCS.STDIN.contextBankId=="99") THEN
		IF ((CUST.DATA.solId=="6000") OR (CUST.DATA.solId=="6001")) THEN
			IF (CUST.DATA.solId=="6000") THEN
				IF (sv_b != "SB778") THEN
					sv_u = urhk_SRV_SetErr("Account Opening for " + sv_b + " not Allowed With FI " )
					BANCS.OUTPUT.successOrFailure = "F"
				ENDIF
			ENDIF
			IF (CUST.DATA.solId=="6001") THEN
				IF ((sv_b != "FS113") AND (sv_b != "FS100")) THEN
					sv_u = urhk_SRV_SetErr("Account Opening for " + sv_b + " not Allowed With FI " )
					BANCS.OUTPUT.successOrFailure = "F"
				ENDIF
			ENDIF
		ELSE
			sv_u = urhk_SRV_SetErr("Account Opening for " + sv_b + " not Allowed With FI " )
			BANCS.OUTPUT.successOrFailure = "F"
		ENDIF
	ELSE		
		IF (BANCS.STDIN.contextBankId=="43") THEN
			print("okay")
		ELSE
			IF ((sv_b != "SB117") AND (sv_b != "SB152") AND (sv_b != "SB777") AND (sv_b != "SB778")) THEN
				 IF (BANCS.STDIN.contextBankId=="50") THEN
					if((CUST.DATA.DccId=="MIC") and (sv_b=="SB135"))then
						print("okay")
				 	else	
						sv_u = urhk_SRV_SetErr("Account Opening for " + sv_b + " not Allowed With FI " )
						BANCS.OUTPUT.successOrFailure = "F"
					endif
				ENDIF
			ENDIF
		ENDIF
	ENDIF
ENDIF

ERROREXIT:

trace off
end-->


