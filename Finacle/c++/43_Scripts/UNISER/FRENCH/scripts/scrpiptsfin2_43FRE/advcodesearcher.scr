#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: advcodesearcher.scr
# Type: NA|default
# Description: Script is to display advertising code
# Author: lakshmi.rajanala
# Date: 6/12/18 5:35 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="014fff6948e81b02253ff902539916956e40b257db4914da3414fa7ae38ee6a1"
	# Script body goes here
	TRACE ON
	
	#=====================================================
	# Check whether CUST Repository exists else create it
	#=====================================================
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	#====================================================
	# Check whether CINTBLK Class exists else create it
	#====================================================
	
	sv_r = CLASSEXISTS("CUST","CINTBLK")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CINTBLK",5)
	#}
	ENDIF
	
	#====================================================
	#Initializing variables
	#====================================================
	
	CUST.CINTBLK.refCode = ""
	CUST.CINTBLK.descCode = ""
	CUST.CINTBLK.delFlag = ""
	CUST.CINTBLK.refRecType="SBF01"
	CUST.CINTBLK.schmRefType="RA"
	BANCS.INPARAM.BINDVARS = ""
	
	sv_s = "recordCount|"
	sv_s = sv_s + " SELECT COUNT(*)"
	sv_s = sv_s + " FROM tbaadm.LRCT "
	sv_s = sv_s + " WHERE ref_rec_type = ?SVAR AND schm_ref_type = ?SVAR "
	sv_s = sv_s + " AND DEL_FLG != 'Y'"
	#sv_s = sv_s + " ORDER BY REF_CODE"
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.CINTBLK.refRecType + "|" + CUST.CINTBLK.schmRefType
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	IF(sv_d == 0)THEN
	#{
		CUST.CINTBLK.recordCount = BANCS.OUTPARAM.recordCount
		PRINT(CUST.CINTBLK.recordCount)
	#}
	ENDIF
	
	sv_u = urhk_SetOrbOut("recordCount|"+CUST.CINTBLK.recordCount)
	PRINT(sv_u)
	
	
	sv_s = "Code,Description,Del|"
	sv_s = sv_s + " SELECT REF_CODE,decode('INFENG','',NVL(lrct.ALT1_ref_desc,lrct.ref_desc),lrct.ref_desc),lrct.del_flg"
	sv_s = sv_s + " FROM tbaadm.LRCT "
	sv_s = sv_s + " WHERE ref_rec_type = ?SVAR AND schm_ref_type = ?SVAR "
	sv_s = sv_s + " ORDER BY REF_CODE"
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.CINTBLK.refRecType + "|" + CUST.CINTBLK.schmRefType
	
	sv_c = urhk_dbCursorOpenWithBind(sv_s)
	PRINT(sv_c)
	
	IF(sv_c != 0)THEN
	#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_n = BANCS.OUTPARAM.DB_CURSOR_NUMBER
	PRINT(sv_n)
	
	sv_f = urhk_dbCursorFetch(sv_n)
	PRINT(sv_f)
	
	sv_i = 1
	
	WHILE(sv_f == 0)
	#{
		CUST.CINTBLK.refCode = BANCS.OUTPARAM.Code
		CUST.CINTBLK.descCode = TOUPPER(BANCS.OUTPARAM.Description)
		CUST.CINTBLK.delFlag = BANCS.OUTPARAM.Del
		
		sv_s = "advCode_" + Format$(sv_i,"%d") + "|" + CUST.CINTBLK.refCode
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "descCode_" + Format$(sv_i,"%d") + "|" + CUST.CINTBLK.descCode
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_s = "delFlag_" + Format$(sv_i,"%d") + "|" + CUST.CINTBLK.delFlag
		PRINT(sv_s)
		sv_u = urhk_SetOrbOut(sv_s)
		PRINT(sv_u)
		
		sv_i = sv_i + 1
		
		sv_f = urhk_dbCursorFetch(sv_n)
		PRINT(sv_f)
		
	#}
	DO
	
	sv_c = urhk_dbCursorClose(sv_n)
	PRINT(sv_c)
	
	
	ENDOFSCRIPT:
		BANCS.OUTPUT.successOrFailure = "S"
END-->
