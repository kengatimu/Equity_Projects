#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: SRV_ModifyXferTranForPTranPost_pre_process_data.scr
# Type: NA|default
# Description: SRV_ModifyXferTranForPTranPost_pre_process_data.scr
# Author:
# Date:
# Modification Log:
#============================================================

#-------------------------------------------------------------------------
#    Name            :
#    Description     : This script is used to find effective available balance of an account
#    Date            : 15-11-2013
#    Author          : Bharath
#    Input           : NA
#    Output			 : NA
#    Called Script   : NA
#    Calling Script  : s_shdwbalchrgmn001.scr
#    Menu Option     : HTM
#    Srl. No                    Date                    Author			Description.
#    ------                     ----------				-------			---------------
#    1.0                        15-11-2013				Bharath			Original Version
#---------------------------------------------------------------------------
<--START
CHECKSUM="092528bcaf30976995ed1603e97b46f6c345d482c6f6869f81ee2c3e857a6711"
	TRACE ON
	
	IF(BANCS.STDIN.onlineOrBatch == "O") THEN
	#{
		CALL("s_shdwbalchrgmn001.scr")
		#CALL("ratetmfxmn001.scr")
	#}
	ENDIF
	
        ## Below steps added to validate treasury RefNum rates and custom map rates
        sv_u = urhk_SRV_NumOfRecs("tranDtl.partTranDetailLL")
        PRINT(sv_u)
        IF (sv_u == 0) THEN
        #{
                sv_a = BANCS.OUTPARAM.srvValue
                PRINT(sv_a)
                IF(sv_a != 0)THEN
                #{
                        sv_l = 0
                        PRINT(sv_l)
                        WHILE(sv_l < sv_a)
                        #{
                                sv_v = Format$(sv_l,"%d")
                                sv_r = "<rec_"+sv_v+">"
                                PRINT(sv_r)
                                sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".treaRefNum")
                                PRINT(sv_u)
                                sv_b = BANCS.OUTPARAM.srvValue
                                PRINT(sv_b)
                                IF(sv_b != "")THEN
                                #{
                                        sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".acctId.crncyCode")
                                        PRINT(sv_u)
                                        sv_c = BANCS.OUTPARAM.srvValue
                                        PRINT(sv_c)
                                        sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".refCrncyCode.crncyCode")
                                        PRINT(sv_u)
                                        sv_d = BANCS.OUTPARAM.srvValue
                                        PRINT(sv_d)
                                        sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".rateCode.refCode")
                                        PRINT(sv_u)
                                        sv_e = BANCS.OUTPARAM.srvValue
                                        PRINT(sv_e)
                                        sv_u = urhk_SRV_GetVal("tranHdr.tranTypeSubType.tranType")
                                        PRINT(sv_u)
                                        sv_f = BANCS.OUTPARAM.srvValue
                                        PRINT(sv_f)
                                        sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".partTranType.code")
                                        PRINT(sv_u)
                                        sv_g = BANCS.OUTPARAM.srvValue
                                        PRINT(sv_g)
                                        sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".rate")
                                        PRINT(sv_u)
                                        sv_h = BANCS.OUTPARAM.srvValue
                                        PRINT(sv_h)
                                        sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".treaRate")
                                        PRINT(sv_u)
                                        sv_i = BANCS.OUTPARAM.srvValue
                                        PRINT(sv_i)
                                        CALL("ratecodehtm001.scr")
                                #}
                                ENDIF

                                sv_l= sv_l+1

                        #}
                        DO
		#}
		ENDIF	
        #}
        ENDIF

        IF(FIELDEXISTS(BANCS.STDIN.menuId))THEN
        #{
                IF(BANCS.STDIN.menuId == "HTM")THEN
                #{
                        CALL("CifChrgCheck.scr")
                #}
                ENDIF
        #}
        ENDIF

	EXITSCRIPT
END-->
