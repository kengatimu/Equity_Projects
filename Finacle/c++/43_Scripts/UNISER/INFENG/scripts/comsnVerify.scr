#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: comsnVerify.scr
# Type: NA|default
# Description: comsnVerify.scr
# Author: shivakrishna.d
# Date: 6/18/18 7:22 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="2cbdfafc7c123a2792194dd0e0fb5706c27c7fe9bf5d936112e585a0144dd4d5"
	# Script body goes here
	TRACE ON
	
	sv_a = REPEXISTS("EQT")
	IF (sv_a == 0) THEN
	#{
		CREATEREP("EQT")
	#}
	ENDIF
	sv_a = CLASSEXISTS("EQT", "COMM")
	IF (sv_a == 0 ) THEN
	#{
		CREATECLASS("EQT", "COMM", 5)
	#}
	ENDIF
	
	PRINT(BANCS.INPUT.commCode)
	EQT.COMM.commCode =BANCS.INPUT.commCode
	EQT.COMM.msg=" "
	EQT.COMM.recInMain = "Y"
	
	sv_j = "commDesc,commCrncy,commVat,commpcnt,mincomm,rcreUser,rcreTime|SELECT  COMM_DESC, CRNCY_CODE, VAT_RATE, COMM_PCNT,MIN_COMM, RCRE_USER_ID, TO_CHAR(RCRE_TIME,'MM-DD-YYYY HH:MI:SS')  FROM CUSTOM.C_COMM_MOD "
	sv_j = sv_j + " WHERE COMM_CODE =?SVAR AND BANK_ID=?SVAR  "
	PRINT(sv_j)
	
	BANCS.INPARAM.BINDVARS =  EQT.COMM.commCode + "|" +  BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_e = urhk_dbSelectWithBind(sv_j)
	PRINT(sv_e)
	
	IF(sv_e!=0)THEN
	#{
		EQT.COMM.msg = "Error in Database Operation!!"
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		sv_u = urhk_SetOrbOut("Error_1|ERR^"+EQT.COMM.msg)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	EQT.COMM.commDesc = BANCS.OUTPARAM.commDesc
	EQT.COMM.commCrncy = BANCS.OUTPARAM.commCrncy
	EQT.COMM.commVat = BANCS.OUTPARAM.commVat
	EQT.COMM.commpcnt = BANCS.OUTPARAM.commpcnt
	EQT.COMM.mincomm = BANCS.OUTPARAM.mincomm
	EQT.COMM.rcreUser = BANCS.OUTPARAM.rcreUser
	EQT.COMM.rcreTime = BANCS.OUTPARAM.rcreTime
	
	PRINT(EQT.COMM.rcreTime)
	##############################################################################
	# Counting Rows from MAIN table
	##############################################################################
	
	sv_d = "mainCount|SELECT count(1) FROM CUSTOM.C_COMM WHERE bank_id=?SVAR AND COMM_CODE=?SVAR"
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + EQT.COMM.commCode
	
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_r = urhk_dbSelectWithBind(sv_d)
	PRINT(sv_r)
	
	IF(sv_r!=0)THEN
	#{
		EQT.COMM.msg = "Error in Fetching Records from DB!!!"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	PRINT(BANCS.OUTPARAM.mainCount)
	##############################################################################
	
	IF(BANCS.OUTPARAM.mainCount==0)THEN
	#{
		EQT.COMM.recInMain = "N"
		GOTO INSERT_IN_MAIN
	#}
	ENDIF
	
	sv_w = "UPDATE CUSTOM.C_COMM set COMM_DESC= ?SVAR, CRNCY_CODE= ?SVAR, VAT_RATE= ?SVAR, COMM_PCNT= ?SVAR,MIN_COMM= ?SVAR, LCHG_USER_ID= ?SVAR, LCHG_TIME= sysdate"
	sv_w = sv_w + " WHERE COMM_CODE = ?SVAR AND BANK_ID = ?SVAR"
	
	PRINT(sv_w)
	
	BANCS.INPARAM.BINDVARS = EQT.COMM.commDesc + "|" + EQT.COMM.commCrncy + "|" + EQT.COMM.commVat + "|" + EQT.COMM.commpcnt + "|" + EQT.COMM.mincomm + "|" +  BANCS.STDIN.userId  +  "|" +  EQT.COMM.commCode + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_e = urhk_dbsqlWithBind(sv_w)
	PRINT(sv_e)
	
	IF(sv_e!=0)THEN
	#{
		EQT.COMM.msg = "Error in Database Operation!!"
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		sv_u = urhk_SetOrbOut("Error_1|ERR^"+EQT.COMM.msg)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	INSERT_IN_MAIN:
		
		IF(EQT.COMM.recInMain=="N")THEN
		#{
			sv_w = "INSERT INTO CUSTOM.C_COMM  (COMM_CODE, COMM_DESC, CRNCY_CODE, VAT_RATE, COMM_PCNT,MIN_COMM, DEL_FLG, RCRE_USER_ID, RCRE_TIME, LCHG_USER_ID, LCHG_TIME, BANK_ID) "
			sv_w = sv_w + " VALUES (?SVAR, ?SVAR, ?SVAR, ?SVAR, ?SVAR,?SVAR, 'N', ?SVAR, to_date(?SVAR,'MM-DD-YYYY HH:MI:SS') , ?SVAR, sysdate, ?SVAR)"
			
			PRINT(sv_w)
			
			BANCS.INPARAM.BINDVARS = EQT.COMM.commCode + "|" + EQT.COMM.commDesc + "|" + EQT.COMM.commCrncy + "|" + EQT.COMM.commVat + "|" + EQT.COMM.commpcnt + "|" + EQT.COMM.mincomm + "|" + EQT.COMM.rcreUser + "|" + EQT.COMM.rcreTime + "|" + BANCS.STDIN.userId  +  "|" +   BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			
			sv_e = urhk_dbsqlWithBind(sv_w)
			PRINT(sv_e)
			
			IF(sv_e!=0)THEN
			#{
				EQT.COMM.msg = "Error in Database Operation!!"
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				sv_u = urhk_SetOrbOut("Error_1|ERR^"+EQT.COMM.msg)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		
		IF(sv_e==0) THEN
		#{
			EQT.COMM.msg = "Record has been Verified Successfully."
		#}
		ENDIF
		
		sv_j= "DELETE FROM CUSTOM.C_COMM_MOD WHERE bank_id=?SVAR AND COMM_CODE =?SVAR "
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + EQT.COMM.commCode
		
		PRINT(BANCS.INPARAM.BINDVARS )
		sv_r = urhk_dbsqlWithBind(sv_j)
		PRINT(sv_r)
		
	ENDOFSCRIPT:
		
		EXITSCRIPT
END-->
