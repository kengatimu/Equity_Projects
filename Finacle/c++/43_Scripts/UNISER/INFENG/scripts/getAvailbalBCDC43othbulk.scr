<--START
CHECKSUM="5214eec61b359bd1777a13dfcd0b2e7c1f39c706e705b44212df021eed1d05b5"
	TRACE ON
	sv_k=""
	sv_y=""
	sv_w=""
	
	sv_q="BENEFICIARYACCOUNT,SEQUENCE|"
	sv_q = sv_q + " select distinct FREE_TEXT1,SEQUENCE from  custom.bulk_cif_drc    where ENTITY_CRE_FLG='S' "
	print(sv_q)
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
        PRINT(BANCS.INPARAM.BINDVARS)
	sv_e = urhk_dbCursorOpenWithBind(sv_q)
	
	IF(sv_e==0) THEN
	#{
		sv_m = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		sv_q = urhk_dbCursorFetch(sv_m)
		print(sv_q)
		IF(sv_q == 1) THEN
		#{
			sv_x = urhk_dbCursorClose(sv_m)
			print(sv_x)
			exitscript
		#}
		ENDIF
		print(sv_q)
		WHILE(sv_q==0)
		#{
			
			sv_a=BANCS.OUTPARAM.BENEFICIARYACCOUNT
			print(sv_a)
			sv_l=BANCS.OUTPARAM.SEQUENCE
			print(sv_l)
			sv_h = urhk_getAcctDetailsInRepository(sv_a)
			print(sv_h)
			IF (sv_h==0) THEN
				sv_k = BANCS.OUTPARAM.EffAvailableAmt
				print(sv_k)
			ENDIF

			
			##################
			sv_h = mid$(BANCS.STDIN.BODDate,0,10)
			print(sv_h)
			sv_s = "getboddate|select to_char((to_date(?SVAR,'DD-MM-YYYY')),'DD-MON-YYYY') from dual"
			BANCS.INPARAM.BINDVARS =  sv_h
			sv_d = urhk_dbSelectWithBind(sv_s)
			print(sv_d)
			IF (sv_d =="0") THEN
			#{
				print(BANCS.OUTPARAM.getboddate)
				sv_y = BANCS.OUTPARAM.getboddate
				print(sv_y)
				
				BANCS.INPARAM.BINDVARS = sv_k + "|" + sv_a + "|" + sv_l
				print(BANCS.INPARAM.BINDVARS)
				sv_j = " update   custom.bulk_cif_drc  set ENTITY_CRE_FLG='Y',ERROR_CODE=?SVAR where FREE_TEXT1=?SVAR and ENTITY_CRE_FLG='S' and SEQUENCE=?SVAR  "
				sv_w = urhk_dbSQLWithBind(sv_j)
				sv_w = urhk_dbSQLWithBind("COMMIT")
				print(sv_j)
			#}
			ENDIF
			
			sv_q = urhk_dbCursorFetch(sv_m)
			print(sv_q)
		#}
		DO
		TRACE OFF
		EXITSCRIPT
		
	END-->
