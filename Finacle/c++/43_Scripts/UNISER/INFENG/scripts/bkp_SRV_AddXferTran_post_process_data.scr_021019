#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: SRV_AddXferTran_post_process_data.scr
# Type: NA|default
# Description: SRV_AddXferTran_post_process_data.scr
# Author:
# Date:
# Modification Log:
#============================================================

#-------------------------------------------------------------------------------------
# Script name           : SRV_AddXferTran_post_process_data.scr
# Date                  : 15-04-2013
# Description           : Call the validation script.
# Author                : Divya
# Calling Script	   	: none
# Called Scripts        : s_chqopndp001.scr
# Menu Option			: HTM
# Srl. No		Date			Author			Description.
# -------		----			------			----------------
# 1.0			15-04-2013		Divya			Original Version
# 2.0			02-07-2013		Bharath			removed call stmt s_chargedp001.scr
# 3.0			11-07-2013		Bharath	 		Added onlineorBatch condition
# 4.0			01-08-2013		Mohan Arudi		Added CIETXN exitscript condition
# 5.0			28-08-2013		Mohan Arudi		Added call statement in CIETXN Block
#-------------------------------------------------------------------------------------
<--START
CHECKSUM="daa5166df9cfe6c96908bc994817f8f83b435f7e691f6daafd98660ec581333f"
	TRACE ON
	
	IF(FIELDEXISTS(BANCS.STDIN.menuId))THEN
	#{
		IF((BANCS.STDIN.menuId == "CIETXN") OR (BANCS.STDIN.menuId == "CIEREV"))THEN
		#{
			CALL ("s_chqopndp001.scr")
			EXITSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.STDIN.menuId))THEN
	#{
		IF(BANCS.STDIN.menuId == "HOCCM")THEN
		#{
			EXITSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	
	IF(BANCS.STDIN.onlineOrBatch == "O") THEN
	#{
		CALL("s_chqopndp001.scr")
	#}
	ENDIF
	
	sv_m = "N"
        sv_u = urhk_SRV_NumOfRecs("tranDtl.partTranDetailLL")
        PRINT(sv_u)
        IF (sv_u == 0) THEN
        #{
                sv_a = BANCS.OUTPARAM.srvValue
                PRINT(sv_a)
                IF(sv_a != 0)THEN
                #{
                        sv_l = 0
                        PRINT(sv_l)
                        WHILE(sv_l < sv_a)
                        #{
                                sv_v = Format$(sv_l,"%d")
                                sv_r = "<rec_"+sv_v+">"
                                PRINT(sv_r)
                                sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".acctDtl.schmCode.schmCode")
                                sv_y = BANCS.OUTPARAM.srvValue
                                PRINT(sv_y)
                                BANCS.INPUT.moduleName = "OT"
                                BANCS.INPUT.parameterId = "CREDITLINE_SCHEMES"

                                CALL("getCustomParamValue.scr")

                                PRINT(BANCS.OUTPARAM.parameterVal)

                                sv_z = BANCS.OUTPARAM.parameterVal

                                PRINT(sv_z)
                                PRINT(sv_y)
                                sv_x = GETPOSITION(sv_z,sv_y)
                                PRINT(sv_x)
                                IF(sv_x != "0")THEN
                                #{
                                        sv_m = "Y"
                                        sv_u = urhk_SRV_GetVal("tranDtl.partTranDetailLL." +sv_r+ ".pTranChrgEventId.eventId")
                                        PRINT(BANCS.OUTPARAM.srvValue)
                                        sv_k = BANCS.OUTPARAM.srvValue

                                #}
                                ENDIF
                                sv_l = sv_l + 1
                        #}
                        DO
                #}
                ENDIF
        #}
        ENDIF

        IF(sv_m == "Y")THEN
        #{
                CALL("crlineChrgCheck.scr")
        #}
        ENDIF
TRACE OFF
END-->
