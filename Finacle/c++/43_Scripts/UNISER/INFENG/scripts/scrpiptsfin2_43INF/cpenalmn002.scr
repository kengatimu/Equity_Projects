#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cpenalmn001.scr
# Type: NA|default
# Description: GETDATA SCRIPT OF CPENAL MENU
# Author: Sridevi
# Date: 7/16/19 16:27 PM
# Modification Log:
#============================================================

<--START

	TRACE ON
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CPNL Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CPNL")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CPNL",5)
	#}
	ENDIF
	
	#==========================================================*
	# Intilializing the Variables
	#==========================================================*
	
	
	###########################################################################
	#validation
	###########################################################################
	CUST.CPNL.errFlg = "N"
	CALL("cpenaldp002.scr")
	
	IF(CUST.CPNL.errFlg == "Y")THEN
	#{
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		sv_u = urhk_SetOrbOut("Error_1|Error^"+CUST.CPNL.errMsg+"^acctId")
		PRINT(sv_u)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	CUST.CPNL.BODDate=""
	CUST.CPNL.bodDate = MID$(BANCS.STDIN.BODDate,0,10)
	PRINT(CUST.CPNL.bodDate)
	sv_e = "bod|select TO_CHAR(TO_DATE(?SVAR,'DD-MM-YYYY'),'DD-MON-YY') from dual"
	BANCS.INPARAM.BINDVARS =CUST.CPNL.bodDate
	sv_f = urhk_dbSelectWithBind(sv_e)
	IF(sv_f == 0)THEN
	#{
		CUST.CPNL.BODDate= BANCS.OUTPARAM.bod
		PRINT(CUST.CPNL.BODDate)
	#}
	ENDIF
		
	print(CUST.CPNL.TOTAL_WAIVED_OFF_AMT)
	sv_z = "0"
	IF(CDOUBLE(CUST.CPNL.TOTAL_WAIVED_OFF_AMT) > CDOUBLE(sv_z))THEN
	#{
		sv_z = CDOUBLE(CUST.CPNL.TOTAL_WAIVED_OFF_AMT) + CDOUBLE(CUST.CPNL.addlWavingOffAmt)
		CUST.CPNL.TOTAL_WAIVED_OFF_AMT_new = sv_z
	#}
	ELSE
	#{
		CUST.CPNL.TOTAL_WAIVED_OFF_AMT_new = CUST.CPNL.addlWavingOffAmt
	#}
	ENDIF
	print(CUST.CPNL.TOTAL_WAIVED_OFF_AMT_new)
	###########################################################################
	#C_PNAT UPDATION
	###########################################################################
	
	sv_s = "UPDATE CUSTOM.C_PNAT SET TOTAL_WAIVED_OFF_AMT = ?SVAR,LAST_WAIVED_OFF_DATE = ?SVAR WHERE FORACID = ?SVAR AND BANK_ID =?SVAR"
			
	BANCS.INPARAM.BINDVARS = CUST.CPNL.TOTAL_WAIVED_OFF_AMT_new + "|" + CUST.CPNL.BODDate + "|" + CUST.CPNL.acctId + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_b = urhk_dbSQLWithBind(sv_s)
	PRINT(sv_b)
	IF(sv_b != 0)THEN
	#{
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		sv_u = urhk_SetOrbOut("Error_1|Error^Failed to update the data^acctId")
		PRINT(sv_u)
		GOTO ENDOFSCRIPT
	#}
	ENDIF

	
	###########################################################################
	#iNSERT - 
	###########################################################################

	
	
	sv_s = "INSERT INTO CUSTOM.C_PNAT_WAIVING(FORACID,ACID,SOL_ID,ACCT_CCY_CODE,ADDITIONAL_WAVING_OFF_AMT,"
	sv_s = sv_s + "WAVING_OFF_BOD_DATE,WAVING_OFF_REASON,RCRE_USER_ID,RCRE_TIME,LCHG_USER_ID,LCHG_TIME,BANK_ID) "
	sv_s = sv_s + " VALUES(?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR)"
			
	BANCS.INPARAM.BINDVARS = CUST.CPNL.acctId + "|" + CUST.CPNL.ACID + "|" + CUST.CPNL.SOL_ID + "|" + CUST.CPNL.ACCT_CCY_CODE 
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.CPNL.addlWavingOffAmt + "|" + CUST.CPNL.BODDate + "|" + CUST.CPNL.waivingOffReason
	BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.userId + "|" + CUST.CPNL.BODDate + "|" + BANCS.STDIN.userId + "|" + CUST.CPNL.BODDate + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_b = urhk_dbSQLWithBind(sv_s)
	PRINT(sv_b)
	IF(sv_b != 0)THEN
	#{
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		sv_u = urhk_SetOrbOut("Error_1|Error^Failed to update the data^acctId")
		PRINT(sv_u)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	CUST.CPNL.resultMsg = "Updated the additional waiving amount successfully"
	sv_u = urhk_SetOrbOut("RESULT_MSG|"+CUST.CPNL.resultMsg)

	SUCCESS:
	sv_z = urhk_SetOrbOut("SuccessOrFailure|Y")
	
	PRINT(sv_z)
	ENDOFSCRIPT:
		
END-->