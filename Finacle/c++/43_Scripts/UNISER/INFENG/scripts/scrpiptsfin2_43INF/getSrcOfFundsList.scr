#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: getSrcOfFundsList.scr
# Type: NA|default
# Description: Source of Funds Searcher Script
# Author: Anshul_Singh04
# Date: 1/30/19 1:45 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="4d9161a98684141e5dc762af4bca4ee50fe009ea19b07fd5b59dbc119f66d4e7"
	# Script body goes here
	TRACE ON
	
	###########################################################################################
	# Check whether AML Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("AML")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("AML")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether FNDS Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("AML","FNDS")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("AML","FNDS",5)
	#}
	ENDIF
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	
	AML.FNDS.srcId = ""
	AML.FNDS.srcOfFnds = " "
	
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
	
	sv_s = "recordCount|SELECT count(1) FROM CUSTOM.C_SFT"
	sv_s = sv_s + " WHERE bank_id = ?SVAR"
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	IF(sv_d == 0)THEN
	#{
		AML.FNDS.recordCount = BANCS.OUTPARAM.recordCount
		PRINT(AML.FNDS.recordCount)
	#}
	ENDIF
	
	sv_u = urhk_SetOrbOut("recordCount|"+AML.FNDS.recordCount)
	PRINT(sv_u)
	
	sv_s = "srcId,srcOfFnds|"
	sv_s = sv_s + " SELECT SOURCE_ID, SOURCE_DESC FROM CUSTOM.C_SFT"
	sv_s = sv_s + " WHERE bank_id = ?SVAR ORDER BY SOURCE_ID"
	
	PRINT(sv_s)
	
	sv_c = urhk_dbCursorOpenWithBind(sv_s)
	PRINT(sv_c)
	
	IF(sv_c != 0)THEN
	#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_n = BANCS.OUTPARAM.DB_CURSOR_NUMBER
	PRINT(sv_n)
	
	sv_f = urhk_dbCursorFetch(sv_n)
	PRINT(sv_f)
	
	sv_i = 1
	
	WHILE(sv_f == 0)
	#{
		AML.FNDS.srcId = BANCS.OUTPARAM.srcId
		AML.FNDS.srcOfFnds = BANCS.OUTPARAM.srcOfFnds
		
		PRINT(AML.FNDS.srcId)
		PRINT(AML.FNDS.srcOfFnds)
		
		########################################################################
		# Code To Replace Special Character ' before setting value in Searcher #
		########################################################################
		
		lv_a = AML.FNDS.srcOfFnds
		
		lv_l = STRLEN(lv_a)
		lv_n = 0
		lv_s = ""
		WHILE(lv_n < lv_l)
		#{
			PRINT(lv_a)
			PRINT(lv_n)
			lv_c = ""
			lv_c = MID$(lv_a,lv_n,1)
			PRINT(lv_c)
			
			IF(lv_c == "'")THEN
			#{
				PRINT("Found One")
				lv_s = lv_s + ""
				PRINT(lv_s)
				GOTO NEXT_CHAR1
			#}
			ENDIF
			lv_s = lv_s + lv_c
			PRINT(lv_s)
			
			NEXT_CHAR1:
				
				lv_n = lv_n + 1
			#}
			DO
			
			PRINT(lv_a)
			PRINT(lv_s)
			
			AML.FNDS.srcOfFnds=lv_s
			
			#####################################################################
			
			sv_s = "srcId_" + Format$(sv_i,"%d") + "|" + AML.FNDS.srcId
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			
			sv_s = "srcOfFnds_" + Format$(sv_i,"%d") + "|" + AML.FNDS.srcOfFnds
			PRINT(sv_s)
			sv_u = urhk_SetOrbOut(sv_s)
			PRINT(sv_u)
			
			sv_i = sv_i + 1
			PRINT(sv_i)
			
			PRINT(sv_n)
			sv_f = urhk_dbCursorFetch(sv_n)
			PRINT(sv_f)
		#}
		DO
		
		sv_c = urhk_dbCursorClose(sv_n)
		PRINT(sv_c)
		
	ENDOFSCRIPT:
		DELETECLASS("AML", "FNDS")
		DELETEREP("AML")
		
		TRACE OFF
		EXITSCRIPT
END-->

