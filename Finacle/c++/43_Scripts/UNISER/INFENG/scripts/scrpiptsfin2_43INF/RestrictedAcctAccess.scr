#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: script name based
# Script: RestrictedAcctAccess.sscr
# Type: RestrictedAcctAccess|NA
# Description: Not Available
#============================================================

#-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=
#        Script to log the access details to tmp file
#-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=
#-=-=-=-=-=-=-=-=-=-=-=   SCRIPT STARTS   -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-
<--START
TRACE ON
print(BANCS.STDIN.mySolId)
	#-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=

               BANCS.INPUT.USERSET_ID="NUL"
               BANCS.INPUT.ACCSET_ID="NUL"
	
	IF(FIELDEXISTS(BANCS.INPUT.uAccount))THEN
	print(BANCS.INPUT.uAccount)
	else
	BANCS.INPUT.uAccount=""
	ENDIF

	


	IF((BANCS.STDIN.contextBankId == "43") AND (BANCS.INPUT.uAccount !=""))THEN
	#{
	
	
	sv_s =""
	sv_s = sv_s + " SET_ID | select SET_ID from tbaadm.sst where Sol_id='"+BANCS.STDIN.mySolId+"' and bank_id='"+BANCS.STDIN.contextBankId+"' and set_id ='BCDC'  and rownum=1"
	print(sv_s)
	sv_u = urhk_dbselectwithBind(sv_s)
	print(sv_u)
	IF (sv_u == 0) THEN
	#{
			BANCS.INPUT.USERSET_ID=BANCS.OUTPARAM.SET_ID
			print(BANCS.INPUT.USERSET_ID)
		else

			BANCS.INPUT.USERSET_ID="ECB"

	#}
	ENDIF


   sv_u = urhk_getAcctDetailsInRepository(BANCS.INPUT.uAccount)
 PRINT(sv_u)


 IF(sv_u == 0)THEN
 #{
         BANCS.INPUT.GSolId = BANCS.OUTPARAM.acctSolId
         print(BANCS.INPUT.GSolId)

ELSE

	BANCS.INPUT.GSolId ="XXX"

 #}
 ENDIF

	sv_s =""
	sv_s = sv_s + " SET_ID | select SET_ID from tbaadm.sst where Sol_id='"+BANCS.INPUT.GSolId+"' and bank_id='"+BANCS.STDIN.contextBankId+"' and set_id ='BCDC'  and rownum=1"
	print(sv_s)
	sv_u = urhk_dbselectwithBind(sv_s)
	print(sv_u)
	IF (sv_u == 0) THEN
	#{
			BANCS.INPUT.ACCSET_ID=BANCS.OUTPARAM.SET_ID
			print(BANCS.INPUT.USERSET_ID)
		else

			BANCS.INPUT.ACCSET_ID="ECB"

	#}
	ENDIF




	
	#}
       ENDIF
		#End BCDC users

	IF((BANCS.STDIN.roleId =="SYSTEM") OR (BANCS.STDIN.userWorkClass > 509 )) THEN
		PRINT("EXEMPT")
		BANCS.INPUT.USERSET_ID="EXEMPT"
		BANCS.INPUT.ACCSET_ID="EXEMPT"
	ENDIF

print(BANCS.INPUT.USERSET_ID)
print(BANCS.INPUT.ACCSET_ID)
#--------------------------------
# 	IF((BANCS.INPUT.ACCSET_ID !="") and (BANCS.INPUT.USERSET_ID !="" )) THEN
#
#		if(BANCS.INPUT.ACCSET_ID !=BANCS.INPUT.USERSET_ID) THEN
###		sv_u = urhk_TBAF_DispMesg("Account Not allowed")
#		BANCS.OUTPUT.scriptFailed="Y"
#                BANCS.OUTPUT.successOrFailure="F"
#		sv_u = urhk_SRV_SetErr("ACCOUNT Not Reachable")
#		PRINT("Account Not allowed")
#                BANCS.OUTPUT.successOrFailure="F"
#		ENDIF
#
#	ENDIF
		exitscript
		#-=-=-=-=-=-=-=-=-=-=-=   SCRIPT ENDS   -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-
END-->
#-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=
