#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: commntVerify.scr
# Type: NA|default
# Description: Commision Maintenance Script
# Author: Anshul_Singh04
# Date: 6/1/18 11:07 AM
# Modification Log:
#============================================================

<--START
CHECKSUM="8f0980513919a78738ed681dd5c7c1b3644e60a6b2468fa627803d21c7b5ce8b"
	# Script body goes here
	TRACE ON
	
	sv_a = REPEXISTS("EQT")
	IF (sv_a == 0) THEN
	#{
		CREATEREP("EQT")
	#}
	ENDIF
	sv_a = CLASSEXISTS("EQT", "COMM")
	IF (sv_a == 0 ) THEN
	#{
		CREATECLASS("EQT", "COMM", 5)
	#}
	ENDIF
	
	PRINT(BANCS.INPUT.commCode)
	EQT.COMM.commCode =BANCS.INPUT.commCode
	EQT.COMM.msg=" "
	EQT.COMM.recInMain = "Y"
	
	sv_j = "commDesc,commCrncy,commVat,commAmt,bacid,rcreUser,rcreTime|SELECT  COMM_DESC, CRNCY_CODE, VAT_RATE, COMM_AMT, BACID, RCRE_USER_ID, TO_CHAR(RCRE_TIME,'MM-DD-YYYY HH:MI:SS')  FROM CUSTOM.C_CPM_MOD "
	sv_j = sv_j + " WHERE COMM_CODE =?SVAR AND BANK_ID=?SVAR  "
	PRINT(sv_j)
	
	BANCS.INPARAM.BINDVARS =  EQT.COMM.commCode + "|" +  BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_e = urhk_dbSelectWithBind(sv_j)
	PRINT(sv_e)
	
	IF(sv_e!=0)THEN
	#{
		EQT.COMM.msg = "Error in Database Operation!!"
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		sv_u = urhk_SetOrbOut("Error_1|ERR^"+EQT.COMM.msg)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	EQT.COMM.commDesc = BANCS.OUTPARAM.commDesc
	EQT.COMM.commCrncy = BANCS.OUTPARAM.commCrncy
	EQT.COMM.commVat = BANCS.OUTPARAM.commVat
	EQT.COMM.commAmt = BANCS.OUTPARAM.commAmt
	EQT.COMM.bacid = BANCS.OUTPARAM.bacid
	EQT.COMM.rcreUser = BANCS.OUTPARAM.rcreUser
	EQT.COMM.rcreTime = BANCS.OUTPARAM.rcreTime
	
	PRINT(EQT.COMM.rcreTime)
	##############################################################################
	# Counting Rows from MAIN table
	##############################################################################
	
	sv_d = "mainCount|SELECT count(1) FROM CUSTOM.C_CPM WHERE bank_id=?SVAR AND COMM_CODE=?SVAR"
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + EQT.COMM.commCode
	
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_r = urhk_dbSelectWithBind(sv_d)
	PRINT(sv_r)
	
	IF(sv_r!=0)THEN
	#{
		EQT.COMM.msg = "Error in Fetching Records from DB!!!"
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	PRINT(BANCS.OUTPARAM.mainCount)
	##############################################################################
	
	IF(BANCS.OUTPARAM.mainCount==0)THEN
	#{
		EQT.COMM.recInMain = "N"
		GOTO INSERT_IN_MAIN
	#}
	ENDIF
	
	sv_w = "UPDATE CUSTOM.C_CPM set COMM_DESC= ?SVAR, CRNCY_CODE= ?SVAR, VAT_RATE= ?SVAR, COMM_AMT= ?SVAR, BACID = ?SVAR, LCHG_USER_ID= ?SVAR, LCHG_TIME= sysdate"
	sv_w = sv_w + " WHERE COMM_CODE = ?SVAR AND BANK_ID = ?SVAR"
	
	PRINT(sv_w)
	
	BANCS.INPARAM.BINDVARS = EQT.COMM.commDesc + "|" + EQT.COMM.commCrncy + "|" + EQT.COMM.commVat + "|" + EQT.COMM.commAmt + "|" + EQT.COMM.bacid + "|" +  BANCS.STDIN.userId  +  "|" +  EQT.COMM.commCode + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_e = urhk_dbsqlWithBind(sv_w)
	PRINT(sv_e)
	
	IF(sv_e!=0)THEN
	#{
		EQT.COMM.msg = "Error in Database Operation!!"
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		sv_u = urhk_SetOrbOut("Error_1|ERR^"+EQT.COMM.msg)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	INSERT_IN_MAIN:
		
		IF(EQT.COMM.recInMain=="N")THEN
		#{
			sv_w = "INSERT INTO CUSTOM.C_CPM  (COMM_CODE, COMM_DESC, CRNCY_CODE, VAT_RATE, COMM_AMT, BACID, DEL_FLG, RCRE_USER_ID, RCRE_TIME, LCHG_USER_ID, LCHG_TIME, BANK_ID) "
			sv_w = sv_w + " VALUES (?SVAR, ?SVAR, ?SVAR, ?SVAR, ?SVAR, ?SVAR, 'N', ?SVAR, to_date(?SVAR,'MM-DD-YYYY HH:MI:SS') , ?SVAR, sysdate, ?SVAR)"
			
			PRINT(sv_w)
			
			BANCS.INPARAM.BINDVARS = EQT.COMM.commCode + "|" + EQT.COMM.commDesc + "|" + EQT.COMM.commCrncy + "|" + EQT.COMM.commVat + "|" + EQT.COMM.commAmt + "|" + EQT.COMM.bacid + "|" + EQT.COMM.rcreUser + "|" + EQT.COMM.rcreTime + "|" + BANCS.STDIN.userId  +  "|" +   BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			
			sv_e = urhk_dbsqlWithBind(sv_w)
			PRINT(sv_e)
			
			IF(sv_e!=0)THEN
			#{
				EQT.COMM.msg = "Error in Database Operation!!"
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				sv_u = urhk_SetOrbOut("Error_1|ERR^"+EQT.COMM.msg)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ENDIF
		
		IF(sv_e==0) THEN
		#{
			EQT.COMM.msg = "Record has been Verified Successfully."
		#}
		ENDIF
		
		sv_j= "DELETE FROM CUSTOM.C_CPM_MOD WHERE bank_id=?SVAR AND COMM_CODE =?SVAR "
		BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + EQT.COMM.commCode
		
		PRINT(BANCS.INPARAM.BINDVARS )
		sv_r = urhk_dbsqlWithBind(sv_j)
		PRINT(sv_r)
		
	ENDOFSCRIPT:
		
		EXITSCRIPT
		
END-->
