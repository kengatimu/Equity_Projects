#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: addAdvCode_hacm.scr
# Type: NA|default
# Description: This script is for HACM FCFTT record
# Author: Meena_Andhavarapu
# Date: 10/11/18 3:54 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="f226d4dcec584534738db5c523503e7ceaf59500ad6b62866b9d203c76c3064e"
	# Script body goes here
	
	TRACE ON
	#============================================================
	# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
	# Product: Finacle Core
	# Category: User Defined
	# Script: addAdvCode_hacm.scr
	# Type: NA|default
	# Description: This script is for HACM FCFTT record
	# Author: lakshmi.rajanala
	# Date: 6/14/18 12:48 PM
	# Modification Log:
	#============================================================
	
	
	#======================================================
	#Creating Repository and Class
	#======================================================
	sv_a = REPEXISTS("CUST")
	IF(sv_a == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	sv_b = CLASSEXISTS("CUST", "HACMCLS")
	IF(sv_b == 0) THEN
	#{
		CREATECLASS("CUST", "HACMCLS", 5)
	#}
	ENDIF
	
	sv_u = urhk_B2k_PrintRepos("BANCS")
	PRINT(sv_u)
	
	#=============================================
	#initializing variables
	#============================================
	CUST.HACMCLS.funcCode=""
	CUST.HACMCLS.foracid=""
	CUST.HACMCLS.advCode=""
	CUST.HACMCLS.creditlineCA = ""
	CUST.HACMCLS.entityFlag=""
	CUST.HACMCLS.errorMsg = " "
	CUST.HACMCLS.errorFlg = "N"
	CUST.HACMCLS.recCount = "0"
	CUST.HACMCLS.acid = " "
	CUST.HACMCLS.crlinepcnt = ""
	CUST.HACMCLS.vatFlg = BANCS.INPUT.vatFlg
	
	#=============================================
	#Fetching values from front end
	#=============================================
	
	sv_r = urhk_B2k_PrintRepos("ONS")
	
	sv_g = GETPOSITION(ONS.CUSTOM.custDataIn,"|")
	sv_l = STRLEN(ONS.CUSTOM.custDataIn)
	sv_k = sv_l - sv_g - 1
	sv_m = TRIM(MID$(ONS.CUSTOM.custDataIn,sv_g,sv_k))
	PRINT(sv_m)
	CUST.HACMCLS.advCode = sv_m
	
	IF(FIELDEXISTS(BANCS.INPUT.funcCode))THEN
	#{
		IF(BANCS.INPUT.funcCode != "") THEN
		#{
			CUST.HACMCLS.funcCode = TOUPPER(TRIM(BANCS.INPUT.funcCode))
			PRINT(CUST.HACMCLS.funcCode)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.advCode))THEN
	#{
		IF (BANCS.INPUT.advCode != "")THEN
		#{
			CUST.HACMCLS.advCode = BANCS.INPUT.advCode
			PRINT(CUST.HACMCLS.advCode)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.Acid))THEN
	#{
		IF(BANCS.INPUT.Acid != "") THEN
		#{
			CUST.HACMCLS.foracid = TOUPPER(TRIM(BANCS.INPUT.Acid))
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.smsChrg))THEN
	#{
		IF(BANCS.INPUT.smsChrg != "") THEN
		#{
			CUST.HACMCLS.smsChrg = TOUPPER(TRIM(BANCS.INPUT.smsChrg))
		#}
		ENDIF
	#}
	ENDIF
	
	
	IF(FIELDEXISTS(BANCS.INPUT.creditlineCA))THEN
	#{
		IF(BANCS.INPUT.creditlineCA != "") THEN
		#{
			CUST.HACMCLS.creditlineCA = TRIM(BANCS.INPUT.creditlineCA)
			PRINT(CUST.HACMCLS.creditlineCA)
		#}
		ENDIF
	#}
	ENDIF
	
	IF(FIELDEXISTS(BANCS.INPUT.crlinepcnt))THEN
	#{
		IF(BANCS.INPUT.crlinepcnt != "") THEN
		#{
			CUST.HACMCLS.crlinepcnt = TRIM(BANCS.INPUT.crlinepcnt)
			PRINT(CUST.HACMCLS.crlinepcnt)
		#}
		ENDIF
	#}
	ENDIF
	#==============================================
	# For Add
	#==============================================
	#IF((BANCS.STDIN.menuId == "HOAACSB") OR (BANCS.STDIN.menuId == "HOAACCA") OR (BANCS.STDIN.menuId == "HOAACOD") OR (BANCS.STDIN.menuId == "HOAACMSB") OR (BANCS.STDIN.menuId == "HOAACMCA") OR (BANCS.STDIN.menuId == "HOAACMOD"))THEN
	IF(CUST.HACMCLS.funcCode == "M") THEN
	#{
		
		sv_s = "recCount|SELECT COUNT(1) FROM TBAADM.FCFTT WHERE "
		sv_s = sv_s + " ACID = (SELECT ACID FROM TBAADM.GAM WHERE FORACID=?SVAR AND BANK_ID=?SVAR) AND "
		sv_s = sv_s + " BANK_ID = ?SVAR "
		BANCS.INPARAM.BINDVARS = CUST.HACMCLS.foracid + "|"+ BANCS.STDIN.contextBankId+ "|"+ BANCS.STDIN.contextBankId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d == 0)THEN
		#{
			CUST.HACMCLS.recCount = BANCS.OUTPARAM.recCount
			PRINT(CUST.HACMCLS.recCount)
		#}
		ENDIF
		
		IF(CUST.HACMCLS.recCount == "0") THEN
		#{
			sv_s= "acid|SELECT ACID FROM TBAADM.GAM WHERE FORACID=?SVAR AND BANK_ID=?SVAR"
			BANCS.INPARAM.BINDVARS = CUST.HACMCLS.foracid + "|"+ BANCS.STDIN.contextBankId
			sv_q = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_q)
			IF(sv_q == 0)THEN
			#{
				CUST.HACMCLS.acid = BANCS.OUTPARAM.acid
				PRINT(CUST.HACMCLS.acid)
			#}
			ENDIF
			sv_s = "INSERT INTO TBAADM.FCFTT(ACID,BANK_ID,FREE_CODE_1,FREE_CODE_2,FREE_CODE_3,"
			sv_s = sv_s + "FREE_CODE_4,FREE_CODE_5,"
			sv_s = sv_s + "DEL_FLG,ENTITY_CRE_FLG,LCHG_USER_ID,"
			sv_s = sv_s + "LCHG_TIME,RCRE_USER_ID,RCRE_TIME,TS_CNT)"
			sv_s = sv_s + "VALUES(?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,"
			sv_s = sv_s + "?SVAR,?SVAR,"
			sv_s = sv_s +  "'N','N',?SVAR,sysdate,?SVAR,sysdate,'0')"
			
			BANCS.INPARAM.BINDVARS=CUST.HACMCLS.acid + "|" + BANCS.STDIN.contextBankId + "|" + CUST.HACMCLS.advCode + "|" + CUST.HACMCLS.vatFlg
			BANCS.INPARAM.BINDVARS= BANCS.INPARAM.BINDVARS + "|" + CUST.HACMCLS.smsChrg
			BANCS.INPARAM.BINDVARS= BANCS.INPARAM.BINDVARS + "|" + CUST.HACMCLS.creditlineCA +"|"+ CUST.HACMCLS.crlinepcnt
			BANCS.INPARAM.BINDVARS= BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.userId + "|" + BANCS.STDIN.userId
			
			sv_q = urhk_dbSQLWithBind(sv_s)
			PRINT(sv_q)
			
			IF(sv_q != 0)THEN
			#{
				#sv_e = urhk_SetOrbOut("SuccessOrFailure|N")
				#PRINT(sv_e)
				sv_e =urhk_SetOrbErr("Error_1|Error^Database INSERT Failed^moduleName")
				PRINT(sv_e)
				CUST.HACMCLS.errorMsg = "Database Update Failed"
				CUST.HACMCLS.errorFlg = "Y"
				GOTO ENDOFSCRIPT
				
			#}
			ENDIF
			#}
		ELSE
			#{
			sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.FCFTT_MOD WHERE "
			sv_s = sv_s + " ACID = (SELECT ACID FROM TBAADM.GAM WHERE FORACID=?SVAR AND BANK_ID=?SVAR) AND "
			sv_s = sv_s + " BANK_ID = ?SVAR "
			BANCS.INPARAM.BINDVARS = CUST.HACMCLS.foracid + "|"+ BANCS.STDIN.contextBankId+ "|"+ BANCS.STDIN.contextBankId
			
			sv_d = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_d)
			
			IF(sv_d == 0)THEN
			#{
				CUST.HACMCLS.recCount = BANCS.OUTPARAM.recCount
				PRINT(CUST.HACMCLS.recCount)
			#}
			ENDIF
			
			IF(CUST.HACMCLS.recCount == "0") THEN
			#{
				sv_s= "acid|SELECT ACID FROM TBAADM.GAM WHERE FORACID=?SVAR AND BANK_ID=?SVAR"
				BANCS.INPARAM.BINDVARS = CUST.HACMCLS.foracid + "|"+ BANCS.STDIN.contextBankId
				sv_q = urhk_dbSelectWithBind(sv_s)
				PRINT(sv_q)
				IF(sv_q == 0)THEN
				#{
					CUST.HACMCLS.acid = BANCS.OUTPARAM.acid
					PRINT(CUST.HACMCLS.acid)
				#}
				ENDIF
				
				sv_s = "INSERT INTO CUSTOM.FCFTT_MOD(ACID,BANK_ID,FREE_CODE_1,FREE_CODE_2,FREE_CODE_3,"
				sv_s = sv_s + "FREE_CODE_4,FREE_CODE_5,"
				sv_s = sv_s + "DEL_FLG,ENTITY_CRE_FLG,LCHG_USER_ID,"
				sv_s = sv_s + "LCHG_TIME,RCRE_USER_ID,RCRE_TIME,TS_CNT)"
				sv_s = sv_s + "VALUES(?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,"
				sv_s = sv_s + "?SVAR,?SVAR,"
				sv_s = sv_s +  "'N','N',?SVAR,sysdate,?SVAR,sysdate,'0')"
				
				BANCS.INPARAM.BINDVARS=CUST.HACMCLS.acid + "|" + BANCS.STDIN.contextBankId + "|" + CUST.HACMCLS.advCode + "|" + CUST.HACMCLS.vatFlg
				BANCS.INPARAM.BINDVARS= BANCS.INPARAM.BINDVARS + "|" + CUST.HACMCLS.smsChrg
				BANCS.INPARAM.BINDVARS= BANCS.INPARAM.BINDVARS + "|" + CUST.HACMCLS.creditlineCA +"|"+ CUST.HACMCLS.crlinepcnt
				BANCS.INPARAM.BINDVARS= BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.userId + "|" + BANCS.STDIN.userId
				
				sv_q = urhk_dbSQLWithBind(sv_s)
				PRINT(sv_q)
				IF(sv_q != 0)THEN
				#{
					#sv_e = urhk_SetOrbOut("SuccessOrFailure|N")
					#PRINT(sv_e)
					sv_e =urhk_SetOrbErr("Error_1|Error^Database INSERT Failed^moduleName")
					PRINT(sv_e)
					CUST.HACMCLS.errorMsg = "Database Update Failed"
					CUST.HACMCLS.errorFlg = "Y"
					GOTO ENDOFSCRIPT
					
				#}
				ENDIF
				#}
			ELSE
				#{
				sv_s = "UPDATE CUSTOM.FCFTT_MOD SET FREE_CODE_1 = ?SVAR , FREE_CODE_2 = ?SVAR , FREE_CODE_3 = ?SVAR ,"
				sv_s = sv_s + " FREE_CODE_4 = ?SVAR , FREE_CODE_5=?SVAR,"
				sv_s = sv_s + "ENTITY_CRE_FLG = 'N' , LCHG_USER_ID = ?SVAR , LCHG_TIME=sysdate WHERE "
				sv_s = sv_s + " ACID = (SELECT ACID FROM TBAADM.GAM WHERE FORACID=?SVAR AND BANK_ID=?SVAR) AND "
				sv_s = sv_s + " BANK_ID = ?SVAR "
				BANCS.INPARAM.BINDVARS = CUST.HACMCLS.advCode+"|" + CUST.HACMCLS.vatFlg + "|" + CUST.HACMCLS.smsChrg
				BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|"  + CUST.HACMCLS.creditlineCA +"|"+ CUST.HACMCLS.crlinepcnt
				BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.userId + "|" + CUST.HACMCLS.foracid + "|"+ BANCS.STDIN.contextBankId+ "|"+ BANCS.STDIN.contextBankId
				
				sv_q = urhk_dbSQLWithBind(sv_s)
				PRINT(sv_q)
				IF(sv_q != 0)THEN
				#{
					#sv_e = urhk_SetOrbOut("SuccessOrFailure|N")
					#PRINT(sv_e)
					sv_e =urhk_SetOrbErr("Error_1|Error^Database Update Failed^moduleName")
					PRINT(sv_e)
					CUST.HACMCLS.errorMsg = "Database Update Failed"
					CUST.HACMCLS.errorFlg = "Y"
					GOTO ENDOFSCRIPT
					
				#}
				ENDIF
			#}
			ENDIF
		#}
		ENDIF
	#}
	ENDIF
	#=========================================
	#For Verify
	#=========================================
	#IF((BANCS.STDIN.menuId == "HOAACVSB") OR (BANCS.STDIN.menuId == "HOAACVCA") OR (BANCS.STDIN.menuId == "HOAACVOD"))THEN
	IF(CUST.HACMCLS.funcCode == "V") THEN
	#{
		sv_s= "acid|SELECT ACID FROM TBAADM.GAM WHERE FORACID=?SVAR AND BANK_ID=?SVAR"
		BANCS.INPARAM.BINDVARS = CUST.HACMCLS.foracid + "|"+ BANCS.STDIN.contextBankId
		sv_q = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_q)
		IF(sv_q == 0)THEN
		#{
			CUST.HACMCLS.acid = BANCS.OUTPARAM.acid
			PRINT(CUST.HACMCLS.acid)
		#}
		ENDIF
		sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.FCFTT_MOD WHERE "
		sv_s = sv_s + " ACID = (SELECT ACID FROM TBAADM.GAM WHERE FORACID=?SVAR AND BANK_ID=?SVAR) AND "
		sv_s = sv_s + " BANK_ID = ?SVAR "
		BANCS.INPARAM.BINDVARS = CUST.HACMCLS.foracid + "|"+ BANCS.STDIN.contextBankId+ "|"+ BANCS.STDIN.contextBankId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d == 0)THEN
		#{
			CUST.HACMCLS.recCount = BANCS.OUTPARAM.recCount
			PRINT(CUST.HACMCLS.recCount)
		#}
		ENDIF
		
		IF(CUST.HACMCLS.recCount == "0") THEN
		#{
			
			sv_s = "UPDATE TBAADM.FCFTT set ENTITY_CRE_FLG = 'Y' , LCHG_USER_ID = ?SVAR , LCHG_TIME=sysdate WHERE "
			sv_s = sv_s + " ACID = ?SVAR AND "
			sv_s = sv_s + " BANK_ID = ?SVAR "
			BANCS.INPARAM.BINDVARS = BANCS.STDIN.userId + "|" + CUST.HACMCLS.acid + "|" + BANCS.STDIN.contextBankId
			
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_q = urhk_dbSQLWithBind(sv_s)
			PRINT(sv_q)
			IF(sv_q != 0) THEN
			#{
				#sv_e=urhk_SetOrbOut("SuccessOrFailure|N")
				#sv_e=urhk_SetOrbOut("Data updation failed")
				#PRINT(sv_e)
                                        CUST.HACMCLS.errorMsg = "Database Update Failed"
                                        CUST.HACMCLS.errorFlg = "Y"

				GOTO ENDOFSCRIPT
			#}
			ENDIF
			#}
		ELSE
			#{
			sv_s = "UPDATE TBAADM.FCFTT set FREE_CODE_1 = ?SVAR , FREE_CODE_2 = ?SVAR, FREE_CODE_3 = ?SVAR, "
			sv_s = sv_s + "FREE_CODE_4 = ?SVAR, FREE_CODE_5=?SVAR,"
			sv_s = sv_s + "ENTITY_CRE_FLG = 'Y' , LCHG_USER_ID = ?SVAR , LCHG_TIME=sysdate WHERE "
			sv_s = sv_s + " ACID = ?SVAR AND "
			sv_s = sv_s + " BANK_ID = ?SVAR "
			
			BANCS.INPARAM.BINDVARS = CUST.HACMCLS.advCode + "|" + CUST.HACMCLS.vatFlg + "|" + CUST.HACMCLS.smsChrg
			BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + CUST.HACMCLS.creditlineCA +"|"+ CUST.HACMCLS.crlinepcnt
			BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + "|" + BANCS.STDIN.userId + "|" + CUST.HACMCLS.acid + "|" + BANCS.STDIN.contextBankId
			
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_q = urhk_dbSQLWithBind(sv_s)
			PRINT(sv_q)
			IF(sv_q != 0) THEN
			#{
				#sv_e=urhk_SetOrbOut("SuccessOrFailure|N")
				#sv_e=urhk_SetOrbOut("Data updation failed")
				#PRINT(sv_e)
                                        CUST.HACMCLS.errorMsg = "Database Update Failed"
                                        CUST.HACMCLS.errorFlg = "Y"

				GOTO ENDOFSCRIPT
			#}
			ENDIF
			
			sv_s = "DELETE FROM CUSTOM.FCFTT_MOD WHERE "
			sv_s = sv_s + " ACID = ?SVAR AND "
			sv_s = sv_s + " BANK_ID = ?SVAR "
			BANCS.INPARAM.BINDVARS = CUST.HACMCLS.acid + "|" + BANCS.STDIN.contextBankId
			
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_q = urhk_dbSQLWithBind(sv_s)
			PRINT(sv_q)
			IF(sv_q != 0) THEN
			#{
				#sv_e=urhk_SetOrbOut("SuccessOrFailure|N")
				#sv_e=urhk_SetOrbOut("Data updation failed")
				#PRINT(sv_e)
                                        CUST.HACMCLS.errorMsg = "Database Update Failed"
                                        CUST.HACMCLS.errorFlg = "Y"

				GOTO ENDOFSCRIPT
			#}
			ENDIF
			
			#IF(sv_q == 0) THEN
			#{
			#	sv_r = urhk_SetOrbOut("SuccessOrFailure|Y")
			#}
			#ENDIF
		#}
		ENDIF
	#}
	ENDIF
	
	#=========================================
	#For Cancel
	#=========================================
	#IF((BANCS.STDIN.menuId == "HOAACVSB") OR (BANCS.STDIN.menuId == "HOAACVCA") OR (BANCS.STDIN.menuId == "HOAACVOD"))THEN
	IF(CUST.HACMCLS.funcCode == "X") THEN
	#{
		sv_s= "acid|SELECT ACID FROM TBAADM.GAM WHERE FORACID=?SVAR AND BANK_ID=?SVAR"
		BANCS.INPARAM.BINDVARS = CUST.HACMCLS.foracid + "|"+ BANCS.STDIN.contextBankId
		sv_q = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_q)
		IF(sv_q == 0)THEN
		#{
			CUST.HACMCLS.acid = BANCS.OUTPARAM.acid
			PRINT(CUST.HACMCLS.acid)
		#}
		ENDIF
		sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.FCFTT_MOD WHERE "
		sv_s = sv_s + " ACID = (SELECT ACID FROM TBAADM.GAM WHERE FORACID=?SVAR AND BANK_ID=?SVAR) AND "
		sv_s = sv_s + " BANK_ID = ?SVAR "
		BANCS.INPARAM.BINDVARS = CUST.HACMCLS.foracid + "|"+ BANCS.STDIN.contextBankId+ "|"+ BANCS.STDIN.contextBankId
		
		sv_d = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_d)
		
		IF(sv_d == 0)THEN
		#{
			CUST.HACMCLS.recCount = BANCS.OUTPARAM.recCount
			PRINT(CUST.HACMCLS.recCount)
		#}
		ENDIF
		
		IF(CUST.HACMCLS.recCount != "0") THEN
		#{
			sv_s = "DELETE FROM CUSTOM.FCFTT_MOD WHERE "
			sv_s = sv_s + " ACID = ?SVAR AND "
			sv_s = sv_s + " BANK_ID = ?SVAR "
			
			#}
		ELSE
			#{
			sv_s = "DELETE FROM TBAADM.FCFTT WHERE "
			sv_s = sv_s + " ACID = ?SVAR AND "
			sv_s = sv_s + " BANK_ID = ?SVAR AND ENTITY_CRE_FLG='N'"
		#}
		ENDIF
		BANCS.INPARAM.BINDVARS = CUST.HACMCLS.acid + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_q = urhk_dbSQLWithBind(sv_s)
		PRINT(sv_q)
		IF(sv_q != 0) THEN
		#{
			sv_e=urhk_SetOrbOut("SuccessOrFailure|N")
			sv_e=urhk_SetOrbOut("Data updation failed")
			PRINT(sv_e)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
	#}
	ENDIF
	
	ENDOFSCRIPT:
		sv_u = urhk_SetOrbOut("errFlg|"+ CUST.HACMCLS.errorFlg)
		sv_u = urhk_SetOrbOut("errMsg|"+ CUST.HACMCLS.errorMsg)
		
		
END-->
