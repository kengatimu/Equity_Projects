<--start
trace on
sv_a = urhk_B2k_printRepos("BANCS")
print(sv_a)

	IF (REPEXISTS("CUST") == 0 ) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	IF (CLASSEXISTS("CUST","DATA") == 0) THEN
	#{
		CREATECLASS("CUST","DATA",5)
	#}
	ENDIF

	sv_u = urhk_SRV_GetVal("free_text")
	CUST.DATA.free_text = trim(BANCS.OUTPARAM.srvValue)
	print(CUST.DATA.free_text)

		if ((CUST.DATA.free_text != "DRCAGT") AND  (CUST.DATA.free_text != "DRCMER") AND (CUST.DATA.free_text != "DRCMOB"))  then
			sv_s = urhk_SRV_SetErr("Remarks Field to be DRCAGT for Agency Or DRCMER for Merchant OR DRCMOB For Mobile")
			print(sv_s)
		endif
	
trace off
end-->