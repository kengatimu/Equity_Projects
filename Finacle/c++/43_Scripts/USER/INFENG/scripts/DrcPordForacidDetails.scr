#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: DrcPordForacidDetails.scr
# Type: NA|default
# Description: Load Hpordm Acct Detaiols
# Author: irungu James
# Date: 02/11/2021 10:40 AM
# Modification Log:
#============================================================

<--START
CHECKSUM="72cfea39df01ad866f594a9080210fd4b4bfdb3f818d390903b4d3f4e2a7d82d"
	TRACE ON
	#==========================================================*
	# create repository
	#==========================================================*
	sv_a = REPEXISTS("CUST")
	IF(sv_a == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	sv_r = CLASSEXISTS("CUST","IBAN")
	IF (sv_r == 0) THEN
	#{
		CREATECLASS("CUST", "IBAN", 5)
	#}
	ENDIF
	
	###########################################################################################
	# Initialize the variables
	###########################################################################################
	CUST.IBAN.errorFlg ="N"
	CUST.IBAN.errorMsg =" "
	CUST.IBAN.ibanNo =""
	CUST.IBAN.acctNum =""
	CUST.IBAN.solId =""
	CUST.IBAN.crncyCode=""
	CUST.IBAN.acctName =""
	CUST.IBAN.entity = ""
	
	IF(FIELDEXISTS(BANCS.INPUT.ibanNo)) THEN
	#{
		IF(TRIM(BANCS.INPUT.ibanNo) != "")THEN
		#{
			CUST.IBAN.ibanNo= TOUPPER(TRIM(BANCS.INPUT.ibanNo))
			PRINT(CUST.IBAN.ibanNo)
		#}
		ENDIF
		#}
	ELSE
		#{
		CUST.IBAN.errorFlg = "Y"
		CUST.IBAN.errorMsg = "IBAN number field does not exists"
		GOTO ENDOFSCRIPT
	#}
	ENDIF


	
	CUST.IBAN.acctNum = CUST.IBAN.ibanNo
	PRINT(CUST.IBAN.acctNum)
	
	sv_s = "acctNumSolId,acctNumCcy,acctName,crncy,entity|"
	sv_s = sv_s + " SELECT SOL_ID,ACCT_CRNCY_CODE,ACCT_NAME,ACCT_CRNCY_CODE,BANK_ID"
	sv_s = sv_s + " FROM TBAADM.GAM "
	sv_s = sv_s + " WHERE FORACID = ?SVAR"
	#sv_s = sv_s + " AND DEL_FLG != 'Y'"
	sv_s = sv_s + " AND ENTITY_CRE_FLG !='N'"
	PRINT(sv_s)
	
	BANCS.INPARAM.BINDVARS = CUST.IBAN.acctNum
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d == 0) THEN
	#{
		CUST.IBAN.solId = BANCS.OUTPARAM.acctNumSolId
		PRINT(CUST.IBAN.solId)
		CUST.IBAN.crncyCode = BANCS.OUTPARAM.acctNumCcy
		PRINT(CUST.IBAN.crncyCode)
		CUST.IBAN.acctName = BANCS.OUTPARAM.acctName
		PRINT(CUST.IBAN.acctName)
		CUST.IBAN.entity = BANCS.OUTPARAM.entity
		PRINT(CUST.IBAN.entity)
		#}
	ELSE
		#{
		CUST.IBAN.errorFlg = "Y"
		CUST.IBAN.errorMsg = "Invalid Account Number"
		GOTO ENDOFSCRIPT
	#}
	ENDIF

	sv_u = urhk_SetOrbOut("errorFlg|"+CUST.IBAN.errorFlg)
        PRINT(sv_u)

        sv_u = urhk_SetOrbOut("errorMsg|"+CUST.IBAN.errorMsg)
        PRINT(sv_u)
	
	sv_u = urhk_SetOrbOut("acctNum|"+CUST.IBAN.acctNum)
	PRINT(sv_u)
	
	sv_u = urhk_SetOrbOut("acctName|"+CUST.IBAN.acctName)
	PRINT(sv_u)
	
	sv_u = urhk_SetOrbOut("solId|"+CUST.IBAN.solId)
	PRINT(sv_u)
	
	sv_u = urhk_SetOrbOut("crncyCode|"+CUST.IBAN.crncyCode)
	PRINT(sv_u)
	
	sv_u = urhk_SetOrbOut("entity|"+CUST.IBAN.entity)
	PRINT(sv_u)
	
	ENDOFSCRIPT:
		sv_u = urhk_SetOrbOut("errorFlg|"+CUST.IBAN.errorFlg)
		PRINT(sv_u)
		
		sv_u = urhk_SetOrbOut("errorMsg|"+CUST.IBAN.errorMsg)
		PRINT(sv_u)
		
		DELETECLASS("CUST", "IBAN")
		DELETEREP("CUST")
		
		TRACE OFF
		
END-->
