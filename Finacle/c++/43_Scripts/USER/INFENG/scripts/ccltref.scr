############################################################################################################

# Source Name                       : ccltref.scr
# Date                              : 01-01-2024
# Description                       : This script will get call when CCLT seacher is invoked
# Input                             : None
# Output                            : None
# Called Script                     : None
# Calling Script                    :
# Author                            : Phelister
# Modification History              :
# < Version No.>    <Date>      <Author Name>       <Description>
#   1               01-01-2024  Phelister              Original Version
#
# Using tablespace 4
############################################################################################################
<--START
	TRACE ON
	
	#==========================================================*
	# CREATING NEW REPOSITORY AND CLASS
	#==========================================================*
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	IF (CLASSEXISTS("CUST", "CCLT") == 0) THEN
	#{
		CREATECLASS("CUST", "CCLT", 5)
	#}
	ENDIF
	sv_a = 1
	print(sv_a)
	
	sv_s = "recCount"
	sv_s = sv_s + " | "
	sv_s = sv_s + " SELECT COUNT(*) FROM (SELECT DISTINCT REF_CODE,RCRE_USER_ID,trunc(RCRE_TIME) "
	sv_s = sv_s + " FROM CUSTOM.CCLT WHERE status_code='V' AND bank_id='"+BANCS.STDIN.contextBankId+"' and RCRE_USER_ID!=?SVAR ) sub "
	print(sv_s)
	
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.userId
	print(sv_s)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	print(sv_d)
	
	CUST.CCLT.recCount = BANCS.OUTPARAM.recCount
	print(CUST.CCLT.recCount)
	
	sv_q = urhk_SetOrbOut("recCount|"+CUST.CCLT.recCount)
	
	sv_s = "refnumber,entryuser,entrydate"
	sv_s = sv_s + " | select DISTINCT REF_CODE,RCRE_USER_ID,trunc(RCRE_TIME) from CUSTOM.CCLT where status_code='V' AND bank_id='"+BANCS.STDIN.contextBankId+"' and RCRE_USER_ID!=?SVAR order by trunc(RCRE_TIME) desc "
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.userId
	print(sv_s)
	
	sv_c = urhk_dbCursorOpenWithBind(sv_s)
	
	IF( sv_c != 0) THEN
	#{
		sv_u = urhk_SRV_SetErr("No records fetched for the entered Account Number")
		BANCS.OUTPUT.SuccessOrFailure = "F"
		GOTO ENDOFSCRIPT
		#}
	ELSE
		#{
		sv_l = BANCS.OUTPARAM.DB_CURSOR_NUMBER
		print(sv_l)
		sv_c = urhk_dbCursorFetch(sv_l)
		print(sv_c)
		
		WHILE( sv_c == 0)
		#{
			CUST.CCLT.count = sv_a
			print(CUST.CCLT.count)
			
			CUST.CCLT.refnumber= BANCS.OUTPARAM.refnumber
			print(CUST.CCLT.refnumber)
			
			CUST.CCLT.entryuser= BANCS.OUTPARAM.entryuser
			print(CUST.CCLT.entryuser)
			
			CUST.CCLT.entrydate= BANCS.OUTPARAM.entrydate
			print(CUST.CCLT.entrydate)
			
			sv_z = "refnumber_" + Format$(sv_a,"%d") + "|" + CUST.CCLT.refnumber
			print(sv_z)
			sv_u = urhk_SetOrbOut(sv_z)
			
			sv_z = "entryuser_" + Format$(sv_a,"%d") + "|" + CUST.CCLT.entryuser
			print(sv_z)
			sv_u = urhk_SetOrbOut(sv_z)
			
			sv_z = "entrydate_" + Format$(sv_a,"%d") + "|" + CUST.CCLT.entrydate
			print(sv_z)
			sv_u = urhk_SetOrbOut(sv_z)
			
			sv_a = sv_a + 1
			sv_c = urhk_dbCursorFetch(sv_l)
			print(sv_c)
		#}
		DO
		sv_c = urhk_dbCursorClose(sv_l)
		print(sv_c)
	#}
	ENDIF
	
	ENDOFSCRIPT:
		exitscript
TRACE OFF		
END-->
