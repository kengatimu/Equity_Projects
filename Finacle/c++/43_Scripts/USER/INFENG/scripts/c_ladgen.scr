<--START
TRACE ON
	IF (REPEXISTS("CUST") == 0) THEN
#{
    CREATEREP("CUST")
#}
ENDIF

sv_r = CLASSEXISTS("CUST","DATA")
IF (sv_r == 0) THEN
#{
    CREATECLASS("CUST","DATA",5)
#}
ENDIF	 
		
		
		CUST.DATA.BODDate = mid$(BANCS.STDIN.BODDate,0,10)	
		
		sv_r = urhk_B2k_PrintRepos("BANCS")
			
		 
        BANCS.INPARAM.BINDVARS = CUST.DATA.BODDate + "|" + CUST.DATA.BODDate + "|" +  BANCS.STDIN.contextBankId + "|" + BANCS.STDIN.contextBankId

		sv_q = "loanAccount,acctSolId|select g.foracid,g.sol_id from tbaadm.gam g,tbaadm.lam a " 
		sv_q = sv_q +  "where g.acid in(select l.acid from tbaadm.lrs l where (NEXT_DMD_DATE <=to_date(?SVAR,'dd-mm-yyyy') or NEXT_INT_DMD_DATE <=to_date(?SVAR,'dd-mm-yyyy') ) and l.bank_id =?SVAR and l.DEL_FLG ='N') " 
		sv_q = sv_q +  "and g.bank_id =?SVAR  and g.acct_cls_flg ='N' "
		sv_q = sv_q +  "and a.acid =g.acid and a.bank_id =g.bank_id and a.chrge_off_flg !='Y' and g.ENTITY_CRE_FLG = 'Y' "
        PRINT(sv_q)

        sv_d = urhk_dbCursorOpenWithBind(sv_q)
        PRINT(sv_d)

        IF(sv_d != 0)THEN
        #{
                BANCS.OUTPUT.successOrFailure = "F"
                sv_e = "No records found"
				goto ENDOFSCRIPT
        #}
        ENDIF

        CUST.DATA.DB_CURSOR_NUMBER = BANCS.OUTPARAM.DB_CURSOR_NUMBER
        PRINT(CUST.DATA.DB_CURSOR_NUMBER)
        sv_d = urhk_dbCursorFetch(CUST.DATA.DB_CURSOR_NUMBER)
        CUST.DATA.count = "1"
        PRINT(sv_d)

    WHILE(sv_d == 0)
    #{
            #get the details
            PRINT(BANCS.OUTPARAM.loanAccount)
            CUST.DATA.loanAccount = BANCS.OUTPARAM.loanAccount
            PRINT(CUST.DATA.loanAccount)
			CUST.DATA.solId = BANCS.OUTPARAM.acctSolId
			print(CUST.DATA.solId)

            sv_m =CINT(CUST.DATA.count)
            PRINT(sv_m)


			sv_n = CUST.DATA.loanAccount
		
			 
		
		sv_h = CUST.DATA.BODDate + "_" + CUST.DATA.solId + "_ladgen.SUC"
		print(sv_h)
		sv_j = CUST.DATA.BODDate + "_" + CUST.DATA.solId + "_ladgen.FAI"
		print(sv_j)
		
		sv_a = sv_n + "_ladgen.DAT"
		print(sv_a)
		sv_c = "MANAGER " + CUST.DATA.solId + " {~~~NULL!~~~} {~~~NULL!~~~} {~~~NULL!~~~} "+sv_n+" "+sv_n+" D I E N N 0 0 {~~~NULL!~~~} N {~~~NULL!~~~} I {~~~NULL!~~~} L"
		print(sv_c)		 
		sv_u = system("echo '"+sv_c+"' >>'"+sv_a+"'")
		print(sv_u)
		
		
		sv_b =  urhk_getEnv("ONS_LOG_DIR")
		print(sv_b)
		sv_b = BANCS.OUTPARAM.envValue
		print(sv_b)


		
		sv_q = "exebatch labx4001 $B2K_SESSION_ID <" +sv_a
		PRINT(sv_q)

		sv_r = SYSTEM(sv_q)
        PRINT(sv_r)


        IF(sv_r == 0) THEN
        #{
            print("Successful")
			#sv_u = system("echo '"+ CUST.DATA.loanAccount +"' >>'"+sv_h+"'")
			#print(sv_u)
			
        #}
        ELSE
        #{
            print("Un-successful")
			#sv_u = system("echo '"+ CUST.DATA.loanAccount +"' >>'"+sv_j+"'")
			#print(sv_u)
        #}
        ENDIF
		
		#sv_k = "rm '"+sv_a+"' "
		#sv_i = SYSTEM(sv_k)
        #print(sv_i)


		#Fetching values
        sv_d = urhk_dbCursorFetch(CUST.DATA.DB_CURSOR_NUMBER)

        # Increment count
        CUST.DATA.count = CINT(CUST.DATA.count)+ 1
		
    #}
    DO
	
		sv_a ="*_ladgen.DAT"

		sv_k = "rm '"+sv_a+"' "
		sv_i = SYSTEM(sv_k)
        print(sv_i)

    sv_u = urhk_dbCursorClose(CUST.DATA.DB_CURSOR_NUMBER)

    ENDOFSCRIPT:
	
exitscript
TRACE OFF
END-->
