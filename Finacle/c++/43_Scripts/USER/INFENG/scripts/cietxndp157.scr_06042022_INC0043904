#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cietxndp157.scr
# Type: NA|default
# Description: Insert to MOBEE_PUSH_SMS
# Author: nagamani.boddu
# Date: 4/9/20 2:32 PM
# Modification Log:
#============================================================

<--START
	
sv_a=urhk_B2k_PrintRepos("BANCS")
PRINT(sv_a)

	
	# Script body goes here
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CIETXN Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CIETXN")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CIETXN",5)
	#}
	ENDIF
	CUST.CIETXN.scriptSuccess="Y"
	CUST.CIETXN.errorMsg=" "
	###################################################################################################
	# Insert into CUSTOM.MOBEE_PUSH_SMS TABLE
	###################################################################################################
	
	sv_s = "INSERT INTO CUSTOM.MOBEE_PUSH_SMS VALUES("
	sv_s = sv_s + "CUSTOM.MOBEE_PUSH_SMS_SEQUENCE.nextval" + ","
	sv_s = sv_s + "?SVAR" + ","
	sv_s = sv_s + "?SVAR" + ","
	sv_s = sv_s + "?SVAR" + ","
	sv_s = sv_s + "?SVAR" + ","
	sv_s = sv_s + "?SVAR" + ","
	sv_s = sv_s + "?SVAR" + ","
	sv_s = sv_s + "?SVAR" + ","
	sv_s = sv_s + "?SVAR" + ","
	sv_s = sv_s + "?SVAR" + ","
	sv_s = sv_s + "?SVAR" + ","
	sv_s = sv_s + "?SVAR" + ","
	sv_s = sv_s + "?SVAR)"
	PRINT(sv_s)
	
	IF(MID$(BANCS.STDIN.userId,0,6) == "FIVUSR")THEN
	#{
		PRINT(BANCS.INPUT.inputData)	
		BANCS.INPARAM.BINDVARS=BANCS.INPUT.inputData
		
	#}
	ENDIF

	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSqlWithBind(sv_s)
	PRINT(sv_d)
	
	
	IF(sv_d != 0)THEN
	#{
		CUST.CIETXN.scriptSuccess = "N"
		CUST.CIETXN.errorMsg="Insert Query Failed"
		
		
		IF(MID$(BANCS.STDIN.userId,0,6) != "FIVUSR")THEN
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			PRINT(sv_u)
			sv_u = urhk_SetOrbOut("Error_1|Error^Insert Query Failed^prodCode")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF

	ELSE

		IF (BANCS.STDIN.contextBankId =="43") THEN
			
		##SendSMS

			BANCS.INPUT.smsMsg = CUST.CIETXN.mobeepush_MESSAGE_TEXT
			BANCS.INPUT.mobileNum = CUST.CIETXN.mobeepush_MOBILE_NUMBER
			BANCS.INPUT.acctId = CUST.CIETXN.randomRefNo
			BANCS.INPUT.AcctCrncy = "USD"							
			CUST.CIETXN.statusSms = "F"
			CALL("Sms_DRC.scr")							
			CUST.CIETXN.statusSms = BANCS.OUTPARAM.status
			PRINT(CUST.CIETXN.statusSms)
			
		
#			sv_s = " insert into  CUSTOM.MOBEE_PUSH_SMS_DRC (REF_NO,LANGUAGE,INSTITUTION_CODE,MOBILE_NUMBER,MESSAGE_ARRIEVED_DATE,MESSAGE_PRIORITY,MESSAGE_STATUS,MESSAGE_VALIDITY_PERIOD,DEPARTMENT,MESSAGE_TEXT,RETRY_COUNT,BANK_ID,IE_TXN_REF_NO) "
#			sv_s = sv_s + " values ( CUSTOM.MOBEE_PUSH_SMS_SEQUENCE.nextval ,?SVAR ,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR, ?SVAR,?SVAR,?SVAR,?SVAR,?SVAR,?SVAR) "
#			PRINT(BANCS.INPARAM.BINDVARS)
#			sv_d = urhk_dbSqlWithBind(sv_s)
#			PRINT(sv_d)
#			IF(sv_d == 0)THEN
#				CUST.CIETXN.EMsg = "Ok"
#			ELSE
#				CUST.CIETXN.EMsg = "Not Ok"
#			ENDIF
		ENDIF

	#}
	ENDIF
	
	IF(MID$(BANCS.STDIN.userId,0,6) == "FIVUSR")THEN
	#{
		CUST.CIETXN.outputData=CUST.CIETXN.scriptSuccess+","+CUST.CIETXN.errorMsg+","
		sv_b=urhk_setOrbOut("outputData|"+CUST.CIETXN.outputData)
		PRINT(sv_b)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	
	ENDOFSCRIPT:
END-->
