#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: s_acctopnvalmn009.scr
# Type: NA|default
# Description: s_acctopnvalmn009.scr
# Author:
# Date:
# Modification Log:
#============================================================

#####################################################################################################################
# Source Name						: s_acctopnvalmn009.scr
# Menu								: HACTM
# Date								: 14-03-2013
# Description						: Doing validation on fileds.
# Calling Script					: getcparamvalue.scr
# Author     						: Shishira
# Bank								: Equity Bank,KENYA
# Modification History				:
#		< Version No.> 		<Date>		<Author Name> 		<Description>
#		 0.1				14-03-2013 		Shishira			Original Version
########################################################################################################################
<--START
CHECKSUM="675fae10ee68be3cb6c89497e6af833aee26ccaba853bd65fd61d3a786e3af95"
	#TRACE ON
	sv_u = urhk_B2k_printRepos("BANCS")
	#==========================================================*
	# CREATING NEW REPOSITORY AND CLASS
	#==========================================================*
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST", "ACCOPEN") == 0) THEN
	#{
		CREATECLASS("CUST", "ACCOPEN", 5)
	#}
	ENDIF
	
	CUST.ACCOPEN.acctNo		= ""
	CUST.ACCOPEN.acctSol	= ""
	CUST.ACCOPEN.wrkClass1 	= ""
	CUST.ACCOPEN.wrkClass2 	= ""
	CUST.ACCOPEN.wrkClass3 	= ""
	CUST.ACCOPEN.wrkClass4 	= ""
	CUST.ACCOPEN.errMsg 	= ""

	######
	IF (BANCS.STDIN.contextBankId =="43") THEN
		IF (FIELDEXISTS(BANCS.STDIN.menuId)) THEN
		IF ((BANCS.STDIN.menuId =="HACM") OR (BANCS.STDIN.menuId =="HACM")) THEN
		IF (FIELDEXISTS(BANCS.STDIN.funcName)) THEN
		IF (BANCS.STDIN.funcName=="SRV_FetchCAAcctMaintDtlForModify") THEN
			IF ((BANCS.STDIN.homeSolId == "5000") OR (BANCS.STDIN.homeSolId == "15000")) THEN
				IF (BANCS.STDIN.userId !="FIVUSR43") THEN
					###
					CUST.ACCOPEN.sysDateadt = BANCS.STDIN.sysDate
					print(CUST.ACCOPEN.sysDateadt)
					CUST.ACCOPEN.getsysdateadtstart="99"
					CUST.ACCOPEN.getsysdateadtend="88"
					CUST.ACCOPEN.getsysdateadt00flg="N"
					CUST.ACCOPEN.getsysdateadt06flg="N"
					sv_s = "getsysdateampm,getsysdateadttime|select substr(to_char(sysdate ,'DD-MON-RRRR HH:MI:SS AM'),22,2) ampm, substr(to_char(sysdate ,'DD-MON-RRRR HH:MI:SS AM'),13,2) adttime from dual "
					print(sv_s)
					BANCS.INPARAM.BINDVARS =  CUST.ACCOPEN.sysDateadt
					sv_d = urhk_dbSelectWithBind(sv_s)
					print(sv_d)
					IF (sv_d =="0") THEN
						CUST.ACCOPEN.getsysdateampm = BANCS.OUTPARAM.getsysdateampm
						print(CUST.ACCOPEN.getsysdateampm)
						CUST.ACCOPEN.getsysdateadttime= BANCS.OUTPARAM.getsysdateadttime
						print(CUST.ACCOPEN.getsysdateadttime)
						IF (CUST.ACCOPEN.getsysdateampm=="PM") THEN
							CUST.ACCOPEN.getsysdateadt00="05"
							print(CUST.ACCOPEN.getsysdateadt00)
							IF (CUST.ACCOPEN.getsysdateadttime=="12") THEN
								CUST.ACCOPEN.getsysdateadttime="00"
								print(CUST.ACCOPEN.getsysdateadttime)
							ENDIF
							IF ( cint(CUST.ACCOPEN.getsysdateadttime) > cint(CUST.ACCOPEN.getsysdateadt00) ) THEN
								print("intime")
								CUST.ACCOPEN.getsysdateadt00flg="Y"
								print(CUST.ACCOPEN.getsysdateadt00flg)
								sv_u = urhk_SRV_SetErr("The menu option is invalid or it is not available for the user at this Moment " )
								BANCS.OUTPUT.successOrFailure = "F"
								GOTO FINISH
							ENDIF
						ENDIF
					ENDIF
					###
				ENDIF
			ENDIF
		ENDIF
		ENDIF
		ENDIF
		ENDIF
	ENDIF
	######
	
	CUST.ACCOPEN.mySolId=BANCS.STDIN.mySolId
	#CUST.ACCOPEN.mySolId="000"
	sv_a = urhk_SRV_GetVal("acctCommonInfo.acctGeneralInfo.acctBasic.foracid")
	PRINT(sv_a)
	CUST.ACCOPEN.acctNo=BANCS.OUTPARAM.srvValue
	PRINT(CUST.ACCOPEN.acctNo)
	
	sv_s = "acctSol,RCRE_USER_ID |SELECT "
	sv_s = sv_s + " sol_id,RCRE_USER_ID FROM TBAADM.GAM "
	sv_s = sv_s + " WHERE foracid=?SVAR "
	sv_s = sv_s + " AND del_flg='N' "
	sv_S = sv_s + " AND entity_cre_flg='Y' "
	sv_s = sv_s + " AND bank_id=?SVAR"
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS = CUST.ACCOPEN.acctNo+"|"+BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF (sv_d==0) THEN
	#{
		CUST.ACCOPEN.acctSol= BANCS.OUTPARAM.acctSol
		PRINT(CUST.ACCOPEN.acctSol)
		CUST.ACCOPEN.USER= BANCS.OUTPARAM.RCRE_USER_ID
		PRINT(CUST.ACCOPEN.USER)
		#}
	ELSE
		#{
		GOTO FINISH
	#}
	ENDIF

		
	IF (BANCS.STDIN.userWorkClass =="083")THEN
		IF (CUST.ACCOPEN.USER !="FIVUSR43") THEN		
			PRINT("Only Channel Accounts for GPC User........." )
			sv_g = "Only Channel Accounts for GPC User ........."
			sv_u =urhk_SRV_SetErr(sv_g)
			GOTO FINISH
		ENDIF
	ENDIF

	
	IF (CUST.ACCOPEN.acctSol !=CUST.ACCOPEN.mySolId) THEN
	#{
		#---------------------------------------
		#Fetching Zone Code
		#---------------------------------------
		BANCS.INPUT.moduleName = "ACCOUNT_OPENING"
		BANCS.INPUT.parameterId = "ACCT_WORKCLS"
		CALL("getcparamvalue.scr")
		
		IF (BANCS.OUTPUT.parameterVal != "")THEN
		#{
			PRINT(BANCS.OUTPUT.parameterVal)
			CUST.ACCOPEN.pZoneCode  = BANCS.OUTPUT.parameterVal
			CUST.ACCOPEN.wrkClass1 = MID$(CUST.ACCOPEN.pZoneCode,0,3)
			PRINT(CUST.ACCOPEN.wrkClass1)
			CUST.ACCOPEN.wrkClass2 = MID$(CUST.ACCOPEN.pZoneCode,4,3)
			PRINT(CUST.ACCOPEN.wrkClass2)
			CUST.ACCOPEN.wrkClass3 = MID$(CUST.ACCOPEN.pZoneCode,12,3)
			PRINT(CUST.ACCOPEN.wrkClass3)
			CUST.ACCOPEN.wrkClass4 = MID$(CUST.ACCOPEN.pZoneCode,8,3)
			PRINT(CUST.ACCOPEN.wrkClass4)
			#}
		ELSE
			#{
			CUST.ACCOPEN.errMsg = "Work Class set up is not done in CPARAM"
			GOTO FINISH
		#}
		ENDIF
		
		IF ((BANCS.STDIN.userWorkClass !=CUST.ACCOPEN.wrkClass1) AND (BANCS.STDIN.userWorkClass !=CUST.ACCOPEN.wrkClass2)  AND (BANCS.STDIN.userWorkClass !=CUST.ACCOPEN.wrkClass3) AND (BANCS.STDIN.userWorkClass !=CUST.ACCOPEN.wrkClass4)) THEN
		#{
			PRINT(" No Intersol_activation ........." )
			sv_g = "No Intersol_activation ........."
			sv_u =urhk_SRV_SetErr(sv_g)
			GOTO FINISH
		#}
		ENDIF
	#}
	ENDIF
	
	CUST.ACCOPEN.staffEmpId = ""
	sv_s = "staffEmpId|SELECT EMP_ID FROM TBAADM.GAM WHERE FORACID = ?SVAR AND BANK_ID =?SVAR"
	PRINT(sv_s)
	
	BANCS.INPARAM.BINDVARS = CUST.ACCOPEN.acctNo+"|"+ BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	IF(sv_d == 0)THEN
	#{
		CUST.ACCOPEN.staffEmpId = BANCS.OUTPARAM.staffEmpId
		PRINT(CUST.ACCOPEN.staffEmpId)
	#}
	ENDIF
	
	IF(CUST.ACCOPEN.staffEmpId == BANCS.STDIN.EmployeeId)THEN
	#{
		
		CUST.ACCOPEN.errMsg = "Cannot modify Own Account"
		GOTO FINISH
	#}
	ENDIF
	
	FINISH:
		IF(CUST.ACCOPEN.errMsg != "") THEN
		#{
			sv_u = urhk_SRV_setErr(CUST.ACCOPEN.errMsg)
		#}
		ENDIF
		#TRACE OFF
		EXITSCRIPT
END-->
