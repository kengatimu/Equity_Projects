#=================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# category: Script Name Based
# script: ubpmn004.scr
# event: NA
# description: NA
#=================================================

<--START
CHECKSUM="62157d225f1118b572483937b79b12a3057db85b09f3cefdbdac19c3edcc30d4"
	# Script body goes here
	#----------------------------------------------------------------------------------------------------------
	# Filename              :       ubpmn004.scr
	# Author                :       Swati Ganjoo
	# Description           :       This script gets called on click of SUBMIT button for all modes(general script)
	# Called Script         :		ubpdp006.scr,ubpdp005.scr,ubpdp015.scr
	# Calling Script        :		ubpdp006.scr,ubpdp005.scr
	# Modification History
	# Sl. #           Date             Author             Modification           Version
	# -----           -----           --------          ----------------        ---------
	#  1           16-04-2013         Swati Ganjoo      Original Version          1.0
	#-----------------------------------------------------------------------------------------------------------
	TRACE ON
	
	#========================================================
	# Check whether CUST Repository exists else create it
	#========================================================
	sv_r = REPEXISTS("CUST")
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	#========================================================
	# Check whether UBP Class exists else create it
	#========================================================
	sv_r = CLASSEXISTS("CUST","UBP")
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","UBP",5)
	#}
	ENDIF
	
	
	sv_u = urhk_b2k_printrepos("BANCS")
	
	CUST.UBP.refId = BANCS.INPUT.referralid
	CUST.UBP.branchId = " "
	CUST.UBP.createdBy = " "
	CUST.UBP.numOfPartTran = " "
	CUST.UBP.verifiedBy = " "
	CUST.UBP.errorFlg = " "
	
	
	
	IF (FIELDEXISTS(BANCS.INPUT.funcCode) == 1) THEN
	#{
		#		print(BANCS.INPUT.billAcctId)
		#		print(BANCS.INPUT.billAmt)
		#		print(BANCS.INPUT.billRefNum)
		#		print(BANCS.STDIN.userId)
		#		print(BANCS.INPUT.billerId)
		#
		#		BANCS.INPARAM.BINDVARS = CUST.UBP.billerId+"|"+CUST.UBP.billAcctId+"|" +BANCS.INPUT.billAmt + "|" + BANCS.STDIN.BODDate + "|" + BANCS.INPUT.billRefNum + "|" + BANCS.STDIN.contextBankId
		#		sv_s = "billcount|"
		#		sv_s = sv_s + " SELECT count(1)"
		#		sv_s = sv_s + " FROM CUSTOM.C_UBP_DETAILS "
		#		sv_s = sv_s + " WHERE BILLER_ID=?SVAR"
		#		sv_s = sv_s + " AND BILLER_ACCT_ID=?SVAR"
		#		sv_s = sv_s + " AND TOTAL_AMT = ?SVAR"
		#		sv_s = sv_s + " AND TRAN_DATE = TO_DATE(?SVAR,'DD-MM-YYYY HH24:MI:SS') "
		#		sv_s = sv_s + " AND BILLER_REF_NUM = ?SVAR"
		#		sv_s = sv_s + " AND BILLER_BNK_ID = ?SVAR"
		#		PRINT(sv_s)
		#		sv_d = urhk_dbSelectWithBind(sv_s)
		#
		#		CUST.UBP.billcount = BANCS.OUTPARAM.billcount
		#		PRINT(CUST.UBP.billcount)
		#
		#		IF(CUST.UBP.billcount!=0)THEN
		#		#{
		#			CUST.UBP.errorFlg = "Y"
		#			CUST.UBP.errorMsg="Bill Payment already done on the same day for the Biller for same amount"
		#			#CUST.UBP.errorMsg =CUST.UBP.errorDesc
		#			CUST.UBP.errorFocus="random"
		#			sv_s = "Error_01|ERROR:^"Transaction Posted/ Refer HACLINQ for tran details / Withdraw Referral "^"+CUST.UBP.errorFocus
		#			#sv_s = "Error_01|ERROR:^"+ CUST.UBP.errorMsg +"^"+CUST.UBP.errorFocus
		#			sv_u = urhk_SetOrbOut(sv_s)
		#			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		#			GOTO ENDOFSCRIPT
		#
		#		#}
		#		ENDIF
		CUST.UBP.funcCode = BANCS.INPUT.funcCode

		IF((CUST.UBP.funcCode == "V") OR (CUST.UBP.funcCode == "X"))THEN
		#{
			IF(FIELDEXISTS(BANCS.INPUT.payRefNum) != 0)THEN
			#{
				CUST.UBP.billPayRefNum = BANCS.INPUT.payRefNum
			#}
			ELSE
			#{
				CUST.UBP.billPayRefNum = ""
			#}
			ENDIF
				
	        	sv_s = "payType,retvalue,lchg_user_id,sol_id|SELECT MODE_OF_PAY,DENOM_VALUE,LCHG_USER_ID,SOL_ID FROM CUSTOM.C_UBP_DETAILS_MOD "
			sv_s = sv_s + "WHERE BILLER_BNK_ID=?SVAR AND BILL_PAY_REF_NUM=?SVAR"
	        	#BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId +"|"+ CUST.UBP.billPayRefNum
	        	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId +"|"+ BANCS.INPUT.payRefNum

        		print(sv_s)
	        	PRINT(BANCS.INPARAM.BINDVARS)
        		sv_d = urhk_dbSelectWithBind(sv_s)

        		IF(sv_d == 0) THEN
        		#{
                		CUST.UBP.payType = BANCS.OUTPARAM.payType
				CUST.UBP.retvalue_tmp = BANCS.OUTPARAM.retvalue
				CUST.UBP.lchg_user_id = BANCS.OUTPARAM.lchg_user_id
				CUST.UBP.sol_id = BANCS.OUTPARAM.sol_id
				PRINT(CUST.UBP.payType)
				PRINT(CUST.UBP.retvalue_tmp)
				PRINT(CUST.UBP.lchg_user_id)
				PRINT(CUST.UBP.sol_id)
				
		                sv_a= ""
                		sv_b = CUST.UBP.retvalue_tmp
                		sv_c = GETPOSITION(sv_b,"~")
                		IF(sv_c!=0) THEN
                		#{
                        		sv_l =STRLEN(sv_b)
                        		WHILE(sv_c !=0)
                        		#{
                                		sv_a =sv_a+MID$(sv_b,0,sv_c - 1)+"|"
                                		sv_b=MID$(sv_b,sv_c,sv_l)
                                		sv_c = GETPOSITION(sv_b,"~")
                                		sv_l =STRLEN(sv_b)
                                		IF (sv_c==0) THEN
                                		#{
                                        		sv_a = sv_a +"|"+sv_b
                                		#}
                                		ENDIF
                        		#}
                        		DO
                        		CUST.UBP.retvalue_tmp = sv_a
                        		PRINT(CUST.UBP.retvalue_tmp)
                		#}
                		ENDIF

				sv_s = "empId|SELECT USER_EMP_ID FROM TBAADM.UPR WHERE USER_ID=?SVAR"
				BANCS.INPARAM.BINDVARS = CUST.UBP.lchg_user_id
	                        sv_d = urhk_dbSelectWithBind(sv_s)

                        	IF(sv_d == 0) THEN
                        	#{
					CUST.UBP.empId = BANCS.OUTPARAM.empId
					PRINT(CUST.UBP.empId)
				#}
				ENDIF

			#}
			ELSE
			#{
				CUST.UBP.payType = BANCS.INPUT.funcCode
				CUST.UBP.retvalue_tmp = ""
				PRINT(CUST.UBP.payType)
				PRINT(CUST.UBP.retvalue_tmp)
			#}
        		ENDIF
		#}
		ELSE
		#{
			CUST.UBP.payType = BANCS.INPUT.funcCode
		#}
		ENDIF

		#IF(TRIM(BANCS.INPUT.funcCode)=="C")THEN
		IF(TRIM(CUST.UBP.payType)=="C")THEN
		#{
			CALL("ubpdp006.scr")
			GOTO ENDOFSCRIPT3
		#}
		ENDIF
		#IF(TRIM(BANCS.INPUT.funcCode)=="T")THEN
		IF(TRIM(CUST.UBP.payType)=="T")THEN
		#{
			CALL("ubpdp005.scr")
			GOTO ENDOFSCRIPT3
			
		#}
		ENDIF
		#IF(TRIM(BANCS.INPUT.funcCode)=="L")THEN
		IF(TRIM(CUST.UBP.payType)=="L")THEN
		#{
			sv_u = urhk_SetOrbOut("RESULT_MSG|<font color=black>" + "The  clearing transaction posted successfully")
			sv_b=urhk_SetOrbOut("SuccessOrFailure|Y")
			
		#}
		ENDIF
		#IF(TRIM(BANCS.INPUT.funcCode)=="R")THEN
		IF(TRIM(CUST.UBP.payType)=="R")THEN
		#{
			
			CALL("ubpdp015.scr")
			GOTO ENDOFSCRIPT3
			
		#}
		ENDIF
	#}
	ENDIF
	ENDOFSCRIPT3:
		
		#IF(( trim(CUST.UBP.refId) != "") AND (CUST.UBP.errorFlg != "Y"))THEN
		#{
		#	print(CUST.UBP.refId)
		#	sv_u = urhk_SetUrhkInp("cmnGrp.referral_id|"+CUST.UBP.refId)
		#	print(sv_u)
		#	sv_u = urhk_ExecSrvNoCommit("SRV_MoveRefDtlsToHistTables|retain_all_output = Y")
		#	print(sv_u)
		#}
		#ENDIF
		
		
		EXITSCRIPT
END-->
