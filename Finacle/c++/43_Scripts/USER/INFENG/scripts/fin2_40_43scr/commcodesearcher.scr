#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: commcodesearcher.scr
# Type: NA|default
# Description: commcodesearcher.scr
# Author: nagamani.boddu
# Date: 6/25/18 4:51 PM
# Modification Log:
#============================================================

<--START
	# Script body goes here
	TRACE ON
	
	#=====================================================
	# Check whether CUST Repository exists else create it
	#=====================================================
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	#====================================================
	# Check whether COMMCODE Class exists else create it
	#====================================================
	
	sv_r = CLASSEXISTS("CUST","COMMCODE")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","COMMCODE",5)
	#}
	ENDIF
	
	#====================================================
	#Initializing variables
	#====================================================
	CUST.COMMCODE.commissionCode = ""
	CUST.COMMCODE.description = ""
	CUST.COMMCODE.crncyCode = ""
	CUST.COMMCODE.vatRate = ""
	CUST.COMMCODE.minComm = ""
	CUST.COMMCODE.commPcnt = ""
	CUST.COMMCODE.bankID = ""
	CUST.COMMCODE.recordCount = ""
	
	sv_s = "recordCount|"
	sv_s = sv_s + " SELECT COUNT(*)"
	sv_s = sv_s + " FROM CUSTOM.C_COMM "
	sv_s = sv_s + " WHERE BANK_ID = ?SVAR"
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_p = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_p)
	
	IF(sv_p == 0) THEN
	#{
		CUST.COMMCODE.recordCount = BANCS.OUTPARAM.recordCount
		PRINT(CUST.COMMCODE.recordCount)
	#}
	ENDIF
	
	sv_u = urhk_SetOrbOut("recordCount|"+CUST.COMMCODE.recordCount)
	PRINT(sv_u)
	
	sv_s = "commissionCode,description,crncyCode,vatRate,minComm,commPcnt|"
	sv_s = sv_s + "SELECT COMM_CODE,COMM_DESC,CRNCY_CODE,VAT_RATE,MIN_COMM,COMM_PCNT "
	sv_s = sv_s + " FROM CUSTOM.C_COMM"
	sv_s = sv_s + " WHERE BANK_ID = ?SVAR"
	PRINT(sv_s)
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	sv_q = urhk_dbCursorOpenWithBind(sv_s)
	PRINT(sv_q)
	IF(sv_q != 0) THEN
	#{
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	sv_n = BANCS.OUTPARAM.DB_CURSOR_NUMBER
	PRINT(sv_n)
	sv_f = urhk_dbCursorFetch(sv_n)
	PRINT(sv_f)
	sv_i = 1
	WHILE(sv_f == 0)
	#{
		CUST.COMMCODE.commissionCode = BANCS.OUTPARAM.commissionCode
		CUST.COMMCODE.description = BANCS.OUTPARAM.description
		CUST.COMMCODE.crncyCode = BANCS.OUTPARAM.crncyCode
		CUST.COMMCODE.vatRate = BANCS.OUTPARAM.vatRate
		CUST.COMMCODE.minComm = BANCS.OUTPARAM.minComm
		CUST.COMMCODE.commPcnt = BANCS.OUTPARAM.commPcnt
		
		sv_s = "commcode_" + FORMAT$(sv_i, "%d") + "|" + CUST.COMMCODE.commissionCode
		PRINT(sv_s)
		sv_l = urhk_SetOrbOut(sv_s)
		PRINT(sv_l)
		
		sv_s = "description_" + FORMAT$(sv_i, "%d") + "|" + CUST.COMMCODE.description
		PRINT(sv_s)
		sv_l = urhk_SetOrbOut(sv_s)
		PRINT(sv_l)
		
		sv_s = "crncyCode_" + FORMAT$(sv_i, "%d") + "|" + CUST.COMMCODE.crncyCode
		PRINT(sv_s)
		sv_l = urhk_SetOrbOut(sv_s)
		PRINT(sv_l)
		
		sv_s = "vatRate_" + FORMAT$(sv_i, "%d") + "|" + CUST.COMMCODE.vatRate
		PRINT(sv_s)
		sv_l = urhk_SetOrbOut(sv_s)
		PRINT(sv_l)
		
		sv_s = "minComm_" + FORMAT$(sv_i, "%d") + "|" + CUST.COMMCODE.minComm
		PRINT(sv_s)
		sv_l = urhk_SetOrbOut(sv_s)
		PRINT(sv_l)
		
		sv_s = "minComm_" + FORMAT$(sv_i, "%d") + "|" + CUST.COMMCODE.minComm
		PRINT(sv_s)
		sv_l = urhk_SetOrbOut(sv_s)
		PRINT(sv_l)
		
		sv_s = "commPcnt_" + FORMAT$(sv_i, "%d") + "|" + CUST.COMMCODE.commPcnt
		PRINT(sv_s)
		sv_l = urhk_SetOrbOut(sv_s)
		PRINT(sv_l)
		
		sv_i = sv_i + 1
		sv_f = urhk_dbCursorFetch(sv_n)
		PRINT(sv_f)
		
	#}
	DO
	
	sv_q = urhk_dbCursorClose(sv_n)
	PRINT(sv_q)
	
	ENDOFSCRIPT:
		BANCS.OUTPUT.successOrFailure = "S"
END-->
