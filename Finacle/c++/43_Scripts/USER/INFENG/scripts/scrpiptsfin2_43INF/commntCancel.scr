#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: commntCancel.scr
# Type: NA|default
# Description: Commision Maintenance Script
# Author: Anshul_Singh04
# Date: 6/1/18 11:07 AM
# Modification Log:
#============================================================

<--START
CHECKSUM="e04480c85f61b2973bb1aa7feccac6ed1736a3311bd55e6c782f3e67bae8b00d"
	# Script body goes here
	TRACE ON
	
	sv_a = REPEXISTS("EQT")
	IF (sv_a == 0) THEN
	#{
		CREATEREP("EQT")
	#}
	ENDIF
	sv_a = CLASSEXISTS("EQT", "COMM")
	IF (sv_a == 0 ) THEN
	#{
		CREATECLASS("EQT", "COMM", 5)
	#}
	ENDIF
	
	PRINT(BANCS.INPUT.commCode)
	PRINT(BANCS.INPUT.funcCode)
	
	EQT.COMM.msg=" "
	
	sv_j= "DELETE FROM CUSTOM.C_CPM_MOD WHERE bank_id=?SVAR AND COMM_CODE =?SVAR  AND LCHG_USER_ID=?SVAR"
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + BANCS.INPUT.commCode + "|" + BANCS.STDIN.userId
	
	PRINT(BANCS.INPARAM.BINDVARS )
	sv_r = urhk_dbsqlWithBind(sv_j)
	PRINT(sv_r)
	
	IF(sv_r!=0)THEN
	#{
		EQT.COMM.msg = "Error in Database Operation!!"
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		sv_u = urhk_SetOrbOut("Error_1|ERR^"+EQT.COMM.msg)
	#}
	ENDIF
	IF(sv_r==0) THEN
	#{
		EQT.COMM.msg = "Record Cancelled Successfully."
	#}
	ENDIF
	
	EXITSCRIPT
	
END-->
