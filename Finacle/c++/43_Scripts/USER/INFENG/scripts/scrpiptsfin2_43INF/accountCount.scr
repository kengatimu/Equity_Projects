#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: accountCount.scr
# Type: NA|default
# Description: This script is used to get the count for the new loan account from CUSTOM table
# Author:
# Date:
# Modification Log:
#============================================================

<--START
CHECKSUM="62bf9e0a6a175db852172d8e45a308904d2550f3dddde5093e4358ebaf08f6c2"
	TRACE ON
	#------------------------------------------------------------------------------
	#	Creating repository and class
	#------------------------------------------------------------------------------
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST","LADISB") == 0) THEN
	#{
		CREATECLASS("CUST","LADISB",5)
	#}
	ENDIF
	
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	sv_u = urhk_b2k_printrepos("BANCS")
	
	CUST.LADISB.errorFlg = "P"
	CUST.LADISB.errorMsg = "No Error"
	CUST.LADISB.count    	= 0
	CUST.LADISB.foracid     	= TOUPPER(BANCS.INPUT.acctNum)
	PRINT(CUST.LADISB.foracid)
	CUST.LADISB.funcCode 			= BANCS.INPUT.funcCode
	PRINT(CUST.LADISB.funcCode)
	
	IF(CUST.LADISB.funcCode == "D") THEN
	#{
		CUST.LADISB.slno 		 =""
		CUST.LADISB.acctno1		 =""
		CUST.LADISB.acctno1Crncy =""
		CUST.LADISB.crdr1        =""
		CUST.LADISB.amount1      =""
		CUST.LADISB.acctno2      =""
		CUST.LADISB.acctno2Crncy =""
		CUST.LADISB.crdr2        =""
		CUST.LADISB.amount2      =""
		
		CUST.LADISB.totAmt		= 0
		
		#--------------------------------------------------------------------------
		# Getting the count of records
		#--------------------------------------------------------------------------
		sv_p = "recCount|"
		sv_p = sv_p +" SELECT"
		sv_p = sv_p +" COUNT(*)"
		sv_p = sv_p +" FROM"
		sv_p = sv_p +" TBAADM.GAM "
		sv_p = sv_p + " WHERE GAM.schm_type = 'LAA'"
		sv_p = sv_p + " AND GAM.cif_id = ("
		sv_p = sv_p +			   " SELECT GAM.cif_id"
		sv_p = sv_p +	                   	" FROM TBAADM.GAM"
		sv_p = sv_p +			   			" WHERE GAM.foracid = ?SVAR"
		sv_p = sv_p + 						" AND GAM.acct_cls_flg = 'N'"
		sv_p = sv_p + 						" AND GAM.del_flg = 'N'"
		sv_p = sv_p + 						" AND GAM.entity_cre_flg = 'Y'"
		sv_p = sv_p +			   			" AND GAM.bank_id = ?SVAR)"
		sv_p = sv_p + " AND GAM.acct_cls_flg = 'N'"
		sv_p = sv_p + " AND GAM.del_flg = 'N'"
		sv_p = sv_p + " AND GAM.entity_cre_flg = 'Y'"
		sv_p = sv_p + " AND GAM.clr_bal_amt != 0"
		sv_p = sv_p + " AND GAM.bank_id = ?SVAR"
		sv_p = sv_p + " AND GAM.foracid != ?SVAR"
		
		BANCS.INPARAM.BINDVARS = ""
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.LADISB.foracid  + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.contextBankId + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + BANCS.STDIN.contextBankId + "|"
		BANCS.INPARAM.BINDVARS = BANCS.INPARAM.BINDVARS + CUST.LADISB.foracid
		PRINT(BANCS.INPARAM.BINDVARS)
	#}
	ENDIF
	
	IF((CUST.LADISB.funcCode == "X") OR (CUST.LADISB.funcCode == "V")) THEN
	#{
		sv_p = "recCount|select count(1) from CUSTOM.C_LADISB_PAYOFF_OLDLOAN where LOAN_FORACID = ?SVAR AND bank_id = ?SVAR AND payoff_flg='N' AND entity_cre_flg='N'"
		BANCS.INPARAM.BINDVARS = BANCS.INPUT.acctNum + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
	#}
	ENDIF
	sv_q = urhk_dbselectWithBind(sv_p)
	
	
	IF(sv_q == 0)THEN
	#{
		PRINT(BANCS.OUTPARAM.recCount)
		CUST.LADISB.recCount = BANCS.OUTPARAM.recCount
		PRINT(CUST.LADISB.recCount)
		#}
	ELSE
		#{
		CUST.LADISB.recCount = 0
		CUST.LADISB.errorFlg = "E"
		CUST.LADISB.errorMsg ="Error in DB operation!"
	#}
	ENDIF
	
	sv_q = urhk_SetOrbOut("recCount|"+CUST.LADISB.recCount)
	sv_u = urhk_SetOrbOut("errorFlg|"+CUST.LADISB.errorFlg)
	sv_u = urhk_SetOrbOut("errorMsg|"+CUST.LADISB.errorMsg)
	
	EXITSCRIPT
	TRACE OFF
END-->
