#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cusTableUpdate.scr
# Type: NA|default
# Description: This script is used for updating custom table
# Author:
# Date:
# Modification Log:
#============================================================

<--START
CHECKSUM="cd1c8a275d01b2636e4e1d01da8e4e318f598c12af5e54e1db41f02df1037b18"
	# Script body goes here
	TRACE ON
	sv_a = REPEXISTS("CUST")
	IF (sv_a == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	sv_a = CLASSEXISTS("CUST", "LADISB")
	IF (sv_a == 0 ) THEN
	#{
		CREATECLASS("CUST", "LADISB", 5)
	#}
	ENDIF
	
	CUST.LADISB.errFlg = " "
	CUST.LADISB.errMSg = " "
	
	CUST.LADISB.LoanAcctNum = BANCS.INPUT.LoanAcctNum
	CUST.LADISB.oldLoanacct = BANCS.INPUT.oldLoanacct
	CUST.LADISB.oldacctCrncy = BANCS.INPUT.oldacctCrncy
	CUST.LADISB.oldTotAmt = BANCS.INPUT.oldTotAmt
	CUST.LADISB.selFlg = BANCS.INPUT.selFlg
	CUST.LADISB.PopUpPageVstd = BANCS.INPUT.PopUpPageVstd
	CUST.LADISB.totalCount = BANCS.INPUT.totalCount
	CUST.LADISB.creditAcctNum = BANCS.INPUT.creditAcctNum
	
	sv_s = "recCnt|select count(1) from CUSTOM.C_LADISB_PAYOFF_OLDLOAN where LOAN_FORACID = ?SVAR AND bank_id = ?SVAR AND payoff_flg='N' AND entity_cre_flg = 'N'"
	BANCS.INPARAM.BINDVARS = CUST.LADISB.LoanAcctNum + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_d = urhk_dbSelectWithBind(sv_s)
	PRINT(sv_d)
	
	IF(sv_d == 0)THEN
	#{
		PRINT(BANCS.OUTPARAM.recCnt)
		CUST.LADISB.recCnt = BANCS.OUTPARAM.recCnt
		PRINT(CUST.LADISB.recCnt)
		#}
	ELSE
		#{
		CUST.LADISB.recCnt = 0
		CUST.LADISB.errFlg = "E"
		CUST.LADISB.errMSg ="Error in DB operation!"
		EXITSCRIPT
	#}
	ENDIF
	
	IF(CUST.LADISB.recCnt > 0) THEN
	#{
		sv_p = ""
		sv_p = "UPDATE CUSTOM.C_LADISB_PAYOFF_OLDLOAN set funccode = ?SVAR where LOAN_FORACID = ?SVAR AND bank_id = ?SVAR AND payoff_flg='N' AND entity_cre_flg='N'"
		BANCS.INPARAM.BINDVARS = BANCS.INPUT.funcCode + "|" + BANCS.INPUT.LoanAcctNum + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		
		sv_q = urhk_dbSQLWithBind(sv_p)
		PRINT(sv_q)
		
		IF(sv_q == 0)THEN
		#{
			sv_d = urhk_dbSqlWithBind("COMMIT")
			PRINT(sv_d)
			#}
		ELSE
			#{
			CUST.LADISB.errFlg = "E"
			CUST.LADISB.errMSg = "Error in updating custom table "
		#}
		ENDIF
	#}
	ENDIF
	
	EXITSCRIPT
	sv_u = urhk_SetOrbOut("errorFlg|"+CUST.LADISB.errFlg)
	sv_u = urhk_SetOrbOut("errorMsg|"+CUST.LADISB.errMSg)
	
	TRACE OFF
END-->
