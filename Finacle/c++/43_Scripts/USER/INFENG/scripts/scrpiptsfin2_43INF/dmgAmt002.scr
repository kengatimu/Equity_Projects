#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: dmgAmt002.scr
# Type: NA|default
# Description: Script to Convert Amount
# Author: Anshul_Singh04
# Date: 7/20/18 11:13 AM
# Modification Log:
#============================================================

<--START
CHECKSUM="9dab713520694666e0c1c8ce7143dedc9b8bf46e6c94fe5ca143e75bcdd3a6f0"
	# Script body goes here
	TRACE ON
	
	sv_a = REPEXISTS("AMT")
	IF (sv_a == 0) THEN
	#{
		CREATEREP("AMT")
	#}
	ENDIF
	sv_a = CLASSEXISTS("AMT", "CONV")
	IF (sv_a == 0 ) THEN
	#{
		CREATECLASS("AMT", "CONV", 5)
	#}
	ENDIF
	AMT.CONV.errorFlg ="N"
	AMT.CONV.Msg = " "
	
	print(sv_x)
	print(sv_c)
	print(sv_b)
	print(sv_n)
	print(sv_m)
	
	sv_u = urhk_SetUrhkInp("fromCrncy.crncyCode|"+ sv_x)
	sv_u = urhk_SetUrhkInp("toCrncy.crncyCode|"+sv_c)
	sv_u = urhk_SetUrhkInp("rateCode.refCode|"+sv_b)
	sv_u = urhk_SetUrhkInp("fromAmt|"+sv_n+"|"+sv_x)
	
	IF(sv_m != "")THEN
	#{
		sv_u = urhk_SetUrhkInp("rate|"+sv_m)
	#}
	ENDIF
	
	sv_u = urhk_SetUrhkOut("rate|rate")
	sv_u = urhk_SetUrhkOut("toAmt|toAmt")
	
	sv_u = urhk_ExecSrvNoCommit("SRV_TieredConvCrncy")
	print(sv_u)
	IF(sv_u == 0)THEN
	#{
		AMT.CONV.rate = BANCS.OUTPARAM.rate
		PRINT(AMT.CONV.rate)
		
		AMT.CONV.toAmt = BANCS.OUTPARAM.toAmt
		PRINT(AMT.CONV.toAmt)
		
		sv_p = GETPOSITION(AMT.CONV.toAmt,"|")
		print(sv_p)
		
		IF(sv_p > 0)THEN
		#{
			AMT.CONV.tranAmt = MID$(AMT.CONV.toAmt,0,sv_p-1)
			PRINT(AMT.CONV.tranAmt)
			AMT.CONV.tranAmtCcy = MID$(AMT.CONV.toAmt,sv_p,3)
			PRINT(AMT.CONV.tranAmtCcy)
		#}
		ENDIF
		#}
	ELSE
		#{
		print(BANCS.OUTPARAM.Fatal_num)
		print(BANCS.OUTPARAM.Error_num)
		print(BANCS.OUTPARAM.Warning_num)
		print(BANCS.OUTPARAM.UserInfo_num)
		print(BANCS.OUTPARAM.Xcpn_num)
		print(BANCS.OUTPARAM.Xcpn_Err_num)
		print(BANCS.OUTPARAM.Xcpn_Xcpn_num)
		print(BANCS.OUTPARAM.Xcpn_Err_num)
		
		sv_d = 1
		sv_n = 1
		sv_c = CINT(BANCS.OUTPARAM.Error_num)
		WHILE (sv_d <= sv_c)
		#{
			sv_f = FORMAT$(sv_d,"%d")
			sv_b = ("BANCS").("OUTPARAM").("ErrorMesg_"+sv_f)
			print(sv_b)
			sv_e = ("BANCS").("OUTPARAM").("ErrorCode_"+sv_f)
			print(sv_e)
			sv_a = ("BANCS").("OUTPARAM").("Tag_"+sv_f)
			print(sv_a)
			sv_d = sv_d + 1
			sv_n = sv_n + 1
			sv_d = sv_c + 1
		#}
		DO
		AMT.CONV.rate = " "
		AMT.CONV.tranAmt = " "
		AMT.CONV.tranAmtCcy = " "
		
		AMT.CONV.errorFlg ="Y"
		AMT.CONV.Msg = sv_b
	#}
	ENDIF
END-->
