#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: cpenalmn001.scr
# Type: NA|default
# Description: GETDATA SCRIPT OF CPENAL MENU
# Author: Sridevi
# Date: 7/16/19 16:27 PM
# Modification Log:
#============================================================

<--START

	TRACE ON
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CPNL Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CPNL")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CPNL",5)
	#}
	ENDIF
	
	#==========================================================*
	# Intilializing the Variables
	#==========================================================*
		CUST.CPNL.LAST_WAIVED_OFF_DATE = " "
		CUST.CPNL.TOTAL_WAIVED_OFF_AMT = " "
		CUST.CPNL.ACCT_NAME = " "
		CUST.CPNL.SOL_ID = " "
		CUST.CPNL.ACCT_CCY_CODE = " "
		CUST.CPNL.PENAL_ACCR_UP_TO_DATE_DR = " "
		CUST.CPNL.PENAL_ACCRUED_AMOUNT_DR = " "
		CUST.CPNL.PENAL_APPL_UP_TO_DATE_DR = " "
		CUST.CPNL.PENAL_APPLIED_AMOUNT_DR = " "

	IF(FIELDEXISTS(BANCS.INPUT.funcCode))THEN
	#{
		IF(TRIM(BANCS.INPUT.funcCode) != "")THEN
		#{
			CUST.CPNL.funcCode = TOUPPER(TRIM(BANCS.INPUT.funcCode))
			PRINT(CUST.CPNL.funcCode)
		#}
		ENDIF
	#}
	ENDIF
	IF(FIELDEXISTS(BANCS.INPUT.acctId))THEN
	#{
		IF(TRIM(BANCS.INPUT.acctId) != "")THEN
		#{
			CUST.CPNL.acctId = TOUPPER(TRIM(BANCS.INPUT.acctId))
			PRINT(CUST.CPNL.acctId)
		#}
		ENDIF
	#}
	ENDIF
	CUST.CPNL.errorFlg ="N"
	CALL("cpenaldp001.scr")
	IF(CUST.CPNL.errorFlg == "Y")THEN
	#{
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		sv_u = urhk_SetOrbOut("Error_1|Error^Enter a valid ODA account Id^acctId")
		PRINT(sv_u)
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	
	
	#==========================================================*
	#Input validation
	#==========================================================*
	IF((CUST.CPNL.funcCode == "I") OR (CUST.CPNL.funcCode == "W")) THEN
	#{
		
		sv_s = "recCount|SELECT COUNT(1) FROM CUSTOM.C_PNAT WHERE FORACID = ?SVAR  AND BANK_ID = ?SVAR"
		BANCS.INPARAM.BINDVARS = CUST.CPNL.acctId + "|" + BANCS.STDIN.contextBankId
		PRINT(BANCS.INPARAM.BINDVARS)
		sv_b = urhk_dbSelectWithBind(sv_s)
		PRINT(sv_b)
		
		IF(sv_b == 0)THEN
		#{
			CUST.CPNL.recCount = BANCS.OUTPARAM.recCount
			PRINT(CUST.CPNL.recCount)
			IF(CUST.CPNL.recCount != "1")THEN
			#{
			
			
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				sv_u = urhk_SetOrbOut("Error_1|Error^Record doesnot exist for the given Account Id "+CUST.CPNL.acctId+"^acctId")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		#}
		ELSE
		#{
			sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
			sv_u = urhk_SetOrbOut("Error_1|Error^Failed to fetch the data^acctId")
			PRINT(sv_u)
			GOTO ENDOFSCRIPT
		#}
		ENDIF
		
		#######################################################
		# Data fetch
		#######################################################
			sv_s = "ACCT_NAME|SELECT ACCT_NAME FROM TBAADM.GAM WHERE FORACID = ?SVAR AND BANK_ID = ?SVAR AND DEL_FLG= 'N' AND ENTITY_CRE_FLG = 'Y'"
			
			BANCS.INPARAM.BINDVARS = CUST.CPNL.acctId + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_b = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_b)
			IF(sv_b == 0)THEN
			#{
				CUST.CPNL.ACCT_NAME = BANCS.OUTPARAM.ACCT_NAME
				PRINT(CUST.CPNL.ACCT_NAME)
			#}
			ELSE
			#{
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				sv_u = urhk_SetOrbOut("Error_1|Error^Failed to fetch the record^acctId")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
			
			sv_s = "SOL_ID,ACCT_CCY_CODE,PENAL_ACCR_UP_TO_DATE_DR,PENAL_ACCRUED_AMOUNT_DR,PENAL_APPL_UP_TO_DATE_DR,PENAL_APPLIED_AMOUNT_DR,"
			sv_s = sv_s + "TOTAL_WAIVED_OFF_AMT,LAST_WAIVED_OFF_DATE|SELECT SOL_ID,ACCT_CCY_CODE,PENAL_ACCR_UP_TO_DATE_DR,PENAL_ACCRUED_AMOUNT_DR,PENAL_APPL_UP_TO_DATE_DR,PENAL_APPLIED_AMOUNT_DR,"
			sv_s = sv_s + "TOTAL_WAIVED_OFF_AMT,LAST_WAIVED_OFF_DATE FROM CUSTOM.C_PNAT WHERE FORACID = ?SVAR AND BANK_ID =?SVAR"
			
			
			BANCS.INPARAM.BINDVARS = CUST.CPNL.acctId + "|" + BANCS.STDIN.contextBankId
			PRINT(BANCS.INPARAM.BINDVARS)
			sv_b = urhk_dbSelectWithBind(sv_s)
			PRINT(sv_b)
			IF(sv_b == 0)THEN
			#{
				CUST.CPNL.SOL_ID = BANCS.OUTPARAM.SOL_ID
				PRINT(CUST.CPNL.SOL_ID)
				CUST.CPNL.ACCT_CCY_CODE = BANCS.OUTPARAM.ACCT_CCY_CODE
				PRINT(CUST.CPNL.ACCT_CCY_CODE)
				CUST.CPNL.PENAL_ACCR_UP_TO_DATE_DR = BANCS.OUTPARAM.PENAL_ACCR_UP_TO_DATE_DR
				PRINT(CUST.CPNL.PENAL_ACCR_UP_TO_DATE_DR)
				CUST.CPNL.PENAL_ACCRUED_AMOUNT_DR = BANCS.OUTPARAM.PENAL_ACCRUED_AMOUNT_DR
				PRINT(CUST.CPNL.PENAL_ACCRUED_AMOUNT_DR)
				IF(BANCS.OUTPARAM.PENAL_APPL_UP_TO_DATE_DR != "")THEN
				#{
						
					print(BANCS.OUTPARAM.PENAL_APPL_UP_TO_DATE_DR)
					CUST.CPNL.PENAL_APPL_UP_TO_DATE_DR = BANCS.OUTPARAM.PENAL_APPL_UP_TO_DATE_DR
				#}
				ENDIF
				IF(BANCS.OUTPARAM.PENAL_APPLIED_AMOUNT_DR != "")THEN
				#{
						
					print(BANCS.OUTPARAM.PENAL_APPLIED_AMOUNT_DR)
					CUST.CPNL.PENAL_APPLIED_AMOUNT_DR = BANCS.OUTPARAM.PENAL_APPLIED_AMOUNT_DR
				#}
				ENDIF
				IF(BANCS.OUTPARAM.TOTAL_WAIVED_OFF_AMT != "")THEN
				#{
						
					print(BANCS.OUTPARAM.TOTAL_WAIVED_OFF_AMT)
					CUST.CPNL.TOTAL_WAIVED_OFF_AMT = BANCS.OUTPARAM.TOTAL_WAIVED_OFF_AMT
				#}
				ENDIF
				
				IF(BANCS.OUTPARAM.LAST_WAIVED_OFF_DATE != "")THEN
				#{
						
					print(BANCS.OUTPARAM.LAST_WAIVED_OFF_DATE)
					CUST.CPNL.LAST_WAIVED_OFF_DATE = BANCS.OUTPARAM.LAST_WAIVED_OFF_DATE
				#}
				ENDIF

				sv_u = urhk_SetOrbOut("acctName|"+ CUST.CPNL.ACCT_NAME)
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("solId|"+ CUST.CPNL.SOL_ID)
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("acctCrncyCode|"+ CUST.CPNL.ACCT_CCY_CODE)
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("pAccrUpToDateDr|"+ CUST.CPNL.PENAL_ACCR_UP_TO_DATE_DR)
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("pAccrAmtDr|"+ CUST.CPNL.PENAL_ACCRUED_AMOUNT_DR)
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("pApplUpToDateDr|"+ CUST.CPNL.PENAL_APPL_UP_TO_DATE_DR)
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("pApplAmtDr|"+ CUST.CPNL.PENAL_APPLIED_AMOUNT_DR)
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("totWaivedOffAmt|"+ CUST.CPNL.TOTAL_WAIVED_OFF_AMT)
				PRINT(sv_u)
				sv_u = urhk_SetOrbOut("lastWaivedOffDate|"+ CUST.CPNL.LAST_WAIVED_OFF_DATE)
				PRINT(sv_u)
				
				sv_z = urhk_SetOrbOut("SuccessOrFailure|Y")
				PRINT(sv_z)						
				GOTO ENDOFSCRIPT
				
			#}
			ELSE
			#{
				sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
				sv_u = urhk_SetOrbOut("Error_1|Error^Failed to fetch the record^acctId")
				PRINT(sv_u)
				GOTO ENDOFSCRIPT
			#}
			ENDIF
		
	
	#}
	ENDIF
	
	
	SUCCESS:
	sv_z = urhk_SetOrbOut("SuccessOrFailure|Y")
	PRINT(sv_z)
	ENDOFSCRIPT:
		
END-->