#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: getChargeTran.scr
# Type: NA|default
# Description: This script is used to get the charge tran Id and to display the same in the result page of LADISB
# Author:
# Date:
# Modification Log:
#============================================================

<--START
CHECKSUM="5f1412c29a2b287d6e9eb294165be57a7f71f78b055bc638b77139ddea18c8d6"
	TRACE ON
	LIBNAME CUSTOMSO
	#------------------------------------------------------------------------------
	# Creating repository and class
	#------------------------------------------------------------------------------
	IF (REPEXISTS("CUST") == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	
	IF (CLASSEXISTS("CUST","LADISB") == 0) THEN
	#{
		CREATECLASS("CUST","LADISB",5)
	#}
	ENDIF
	sv_u = urhk_B2k_PrintRepos("BANCS")
	
	#----------------------------------------------------------------
	# Reading disbursement charge tran id
	#----------------------------------------------------------------
	sv_t = urtn_fileOpen("LadisbChrg.txt|r")
	IF (sv_t < 0) THEN
	#{
		PRINT("file cannot be open")
		GOTO ENDOFSCRIPT
	#}
	ENDIF
	CUST.LADISB.dataFileIndex = sv_t
	PRINT(CUST.LADISB.dataFileIndex)
	sv_e = urtn_fileRead(CUST.LADISB.dataFileIndex+ "|CUST.LADISB.disbChrgTranId")
	PRINT(sv_e)
	PRINT(CUST.LADISB.disbChrgTranId)
	IF(CUST.LADISB.disbChrgTranId != "") THEN
	#{
		sv_u = urhk_SetOrbOut("tranId1|" + CUST.LADISB.disbChrgTranId)
	#}
	ENDIF
	sv_u = SYSTEM("rm -f LadisbChrg.txt")
	PRINT(sv_u)
	sv_u = urtn_fileClose(CUST.LADISB.dataFileIndex)
	
	ENDOFSCRIPT:
		EXITSCRIPT
END-->
