<--START
	
	# Script body goes here
	TRACE ON
	
	libname CUSTOMSO
	
	###########################################################################################
	# Check whether CUST Repository exists else create it
	###########################################################################################
	
	sv_r = REPEXISTS("CUST")
	
	IF(sv_r == 0) THEN
	#{
		CREATEREP ("CUST")
	#}
	ENDIF
	
	###########################################################################################
	# Check whether CCRRM Class exists else create it
	###########################################################################################
	
	sv_r = CLASSEXISTS("CUST","CCRRM")
	
	IF(sv_r == 0) THEN
	#{
		CREATECLASS ("CUST","CCRRM",5)
	#}
	ENDIF
	
	sv_u = urhk_B2k_PrintRepos("BANCS")
        PRINT(sv_u)

	###########################################################################################
	# Initialize the variables
	###########################################################################################
				
				print(CUST.CCRRM.cust_id)
				
				CUST.CCRRM.failurerpt=""
			CUST.CCRRM.successrpt = "AccountManagerSuccess_"+CUST.CCRRM.refcode+".SUC"
				 
				
			
				#==========================
				# Set values to front end
				#==========================
				
				
					sv_s = "schmType,foracid|SELECT SCHM_TYPE,foracid FROM TBAADM.GAM WHERE bank_id='43' and  schm_type in ('SBA','CAA','LAA') and acct_cls_flg='N' and cif_id = '"+CUST.CCRRM.cust_id+"' "
					print(sv_s)
					
					sv_g = urhk_dbCursorOpenWithBind(sv_s)
					PRINT(sv_g)
					
					IF(sv_d != 0)THEN
					#{
						BANCS.OUTPUT.successOrFailure = "F"
						sv_e = "file is being validated check after some time"
						sv_u = urhk_SetOrbOut(sv_e)
					#}
					ELSE
					#{
		
					CUST.CCRRM.db_cursor_no_hacm = BANCS.OUTPARAM.DB_CURSOR_NUMBER
					PRINT(CUST.CCRRM.db_cursor_no_hacm)
					sv_d = urhk_dbCursorFetch(CUST.CCRRM.db_cursor_no_hacm)
					CUST.CCRRM.count = "1"
					PRINT(sv_d)
					
					WHILE(sv_d == 0)
					#{
                    
					CUST.CCRRM.schmType = BANCS.OUTPARAM.schmType
					PRINT(CUST.CCRRM.schmType)
								CUST.CCRRM.foracid = BANCS.OUTPARAM.foracid
					print(CUST.CCRRM.foracid)


#					IF(CUST.CCRRM.schmType == "LAA") THEN
#					#{
#					    sv_a = urhk_SetUrhkInp("acctCommonInfo.acctGeneralInfo.acctBasic.foracid|" + CUST.CCRRM.foracid)
#
#					    sv_s = urhk_ExecSrv("SRV_FetchLAAcctDtlForVerify|retain_all_output = Y")
#					    PRINT(sv_s)
#					    IF (sv_s != 0) THEN
#					    #{
#						sv_u = system("echo '"+CUST.CCRRM.foracid+"' >>changeAcctMgrReport.FAL")
#						GOTO ERRORCHECK
#					    #}
#					    ENDIF
#					    
#					    sv_a = urhk_CopyOutToIn("")
#					    PRINT(sv_a)
#					    
#					    sv_a = urhk_SetUrhkInp("acctCommonInfo.acctGeneralInfo.acctCustInfo.psNextPrintDate|" + BANCS.STDIN.BODDate)
#					    PRINT(sv_a)
#					    sv_a = urhk_SetUrhkInp("acctCommonInfo.acctGeneralInfo.acctBasic.foracid|" + CUST.CCRRM.foracid)
#					    PRINT(sv_a)
#					    
#					    sv_s = urhk_ExecSrv("SRV_VerifyLAAcct|retain_all_output = Y||same_user_verify = Y|call_mode = N")
#					    PRINT(sv_s)
#					    IF (sv_s != 0) THEN
#					    #{
#						sv_u = system("echo '"+CUST.CCRRM.foracid+"' >>changeAcctMgrReport.FAL")
#						PRINT(sv_u)
#						GOTO ERRORCHECK
#					    #}
#					    ELSE
#					    #{
#						sv_u = system("echo '"+CUST.CCRRM.foracid+"' >>" +CUST.CCRRM.successrpt)
#						PRINT(sv_u)
#						sv_a = "exebatch babx4040 $B2K_SESSION_ID "  +CUST.CCRRM.successrpt+ " "+CUST.CCRRM.successrpt+ " 1 N"
#						PRINT(sv_a)
#						sv_i = SYSTEM(sv_a)
#										##GOTO ENDOFSCRIPT
#					    #}
#					    ENDIF
#					#}
#					ENDIF

								
					IF(CUST.CCRRM.schmType == "SBA") THEN
					#{
					    sv_a = urhk_SetUrhkInp("acctCommonInfo.acctGeneralInfo.acctBasic.foracid|" + CUST.CCRRM.foracid)

					    sv_s = urhk_ExecSrv("SRV_FetchSBAcctMaintDtlForVerify|retain_all_output = Y|call_mode = N")
					    
					    PRINT(sv_s)
					    IF (sv_s != 0) THEN
					    #{
						sv_u = system("echo '"+CUST.CCRRM.foracid+"' >>changeAcctMgrReport.FAL")
						GOTO ERRORCHECK
					    #}
					    ENDIF
					    
					    sv_a = urhk_CopyOutToIn("")
					    PRINT(sv_a)
					    
					    sv_a = urhk_SetUrhkInp("acctCommonInfo.acctGeneralInfo.acctCustInfo.psNextPrintDate|" + BANCS.STDIN.BODDate)
					    PRINT(sv_a)
					    sv_a = urhk_SetUrhkInp("acctCommonInfo.acctGeneralInfo.acctBasic.foracid|" + CUST.CCRRM.foracid)
					    PRINT(sv_a)
					    
					    sv_s = urhk_ExecSrv("SRV_VerifySBAcct|retain_all_output = Y||same_user_verify = Y|call_mode = N")
					    PRINT(sv_s)
					    IF (sv_s != 0) THEN
					    #{
						sv_u = system("echo '"+CUST.CCRRM.foracid+"' >>changeAcctMgrReport.FAL")
						PRINT(sv_u)
						GOTO ERRORCHECK
					    #}
					    ELSE
					    #{
						sv_u = system("echo '"+CUST.CCRRM.foracid+"' >>" +CUST.CCRRM.successrpt)
						PRINT(sv_u)
										sv_a = "exebatch babx4040 $B2K_SESSION_ID "  +CUST.CCRRM.successrpt+ " "+CUST.CCRRM.successrpt+ " 1 N"
					PRINT(sv_a)
					sv_i = SYSTEM(sv_a)
										##GOTO ENDOFSCRIPT
					    #}
					    ENDIF
					#}
					ENDIF
					
					IF(CUST.CCRRM.schmType == "CAA") THEN
					#{
					    sv_a = urhk_SetUrhkInp("acctCommonInfo.acctGeneralInfo.acctBasic.foracid|" + CUST.CCRRM.foracid)

					    sv_s = urhk_ExecSrv("SRV_FetchCAAcctMaintDtlForVerify|retain_all_output = Y|call_mode = N")
					    
					    PRINT(sv_s)
					    IF (sv_s != 0) THEN
					    #{
						sv_u = system("echo '"+CUST.CCRRM.foracid+"' >>changeAcctMgrReport.FAL")
						GOTO ERRORCHECK
					    #}
					    ENDIF
					    
					    sv_a = urhk_CopyOutToIn("")
					    PRINT(sv_a)
					    
					    sv_a = urhk_SetUrhkInp("acctCommonInfo.acctGeneralInfo.acctCustInfo.psNextPrintDate|" + BANCS.STDIN.BODDate)
					    PRINT(sv_a)
					    sv_a = urhk_SetUrhkInp("acctCommonInfo.acctGeneralInfo.acctBasic.foracid|" + CUST.CCRRM.foracid)
					    PRINT(sv_a)
					     
					    
					    sv_s = urhk_ExecSrv("SRV_VerifyCAAcct|retain_all_output = Y||same_user_verify = Y|call_mode = N")
					    PRINT(sv_s)
					    IF (sv_s != 0) THEN
					    #{
						sv_u = system("echo '"+CUST.CCRRM.foracid+"' >>changeAcctMgrReport.FAL")
						PRINT(sv_u)
						GOTO ERRORCHECK
					    #}
					    ELSE
					    #{
						sv_u = system("echo '"+CUST.CCRRM.foracid+"' >>" +CUST.CCRRM.successrpt)
						PRINT(sv_u)
										sv_a = "exebatch babx4040 $B2K_SESSION_ID "  +CUST.CCRRM.successrpt+ " "+CUST.CCRRM.successrpt+ " 1 N"
						PRINT(sv_a)
						sv_i = SYSTEM(sv_a)
										###GOTO ENDOFSCRIPT
					    #}
					    ENDIF
					#}
					ENDIF

					ERRORCHECK:

					#----------------------------------------------------------------------------------------------------
					# SRV Error check
					#----------------------------------------------------------------------------------------------------
					
					IF (sv_s != 0) THEN
					#{
						IF(FIELDEXISTS(BANCS.OUTPARAM.Error_num) == 1) THEN
						#{
							print(BANCS.OUTPARAM.Error_num)
							sv_v = BANCS.OUTPARAM.Error_num
						#}									
						ELSE
						#{
							sv_v = 0
						#}
						ENDIF
						IF(sv_v > 0)THEN
						#{
							sv_u = system("echo 'Error'")
							sv_i = 1
							WHILE (sv_i <= sv_v)
							#{
								sv_f = FORMAT$(sv_i,"%d")
								sv_e = ("BANCS").("OUTPARAM").("ErrorMesg_"+sv_f)
								sv_e = sv_e + " " + ("BANCS").("OUTPARAM").("Tag_"+sv_f)
								sv_e = sv_e + " Error Code:" + ("BANCS").("OUTPARAM").("ErrorCode_"+sv_f)
								print(sv_e)

								sv_i = sv_i + 1
								sv_u = system("echo '"+sv_e+"' >>BatchChAcctError.log")
								print(sv_u)
							#}
							DO
						#}
						ENDIF
					#}
					ENDIF

					sv_d = urhk_dbCursorFetch(CUST.CCRRM.db_cursor_no_hacm)
					print(sv_d)
				  
					 # Increment count
					CUST.CCRRM.count = CINT(CUST.CCRRM.count)+ 1
					print(CUST.CCRRM.count)
				#}
				do

				sv_u = urhk_dbCursorClose(CUST.CCRRM.db_cursor_no_hacm)
				print(sv_u)
	ENDOFSCRIPT:

END-->
