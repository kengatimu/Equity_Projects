#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: comsnCancel.scr
# Type: NA|default
# Description: comsnCancel.scr
# Author: shivakrishna.d
# Date: 6/18/18 7:19 PM
# Modification Log:
#============================================================

<--START
CHECKSUM="8c5cba413f45ef46806095ad272f85d8e56a46f7eb26c1cd2a274d0fedc917d7"
	# Script body goes here
	TRACE ON
	
	sv_a = REPEXISTS("EQT")
	IF (sv_a == 0) THEN
	#{
		CREATEREP("EQT")
	#}
	ENDIF
	sv_a = CLASSEXISTS("EQT", "COMM")
	IF (sv_a == 0 ) THEN
	#{
		CREATECLASS("EQT", "COMM", 5)
	#}
	ENDIF
	
	PRINT(BANCS.INPUT.commCode)
	PRINT(BANCS.INPUT.funcCode)
	
	EQT.COMM.msg=" "
	
	sv_j= "DELETE FROM CUSTOM.C_COMM_MOD WHERE bank_id=?SVAR AND COMM_CODE =?SVAR  AND LCHG_USER_ID=?SVAR"
	BANCS.INPARAM.BINDVARS = BANCS.STDIN.contextBankId + "|" + BANCS.INPUT.commCode + "|" + BANCS.STDIN.userId
	
	PRINT(BANCS.INPARAM.BINDVARS )
	sv_r = urhk_dbsqlWithBind(sv_j)
	PRINT(sv_r)
	
	IF(sv_r!=0)THEN
	#{
		EQT.COMM.msg = "Error in Database Operation!!"
		sv_u = urhk_SetOrbOut("SuccessOrFailure|N")
		sv_u = urhk_SetOrbOut("Error_1|ERR^"+EQT.COMM.msg)
	#}
	ENDIF
	IF(sv_r==0) THEN
	#{
		EQT.COMM.msg = "Record Cancelled Successfully."
	#}
	ENDIF
	
	EXITSCRIPT
END-->
