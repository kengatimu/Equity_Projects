<--START
TRACE ON
    #sv_a=urhk_SetUrhkInp("acctFreezeCriteria.acctId.foracid|"+CUST.ODACT.foracid)
    sv_a=urhk_SetUrhkInp("acctFreezeCriteria.acctId.foracid|"+CUST.ODACT.DrActNum)
    PRINT(sv_a)
    sv_a=urhk_SetUrhkInp("acctFreezeCriteria.freezeCode.code|T")
    PRINT(sv_a)
    sv_a=urhk_SetUrhkInp("acctFreezeCriteria.freezeReason.refCode|ODCAP")
    PRINT(sv_a)
    sv_a=urhk_SetUrhkInp("acctFreezeCriteria.freezeRemarks|ODACNTFREEZE")
    sv_u = urhk_ExecSrv("SRV_FetchAcctsForFreeze|retain_all_output = Y")
    print(sv_u)

    IF (sv_u != 0) THEN
    #{

		IF (FIELDEXISTS(BANCS.OUTPARAM.Error_num) == 1) THEN
		#{
			PRINT(BANCS.OUTPARAM.Error_num)
			sv_v = BANCS.OUTPARAM.Error_num
		#}
		ELSE
		#{
			sv_v = 0
		#}
		ENDIF
		IF (sv_v > 0) THEN
		#{
			sv_i = 1
			WHILE (sv_i <= sv_v)
			#{
				sv_f = FORMAT$(sv_i,"%d")
				sv_e = ("BANCS").("OUTPARAM").("ErrorMesg_"+sv_f)
				PRINT(sv_e)
				sv_e = ("BANCS").("OUTPARAM").("ErrorCode_"+sv_f)
				PRINT(sv_e)
				sv_e = ("BANCS").("OUTPARAM").("Tag_"+sv_f)
				PRINT(sv_e)
				sv_i = sv_i + 1
			#}
			DO
		#}
		ENDIF
    #}
    ELSE
    #{
        sv_u = urhk_CopyOutToIn("")
        PRINT(sv_u)

        sv_a=urhk_SetUrhkInp("acctFreezeLL.<rec_0>.key.serial_num|0")
        #sv_a=urhk_SetUrhkInp("acctFreezeLL.<rec_0>.acctId.foracid|"+CUST.ODACT.foracid)
        sv_a=urhk_SetUrhkInp("acctFreezeLL.<rec_0>.acctId.foracid|"+CUST.ODACT.DrActNum)
        #sv_a=urhk_SetUrhkInp("acctFreezeLL.<rec_0>.acctId.acid")
        sv_a=urhk_SetUrhkInp("acctFreezeLL.<rec_0>.acctId.crncyCode|"+CUST.ODACT.crncyCode)
        sv_a=urhk_SetUrhkInp("acctFreezeLL.<rec_0>.acctId.solId|"+CUST.ODACT.schmCode)
        sv_a=urhk_SetUrhkInp("acctFreezeLL.<rec_0>.freezeCode.code|T")
        sv_a=urhk_SetUrhkInp("acctFreezeLL.<rec_0>.freezeReason.refCode|ODCAP")
        sv_a=urhk_SetUrhkInp("acctFreezeLL.<rec_0>.selFlg|t")

        sv_u = urhk_ExecSrv("SRV_FreezeAcctsAutoVerify|same_user_verify = Y|retain_all_output = Y")
        IF (sv_u != 0) THEN
        #{

            IF (FIELDEXISTS(BANCS.OUTPARAM.Error_num) == 1) THEN
            #{
                PRINT(BANCS.OUTPARAM.Error_num)
                sv_v = BANCS.OUTPARAM.Error_num
            #}
            ELSE
            #{
                sv_v = 0
            #}
            ENDIF
            IF (sv_v > 0) THEN
            #{
                sv_i = 1
                WHILE (sv_i <= sv_v)
                #{
                    sv_f = FORMAT$(sv_i,"%d")
                    sv_e = ("BANCS").("OUTPARAM").("ErrorMesg_"+sv_f)
                    PRINT(sv_e)
                    sv_e = ("BANCS").("OUTPARAM").("ErrorCode_"+sv_f)
                    PRINT(sv_e)
                    sv_e = ("BANCS").("OUTPARAM").("Tag_"+sv_f)
                    PRINT(sv_e)
                    sv_i = sv_i + 1
                #}
                DO
            #}
            ENDIF
        #}
        ENDIF
    #}
    ENDIF

TRACE OFF
END-->

