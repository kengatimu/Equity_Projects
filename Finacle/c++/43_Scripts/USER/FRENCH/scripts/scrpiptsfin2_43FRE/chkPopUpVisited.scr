#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: chkPopUpVisited.scr
# Type: NA|default
# Description: This script is used to check if the user has visited the pop up page or not, based on which error message will be thrown.
# Author:
# Date:
# Modification Log:
#============================================================

<--START
CHECKSUM="eb1a7d993e6c9590a3e24cca65045d7d83abef25902e74e175c907f0106b6da1"
	TRACE ON
	
	sv_a = REPEXISTS("CUST")
	IF (sv_a == 0) THEN
	#{
		CREATEREP("CUST")
	#}
	ENDIF
	sv_a = CLASSEXISTS("CUST", "LADISB")
	IF (sv_a == 0 ) THEN
	#{
		CREATECLASS("CUST", "LADISB", 5)
	#}
	ENDIF
	
	sv_a = urhk_B2K_printRepos("CUST")
	PRINT(sv_a)
	
	sv_a = urhk_B2K_printRepos("BANCS")
	PRINT(sv_a)
	CUST.LADISB.foracid             = TOUPPER(BANCS.INPUT.acctNum)
	PRINT(CUST.LADISB.foracid)
	CUST.LADISB.funcCode                    = BANCS.INPUT.funcCode
	PRINT(CUST.LADISB.funcCode)
	CUST.LADISB.errorFlg = " "
	CUST.LADISB.errorMsg = " "
	
	
	sv_p = ""
	sv_p = "recCount|select count(1) from CUSTOM.C_LADISB_PAYOFF_OLDLOAN where LOAN_FORACID = ?SVAR AND bank_id = ?SVAR AND payoff_flg='N' AND entity_cre_flg='N' AND "
	
	IF(CUST.LADISB.funcCode == "D") THEN
	#{
		sv_p = sv_p + "funccode='D'"
	#}
	ENDIF
	
	IF(CUST.LADISB.funcCode == "V") THEN
	#{
		sv_p = sv_p + "funccode='V'"
	#}
	ENDIF
	
	IF(CUST.LADISB.funcCode == "X") THEN
	#{
		sv_p = sv_p + "funccode='X'"
	#}
	ENDIF
	BANCS.INPARAM.BINDVARS = BANCS.INPUT.acctNum + "|" + BANCS.STDIN.contextBankId
	PRINT(BANCS.INPARAM.BINDVARS)
	
	sv_q = urhk_dbselectWithBind(sv_p)
	
	IF(sv_q == 0)THEN
	#{
		PRINT(BANCS.OUTPARAM.recCount)
		CUST.LADISB.recCount = BANCS.OUTPARAM.recCount
		PRINT(CUST.LADISB.recCount)
		#}
	ELSE
		#{
		CUST.LADISB.recCount = 0
		CUST.LADISB.errorFlg = "E"
		CUST.LADISB.errorMsg ="Error in DB operation!"
	#}
	ENDIF
	
	sv_q = urhk_SetOrbOut("recCount|"+CUST.LADISB.recCount)
	sv_u = urhk_SetOrbOut("errorFlg|"+CUST.LADISB.errorFlg)
	sv_u = urhk_SetOrbOut("errorMsg|"+CUST.LADISB.errorMsg)
	
	TRACE OFF
	
END-->
