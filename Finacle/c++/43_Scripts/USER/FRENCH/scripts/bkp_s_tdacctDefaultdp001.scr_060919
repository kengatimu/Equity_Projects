#============================================================
# IMPORTANT: Auto Generated Header. DO NOT CHANGE MANUALLY.
# Product: Finacle Core
# Category: User Defined
# Script: s_tdacctDefaultdp001.scr
# Type: NA|default
# Description: s_tdacctDefaultdp001.scr
# Author:
# Date:
# Modification Log:
#============================================================

#---------------------------------------------------------------------------------
#	Name			: s_tdaacctDefaultdp001.scr
#	Description		: Script will be used for defaulting values based on the conditions.
#	Date			: 02-02-2013
#	Author			: GOKULKRISHNA
#	Input			: NA
#	Output			: NA
#	Called Script	: SRV_FetchDfltTDAcctDtls_post_process_data.scr
#	Calling Scrip	: NA
#	Menu Option		: HOAACTD
#	Srl. No			Date			Author			Description.
#	-------			---------		-------			---------------
#	1.0			02-02-2013		GOKULKRISHNA			Original Version
#	2.0			06-08-2019		Irungu James			Restrict Migrated Scheme Codes
#----------------------------------------------------------------------------------
<--START
CHECKSUM="7b27b5c890a10524893a13e9887d51978eea8c87f88b89806f11a18c74539d4c"
	TRACE ON
	
	#Defaulting of Deposit Receipt (R) for TDA
	sv_u = urhk_SRV_SetVal("dfltTdAcct.acctCommonInfo.acctGeneralInfo.acctCustInfo.pbPsCode.code|R")

       	sv_a=urhk_B2k_PrintRepos("BANCS")

        IF (REPEXISTS("CUST") == 0) THEN
        #{
                CREATEREP("CUST")
        #}
        ENDIF
        IF (CLASSEXISTS("CUST","TDOPN") == 0) THEN
        #{
                CREATECLASS("CUST","TDOPN",5)
        #}
        ENDIF

#------------------------------------------------------------------------------
# Initaializing variables
#------------------------------------------------------------------------------
CUST.TDOPN.errMsg = ""


        sv_u =  urhk_SRV_GetVal("acctCrit.schmCode.schmCode")
        IF(sv_u == 0)THEN
        #{
                CUST.TDOPN.schmCode = BANCS.OUTPARAM.srvValue
                PRINT(CUST.TDOPN.schmCode)
                #}
        ELSE
                #{
                CUST.TDOPN.errMsg = "Error in getting schmCode from frontend"
                GOTO ENDOFSCRIPT
        #}
        ENDIF

IF (BANCS.STDIN.contextBankId == "43") THEN

        CUST.TDOPN.Migr= MID$(CUST.TDOPN.schmCode,0,2)
        print(CUST.TDOPN.Migr)

                sv_a = STRLEN(CUST.TDOPN.schmCode)
                print(sv_a)
                CUST.TDOPN.Schm= MID$(CUST.TDOPN.schmCode,2,sv_a)
                PRINT(CUST.TDOPN.Schm)

        IF (CUST.TDOPN.Migr == "MG") THEN
                CUST.TDOPN.errMsg = "Kindly Use scheme code TD" + CUST.TDOPN.Schm + " ."
                GOTO ENDOFSCRIPT
        Else
		GOTO ENDOFSCRIPT
        ENDIF
ENDIF

ENDOFSCRIPT:
IF(CUST.TDOPN.errMsg != "") THEN
#{
        sv_u = urhk_SRV_setErr(CUST.TDOPN.errMsg)
#}
ENDIF

	TRACE OFF
	EXITSCRIPT
END-->

