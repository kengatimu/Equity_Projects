Integration Test

1.0 Heartbeat
     Test World Remit Heartbeat. done
     Simulate Failure and Success. done

2.0 Transaction Processing
    i. Call TransactionStart webhook if heartbeat is okay. done
    ii. Fetch transaction(s). done
    iii. Check if transaction exists in the db. done

     2.0.1 If transaction does not exist
        i. Save the transaction. done
        ii. Simulate a save failure. done
        iii. If save fails, continue to the next transaction in the list. done
        iv. Otherwise, lock the transaction. done

     2.0.2 If transaction exists - simulate an existing transaction. done
        i. Check if it is updating the statuses. done
        ii. Set/failed with 0040. done 
        iii. Check if the table statuses are updated. done
        iv. Also simulate a null setFailedStatusCodeMap response and check if db is updated. done
        v. Check if it goes to the next record. done

     2.1.0 Simulate a lock failure. done
        i. Check if statues are updated properly. done
        ii. Check if it goes to the next record. done
    
     2.1.1 Simulate a lock success. done
        i. Check if all statuses are updated properly. done

2.1 Technical Service Operation - Request
    i. Simulate a failed technical service heartbeat. done
    
    2.1.0 Set/Acknowledged Failure
        i. Check all the db statuses -  PENDING_TIMEOUT. done
        ii. Check if it goes to the next record. done

     2.1.1 Set/Acknowledged Success
        i. Check all the db statuses -  PENDING_TIMEOUT. done
        ii. Check if it goes to the next record. done
    
     ii. Simulate a Success technical service heartbeat. done
     iii. If heartbeat is a Success. done

     2.1.2 Send to Kafka
        i. Simulate kafka failure
        ii. Check if statuses are updated properly

    2.1.3 Kafka - Success
       i. Check if statuses are updated properly
       ii. Check if it goes to the next record 
 
2.2 Technical Service Operation - Response
      i. Simulate a success kafka response
      ii. Check if lock status is Success

     2.2.1 Lock status - Failure
         i. Simulate a set/lock
         ii. If set/lock fails
         iii. Simulate a set/acknowledged
         iv. Check if statuses are updated properly
         v. If set/lock success
         vi. Simulate a set/paid, set/failed, or set/acknowledged
         vii. Check if statuses are updated properly




Drop table:

drop table [dbo].[transaction_details];
drop table [dbo].[transaction_status_details];
drop table [dbo].[set_paid_status_details];
drop table [dbo].[set_lock_status_details];
drop table [dbo].[set_failed_status_details];
drop table [dbo].[set_acknowledge_status];
drop table [dbo].[sender_details];
drop table [dbo].[payload_details];
drop table [dbo].[receiver_details];



//        Map<String, Object> queryParamMap = new HashMap<>();
//        String uri;
//
//        queryParamMap.put("wrid", worldRemitId);
//        queryParamMap.put("reference", referenceUnifiedId);
//
//        UriComponents builder = UriComponentsBuilder
//            .fromHttpUrl(lockUrlString)
//            .buildAndExpand(queryParamMap);


message = referenceUnified+ ": Set Transaction As Locked Was Successful".toUpperCase();
            composeInfoLoggerMessage(message);

            message = "Doing Blah Blah Blah: ".toUpperCase() + referenceUnified;
            composeInfoLoggerMessage(message);
     
   
   













