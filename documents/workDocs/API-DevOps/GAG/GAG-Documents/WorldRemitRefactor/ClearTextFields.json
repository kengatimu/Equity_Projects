Refactor

1. Business Service

	1.0.0 Send To Kafka
	
		i. Prepare TechnicalServiceDTO with the necessary fields
		ii. Sign the necessary fields
			a. if it is productCode is BankTransfer, clear text fields should be:
				 salt 
				 + accountNumber 
				 + bic 
				 + referenceUnified 
				 + paymentType (productCode)
				 + BigDecimalValueOfSettlementAmount.setScale(2, RoundingMode.CEILING) 
				 + settlementCurrencyCode
				 + channelId
				 
			b. else if productCode is MobileWallet, clear text fields should be:
				 salt 
				 + walletName.toUpperCase 
				 + recepientMobileNumber 
				 + referenceUnified 
				 + paymentType (productCode)
				 + BigDecimalValueOfSettlementAmount.setScale(2, RoundingMode.CEILING) 
				 + settlementCurrencyCode
				 + channelId
				 
		ii. Send the TechnicalServiceDTO json string to kafka
		
	1.0.1 Status Check
		i. Prepare StatusCheckDTO with the necessary fields
		ii. Sign the necessary fields
			a. Clear text fields should be:
				 salt
				 + referenceUnified 
				 + channelId

2. Technical Service
	1.0.0 Receive On Kafka
		
		i. Verify signature
		ii. Using @valid annotation, do request validations
		iii. Validate the mandatory data 
		iv. persist record
		v. Create TransactionProcessorDTO with necessary fields
		vi. Sign the necessay fields
			a. if it is productCode is BankTransfer, clear text fields should be:
				 salt 
				 + accountNumber 
				 + bic 
				 + referenceUnified 
				 + paymentType (productCode)
				 + BigDecimalValueOfSettlementAmount.setScale(2, RoundingMode.CEILING) 
				 + settlementCurrencyCode
				 + channelId
				 
			b. else if productCode is MobileWallet, clear text fields should be:
				 salt 
				 + walletName.toUpperCase 
				 + recepientMobileNumber 
				 + referenceUnified 
				 + paymentType (productCode)
				 + BigDecimalValueOfSettlementAmount.setScale(2, RoundingMode.CEILING) 
				 + settlementCurrencyCode
				 + channelId
		vii. Send the request to Transaction Processor MS via RestTemplate

	1.0.1 Status Check
		i. Check transaction status from status table

	1.0.2 CroneJob
		i. Create a job that runs every 3 minutes for checking transaction status

	1.0.3 Create Mobile Validation Service

	1.0.4 Create Account Validation Service

	1.0.5 Call outbound for pesalink, otherwise call gag
		



				 