logging:
  level:
    ROOT: INFO
    org.hibernate: INFO
    com.equitybank.gag.telco_b2c: INFO
    com.netflix.discovery.DiscoveryClient: INFO

server:
  port: 8501
  ssl:
    enabled: false
    keystore-location: '/Users/bishop/Programming/projects/work/WorkSpace/NetBeans/GAG/GAGApplications/Broker-Migration/certs/EQUITY-MTN-SS.p12'
    keystore-password: 'Ja6XzA129fT4'
    keystore-alias: 'tomcat'
    keystore-type: 'PKCS12'

spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/gag?useSSL=false&allowPublicKeyRetrieval=true
    username: bishop
    password: bishop
    hikari:
      poolName: Hikari
      auto-commit: false
      maximum-pool-size: 30
      minimum-idle: 10
      idle-timeout: 30000  # Close idle connections after 30s
      connection-timeout: 20000  # Wait max 20s for a connection
      max-lifetime: 1800000  # Recycle connections every 30 mins
      # leak-detection-threshold: 15000  # Detect leaked connections after 15s
      data-source-properties:
        cachePrepStmts: true
        prepStmtCacheSize: 500
        prepStmtCacheSqlLimit: 4096
        useServerPrepStmts: true
  jpa:
    hibernate:
      dialect: org.hibernate.dialect.MySQL8Dialect  # Update dialect for MySQL 8
      ddl-auto: update
    show-sql: false

  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: false

mtn:
  ss:
    credentials:
      username: 'login'
      password: 'password'
    float:
      buy:
        url: 'https://localhost:9443/api/v1/mtn/floatpurchase'
      status:
        url: 'https://localhost:9443/api/v1/mtn/floatpurchase/status'
    namecheck:
      url: 'https://localhost:9443/api/v1/mtn/floatpurchase/namecheck'