# ============================================================
# DR Platform API — Base Configuration
# Contains ONLY app identity and profile routing.
# All other config lives in application-{sit|uat|prod}.yml
# Active profile: set via SPRING_PROFILES_ACTIVE env var
# Default fallback: sit
# ============================================================

spring:
  application:
    name: dr-platform-api
  profiles:
    active: sit
