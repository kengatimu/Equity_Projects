#name: Prisma Code Scan
#
#on:
#  pull_request:
#    branches:
#      - main
#
#jobs:
#  code-scan:
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v3
#
#      - name: Download twistcli
#        run: |
#          curl -k -u "$PRISMA_USER:$PRISMA_PASSWORD" "$PRISMA_CONSOLE_URL/api/v1/util/twistcli" -o twistcli
#          chmod +x twistcli
#
#      - name: Scan Docker image and fail on critical CVEs
#        run: |
#          ./twistcli images scan \
#            --address "$PRISMA_CONSOLE_URL" \
#            --user "$PRISMA_USER" \
#            --password "$PRISMA_PASSWORD" \
#            --details \
#            --output-file scan-results.json \
#            my-app-image
#
#          CRITICAL_COUNT=$(jq '[.vulnerabilities[] | select(.severity == "critical")] | length' scan-results.json)
#
#          echo "Critical vulnerabilities found: $CRITICAL_COUNT"
#
#          if [ "$CRITICAL_COUNT" -gt 0 ]; then
#            echo "❌ Critical vulnerabilities detected. Failing the build."
#            exit 1
#          else
#            echo "✅ No critical vulnerabilities found."
#          fi
#        env:
#          PRISMA_USER: ${{ secrets.PRISMA_USER }}
#          PRISMA_PASSWORD: ${{ secrets.PRISMA_PASSWORD }}
#          PRISMA_CONSOLE_URL: ${{ secrets.PRISMA_CONSOLE_URL }}
