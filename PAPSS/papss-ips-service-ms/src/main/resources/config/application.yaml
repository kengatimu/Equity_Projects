spring:
  application:
    name: papss-ips-service-ms
  profiles:
    active: sit

#eureka:
#  instance:
#    hostname: papss-ips-service-ms
#    prefer-ip-address: false  # Use hostname instead of IP
#  client:
#    service-url:
#      defaultZone: http://localhost:8765/eureka/

#actuator configs
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    shutdown:
      enabled: true #for gracefully shutting down the app - http://localhost:<PortNumber>/actuator/shutdown
    health:
      show-details: when_authorized
  info:
    env:
      enabled: true